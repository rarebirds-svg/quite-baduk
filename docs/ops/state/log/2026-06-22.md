# 2026-06-22 운영 로그

- 02:00 dev-cycle 자율 버그 사이클 실행
  - 이슈 선택: 열린 이슈 0건 (`gh issue list --state open` → `[]`, repo rarebirds-svg/quite-baduk). `bug`·`small` 적격 이슈 없음.
  - **처리할 버그 없음** — 종료.
  - Telegram 보고: 이번 세션에 `reply`/telegram 채널 도구 미노출(ToolSearch에서 telegram·reply 미검출, 연결 MCP는 Canva·Context7·Google Drive뿐). 발송 불가 — 사유는 도구 미노출. 다음 launchd 실행(`--channels` 부여)에서 정상 발송 기대. 보고 내용: "[inkbaduk 운영] dev-cycle — 처리할 버그 없음(열린 이슈 0건)."

- 12:00 오케스트레이터 실행 (12:30 슬롯)
  - **healthcheck** 🟢 — prod backend 200 OK(db:true, katago_alive:false 정보용·lazy spawn), web 200 OK, launchd api 923/web 912 가동, 디스크 6%, plist drift 없음(exit 0). staging 중단(정책상 정상).
  - **backup-verify** 🟢 — 최신 baduk-20260622T040004.db.gz, integrity_check=ok, 7 tables. daily/weekly/monthly = 14/5/1.
  - **bug-scan** 🟢 — 신규 버그 없음. err 로그의 `TypeError: bytes is not JSON serializable`(errors.py val_handler) 2건은 **stale 오탐**: 트레이스백 라인번호가 ce9d745(6/11 fix `_safe_validation_errors`) *이전* 레이아웃과 일치하고, 두 발생(log line 221457·221549) 모두 현 prod 프로세스(PID 923, 6/13 22:49 기동, line 221583) *이전*. 수정 가동 후 재발 0건 — [[bug-scan-verify-recency]] 정책대로 누적이 아닌 최근성 확인 후 이슈 미생성.
  - **backlog-triage** 🟢 — 미분류 이슈 0건.
  - **pr-watch** 🟢 — 열린 PR 0건.
  - Telegram 보고: 발송 불가 — **도구 미노출**. ToolSearch("telegram reply send"·"reply respond inbound") 두 차례 호출했으나 telegram 채널 reply 도구가 세션에 노출되지 않음(연결 MCP는 Canva·Context7·Google Drive뿐; reply류는 Canva reply-to-comment·PushNotification만). 02:00 dev-cycle 세션과 동일 상태. 보고 예정 내용: "[inkbaduk 운영] 12:30 정상 — prod OK(web·api 200), 백업 신선(6/22 04:00 integrity OK), 버그 신규 0(bytes-TypeError는 stale 오탐), 미분류·PR 0, 승인대기 0. 최근 CWI ingest 6/21 03:05 new=200, 최근 분석 2026-W25."

- 18:00 오케스트레이터 실행 (18:30 슬롯)
  - **healthcheck** 🟢 — prod backend 200 OK(db:true, katago_alive:false 정보용·lazy spawn), web 200 OK, launchd api 923/web 912 가동, 디스크 6%, plist drift 없음(exit 0). staging 중단(정책상 정상).
  - **backup-verify** 🟢 — 최신 baduk-20260622T040004.db.gz(6/22 04:00, mtime 24h 이내), integrity_check=ok, 7 tables. daily/weekly/monthly = 14/5/1.
  - **bug-scan** 🟢 — 신규 버그 없음. err 로그 `bytes is not JSON serializable` 2건(line 221457·221549) 재확인 — 트레이스백이 `errors.py:21 in val_handler`을 가리키나 *현재* 코드 line 21은 `_safe_validation_errors`(fix ce9d745, 6/11 커밋 HEAD 반영, `custom_encoder={bytes:...}`)이고 val_handler은 line 33 → 라인번호 불일치 = pre-fix 구버전 잔재. 두 발생 모두 현 prod 프로세스(PID 923) 기동 *이전*, err 로그 mtime 6/21 17:47 이후 무기록 = 재발 0. [[bug-scan-verify-recency]]대로 최근성 확인 후 이슈 미생성.
  - **backlog-triage** 🟢 — 미분류 이슈 0건(`gh issue list --search "no:label"` → 빈).
  - **pr-watch** 🟢 — 열린 PR 0건.
  - 비고: `gh`가 launchd PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 실행(auth: rarebirds-svg OK).
  - Telegram 보고: 발송 불가 — **도구 미노출**. ToolSearch("telegram reply send"·"inbound respond notify") 두 차례 호출, telegram reply 도구가 세션에 미노출(연결 MCP는 Canva·Context7·Google Drive뿐). 02:00·12:30 세션과 동일. 보고 예정 내용: "[inkbaduk 운영] 18:30 정상 — prod OK(web·api 200), 백업 신선(6/22 04:00 integrity OK), 버그 신규 0, 미분류·PR 0, 승인대기 0. 최근 CWI ingest 6/21 03:05 new=200, 최근 분석 2026-W25."
