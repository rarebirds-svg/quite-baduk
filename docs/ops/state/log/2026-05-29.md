# 2026-05-29

## 02:00 dev-cycle (자율 버그 사이클)

- 적격 이슈 없음 (`gh issue list --state open --label bug,small` 결과 0건).
- 처리할 버그 없음 — 종료.

## 12:00 오케스트레이터

- **헬스체크** — prod backend 200 OK (db:true, katago_alive:false=정보용·정상), prod web 200 OK, `com.baduk.api`(PID 1466)·`com.baduk.web`(PID 1455) 가동, 디스크 4%, plist drift 없음. staging 중단(정책상 정상). 전부 정상.
- **백업 검증** — 최신 `baduk-20260529T040005.db.gz`(약 8h, 신선), daily/weekly/monthly = 10/1/0, 복원 드릴 integrity_check=ok·테이블 7개. 정상.
- **버그 스캔** — `baduk-api.err`에서 WS send-after-close `RuntimeError`(ws.py:241) 18회 누적 탐지(여러 서버 세대 반복, 현재 세대 0회). 기존 열린 bug 이슈 없어 신규 #39 생성(자율). 클라이언트가 place_move 중 끊으면 후속 send_json이 닫힌 소켓에 쓰기 → 사용자 영향 없으나 로그 노이즈. dev-cycle 처리 권장.
- **백로그 트리아지** — 미분류 이슈 0건. 할 일 없음.
- **PR 감시** — 열린 PR 0건. 정상.
- **기타** — 보류 승인 0건. 최근 CWI ingest 2026-05-27 21:41(fetched=0 new=0). 최근 주간 리포트 2026-W21.
- **Telegram 보고** — 아래 요약 발송 시도. 이 세션에 Telegram reply/send 도구가 노출되지 않음(MCP 미로드) → 발송 불가. 사유: 도구 미노출(환경 변수 자가진단 아님). 보고 내용은 본 로그로 대체 기록.

```
[inkbaduk 운영] 정기 상태 요약
헬스 OK — prod backend·web 200, 디스크 4%, plist drift 없음 (staging 중단·정책상 정상)
백업 OK — 최신 05-29 04:00, 복원 드릴 integrity ok, daily 10
보류 승인 0건
최근 CWI ingest: 신규 0 (2026-05-27 21:41)
최근 분석: 2026-W21
버그스캔: WS send-after-close RuntimeError 18회 → 이슈 #39 신규 생성 (dev-cycle 대기)
시각: 2026-05-29 12:00
```

## 18:00 오케스트레이터 (18:30 슬롯)

- **헬스체크** — prod backend 200 OK (db:true, katago_alive:false=정보용·정상), prod web 200 OK, `com.baduk.api`(PID 1466)·`com.baduk.web`(PID 1455) 가동, 디스크 4%, plist drift 없음(exit 0). staging 중단(정책상 정상). 전부 정상.
- **백업 검증** — 최신 `baduk-20260529T040005.db.gz`(약 14h, 신선), daily/weekly/monthly = 10/1/0, 복원 드릴 integrity_check=ok·테이블 7개. 정상.
- **버그 스캔** — `baduk-api.err`에서 `sqlite3.OperationalError: database is locked`(UPDATE sessions.last_seen_at) 누적 2516회를 반복 에러로 오인해 이슈 #40 생성 → 즉시 오탐 확인 후 종료. 대표 트레이스 타임스탬프 2026-05-25 14:06(KST, 05:06 UTC)으로 **PR #30**("last_seen_at 60s 디바운스 캐시" 머지 2026-05-25 12:50 UTC) 이전 발생분이며, 현재 prod 프로세스(PID 1466) 기동 이후 신규 0회. dashboard.md에도 이미 PR #30 이후 미발생으로 기록돼 있었음 — 스캔 전에 대조하지 못한 절차 실수. **신규 진짜 버그 0건.** 기존 #39(WS send-after-close)는 열린 상태 유지.
- **백로그 트리아지** — 미분류 이슈 0건. 할 일 없음.
- **PR 감시** — 열린 PR 0건. 정상.
- **기타** — 보류 승인 0건. 최근 CWI ingest 2026-05-27 21:41(fetched=0 new=0). 최근 주간 리포트 2026-W21.
- **Telegram 보고** — reply/send 도구를 ToolSearch로 실제 조회했으나 이 세션에 노출되지 않음(telegram 채널 reply 도구 MCP 미로드, "+telegram" 매칭 0건). 사유: 도구 미노출(환경 변수·플래그 자가진단 아님). 보고 내용은 본 로그로 대체.

```
[inkbaduk 운영] 정기 상태 요약
헬스 OK — prod backend·web 200, 디스크 4%, plist drift 없음 (staging 중단·정책상 정상)
백업 OK — 최신 05-29 04:00, 복원 드릴 integrity ok, daily 10
보류 승인 0건
최근 CWI ingest: 신규 0 (2026-05-27 21:41)
최근 분석: 2026-W21
버그스캔: 신규 0건 (database-locked 후보는 PR #30 픽스 전 발생분 — 오탐 #40 닫음). 미해결 #39 유지
시각: 2026-05-29 18:00
```
