# 2026-06-23 운영 로그

- 02:00 dev-cycle 자율 버그 사이클 실행
  - 이슈 선택: 열린 이슈 0건 (`gh issue list --state open` → `[]`, repo rarebirds-svg/quite-baduk). `bug`·`small` 적격 이슈 없음.
  - **처리할 버그 없음** — 종료.
  - 비고: `gh`가 launchd PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 실행(auth OK).
  - Telegram 보고: 발송 불가 — **reply 도구 미노출**. ToolSearch("telegram reply send message notify inbound")로 확인, telegram 채널 reply 도구가 세션에 노출되지 않음(노출된 알림류는 PushNotification·Monitor·Canva reply뿐, 연결 MCP는 Canva·Context7·Google Drive). 6/22 02:00·12:30·18:30 세션과 동일 상태. 보고 예정 내용: "[inkbaduk 운영] dev-cycle — 처리할 버그 없음(열린 이슈 0건)."

- 12:00 오케스트레이터 정기 실행 (12:30 슬롯)
  - **헬스체크 🟢 정상** — prod backend 200 OK(`{"status":"ok","db":true,"katago_alive":false}`, lazy spawn 정보용), prod web 200 OK, launchctl `com.baduk.web` PID 912 / `com.baduk.api` PID 923 가동, 디스크 6%, plist drift 없음(sync-launchd --check exit 0). staging 중단(정책상 정상).
  - **백업 검증 🟢 정상** — 최신 `baduk-20260623T040005.db.gz`(04:00, 30h 이내), 티어 daily 14 / weekly 5 / monthly 1, 복원 드릴 integrity_check=ok·7 tables.
  - **버그 스캔 — 신규 버그 없음** — `baduk-api.err`는 INFO 로그만(WS accept/open/close 정상 흐름), 스택트레이스·예외 없음. 파일 최종 수정 6/21 17:47 이후 신규 에러 0. 후보 없음.
  - **백로그 트리아지 — 할 일 없음** — 미분류(no:label) 이슈 0건.
  - **PR 감시 — 정상** — 열린 PR 0건.
  - 비고: `gh`가 launchd PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 실행(auth OK).
  - Telegram 보고: 발송 불가 — **reply 도구 미노출**. ToolSearch("telegram reply send message notify channel")로 확인, telegram 채널 reply 도구가 세션에 노출되지 않음(노출 알림류는 PushNotification·Monitor·SendMessage·Canva reply뿐, 연결 MCP는 Canva·Context7·Google Drive·claude_design). 보고 예정 내용 아래.
    ```
    [inkbaduk 운영] 정기 헬스 요약 (12:30 슬롯)
    헬스: prod OK (backend·web 200, 디스크 6%, drift 없음)
    백업: 06-23 04:00 OK (integrity ok, 7 tables)
    보류 승인: 0건
    최근 CWI ingest: 2026-06-21 03:05 (new=200, dup=837, err=0)
    최근 분석: 2026-W25
    버그·PR: 신규 버그 0, 열린 PR 0
    시각: 2026-06-23 12:00
    ```

- 18:00 오케스트레이터 정기 실행 (18:30 슬롯)
  - **헬스체크 🟢 정상** — prod backend 200 OK(`{"status":"ok","db":true,"katago_alive":false}`, lazy spawn 정보용), prod web 200 OK, launchctl `com.baduk.web` PID 912 / `com.baduk.api` PID 923 가동, 디스크 6%, plist drift 없음(sync-launchd --check exit 0). staging 중단(정책상 정상).
  - **백업 검증 🟢 정상** — 최신 `baduk-20260623T040005.db.gz`(04:00, 30h 이내), 티어 daily 14 / weekly 5 / monthly 1, 복원 드릴 integrity_check=ok·7 tables.
  - **버그 스캔 — 신규 버그 없음** — `baduk-api.err`(mtime 6/21 17:47, 신규 미기록)를 grep 전수 확인. 발견된 유일한 예외는 `Object of type bytes is not JSON serializable`(2회, `app/errors.py` val_handler) — 비JSON 본문(`b'{"nickname":"deploy-check-77"}'`, 배포 프로브) 검증 실패 시 핸들러가 raw bytes를 직렬화하려다 500으로 둔갑하던 버그. **이미 수정 완료**(commit `ce9d745`, 6/11 "비JSON 본문 검증 오류가 500으로 둔갑하는 버그 수정" — 현재 `errors.py`에 `_safe_validation_errors`+bytes custom_encoder 존재). 로그 항목은 수정 배포 이전 stale 기록. 신규 이슈 불필요. (recency 우선 — `[[bug-scan-verify-recency]]`)
  - **백로그 트리아지 — 할 일 없음** — 미분류(no:label) 이슈 0건.
  - **PR 감시 — 정상** — 열린 PR 0건.
  - 비고: `gh`가 launchd PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 실행(auth OK).
  - Telegram 보고: 발송 불가 — **reply 도구 미노출**. ToolSearch 2회(telegram reply/send/notify/inbound/channel·chat/bot/dm/outbound)로 확인, telegram 채널 reply 도구가 세션에 미노출(노출된 알림류는 PushNotification·Monitor·SendMessage·Canva뿐). 플러그인 bin(`.../telegram/0.0.6/bin/`)도 빈 디렉터리. 채널 설정(`~/.claude/channels/telegram/{.env,access.json}`)은 존재하나 reply 도구가 이 세션에 로드되지 않음. 6/22·6/23 12:00 세션과 동일 상태. 보고 예정 내용 아래.
    ```
    [inkbaduk 운영] 정기 헬스 요약 (18:30 슬롯)
    헬스: prod OK (backend·web 200, 디스크 6%, drift 없음)
    백업: 06-23 04:00 OK (integrity ok, 7 tables)
    보류 승인: 0건
    최근 CWI ingest: 2026-06-21 03:05 (new=200, dup=837, err=0)
    최근 분석: 2026-W25
    버그·PR: 신규 버그 0, 열린 PR 0
    시각: 2026-06-23 18:00
    ```
