# 2026-07-30

- 02:00 dev-cycle: **#70 처리 완료** — `fix/issue-70` / `4ff419a` / PR #72.
  - **적격 이슈 집합.** `gh issue list --state open` 결과 **2건** — #70(`bug`·`small`), #65(`bug`·`prio:high`). #65는 라벨상 우선순위가 높지만 7/24 사이클이 이미 `fix/issue-65`/`386567d`/PR #66으로 처리해 뒀고 머지만 남았다(🟡 · `AP-20260725-01`). 중복 브랜치·PR을 만들지 않으려 건너뛰고 **#70을 잡았다**. PR #66 상태 실측 — `OPEN`·`MERGEABLE`·`CLEAN`, 생성 후 **6일** 머지 대기.
  - **버그 성격.** `ops/check-staleness.sh`의 `extract_last_ts()`가 로그의 **마지막 `[YYYY-MM-DD HH:MM:SS]` 패턴**만 뽑아 임계와 비교했다. 그런데 래퍼(`run-*.sh`)는 잡이 실패해도 `[ts] 비정상 종료` 뒤에 `[ts] <잡> 종료`를 **무조건** 남긴다(`run-dev-cycle.sh:18-20` 등 6개 래퍼 전부 동일 구조). 그래서 인증 실패로 1초 만에 죽은 잡도 "방금 실행됨"으로 보였고, 크래시 루프는 매 사이클 timestamp를 새로 찍어 **영구히 은폐**된다.
  - **실사례 재현.** 로그 실측으로 7/28 3회 유실을 확인했다 — `dev-cycle-runs.log:555-560`(02:00 시작 → 비정상 종료 → 종료), `orchestrator-runs.log:1279-1284`(12:00), `1285-1290`(18:00). 세 건 모두 `비정상 종료` 직후 정상 `종료` 마커가 붙어 있다.
  - **수정 방향.** 이슈가 제안한 두 안 중 **성공 종료 마커 기준** 쪽을 택했다. `JOBS` 항목에 잡별 성공 마커를 4번째 필드로 추가(`orchestrator 종료` … `backup 완료` — backup만 `완료`로 다르다), `extract_last_ts()`를 awk 상태 머신으로 교체해 **직전에 `비정상 종료`가 찍힌 종료 마커는 성공으로 세지 않는다**. `시작` 라인에서 플래그를 리셋한다. incident·알림 문구도 `마지막 실행` → `마지막 성공 실행`, 빈 값은 `(성공 실행 기록 없음)`으로 정정.
  - **제안 중 넣지 않은 것.** `Failed to authenticate` 출현 자체를 별도 incident 유형으로 승격하는 안은 보류했다. 이 수정만으로 해당 모드가 임계 초과 시 stale로 잡히고, **실패 문자열 특정 감지는 다음번 실패 사유가 바뀌면 같이 낡는다** — 감지 근거를 "무엇으로 죽었나"가 아니라 "성공한 지 얼마나 됐나"에 두는 편이 오래간다. 필요하면 별도 이슈가 낫다고 코멘트에 남겼다.
  - **TDD.** 신규 `ops/tests/test-check-staleness.sh` — 임시 ROOT에 6개 잡 픽스처 로그를 깔고 **실제 스크립트를 그대로 실행**해 `incidents.md`에 기록된 잡 이름을 대조한다. 케이스 1(인증 실패 즉시 종료)이 **수정 전 FAIL(통과 5/실패 1) → 수정 후 PASS(통과 6/실패 0)**로 회귀를 재현·고정했다. 나머지 5 케이스는 과잉 교정 가드다 — 정상 성공, `backup 완료` 마커 인정, 실패 후 재실행 성공, 빈 로그(기존 동작 보존), **본문이 '비정상 종료' 문자열을 인용해도 오판 없음**(timestamp 접두어 없으면 마커가 아니다 — 이 로그 파일 자체가 그 단어를 쓰므로 실재하는 위험이다).
  - **테스트 가능하게 만든 변경 1줄.** `ROOT="${ROOT:-/Users/daegong/projects/baduk}"` — 기본값은 그대로라 prod 동작 불변. 이게 없으면 테스트가 실제 `incidents.md`에 쓰고 Telegram을 쏜다.
  - **실제 prod 로그 대조 — 오탐 0건.** `docs/ops/state/log/*.log` 사본을 임시 ROOT에 넣고 실행 → `신규 incident 0 건`. 잡별 마지막 성공 시각은 orchestrator `7/29 18:05:19` · dev-cycle `7/29 02:04:20` · content-draft `7/29 02:01:02` · content-ingest `7/26 03:11:00` · analytics-weekly `7/26 09:01:26` · backup `7/29 04:00:07`. **구 로직과 갈린 건 dev-cycle 하나뿐**인데(구=`7/30 02:00:03`), 구 로직이 *진행 중인 이번 사이클의 `시작` 라인*을 신선도로 셌기 때문이고 신규 로직이 마지막 **완료된** 성공을 보는 것이 의도한 동작이다.
  - **7/28 실사례 대입 검증.** 그날 20:00 watchdog 사이클 기준으로 계산하면 — orchestrator 구 **2.0h**(감지 안 됨) → 신규 **25.9h**(임계 18h, **감지**), dev-cycle 구 **18.0h**(감지 안 됨) → 신규 **41.9h**(임계 30h, **감지**). 즉 이 수정이 있었다면 24시간 공백이 그날 저녁에 알림으로 나갔다.
  - **검증 범위.** `bash -n` 통과. shellcheck는 미설치라 건너뜀. 백엔드·프론트엔드 코드는 건드리지 않아 pytest·vitest는 무관(변경 파일 `ops/check-staleness.sh`, 신규 `ops/tests/test-check-staleness.sh` 둘뿐).
  - **PR #72** — CI **4잡 전부 SUCCESS**(backend 1m7s · frontend 56s · app-shell-build 1m3s · **e2e 2m9s**), `MERGEABLE`. e2e가 그린인 것도 확인했다 — 이제 e2e ❌는 "선재 실패"가 아니라 그 PR이 깨뜨린 신호로 읽어야 하므로(`[[ci-e2e-disabled]]`) 통과 여부를 명시적으로 봤다. 이슈 #70에 브랜치·SHA·PR 코멘트를 남기고 `in-progress` 라벨 제거. **머지는 하지 않았다(🟡 — 사람).**
  - **머지 대기 누적.** 이번 PR #72를 포함해 열린 PR **3건** — #66(6일), #71, #72. `AP-20260725-01`·`AP-20260723-01`은 여전히 사람 판단 대기다.
  - 결과 Telegram 1건 발송(message_id **2479**). `reply` 도구는 이번에도 미노출이라 Bot API 직접 호출 — 한글 본문을 파일에 쓰고 `-F "text=</파일"` + `LC_ALL=en_US.UTF-8`로 보냈다(`[[dev-cycle-telegram-botapi]]`).

## 12:00 오케스트레이터 — 러닝북 5종

**결론.** prod 정상, 신규 이슈 0건, 주의 PR 0건. 사람 판단 대기가 **3건으로 늘었다**(보류 승인 3 · 열린 PR 3).

- **헬스체크** — backend `{"status":"ok","db":true,"katago_alive":true}` · web 200. launchctl PID **api 512 / web 497 = 7/29 12:00·18:00과 동일**(36시간 무재기동, api uptime `1일 11시간 59분`, 기동 `Wed Jul 29 00:01:09`). 디스크 **6%**. `sync-launchd.sh --check` exit 0·무출력 = drift 없음. staging 중단(정책상 정상). watchdog 신규 incident 0건.
- **백업 검증** — 최신 `baduk-20260730T040005.db.gz`(**7/30 04:00**, 3.02MB). 신선도 정상(`-mtime +1` 미해당). 티어 daily **14** / weekly **8** / monthly **2**. 복원 드릴 `integrity_check=ok`, **9 tables**. 회전 정상(7/29분 → 7/30분).
- **버그 스캔 — 신규 0건.** `.err`는 **225,398줄 / mtime `Jul 29 00:01`** 로 7/29 12:00·18:00과 **바이트 단위 완전 동일** = 현 프로세스 기동 이후 36시간 트레이스백 **0건**. 같은 구간 `.log`는 진행 중(206,908줄)이라 로그 정지가 아니라 진짜 무에러다. 누적 `database is locked` 2,556건은 전부 구 프로세스 구간이라 판정에 쓰지 않는다(`[[bug-scan-verify-recency]]`). 열린 `bug` 이슈 #65·#70과 중복될 신규 후보도 없음.
- **백로그 트리아지** — `no:label` 열린 이슈 **0건**. 조치 없음.
- **PR 감시** — 열린 PR **3건(#66·#71·#72)**, 전부 `MERGEABLE`/`CLEAN` + CI 4잡 SUCCESS. **주의 PR 0건** — #66이 생성 후 **5.4일**로 7일 정체 기준에 아직 미달이다(다음 실행쯤 넘길 가능성).

### 이번 사이클에서 확인한 것

- **`POST /api/games` 106건은 최근 트래픽이 아니다 — 창 크기 함정.** `tail -3000` 창에 `POST /api/games` 201×4 · `/hint` 200×102가 잡혀 "대국 트래픽 발생"으로 보였으나, DB 실측상 **최신 게임은 여전히 id 335(7/26 11:11 시작, 11:58 종료)** 이고 힌트도 전부 335 건이다. 액세스 로그에 timestamp가 없어 3,000줄 창이 7/26까지 거슬러 올라간 것. `tail -1500`·`tail -500`으로 좁히면 `/api/games`·`/api/ws/` **0건**이다. → `[[bug-scan-verify-recency]]`의 "창 크기를 최근성 대리지표로 쓰지 말 것"이 이번에도 적중.
- **따라서 #69(대국 프리즈) 라이브 검증은 4사이클 연속 무부하 알리바이다.** 현 기동(7/29 00:01) 이후 실사용 대국이 **한 판도 없었다**. `38fd9e3` 배포 효과는 여전히 미검증이며, 다음 실사용 대국 구간이 진짜 판정 시점이다.
- **최근 500줄 트래픽 구성** — health **299** · `/api/spectate/pro/{id}` **135** · `POST /api/analytics/hit` **20** · `/api/session` 20 · sitemap 4 · themes 3. **5xx 0건**(최근 3,000줄 기준). 프로 상세 밀도는 7/29 18:00(87건)보다 **또 올랐다**.
- **`visit_hits` 54행으로 3사이클째 +0 — 버그 아님으로 규명.** 같은 구간 `POST /api/analytics/hit`이 **23건 전부 204**인데 행이 안 늘어 의심했으나, `backend/app/api/analytics.py:33-35`에서 `is_bot(ua)`이면 **적재 없이 204를 반환**한다. 즉 이 비콘들은 JS를 실행하는 크롤러(Googlebot 등)가 쏜 것이고 봇 제외 필터가 의도대로 동작한 결과다. 최신 사람 방문은 `2026-07-28 22:06:34 UTC`(= 7/29 07:06 KST, `/glossary/samsam`).
- **메일설정 취약점 스캐너 프로브 유입 — 조치 불필요.** 최근 3,000줄에 `/api/v1/smtp`·`/api/v1/get/dkim/private/`·`/api/v4/email/test`·`/api/v2/meta/plugins` 등 **15건, 전부 404**. 인터넷 배경 잡음 수준이고 노출된 경로가 없어 incident로 만들지 않는다. 반복·증가하면 재검토.
- **`AP-20260723-01`의 대가가 8일째 실측된다.** 이번 세션 시작 시에도 `orchestrator-runs.log` 12:00:05 항목에 `Write(...)` deny 실효 없음 경고 **2건이 그대로** 찍혔다. prod DB·모델 가드레일은 여전히 작동하지 않는다.

### 조치

- **승인 제안 `AP-20260730-01` 신규 등재** — PR #72(watchdog 성공 마커 판정, #70 수정) 머지(🟡). 02:00 dev-cycle이 만들어 둔 PR인데 승인 큐에는 없어서 이번에 등재했다. 변경 파일이 `ops/check-staleness.sh` + 테스트 2개뿐이라 **prod 재기동 불필요**하고, 머지+`git pull`만으로 라이브 반영된다.
- `AP-20260725-01`(120시간)·`AP-20260723-01`(168시간)에 긴급도·진행 갱신 추가.
- 대시보드·pending-approvals 갱신. incidents 신규 0건.
- Telegram 요약 발송 완료(message_id **2481**). 이번 세션에도 `reply` 도구가 노출되지 않아 Bot API 직접 호출 — 한글 본문을 파일에 쓰고 `-F "text=</파일"` + `LC_ALL=en_US.UTF-8`(`[[dev-cycle-telegram-botapi]]`).

## 18:00 오케스트레이터 — 러닝북 5종

**결론.** prod 정상, 신규 이슈 0건, 주의 PR 0건. 사람 판단 대기 3건 유지(보류 승인 3 · 열린 PR 3). 조치가 필요한 신규 사실은 없었다.

- **헬스체크** — backend `{"status":"ok","db":true,"katago_alive":true}` · web 200. launchctl PID **api 512 / web 497 = 12:00과 동일**(42시간 무재기동, 기동 `Wed Jul 29 00:01:09`). 디스크 **6%**. `sync-launchd.sh --check` exit 0·무출력 = drift 없음. staging 중단(정책상 정상). watchdog 신규 incident 0건.
- **백업 검증** — 최신 `baduk-20260730T040005.db.gz`(7/30 04:00, 3.02MB) — 12:00과 동일 파일(다음 회전은 7/31 04:00). 신선도 정상(`-mtime +1` 미해당). 티어 daily **14** / weekly **8** / monthly **2**. 복원 드릴 `integrity_check=ok`, **9 tables**.
- **버그 스캔 — 신규 0건.** `.err`는 **225,398줄 / mtime `Jul 29 00:01`** 로 7/29 12:00·18:00·7/30 12:00과 **바이트 단위 완전 동일** = 현 프로세스 기동 이후 **42시간** 트레이스백 0건. 같은 구간 `.log`는 207,036줄(12:00 대비 +128줄, mtime `Jul 30 18:01`)로 계속 진행 중이라 로그 정지가 아니라 진짜 무에러다. 열린 `bug` 이슈 #65·#70과 중복될 후보도 없음.
- **백로그 트리아지** — `no:label` 열린 이슈 **0건**. 조치 없음.
- **PR 감시** — 열린 PR **3건(#66·#71·#72)**, 전부 `MERGEABLE` + CI 4잡 SUCCESS(재실측). **주의 PR 0건** — #66이 생성 후 **약 136시간(5.7일)** 로 7일 정체 기준 미달. **정체 전환 시점은 8/1 02:06 KST** 로, 23:30 사이클도 아직 미달이고 8/1 12:00 사이클에서 처음 "정체"로 잡힌다.

### 이번 사이클에서 확인한 것

- **#69(대국 프리즈) 라이브 검증은 5사이클 연속 무부하 알리바이.** DB 최신 게임은 여전히 **id 335(7/26 11:11 시작 → 11:58 종료)** 이고, 최근 1,200줄에 `POST /api/games`·`/api/ws/games` **0건**이다. 현 기동(7/29 00:01) 이후 실사용 대국 0판 = `38fd9e3` 배포 효과는 여전히 미검증. 다음 실사용 대국이 진짜 판정 시점이다.
- **최근 400줄 트래픽 구성** — health **252** · `/api/spectate/pro/{id}` **123** · sitemap 4 · themes 2 · settings 2. **5xx 0건**(최근 2,000줄 기준). 프로 상세 밀도는 12:00(135건/500줄)과 사실상 동일 수준이다.
- **스캐너 프로브가 자격증명 계열로 확장됐다 — 노출 0건 확인.** 12:00에 본 메일설정 프로브(`/api/v1/smtp` 등)에 이어 이번 창에는 `/api/.env`(3 IP: 195.178.110.31 · 38.133.142.103 등) · `/api/v1/credentials` · `/api/v1/get/dkim/private/` · `/api/v4/config/client` 가 추가됐다. **전부 404**이고, `curl`로 `/api/.env`·`/api/v1/settings`를 직접 때려도 **404** 확인 = 라우팅되는 경로가 실제로 없다. 인터넷 배경 잡음이라 incident·이슈로 만들지 않는다. 다만 12:00 대비 프로브 *종류*가 늘었으므로 다음 사이클에도 경로 목록을 관찰한다.
- **`AP-20260723-01`의 대가 8일째 유지** — prod DB·모델 deny 규칙이 여전히 `Write(...)` 형식이라 무실효. PR #71이 머지만 남은 상태.

### 조치

- 신규 승인 제안 없음. 기존 3건(`AP-20260730-01`·`AP-20260725-01`·`AP-20260723-01`) 모두 사람 판단 대기 유지.
- 대시보드 갱신. incidents 신규 0건.
- Telegram 요약 발송 완료(message_id **2482**). 이번 세션에도 `reply` 도구가 노출되지 않아(`ToolSearch`로 확인 — Telegram 발송 도구 없음) Bot API 직접 호출(`[[dev-cycle-telegram-botapi]]`).
  - **1차 시도 404 실패 → 원인 규명.** `set -a; . ~/.claude/channels/telegram/.env`를 별도 Bash 호출에서 실행했더니 다음 호출에 `TELEGRAM_BOT_TOKEN`이 비어 URL이 `/bot/sendMessage`가 됐다. **하네스는 Bash 호출 간 셸 환경변수를 보존하지 않는다** — 토큰 source와 `curl`을 **같은 호출 안에** 넣어야 한다. 다음 세션은 이 순서를 처음부터 지킬 것.
