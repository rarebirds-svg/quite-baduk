# 2026-07-26

- 02:00 dev-cycle: **처리할 버그 없음** — 신규 작업 없이 종료(net-zero, 커밋·브랜치·PR 생성 없음).
  - 이슈 선택. `gh issue list --state open` 결과 **열린 이슈 1건뿐**(`#65`, 라벨 `bug`·`prio:high`). `feature`·`in-progress` 없어 라벨상으로는 적격이지만, **7/25 사이클이 이미 처리해 PR #66이 열려 있다** — 재처리하면 중복 브랜치·중복 PR이 된다. `in-progress` 부착도 하지 않았다.
  - **PR #66 상태 확인.** `fix/issue-65` / `386567d`, CI 4잡 **전부 SUCCESS**(backend·frontend·app-shell-build·e2e). 7/24 17:06Z 생성 후 **약 33시간** 사람 머지 대기. 머지는 🟡라 손대지 않음. 승인 요청은 `AP-20260725-01`로 이미 등재돼 있어 신규 등재 없음.
  - **#65 최근성 재확인 — 판정 보류가 정답이다(`[[bug-scan-verify-recency]]`).**
    - prod api가 **7/25 21:21:50에 재기동**됐다(PID 5597→**40446**, err 마커 line 225181). `main` HEAD `335086d`의 커밋 시각이 21:27:21로 재기동 **5분 뒤**라, 이 재기동은 그 커밋 배포가 아니라 사람의 작업 세션 중 재기동으로 보인다(원인 규명은 오케스트레이터 범위).
    - 마커 이후 `database is locked` **0건**. 하지만 이번에도 **완화 신호로 읽으면 안 된다** — 같은 구간에서 `/api/spectate/pro/` 요청이 **25건**뿐이다(7/25 18:00 이후 상한 기준, 전체 요청 6,919건 중). #65는 크롤러가 프로 기보 상세를 훑을 때 터지는 경합인데 그 부하 자체가 없었다. 7/25 12:00·18:00과 같은 "부하 부재로 인한 0건" 구간이다.
    - 로그 채널은 분리해서 봤다(`[[api-log-stdout-vs-stderr]]`) — 요청량은 `.log`(182,969줄, mtime 07-26 02:00), 트레이스백·기동 마커는 `.err`(225,184줄, mtime 07-25 21:21). 12:00 실행이 저질렀던 "`.err` 4줄 = 트래픽 0" 오독을 반복하지 않았다.
    - 같은 구간 **5xx 0건**. 즉 사용자 노출 오류도 없었으나, 이것도 저부하의 결과다.
  - **#66이 미머지라 #65 수정은 여전히 prod에 없다.** 다음 크롤러 유입 구간에서 락 500이 재현될 여지는 그대로다. 머지 촉구가 이번 사이클의 유일한 액션 가능 항목.
  - 보류 승인 **2건 대기 유지** — `AP-20260725-01`(PR #66 머지, 약 14시간), `AP-20260723-01`(settings.json deny `Write`→`Edit`, **72시간 무응답**).
  - 워크트리 `.worktrees/dev-cycle`는 `fix/issue-65`(`386567d`)에 그대로 둔다. PR이 열려 있어 정리 대상이 아니다.
  - Telegram 보고 1건 발송.

## 09:00 · 주간 분석 리포트 (analytics-weekly)
- `docs/ops/state/reports/2026-W30.md` 작성. 대국 18국(전주 0국) / 누적 331국, 신규 세션 1건(누적 1), 글로서리 27편·FAQ 12편(합 +12), 보류 승인 2건.
- `/api/stats`는 **HTTP 401**(인증 필요)로 조회 불가 — DB 카운트로 대체. `/api/health`는 200.
- Telegram 요약 발송 완료 (message_id 2452).

## 12:00 · 오케스트레이터 (러닝북 5종)

- **헬스체크 OK.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web 200. launchctl 가동 — api PID **40446** / web PID **54913**. 디스크 **6%**, `sync-launchd.sh --check` exit 0(drift 없음). staging 중단 = 정책상 정상. 신규 incident 0건.
  - api PID 5597→40446 변경은 7/25 21:21 재기동 건으로 **02:00 사이클이 이미 규명**했다(사람 작업 세션 중 재기동). 신규 사건 아님.
- **백업 검증 OK.** 최신 `baduk-20260726T040005.db.gz`(04:00, 3.0MB) — `-mtime +1` 미해당 = 신선. 티어 14/8/2. 복원 드릴 `integrity_check=ok`, **테이블 9개**. 7/25 대시보드가 "다음 04:00 백업부터 9 tables"로 예측한 것이 **그대로 확인**(#67의 `visit_hits`·`search_queries` 포함).
- **버그 스캔 — 신규 이슈 0건. 다만 이번엔 처음으로 "부하 있는 구간"이 잡혔다.**
  - `.err`는 **라인 수·mtime 모두 불변**(225,184줄 / 07-25 21:21). 즉 7/25 21:21 재기동 이후 **트레이스백·경고 출력 0건**.
  - 그런데 이번엔 저부하가 아니다. 액세스 로그 `.log`는 02:00의 182,969줄 → **203,037줄(+20,068줄/10시간)**, 그 구간에서 `/api/spectate/pro/` **219건**, **5xx 0건**. 02:00 사이클이 본 25건과 달리 경합이 날 만한 밀도의 첫 구간이다(`[[api-log-stdout-vs-stderr]]` 준수 — 요청량은 `.log`, 트레이스백은 `.err`).
  - **그래도 #65는 미해결로 유지한다.** PR #66이 미머지라 **수정은 prod에 없다** — 219건 무사고는 수정 효과가 아니라 7/23~24 크롤러 버스트 수준의 밀도가 재현되지 않았다는 뜻이다. 버스트 구간(17.7h/16건)의 요청 밀도를 실측할 방법이 없어 219건이 그 임계에 도달했는지는 **판정 불가**. 낙관도 기각도 하지 않는다(`[[bug-scan-verify-recency]]`).
  - 열린 `bug` 이슈는 #65 하나뿐이고 새 후보 에러가 없어 신규 이슈 생성 0건.
- **백로그 트리아지 — 미분류 0건.** `no:label` 결과 빈 배열, 할 일 없음.
- **PR 감시 — 열린 PR 1건, 정상.** #66 `mergeable=MERGEABLE`·`mergeStateStatus=CLEAN`, CI **4잡 전부 SUCCESS**(backend·frontend·app-shell-build·e2e). 7/24 17:06Z 생성 = **약 43시간**으로 7일 정체 기준 미달. 충돌·CI 실패 없음 → 주의 PR 0건. 머지는 🟡(AP-20260725-01)이라 손대지 않음.
- **보류 승인 2건 — 둘 다 무응답 지속.**
  - `AP-20260725-01` (PR #66 머지) — 등재 7/25 12:00 기준 **24시간**. 미머지인 동안 조회수 락 500 노출이 라이브에 잔존.
  - `AP-20260723-01` (`settings.json` deny `Write`→`Edit`) — 등재 7/23 12:00 기준 **72시간**. **이번 실행 `orchestrator-runs.log`에도 동일 경고 2건 재출력** = 미해소 재확인(`[[settings-deny-write-vs-edit]]`).
- **대시보드 stale 수치 3건 정정** — 실측으로 갱신했다.
  - 프로 기보 수 `1111(825+286)` → **2911(masterpiece 2611 + world 300)**. 7/26 03:11 CWI ingest(`fetched=2060 new=200 duplicate=1860 error=0`) 반영 포함. 대시보드 행이 오래 갱신되지 않아 실제와 1800건 벌어져 있었다.
  - sitemap URL `약 1137` → **2979**(prod `/sitemap.xml` `<loc>` 실측).
  - 최근 주간 리포트 `W29` → **W30**(09:00 생성분).
- **부수 확인.** `visit_hits` **42행**(7/25 8행 → 증가, 비콘 정상 동작 지속). 신규 `prune-visits` 잡 04:15 정상 실행(`deleted=0 retention_days=180`). watchdog 신규 incident 0건. 프로 상세 `GET /api/spectate/pro/115` **200**.
- Telegram 인바운드 답신 없음(inbox 최신 7/22) → 승인 처리 절차 미해당.

## 18:00 · 오케스트레이터 (러닝북 5종)

- **헬스체크 OK.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web 200. launchctl 가동 — api PID **40446** / web PID **54913**(12:00과 동일, 무재기동). 디스크 **6%**, `sync-launchd.sh --check` exit 0(drift 없음). staging 중단 = 정책상 정상. 신규 incident 0건, watchdog `WD-20260726-*` 0건.
- **백업 검증 OK.** 최신 `baduk-20260726T040005.db.gz`(04:00, 3.0MB) — `-mtime +1` 미해당 = 신선. 티어 **14/8/2**. 복원 드릴 `integrity_check=ok`, 테이블 **9개**. 12:00과 동일 아티팩트(다음 백업은 7/27 04:00).
- **버그 스캔 — 신규 이슈 0건. 이번 6시간은 다시 저부하 구간이다.**
  - `.err`는 **라인 수·mtime 여전히 불변**(225,184줄 / 07-25 21:21). 7/25 21:21 재기동 이후 **20시간 39분간 트레이스백 0건**.
  - 12:00 이후 델타 실측 — `.log` 203,037 → **204,575줄(+1,538줄/6시간)**. 그 구간 `/api/spectate/pro/` **17건**, **5xx 0건**(200×1,536 · 401×1 · 204×1).
  - **12:00의 "부하 있는 구간(219건/10시간)"과 대비된다** — 이번 6시간은 17건으로 밀도가 다시 떨어졌다. 즉 오늘 오전의 219건은 지속 부하가 아니라 일시적 유입이었고, 지금은 경합이 날 밀도가 아니다.
  - **#65 판정 보류 유지.** PR #66 미머지 = 수정이 prod에 없다. 0건은 여전히 저부하의 결과이지 완화의 증거가 아니다(`[[bug-scan-verify-recency]]`). 로그 채널 분리 준수(`[[api-log-stdout-vs-stderr]]`) — 요청량은 `.log`, 트레이스백은 `.err`.
  - 열린 `bug` 이슈는 #65 하나뿐, 새 후보 에러 없음 → 이슈 생성 0건.
- **백로그 트리아지 — 미분류 0건.** `no:label` 결과 빈 배열, 할 일 없음.
- **PR 감시 — 열린 PR 1건, 정상.** #66 `mergeable=MERGEABLE`·`mergeStateStatus=CLEAN`, CI **4잡 전부 pass**(backend 1m0s · frontend 51s · app-shell-build 50s · e2e 2m20s). 7/24 17:06Z 생성 = **약 49시간**으로 7일 정체 기준 미달. 충돌·CI 실패 없음 → 주의 PR 0건. 머지는 🟡(AP-20260725-01).
- **보류 승인 2건 — 둘 다 무응답 지속.**
  - `AP-20260725-01` (PR #66 머지) — 등재 7/25 12:00 기준 **30시간**.
  - `AP-20260723-01` (`settings.json` deny `Write`→`Edit`) — 등재 7/23 12:00 기준 **78시간**. **이번 실행 `orchestrator-runs.log`에도 동일 경고 2건 재출력** = 4일째 미해소(`[[settings-deny-write-vs-edit]]`).
- **부수 확인.** `visit_hits` **43행**(12:00 42행 → +1, 비콘 동작 지속). `pro_games` **2911**(12:00 정정값 유지, 다음 ingest는 8/2 일요일). `prune-visits` 04:15 정상(`deleted=0 retention_days=180`). 최근 주간 리포트 **2026-W30**.
- **오케스트레이터 주기 불일치 재확인(미해소).** `com.inkbaduk.ops-orchestrator.plist`의 `StartCalendarInterval`은 여전히 **12:00·18:00 2회**이고 러닝북·프롬프트는 4회(06:30·12:30·18:30·23:30)로 적는다. 이번 실행도 18:00 트리거 = plist대로다(누락 아님). 문서를 2회로 맞출지 plist를 4회로 늘릴지는 사람 판단(후자는 🟡).
- Telegram 인바운드 답신 없음(inbox 최신 7/22) → 승인 처리 절차 미해당.
- **Telegram 상태 요약 발송 완료 (message_id 2455).** 세션에 `reply` 도구가 노출되지 않아 `[[dev-cycle-telegram-botapi]]` 절차대로 Bot API 직접 호출(`~/.claude/channels/telegram/.env` 토큰 + `access.json` chat_id `1241277614`, 한글 본문은 파일에 써서 `text@파일` + `LC_ALL=en_US.UTF-8`). `ok=true`.
