# 운영 로그 — 2026-06-17

## 02:00 자율 버그 사이클 (dev-cycle)

### 이슈 선택
- `/opt/homebrew/bin/gh issue list --state open --json number,title,labels` → 열린 이슈 0건.
- `bug`/`small` 라벨 적격 이슈 없음 → **처리할 버그 없음**.
- 종료. worktree·브랜치 생성 안 함.

### 비고
- `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 조회.
- 6/13~6/16과 동일하게 신규 열린 이슈 없음.

### Telegram 보고
- telegram reply/send 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`(`ops/ops.env`)로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=True, message_id=2254).

## 02:00 콘텐츠 초안 생성 사이클 (content-draft)

### 종류·슬러그 선택
- glossary 13/18=0.722 vs faq 7/9=0.778 → 더 뒤처진 **glossary** 선택.
- 미작성 후보(알파벳순): kkutnaegi, okjip, poseok, seryeok-silli, susangjeon → 첫 항목 `kkutnaegi`(끝내기).

### 초안·QA·게시
- 초안 작성 → korean-copy-qa QA → 후수 정의·끝내기 가치 계산 두 문장 정확성 보완 반영(QA 보류 없음).
- 덮어쓰기 가드 통과 → `web/content/glossary/kkutnaegi.md` 단독 스테이징·커밋(53a1cd0) + `git push origin main` 성공. /glossary/kkutnaegi 즉시 노출.

### Telegram 보고
- send 도구 세션 미노출 → Bot API `sendMessage` 직접 호출(토큰 `~/.claude/channels/telegram/.env`, chat_id `1241277614`) → **발송 성공** (ok=True, message_id=2255). "kkutnaegi (glossary) 게시 완료 — /glossary/kkutnaegi".

## 12:30 오케스트레이터 실행

- **헬스체크** 🟢 — prod backend `{"status":"ok","db":true,"katago_alive":false}` 200 OK, prod web 200 OK. launchctl: api pid 923 / web pid 912 가동. staging 중단(정책상 정상). 디스크 5%. launchd plist drift 없음(exit 0). `katago_alive:false`는 lazy spawn 정보 신호 — 경보 아님.
- **백업 검증** 🟢 — 최신 baduk-20260617T040002.db.gz (오늘 04:00, 신선). 티어 daily 14 / weekly 4 / monthly 1. 복원 드릴 integrity_check=ok, 테이블 7개.
- **버그 스캔** 🟢 — baduk-api.err 최근 로그 INFO만(스택트레이스·예외 없음). 신규 버그 없음.
- **백로그 트리아지** 🟢 — 미분류 열린 이슈 0건. 할 일 없음.
- **PR 감시** 🟢 — 열린 PR 0건. 주의 PR 없음.
- **보고 데이터** — 보류 승인 0건. 최근 CWI ingest 2026-06-14 03:04 (new=200, dup=638, error=0). 최근 분석 리포트 2026-W24.
- **Telegram 보고** — reply/send 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=True, message_id=2256).

## 18:30 오케스트레이터 실행

- **헬스체크** 🟢 — prod backend `{"status":"ok","db":true,"katago_alive":false}` 200 OK, prod web 200 OK. launchctl: api pid 923 / web pid 912 가동. staging 중단(정책상 정상). 디스크 5%. launchd plist drift 없음(exit 0). `katago_alive:false`는 lazy spawn 정보 신호 — 경보 아님.
- **백업 검증** 🟢 — 최신 baduk-20260617T040002.db.gz (오늘 04:00, 신선). 티어 daily 14 / weekly 4 / monthly 1. 복원 드릴 integrity_check=ok, 테이블 7개.
- **버그 스캔** 🟢 — baduk-api.err에 `TypeError: bytes is not JSON serializable` 2건(line 221457·221549) 발견. 단, 트레이스백의 `errors.py:21 val_handler`는 **수정 이전** 버전 라인번호 — 현 코드는 `_safe_validation_errors`(custom_encoder)로 이미 처리. 해당 버그는 `ce9d745 fix(api): 비JSON 본문 검증 오류가 500으로 둔갑하는 버그 수정`으로 이미 패치됨. err 로그 mtime 6/16 23:22, 마지막 TypeError 이후 47줄은 재시작 INFO뿐. → stale, **신규 버그 없음**(recency 검증 적용 [[bug-scan-verify-recency]]).
- **백로그 트리아지** 🟢 — 미분류 열린 이슈 0건. 할 일 없음.
- **PR 감시** 🟢 — 열린 PR 0건. 주의 PR 없음.
- **보고 데이터** — 보류 승인 0건. 최근 CWI ingest 2026-06-14 03:04 (new=200, dup=638, error=0). 최근 분석 리포트 2026-W24.
- **Telegram 보고** — reply/send 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=True, message_id=2257).
- **로그 정리** — 12:30 항목 끝에 직전 세션이 남긴 도구 호출 누수(`</content>`·`</invoke>`) 제거.
