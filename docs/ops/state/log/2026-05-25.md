# 운영 로그 2026-05-25

## 02:00 — 자율 버그 사이클 (launchd 트리거 · 실제 실행 21:28 KST)

- **이슈 후보 조회** — `gh issue list --state open` 결과 0건. `bug`·`small` 라벨 모두 0건. 적격 이슈 없음.
- **처리 결과** — 처리할 버그 없음. worktree 작업·커밋·PR 없음.
- **참고** — 어제(05-24)까지 열려 있던 #12·#13은 각각 10:37·07:09에 closed. 신규 미트리아지 이슈도 0건.
- **Telegram 보고** — `~/.claude/channels/telegram/.env` 봇 토큰 + `ops/ops.env` `TELEGRAM_CHAT_ID`로 curl 직접 발송 (reply 도구 미노출).

## 21:30 — 운영 오케스트레이터 (정기 18:30 KST 트리거, watchdog 21:22 kickstart 직후)

- **healthcheck** — prod-backend 200 OK (`db:true, katago_alive:false` — lazy spawn,
  경보 아님). prod-web 200 OK. `com.baduk.api` PID 19236 / `com.baduk.web` PID 62828
  launchd 가동. staging backend·web 중단됨(정상). 디스크 5%.
  `ops/sync-launchd.sh --check` exit 0 — plist drift 없음.
- **backup-verify** — 최신 `baduk-20260525T040005.db.gz` (약 17h 경과 < 30h, daily 5 /
  weekly 1 / monthly 0). 복원 드릴 `integrity_check: ok` + 테이블 7개 — 정상.
- **bug-scan** — `~/Library/Logs/baduk-api.err`에 `sqlite3.OperationalError: database
  is locked` 누적 2262건, 마지막 발생 17:56. `sessions.last_seen_at` UPDATE 핫패스
  (`app/deps.py:51`). PR #30이 정확히 이 문제의 60s 디바운스 캐시 수정안 — 이미 추적
  중인 사안이므로 신규 이슈 후보 0건. 다른 후보 없음.
- **backlog-triage** — 오픈 이슈 0건, 미분류 0건.
- **pr-watch** — 열린 PR 1건 (#30 `feat(db): last_seen_at … 60s 디바운스 캐시`). CI:
  backend/frontend/e2e 모두 SUCCESS. mergeable MERGEABLE / mergeStateStatus CLEAN.
  정체·충돌 없음.
- **신규 제안** — AP-20260525-02 (PR #30 머지). dev-cycle이 등록한 AP-20260525-01
  (chuk 글로서리 게시)과 충돌 회피 차원에서 02 사용.
- **최근 CWI ingest** — 2026-05-25 21:28 fetched=0 new=0 (`cwi.index.unchanged`).
- **최근 분석 리포트** — 2026-W21.
- **watchdog** — 21:22 첫 수동 kickstart로 5건 incident (WD-20260525-2122xx
  orchestrator/dev-cycle/content-draft/content-ingest/backup) 기록 — 41h 무음의 사후
  감지. 이번 실행이 회복 첫 정기 사이클.
- **Telegram 보고** — reply 도구 미노출. `~/.claude/channels/telegram/.env` +
  `ops/ops.env`로 curl 직접 발송. 헬스 요약 1건 (ok), AP-20260525-02 승인 요청 1건
  (ok, message_id 1969).

## 21:30 — 콘텐츠 초안 사이클 (launchd 토요일 02:00 트리거 · 실제 실행 21:30 KST)

- **선택** — 글로서리 시드 미작성 슬러그 알파벳 순 첫 번째 `chuk`(축). 이미 게시된 슬러그: `bik`·`dan-gup` (글로서리), `ai-baduk-vs-human` (FAQ).
- **초안 작성** — `docs/ops/content/drafts/chuk.md` 신규 (frontmatter + 3단락, draft_by `agent v1`).
- **korean-copy-qa** — 통과 + 3건 수정 반영. ① 축머리 정의를 "직선 경로의 끝쪽"→"경로 위에"로 정정(추격 경로 어느 지점이든 활로 회복 시 축 불성립), ② "반" 첫 등장에 "반(바둑판)" 병기, ③ 입문자 친화로 "사석 작전"→"돌을 잡는 가장 기본적인 작전"으로 풀어 씀. 사실 오류·콜론 종결·번역투 없음.
- **AP 등록** — `AP-20260525-01` `pending-approvals.md`에 추가(라이브 게시 🟡). 같은 날 등록된 AP-20260525-02(PR #30 머지)와 ID 충돌 없음.
- **Telegram 발송** — 성공(HTTP 200, message_id 1966, chat 1241277614). `ops/notify-telegram.sh`는 채널 디렉터리 .env를 fallback으로 source하지 않아 토큰 미설정으로 exit 1 — 채널 .env에서 직접 source 후 curl 호출로 우회. notifier 개선은 후속 후보 ([[orchestrator-no-telegram]] 메모리 참조).

## 21:50 — AP-20260525-02 처리 (PR #30 머지)

- **실행** — `gh pr merge 30 --squash --delete-branch` 성공. squash commit `51976f3` (`feat(db): last_seen_at 매 요청 UPDATE를 60s 디바운스 캐시로 대체 (#30)`). 로컬 main fast-forward 동기화 (19ffa0f → 51976f3, +347 -32, `backend/app/last_seen_cache.py`·`backend/tests/test_last_seen_cache.py` 신규).
- **로컬 cleanup** — `feat/last-seen-at-debounce` 로컬 브랜치는 `.claude/worktrees/last-seen-debounce` worktree 점유로 자동 삭제 실패 — 보존.
- **배포 (별도 🟡)** — `database is locked` 해소를 위한 prod 적용은 backend launchd 재시작(`launchctl kickstart -k gui/$(id -u)/com.baduk.api`)이 필요. AP-02 텍스트가 "배포는 별도"라고 명시했으므로 이번 머지는 main 변경까지만, 재시작은 다음 승인 사이클.
- **큐 정리** — `pending-approvals.md`에서 AP-20260525-02 제거.
