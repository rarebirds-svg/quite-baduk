# 운영 로그 2026-05-24

## 01:54 — 운영 오케스트레이터 (24일 첫 실행 · launchd가 22:30/14:00·KST 대신 깨운 비정기 트리거)

- **healthcheck** — prod-backend 200 OK (`db:true, katago_alive:false` — lazy spawn 정책에
  따라 경보 아님). prod-web 200 OK. `com.baduk.api` PID 58016 / `com.baduk.web` PID 62828
  launchd 가동(마지막 종료 -15는 직전 실행 흔적). staging backend·web 모두 중단됨(정상).
  디스크 5%.
- **backup-verify** — 최신 `baduk-20260523T040005.db.gz` (약 21시간 경과 < 30시간, daily 3
  / weekly 0 / monthly 0). 복원 드릴 `integrity_check: ok` + 테이블 7개 — 정상. 24일 04시
  백업은 launchd 일정상 아직 미생성.
- **bug-scan** — `~/Library/Logs/baduk-api.err` 최신 스택은 23일 14:29 KST
  `sqlite3.OperationalError: database is locked` (`sessions` UPDATE `last_seen_at`,
  `app/deps.py:51`). 기존 이슈 #13으로 추적 중 — 신규 후보 없음. KataGo 죽음 흔적 없음.
- **backlog-triage** — 미분류 이슈 0건.
- **pr-watch** — 열린 PR 6건 (어제 18시 8건에서 #10·#11 머지로 감소).
  - MERGEABLE + frontend CI 실패: #14 (dev-cycle 산출물).
  - MERGEABLE/UNSTABLE + e2e FAILURE(과거 run) + 정체(24일+): #6, #7. e2e는 23일
    `chore(ci): e2e 잡 일시 disable`로 비활성화 — rebase·push 시 SKIPPED로 갱신 예상.
  - CONFLICTING/DIRTY + 정체(24일+) + e2e FAILURE: #5, #8, #9.
- **신규 이슈** — 0건. **보류 승인** — 1건 (AP-20260523-03 bik 글로서리 게시 대기).
- **최근 CWI ingest** — 2026-05-23 19:02 fetched=0 new=0 (신규 0).
- **최근 분석 리포트** — 2026-W21.
- **Telegram 보고 사후 정정 (2026-05-24 09:30 사용자 확인)** — 위 "미발송" 단정은
  오케스트레이터의 stale 환경 자가진단 함정. 실제로는 launchd plist가
  `ops/run-orchestrator.sh`를 호출하고 그 안에서 `--channels
  plugin:telegram@claude-plugins-official`이 이미 부여돼 있음. 채널 진실 공급원은
  `~/.claude/channels/telegram/{.env,access.json}`(4월 16일 설정, bot 데몬 가동 중,
  inbox 트래픽 정상)이며 `ops/ops.env`의 토큰 유무는 무관하다.
  [[orchestrator-no-telegram]] 메모리 참조.

## 02:00 — 운영 오케스트레이터 (2회차 · 01:54 직후 launchd 재기동)

- **healthcheck** — prod-backend 200 OK (`db:true, katago_alive:false` 정보, lazy 정책).
  prod-web 200 OK. `com.baduk.api` PID 58016 / `com.baduk.web` PID 62828 가동 유지.
  staging 중단(정상). 디스크 5%.
- **backup-verify** — 최신 `baduk-20260523T040005.db.gz` 약 22시간 경과(< 30시간),
  daily 3 / weekly 0 / monthly 0. 복원 드릴 `integrity_check: ok` + 테이블 7개. 24일 04시
  백업은 아직 미생성(launchd 일정 대기).
- **bug-scan** — 신규 후보 없음. 기존 `sqlite3 database is locked` 스택은 23일 14:29
  마지막 발생 그대로(이슈 #13 추적 중). 추가 KataGo·web 오류 없음.
- **backlog-triage** — 미분류 이슈 0건.
- **pr-watch** — 열린 PR 6건 (01:54와 동일 — 변동 없음).
  - MERGEABLE + frontend FAILURE: #14.
  - MERGEABLE + e2e FAILURE(정체 24일+): #7, #6.
  - CONFLICTING + e2e FAILURE(정체 24일+): #5, #8, #9.
- **신규 이슈** — 0건. **보류 승인** — 1건 (AP-20260523-03 bik 글로서리 게시).
- **최근 CWI ingest** — 2026-05-23 19:02 fetched=0 new=0 (신규 0).
- **최근 분석 리포트** — 2026-W21.
- **Telegram 보고 사후 정정 (09:30)** — 01:54 항목과 동일 사유 — 환경 자가진단 함정.
  Telegram 채널은 가동 중. 사후 보고 1건은 다음 정기(12:00) 세션에서 정상화될 것.

## 02:00 — 자율 버그 사이클 (launchd 트리거)

- **이슈 후보** — 열린 이슈 2건. #12 (bug+small) / #13 (bug only).
- **처리 결과** — 처리할 신규 버그 없음.
  - #12 — 직전 사이클이 이미 처리 완료. `fix/issue-12` `54190bf` 푸시 + PR #14 OPEN
    상태. 머지는 🟡(사람 전용)이라 본 사이클의 추가 작업 없음. 시작 시 잘못 단
    `in-progress` 라벨은 즉시 제거.
  - #13 — `small` 라벨 부재로 자율 경로 부적격(`sqlite database is locked` — DB 동시성
    설계 변경 필요, 사람 판단 영역).
- **Telegram 보고 사후 정정 (09:30)** — dev-cycle 세션도 동일한 환경 자가진단 함정에
  걸렸다. 실제 채널은 가동 중. dev-cycle은 보통 PR 생성 시점에만 Telegram을 호출하는데,
  본 사이클은 신규 PR이 없어 발송 트래픽 자체가 0이다.

## 09:00 — 주간 분석 리포트 (launchd 일요일 트리거)

- **idempotent 체크** — `docs/ops/state/reports/2026-W21.md` 이미 존재(23일 21:19 생성). 본
  세션은 재실행 안전 가드에 따라 통계 수집·재작성 없이 즉시 종료.
- **Telegram 알림** — 신규 작성이 없어 푸시 생략(중복 알림 방지).

## 11:00 — AP-20260523-03 큐 정리 (사용자 세션)

- **승인 큐 stale 정리** — `pending-approvals.md`의 AP-20260523-03 (bik 글로서리 게시)
  항목 제거. 실제 작업은 5월 23일 23:51에 커밋 `3009827 content(glossary): bik 게시
  (3c 검증)`로 이미 완료된 상태였으나 큐에서 제거되지 않아 stale 상태로 남아 있었다.
- **확인 근거** — `git ls-files web/content/glossary/bik.md`로 main 추적 확인.
  `docs/ops/content/drafts/bik.md`는 이미 이동·삭제됨(drafts/는 `.gitkeep`만).
- **재발 방지** — 콘텐츠 게시 자동화 사이클이 큐 항목을 제거하지 않는 누락이 있다면
  후속 점검 필요. 현재로선 1회성 정리.

## 11:30 — PR #22 생성 (e2e 재구성 + CI docker 제거 Phase 1)

- **브랜치** — `chore/e2e-rewrite-nickname-flow`, 9커밋. 설계·계획 문서 별도 main 커밋.
- **로컬 검증** — alt port 18000/13000 native stack에서 9 passed / 1 skipped / 0 failed
  (theme_lang는 사전 UI 회귀로 skip). 2회 연속 그린.
- **CI 진행** — backend·frontend SUCCESS, e2e IN_PROGRESS.
- **후속 (Phase 2)** — docker-compose.yml/Dockerfile/README/CLAUDE.md 정리 별도 PR 예정.
