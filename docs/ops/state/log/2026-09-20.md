# 2026-09-20
- 04:30 dev-cycle — **#101 처리 완료 — PR [#102](https://github.com/rarebirds-svg/quite-baduk/pull/102) 생성, 머지 대기(🟡).** 적격 이슈 3건(#97·#99·#101) 중 #97·#99는 PR #98·#100 대기 중이라 제외, **#101(사용자 기권 `finished_at` NULL, `bug`·`small`·`prio:low`) 선점**. `.worktrees/dev-cycle`을 `origin/main`(`54606b4`) 기준 `fix/issue-101`로 전환. TDD — `tests/api/test_games_extra.py`에 `test_user_resign_records_finished_at` 추가(기권 응답·재조회 모두 `finished_at` non-null) → 수정 전 `assert None is not None` 실패 재현 → `game_service.py` `resign_game`에 `import datetime as _dt; game.finished_at = _dt.datetime.now(_dt.UTC)` 2줄 추가(다른 종료 경로 4곳과 동일한 지역 import 패턴) → 통과. 검증 — `pytest` **610 passed**, `ruff check .` clean, `mypy app` strict clean. Fable 리뷰(general-purpose, 워크트리 경로 고정) **APPROVE** — `models/game.py` `DateTime | None`·`schemas/game.py` `UtcDatetime`과 타입·TZ 일치, `admin.py:765` 일별 종료 집계에 기권 대국 포함되고 web 소비처는 전부 `string | null` 허용이라 부작용 0, 수정 전 실패·후 통과 독립 재확인. 커밋 `4d56c67` push → PR #102 생성(본문에 검증·미포함 사항 기재), **CI 4잡(backend·frontend·app-shell-build·e2e) 전부 pass** 확인. 이슈 #101에 브랜치·SHA·PR 코멘트 남기고 `in-progress` 제거. **기존 99건 백필은 PR 범위 밖**(`max(moves.played_at)` 근사 가능, 사람 판단). Codex 교차 리뷰는 헤드리스 MCP 인증 행 이력으로 생략(Fable 단독). 잡 상태 `ok` 기록. 로그 커밋은 관례대로 09:00 오케스트레이터가 묶어서 처리. 열린 PR은 이제 3건(#98·#100·#102) — 09:00 오케스트레이터가 승인 카드(AP) 등록 대상.
- 05:00 news-hook — **게시 완료 — 여자바둑리그 준플레이오프 속보로 갱신(`7e616dd`, main push 완료).** 최근 7일 탐색에서 농심배 1라운드 후속·명인전·삼성화재배는 7일 밖이거나 결과 미확정이라 제외. 한국기원 여자바둑리그 공식 뉴스 1919(9/18 정규리그 종료: 평택 10연승 1위·김은지 21연승 마감·정유진과 공동 다승왕)·1920(9/20 준PO: 삼척이 부안에 2-1 역전승, 3위 팀 1승 규정으로 PO 직행, 23일 19시 철원과 PO)을 원문 curl로 확정하고, 최신인 1920을 `source_url`로 채택(기존 9/13 농심배 기사와 상이). 사실검증 서브에이전트(general-purpose·fable, 전경 동기) 응답 '일치'(사실·번역 모두 통과, "정규리그 2위 철원"은 1919 원문으로 별도 확인). `web/content/news-hook.json` 단일 파일만 스테이징·커밋·push(1회 성공, rebase 불필요). 재빌드·재기동 없음. 잡 상태 `ok` 기록.
- 09:01 analytics-weekly: 2026-W38 리포트 생성 (`docs/ops/state/reports/2026-W38.md`) — 방문 88 hits(전주 129), 게임 33건(전주 42), 글로서리 35·FAQ 14, 보류 승인 2건.
- 09:00 orchestrator(am) — **헬스 전 항목 정상, 밤사이 대국 0·트레이스백 0·신규 버그 0, 주간 ingest +200 정상, PR #102 신규 승인 카드 AP-20260920-01 등재, AP-20260916-01 96시간째·AP-20260917-01 72시간째 대기.** 헬스체크 — `/api/health` 200 `{db:true, katago_alive:true, migrations {current:0020, head:0020, pending:false}}`, web 200, 공개 `inkbaduk.com` 200·`/api/health` 200, `com.baduk.api` PID 5743(9/12 13:46 기동 유지, 187시간)·`com.baduk.web` PID 89541(9/8 23:56 기동 유지), staging 중단(정책상 정상), 디스크 8%, plist drift 없음(exit 0), Claude 인증 `ok D-12`(만료 10-02 23:41). 백업 — **신규 `baduk-20260920T040005.db.gz` 4,680,093B**(전일 4,565,192B 대비 **+114,901B** = 03:17 주간 ingest new=200과 정합, `[[backup-size-spike-weekly-ingest]]`), `find -mtime +1` 출력 없음, daily 14 / weekly 8 / monthly 4, 복원 드릴 `integrity_check` ok·테이블 11 = **85회 연속** 통과. 버그 스캔 — `.err` **+0줄**(235,520 유지, mtime 9/19 11:44) = 밤사이 WS·트레이스백·IntegrityError·locked 전부 0. **일요일 03:16 전후 `database is locked` 0줄** = #95 발효 후 2주 연속 무사(`[[database-locked-sunday-ingest]]`). `.log` 326,129 → 326,681줄(**+552**) 분포 200 546·401 3·404 2·**500 0**(429 0). 소스 — 127.0.0.1 SSR·헬스 402(`/api/spectate/pro/N` 352·health 158)·`34.62.82.165` 2(`/api/.env`·`/api/phpinfo.php` 404 = 스캐너 프로브, 무해)·`20.168.128.82`·`135.119.95.48`·`135.119.239.51`·`1.237.136.170` 각 1. 401 3은 09:00 `127.0.0.1` `GET /api/stats` = analytics-weekly 잡의 비인증 프로브(지난주 같은 시각 2회와 동일 패턴, 무해). `/api/spectate/pro?[object Object]` **10회**(SSR 7 + 외부 3, #99 미반영 재현, 누적 675 실측). SSR발 `pick/monthly/2131-07~12` 6회 200 — 크롤러 미래 연월 순회 장기 패턴, 무해. `baduk-web.err` **+0줄**(3,506). **DB 실측** — `max(games.id)` 506 유지(밤사이 신규 대국 0), 9/19 21:00 이후 착수 0(마지막 착수 9/19 11:44:43 KST #506, 21시간 무착수), #506 `active` 127수 미종료 유지, `visit_hits` 645(+0/12h), `pro_games` **4,511**(+200), `sessions` 9. KataGo 슬롯 로그 신규 spawn 0. 중복 차단 — 후보 에러 0건이라 신규 이슈 없음("신규 버그 없음"). 백로그 트리아지 — 미분류 이슈 0건. PR 감시 — 열린 PR **3건** [#98](https://github.com/rarebirds-svg/quite-baduk/pull/98)(9/16 04:34 KST 생성, 100.5h)·[#100](https://github.com/rarebirds-svg/quite-baduk/pull/100)(9/17 04:45 KST 생성, 76h)·**[#102](https://github.com/rarebirds-svg/quite-baduk/pull/102)(9/20 04:33 KST 생성, 4.5h, 신규)** 모두 CI 4잡 SUCCESS·`MERGEABLE`/`CLEAN`·`merge-tree` 충돌 0(main보다 10·8·1커밋 뒤, ops 문서·콘텐츠뿐), 브랜치 로컬=origin(`056ca3b`·`4d77d1c`·`4d56c67`), 정체(7일)·충돌·CI 실패 0건 — "열린 PR 모두 정상". `main` CI 최근 3런 success(최근 9/20 05:04 KST `7e616dd`). **잡 상태 4/4 `ok`** — dev-cycle 04:34(#101 → PR #102)·news-hook 05:04(여자바둑리그 준PO 게시, `7e616dd` main push)·**analytics-weekly 09:01(2026-W38 리포트 생성 — 방문 88건, 전주 129에서 −41)**·content-draft 9/19 02:03. content-ingest 03:19 `fetched=3674 new=200 duplicate=3474 error=0`, IndexNow 200건 제출 200 — sitemap `<loc>` **4,592**(9/13 4,390 대비 +202 = ingest 200 + 글로서리 1 + 월간 픽 1). 글로서리 35편·FAQ 14편 실측(변동 없음). 배포 갭 0(`HEAD`=`origin/main`=`7e616dd`, 9/12 재기동 이후 코드 커밋 없음 — 콘텐츠·docs만, BUILD_ID 9/8 유지). watchdog 밤사이 `신규 incident 0 건`, `[api] OK [web] OK`. 승인 — **AP-20260920-01 신규 등재**(PR #102 머지 + api 재기동, #101 기권 `finished_at`, `prio:low`, 기존 99건 백필은 범위 밖·사람 판단), **AP-20260916-01** 대기 유지(96시간째 · 8회 재확인, #97 실사용자 3회 연속 500 긴급도 유지), **AP-20260917-01** 대기 유지(72시간째 · 6회 재확인). 세 건 모두 CI·충돌 재확인 통과, 마지막 착수 9/19 11:44 이후 21시간 무착수·밤사이 대국 0이라 지금이 재기동 적기 — 세 PR을 한 회차로 머지하면 api 재기동 1회·web 재빌드+재기동 1회로 끝남. 관찰 유지 — 슬롯별 KataGo 재spawn 이슈 미등록 참고, #97 고아 `active` 행(#480·#502~#504)·#506 미종료 행은 이슈 범위 밖 부수 사항.
- 09:04 orchestrator(am) — am 다이제스트 발송 완료(`send.py` exit 0, 마커 `~/.ops-report/markers/inkbaduk-am-2026-09-20` 생성). 전체 ✅, 승인 카드 3장(AP-20260916-01 96시간째·AP-20260917-01 72시간째·AP-20260920-01 신규) 동봉, 마지막 착수 9/19 11:44 이후 21시간 무착수·밤사이 대국 0으로 재기동 적기 문구 포함. 자동화 행 텍스트는 40자 한도에 맞춰 `ingest +200 · W38 88방문`으로 축약.
- 21:00 orchestrator(pm) — **헬스 전 항목 정상, 낮 동안 대국 8판·492수(세션 33)·트레이스백 0·신규 버그 0, #513·#514 진행 중이라 재기동 부적기 — 09:00의 "재기동 적기" 판단 철회, AP 3건 대기 유지.** 헬스체크 — `/api/health` 200 `{db:true, katago_alive:false, migrations {current:0020, head:0020, pending:false}}`(`katago_alive:false`는 정보용 — `baduk-katago-3.err`에 20:57:30 KST 13路 spawn·`Initializing board` 실측, 매 사용 후 재spawn 정상), web 200, 공개 `inkbaduk.com` 200·`/api/health` 200, `com.baduk.api` PID 5743(9/12 13:46 기동 유지, 199시간)·`com.baduk.web` PID 89541(9/8 23:56 기동 유지), staging 중단(정책상 정상), 디스크 8%, plist drift 없음(exit 0), Claude 인증 `ok D-12`(만료 10-02 23:41). 백업 — 09:00과 동일 파일 `baduk-20260920T040005.db.gz` 4,680,093B 재검증, `find -mtime +1` 출력 없음, daily 14 / weekly 8 / monthly 4, 복원 드릴 `integrity_check` ok·테이블 11 = **86회 연속** 통과. 버그 스캔 — `.err` 235,520 → 235,658줄(**+138**, mtime 20:57:34) **전부 WS `[accepted]`/`connection open`/`closed`**(#507 2·#508 1·#509 9·#510 1·#511 9·#512 14·#513 8·#514 2), 트레이스백·`database is locked`·IntegrityError·`illegal move` 0. `.log` 326,681 → 327,400줄(**+719**) 분포 200 691·201 8·204 10·401 9·**500 0·404 0·429 0**. 401 9는 `110.13.175.133`(7)·`110.8.160.121`(2)·`2001:e60:9530…`(1)의 익명 `GET /api/session` = 비로그인 방문자 정상 흐름(직후 `POST /api/analytics/hit` 204). 소스 — 127.0.0.1 SSR·헬스 408(`/api/spectate/pro/N` 372·health 159), `2001:e60:11b6…` 세션 33 대국 트래픽, `52.233.87.84`·`118.235.66.191` 등 소량. `/api/spectate/pro?[object Object]` **9회**(SSR 8 + 외부 `52.233.87.84` 1, #99 미반영 재현, 누적 684). `baduk-web.err` **+0줄**(3,506). **DB 실측** — `max(games.id)` 506 → **514**(+8, 전부 세션 33 · 7k · 흑 · 13路 · 치석 0, 07:57~20:50 KST 생성). #507 88수 **B+4.5 사용자 승**(16:16 종료), #509 94수 **B+8.5 사용자 승**(16:43), #511 62수 **B+R AI 기권**(18:08, `finished_at` 기록됨 — 사용자 기권이 아니라 #101 경로 아님), #512 122수 **W+13.5 AI 승**(19:04), **#513 96수 `active`(마지막 착수 20:50)·#514 30수 `active`(마지막 착수 20:57:31 KST, 사이클 시작 3분 전) = 진행 중**, #508·#510 0수 고아 `active`(각각 15초·26초 뒤 #509·#511 재생성, #510→#511은 6k→7k 급수 변경 — 500 없음·치석 0이라 #97과 무관, 이슈 범위 밖 부수 사항). 12시간 착수 **492수**, 최근 5분 착수 2. `visit_hits` 655(+10/12h), `sessions` 8(−1, 11:46 UTC `session_purge` 1건 정상), `pro_games` 4,511(변동 없음). #506 `active` 127수 미종료 유지(9/19). 중복 차단 — 후보 에러 0건이라 신규 이슈 없음("신규 버그 없음"). 백로그 트리아지 — 미분류 이슈 0건. PR 감시 — 열린 PR **3건** [#98](https://github.com/rarebirds-svg/quite-baduk/pull/98)(112.5h)·[#100](https://github.com/rarebirds-svg/quite-baduk/pull/100)(88h)·[#102](https://github.com/rarebirds-svg/quite-baduk/pull/102)(16.5h) 모두 CI 4잡 SUCCESS·`MERGEABLE`/`CLEAN`·`merge-tree` 충돌 0(main보다 11·9·2커밋 뒤, ops 문서·콘텐츠뿐), 브랜치 로컬=origin(`056ca3b`·`4d77d1c`·`4d56c67`), 정체(7일)·충돌·CI 실패 0건 — "열린 PR 모두 정상". `main` CI 최근 3런 success(최근 9/20 09:04 KST `1ee32f7`). **잡 상태 4/4 `ok`**(09:00 이후 변동 없음 — dev-cycle 04:34·news-hook 05:04·analytics-weekly 09:01·content-draft 9/19 02:03). sitemap `<loc>` 4,592·글로서리 35편·FAQ 14편(변동 없음). 배포 갭 0(`HEAD`=`origin/main`=`1ee32f7`, 9/12 재기동 이후 코드 커밋 없음, BUILD_ID 9/8 유지). watchdog 낮 `신규 incident 0 건`, `[api] OK [web] OK`. 승인 — **AP-20260916-01** 대기 유지(108시간째 · 9회 재확인), **AP-20260917-01** 대기 유지(84시간째 · 7회 재확인), **AP-20260920-01** 대기 유지(12시간째 · 1회 재확인). 세 건 CI·충돌 재확인 통과. **재기동 적기 판단 철회** — 09:00엔 21시간 무착수였으나 낮 07:00~21:00 내내 세션 33이 대국했고 #514가 3분 전 착수라 지금 api 재기동은 WS 단절 위험. 실행 시 절차 (4) 최근 5분 착수 확인 필수, 심야·새벽 회차 권장. web 단독 재기동(AP-20260917-01)은 대국 WS와 무관하나 한 회차 묶음 권장은 유지.
- 21:04 orchestrator(pm) — pm 다이제스트 발송 완료(`send.py` exit 0, 마커 `~/.ops-report/markers/inkbaduk-pm-2026-09-20` 생성). 전체 ✅, 승인 카드 3장(AP-20260916-01 108시간째·AP-20260917-01 84시간째·AP-20260920-01 12시간째) 동봉, 승인 행에 "#514 진행 중, 지금 재기동 부적기" 명시. 상태 커밋 `6badb23` main push.

## 23:04~23:07 승인 3건 일괄 발효 (사람 요청 온디맨드 세션)

- #98·#102 머지 → pull → api 재기동(23:05:28, health 200/4s). #100 머지 → pull → web 빌드(BUILD_ID `SexzBOHA9_TcN4PxtrauS`) → web 재기동(23:06:25).
- 재기동 창: 마지막 착수 21:11 KST, WS 접속 0. 트레이스백 0.
- #99 검증: SSR 질의 정상. 액세스 로그 `[object Object]` 외부 IP 2건은 옛 번들 클라이언트 — 회귀 아님.
- 승인 큐 0건. 상세는 `pending-approvals.md`.
- 23:1x AP-20260920-02 등재 — #101 잔존 99건 `finished_at` 백필(prod DB 직접 쓰기 🟡). 사본 드라이런 통과, SQL `ops/sql/2026-09-20-backfill-resigned-finished-at.sql`. 재기동 불필요.
