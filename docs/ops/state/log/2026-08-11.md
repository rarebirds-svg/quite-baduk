# 2026-08-11

- 02:00 dev-cycle: **처리할 버그 없음 — 열린 이슈 0건.** `gh issue list --state open` 길이 **0**으로 `bug`·`small` 적격 이슈가 존재하지 않는다. 선점·워크트리·구현·PR 단계 전부 미실행이며 커밋 0건이다.
  - 빈 결과가 조회 실패가 아님을 확인했다 — `gh auth status` 로그인 정상(`rarebirds-svg`, scopes `gist,read:org,repo,workflow`), `gh repo view` = `rarebirds-svg/quite-baduk`, `--state all` 조회는 #78·#76·#74·#70·… 정상 반환. 즉 0건은 **인증·리포 해석 실패가 아니라 실제 백로그 고갈**이다.
  - 열린 PR도 **0건**이라 이번 사이클이 사람에게 넘길 🟡(머지) 항목도 생기지 않았다. 8/8 06:47 #79 머지 이후 나흘째 신규 PR 없음.
  - 무작업 4일 연속이다(8/8~8/11). 백로그가 빈 것은 8/10 오케스트레이터 두 사이클이 "열린 이슈 0건·미분류 0건"으로 이미 확인한 상태와 정합한다.
  - `ops/report-job-status.sh dev-cycle ok "열린 이슈 0건 — 무작업"` 기록. 09:00 다이제스트가 합산한다.

- 09:00 오케스트레이터 사이클(`am`): 러닝북 5종 전부 수행, **전 항목 정상 · 신규 incident 0건 · 대기 승인 0건**.
  - **헬스체크 — prod 정상.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web :3000 200. `launchctl`에서 `com.baduk.api` PID **88640**, `com.baduk.web` PID **88644** 둘 다 가동. staging은 backend·web 모두 `중단됨`이나 상시 가동이 필수가 아니라 **중단으로만 기록**하며 경보가 아니다. 디스크 사용률 **6%**(임계 90% 대비 여유). `ops/sync-launchd.sh --check` **exit 0 · 무출력**으로 plist drift 없음. sleep/wake 진단은 watchdog가 stale을 잡지 않아 생략했다.
    - `katago_alive:true`는 정보용 필드로만 읽었다(러닝북 명시 — 이 필드 단독으로 incident·재시작 제안 금지).
    - 최초 `ops/stack.sh`·`ops/sync-launchd.sh` 호출이 `no such file or directory`로 실패했으나 이는 상대경로 실행 실패였고, `bash ops/...`로 재실행해 정상 결과를 얻었다. 스크립트 부재가 아님을 `ls ops/`로 확인했다 — **장애가 아니다.**
  - **백업 검증 — 정상.** 최신 `~/baduk-backups/daily/baduk-20260811T040005.db.gz`(**8/11 04:00**, 3.4MB)로 5시간 전이며 `find -mtime +1` 무출력 = 30시간 임계 미달. 티어 daily **14** · weekly **8** · monthly **3**. 복원 드릴 실행 — `PRAGMA integrity_check` = **`ok`**, 테이블 수 **9**. 드릴 파일은 삭제했다.
  - **버그 스캔 — 신규 0건.** `~/Library/Logs/baduk-api.err`의 **mtime이 여전히 8/8 15:48**이다 = 재기동 이후 **약 65시간 신규 트레이스백 0건**. 최근 3,000줄의 `Traceback` 32건은 전부 그 mtime 이전(=8/8 재기동 전) 구간이라 후보가 아니다. 누적 카운트가 아니라 최근성으로 판정했다(`[[bug-scan-verify-recency]]`).
    - **액세스 로그까지 넓혀 확인**했다(`[[bug-scan-widen-to-access-log]]`). 최근 5,000줄 상태코드 분포는 200 **3,691** / 401 **1,113** / 404 **122** / 204 **68** / 201 **3**으로 **5xx 0건**이다.
    - **어드민 401 밴드는 이번에도 옛 구간에 갇혀 있다 — #78 무재발 5일째.** 4개 어드민 엔드포인트가 262회씩 균등(합 **1,048건**)이지만, 실제 라인 위치를 실측하니 **231,469~232,613행**에 갇혀 있고 파일 총 길이는 **234,959행**이다. 즉 **가장 최근 2,346줄에는 어드민 401이 0건**이다. 이 밴드가 창에 남아 보이는 것은 tail 창이 5,000줄이라서일 뿐, 새 발생이 아니다.
    - 404 **122건**은 `/api/.env`·`/api/v1/secrets`·`/api/v2/phpinfo.php`·`/api/exec` 등 **인터넷 스캐너 프로빙**이다(각 1~7건, 존재하지 않는 경로). 애플리케이션 버그가 아니라 정상 거절이라 이슈화하지 않는다. 잔여 401 63건은 `GET /api/session`(비로그인 방문자·크롤러) 정상 거절이다.
    - DB 실측 — `games` `max(id)` **337**, `moves` `max(played_at)` **2026-08-06 13:04:14**(UTC). 로그 tail 2,346줄에 게임 경로 요청이 하나도 없는 것과 정합한다(`[[in-progress-game-detection]]`, `[[access-log-recency-via-db]]`). 진행 중 대국 0건.
  - **백로그 트리아지 — 대상 없음.** `gh issue list --state open` **0건**, `--search "no:label"` **빈 배열**. 분류한 이슈 0건, 사람 분류 필요 0건. 8/9부터 이어진 백로그 공백이 계속되며, 버그 스캔이 후보를 못 찾는 근거(트레이스백 0 · 5xx 0 · #78 무재발)가 위에 있으므로 이 공백은 **자동화 장애가 아니라 상류가 조용하다는 신호**다.
  - **PR 감시 — 열린 PR 0건.** CI 실패·충돌·정체 전부 해당 없음. 8/8 06:47 #79 머지 이후 나흘째 신규 PR이 없다.
  - **자동화 잡 — 보고된 1건 `ok`.** `state/jobs/dev-cycle.json` = `{"status":"ok","summary":"열린 이슈 0건 — 무작업","at":"2026-08-11T02:00:47+09:00"}`. `content-draft`(`Weekday 3·6` 02:00 → 다음 **8/12 수**)·`analytics-weekly`(`Weekday 0` 09:00 → 다음 **8/16 일**)는 오늘(**화**)이 실행 요일이 아니라 **파일 부재가 정상**이며 `fail`로 읽지 않는다. 신선도는 watchdog 책임이라 이 행에서 판정하지 않는다.
  - **watchdog — 신규 incident 0건.** `ROOT=/Users/daegong/projects/baduk ops/check-staleness.sh` 실행(`[[ops-script-live-default-root]]`대로 ROOT를 명시). 출력 `watchdog 검사 완료 — 신규 incident 0 건`.
  - **배포 갭 0 · 라이브 반영도 확인.** `git rev-list --count HEAD..origin/main` = **0**이고, 머지≠배포이므로(`[[merge-is-not-deploy]]`) 라이브 반영까지 교차 확인했다. prod 프로세스는 **8/8 15:48:51** 기동(api 88640 · web 88644), `web/.next/BUILD_ID` mtime도 **8/8 15:48**이다. 그 이후 들어온 커밋 5건(`c2efbf7`·`08f9bda`·`e8fc5fd`·`9176f21`·`166cb66`)의 변경 파일을 전수 확인하니 **`docs/ops/**` · `ops/*.sh` · `web/tests/admin-auth-expiry.test.tsx`뿐**으로, `web/app`·`web/components`·`backend/app` 런타임 코드는 하나도 없다. 즉 **재빌드·재기동이 필요한 유입이 0건**이라 3일째 무재기동은 갭이 아니라 정상이며, `deploy` 행은 `ok`다.
  - **대기 승인 0건.** `state/pending-approvals.md` "대기 중" = `(없음)`. AP-20260808-01·AP-20260805-01이 8/8에 해소된 이후 신규 🟡 제안이 없다 — 이번 사이클에도 승인이 필요한 액션을 수행하지 않았다.

- 21:00 오케스트레이터 사이클(`pm`): 러닝북 5종 전부 수행, **전 항목 정상 · 신규 incident 0건 · 대기 승인 0건**. 이번 사이클의 핵심은 **5일간 0건이던 대국 활동이 재개됐고, 그 첫 대국이 무결하게 끝났다는 실증**이다.
  - **🆕 대국 활동 재개 — 게임 #338이 103수로 정상 종료.** 09:00까지 5일째 `max(games.id)`=337에 고정돼 있던 값이 **338**로 올라갔다. `moves` 실측 `count`=**103**, 구간 `2026-08-11 11:21:53 ~ 11:45:53 UTC` = **20:21~20:45 KST 약 24분**이다(`[[in-progress-game-detection]]`). 09:00 사이클이 "장애가 아니라 유입 신호"로 남긴 관찰이 인프라 쪽 원인이 아니었음을 사용자 유입이 직접 확인해 줬다.
    - **품질도 무결하다.** 최근 3,000줄에서 `/api/games*` 요청은 **200 15건 + 201 1건**으로 4xx·5xx가 하나도 없고, 그중 `POST /api/games/N/hint`가 **14건 전부 200**이다. 즉 대국 생성·착수·힌트(=KataGo 응수) 전 경로가 실사용 부하에서 정상 동작했다.
    - **`database is locked` 재발 0건 — 8/8 배포 이후 첫 실사용 검증이다.** 마지막 발생은 `.err` **225,347행**인데 현 기동 마커(`Started server process [88640]`)가 **225,423행**이라 전부 기동 *이전* 구간이다. 종전 사이클들이 "8/6 이후 대국 0건이라 알리바이가 강해진 게 아니다"라고 유보해 둔 판정에, 이번에 **103수 + 힌트 14회라는 실제 쓰기 부하**가 붙었는데도 락 에러가 0건이다(`[[database-locked-genmove]]`). 해소 확정으로 읽기엔 1국이라 표본이 얇지만, 유보 사유였던 "부하 부재"는 사라졌다.
  - **헬스체크 — prod 정상.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web :3000 200. `launchctl` `com.baduk.api` PID **88640** / `com.baduk.web` PID **88644** 둘 다 가동, `lstart` = `Aug 8 15:48:51` = **77시간 무재기동**. staging은 backend·web 모두 `중단됨`이나 상시 가동 필수가 아니라 **중단으로만 기록**(경보 아님). 디스크 **6%**(임계 90%). `ops/sync-launchd.sh --check` **exit 0 · 무출력** = plist drift 없음.
    - `katago_alive:false`는 러닝북 명시대로 **정보용**이라 단독으로 incident·재시작 사유가 아니다. 오히려 이번엔 반증이 명확하다 — 15분 전(20:45)까지 힌트 14건이 전부 200으로 응답했으므로 어댑터는 필요할 때 정상 동작했고, 지금 `false`인 것은 호출이 끊긴 뒤의 lazy 상태다. `false` 단독 경보가 왜 오탐인지를 보여주는 교과서적 사례다.
    - sleep/wake 진단은 watchdog가 잡 stale을 잡지 않아 생략했다.
  - **백업 검증 — 정상.** 최신 `~/baduk-backups/daily/baduk-20260811T040005.db.gz`(**8/11 04:00**, **3,406,435B**)로 17시간 전이며 `find -mtime +1` 무출력 = 30시간 임계 미달. 티어 daily **14** · weekly **8** · monthly **3**(daily 0 아님). 복원 드릴 `PRAGMA integrity_check` = **`ok`**, 테이블 수 **9**로 **12회 연속** 통과. 드릴 파일은 삭제했다. 다음 백업 8/12 04:00.
    - 이번 백업(04:00)은 게임 #338(20:21~)보다 **앞서므로** 백업본에는 338이 없다. 정상이며 다음 8/12 04:00 백업에 포함된다.
  - **버그 스캔 — 신규 0건.** `~/Library/Logs/baduk-api.err` mtime이 09:00의 `8/8 15:48`에서 **`8/11 20:46`으로 갱신**됐으나, **갱신분은 에러가 아니다** — 현 기동 마커(225,423행) 이후 파일 끝(225,429행)까지 **6줄뿐이고 전부 게임 #338의 WS accept/open/close INFO**다. mtime 갱신을 그대로 "신규 에러"로 읽지 않고 기동 마커 기준으로 잘라 확인했다(`[[bug-scan-verify-recency]]`, `[[api-log-stdout-vs-stderr]]`).
    - **액세스 로그까지 넓혀 확인**했다(`[[bug-scan-widen-to-access-log]]`). 최근 5,000줄 분포는 200 **3,679** / 401 1,118 / 404 122 / 204 73 / 201 5이고, 최근 3,000줄 **5xx 0건**이다.
    - **✅ #78 무재발 5일째 — 이번엔 "없음"이 아니라 "정상 200"으로 확인됐다.** 최근 **2,000줄에 401이 단 1건도 없고**, `.log` 최말단(235,229~235,230행 / 총 235,236행)의 어드민 요청 `GET /api/admin/sessions`·`GET /api/admin/games`가 **둘 다 200 OK**다. 종전 사이클들은 "밴드가 옛 라인에 갇혀 있다"는 부재 증명이었는데, 이번에는 **어드민 세션이 살아서 200을 받고 있다**는 존재 증명이 더해졌다.
    - 404 **122건**은 종전과 동일한 크리덴셜 스캐너 스윕이다 — `POST /api/v1/auto_login` 7 · `GET /api/.env` 6 · `POST /api`·`/api/run`·`/api/exec` · `/api/{env,settings,config,config.json}` 등 존재하지 않는 경로 탐침으로 전량 404다. 애플리케이션 버그가 아니라 정상 거절이라 이슈화하지 않는다.
    - web `.err`는 mtime `8/8 15:50` **불변**이다. `Failed to find Server Action` 마지막 발생이 **3,097~3,119행**이고 현 웹 프로세스(`node:88656`)의 첫 라인이 **3,130행**이라 전량 재기동 *이전* 구간으로, 8/8 승인 아카이브의 판정과 같다.
  - **백로그 트리아지 — 대상 없음.** `gh issue list --state open` **0건**, `--search "no:label"` **빈 배열**. 분류한 이슈 0건, 사람 분류 필요 0건.
  - **PR 감시 — 열린 PR 0건.** CI 실패·충돌·정체 전부 해당 없음. 8/8 06:47 #79 머지 이후 닷새째 신규 PR이 없다.
  - **자동화 잡 — 보고된 1건 `ok`.** `state/jobs/dev-cycle.json` = `{"status":"ok","summary":"열린 이슈 0건 — 무작업","at":"2026-08-11T02:00:47+09:00"}`. `content-draft`(`Weekday 3·6` 02:00 → 다음 **8/12 수**)·`analytics-weekly`(`Weekday 0` 09:00 → 다음 **8/16 일**)는 오늘(**화**)이 실행 요일이 아니라 **파일 부재가 정상**이며 `fail`로 읽지 않는다. 신선도는 watchdog 책임이라 이 행에서 판정하지 않는다.
  - **배포 갭 0 · 라이브 반영 확인.** `HEAD..origin/main` **0** / `origin/main..HEAD` **0**. 머지≠배포이므로(`[[merge-is-not-deploy]]`) 라이브까지 교차 확인했다 — 8/8 15:48 재기동 이후 유입 커밋 중 09:00 이후 신규는 `d6e8f92` 하나이고 변경 파일이 `docs/ops/state/**`뿐이다. 재기동 이후 전체 유입을 통틀어도 `docs/**`·`ops/*.sh`·`web/tests/**`뿐으로 **`backend/app/**`·`web/{app,components,lib,store}/**` 런타임 코드 변경 0건**이라, `BUILD_ID` mtime `Aug 8 15:48` 고정과 77시간 무재기동은 갭이 아니라 정상이다. `deploy` 행은 `ok`.
  - **⚠️ watchdog — inkbaduk은 정상이나 popory `am` 다이제스트 미발송 경보가 하루 종일 반복됐다.** `watchdog.out.log`가 `신규 incident 1 건`을 연속 출력해 파고들었더니, 잡 stale이 아니라 **`ops/check-staleness.sh`의 다이제스트 마커 검사**가 원인이었다. 마커 디렉터리 실측 결과 `inkbaduk-am-2026-08-11`(09:03)·`popory-pm-2026-08-11`(21:00)은 있는데 **`popory-am-2026-08-11`이 없다** — 즉 **inkbaduk 쪽 미발송이 아니라 sibling 프로젝트 popory의 오늘 오전 다이제스트가 빠진 것**이다. 이 경보는 `incidents.md`에 append하지 않고 카운터만 올리므로 8월 항목이 0인 것과도 정합한다(`grep "2026-08" incidents.md` 무출력).
    - 09:00 사이클이 `신규 incident 0 건`을 본 것은 오전 슬롯 +30분(09:30) 이전이라 검사 자체가 skip됐기 때문이다 — 판정이 틀렸던 게 아니라 시점 문제다.
    - popory는 이 오케스트레이터의 범위 밖이고 `popory-pm`은 21:00에 정상 생성됐으므로 오늘 오전 1회성으로 보인다. **inkbaduk incident로 올리지 않고 관찰로만 남긴다.**
    - **🔎 미커밋 변경 1건을 발견해 그대로 뒀다.** `ops/check-staleness.sh`에 `DIGEST_COOLDOWN_SECS=21600`(6h)을 도입해 다이제스트 미발송 경보에만 긴 쿨다운을 적용하는 편집이 있고, 파일 mtime이 **21:00 정각**이다. 이 세션이 저작한 것이 아니며(작업 시작 시점의 `git status`에 이미 있었다) 같은 시각에 도는 popory 사이클의 작업으로 보인다. 편집 자체는 자기완결적이고 의도도 타당하다 — watchdog 주기(1h)와 쿨다운(1h)이 같으면 매 실행마다 쿨다운이 갓 만료돼 억제가 무효가 되며, 실제로 오늘 그 형태로 시간당 경보가 반복됐다. **다른 세션의 저작물을 대신 커밋하지 않는다는 판단으로 미커밋 상태를 유지했고, 이 커밋에도 포함하지 않았다.** 다음 사이클에 여전히 미커밋이면 사람에게 확인을 받는다.
    - `ROOT=/Users/daegong/projects/baduk bash ops/check-staleness.sh` 수동 실행 결과 `[digest-popory-am] 미발송이지만 cooldown — skip notify` → **`신규 incident 0 건`**으로, 새 6h 쿨다운이 실제로 억제하는 것까지 확인했다. ROOT는 명시했다(`[[ops-script-live-default-root]]`).
  - **대기 승인 0건.** `state/pending-approvals.md` "대기 중" = `(없음)`. 이번 사이클에도 승인이 필요한 액션을 수행하지 않았다.
