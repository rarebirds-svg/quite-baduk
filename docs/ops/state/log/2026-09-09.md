# 2026-09-09

- 02:00 content-draft — glossary `chaksu-geumji`(착수금지) 초안 생성 → korean-copy-qa 통과(치명 0, 수정 권장 5건 전부 반영 — 자기 돌 "잡히는" 동사 방향·"반칙"→"둘 수 없는 자리"·옥집 사례 일반화 완화·자충 문장 단순화·seoTitle 중의성 제거, related 4개 실존 확인) → main 게시·push(b0e7630)·IndexNow 200·라이브 200. 선택 근거: glossary 22/57(0.386) < faq 10/24(0.417). 잡 상태 `ok` 기록(state/jobs/content-draft.json).
- 04:30 dev-cycle — **처리할 버그 없음**(2일 연속). 열린 `bug` 이슈 3건(#81·#84·#87)은 모두 PR이 열려 사람 머지 대기 중(#81 → PR [#83](https://github.com/rarebirds-svg/quite-baduk/pull/83) `fix/issue-81` 3490f28, #84 → PR [#86](https://github.com/rarebirds-svg/quite-baduk/pull/86) `fix/issue-84` cb4450a, #87 → PR [#95](https://github.com/rarebirds-svg/quite-baduk/pull/95) `fix/issue-87` 26ddff7). 세 PR 모두 CI 4잡 SUCCESS, GitHub `mergeable=MERGEABLE/CLEAN`, `git merge-tree`로 origin/main 대비 충돌 없음, 임시 워크트리에서 #83→#86→#95 순차 실머지도 충돌 없음 재확인. 이슈에 완료 코멘트 존재, `in-progress` 잔존 없음. `.worktrees/dev-cycle`은 `fix/issue-87` 26ddff7 = `origin/fix/issue-87`로 미커밋·미push 잔여 없음. 재선택하면 중복 작업이므로 착수하지 않음. 코드 변경 0, 잡 상태 `ok` 보고. 머지는 🟡 사람 승인 대기 유지 — #83·#86은 9/2·9/4 생성 후 5~7일째 미회신.

- 09:00 오케스트레이터 (am) — **OPS-20260907-01 해소 확인 — 9/8 23:55 사람 배포(#96)로 마이그레이션 0020 적용, 착수 재개. prod 정상 복귀.**
  - 장애 해소 경위: 사람이 9/8 23:52 PR [#96](https://github.com/rarebirds-svg/quite-baduk/pull/96)(`fix(ops)`: 기동 시 자동 `alembic upgrade head`·`/api/health`에 `migrations` 리비전 노출) 머지 → 23:55:44 prod 트리 `git pull`(reflog `7794a8f`) → `com.baduk.api` 재기동(PID 89132, `.err`에 `Running upgrade 0019 -> 0020`) → web 재빌드·재기동(PID 89541, BUILD_ID 23:56). AP-20260907-01 승인 회신 없이 직접 실행 — 제안 후 약 39시간, 장애 총 약 48시간(9/7 00:06 ~ 9/8 23:55).
  - 헬스체크: backend `/api/health` 200 `{"db":true,"katago_alive":false,"migrations":{"current":"0020","head":"0020","pending":false}}`(#96 신규 필드로 리비전 갭이 헬스에 직접 보인다)·web 200, `com.baduk.api` PID 89132·`com.baduk.web` PID 89541. staging 중단(정책상 정상). 디스크 7%. plist drift 없음(exit 0). Claude 인증 **D-23**(만료 10-02 23:41). `alembic_version` **0020** = head. 직접 프로브 `GET /api/spectate` **200**(9/7 09:00 이후 5회 연속 500이었음).
  - 백업 검증: 최신 `baduk-20260909T040005.db.gz`(4,433,818B, **+66,509B** — 착수 재개 반영) 신선, daily 14 / weekly 8 / monthly 4, 복원 드릴 integrity_check **ok**·테이블 **11**(`accounts` 추가)·사본 `alembic_version` **0020** = **67회 연속** 통과. 마이그레이션 적용 후 첫 백업.
  - 버그 스캔: `.err` 234,919줄(+1,384), 신규 트레이스백 **8건 — 전부 복구 전**(`sessions.account_id` SELECT 5·INSERT 3, IP `1.237.136.170` 1개). `Running upgrade 0019 -> 0020` 라인(234,906) 이후 14줄은 startup·WS accepted뿐, **트레이스백 0**. `.log` 309,099줄(+18,511) 상태 분포 200 18,474 / 204 18 / 500 8 / 401 5 / 201 5, 429 0. 500 8건은 전부 같은 IP의 `/api/session`(GET 5·POST 3)이고 신규 구간 초반(290,741행 이전)에 몰려 있어 재기동 전. **복구 후 500 0건.** 200 대부분은 `127.0.0.1` 발 프로 기보 SSR. web `.err` 3,418(+35) 중 `Failed to find Server Action` 3 — 23:56 재빌드 후 stale 클라이언트 재생(기준선, 이슈 아님). 신규 버그 이슈 0건 — 8건은 해소된 OPS-20260907-01 증상.
  - DB 실측: `max(games.id)` **456(+3)**, 복구 후(9/8 14:55 UTC 이후) **110수·대국 2건**, 마지막 수 `2026-09-08 18:32:40 UTC` = **9/9 03:32 KST** — 9/6 23:18 이후 52시간 만에 착수 재개. `POST /api/session` 201 2건(세션 발급 정상화). `visit_hits` 493(**+17/12h**, 마지막 9/9 08:24 KST).
  - 백로그 트리아지: 미분류 0건. 열린 이슈 3건(#87·#84·#81, 전부 `bug`) 변동 없음, 전부 PR 머지 대기.
  - PR 감시: 열린 PR 3건 — [#95](https://github.com/rarebirds-svg/quite-baduk/pull/95)·[#86](https://github.com/rarebirds-svg/quite-baduk/pull/86)·[#83](https://github.com/rarebirds-svg/quite-baduk/pull/83) 전부 CI 4잡 SUCCESS·`MERGEABLE`. 7일 정체 0건(#83은 내일 9/10 04:46 전환). **주의 PR 0건.** main CI 최신 런 `b0e7630`·`7794a8f`(#96) **success**.
  - 잡 상태: `state/jobs/*.json` 4/4 `ok` — dev-cycle 9/9 04:31(처리할 버그 없음 2일 연속)·content-draft 9/9 02:04(`chaksu-geumji`)·analytics-weekly 9/6·news-hook 9/6. 배포 갭 `HEAD..origin/main` **0**, 라이브 = HEAD `b0e7630`(콘텐츠 커밋, force-dynamic이라 재빌드 불필요), 코드 최신 `7794a8f`는 23:55 api 기동·23:56 BUILD_ID로 반영 확인 — **미반영 없음.**
  - 장애 대응(incident.md): OPS-20260907-01 **해소**로 incidents.md 제목·해소 줄 갱신. 재발 방지 후보 (1) 헬스체크 리비전 비교·(2) 기동 시 자동 upgrade는 #96으로 반영, (3) 5xx watchdog 신호는 미착수(백로그 후보로만 기록, 이슈 미생성).
  - 승인 큐: AP-20260907-01 → 처리 완료(사람 직접 실행)로 이동. 남은 3건 재확인 — AP-20260907-02 48시간째·4회(#95, 재기동 불필요·9/13 03:00 ingest 전 발효 마감), AP-20260903-01 144시간째·11회(#83), AP-20260905-01 96시간째·8회(#86). 착수가 재개됐으므로 #83·#86 재기동 절차의 진행 중 대국 재확인이 다시 필수. 권장 순서 #83 → #86 → #95 머지·pull → api 재기동 1회.
  - 다이제스트: `slot: am`, service `ok`·jobs `ok`·deploy `ok`·anomaly `ok`(트레이스백 8건은 복구 전, 복구 후 0), approval `hold` 3건 카드 동봉. `kind: alert` 없음(fail 없음). 발송 결과는 아래 발송 기록 참조.
  - 발송 기록: 09:05 am 다이제스트(전체 ✅) + 승인 카드 3건 exit 0, 마커 `~/.ops-report/markers/inkbaduk-am-2026-09-09` 생성 확인. 페이로드는 `/tmp/ops-payload/digest-20260909-am.json`에 쓰고 `send.py`를 직접 호출해 1회 성공. `kind: alert`는 fail 항목이 없어 미발송. 상태 파일은 이 사이클 커밋으로 main에 push.
