# 2026-08-05

- 02:00 content-draft: **처리할 토픽 없음 — 글로서리·FAQ 시드 전량 게시 완료. 초안·게시 0건.**
  - **커버리지 — glossary 18/18, faq 9/9.** `seed-glossary.yml` 18개 슬러그를 `web/content/glossary/`(27개 파일) 대조, `seed-faq.yml` 9개를 `web/content/faq/`(12개 파일) 대조. `docs/ops/content/drafts/`는 비어 있어 인계받을 미완 초안도 없다. 두 종류 모두 미작성 후보 0건이므로 러닝북 2단계 규칙대로 즉시 종료했고, 기존 항목을 임의 개정하지 않았다.
  - **유일한 파일명 미스 `kasaeng`은 오탐이다.** 시드의 `kasaeng`(사활)은 `web/content/glossary/sahwal.md`로 이미 게시돼 있다 — frontmatter `title: 사활`·`created_at: 2026-07-22`로 확인했다. 과거 slug 통합(옛 `kasaeng` → `sahwal`, 리다이렉트 308)의 잔재라 시드 쪽 슬러그만 옛 이름으로 남은 것이다. 파일 존재 여부가 아니라 frontmatter title로 판정해야 하는 사례(`[[glossary-sahwal-slug]]`). 이걸 미작성으로 읽고 `kasaeng.md`를 새로 쓰면 같은 내용의 중복 페이지가 생기고 308 리다이렉트와 충돌한다 — 게시하지 않은 이유가 이것이다.
  - **시드 자체를 고치지 않았다.** `seed-glossary.yml`의 `kasaeng`을 `sahwal`로 바꾸면 이 오탐이 영구히 사라지지만, 시드는 콘텐츠 파일이 아니라 운영 설정이라 이 사이클의 🟢 자율 범위(글로서리·FAQ 마크다운 게시) 밖이다. 다음 사이클도 같은 판정을 반복하게 되므로 사람이 한 줄 고쳐 두면 좋다 — 지금은 기록만 남긴다.
  - **게시·push·IndexNow·Telegram 전부 미실행.** 게시할 파일이 없으므로 6단계 전체를 건너뛰었다. 코드·콘텐츠 변경 0건(이 로그 파일 제외), 작업 트리 스테이징 없음.
  - **관찰 — 시드 고갈이다.** 파이프라인 고장이 아니라 18+9개 시드를 전부 소진한 상태다. 이 잡이 다시 일하려면 시드 파일에 새 토픽 항목이 추가돼야 한다. 주 2회 기동을 유지할지, 시드를 보강할지는 🟡 판단이라 여기서 하지 않는다.

- 02:00 dev-cycle: **처리할 버그 없음 — 적격 이슈 0건. 유일한 열린 이슈 #76은 이미 PR #77(CI 4/4 green)로 처리돼 사람 머지 대기 상태다. 누락돼 있던 러닝북 4단계 이슈 코멘트만 보충했다.**
  - **적격 이슈 0건.** `gh issue list --state open` = **#76 단 1건**이고 라벨이 `enhancement`뿐이라 선택 기준(`bug` 또는 `small` 보유)에 미달한다. 8/1~8/4의 "열린 이슈 자체가 0건"과는 다른 상황이다 — 큐에 항목은 있으나 **이미 소비된 항목**이다.
  - **#76은 이번 사이클이 착수할 대상이 아니었다 — 08-04 사이클이 끝냈다.** `.worktrees/issue-76`가 `fix/issue-76` @ **9f115bb**로 존재하고, `git ls-remote --heads origin fix/issue-76` = 같은 SHA로 **이미 push됨**, [PR #77](https://github.com/rarebirds-svg/quite-baduk/pull/77) **OPEN**이다. 즉 `[[dev-cycle-unpushed-branches]]`가 기록한 "로컬에만 두고 push 누락" 패턴의 재발이 **아니다** — push·PR까지 정상 완료돼 있었다.
    - **CI는 전부 통과했다.** `statusCheckRollup` = backend **SUCCESS** · frontend **SUCCESS** · app-shell-build **SUCCESS** · e2e **SUCCESS**, `mergeable` = `MERGEABLE` / `mergeStateStatus` = `CLEAN`. e2e까지 green이므로 `[[ci-e2e-disabled]]`의 "이제 ❌는 그 PR이 깨뜨린 신호" 판정 기준에서도 문제 없음.
    - **변경 범위 6파일 / +82 −5.** `web/lib/formatResult.ts`(신규) · `web/tests/lib/formatResult.test.ts`(신규) · `app/history/page.tsx` · `components/screens/GamePlayScreen.tsx` · `lib/i18n/{ko,en}.json`. 이슈가 경고한 i18n 키 충돌(`game.result`가 main에서 문자열이라 하위 중첩 시 `t()`가 키를 그대로 반환)은 새 키를 **`game.resultText.*`** 별도 네임스페이스에 두는 방식으로 회피했고 기존 `game.result` 사용처는 미변경이다.
  - **🟢 보충한 것 — 이슈 코멘트 1건.** 러닝북 3단계 말미가 요구하는 "이슈에 브랜치명·커밋 SHA·PR 링크 코멘트"가 #76에 **누락**돼 있었다(코멘트 0건). 브랜치·SHA·PR·CI 상태·i18n 처리 방식을 담아 [issuecomment-5182191818](https://github.com/rarebirds-svg/quite-baduk/issues/76#issuecomment-5182191818)로 남겼다. 이게 없으면 이슈만 보는 사람이 "미착수"로 오인해 중복 착수할 수 있다. `in-progress` 라벨은 붙어 있지 않아(`gh issue list --state all --label in-progress` = `[]`) 뗄 것도 없었다.
  - **라벨을 고치지 않았다.** #76의 실제 규모(유틸 1개 + 테스트 + 호출부 3곳 + i18n 키)는 `small`에 가깝고 라벨링은 🟢지만, 이미 PR까지 나가 머지되면 자동 close될 항목이라 라벨 변경은 순수 잡음이다. 다만 **다음에 비슷한 크기의 이슈를 열 때 `small`을 붙이면** dev-cycle이 라벨 필터에서 걸러내지 않고 바로 집어간다 — 이번엔 `enhancement`만 붙어 있어 기준상 부적격이었다.
  - **코드 변경 0건.** 이 로그 파일과 GitHub 이슈 코멘트 1건이 전부다. worktree에서 구현·커밋한 것 없음. **머지는 하지 않았다**(🟡 — PR #77은 사람 대기).
  - **배포 갭 없음.** `git rev-list --count HEAD..origin/main` = **0**, `HEAD` = `origin/main` = **13eb069**. #75(웹 `output:"standalone"` 제거) 머지분까지 작업 트리에 반영돼 있다. 다만 이 커밋은 `next.config.js`를 바꾸므로 **prod 웹 재빌드·재기동 전까지는 라이브에 효과가 없다**(`[[merge-is-not-deploy]]`) — 재기동은 🟡라 여기서 하지 않는다. 보류 승인 0건.
  - **⚠️ 발견 — `.worktrees/issue-76/web/node_modules`가 심링크라 `.gitignore`를 빠져나간다.** 08-04 사이클이 메인 트리 `web/node_modules`로 건 심링크(`Aug 4 22:31`)인데, `.gitignore:12`의 패턴이 `node_modules/`로 **슬래시 종결**이라 디렉터리에만 매치하고 심링크는 매치하지 않는다 — `git status`에 `?? web/node_modules`로 뜬다. 그 워크트리에서 누가 `git add -A`를 하면 심링크가 커밋된다. 스크래치 워크트리라 지금 당장의 위험은 낮고, 남의 셋업을 지우거나 `.gitignore`를 고치는 것(코드·설정 변경 → PR 경로)은 이 사이클 범위 밖이라 **기록만 남긴다**. 사람이 정리한다면 심링크 삭제 또는 `.gitignore`에 `node_modules` 한 줄 추가 중 하나면 된다.
  - **Telegram 발송 완료 — message_id 2510.** `ToolSearch`로 실제 조회했으나 `reply` 도구가 이 세션에 미노출이라(`SendMessage`는 에이전트 간 통신용) Bot API 직접 호출로 발송했다. 한글 본문을 `/tmp` 파일에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"`로 보냈다(`[[dev-cycle-telegram-botapi]]`). 토큰은 `~/.claude/channels/telegram/.env`, chat_id는 `access.json`의 `allowFrom`에서 읽었다. 환경 자가진단으로 "발송 불가"를 사전 판정하지 않았다.
  - **관찰 — 4일 만에 큐가 실제로 돌았다.** 8/1~8/4는 이슈 유입 0건이었는데, 8/4 12:00 오케스트레이터가 웹 로그로 스캔 범위를 넓혀 #74를 만들었고 그게 #75로 머지됐으며, 브랜치 정리 중 발견된 #76도 같은 날 PR까지 갔다. 유입원(오케스트레이터 버그 스캔)이 다시 작동하면 dev-cycle도 따라 작동한다는 뜻이다 — 무작업 로그가 이어질 때 고쳐야 할 곳은 이 잡이 아니라 유입원이다.

## 12:00 오케스트레이터

5개 러닝북 전부 수행. **서비스는 무사고이나, 지표가 가려 놓은 배포 갭 1건을 찾아 AP-20260805-01로 제안했다.**

- **헬스체크 — prod 정상.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web 200. PID api **93501** / web **93522**가 6사이클 연속 동일 = 8/2 22:11 배포 이후 **62시간 무재기동**. 디스크 **6%**, plist drift 없음(`sync-launchd.sh --check` exit 0·무출력), staging 중단 = 정책상 정상. watchdog `신규 incident 0건`·`[api] OK`·`[web] OK`. 신규 incident 0건.

- **백업 검증 — 정상.** 최신 `baduk-20260805T040005.db.gz`(8/5 04:00, 3,282,644B ≈ 3.13MB), 생성 8시간 전이라 `-mtime +1` 미해당. 티어 **14 / 8 / 3**. 복원 드릴 `integrity_check=ok` · **9 tables**. 8/4분(3,281,828B) 대비 +816B = 주간 ingest 없는 평일의 정상 증분.

- **🔴 이번 사이클의 실질 소득 — `git rev-list --count HEAD..origin/main` = 0이 배포 갭을 가리고 있었다.** 그 지표는 *작업 트리가 origin을 따라잡았는가*만 보지 *실행 중인 프로세스가 작업 트리를 반영하는가*는 보지 않는다(`[[merge-is-not-deploy]]`). 02:00 dev-cycle 로그도 같은 지표로 "배포 갭 없음"을 적었는데, 그 시점엔 #75 한 건이었고 06:25에 #77이 추가로 들어와 지금은 **2건**이다.
  - **결정적 증거 — 빌드 산출물에 #77 코드가 없다.** `grep -rl 'resultText' web/.next/{static,server}` = **0개 파일**. #77이 추가한 `game.resultText.*` i18n 네임스페이스가 번들에 아예 없으므로 `a59b260` 미반영이 확정이다.
  - **시각 증거** — HEAD `a59b260`는 **8/5 06:25:31** `pull --ff-only`, 직전 `13eb069`는 **8/4 22:29:04** pull. 그런데 `web/.next/BUILD_ID` mtime은 **`Aug 4 22:23`**으로 두 pull보다 **먼저**다. 웹 프로세스 기동은 그보다도 이른 `Aug 2 22:11:42`.
  - **#75도 프로세스 기준 미반영** — `web/.next/standalone/` 디렉터리는 사라져 있어 22:23 빌드 자체는 이미 standalone을 안 만들지만, `baduk-web.err`의 마지막 기동 블록에는 `⚠ "next start" does not work with "output: standalone"`가 남아 있다 = 지금 떠 있는 프로세스는 여전히 standalone 설정으로 기동된 것이다.
  - **부수 위험** — 8/2 22:11 기동한 `next start`가 8/4 22:23에 통째로 갈린 `.next`를 물고 있다. Next.js는 기동 시 BUILD_ID·매니페스트를 메모리에 올리고 청크는 디스크에서 서빙하므로 **메모리(구 빌드) ↔ 디스크(신 빌드) 불일치** 상태다. 지금 `/` 200이고 5xx 0건이라 당장 터지진 않았으나 정상 배포 경로가 아니며, 재기동으로 해소된다.
  - 배포는 🟡라 직접 하지 않고 **AP-20260805-01**로 제안했다. 판별 검사 2종(#77 = `resultText` grep ≥1, #75 = 기동 경고 소멸)을 실행 절차에 넣어, 다음 사이클이 발효 여부를 추측 없이 확인할 수 있게 했다.

- **버그 스캔 — 신규 0건. 두 로그가 모두 정지 상태다.** 백엔드 `.err` **225,409줄 / mtime `Aug 2 22:11`**로 4사이클 연속 바이트 단위 동일 = **62시간 트레이스백 0건**. 현 기동 마커(line 225406) 이후 `Traceback|Error` **0건** 재확인, 마지막 `database is locked`(line 225347)는 여전히 기동 *이전* 구간이다(`[[bug-scan-verify-recency]]`). 웹 `.err`도 mtime `Aug 3 06:16` 불변 = **2일 이상 신규 0건**. `Failed to find Server Action`(ID가 `"x"` 12 · `"0000…"` 11 · 8자리 난수 다수)은 8/4 판정대로 외부 스캐너 프로브라 이슈화하지 않았다 — 중복 이슈 미생성.

- **백로그 트리아지 — 대상 0건.** `gh issue list --state open` 빈 결과, 미분류 0건. #74는 PR #75와 함께, #76은 PR #77과 함께 CLOSED됐다. 사람 분류 필요 항목 없음.

- **PR 감시 — 열린 PR 0건, 주의 PR 0건.** 2사이클 연속. main CI 최근 5런 전부 `success`. 다만 병목이 사라진 게 아니라 **PR 큐 → 배포 큐로 옮겨갔다**(위 항목).

- **트래픽** — 최근 2,000줄에 **5xx 0건**, 프로 상세 **203건**. **진행 중 대국 0건**(`WebSocket /api/ws/games` 0건, 마지막 대국 #336은 8/1 19:15 종료) = AP-20260805-01의 재기동 잔여 위험이 낮다는 근거.

- **미해결(사람 판단 대기) 유지** — 프롬프트·러닝북 5종의 주기 표기 "4회/일"과 실제 plist "12:00·18:00 2회/일" 불일치. 이번에도 자율 변경하지 않았다(`[[orchestrator-schedule-2x-not-4x]]`).

- **승인 답신 처리 — 해당 없음.** 이 세션은 launchd 정시 기동이고 인바운드 큐 최신 항목은 `~/.claude/channels/telegram/inbox/`의 **7/22 22:48**분이라 신규 답신이 없다. telegram-protocol.md "처리" 절차 대신 통상 루프를 수행했다.

- **Telegram 발송 완료 — message_id 2513.** `ToolSearch`로 실제 조회한 결과 `reply` 도구가 이 세션에 미노출이라(`SendMessage`는 에이전트 간 통신용) Bot API를 직접 호출했다. 한글 본문을 `/tmp/tg-orch-20260805-1200.txt`에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"`로 보냈다(`[[dev-cycle-telegram-botapi]]`). 토큰은 `~/.claude/channels/telegram/.env`, chat_id는 `access.json`의 `allowFrom`에서 읽었다. 환경 자가진단으로 "발송 불가"를 사전 판정하지 않았다.

- **메모리 갱신 — `[[merge-is-not-deploy]]`.** "매 사이클 `git rev-list --count HEAD..origin/main`으로 배포 갭을 확인한다"는 기존 How-to-apply가 이번 사례에서 **틀린 것으로 드러나** 수정했다. 그 지표는 0이어도 라이브가 뒤처질 수 있다. 대신 3종 실측(BUILD_ID mtime vs 마지막 pull 시각 / prod 기동 시각 vs 빌드 시각 / 빌드 산출물 신규 심볼 grep)을 함께 보도록 고쳤다. 신규 메모리를 만들지 않고 기존 항목을 갱신했다.

## 18:00 오케스트레이터

5개 러닝북 전부 수행. **서비스는 계속 무사고. 12:00에 제안한 AP-20260805-01은 6시간째 미처리이며, 판별 검사 3종을 다시 돌려 배포 갭이 그대로임을 재확인했다.**

- **헬스체크 — prod 정상.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web 200. PID api **93501** / web **93522**가 **7사이클 연속** 동일, `ps lstart` = `Sun Aug 2 22:11:37`(api) / `22:11:42`(web) = **67시간 49분 무재기동**. 디스크 **6%**, plist drift 없음(`sync-launchd.sh --check` exit 0·무출력), staging `중단됨` = 정책상 정상. `check-staleness.sh` 직접 실행 `신규 incident 0건`, watchdog 최근 3사이클 `[api] OK`·`[web] OK`. 신규 incident 0건.

- **백업 검증 — 정상.** 최신 `baduk-20260805T040005.db.gz`(8/5 04:00, **3,282,644B**), 생성 14시간 전이라 `-mtime +1` 미해당. 티어 **14 / 8 / 3**(12:00과 동일). 복원 드릴 `integrity_check=ok` · **9 tables** — 12:00에 이어 **2회 연속** 통과. 다음 백업 8/6 04:00.

- **🔴 배포 갭 미해소 — AP-20260805-01 제안 후 6시간, 판별 검사 3종 전부 12:00과 동일하다.** 중복 제안을 만들지 않고 기존 항목의 근거만 재확인했다.
  - **`grep -rl 'resultText' web/.next/{static,server}` = 여전히 0개 파일** — #77(`a59b260`)이 추가한 `game.resultText.*` 네임스페이스가 번들에 없다. 미반영 확정 유지.
  - **`web/.next/BUILD_ID` mtime = `Aug 4 22:23`** 불변 — 재빌드가 일어나지 않았다.
  - **웹 프로세스 `ps lstart` = `Sun Aug 2 22:11:42`** 불변 — 재기동이 일어나지 않았다. 즉 실행 절차 1·2단계 모두 미수행이다.
  - `git rev-list --count HEAD..origin/main` = **0**, HEAD = `a59b260`. 이 지표가 여전히 0으로 "정상"을 보고하고 있어, 이것만 보면 갭이 안 보인다는 12:00의 판정이 재확인됐다(`[[merge-is-not-deploy]]`).
  - **재기동 잔여 위험은 여전히 낮다** — 최근 2,000줄 `ws/games` **0건**, `games` 테이블 **336**으로 8/1 이후 신규 대국 0건. 진행 중 대국이 없다.

- **버그 스캔 — 신규 0건. 두 로그 모두 정지 상태 유지.** 백엔드 `.err` **225,409줄 / mtime `Aug 2 22:11`**로 **5사이클 연속** 바이트 단위 동일 = **67시간 트레이스백 0건**. 마지막 traceback은 line 225222, 마지막 `database is locked`는 line 225347로 둘 다 현 기동 마커(line 225406) *이전* 구간이다 — 누적 카운트가 아니라 발생 시점으로 판정했다(`[[bug-scan-verify-recency]]`). 웹 `.err`도 **2,975줄 / mtime `Aug 3 06:16`** 불변 = **2일 12시간 신규 0건**. 이슈 생성 0건.

- **백로그 트리아지 — 대상 0건.** `gh issue list --state open --search "no:label"` = `[]`. 열린 `bug` 이슈도 0건. 사람 분류 필요 항목 없음.

- **PR 감시 — 열린 PR 0건, 주의 PR 0건.** 3사이클 연속. CI 실패·충돌·정체 모두 해당 없음. 병목은 PR 큐가 아니라 배포 큐에 있다(위 항목).

- **트래픽 — 최근 2,000줄 5xx·404 0건.** `.log` **220,287줄**(8/4 18:00 216,841 → 24시간 +3,446). 상태코드 **200 1,996 · 401 2 · 204 2**. 구성은 `/api/admin/*` **1,867건**(summary·sessions·games·engine이 각 467·467·467·466 = 어드민 대시보드 폴링 1주기 4콜) · health 103 · 프로 상세 6 · themes 5 · sitemap 5 · 월간 픽 3 · session 2 · `analytics/hit` 2.
  - **8/2·8/3의 어드민 401 폭주와는 다른 패턴이다** — 그때는 세션 만료로 401이 580·208건 쌓였는데, 이번 1,867건은 **전부 200**이다. 재로그인 후 정상 폴링 중이라는 뜻이라 incident·이슈 미생성.
  - **프로 상세는 6건으로 급감했다.** 8/4 18:00 창의 149.5건/h 버스트가 완전히 잦아들었고, 그 사이 5xx는 0이었다. 크롤러 스윕 종료의 정상 감쇠다.

- **사람 방문 지속** — `visit_hits` **84행**(8/4 18:00 78행 → **+6**), 최신 `2026-08-05 08:48:50 UTC` = **8/5 17:48 KST**로 이 세션 12분 전이다. 델타의 `analytics/hit` 2건과 정합 = 봇 필터를 통과한 실제 사람 유입. `games` **336**(신규 대국 0건, 4일째), `pro_games` **3111**(불변).

- **콘텐츠 ingest — stale 아님.** 최근 실행 `2026-08-02 03:12`(fetched=2260 new=200 duplicate=2060 error=0). `com.inkbaduk.content-ingest.plist`를 실측하니 `StartCalendarInterval` = `Weekday 0 / Hour 3` = **주 1회 일요일 03:00**이라 평일에 "3일 전 실행"인 것이 정상이다. 다음 실행 **8/9(일)**.

- **미해결(사람 판단 대기) 유지** — 프롬프트·러닝북 5종의 주기 표기 "4회/일"과 실제 plist "12:00·18:00 2회/일" 불일치. 이번에도 자율 변경하지 않았다(`[[orchestrator-schedule-2x-not-4x]]`).

- **승인 답신 처리 — 해당 없음.** launchd 정시 기동이며 인바운드 신규 답신이 없다. 통상 루프를 수행했다.

- **Telegram 발송 완료 — message_id 2514.** `ToolSearch`로 실제 조회한 결과 `reply` 도구가 이 세션에 미노출이라(`SendMessage`는 에이전트 간 통신용) Bot API를 직접 호출했다. 한글 본문을 `/tmp/tg-orch-20260805-1800.txt`에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"`로 보냈다(`[[dev-cycle-telegram-botapi]]`). 환경 자가진단으로 "발송 불가"를 사전 판정하지 않았다.
  - **⚠️ 첫 시도가 `Bad Request: chat_id is empty`로 실패했다 — 파싱 오류였다.** `access.json`의 `allowFrom`을 dict 리스트로 가정하고 `.get('id')`를 꺼냈는데, 실제 구조는 **평문 문자열 리스트**(`{"dmPolicy":"allowlist","allowFrom":["1241277614"],...}`)다. 문자열을 그대로 `chat_id`로 넣어 재시도해 성공했다. 메모리 `[[dev-cycle-telegram-botapi]]`에 이 구조를 명시해 다음 세션이 같은 곳에서 헛돌지 않게 했다.
