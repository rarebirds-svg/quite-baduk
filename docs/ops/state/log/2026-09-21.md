# 2026-09-21

- 04:30 dev-cycle — **처리할 버그 없음.** `gh issue list --state open` 열린 이슈 0건(REST `issues?state=open` PR 제외 0건 재확인). 마지막 적격 3건(#97·#99·#101)은 9/20 23:04~23:07 사람 세션에서 PR #98·#100·#102 머지·발효로 전부 CLOSED. worktree 전환·코드 변경·커밋·PR 없음. `.worktrees/dev-cycle`은 머지 완료된 `fix/issue-101`(`4d56c67`)에 남아 있음 — 다음 적격 이슈 때 `origin/main` 기준 재전환하면 되므로 손대지 않음. 잡 상태 `ok` 기록. 로그 커밋은 관례대로 09:00 오케스트레이터가 묶어서 처리.
- 09:00 orchestrator(am) — **재기동 후 첫 사이클, 헬스 전 항목 정상 — 밤사이 무착수·트레이스백 0·신규 버그 0, 열린 이슈·PR 0건, #99 발효 검증(재기동 후 SSR 질의 5/5 정상), AP-20260920-02 백필 1건 대기(10시간째 · 1회 재확인, 실행 조건 충족).** 헬스체크 — `/api/health` 200 `{db:true, katago_alive:true, migrations {current:0020, head:0020, pending:false}}`, web 200, 공개 `inkbaduk.com` 200·`/api/health` 200, `com.baduk.api` PID 94717(9/20 23:05:28 기동, 10시간)·`com.baduk.web` PID 94911(9/20 23:06:25 기동, BUILD_ID `SexzBOHA9…` 23:06:14), `baduk-katago-0.err` mtime 23:05:31(재기동 직후 spawn), staging 중단(정책상 정상), 디스크 8%, plist drift 없음(exit 0), Claude 인증 `ok D-11`(만료 10-02 23:41). 백업 — 신규 `baduk-20260921T040004.db.gz` 4,713,337B(전일 대비 +33,244B, 04:00:04~06 생성·9/7분 보존 정리), `find -mtime +1` 출력 없음, daily 14 / weekly 8 / monthly 4, 복원 드릴 `integrity_check` ok·테이블 11·games 514·pro_games 4,511 = **87회 연속** 통과. 버그 스캔 — `.err` 235,658 → 235,677줄(**+19**) 전부 재기동 전후 기동 로그(#514 WS 3줄 + PID 5743 shutdown 4줄 + alembic 2줄 + PID 94717 startup 4줄 등), **재기동 마커(L235,674) 이후 트레이스백·`database is locked`·IntegrityError·`illegal move` 0, WS accepted 0**. `tail -300`에 보이는 `KataGo rejected play B D4: illegal move` 트레이스백은 9/19 11:06 #97 재현분(재기동 전, #98 발효로 종결)이라 신규 아님. `.log` 327,400 → 327,784줄(**+384**) 분포 200 377·404 6·**500 0·401 0·429 0** + `session_purge` JSON 1줄(01:05 KST removed 1). 404 6건 전부 `34.147.157.243`의 `/api/.env`·`/api/v1~v3/.env`·`/api/dev/.env`·`/api/staging/.env` 시크릿 탐색 스캐너 — 전부 404, 실 파일 노출 없음. 소스 — 127.0.0.1 224(SSR `/api/spectate/pro/N` 186·health 161), `2a06:98c0:3600::103`(Cloudflare 경유 크롤) 144, `122.128.252.5` 3(`/api/games/514` 조회 21:0x KST), 외부 소량. `[object Object]` 4건(누적 688) — 외부 3(`20.106.191.53`·`20.83.175.149`·`135.119.239.132` = 옛 번들 클라이언트) + `127.0.0.1` 1건은 L327,450으로 마지막 `/api/games/514`(L327,413 ≈ 21:11 KST) 이후·`session_purge`(L327,554 = 01:05 KST) 이전 구간 = **옛 web 빌드(23:06 이전)의 마지막 SSR**로 판정. **`session_purge` 이후 SSR `/api/spectate/pro?` 5건 전부 `collection=masterpiece&sort=recent&limit=50&offset=0` → #99 발효 검증 완료.** `baduk-web.err` 3,506 → 3,508줄(+2) = 새 프로세스(node 94924) 기동 경고 `--localstorage-file` 2줄, Server Action 오류 신규 0. **DB 실측** — `max(games.id)` 514 유지, 재기동(9/20 14:05 UTC) 이후 대국 생성 0·착수 0, 마지막 착수 **9/20 21:11:19 KST**(#514 60수 — 21:00 사이클의 30수에서 +30 후 중단), #513 96수·#514 60수 `active` 미종료 유지, `resigned`+`finished_at NULL` 99건 유지(신규 기권 없음 → #101 발효 검증은 다음 사용자 기권 대기). `visit_hits` 655(+0/12h), `sessions` 7(−1, purge). 중복 차단 — 후보 에러 0건이라 신규 이슈 없음("신규 버그 없음"). 백로그 트리아지 — 열린 이슈 0건·미분류 0건. PR 감시 — 열린 PR 0건. `main` CI 최근 3런 success(`c8b7930`·`e934a62`·`b7e33bb`, 9/20 23:06~23:10 KST). **잡 상태 4/4 `ok`**(dev-cycle 04:30 "열린 이슈 0건 · 처리할 버그 없음", news-hook 9/20 05:04, analytics-weekly 9/20 09:01, content-draft 9/19 02:03). sitemap `<loc>` 4,592·글로서리 35편·FAQ 14편(변동 없음). 배포 갭 0(`HEAD`=`origin/main`=`b7e33bb`), 마지막 코드 커밋 `c8b7930`(23:05:57)이 web 빌드(23:06:14)·재기동에 포함 → **라이브 = main**. watchdog 밤사이 `신규 incident 0 건`, `[api] OK [web] OK`. 승인 — **AP-20260920-02**(기권 대국 `finished_at` 99건 백필, prod DB 직접 쓰기 🟡) 대기 유지(10시간째 · 1회 재확인). 실행 조건 재확인 — 절차 (1) 마지막 착수 9/20 21:11 KST 이후 12시간 무착수 충족, SQL `ops/sql/2026-09-20-backfill-resigned-finished-at.sql` 573B 존재, 04:00 daily 백업 신선 → 승인만 오면 즉시 실행 가능. 🟡라 미실행. 신규 incident 없음.
- 09:05 orchestrator(am) — am 다이제스트 발송 완료(`send.py` exit 0, 마커 `~/.ops-report/markers/inkbaduk-am-2026-09-21` 생성). 전체 ✅, 승인 카드 1장(AP-20260920-02 10시간째 · 1회 재확인, "12h 무착수 — 지금 실행 적기" 명시) 동봉. 상태 커밋 main push.
- 21:00 orchestrator(pm) — **전 항목 정상 — 낮 동안 착수 0·트레이스백 0·5xx 0·신규 버그 0, 열린 이슈·PR 0건, 사람 방문 1건(랜딩만), AP-20260920-02 백필 1건 대기(22시간째 · 2회 재확인, 실행 조건 충족 지속).** 헬스체크 — `/api/health` 200 `{db:true, katago_alive:true, migrations {current:0020, head:0020, pending:false}}`, web 200, 공개 `inkbaduk.com` 200·`/api/health` 200, `com.baduk.api` PID 94717(9/20 23:05:28 기동, 22시간)·`com.baduk.web` PID 94911(9/20 23:06:25 기동, BUILD_ID `SexzBOHA9…` 23:06:14 유지), launchctl `com.baduk.api` 두 번째 컬럼 `-15`는 9/20 `kickstart -k`(SIGTERM)의 직전 종료 코드 잔존이며 현 PID 가동 중, staging 중단(정책상 정상), 디스크 8%, plist drift 없음(exit 0), Claude 인증 `ok D-11`(만료 10-02 23:41). 백업 — 최신 `baduk-20260921T040004.db.gz` 4,713,337B(am과 동일 파일, 다음 생성 9/22 04:00), `find -mtime +1` 출력 없음, daily 14 / weekly 8 / monthly 4, 복원 드릴 `integrity_check` ok·테이블 11·games 514·pro_games 4,511 = **88회 연속** 통과. 버그 스캔 — `.err` 235,677 → 235,677줄(**+0**) = 낮 12시간 트레이스백·`database is locked`·IntegrityError·`illegal move` 전부 0. `.log` 327,784 → 328,021줄(**+237**) 분포 200 230·204 2·401 3·404 2·**500 0·429 0**. 401 3건은 `/api/session` 익명 조회(`211.249.71.109` 2 + Facebook 크롤러 `2a03:2880::` 1) = 비로그인 정상 응답. 404 2건은 `34.156.22.151`의 `/api/phpinfo.php`·`/api/.env` 스캐너 — 노출 없음. 소스 — Cloudflare 프로브 `2a06:98c0:3600::103` 144(전부 `/api/health`), `127.0.0.1` SSR 약 75(`/api/spectate/pro/N` 33·health 13·`pro` 8·themes 7·sitemap 7·monthly pick 5), `211.249.71.109` 5, Facebook 크롤러 4. **`[object Object]`·`undefined` 0건(누적 688 유지) — 재기동 후 낮 사이클에서 옛 번들 클라이언트 잔존까지 0.** `baduk-web.err` 3,508줄(+0), Server Action 오류 0. **사람 방문 1건** — 10:17 KST `211.249.71.109`(KR·desktop·direct) 랜딩 `/`(`visit_hits` 656, +1) → `/api/auth/providers`·`/api/daily-challenge` 조회 후 대국 생성 없이 이탈. 같은 시간대 Facebook 크롤러(`2a03:2880:16ff/23ff::`)가 랜딩·`daily-challenge`·`auth/providers`·`analytics/hit`을 순회 = 링크 공유 미리보기 추정(트래픽 증거일 뿐 이상 아님). **DB 실측** — `max(games.id)` 514 유지, 재기동(9/20 23:05) 이후 대국 생성 0·착수 0, 마지막 착수 **9/20 21:11:19 KST**(36시간 무착수), #513 96수·#514 60수 `active` 미종료 유지, `resigned`+`finished_at NULL` 99건 유지(신규 기권 없음 → #101 발효 검증 계속 대기), `sessions` 7. 중복 차단 — 후보 에러 0건이라 신규 이슈 없음("신규 버그 없음"). 백로그 트리아지 — 열린 이슈 0건·미분류 0건. PR 감시 — 열린 PR 0건. `main` CI 최근 5런 success(`83be910` 09:05 포함, 9/20 23:05~9/21 09:05). **잡 상태 4/4 `ok`**(dev-cycle 9/21 04:30, news-hook 9/20 05:04, analytics-weekly 9/20 09:01, content-draft 9/19 02:03 — 다음 content-draft는 주기상 9/22~26 사이). 글로서리 35편·FAQ 14편(변동 없음). 배포 갭 0(`HEAD`=`origin/main`=`83be910`, 마지막 코드 커밋 `c8b7930`은 라이브 빌드·재기동에 포함) → **라이브 = main, 재기동 필요 없음**. watchdog 낮 동안 `신규 incident 0 건`, `[api] OK [web] OK`. 승인 — **AP-20260920-02**(기권 대국 `finished_at` 99건 백필, prod DB 직접 쓰기 🟡) 대기 유지(22시간째 · 2회 재확인). 실행 조건 재확인 — 절차 (1) 36시간 무착수·WS 접속 0 충족, SQL `ops/sql/2026-09-20-backfill-resigned-finished-at.sql` 존재, 04:00 daily 백업 신선(드릴 통과) → 승인만 오면 즉시 실행 가능. 🟡라 미실행. 신규 incident 없음.
- 21:03 orchestrator(pm) — pm 다이제스트 발송 완료(`send.py` exit 0, 마커 `~/.ops-report/markers/inkbaduk-pm-2026-09-21` 생성). 전체 ✅, 승인 카드 1장(AP-20260920-02 22시간째 · 2회 재확인, "36h 무착수·WS 0 — 락 충돌 없음" 명시) 동봉. 상태 커밋 main push.
