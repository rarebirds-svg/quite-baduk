# 2026-07-16 운영 로그

## dev-cycle (02:00)

- 이슈 선택: `gh issue list --state open --json number,title,labels` → 열린 적격 이슈 1건 (#54 `chore`+`small`, `feature`·`in-progress` 없음).
- **#54는 이미 처리 완료 상태** — 이전 dev-cycle 세션이 브랜치 `fix/issue-54`(커밋 `73cb3cf`)·PR #55를 만들어 사람 머지 대기 중(🟡). 신규 코드 변경 없이 중복 처리 방지.
- PR #55 상태 확인: `state:OPEN`, `mergeable:MERGEABLE`, `mergeStateStatus:UNSTABLE`. 체크 4개 중 backend·frontend·app-shell-build **pass**, **e2e만 fail**.
  - e2e 실패는 이 PR과 무관 — 변경 범위는 `ops/run-*.sh` 셸 래퍼(Homebrew PATH prepend)뿐이라 e2e 흐름에 영향 없음. 기존 e2e stale 이슈(`[[ci-e2e-disabled]]`)와 일치.
  - 남은 단계는 사람의 PR #55 리뷰·머지(🟡)뿐 — 자율 세션 처리 불가.
- 처리할 신규 버그 없음 → worktree 생성·코드 변경·커밋 없이 종료.
- 부수 확인: 이 launchd 세션 자신도 첫 `gh` 호출에서 exit 127 재현(#54 증상 그대로) — PATH 우회로 진행. PR #55 머지 시 근본 해소 예정.
- Telegram 보고: reply 도구 미노출 시 `~/.claude/channels/telegram/` 토큰으로 Bot API `sendMessage` 직접 발송.

## orchestrator (12:30)

- **healthcheck** ✅ — prod backend `{"status":"ok","db":true,katago_alive:false(lazy, 정보용)}`, web 200 OK. launchctl PID api **1516** / web **1504** (7/15과 동일 → 무중단 지속). 디스크 6%. plist drift 없음(sync-launchd exit 0). staging 중단(정책상 정상).
- **backup-verify** ✅ — 최신 `baduk-20260716T040005.db.gz`(신선, <30h), integrity `ok`, 7 tables. 티어 daily 14 / weekly 8 / monthly 2.
- **bug-scan** — baduk-api.err tail 80줄: 정상 WebSocket INFO 트래픽(대국 313번까지)·정상 재시작만. 스택 트레이스·예외 0건 → 신규 버그 없음.
- **backlog-triage** — 미분류 이슈 0건(`no:label` 빈 배열). 열린 이슈는 #54(chore+small, 라벨 완료)뿐.
- **pr-watch** — 열린 PR 1건: #55. core 3체크(backend·frontend·app-shell-build) ✅, e2e ❌(`[[ci-e2e-disabled]]` 옛 흐름 가정, PR 범위와 무관). MERGEABLE. **주의 PR 1건** — 사람 머지 대기(🟡).
- 보류 승인 0건. 최근 CWI ingest 2026-07-12(new=200). 최근 주간 리포트 2026-W28.
- Telegram 상태 요약 발송.

## orchestrator (18:30)

- **healthcheck** ✅ — prod backend `{"status":"ok","db":true,"katago_alive":false}`(lazy spawn, 정보용 — 경보 아님), web 200 OK. launchctl PID api **1516** / web **1504** — 12:30과 동일. `ps` 확인 결과 api 1516은 **Sun Jul 12 00:53:09 기동, elapsed 4일 17시간** → 무중단 지속. 디스크 6%. plist drift 없음(exit 0). staging 중단(정책상 정상).
- **backup-verify** ✅ — 최신 `baduk-20260716T040005.db.gz`(7/16 04:00, `find -mtime +1` 무출력 = 신선), integrity `ok`, 7 tables. 티어 daily 14 / weekly 8 / monthly 2.
- **bug-scan** — 신규 버그 0건. **단, 12:30 세션보다 깊게 스캔해 스택 트레이스 2건 발견 후 오탐으로 판정**:
  - 증상: `TypeError: Object of type bytes is not JSON serializable` @ `backend/app/errors.py:21 val_handler` (err 로그 line 221457·221549, 2회).
  - **판정: stale — 이미 수정된 버그.** 근거 3가지. ① 커밋 `ce9d745`(6/11) "비JSON 본문 검증 오류가 500으로 둔갑하는 버그 수정"이 정확히 이 원인(pydantic v2가 파싱 실패 시 `input`에 raw bytes)을 `jsonable_encoder`+bytes 치환 디코딩으로 수정, 회귀 테스트(`test_error_response.py`) 동반. ② **트레이스백의 줄 번호가 현재 파일과 불일치** — 현 `errors.py:21`은 `_safe_validation_errors`의 `jsonable_encoder` 호출부이고 `val_handler`는 33행. 즉 로그의 21행=`val_handler`는 **수정 전 파일**의 레이아웃 → 에러는 6/11 픽스 이전 발생. ③ 로그 위치가 PID 33051→44935 재시작 이전 = 현 프로세스(1516, 7/12 기동) 3세대 이상 전.
  - `[[bug-scan-verify-recency]]` 원칙 적용 — 누적 카운트(2회)만 보고 이슈를 만들었으면 #41·#42류 오탐 재발이었음. 이슈 생성 안 함.
- **backlog-triage** — 미분류 이슈 0건(`no:label` 빈 배열). 열린 이슈 1건 = #54(chore+small, 라벨 완료). 할 일 없음.
- **pr-watch** — 열린 PR 1건: #55 `fix(ops): launchd 진입 래퍼에 Homebrew PATH 고정`(7/14 생성, 2일 경과 → 정체 아님), MERGEABLE. core 3체크 ✅, **e2e ❌**.
  - **e2e 실패는 PR #55와 무관 — main 기준 선재(pre-existing) 실패.** `gh run list --branch main`: 7/1·7/3·7/7 CI 전부 `failure`. PR 변경 범위는 `ops/*.sh` 7개 셸 래퍼뿐이라 e2e 흐름에 영향 불가.
  - **메모리 정정 필요** — `[[ci-e2e-disabled]]`는 e2e 잡이 `if: false`로 비활성이라 기록하지만, `.github/workflows` 확인 결과 **`if:` 조건 없이 재활성 상태**이고 main에서 RED. "비활성이라 무시 가능"이 아니라 "활성인데 깨져 있음"이 현재 사실.
- **#54 증상 이 세션에서도 재현** — 첫 `gh` 호출 exit 127(`command not found`). 세션 PATH=`/usr/bin:/bin:/usr/sbin:/sbin:+플러그인 경로`로 `/opt/homebrew/bin` 부재. `/opt/homebrew/bin` prepend로 우회해 3개 러닝북(bug-scan 중복차단·backlog-triage·pr-watch) 정상 수행. PR #55 머지 시 근본 해소.
- **승인 제안 AP-20260716-01 등록** — PR #55 머지(🟡). 4세션 연속 "머지 대기"로만 기록되고 정식 제안이 없었기에 큐에 올림.
- 최근 CWI ingest 2026-07-12(new=200). 최근 주간 리포트 2026-W28.
- Telegram 상태 요약 + AP-20260716-01 제안 발송.
