# 2026-06-24 운영 로그

- 02:00 자율 버그 사이클 (dev-cycle)
  - 이슈 선택: `gh issue list --state open` 결과 열린 이슈 0건. `bug`·`small` 적격 이슈 없음.
  - 참고: 최근 닫힌 이슈 #39~#42(database-locked·WS 계열) 모두 CLOSED 상태, 신규 미분류 버그 없음.
  - 결과: **처리할 버그 없음.** 코드 변경·커밋·핸드오프 없이 종료.
  - Telegram 보고: reply/telegram 채널 도구가 이 세션에 미노출(ToolSearch 3회, 연결 MCP는 Canva·Context7·Google Drive뿐). 환경변수 자가진단이 아닌 실제 도구 미노출 — 발송 불가. 결과가 무처리(routine)라 PushNotification도 생략. 다음 launchd 실행(`--channels`)에서 정상화 기대.

- 02:00 콘텐츠 초안 사이클 (content-draft)
  - 종류 선택: 게시 비율 glossary 15/18=0.83 vs faq 7/9=0.78 → 더 뒤처진 **faq** 선택. 미작성 후보(resign-and-pass·spectate-pro) 중 알파벳 첫 항목 `resign-and-pass`.
  - 초안 작성 → korean-copy-qa 점검. QA의 흐름 지적(반복 문장) 반영. QA의 "한국 룰=순수 영토 계가" 주장은 기존 게시 `gyega.md`·시드 힌트(territory+잡은 돌)와 배치되어 **미반영**(사실 검증 후 기각).
  - 게시 🟢 자율: `web/content/faq/resign-and-pass.md` 커밋 `cfaec4f` → `git push origin main` 성공(79efa46..cfaec4f). 덮어쓰기 가드 통과.
  - Telegram 사후 보고 발송 완료(`ops/notify-telegram.sh`): "resign-and-pass (faq) 게시 완료 — /faq/resign-and-pass".

- 12:00 오케스트레이터 12:30 실행 (healthcheck·backup-verify·bug-scan·backlog-triage·pr-watch)
  - 헬스: prod backend `{"status":"ok","db":true}` 200, web 200, launchctl api 923 / web 912 가동. staging 중단(정상). 디스크 6%. plist drift 없음(exit 0).
  - 백업: 최신 `baduk-20260624T040001.db.gz`, mtime 1일 이내, integrity_check=ok, 7 tables. daily 14 / weekly 5 / monthly 1.
  - 버그 스캔: `baduk-api.err` tail INFO만(WS accept/close 정상), 스택트레이스·예외 0. 신규 버그 없음.
  - 백로그 트리아지: 미분류 이슈 0건 — 할 일 없음.
  - PR 감시: 열린 PR 0건 — 주의 PR 없음.
  - Telegram: `ops/notify-telegram.sh`로 상태 요약 1건 발송 성공(exit 0). reply MCP 도구는 미노출이나 스크립트 경로 정상.

- 18:00 오케스트레이터 18:30 실행 (healthcheck·backup-verify·bug-scan·backlog-triage·pr-watch)
  - 헬스: prod backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, launchctl api 923 / web 912 가동. staging 중단(정상). 디스크 6%. plist drift 없음(exit 0).
  - 백업: 최신 `baduk-20260624T040001.db.gz`, mtime 1일 이내, integrity_check=ok, 7 tables. daily 14 / weekly 5 / monthly 1.
  - 버그 스캔: `baduk-api.err`에 `TypeError: Object of type bytes is not JSON serializable` 2건(line 221457·221549) 발견 — **조사 후 무처리**. (1) err 로그 mtime Jun 21로 3일째 신규 stderr 0. (2) 트레이스백의 `val_handler`가 line 21 위치 = **수정 전 옛 코드**. 현 코드는 `errors.py:12` `_safe_validation_errors`(bytes custom_encoder)로 이미 방어. (3) 수정 커밋 `ce9d745`는 Jun 11 21:12, 현 prod 프로세스 PID 923은 Jun 13 22:49 기동 = **수정 포함본 가동 중**. 두 TypeError는 923 기동 이전(수정 전) 죽은 프로세스 산물이며 923 기동 후 재발 0. **이슈 미생성** (recency·이미 수정 확인, `[[bug-scan-verify-recency]]`).
  - 백로그 트리아지: 미분류 이슈 0건 — 할 일 없음.
  - PR 감시: 열린 PR 0건 — 주의 PR 없음.
  - Telegram: 아래 보고 절차대로 발송.
