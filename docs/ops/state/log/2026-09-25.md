# 2026-09-25

- 04:30 dev-cycle — **처리할 버그 없음.** `gh issue list --state open` 열린 이슈 0건(9/21~9/24와 동일). 열린 PR 0건. 시작 시 미push 브랜치 점검 — 로컬 `fix/*` 6종(`ai-resign-score-margin`·`issue-78`·`issue-81`·`issue-84`·`issue-87`·`issue-101`) 전부 `gh pr list --state all --head`로 PR MERGED 확인(#82·#79·#83·#86·#95·#102), 원격 브랜치는 머지 후 삭제된 상태라 미push가 아님. `.worktrees/dev-cycle`는 `fix/issue-101`=`origin/fix/issue-101`(ahead 0). worktree 전환·코드 변경·커밋·PR 없음. `main`=`fd9721d`, `origin/main`=`fd9721d`. 잡 상태 `ok` 기록. 로그 커밋은 관례대로 09:00 오케스트레이터가 묶어서 처리.
- 09:00 orchestrator(am) — **전 항목 정상 — 밤사이 착수 0·트레이스백 0·5xx 0·신규 버그 0, 열린 이슈·PR 0건, 사람 방문 0건, AP-20260920-02 백필 1건 대기(106시간째 · 9회 재확인, 실행 조건 충족 지속).** 헬스체크 — `/api/health` 200 `{db:true, katago_alive:true, migrations {current:0020, head:0020, pending:false}}`, web 200, 공개 `inkbaduk.com` 200·`/api/health` 200, `com.baduk.api` PID 94717(9/20 23:05:28 기동, 106시간)·`com.baduk.web` PID 94911(9/20 23:06:25 기동, BUILD_ID `SexzBOHA9…` 유지), staging 중단(정책상 정상), 디스크 8%, plist drift 없음(exit 0), Claude 인증 `ok D-7`(만료 10-02 23:41). 백업 — 신규 `baduk-20260925T040001.db.gz` 4,715,307B(전일 대비 +368B, 04:00:01~02 생성·9/11분 보존 정리), `find -mtime +1` 출력 없음, daily 14 / weekly 8 / monthly 4, 복원 드릴 `integrity_check` ok·테이블 11·games 514·pro_games 4,511 = **95회 연속** 통과. 버그 스캔 — `.err` 235,677 → 235,677줄(**+0**) = 재기동(9/20 23:05) 이후 106시간 트레이스백·locked 0. `.log` 337,099 → 337,850줄(**+751**) 분포 200 744·404 6·`session_purge` info 1(removed 2)·**500 0·401 0·429 0**. 404 6건은 폴란드 `45.138.12.22`(TC DATACENTER) 한 대가 `/api/.env`·`/api/phpinfo.php`·`/api/v1/canal/config/1/1`을 각 2회 훑은 스캐너(전부 404, 9/24 pm GCP 스캐너와 같은 패턴, 이슈 아님). `[object Object]`·`undefined` 0건, POST 0건, `ws/games` 0. 경로는 크롤러의 `/spectate/pro/{id}` SSR 553페이지 + 헬스 프로브 159 + 월간 픽 14(`2138-02`~`2139-02`·`2028-01` 미래 월 순회, 전부 200). `web.err` 3,530 → 3,552줄(**+22**) = `Failed to find Server Action "x"` 스택 2건 — 누적 25회, 액세스 로그에 대응 `POST /` 0건이라 **외부 스캐너의 `Next-Action` 헤더 프로브로 판정, incident·이슈 아님**(기준선 3,552줄). KataGo 슬롯 `.err` 4종 모두 9/20 23:05 이후 갱신 없음(AI 응수 호출 0 = 착수 0과 정합). DB(`backend/data/baduk.db` readonly) — `games` max id 514(신규 0), `moves` 38,596건·마지막 착수 9/20 21:11 KST(108시간 무착수), `resigned`·`finished_at NULL` 99건 유지. `visit_hits` 669 → 669(**+0**) = 밤사이 사람 방문 0건(마지막 방문 9/24 18:33 KST 네이버 모바일→`/spectate/pro/2880`). 백로그 트리아지 — 열린 이슈 0건, `no:label` 0건(할 일 없음). PR 감시 — 열린 PR 0건, `main` CI 최근 3런 success(`fd9721d` 9/24 21:03 포함). 배포 — `HEAD`=`origin/main`=`fd9721d`, `rev-list HEAD..origin/main` 0. 재기동 이후 main 코드 커밋 0(마지막 코드 커밋 `c8b7930` 9/20 23:05:57 < BUILD_ID 생성 23:06:14, 이후는 ops·글로서리 force-dynamic 콘텐츠뿐) → **라이브 = main, 재기동 필요 없음.** 잡 — `state/jobs` 4/4 `ok`(dev-cycle 04:30 "열린 이슈 0건", content-draft 9/23 02:04 `du-nun`, analytics-weekly 9/20 09:01 W38, news-hook 9/20 05:04). 승인 — AP-20260920-02 실행 조건 재확인(마지막 착수 108시간 전·WS 0, SQL 573B 존재, 9/25 04:00 백업 신선·드릴 통과) → 9회 재확인 기록, 승인 회신 없음. 상태 파일(`dev-cycle.json`·잡 로그·오늘 로그) 묶음 커밋. am 다이제스트 발송 결과는 아래 줄에.
- 09:04 orchestrator(am) 다이제스트 — `send.py` exit 0, 마커 `~/.ops-report/markers/inkbaduk-am-2026-09-25` 생성. 5영역 ok·ok·ok·ok·hold(AP-20260920-02 승인 카드 1장 동봉, 106시간째 · 9회 재확인). 상태 파일 커밋 `f0cc392` push 완료.
- 21:00 orchestrator(pm) — **prod 전 항목 정상 · 5일 만의 실사용(대국 8건·착수 408수) · #101 발효 검증 완료 · `main` CI 빨강은 SQLAlchemy 2.1.0 드리프트(#103)로 규명해 PR #104 즉시 제출 · 승인 2건 대기(AP-20260925-01 신규, AP-20260920-02 10회 재확인).** 헬스체크 — `/api/health` 200 `{db:true, katago_alive:false, migrations {current:0020, head:0020, pending:false}}`(`katago_alive` false는 14:17 이후 AI 호출 없음, 정보용), web 200, 공개 `inkbaduk.com` 200·`/api/health` 200, `com.baduk.api` PID 94717(9/20 23:05:28 기동, 118시간)·`com.baduk.web` PID 94911(9/20 23:06:25 기동, BUILD_ID `SexzBOHA9…` 유지), staging 중단(정책상 정상), 디스크 8%, plist drift 없음(exit 0), Claude 인증 `ok D-7`(만료 10-02 23:41). 백업 — 최신 `baduk-20260925T040001.db.gz` 4,715,307B(04:00 생성, `find -mtime +1` 출력 없음), daily 14 / weekly 8 / monthly 4, 복원 드릴 `integrity_check` ok·테이블 11·games 514·pro_games 4,511 = **96회 연속** 통과. **사용 활동** — DB(`backend/data/baduk.db` readonly) `games` max id 514→**522**(+8, #515~#522 전부 `114.206.44.109` KR 한 사용자, 12:32~14:18 KST), `moves` 38,596→**39,004**(+408), 마지막 착수 9/25 14:17:38 KST. #516 19路 6k balanced 234수 W+39.5 종료(무르기 3), **#522 19路 6k territorial 172수 사용자(흑) 기권 W+R → `finished_at` 2026-09-25 05:18:06Z 기록 = #101/#102 발효 검증 완료**(9/20 재기동 후 첫 사용자 기권). #515·#517·#518(9路)·#519(13路) 0수, #520(치석 9)·#521 1수 `active` 방치 — #97 발효 확인(치석 대국 #520 생성·착수 정상, `illegal move` 없음). KataGo 슬롯 `.err` 4종 13:49~14:17 KST spawn·`GTP ready` 정상. 버그 스캔 — `.err` 235,677 → 235,701줄(**+24**) 전부 `WebSocket /api/ws/games/515~522 [accepted]`·open·closed INFO, 재기동 후 118시간 트레이스백·locked·ERROR 0. `.log` 337,850 → 338,780줄(**+930**) 분포 200 904·204 10(`analytics/hit`)·201 9(`POST /api/games` 8·`POST /api/session` 1)·**401 4(`GET /api/session` 익명 첫 방문 응답, 정상)·404 2(`103.206.253.106` `/api/session/properties` 프로브)·500 0·429 0**, `[object Object]`·`undefined` 0건, `ws/games` 0(WS는 `.err`에만 찍힘). 경로는 크롤러 `/spectate/pro/{id}` SSR 626 + 헬스 프로브 157 + 세션·관리자·대국 API. `web.err` 3,552 → 3,552줄(**+0**). `visit_hits` 669 → **679(+10)** = 사람 방문 10건(네이버 검색 4 12:30~13:16 KST → 대국 사용자, 직접 5, 구글 모바일 → `/glossary/chaksu-geumji` 1 19:09 KST). **CI** — `main` 최근 런 `f0cc392`·`b91c31a`(둘 다 9/25 09:04, docs(ops) 커밋) **backend 잡 failure**: `app/api/admin.py:336: error: Set comprehension has incompatible type Set[int | None]; expected Set[int]`. 성공 런 `fd9721d`(9/24 21:03)와 코드 차이 없음 → 두 런의 `Successfully installed` 라인 diff로 **`sqlalchemy-2.0.54 → 2.1.0`**(그 외 google-auth·ruff 패치만, mypy 2.3.1 동일) 확인. `pyproject` 핀 `>=2.0`이라 fresh 설치가 2.1.0을 받았고 2.1 스텁이 `Game.session_id: Mapped[int | None]`을 `select()` 행 타입까지 전파. 로컬 venv(SA 2.0.49·mypy 1.20.1) `mypy app` 통과 — `[[backend-dep-drift-ci]]` 케이스. **이슈 #103 생성**(bug) → worktree `.worktrees/ci-mypy`에서 `admin.py:336`에 `if sid is not None` 한 줄 추가(`e8854b0`), 로컬 `mypy app`·`ruff` 통과·`tests/api/test_admin*.py` 35 passed → `fix/admin-connected-sids-optional` push → **PR #104**. PR CI 21:2x 기준 app-shell-build·frontend·**backend pass**(같은 fresh 설치 = SA 2.1.0에서 ruff·mypy·pytest·커버리지 통과 → SA 2.1 런타임 호환 확인), e2e 진행 중(다음 사이클 pr-watch 확정). prod 무관(prod venv 2.0.49, 재기동 이후 코드 변경 없음). worktree 제거. 머지는 🟡라 **AP-20260925-01 등재**(재기동 선택, `sqlalchemy<2.1` 핀 여부는 사람 판단). 백로그 트리아지 — 열린 이슈 #103 1건(bug 라벨 부여됨), `no:label` 0건. PR 감시 — 열린 PR #104 1건(신규, CI 진행 중, 충돌·정체 없음). 배포 — `HEAD`=`origin/main`=`b91c31a`, `rev-list HEAD..origin/main` 0, 재기동 이후 main 코드 커밋 0 → **라이브 = main**. 잡 — `state/jobs` 4/4 `ok`(dev-cycle 04:30, content-draft 9/23 02:04, analytics-weekly 9/20 09:01, news-hook 9/20 05:04). 승인 — AP-20260920-02 실행 조건 재확인(마지막 착수 14:17 KST 이후 6.7시간 무착수·WS 0, SQL 573B 존재, 04:00 백업 신선·드릴 통과, #522는 `finished_at` 있어 대상 99건 불변) → 10회 재확인 기록, 승인 회신 없음. 상태 파일 커밋. pm 다이제스트 발송 결과는 아래 줄에.
