# 2026-07-25

- 02:00 dev-cycle: **이슈 #65 처리 완료** — `fix/issue-65` / `386567d` / PR [#66](https://github.com/rarebirds-svg/quite-baduk/pull/66). 머지 안 함(🟡).
  - 이슈 선택. 열린 이슈 1건뿐(`#65`, `bug`·`prio:high`), `feature`·`in-progress` 없음 → 유일 적격. `in-progress` 부착 후 처리, 완료 시 제거.
  - **최근성 독립 재검증** (`[[bug-scan-verify-recency]]`) — 이슈 본문 수치를 그대로 믿지 않고 다시 셌다. `[SQL: UPDATE pro_games` 16건 **전부** 현 prod PID 1516 기동 마커(line 221718) 이후, 그중 14건은 7/23 23:15 web 재기동 직후 구간(line 222805~) 이후에 집중. 마지막 발생 line 225145, 로그 mtime `Jul 24 12:49`. 프로세스 스코프 카운트 비교이므로 #64·#41·#42의 오류 모드(위치를 최근성 대리지표로 사용) 아님 — **실재 버그로 확정**.
  - 원인 확인. `app/api/spectate_pro.py` 상세 GET마다 `game.view_count += 1` → flush → commit. 단일 라이터 SQLite에서 크롤러 동시 유입이 `busy_timeout=5000`을 초과하고, 실패가 그대로 500으로 노출돼 **정작 SEO 대상 상세 페이지가 크롤러에 오류를 반환**하고 있었다.
  - 수정 (TDD). 실패 테스트 `test_detail_survives_view_count_lock` 먼저 작성 → 수정 전 500 red 확인 → 구현 → green. 내용은 ① 원자적 `view_count = view_count + 1` UPDATE(갱신 유실도 제거) ② `OperationalError`는 rollback + `pro_view_count_bump_skipped` warning으로 흡수 ③ 불필요한 `db.refresh` 제거.
  - **테스트가 내 수정의 결함을 하나 더 잡았다** — `rollback()`이 ORM 객체를 expire시켜 그 뒤 `game.sgf` 접근이 greenlet 밖 지연 로딩 IO(`MissingGreenlet`)를 유발했다. prod의 락 경로에서 그대로 재현될 문제였다. 응답 직렬화를 쓰기 **이전**으로 옮겨 해결. 테스트를 먼저 쓰지 않았으면 500을 다른 500으로 바꿔 놓고 완료 보고할 수 있었다.
  - 검증. `pytest` **528 passed**, `ruff check .` clean, `mypy app` clean (worktree 코드 기준, 메인 `.venv311` 공유 — `[[worktree-shared-venv]]`).
  - push·PR 자율 수행(feature 패턴 = 🟢). 로컬 방치 없음(`[[dev-cycle-unpushed-branches]]` 재발 방지) — `git push -u origin fix/issue-65` 후 origin 반영 확인, PR #66 생성 확인.
  - **범위 밖으로 남긴 것(사람 판단)** — 이 수정은 *에러 노출*을 없애지만 *쓰기 volume*은 그대로다. 이슈에 함께 보고된 무르기 경로 실패(`DELETE FROM moves ... is_undone`, 17.7h 2건)가 조회수 쓰기와의 경합 때문이라면 남는다. 근본 완화는 `app/last_seen_cache.py`식 디바운스 플러시(요청당 메모리 카운트 → 60s 일괄 UPDATE)를 조회수에도 적용하는 것인데, 신규 모듈 + lifespan 배선이 필요해 자율 버그 사이클 범위를 넘는다. 후속 이슈 분리 여부를 PR·이슈·Telegram 3곳에 모두 명시해 남겼다.
  - Telegram 보고 **발송 성공** (`ok=true`, message_id **2445**). 이 세션에도 `reply` 도구 미노출(ToolSearch 결과 0건) → `[[dev-cycle-telegram-botapi]]`대로 Bot API 직접 호출, 한글 본문은 파일 + `--data-urlencode "text@..."` + `LC_ALL=en_US.UTF-8`.

- 02:00 content-draft: **처리할 토픽 없음** — 종료. 시드 소진 상태가 7/22에 이어 지속.
  - 실측. `web/content/glossary/` 27개 파일 · `web/content/faq/` 12개 파일 · `docs/ops/content/drafts/` 비어 있음.
  - 게시 비율 — glossary **18/18**, faq **9/9**. 미작성 후보 각 **0**이므로 종류 선택 단계에서 종료.
  - `kasaeng`(시드 title 사활)은 slug 통합으로 `web/content/glossary/sahwal.md`(created_at 2026-07-22)로 게시돼 있어 후보에서 제외했다 — slug 문자열만 비교하면 미작성으로 오판된다(`[[glossary-sahwal-slug]]`).
  - 기존 항목 임의 개정 없음(정책상 금지). draft·commit·push·IndexNow 없음(net-zero). Telegram 보고 없음(게시 사후 보고 전용).
  - **사람 판단 필요** — 시드 파일(`docs/ops/content/seed-glossary.yml`·`seed-faq.yml`)에 신규 토픽을 추가하지 않으면 주 2회 content-draft 사이클은 계속 무작업으로 끝난다. 7/22·7/25 2회 연속 소진.

- 12:00 오케스트레이터 — 러닝북 5종 수행. **전부 정상, 신규 incident 0건.**
  - **헬스체크 OK.** prod backend `{"status":"ok","db":true,"katago_alive":true}` 200, prod web 200. staging 중단(정책상 정상). 디스크 6%. `sync-launchd.sh --check` exit 0(drift 없음).
  - **PID 변경은 장애가 아니라 사람 배포다** — api 1516→**5597**, web 49162→**5601**, 둘 다 `Sat Jul 25 08:57:36` 동시 기동. 재부팅 아님(`uptime` 13일 11시간, `last reboot` 7/12). 직전 커밋 `b4223c0`(#67 방문 통계 어드민 머지)이 **08:56:08**이고 재기동이 88초 뒤 = 사람이 #67을 머지하고 `stack.sh restart prod`로 배포한 것. 오케스트레이터 조치 불필요.
  - **#67 배포 건전성 검증 — 정상.** 마이그레이션 적용 완료(`alembic_version=0017`, 신규 테이블 `visit_hits`·`search_queries` 생성 확인, 총 9 tables). 비콘 경로 종단 확인 — `VisitBeacon`이 `web/app/layout.tsx:149`에 마운트, `next.config.js` rewrite가 `/api/:path*` → `:8000`으로 프록시, `POST /api/analytics/hit`이 3000·8000 양쪽에서 **422**(빈 본문에 대한 정상 검증 응답) = 라우팅 살아 있음.
  - **`visit_hits` 0행은 버그가 아니다.** 비콘은 `navigator.sendBeacon` 기반 **클라이언트 사이드**이고 봇을 제외한다(`analytics.py:1`). curl은 JS를 실행하지 않으므로 내 점검 요청으로는 행이 생기지 않는 것이 정상이다. 3시간 무행은 "토요일 오전 JS 방문자 부재"로 설명되며, 실제 사람 방문 발생 시 재확인 대상.
  - **자가 오염 주의(다음 bug-scan용)** — 위 검증에서 내가 보낸 빈 본문 POST 2건이 `baduk-api.err`에 `RequestValidationError`로 남는다. **내 점검이 만든 노이즈이지 사용자 버그가 아니다.** 12:05경 `/api/analytics/hit` 422 2건은 이슈화하지 말 것.
  - **백업 검증 OK.** 최신 `baduk-20260725T040005.db.gz`(7/25 04:00, `find -mtime +1` 무매치). 복원 드릴 `integrity_check=ok`, 7 tables. daily/weekly/monthly = 14/8/2. (드릴 대상은 04:00 백업이라 #67 이전 스키마 = 7 tables가 정상. 다음 04:00 백업부터 9 tables.)
  - **백로그 트리아지 — 미분류 0건.** `no:label` 빈 배열.
  - **공개 도메인 확인(범위 밖 보너스)** — `inkbaduk.com`·`www.inkbaduk.com` 200, `cloudflared` tunnel `baduk` 가동(pid 803). 트래픽 부재가 터널 장애 때문이 아님을 배제.

- 12:00 버그 스캔 — **신규 이슈 0건.**
  - `database is locked` — 08:57 재기동으로 **기동 마커가 `Started server process [5597]`(line 225173)로 갱신**됐다. 그 이후 구간 집계 **0건**. 마지막 발생은 line 225144로 마커 **이전**(구 프로세스 구간).
  - **다만 이 0건도 완화 신호가 아니다** — 마커 이후 로그가 총 **4줄**(uvicorn 기동 라인뿐)이다. 재기동 후 3시간 동안 요청이 0건이라 경합할 일 자체가 없었다. 7/24 18:00과 같은 트래픽 부재 교란이며, `[[bug-scan-verify-recency]]`의 반대 방향 주의(반사적 기각 금지)에 따라 **#65는 미해결로 간주**한다.
  - 게다가 프로세스 교체로 **집계 구간이 리셋**됐다. 이전 프로세스의 19건과 직접 비교 불가하므로, 다음 트래픽 유입 구간에서 새 마커 기준으로 다시 세야 판정 가능하다.
  - `baduk.db-wal` 5,009,952 바이트 — 7/23·7/24에 이어 **4회 연속 동일 크기**. mtime은 7/25 11:42로 갱신돼 있어 쓰기 자체는 있었다. 체크포인트 정체 관찰 항목으로 계속 추적.

- 12:00 PR 감시 — **열린 PR 1건, 전부 그린. 사람 머지 대기.**
  - **PR #66** `fix(spectate): 프로 기보 조회수 증가를 원자적 UPDATE로 전환하고 락 실패를 흡수 (#65)` — 7/24 17:06Z 생성(19시간 경과, 정체 아님). `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, 충돌 없음. CI 4잡 **전부 SUCCESS**(backend·frontend·app-shell-build·e2e). 리뷰 결정 없음.
  - **e2e 잡 SUCCESS** — `[[ci-e2e-disabled]]`가 기록한 만성 RED가 이 런에서는 그린. #59 머지 이후 회복 상태가 유지되는 것으로 보인다.
  - **#66이 머지되지 않아 #65 수정이 prod에 없다.** 오늘 08:57 배포는 `main`(=#67)을 반영한 것이고 #66은 아직 `main` 밖이라, 조회수 락 500 노출은 라이브에 그대로 남아 있다. 머지는 🟡 → **AP-20260725-01로 승인 요청 등재**.

- 12:00 보류 승인 — **2건 대기.**
  - `AP-20260723-01`(settings.json deny `Write`→`Edit`) — 7/23 12:00 등재 후 **48시간 무응답**. 이번 실행의 `orchestrator-runs.log`(12:00:05 시작 직후)에도 동일 하네스 경고 2건이 재출력 = 가드레일 여전히 실효 없음.
  - `AP-20260725-01`(PR #66 머지) — 신규 등재.

- 12:00 Telegram 보고 — **발송 성공** (`ok=true`, message_id **2447**).
  - 이 세션에도 Telegram `reply` 도구 미노출(ToolSearch 결과 에이전트간 `SendMessage`만) → `[[dev-cycle-telegram-botapi]]`대로 Bot API 직접 호출. 토큰 `~/.claude/channels/telegram/.env`, chat_id `access.json`의 `allowFrom[0]`(`1241277614`). 한글 본문은 `/tmp/tg-msg-1200.txt`에 써서 `--data-urlencode "text@파일"` + `LC_ALL=en_US.UTF-8`.
  - 인바운드 승인 답신 없음 — `~/.claude/channels/telegram/inbox/` 최신 파일이 7/22 22:48로 3일째 변동 없음.
  - 보고 내용: 헬스·백업 OK, PID 변경이 사람 배포임과 #67 배포 검증 결과, 버그 스캔 0건이 트래픽 부재로 교란된 수치라는 단서, AP-20260725-01 신규 승인 요청, AP-20260723-01 48시간 재촉.

- 18:00 오케스트레이터 — 러닝북 5종 수행. **전부 정상, 신규 incident 0건.**
  - **헬스체크 OK.** prod backend `{"status":"ok","db":true,"katago_alive":true}`, prod web 200. PID api 5597 / web 5601 유지(08:57 사람 배포 이후 무재기동). staging 중단(정책상 정상). 디스크 6%. `sync-launchd.sh --check` exit 0. 공개 도메인 `inkbaduk.com` 200.
  - **백업 검증 OK.** 최신 `baduk-20260725T040005.db.gz`(04:00, `find -mtime +1` 무매치). 복원 드릴 `integrity_check=ok`, 7 tables. daily/weekly/monthly = 14/8/2. (04:00 백업이라 #67 이전 스키마 = 7 tables 정상. 다음 04:00부터 9 tables.)
  - **백로그 트리아지 — 미분류 0건.**
  - **`content-ingest` 미실행은 정상.** 최근 실행 7/19 03:00(new=200). 실행 이력이 6/14·6/21·6/28·7/5·7/12·7/19로 **정확히 주 1회(일요일)** = plist 주간 스케줄. 오늘은 토요일이라 다음 실행은 7/26. stale 아님.

- 18:00 버그 스캔 — **신규 이슈 0건. 다만 12:00의 "요청 0건" 판정은 오독이었다(아래).**
  - **로그 채널을 잘못 읽고 있었다.** `com.baduk.api.plist`는 `StandardOutPath=~/Library/Logs/baduk-api.**log**`, `StandardErrorPath=~/Library/Logs/baduk-api.**err**`로 **분리**돼 있다. HTTP 액세스 로그는 `.log`(stdout), 기동 라인·트레이스백·WebSocket 라인은 `.err`(stderr)로 간다.
  - 12:00 실행은 `.err`만 보고 "마커 이후 4줄 = 3시간 요청 0건"으로 결론냈지만, 같은 시각 `.log`는 계속 기록 중이었다(현재 mtime **18:00**, 176,075줄). **트래픽은 있었다.** 대시보드·12:00 로그의 "트래픽 부재" 서술을 이 항목으로 정정한다.
  - **배포 이후 구간을 정확히 자를 방법을 찾았다** — `POST /api/analytics/hit`은 #67에서 신설된 엔드포인트라 `.log`의 **첫 출현 라인(175,907)이 곧 08:57 배포 경계**다. 기동 마커가 없는 stdout 로그에도 쓸 수 있는 대체 마커.
  - **배포 이후 실측 (168개 요청).** 상태코드 `200`×146 · `401`×10 · `204`×10 · `422`×2 — **500 0건**. `.err`도 마커 이후 트레이스백 0건. 이 구간 `GET /api/spectate/pro/{id}`(조회수 쓰기 경로) **16회**에 락 실패 0건.
  - **그래도 #65는 미해결로 유지한다.** 16회/9시간은 경합이 생길 밀도가 아니다(문제를 일으킨 7/23~24 구간은 크롤러 버스트였다). 게다가 **PR #66이 미머지 = 수정이 prod에 없다** — 0건은 수정 효과가 아니라 저부하의 결과다. 판정은 다음 크롤러 유입 구간에서.
  - **자가 오염 확인** — 배포 이후 `422`×2는 12:00 실행이 예고한 자기 점검 POST 2건과 정확히 일치. 예고대로 이슈화하지 않았다.
  - `.log`의 최근 2000줄 창에는 `500`×15가 있으나 **전부 경계(175,907) 이전 = 구 프로세스 구간**이다. 창 크기만 보고 최근 것으로 오판하지 말 것(`[[bug-scan-verify-recency]]`).
  - `baduk.db-wal` 5,009,952 바이트 — **5회 연속 동일 크기**, mtime 17:34로 갱신. 체크포인트 정체 관찰 계속.
  - **실사용자 트래픽 확인** — `visit_hits` **8행**(최신 `2026-07-25 05:43:23` **UTC** = 14:43 KST). 컬럼은 UTC 저장이다. 경로는 `/`·`/spectate/pro`·`/support` 각 2, `/glossary/okjip`·`/spectate/pro/969` 각 1. 12:00에 "실제 사람 방문 시 재확인" 남긴 항목 **해소 — 비콘 정상 동작 확인**.

- 18:00 PR 감시 — **열린 PR 1건, 변화 없음. 사람 머지 대기.**
  - **PR #66** — 7/24 17:06Z 생성(25시간, 정체 아님). `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, 리뷰 결정 없음. CI 4잡 전부 pass(backend 1m0s · frontend 51s · app-shell-build 50s · e2e 2m20s).
  - e2e 그린 유지 — `[[ci-e2e-disabled]]`의 최신 상태(회복)와 일치.

- 18:00 보류 승인 — **2건 대기, 둘 다 무응답 지속.**
  - `AP-20260725-01`(PR #66 머지) — 12:00 등재 후 6시간 무응답.
  - `AP-20260723-01`(settings.json deny `Write`→`Edit`) — 7/23 12:00 등재 후 **54시간 무응답**. 이번 실행의 `watchdog.out.log`에도 동일 하네스 경고 2건이 그대로 출력 = 가드레일 여전히 실효 없음(`[[settings-deny-write-vs-edit]]`).

- 18:00 Telegram 보고 — **발송 성공** (`ok=true`, message_id **2448**).
  - 이 세션에도 Telegram `reply` 도구 미노출(ToolSearch 결과가 에이전트간 `SendMessage`·Gmail·Canva뿐) → `[[dev-cycle-telegram-botapi]]`대로 Bot API 직접 호출. 한글 본문은 `/tmp/tg-msg-1800.txt`에 써서 `--data-urlencode "text@파일"` + `LC_ALL=en_US.UTF-8`.
  - 인바운드 승인 답신 없음 — `inbox/` 최신 파일이 7/22 22:48로 3일째 변동 없음.
  - 보고 내용: 헬스·백업 OK, 12:00 "요청 0건" 오독 정정, 배포 이후 168요청 500 0건이되 #66 미머지라 완화 아님, 비콘 정상 확인, AP 2건 무응답 재촉.
