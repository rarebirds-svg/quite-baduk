# 2026-07-23

- 18:00 오케스트레이터 (러닝북 5종 수행)
  - **헬스체크** — prod backend `{"status":"ok","db":true,"katago_alive":false}` OK, web 200 OK. launchctl `com.baduk.api` PID 1516 / `com.baduk.web` PID 10162 — 둘 다 12:00과 동일(변동 없음). `katago_alive:false`는 lazy spawn 정보용(경보 아님). staging 중단(정책상 정상). 디스크 6%. plist drift 없음(exit 0). **api 무중단 11일 17시간**.
  - **백업 검증** — 최신 `baduk-20260723T040005.db.gz`(7/23 04:00, `-mtime +1` 미출력 = 신선). daily/weekly/monthly = 14/8/2. 복원 드릴 `integrity_check=ok`, 테이블 7개. 정상.
  - **버그 스캔 — 신규 이슈 0건.** 다만 이번 실행에서 **오탐 이슈 #64를 생성했다가 같은 세션에서 자진 철회·close**했다. 경위와 정정은 아래 별도 항목.
  - **백로그 트리아지** — 열린 이슈 0건, 미분류 0건(`no:label` 빈 배열). 분류 작업 없음. 사람 분류 필요 항목 없음.
  - **PR 감시** — 열린 PR 0건. CI 실패·충돌·정체 해당 없음.
  - **보류 승인** — `AP-20260723-01`(settings.json deny `Write(...)`→`Edit(...)` 정정) **여전히 대기 1건**. 12:00 등재 이후 답신 없음. 상태 변화 없어 재제안하지 않고 요약에만 리마인드 포함.
  - **⚠️ 자체 오탐 — 이슈 #64 생성 후 철회 (`[[bug-scan-verify-recency]]` 3차 재발)**
    - 경위. `database is locked`를 "현재진행형"으로 오판해 이슈 #64 생성. 근거로 삼은 것은 (a) 누적 2522회, (b) 최근 5000줄 내 50회, (c) 마지막 발생이 파일 끝에서 22줄 지점 + 파일 mtime 17:43(17분 전).
    - 오류. 최근성을 **파일 내 위치**로 쟀다. 현 프로세스 기동 마커(`Started server process [1516]`)가 **line 221718 / 전체 222287** — 즉 **로그의 99.7%가 7/12 재기동 이전 백로그**다. "끝 근처"가 "최근"을 뜻하지 않는다. (b)의 50회도 전부 재기동 전 구간.
    - 실제. 기동 이후 매치 6줄, async SQLAlchemy는 1건당 raw+래핑 2줄 출력 → **11일간 실질 3건**. 그중 마지막 1건(game 322 무르기 `DELETE FROM moves`)은 **12:00 실행이 이미 확인·기각한 바로 그 건**이다. 12:00 이후 신규 발생 **0건**(로그 증가분 3줄, 전부 WS connect/close).
    - 처리. #64에 정정 코멘트 게시 후 `not planned`로 close. 완화책 `busy_timeout=5000`+WAL(`d469164`)은 **정상 작동 중**이며 재기동 전후 발생률 급감이 이를 뒷받침. #13·#40·#41·#42에 이은 5번째 중복 이슈 근거 없음.
    - 재발 방지. `[[bug-scan-verify-recency]]` 메모리를 갱신 — 기동 마커 기준 구간 한정 집계를 **1번 필수 단계**로 승격, 에러당 2줄 보정 명시, 직전 실행 로그 선독을 절차에 추가. `tail -N`·누적 `grep -c`·파일 내 위치는 최근성 대리지표로 쓰지 않는다고 못박음.
    - 메타 교훈. 12:00 실행 로그를 **먼저** 읽었으면 즉시 걸러졌다 — 직전 실행이 같은 에러를 명시적으로 기각한 기록이 있었다.
  - **Telegram 보고 — 발송 성공(message_id 2438).** 이 세션에는 telegram `reply` 도구가 노출되지 않았고 deferred 목록에도 없었다(ToolSearch 확인 — SendMessage·PushNotification·Canva·Firecrawl만 매치). `[[dev-cycle-telegram-botapi]]`대로 "미발송"으로 적지 않고 Bot API 직접 호출로 폴백 — `~/.claude/channels/telegram/.env`의 `TELEGRAM_BOT_TOKEN` + `access.json`의 chat_id `1241277614`, 한글 본문은 파일에 써서 `--data-urlencode text@파일` + `LC_ALL=en_US.UTF-8`. 응답 `ok=true`. 요약 본문에 헬스·백업·보류 승인 1건·최근 CWI ingest(7/19 신규 200)·최근 분석(2026-W29)과 위 오탐 경위를 포함.
- 12:00 오케스트레이터 (러닝북 5종 수행)
  - **헬스체크** — prod backend `{"status":"ok","db":true}` 200 OK, web 200 OK. launchctl `com.baduk.api` PID 1516 / `com.baduk.web` PID **10162**. `katago_alive:false`는 lazy spawn 정보용(경보 아님). staging 중단(정책상 정상). 디스크 6%. plist drift 없음(exit 0).
  - **web PID 변경 설명됨(장애 아님)** — 29437 → 10162, 기동 `7/22 23:53:03`. 같은 시각대 커밋 `f35909e`(#63 SEO)·`3b8054f`·`6c5a758`·`758bf4f`(23:45~23:52)과 `web/.next` 재빌드(23:52~23:53)에 정확히 대응 = **의도된 콘텐츠·SEO 배포**. 크래시 흔적 없음.
  - **api 무중단 11일 11시간** — PID 1516, 기동 `7/12 00:53:09` 불변.
  - **백업 검증** — 최신 `baduk-20260723T040005.db.gz`(7/23 04:00, 신선도 OK — `-mtime +1` 미출력). daily/weekly/monthly = 14/8/2. 복원 드릴 `integrity_check=ok`, 테이블 7개. 정상.
  - **버그 스캔 — 신규 이슈 0건**. `database is locked` 누적 2522회는 **거의 전부 7/12 이전 이력**. 현 프로세스 기동(로그 line 221718) 이후로는 566줄 중 locked 6줄 = **11일간 실질 ~3건**(에러당 2줄 출력). `[[bug-scan-verify-recency]]` 지침대로 누적 카운트가 아닌 최근성으로 판정 — #41·#42와 동일한 오탐 패턴을 반복하지 않음. 완화책 `busy_timeout=5000`은 `d469164`(5/24, #25)로 main 반영돼 **현 구동 프로세스에 이미 적용된 상태**이며, 잔여는 undo 경로(`DELETE FROM moves WHERE game_id=? AND is_undone IS 1`, 게임 322) 저빈도 잔존. 기존 #13·#40·#41·#42가 모두 CLOSED라 5번째 중복 이슈는 생성하지 않음.
  - **web `Failed to find Server Action` 48건 — 자동 해소 확인**. 마지막 발생 line 2870 < 재기동 마커 line 2891 = **배포 이전** 발생. 구 번들을 쥔 클라이언트의 전형적 배포 직후 증상이며, 23:57 이후 12시간 신규 0건(파일 mtime 정지). 조치 불요.
  - **백로그 트리아지** — 열린 이슈 **0건**, 미분류 **0건**(`no:label` 빈 배열). 분류 작업 없음. 사람 분류 필요 항목 없음.
  - **PR 감시** — 열린 PR **0건**. CI 실패·충돌·정체 해당 없음.
  - **신규 발견 — 🟡 `AP-20260723-01`: settings.json deny 규칙 무효화**. orchestrator-runs.log에 하네스 경고 2건 기록됨. `Write(backend/data/*.db)`·`Write(backend/katago/models/*)` deny는 **파일 권한 검사에 매칭되지 않는다** — `Edit(path)` 규칙만 모든 파일 편집 도구를 포괄. 즉 **라이브 prod DB(`backend/data/baduk.db`, 10.9MB, 백업 원본)와 KataGo 모델이 현재 에이전트 쓰기로부터 실질 보호되지 않음**. 설정 변경이라 정책상 🟡 → 자율 수정하지 않고 승인 큐 등재·보고.
  - 관측(사람 판단 대기) — `docs/ops/state/` 미커밋 파일 **35개** 누적(7월 일일 로그 23건 + 주간 리포트 W27~W29 등). 마지막 일괄 커밋은 `2d79400`("6월 상태 로그·주간 리포트 커밋")로, 7월분이 통째로 git 이력에 없음. ops state를 `main`에 커밋·push하는 것은 콘텐츠(🟢)도 코드(🟡)도 아닌 모호 등급이라 정책 L16(보수 처리)에 따라 **자율 실행하지 않고 보고만** 함. 6월처럼 일괄 커밋할지 사람 판단 필요.
  - 관측(조치 불요) — web.err에 `"next start" does not work with "output: standalone"` 경고가 재기동마다 반복. 장기 존속 조건이고 prod는 200 정상 응답이라 이번 스캔에서는 이슈화하지 않음. 사람 인지용으로만 기록.
- 02:00 dev-cycle: **처리할 버그 없음** — `gh issue list --state open`가 빈 배열 반환(적격 `bug`/`small` 이슈 0건, 열린 이슈 전체 0건). gh 인증 정상(에러 없이 exit 0). 선점 라벨 부착 없음, worktree 손대지 않음(net-zero). 커밋·push·PR 없음. #55(launchd PATH에 gh 부재 해소)로 이번 세션은 gh 수동 prepend 불필요 — 실제로 gh가 정상 동작함. Telegram 결과 보고 1건 발송.
