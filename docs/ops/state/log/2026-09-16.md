# 2026-09-16

- 02:00 content-draft — faq `choose-ai-rank`(내 실력에 맞는 AI 급수는 어떻게 고르나요?) 초안 생성 → korean-copy-qa 통과(치명 0, 선택 개선 4건 중 3건 반영 — 첫 단락 문장 호응 분리·"5급 전후"를 예시로 완곡화·"치석/접바둑" 중복 축약·ai-strength-levels 상호 참조를 마크다운 링크로; 청유형 어조는 How-to 성격이라 유지). 시드 힌트의 "질문 두 개 급수 추천 퀴즈"는 코드베이스에 없어 본문에서 제외하고 실제 UI(9급~9단·호선/2~9점 치석·"추천 상대" 목록)만 서술. main 게시·push(eb90a0e)·IndexNow 200·라이브 200. 선택 근거: faq 10/24(0.417) < glossary 24/57(0.421). 잡 상태 `ok` 기록(state/jobs/content-draft.json). QA 부수 발견: 게시본 `ai-strength-levels.md`의 "입문부터" 문구가 실제 선택지(9급~9단)와 어긋날 수 있음 — 미수정, 후속 검토 대상.
- 04:30 dev-cycle — **#97 처리 완료 → PR [#98](https://github.com/rarebirds-svg/quite-baduk/pull/98) 생성(머지 🟡 사람 대기).** 적격 이슈 #97(`bug`) 1건뿐. `.worktrees/dev-cycle`에 `fix/issue-97` 브랜치(origin/main `eb90a0e` 기준). TDD — 실제 GTP처럼 같은 크기 `boardsize`에 보드를 유지하고 점유 좌표 `play`에 오류를 내는 `MockKataGoAdapter` 서브클래스로 회귀 테스트 작성(`backend/tests/api/test_create_game_stale_board.py`), 이슈와 동일한 `KataGo rejected play B D4: illegal move`로 실패 확인 → `create_game`의 `set_boardsize` 직전에 `clear_board()` 추가(`_reseed_adapter`와 같은 순서·주석) → 통과. pytest 610 passed·커버리지 82%·`ruff check .`·`mypy app` 통과. 리뷰(fable, 전경 동기) 승인 — `clear_board` no-op으로 되돌리면 테스트가 실패함을 리뷰어가 독립 확인, `set_boardsize` 호출 다른 3경로(`_reseed_adapter`·`daily.py`·`analysis.py`)는 모두 `clear_board` 선행 확인, 어댑터 `_replay.plays`도 비워져 재기동 시 옛 수 재적용 없음. 커밋 `056ca3b` push, 이슈 코멘트·`in-progress` 해제. 이슈 부수 사항(#480 고아 `active` 행 정리)은 범위 밖으로 두고 PR 본문에 명시. 잡 상태 `ok` 기록.
- 09:00 orchestrator(am) — **전 항목 정상, 신규 버그 0, PR #98 머지 승인 요청(AP-20260916-01) 등재.** 헬스체크 — `/api/health` 200 `{db:true, migrations {current:0020, head:0020, pending:false}}`(`katago_alive:false`는 lazy spawn 정보용), web 200, `com.baduk.api` PID 5743(9/12 13:46 기동 유지, 91시간 무사고)·`com.baduk.web` PID 89541(9/8 23:56 기동 유지), staging 중단(정책상 정상), 디스크 8%, plist drift 없음(exit 0), Claude 인증 `ok D-16`(만료 10-02 23:41). 백업 — `baduk-20260916T040003.db.gz` 4,525,500B(9/15 4,522,654B, +2,846B) 04:00:04 완료, `find -mtime +1` 출력 없음, 보존 정리 9/2분, daily 14 / weekly 8 / monthly 4, 복원 드릴 `integrity_check` ok·테이블 11 = **77회 연속** 통과. 버그 스캔 — `.err` **+0줄**(235,098줄, mtime 9/15 13:50 유지 — 마지막 항목은 이미 #97로 등록된 `KataGo rejected play B D4: illegal move` 트레이스백 + #481 WS), **트레이스백 신규 0·IntegrityError 0·locked 0**. `.log` 323,313 → 323,609줄(**+296**) 분포 200 287·404 9, **5xx 0·429 0** — 404 9건은 전부 스캐너 프로브(`181.233.125.233` `/api/session/reset_password`×3·`/api/session/properties`×2 — 백엔드에 없는 경로, GCP `35.228.85.87` `/api/.env`·`/api/v1/.env`·`/api/v2/.env`), 소스는 Cloudflare 모니터 144·127.0.0.1 SSR 139. `baduk-web.err` +0(기준선 3,495, mtime 9/14 13:59). DB — `games max(id)` 481, **9/15 21:00 이후 착수 0**(마지막 수 #481 9/15 13:49 KST, 진행 중 0, #480은 0수 고아 `active`), `visit_hits` 579(+0/12h), `pro_games` 4311. 열린 `bug` 이슈 #97 1건뿐이고 PR #98이 대응 중이라 중복 없음 → **신규 이슈 0건**. 백로그 트리아지 — 미분류 이슈 0건, 할 일 없음. PR 감시 — 열린 PR 1건 [#98](https://github.com/rarebirds-svg/quite-baduk/pull/98)(9/16 04:34 KST 생성, CI 4잡 SUCCESS, `MERGEABLE`/`CLEAN`, `merge-tree` 충돌 0, 정체 아님) = **열린 PR 모두 정상, 주의 PR 0건**. `main` CI 최근 3런 success(최근 9/15 17:04 UTC, `eb90a0e`). 배포 — `git rev-list --count HEAD..origin/main` **0**, 라이브 api `c0d8d05` 이후 유입은 콘텐츠(`eb90a0e` FAQ·`157e202` news-hook)뿐이라 force-dynamic으로 재빌드·재기동 불필요, **미반영 0**. 잡 상태 4/4 `ok`(dev-cycle 04:35·content-draft 02:04·analytics-weekly 9/13·news-hook 9/13). watchdog — 9/16 WD 마커 0건(마지막 WD-20260915-090345, 9/14 결손 파생분 종료). 승인 — 대기 0건이던 큐에 **AP-20260916-01**(PR #98 머지 + pull + api 재기동, 🟡) 신규 등재, 다이제스트 승인 카드로 발송. 관찰 유지(이슈 미등록) — 슬롯별 KataGo 사용 직후 소멸·재spawn, 게시본 `ai-strength-levels.md` "입문부터" 문구 검토(content-draft 02:00 부수 발견).
- 09:03 orchestrator(am) 보고 — am 다이제스트 발송 성공(`send.py` exit 0, 전체 ✅, 승인 카드 AP-20260916-01 1건 동봉, 마커 `~/.ops-report/markers/inkbaduk-am-2026-09-16` 생성). 페이로드 사본 `/tmp/digest-20260916-am.json`.
