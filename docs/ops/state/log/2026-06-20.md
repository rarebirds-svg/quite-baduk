# 운영 로그 — 2026-06-20

## 02:00 자율 버그 사이클 (dev-cycle)

### 이슈 선택
- `/opt/homebrew/bin/gh issue list --state open --json number,title,labels` → 열린 이슈 0건.
- `bug`/`small` 라벨 적격 이슈 없음 → **처리할 버그 없음**.
- 종료. worktree·브랜치 생성 안 함.

### 비고
- `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 조회.
- 6/13~6/19과 동일하게 신규 열린 이슈 없음.

### Telegram 보고
- telegram reply/send 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`(`ops/ops.env`)로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=True, message_id=2264).

## 02:00 콘텐츠 초안 사이클 (content-draft)
- most-behind 비율 동률(glossary 14/18, faq 7/9 모두 0.778) → 타이브레이크 glossary. 미작성 후보 알파벳 첫 항목 `okjip` 선택.
- 초안 작성 → korean-copy-qa: 사실 오류 1건(중앙 옥집은 대각선 2곳 이상 필요인데 "하나라도"로 기술) + 문체 3건 지적 → 본문 수정 반영. 게시 보류 없음.
- web/content/glossary/okjip.md 게시(커밋 79efa46, main push), Telegram 사후 보고 발송.

## 12:00 오케스트레이터 정기 실행

### 헬스체크 🟢
- prod backend `/api/health` 200 (db:true, katago_alive:false=lazy spawn·정보용), prod web 200.
- launchctl: com.baduk.api PID 923 / com.baduk.web PID 912 가동.
- 디스크 6%, plist drift 없음(`sync-launchd.sh --check` exit 0).
- staging: 중단 (정책상 정상, 경보 아님).

### 백업 검증 🟢
- 최신 baduk-20260620T040001.db.gz (오늘 04:00, mtime+1 미해당=신선).
- 티어 daily 14 / weekly 4 / monthly 1.
- 복원 드릴: integrity_check=ok, tables=7. 정상.

### 버그 스캔 🟢
- `~/Library/Logs/baduk-api.err` 마지막 수정 Jun16 23:22 — 이후 신규 stderr 없음.
- `TypeError: bytes is not JSON serializable`(errors.py:21 val_handler) 2회, 모두 Jun16 이전·stale. 최근 발생 아님([[bug-scan-verify-recency]] 적용) → 이슈 미생성.
- 신규 버그 없음.

### 백로그 트리아지 🟢
- 미분류(no:label) 이슈 0건 — 처리 없음.

### PR 감시 🟢
- 열린 PR 0건. 주의 PR 없음.

### Telegram 보고
- telegram reply/send 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`로 Bot API `sendMessage` 직접 호출 → **발송 성공**(ok=true, message_id=2268).

### 상태 갱신
- dashboard 스택·백업·콘텐츠 인덱스(글로서리 15/18·FAQ 7/9, 6/19 기준 stale였음) 갱신.

## 18:00 오케스트레이터 정기 실행

### 헬스체크 🟢
- prod backend `/api/health` 200 (db:true, katago_alive:false=lazy spawn·정보용), prod web 200.
- launchctl: com.baduk.api PID 923 / com.baduk.web PID 912 가동.
- 디스크 6%, plist drift 없음(`sync-launchd.sh --check` exit 0).
- staging: 중단 (정책상 정상, 경보 아님).

### 백업 검증 🟢
- 최신 baduk-20260620T040001.db.gz (오늘 04:00, 신선).
- 티어 daily 14 / weekly 4 / monthly 1.
- 복원 드릴: integrity_check=ok, tables=7. 정상.

### 버그 스캔 🟢
- `~/Library/Logs/baduk-api.err` 마지막 수정 Jun16 23:22 — 이후 신규 stderr 없음.
- `TypeError: bytes is not JSON serializable` 2회(라인 221457·221549) 재확인. 트레이스백 라인번호가 현행 코드와 불일치(val_handler가 옛 line 21에서 직접 JSONResponse 호출) → **수정 전 코드의 stale 로그**. 수정 커밋 `ce9d745`(6/11) 이후 PID 923(6/13 22:49 기동)이 수정본 가동, 이후 재발 0건. `errors.py:_safe_validation_errors`가 bytes를 치환 디코딩으로 방어. [[bug-scan-verify-recency]] 적용 → 이슈 미생성.
- 신규 버그 없음.

### 백로그 트리아지 🟢
- 미분류(no:label) 이슈 0건 — 처리 없음.

### PR 감시 🟢
- 열린 PR 0건. 주의 PR 없음.

### Telegram 보고
- telegram reply/send 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`로 Bot API `sendMessage` 직접 호출 → **발송 성공**(ok=true, message_id=2269).
