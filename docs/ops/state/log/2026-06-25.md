# 2026-06-25 운영 로그

- 02:00 자율 버그 사이클 (dev-cycle)
  - 이슈 선택: `gh issue list --state open` 결과 열린 이슈 0건. `bug`·`small` 적격 이슈 없음.
  - 참고: 최근 닫힌 이슈 #39~#42(database-locked·WS 계열) 모두 CLOSED, 신규 미분류 버그 없음.
  - 결과: **처리할 버그 없음.** 코드 변경·커밋·핸드오프 없이 종료.
  - Telegram 보고: `ops/notify-telegram.sh`로 "처리할 버그 없음" 요약 발송 성공(exit 0).

- 12:30 오케스트레이터 정기 실행 (healthcheck·backup-verify·bug-scan·backlog-triage·pr-watch)
  - **헬스체크**: prod backend·web 200 OK, db OK, katago_alive:true, 디스크 6%, plist drift 없음(exit 0). launchctl PID api 923 / web 912. staging 중단(정책상 정상). → 정상.
  - **백업 검증**: 최신 baduk-20260625T040004.db.gz (어제 04:00 이후 신선), integrity_check=ok, 7 tables. daily/weekly/monthly = 14/5/1. → 정상.
  - **버그 스캔**: api.err에서 `TypeError: Object of type bytes is not JSON serializable` 2회 관찰(2026-06-24, 구 프로세스 33051, 이후 4회 재시작됨). 최신 라이브 프로세스(923)에는 미재현이나 코드 경로 잔존 → latent 버그로 판단. `POST /api/session`에 잘못된 Content-Type 본문이 들어오면 422 대신 500 크래시. 중복 없음 → **이슈 #53 생성** (label: bug).
  - **백로그 트리아지**: 미분류 이슈 0건 — 할 일 없음.
  - **PR 감시**: 열린 PR 0건 — 주의 PR 없음.
  - **Telegram 보고**: `ops/notify-telegram.sh`로 정기 점검 요약 발송 성공(exit 0). 헬스 OK·보류 승인 0·CWI 06-21(new=200)·분석 2026-W25 포함.

- 18:30 오케스트레이터 정기 실행 (healthcheck·backup-verify·bug-scan·backlog-triage·pr-watch)
  - **헬스체크**: prod backend·web 200 OK, db OK, katago_alive:true, 디스크 6%, plist drift 없음(exit 0). launchctl PID api 923 / web 912. staging 중단(정책상 정상). → 정상.
  - **백업 검증**: 최신 baduk-20260625T040004.db.gz(신선), integrity_check=ok, 7 tables. daily/weekly/monthly = 14/5/1. → 정상.
  - **버그 스캔 + 이슈 #53 종료**: 12:30 run이 만든 #53은 **오탐으로 확인되어 종료**. 근거 — (1) 수정 커밋 `ce9d745`(6/11)가 `errors.py`에 `_safe_validation_errors()`(bytes custom_encoder) 추가, HEAD에 포함. (2) 현 prod PID 923은 6/13 22:49 기동(수정 이후)이며 기동 라인(err 221583) 이후 동일 TypeError 0건. (3) 문제의 TypeError 라인(221457·221549)은 923 기동 라인보다 *앞서고* 트레이스백이 수정 전 라인 구조를 보임 → Jun 13 재시작 이전 구버전 기록. (4) 회귀 테스트 `test_non_json_body_returns_422_not_500` 통과. '12:30 run의 코드 경로 잔존' 판단은 fix가 이미 들어간 걸 놓친 것. → bug-scan 신규 0건, **이슈 #53 close(completed)**. recency-verify 메모리에 재발 사례 추가.
  - **백로그 트리아지**: 미분류 이슈 0건 — 할 일 없음.
  - **PR 감시**: 열린 PR 0건 — 주의 PR 없음.
  - **Telegram 보고**: `ops/notify-telegram.sh`로 정기 점검 요약 발송 성공(exit 0). 헬스 OK·보류 승인 0·#53 오탐 종료·CWI 06-21(new=200)·분석 2026-W25 포함. (telegram 플러그인 reply 도구는 이 세션에 미노출 — 검증된 notify 스크립트 경로 사용.)
