# 2026-08-15

- 02:00 dev-cycle: **처리할 버그 없음 — 열린 이슈 0건.** 백로그 고갈 7일째(8/9~8/14에 이어). 선점·워크트리·구현·push·PR 단계 전부 미실행, 커밋 0건, 라벨 조작 0건이다.
  - 빈 결과가 조회 실패가 아님을 확인했다. `gh auth status` 정상(rarebirds-svg, keyring 토큰 유효, scopes `repo`·`workflow` 포함), 리포는 `rarebirds-svg/quite-baduk`으로 해석되며 `gh repo view --json issues`의 `totalCount`가 **0**이다. `--state all`은 #78·#76·#74·#70·#65·#64·#58·#56·#54·#53을 정상 반환하므로 API 경로는 살아 있다 — `bug`·`small` 적격 후보 자체가 없는 것이지 조회가 실패한 것이 아니다.
  - 잔여물 점검. 열린 PR **0건**이라 사람에게 넘길 🟡 머지 대기 항목이 없고, `.worktrees/dev-cycle`은 `status --porcelain=v1 -b`가 브랜치 행 하나만 출력해 **클린**이다(`fix/issue-78` 체크아웃, upstream과 동기 — #78은 이미 CLOSED라 잔여 작업 아님).
  - **⚠️ 신규 관찰 — 로컬 `main`이 `origin/main`보다 3커밋 앞서 있다(push 정체).** `origin/main` tip은 `ea53b81`(8/13 09:00 사이클)에 멈춰 있고, 그 뒤 오케스트레이터 ops 커밋 3건 `be1cd64`(8/13 21:00)·`09ece53`(8/14 09:00)·`1aef335`(8/14 21:00)이 **로컬에만 있다**. 커밋은 정상 생성됐으나 push 단계가 8/13 21:00 사이클부터 사흘째 누락된 것으로 보인다.
    - 배포 영향은 없다. 반대 방향 갭 `HEAD..origin/main` = **0**이라 라이브 작업 트리가 원격보다 뒤처진 상태는 아니고, 미push분은 전부 `docs/ops/state/**` 운영 로그라 앱 코드가 아니다.
    - **dev-cycle이 push하지 않고 남긴다.** `main` push는 코드·설정이든 ops 문서든 dev-cycle 권한 밖(🟡 행위 규범)이고, 이 커밋들의 소유자는 09:00 오케스트레이터다. 오케스트레이터가 다음 사이클에서 push 실패 원인(자격증명·네트워크·단순 단계 누락)을 확인하고 처리해야 한다.
  - 로컬 브랜치 3종(`feat/format-game-result`·`feat/i18n-ja-zh`·`fix/auto-resign-respect-score-margin`)은 8/13 규명대로 `[origin/…: gone]` = **push됐다가 origin에서 삭제된** 2026-04-30 수동 실험분이며 dev-cycle이 흘린 산출물이 아니다(`[[dev-cycle-unpushed-branches]]`가 경계하는 패턴과 무관). 손대지 않았고 삭제 여부는 사람 판단에 남긴다.
  - `ops/report-job-status.sh dev-cycle ok "열린 이슈 0건 — 무작업"` 기록. `fail`이 아니므로 새벽 즉시 경보는 보내지 않았고, Telegram 발송은 프롬프트대로 생략했다 — 09:00 다이제스트가 `자동화` 행에 합산한다.
  - 로그 파일은 커밋하지 않고 둔다 — 8/10~8/14 선례대로 `docs/ops/state/**` 커밋은 09:00 오케스트레이터가 일괄 수행한다.
  - 오늘은 **토요일**(Weekday 6)이라 content-draft(`Weekday 3·6` = 수·토)가 예정돼 있다. 해당 사이클 결과는 별도 항목으로 이 파일에 추가된다.

- 02:01 content-draft: **처리할 토픽 없음** — 글로서리 18/18 · FAQ 9/9 시드 소진 상태 유지. `ops/report-job-status.sh content-draft ok` 기록.

## 09:00 오케스트레이터 사이클 (`am`)

러닝북 5종(헬스체크·백업 검증·버그 스캔·백로그 트리아지·PR 감시)을 전부 수행했다. **전 항목 정상**이라 경보는 보내지 않았다.

### 서비스

- backend `{"status":"ok","db":true,"katago_alive":false}` **200**, web :3000 **200**, 공개 도메인 `inkbaduk.com` **200**.
- `launchctl` PID api **88640** / web **88644**, `lstart Aug 8 15:48:51` = **161시간(6.7일) 무재기동**.
- 판정 메모 유지 — `com.baduk.api` 행 2번째 컬럼 `-15`는 실패가 아니라 직전 인스턴스의 SIGTERM(8/8 `stack.sh restart prod`) 기록이다. 현재 상태는 1번째 컬럼 PID가 결정한다.
- `katago_alive:false`는 러닝북 명시대로 정보용이다 — 마지막 AI 호출(8/11 20:45 KST) 이후의 lazy 상태이며 경보 신호가 아니다.
- 디스크 **6%**, `sync-launchd.sh --check` exit 0·무출력(drift 없음), staging 중단 = 정책상 정상.

### 백업

- 최신 `baduk-20260815T040005.db.gz` (**3,415,199B**, 5시간 전) — 30h 임계 여유. daily/weekly/monthly = **14 / 8 / 3**.
- 복원 드릴 `integrity_check` = `ok` · 테이블 **9** · `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC로 **라이브 DB와 완전 일치** = 이 델타의 백업 지연 노출 0. **19회 연속 통과**.
- 신규 대국 0인데 3,414,916 → **3,415,199B(+283B)** 자랐다. 증가분은 `visit_hits`(드릴본 **170행**) 등 부수 테이블 유입 = 대국 0국 구간에도 방문이 살아 있다는 별도 증거다.

### 이상 스캔

- **`.err`가 84시간 델타에 단 한 줄도 늘지 않았다 — 일곱 사이클 연속 라인 수 동일.** 총 **225,429행**이 8/12 09:00 이래 여섯 사이클과 완전 동일하고 mtime도 `8/11 20:46` 불변이다.
- 반면 `.log`는 236,576 → **236,771행(+195)** 으로 살아 있다. 조합의 의미는 트래픽 0이 아니라 **에러 없는 트래픽**이다(`[[api-log-stdout-vs-stderr]]`). 증가폭은 +382 → +179 → +130 → **+195**로 3사이클 연속 감소가 이번에 반등했다.
- 최근 3,000줄 상태코드 200 **2,848** / 204 59 / 401 57 / 404 32 / 201 2. **5xx는 최근 20,000줄에 0건** — 마지막 5xx는 175,237행으로 파일 끝에서 **61,534줄** 떨어져 있다(`[[bug-scan-verify-recency]]`).
- **#78 무재발 11일째.** 어드민 401 밴드 마지막 발생은 **232,613행**으로 고정인데 파일만 자라 **최근 4,158줄에 어드민 401 0건**(8/14 21:00엔 3,963줄). 401 57건 실측 내역 = `/api/session` 55 + `/api/stats` 2로 어드민 401은 0건이다.
- **`database is locked` 재발 0건 — 11일째.** 마지막 발생 `.err` **225,347행** < 현 기동 마커 **225,423행**(`[[database-locked-genmove]]`). 근본 수정(수 커밋 후 genmove)은 미완이라 회귀 감시는 유지한다.
- 404 32건은 전부 스캐너 봇이다 — `/api/.env`·`/api/env`·`/api/config`·`/api/v1/credentials` 등 시크릿 탐침. **이번엔 SSRF 탐침이 섞였다**(`/api/webhook?url=http://169.254.169.254/latest/meta-data/iam/security-credentials/`, `/api/v1/fetch?url=…` 각 2건 = 클라우드 인스턴스 메타데이터 노림수). 전부 **404 거절**이고 해당 라우트는 애초에 존재하지 않으며, prod는 맥미니라 169.254.169.254 메타데이터 서비스 자체가 없다 — 조치 불요. 실제 앱 라우트 404는 0건이다.
- 신규 GitHub `bug` 이슈 **0건 생성**(후보 없음).

### 큐 4종

- 열린 이슈 **0건**(`--label bug` 0, 미분류 0) — 백로그 공백 이레째.
- 열린 PR **0건** — 8/8 06:47 #79 머지 이후 열흘째 신규 PR 없음. CI 실패·충돌·정체 각 0건.
- 승인 대기 **0건** — `pending-approvals.md` '## 대기 중' = `(없음)`. 이번 사이클 수행분은 전부 🟢 자율 범위라 신규 🟡 등재 없음.
- 배포 갭 **0커밋** + 라이브 반영 확인. `BUILD_ID`는 8/8 15:48 그대로지만 그 이후 `web/`·`backend/` 커밋이 테스트 파일 1건뿐이라 번들이 HEAD와 동등하다.

### ✅ 처리 — push 정체 3커밋 해소

02:00 dev-cycle이 관찰해 오케스트레이터에게 넘긴 건이다. 로컬 `main`이 `origin/main`(`ea53b81`, 8/13 09:00)보다 **3커밋 앞선** 상태였다 — `be1cd64`·`09ece53`·`1aef335`, 전부 `docs/ops/state/**` 운영 로그(9파일 +380/−11).

- **`git push origin main` 성공** — `ea53b81..1aef335`, exit 0, 이후 `origin/main..HEAD` = **0**.
- **원인은 자격증명·네트워크가 아니라 단순 단계 누락이다.** 재시도가 인증 프롬프트 없이 즉시 통과했으므로 8/13 21:00~8/14 21:00 세 사이클이 커밋 후 push를 빠뜨린 것이다.
- 등급 판단 — `origin/main` tip 자체가 8/13 사이클의 ops 커밋인 데서 보듯 `docs/ops/state/**` 운영 로그의 `main` push는 확립된 관행이며, 코드·설정 변경이 아니라 🟡 승인 대상이 아니다.
- **재발 방지는 관행 확인으로 둔다** — 매 사이클 마지막에 `git rev-list --count origin/main..HEAD` = 0을 확인한다.

### 기록해 둘 것 두 가지

1. **신규 대국이 84시간째 없다.** `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC로 일곱 사이클 연속 동일. 인프라는 전 항목 정상이므로 장애가 아니라 **수요 부재**이고, 3주 연속 사용량 감소(18 → 5 → 1국)의 연장선이다. 제품 판단 영역이라 경보로 올리지 않는다. 다만 `.log` 증가폭 반등(+130 → +195)과 `visit_hits` 170행은 방문 자체는 얇게나마 유지되고 있음을 보여준다 — 내일 8/16 W33 리포트에서 확인할 것.
2. **`jobs/analytics-weekly.json` 부재는 오늘까지는 정상이다.** `com.inkbaduk.analytics-weekly`는 `Weekday 0` = 일요일 주 1회 잡이고 마지막 실행(8/9 09:01)이 상태파일 방식(`report-job-status.sh`, 같은 날 19:38 도입) 이전이라 아직 한 번도 이 방식으로 돈 적이 없다. **내일 8/16(일) 실행 후에도 파일이 없으면 그때는 배선 결함**으로 전환한다. 같은 날 CWI 주간 ingest(일요일 03:00)도 예정돼 있다.

## 21:00 오케스트레이터 사이클 (`pm`)

러닝북 5종 전부 수행. **전 항목 정상, 신규 incident 0건.**

### 서비스

- backend `{"status":"ok","db":true,"katago_alive":false}` **200**, web **200**, 공개 도메인 `inkbaduk.com` **200**.
- `launchctl` PID api **88640** / web **88644**, `lstart Aug 8 15:48:51` = **173시간(7.2일) 무재기동**.
- 디스크 **6%**, `sync-launchd.sh --check` exit 0·무출력(drift 없음), staging 중단 = 정책상 정상.
- 판정 메모 유지 — `com.baduk.api` 행 2번째 컬럼 `-15`는 직전 인스턴스의 SIGTERM 기록이지 실패가 아니다. `katago_alive:false`도 러닝북 명시대로 정보용이다(마지막 AI 호출 8/11 20:45 이후 lazy).

### 백업

- 최신 `baduk-20260815T040005.db.gz` (**3,415,199B**) — 09:00과 같은 파일로 17시간 전, 30h 임계 미달. daily/weekly/monthly = **14 / 8 / 3**.
- 복원 드릴 `integrity_check` = `ok` · 테이블 **9** — **20회 연속 통과**. 드릴본 `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC가 라이브 DB와 일치.
- 다음 생성은 8/16 04:00. 같은 날 03:00 CWI 주간 ingest가 예정돼 있으므로 8/16 백업은 크기 급증이 예상되며 이는 정상이다(`[[backup-size-spike-weekly-ingest]]`).

### 이상 스캔

- **`.err`가 96시간 델타에 단 한 줄도 늘지 않았다 — 여덟 사이클 연속 라인 수 동일.** 총 **225,429행**이 8/12 09:00 이래 일곱 사이클과 완전 동일하고 mtime도 `8/11 20:46` 불변이다.
- `.log`는 236,771 → **237,734행(+963)** 으로, 여덟 사이클 만에 처음으로 세 자릿수 후반대 증가다(직전 +195). 조합의 의미는 여전히 **에러 없는 트래픽**이다(`[[api-log-stdout-vs-stderr]]`).
- 최근 3,000줄 상태코드 200 **2,902** / 401 34 / 204 33 / 404 28 / 201 2. **5xx는 최근 20,000줄에 0건** — 마지막 5xx는 175,237행으로 파일 끝에서 **62,497줄** 떨어져 있다(`[[bug-scan-verify-recency]]`).
- **#78 무재발 12일째.** 어드민 401 밴드 마지막 발생은 **232,613행** 고정인데 파일만 자라 **최근 5,121줄에 어드민 401 0건**(09:00엔 4,158줄). 401 34건 실측 내역은 전부 `/api/session`이다.
- **`database is locked` 재발 0건 — 12일째.** 마지막 발생 `.err` **225,347행** < 현 기동 마커 **225,423행**(`[[database-locked-genmove]]`). 근본 수정(수 커밋 후 genmove) 미완이라 회귀 감시는 유지한다.
- 404 28건은 전부 스캐너 봇이다 — `/api/.env`·`/api/v1/env`·`/api/v1/credentials`·`/api/v1/config` 등 시크릿 탐침과 09:00에 이어 SSRF 탐침(`/api/webhook?url=http://169.254.169.254/…`·`/api/v1/fetch?url=…` 각 2건)이 반복됐다. 전부 404 거절, 해당 라우트 부재, prod는 맥미니라 메타데이터 서비스 자체가 없다 — 조치 불요. 실제 앱 라우트 404는 0건.
- 신규 GitHub `bug` 이슈 **0건 생성**(에러 후보 없음).

### +963 델타의 정체를 분해했다 — 사람 방문이 아니다

이번 증가폭 반등은 09:00에 "방문 반등 가능성"으로 적어 둔 것과 **다른 원인**이었다. 963줄을 요청자별로 분해했다.

- **773건 = `127.0.0.1` → `/api/spectate/pro/*`.** Next.js SSR이 프로 기보 상세를 서버사이드 fetch한 것으로, 크롤러가 `/spectate/pro/<id>`를 순차 순회(79·94~99·939·3126 …)한 결과다.
- **157건 = `/api/health`** (외부 141 + 로컬 16). 외부 141건은 공개 도메인 경유 Cloudflare Worker 모니터 프로브다(`[[stability-architecture]]`).
- 나머지는 sitemap 9 · themes 7 · 월간 픽 18 등 크롤 부수 요청.
- **실제 사람 방문(`POST /api/analytics/hit`)은 이 12시간 델타에 단 1건이다.**

**따라서 09:00 로그의 "방문 자체는 얇게나마 유지" 판단은 이번 실측으로 정정한다.** `visit_hits` 일별 실측은 8/8 **17** → 8/9 12 → 8/10 5 → 8/11 10 → 8/12 9 → 8/13 6 → 8/14 3 → 8/15 **1**로 **일주일 연속 하강**이며, `.log` 증가폭은 사람 트래픽 지표가 아니라 봇·SSR·헬스프로브 지표다. 대국 0국과 방문 감소는 같은 방향을 가리킨다.

- 부수 확인 — 월간 픽 요청에 `2058-01`~`2058-12` 같은 미래 월이 섞여 있으나 **버그가 아니다.** `/api/spectate/pro/pick/monthly/2058-01`은 200과 함께 실재 기보(id 2709, `2019-01-28`)를 반환한다. 이 엔드포인트는 날짜 필터가 아니라 yyyymm을 키로 한 결정적 선택이므로 임의 월에 대해 정상 동작한다. `pro_games`의 `game_date` 최대 연도는 **2026**으로 미래 날짜 오염도 없다.

### 큐 4종

- 열린 이슈 **0건**(`--label bug` 0, 미분류 0) — 백로그 공백 여드레째. 트리아지 대상 0건.
- 열린 PR **0건** — 8/8 06:47 #79 머지 이후 열흘째 신규 PR 없음. CI 실패·충돌·정체 각 0건.
- 승인 대기 **0건** — `pending-approvals.md` '## 대기 중' = `(없음)`. 이번 사이클 수행분은 전부 🟢 자율 범위라 신규 🟡 등재 없음.
- 배포 갭 **0커밋** + 라이브 반영 확인. `BUILD_ID`는 8/8 15:48 그대로지만 그 이후 `web/`·`backend/`·`e2e/` 커밋은 테스트 파일 1건(`e8fc5fd`)과 devDependency 등재 1건(`1403074`)뿐이라 런타임 번들은 HEAD와 동등하다 — 재빌드·재기동 불요.

### 잡 상태

- `dev-cycle` **ok** (`열린 이슈 0건 — 무작업`, 8/15 02:01), `content-draft` **ok** (`처리할 토픽 없음`, 8/15 02:01). 오늘은 토요일이라 content-draft(`Weekday 3·6`)가 예정대로 돌았다.
- `analytics-weekly.json`은 여전히 부재이며 **오늘까지는 정상**이다(위 09:00 항목과 동일 근거). **내일 8/16(일) 09:00 실행 후에도 없으면 배선 결함으로 전환한다** — 이 판정은 다음 사이클로 넘긴다.
- watchdog는 이 구간 전부 `신규 incident 0 건` · `[api] OK` · `[web] OK`.

### 내일(8/16, 일) 확인할 것

1. **W33 주간 리포트** — 대국 0국이 확정적이므로 4주 연속 감소(18 → 5 → 1 → 0)가 기록될 전망이다. `visit_hits` 주간 합계도 함께 볼 것.
2. **CWI 주간 ingest**(03:00) — `pro_games` 3311에서 증가 여부, sitemap·백업 크기 정합.
3. **`analytics-weekly.json` 최초 생성 여부**(09:00) — 미생성이면 배선 결함 전환.
