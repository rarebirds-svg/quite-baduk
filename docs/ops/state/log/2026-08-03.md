# 2026-08-03

- 02:00 dev-cycle: **처리할 버그 없음 — 이슈 0건·PR 0건으로 이틀 연속 무작업.**
  - **적격 이슈 0건.** `gh issue list --state open --json number,title,labels` 결과가 빈 배열(`[]`)이다. 라벨 필터(`bug`·`small` 있고 `feature`·`in-progress` 없음) 이전에 **열린 이슈 자체가 0건**이라 선택·선점 단계가 성립하지 않는다. `gh issue list --state all --label in-progress`도 빈 결과라 뗄 라벨도 없다(중단된 이전 사이클의 잔여 선점 없음).
  - **열린 PR 0건.** `gh pr list --state open`이 빈 결과다. 어제(8/2)와 동일하게 사람 머지를 기다리는 항목이 없다.
  - **작업 트리가 `origin/main`과 동일.** `git fetch origin` 후 `git rev-list --count HEAD..origin/main` = **0**, 양쪽 모두 `fe4d755`다. 8/2 사람 지시 수동 배포로 4커밋 승급이 완료된 뒤 새 유입이 없다 — 배포 갭 없음(`[[merge-is-not-deploy]]`).
  - **워크트리 미사용.** 착수할 이슈가 없어 `.worktrees/dev-cycle` 브랜치 생성·커밋·push·PR 생성을 하지 않았다. 코드 변경 0건(이 로그 파일 제외).
  - **Telegram 발송 완료 — message_id 2499.** `reply` 도구는 `ToolSearch`로 실제 조회했으나 이 세션에 미노출이라(MCP 미로드) Bot API 직접 호출로 발송했다. 한글 본문을 `/tmp` 파일에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"`로 보냈다 — 이번엔 `-F` 오용 없이 한 번에 성공했다(`[[dev-cycle-telegram-botapi]]`).
  - **범위 판단.** 이슈 큐가 비었다는 이유로 임의의 리팩터링·개선을 자율 착수하지 않았다. dev-cycle은 이슈 큐를 소비하는 잡이고, 이슈를 자동 생성하는 별도 bug-scan 잡은 `ops/`에 없다. 사람이 이슈를 열거나 오케스트레이터·watchdog가 장애를 이슈로 승격하기 전까지 이 잡은 계속 무작업으로 종료한다.

- 12:00 orchestrator: **러닝북 5종 전부 통과. 10일 묵은 배포 갭이 해소됐고, 머지된 4건 전부를 라이브 실증했다.**

  ### 헬스체크 — 정상
  - prod backend `{"status":"ok","db":true,"katago_alive":true}` **200**, prod web **200**. launchctl `com.baduk.api` PID **93501** / `com.baduk.web` PID **93522** 둘 다 PID 보유 = 가동.
  - **PID가 512/497 → 93501/93522로 바뀌었다 — 장애가 아니라 8/2 22:11 사람 수동 배포다.** `ps lstart` = `Sun Aug 2 22:11:37`(api) / `22:11:42`(web). AP-20260802-01 실행 절차 3단계와 정확히 일치한다. 11사이클 이어지던 무재기동 구간(4일 18시간)은 이것으로 종료.
  - `katago_alive:**true**` — 이번엔 true다(경보 신호 아님, 러닝북 L19~23).
  - 디스크 **6%**(경보선 90% 대비 여유), `ops/sync-launchd.sh --check` **exit 0·무출력** = plist drift 없음.
  - staging `중단됨` = 정책상 정상(경보 아님).
  - watchdog `ops/check-staleness.sh` — `신규 incident 0 건`. **이것이 #72 새 판정 로직의 첫 정상 실행이다.**
  - 신규 incident **0건**.

  ### 백업 검증 — 정상
  - 최신 `baduk-20260803T040005.db.gz` (**8/3 04:00**, 3,281,476B ≈ 3.13MB). 8/2분 → 8/3분 회전 정상, `find -mtime +1` 미해당 = 신선.
  - 복원 드릴 `PRAGMA integrity_check` = **ok**, 테이블 **9개**. 티어 **daily 14 / weekly 8 / monthly 3**(전 사이클과 동일, daily 14는 보존 상한).

  ### 버그 스캔 — 신규 이슈 0건
  - **현 기동 이후 트레이스백 0건.** `Started server process [93501]` 마커가 `.err` line **225406**이고 전체가 225,409줄이라 기동 이후 내용은 **4줄**(uvicorn 기동 라인)뿐이다. `.err` mtime도 8/2 22:11에서 멈춰 있다 = **재기동 후 14시간 무에러**.
  - **`database is locked` 16건은 전부 배포 *이전* 구간이다.** 마지막 발생이 line **225347** < 기동 마커 **225406**. 카운트가 아니라 발생 시점으로 판정했다(`[[bug-scan-verify-recency]]`) — 누적 카운트만 봤다면 #41·#42처럼 오탐 이슈를 또 만들 뻔했다. #66 배포로 닫힌 것으로 본다.
  - 열린 `bug` 이슈 **0건**이라 중복 차단 단계는 공회전. 신규 이슈 생성 0건.

  ### 백로그 트리아지 — 할 일 없음
  - `gh issue list --state open --search "no:label"` = 빈 배열. 애초에 열린 이슈 자체가 0건이라 분류 대상 없음. "사람 분류 필요" 항목 0건.

  ### PR 감시 — 열린 PR 0건
  - `gh pr list --state open` 빈 결과. CI 실패·충돌·정체 **0건**. 8/1 23:00 4건 머지 이후 신규 PR 없음.

  ### 🟢 이번 사이클의 핵심 — AP-20260802-01 완료 및 4건 라이브 실증
  사람이 **8/2 22:10~22:11**에 실행 절차 1~3단계를 수동 수행했다. 6단계 판별 검사까지 포함해 전부 검증했다.
  - **#71 (settings deny `Write`→`Edit`) — 결정적 증거 확보.** 이번 세션 시작 로그(`orchestrator-runs.log` **12:00:05**)에 `Write(...)` deny 경고 2건이 **처음으로 사라졌다**. 8/2 12:00·18:00 두 사이클 연속 재출력되던 바로 그 경고다. `.claude/settings.json` 실측도 `Edit(...)` 형태 확인. **11일간 실효 없던 prod DB·KataGo 모델 가드레일이 살아났다**(`[[settings-deny-write-vs-edit]]`).
  - **#72 (watchdog 성공 마커 판정)** — `check-staleness.sh` 정상 실행, 신규 incident 0건. OPS-20260728-01류 크래시 루프 무음 사각지대가 닫혔다.
  - **#66 (프로 기보 조회수 원자적 UPDATE)** — `/spectate/pro/1` **200**, API 정상 JSON. 재기동 후 14시간 **5xx 0건**.
  - **#73 (용어집 board 다이어그램)** — `/glossary` 200, `/glossary/sahwal`에 **`<svg>` 3개 렌더** = 신규 렌더러가 번들에 들어갔음을 확인(`[[glossary-sahwal-slug]]`).

  ### 🟢 #66 부하 실증 — 관측 이래 최고 밀도에서 5xx 0건
  최근 2,000줄 구성 — **프로 상세 1,158건**(직전 최고를 크게 상회) · health 524 · `/api/admin/*` 208 · themes 22 · sitemap 22 · `analytics/hit` 4. 상태코드는 **200 1,771 · 401 215 · 404 9 · 204 4로 5xx 0건**. `.log` 214,219 → **215,231줄**(18시간 +1,012). 원자적 UPDATE가 이 밀도를 무에러로 처리한다.

  ### 주의(경보 아님) 2건
  - **`/api/admin/*` 401 208건** — `summary`·`sessions`·`games`·`engine` 4개 엔드포인트가 정확히 **52회씩** = 어드민 대시보드 폴링 1주기당 4콜이 세션 만료 상태로 반복되는 패턴. 8/2 12:00(580건)과 동일 성질이고 서비스 결함이 아니다. 재로그인으로 해소. incident 미기록.
  - **404 9건 — 스캐너 프로브.** `POST /api/graphql` 3 · `POST /api/gql` 3 · `POST /api` 3. GraphQL 엔드포인트 탐색으로, 본 리포는 GraphQL을 쓰지 않아 라우팅 경로가 없다(전부 404). 종류가 메일설정·자격증명 계열에서 GraphQL로 옮겨간 것만 기록해 둔다.

  ### 📌 문서 부정확 발견 (조치 안 함 — 사람 판단 필요)
  오케스트레이터 세션 프롬프트와 러닝북 5종 전부가 주기를 **"매일 06:30·12:30·18:30·23:30"**(4회/일)로 적고 있으나, **실제 설치된 스케줄은 12:00·18:00 2회/일**이다.
  - `~/Library/LaunchAgents/com.inkbaduk.ops-orchestrator.plist`의 `StartCalendarInterval` = `{12:00}`, `{18:00}` 2개뿐이고, **repo `ops/launchd/` 원본도 동일**하다 → `sync-launchd.sh --check`가 통과하는 게 맞고 **drift가 아니다**.
  - `orchestrator-runs.log` 실행 이력도 7/31·8/1·8/2 전부 `12:00:05`·`18:00:05` 두 건씩뿐이다. 즉 **누락된 실행이 아니라 애초에 예정에 없던 실행**이다 — DRIFT·SLEEP incident로 올리지 않았다.
  - 참고로 `pmset -g` `sleep 0 (sleep prevented by Claude)` = 시스템 슬립 자체가 꺼져 있어 sleep 기인 누락 가설도 성립하지 않는다.
  - **문서를 실제(2회)에 맞출지, plist를 문서(4회)에 맞출지는 사람 판단 사항**이라 자율 변경하지 않았다. plist 수정은 실행 빈도를 2배로 올리는 변경이라 🟡에 가깝다.

  ### 부수 관측
  - **사람 방문이 재개됐다.** `visit_hits` **70행**(8/2 18:00 69행 → +1), 최신 방문 `2026-08-02 20:47:47 UTC` = **8/3 05:47 KST**. 18시간 0건이던 직전 사이클과 달리 실제 사람 유입이 있었다.
  - `games` **336**(#336 8/1 19:15 종료 이후 신규 대국 0건), `pro_games` **3111**(8/2 ingest 이후 불변, 다음 ingest 8/9 일요일).
  - 코드 변경·커밋·푸시·머지 **0건**. 상태 파일(dashboard·일일 로그·pending-approvals)만 갱신.
  - **Telegram 발송 완료 — message_id 2502.** `reply` 도구는 `ToolSearch`로 실제 조회했으나 이 세션에 미노출이라(MCP 미로드) Bot API 직접 호출로 발송했다. 한글 본문을 `/tmp/ops-msg-20260803.txt`에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"`로 보냈다(`[[dev-cycle-telegram-botapi]]`). 인프라 상태 메모리를 근거로 "발송 불가" 사전 판단하지 않고 실제 호출했다.

- 18:00 orchestrator: **러닝북 5종 전부 통과. 배포 후 첫 6시간 델타가 4xx·5xx 0건으로, 12:00에 실증한 4건이 무사고로 안착했다.**

  ### 헬스체크 — 정상
  - prod backend `{"status":"ok","db":true,"katago_alive":true}` **200**, prod web **200**. launchctl `com.baduk.api` PID **93501** / `com.baduk.web` PID **93522** — 12:00과 동일 PID, `ps etime` **19:49:46**(api) / **19:49:41**(web)로 8/2 22:11 배포 이후 **무재기동 19시간 50분**.
  - `katago_alive:true` 2사이클 연속(경보 신호 아님, 러닝북 L19~23).
  - 디스크 **6%**(경보선 90%), `ops/sync-launchd.sh --check` **exit 0·무출력** = plist drift 없음. staging `중단됨` = 정책상 정상.
  - watchdog `check-staleness.sh` — 최근 3사이클 전부 `신규 incident 0 건`, `[api] OK` `[web] OK`. #72 판정 로직 안정 동작.
  - 신규 incident **0건**.

  ### 백업 검증 — 정상
  - 최신 `baduk-20260803T040005.db.gz` (**8/3 04:00**, 3,281,476B ≈ 3.13MB) — 생성 14시간 전, `find -mtime +1` 미해당 = 신선.
  - 복원 드릴 `PRAGMA integrity_check` = **ok**, 테이블 **9개**. 12:00에 이어 **2회 연속 ok**. 티어 **daily 14 / weekly 8 / monthly 3**(불변).

  ### 버그 스캔 — 신규 이슈 0건
  - **`.err`가 12:00과 완전히 동일하다** — 225,409줄·mtime **8/2 22:11**로 둘 다 정지. 재기동 후 **19시간 50분 트레이스백 0건**.
  - `database is locked` 마지막 발생은 여전히 line **225347** < 현 기동 마커 **225406** = 전부 배포 *이전* 구간. 카운트가 아닌 발생 시점으로 판정했다(`[[bug-scan-verify-recency]]`).
  - 열린 `bug` 이슈 0건이라 중복 차단 단계 공회전. 신규 이슈 생성 0건.

  ### 백로그 트리아지 — 할 일 없음
  - `gh issue list --state open --search "no:label"` = 빈 배열. 열린 이슈 자체가 0건이라 분류 대상 없음. "사람 분류 필요" 0건.

  ### PR 감시 — 열린 PR 0건
  - `gh pr list --state open` 빈 결과. CI 실패·충돌·정체 **0건**. 8/1 23:00 4건 머지 이후 신규 PR 없음.

  ### 🟢 12:00→18:00 델타 절단 — 162건 전부 200
  창 크기를 추정하지 않고 12:00 시점 줄 번호(215,231)에서 직접 절단했다. `.log` 215,231 → **215,393줄 = 6시간 162줄**.
  - 구성 — health **78**(약 4.6분 간격 = 모니터 정상) · **프로 상세 68**(≈ **11.3건/h**) · themes 4 · sitemap 4 · 월간 픽 8.
  - **상태코드 200 162건 = 4xx·5xx 0건.** 12:00이 "관측 이래 최고 밀도에서 5xx 0건"으로 본 #66 판정이 다음 6시간에서도 유지됐다.
  - 프로 상세 밀도는 12:00 창의 고밀도 구간(누적 1,226건) 대비 11.3건/h로 내려왔다 — 크롤러 버스트가 잦아든 정상 감쇠이지 장애가 아니다(전부 200).
  - 월간 픽 요청에 **2030-01~2030-07** 미래 월이 섞여 있다. 크롤러가 픽 URL 패턴을 미래로 확장 탐색한 것이며 **전부 200**(라우트가 빈 결과를 정상 응답) — 결함 아님, 기록만 남긴다.

  ### 부수 관측
  - **사람 방문은 6시간 0건.** `visit_hits` **70행**(12:00과 동일, +0), 최신 방문 `2026-08-02 20:47:47 UTC` = 8/3 05:47 KST로 불변. 델타의 `analytics/hit`도 0건이라 위 162건은 봇·모니터 유입이다.
  - `games` **336**(#336 8/1 19:15 종료 이후 신규 대국 0건), `pro_games` **3111**(불변, 다음 ingest **8/9 일요일**).
  - **잡 신선도 전부 정상** — `prune-visits` 8/3 04:15 `deleted=0 retention_days=180`, `analytics-weekly` 8/2 09:01(2026-W31 리포트), `content-draft` 8/1 02:00(시드 소진 무작업), `content-ingest` 8/2 03:12(주 1회 일요일 잡이라 평일 공백은 stale 아님).
  - **보류 승인 0건** — AP-20260802-01이 완료 처리되어 "대기 중"이 비었다.
  - 코드 변경·커밋·푸시·머지 **0건**. 상태 파일(dashboard·일일 로그)만 갱신.
  - **📌 12:00이 올린 "주기 문서 부정확"(문서 4회/일 vs 실제 12:00·18:00 2회/일)은 그대로 미해결이다.** 사람 판단 사항이라 이번에도 자율 변경하지 않았다.
  - **Telegram 발송 완료 — message_id 2503.** `reply` 도구를 `ToolSearch`로 실제 조회했으나 이 세션에 미노출이라(MCP 미로드) Bot API 직접 호출로 발송했다. 한글 본문을 `/tmp/ops-msg-20260803-1800.txt`에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"`로 보냈다(`[[dev-cycle-telegram-botapi]]`). 인프라 상태 메모리를 근거로 "발송 불가"로 사전 판단하지 않고 실제 호출했다.
