# 운영 로그 — 2026-06-11

## 02:00 자율 버그 사이클 (dev-cycle)

### 이슈 선택
- `gh issue list --state open` → 열린 이슈 0건.
- `bug`/`small` 라벨 적격 이슈 없음 → **처리할 버그 없음**.
- 종료. worktree·브랜치 생성 안 함.

## 12:30 오케스트레이터 정기 실행

### 헬스체크 ✅
- prod backend 200 OK (`{"status":"ok","db":true,"katago_alive":false}` — lazy spawn, 정상), prod web 200 OK.
- launchctl: `com.baduk.api` PID 2154 / `com.baduk.web` PID 1825 — 둘 다 가동.
- 디스크 4%, plist drift 없음(`sync-launchd.sh --check` exit 0 무출력).
- staging 중단됨 (정책상 정상, 경보 아님).

### 백업 검증 ✅
- 최신 `baduk-20260611T040005.db.gz` — `find -mtime +1` 미출력(신선), integrity_check=ok, 7 tables.
- 티어 daily 14 / weekly 3 / monthly 1.

### 버그 스캔 — 신규 0
- `~/Library/Logs/baduk-api.err` tail 80: INFO 라인만, 스택 트레이스·예외 없음. err 로그 mtime Jun 10 00:04(최근 신규 에러 없음).
- `games/NaN` 403는 127.0.0.1 로컬 테스트 트래픽 — prod 버그 아님. 신규 이슈 생성 안 함.

### 백로그 트리아지 — 0건
- `no:label` 이슈 `[]` — 미분류 이슈 없음.

### PR 감시 — 주의 1건
- **PR #47** "feat: Capacitor 앱 셸 — Android 우선 스토어 배포 기반" (2026-06-10 생성, 정체 아님, MERGEABLE).
  - CI: backend **FAILURE**, frontend SUCCESS, app-shell-build SUCCESS, e2e SKIPPED.
  - 사유: backend 체크 실패. 머지(🟡)·CI 수정은 dev 작업 — 오케스트레이터는 보고만. 머지 보류 권장.

### Telegram 보고
- `reply` 도구를 세션에서 호출 시도 → 도구 미노출(telegram 플러그인 MCP 미로드, ToolSearch에서 `reply`·`SendMessage` 미발견). 환경변수·플래그 자가진단은 하지 않음.
- 발송하려던 본문:
  ```
  [inkbaduk 운영] 12:30 정기점검 — 헬스 OK
  prod backend·web 200 OK · 디스크 4% · 백업 신선(2026-06-11T04:00, integrity OK)
  버그스캔 신규 0 · 미분류 이슈 0 · 보류 승인 0건
  주의 PR 1건: #47 Capacitor 앱 셸 — backend CI 실패(머지 보류)
  최근 CWI ingest: 2026-06-07 09:22 (new=200) · 최근 분석: 2026-W23
  시각: 2026-06-11 12:30
  ```

## 18:00 오케스트레이터 정기 실행

### 헬스체크 ✅
- prod backend 200 OK (`{"status":"ok","db":true,"katago_alive":false}` — lazy spawn, 정상), prod web 200 OK.
- launchctl: `com.baduk.api` PID 2154 / `com.baduk.web` PID 1825 — 둘 다 가동.
- 디스크 4%, plist drift 없음(`sync-launchd.sh --check` exit 0 무출력).
- staging 중단됨 (정책상 정상, 경보 아님).

### 백업 검증 ✅
- 최신 `baduk-20260611T040005.db.gz` — `find -mtime +1` 미출력(신선), integrity_check=ok, 7 tables.
- 티어 daily 14 / weekly 3 / monthly 1.

### 버그 스캔 — 신규 0
- `~/Library/Logs/baduk-api.err` tail: WS 연결 INFO 라인만(스택 트레이스·예외 없음). `games/NaN` 403 4건은 127.0.0.1 로컬 테스트 트래픽 → 정상 거부(403), prod 버그 아님.
- 열린 `bug` 이슈 0건. 신규 이슈 생성 안 함.

### 백로그 트리아지 — 0건
- `no:label` 이슈 없음 — 미분류 이슈 없음.

### PR 감시 — 주의 1건 (12:30과 동일, 변화 없음)
- **PR #47** "feat: Capacitor 앱 셸" (2026-06-10 생성, 정체 아님, MERGEABLE).
  - CI: backend **FAILURE**, frontend SUCCESS, app-shell-build SUCCESS, e2e SKIPPED.
  - 실패 원인 확인: `tests/api/test_ws_token_auth.py::test_ws_accepts_query_token_without_cookie - ValueError: Connection closed` 1건 실패(521 통과, 커버리지 80.93%는 통과). WS 테스트 단건 — flaky 의심. 머지(🟡)·CI 수정은 dev 작업, 보고만. 머지 보류 권장.

### Telegram 보고
- 텔레그램 채널 reply 도구를 세션에서 탐색(ToolSearch `telegram`·`SendMessage`·`reply`) → **도구 미노출**(plugin:telegram MCP가 이 세션에 callable로 로드되지 않음; 노출된 메시징 도구는 `RemoteTrigger`·`PushNotification`·Canva뿐). 환경변수·플래그 자가진단은 하지 않음.
- 발송하려던 본문:
  ```
  [inkbaduk 운영] 18:00 정기점검 — 헬스 OK
  prod backend·web 200 OK · 디스크 4% · 백업 신선(2026-06-11T04:00, integrity OK)
  버그스캔 신규 0 · 미분류 이슈 0 · 보류 승인 0건
  주의 PR 1건: #47 Capacitor 앱 셸 — backend CI 실패(WS 테스트 단건, 머지 보류)
  최근 CWI ingest: 2026-06-07 09:22 (new=200) · 최근 분석: 2026-W23
  시각: 2026-06-11 18:00
  ```
