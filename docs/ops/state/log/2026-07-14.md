# 2026-07-14 운영 로그

## dev-cycle (02:00)

**처리할 버그 없음** — 적격 이슈 0건으로 no-op 종료. 코드 변경·커밋·브랜치 없음.

- 이슈 조회: `gh issue list --state open` **0건**. `bug`·`small` 라벨 후보도 0건 → 러닝북의 "적격 이슈가 없으면 종료" 경로. 열린 PR도 **0건**.
- **오탐 1건 발생·자체 정정 (#12)** — 중간에 "닫힌 이슈 #12의 수정이 main에 반영되지 않았다"고 판단해 이슈를 재오픈하고 `in-progress`를 달았으나, **오판이었다.** `main`에는 이미 `"filterNicknamePh": "Search..."`가 있다(커밋 `ff36bc7`, PR #28).
  - 원인 — `git diff origin/main...fix/issue-12`(**three-dot**)를 "현재 main 대비 diff"로 오독. three-dot은 **병합 기준점(merge-base) 대비** diff라, 그 후 main에 들어온 동일 수정이 보이지 않아 "미반영"처럼 보였다.
  - 검증 — 브랜치 커밋을 최신 `origin/main` 위로 cherry-pick하니 **empty**(적용할 변경 없음). `git grep origin/main -- web/lib/i18n/en.json`으로 직접 확인 → 이미 `Search...`.
  - 원복 완료 — 이슈 #12 재close(경위 코멘트 첨부), `in-progress` 라벨 제거, 임시 브랜치 `fix/issue-12-placeholder` 삭제, 워크트리 clean. **순 변경 0.**
  - 교훈 — "이 수정이 main에 들어갔나"는 three-dot diff가 아니라 `git grep <ref> -- <path>` / `git merge-base --is-ancestor` / cherry-pick-empty 로 판정할 것. 메모 `[[git-three-dot-diff-trap]]` 기록.
- 부수 확인 — 남은 로컬 브랜치 정리 상태 ([[dev-cycle-unpushed-branches]] 검증 차원):
  - **병합됨**(원격 브랜치만 미삭제, 무해) — `fix/non-json-body-500`, `feat/last-move-dot-marker`.
  - **수정 내용이 다른 커밋으로 main에 이미 반영됨**(= 브랜치는 superseded, 폐기 가능) — `fix/sqlite-busy-timeout`(main `db.py:30`에 `PRAGMA busy_timeout=5000` 존재), `fix/auto-resign-respect-score-margin`(main `game_service.py`에 resign threshold 로직 존재), `fix/scoring-gate-too-strict`(main에 dead-stone/ownership 로직 존재), `fix/issue-12`(위 참조).
  - **미반영 · 미병합 · PR 없음 → 사람 판단 필요** — `feat/format-game-result`(SGF 결과 문자열 한글화, main에 `formatResult` 부재), `feat/i18n-ja-zh`(ja/zh 로케일 추가, main `web/lib/i18n/`에 ko·en만 존재), `feat/last-seen-at-debounce`(3커밋 앞섬, 7/13부터 이월). 셋 다 **feature 경로라 dev-cycle 자율 범위 밖** — PR 생성 또는 폐기 결정 필요.
- 도구 메모 — `gh` PATH 부재 재현(exit 127). `/opt/homebrew/bin`을 PATH에 추가해 우회. launchd 세션에서 4회 연속 동일 증상 — 래퍼에 PATH 고정을 넣을 만하다.
- Telegram: 결과 1건 발송.

## 오케스트레이터 (12:30)

**모든 러닝북 정상 — prod 이상 없음.**

- **healthcheck** ✅ — prod backend `{"status":"ok","db":true,"katago_alive":false}`, web 200 OK. launchctl `com.baduk.api` PID 1516 / `com.baduk.web` PID 1504 가동. 디스크 6%. plist drift 없음(`sync-launchd.sh --check` exit 0). staging은 중단(정책상 정상). `katago_alive:false`는 lazy spawn 정보 필드 — 경보 아님(러닝북 명시).
- **backup-verify** ✅ — 최신 `baduk-20260714T040005.db.gz` (7/14 04:00, 8시간 전 → 신선). 티어 daily 14 / weekly 8 / monthly 2. 복원 드릴 `integrity_check=ok`, 테이블 7개.
- **bug-scan** — **신규 버그 0건.** `baduk-api.err` 최종 기록이 7/12 09:15이고 최근 구간은 전부 INFO(uvicorn 기동·WS 연결 open/closed). 스택 트레이스·반복 예외 없음. 열린 `bug` 이슈도 0건.
- **backlog-triage** — 미분류 이슈 **0건** (`no:label` 빈 배열). 분류 작업 없음.
- **pr-watch** — 열린 PR **0건**. CI 실패·충돌·정체 PR 없음.
- **도구 이슈 → 이슈화(🟢 자율)** — `gh`가 launchd 세션 PATH에 없어 3연속 exit 127 (dev-cycle과 동일 증상, 누적 5회+). `/opt/homebrew/bin` 추가로 우회한 뒤, 래퍼 PATH 고정을 제안하는 **chore #54**를 생성(`chore`·`small`). 근본 수정은 `ops/*.sh` 변경이라 dev-cycle/PR 경로로 넘김.
- 승인 대기 **0건**. 최근 CWI ingest 7/12 03:08 (new=200, error=0). 최근 분석 리포트 2026-W28.
- Telegram: 상태 요약 1건 발송.

## 오케스트레이터 (18:30)

**모든 러닝북 정상 — prod 이상 없음. 신규 이슈·PR 0건.**

- **healthcheck** ✅ — prod backend `{"status":"ok","db":true,"katago_alive":false}`, web 200 OK. launchctl `com.baduk.api` PID 1516 / `com.baduk.web` PID 1504 (12:30과 동일 PID → 재기동 없이 연속 가동). 디스크 6%. plist drift 없음(exit 0). staging 중단(정책상 정상). `katago_alive:false`는 lazy spawn 정보 필드 — 경보 아님.
- **backup-verify** ✅ — 최신 `baduk-20260714T040005.db.gz` (7/14 04:00, 14시간 전 → 신선, `-mtime +1` 미해당). 티어 daily 14 / weekly 8 / monthly 2. 복원 드릴 `integrity_check=ok`, 테이블 7개.
- **bug-scan** — **신규 버그 0건.** `baduk-api.err` 최종 기록이 여전히 7/12 09:15 (18:30 시점 기준 2일 이상 무변동), 최근 구간에 스택 트레이스·반복 예외 없음. 열린 `bug` 이슈 0건. ([[bug-scan-verify-recency]] — 누적 카운트가 아니라 최종 기록 시각으로 판정)
- **backlog-triage** — 미분류 이슈 **0건**. 열린 이슈는 chore #54 (`chore`·`small`) 1건뿐, 이미 라벨 있음. 분류 작업 없음.
- **pr-watch** — 열린 PR **0건**. CI 실패·충돌·정체 PR 없음.

### 정정 — "미병합 브랜치" 항목은 오탐이었음 (06:30·7/13 기록 수정)

원격 브랜치 2개(`origin/feat/last-seen-at-debounce`, `origin/fix/issue-39`)가 `git branch -r --no-merged origin/main`에 계속 잡혀 "미병합 · PR 없음 → 사람 판단 대기"로 이월돼 왔으나, **둘 다 이미 main에 반영된 상태**다.

| 브랜치 | PR | 실제 상태 |
|---|---|---|
| `feat/last-seen-at-debounce` | **#30 MERGED** | main에 `backend/app/last_seen_cache.py`·`deps.py`·`ws.py`·`session_purge.py` 반영 확인 (`git grep last_seen_cache origin/main`) |
| `fix/issue-39` | **#45 MERGED** | main `ws.py`에 RuntimeError 가드 2곳 존재. 이슈 #39도 6/5 CLOSED |

- **원인** — 두 PR 모두 **squash merge**라 브랜치의 원본 커밋이 main의 조상이 아니다. 그래서 `--no-merged`가 "미병합"으로 표시한다. `gh pr list --head <브랜치>`가 MERGED를 반환하고 `git grep <ref>`로 내용이 main에 있음이 확인된다.
- **교훈** — [[git-three-dot-diff-trap]]의 squash-merge 변종. 브랜치 병합 여부는 `--no-merged`가 아니라 **PR 상태 + `git grep <ref>` 내용 확인**으로 판정한다. 메모리 갱신함.
- **조치** — 대시보드 "미병합 브랜치" 행에서 이 두 건 제거(사람 판단 불필요). 원격 브랜치 삭제는 정책 미기재 → 보수적으로 사람 판단 사항으로 남김(무해한 잔여물).
- 06:30 기록의 로컬 전용 브랜치 3건 중 `feat/format-game-result`·`feat/i18n-ja-zh`는 여전히 main 미반영 — **사람 판단 필요 항목은 이 2건뿐**.

- 승인 대기 **0건**. 최근 CWI ingest 7/12 03:08 (new=200, error=0). 최근 분석 리포트 2026-W28.
- 도구 메모 — `gh` PATH 부재 재현(launchd 세션, 5회 연속). `/opt/homebrew/bin` 추가로 우회. chore #54 미해결.
- Telegram: 상태 요약 1건 발송 ✅ (msg_id 2375). 세션에 `reply` 도구 미노출 → [[dev-cycle-telegram-botapi]]에 따라 `~/.claude/channels/telegram/{.env,access.json}`의 토큰·chat_id로 Bot API 직접 호출, `ok=true` 확인. "미발송"으로 기록하지 않음.
