# 2026-08-07

- 02:00 dev-cycle: **처리할 버그 없음 — 열린 이슈 0건으로 이틀 연속 큐가 완전히 비었다. 코드 변경 0건.**
  - **적격 이슈 0건.** `gh issue list --state open` = `[]`(전체 0건). 8/6과 동일한 상태가 24시간 유지됐다. `bug`·`small` 라벨 필터 이전에 열린 이슈 자체가 없으므로 선택 단계에서 종료했다.
  - **빈 결과가 조회 실패의 위장이 아님을 검증했다.** `gh auth status` = 계정 `rarebirds-svg`, 활성 계정, 토큰 scope `gist`·`read:org`·`repo`·`workflow` 정상. 리포 해석 `rarebirds-svg/quite-baduk`. 추가로 `gh issue list --state closed`가 #76·#74·#70을 정상 반환하는 것을 교차 확인했다 — 같은 명령이 다른 필터에서는 데이터를 돌려주므로 `[]`는 인증 만료나 API 장애가 아니라 실제 공백이다. 이 교차 확인 없이 `[]`를 그대로 믿으면 인증 만료를 "무작업"으로 오독하게 된다.
  - **선점·구현·커밋·push·PR 전부 미실행.** 선택할 이슈가 없어 러닝북 자율 경로 1~4단계를 통째로 건너뛰었다. `.worktrees/dev-cycle`은 `fe4d755` detached·클린 상태 그대로 두었고 브랜치를 새로 만들지 않았다. **머지 없음**(🟡 — 열린 PR 0건이라 애초에 대상 없음).
  - **누락 점검 3종 전부 클린.** 무작업 사이클이라도 과거 실패 모드는 실측했다.
    - `in-progress` 라벨 잔류 — `gh issue list --state all --label in-progress` = `[]`. 이전 사이클이 떼지 않고 남긴 선점 라벨 없음.
    - **미push 브랜치 없음**(`[[dev-cycle-unpushed-branches]]`) — 열린 PR 0건이고, 로컬 `fix/*`·`feat/*` 브랜치 3개(`feat/format-game-result` f184256 · `feat/i18n-ja-zh` 7d5a4b2 · `fix/auto-resign-respect-score-margin` deccf30)는 `git branch -vv`에서 전부 upstream `[gone]` = **머지 후 원격이 정리된 잔재**이지 리뷰 대기 중인 미push 작업이 아니다. 삭제는 하지 않았다(등급 미정 → 보수적으로 사람 판단).
    - 배포 갭 — `git fetch origin` 후 `git rev-list --count HEAD..origin/main` = **0**, HEAD = `origin/main` = **a59b260**.
  - **📌 AP-20260805-01은 여전히 대기 — 제안 후 약 54시간.** `pending-approvals.md`에서 상태 `대기`를 확인만 했다. 재빌드·재기동은 🟡라 실행하지 않았고, **중복 제안(AP-20260807-*)도 만들지 않았다** — 같은 내용을 매 사이클 새 ID로 쌓으면 승인 큐가 노이즈로 막힌다. 판별 검사 재실행은 오케스트레이터 사이클(12:00·18:00)의 담당이라 여기서 중복 측정하지 않았다. 배포 갭 0이 라이브 반영을 뜻하지 않는다는 점은 유효하다(`[[merge-is-not-deploy]]`).
  - **Telegram 발송 완료 — message_id 2519.** `ToolSearch`로 실제 조회했으나 `reply` 도구가 이 세션에 미노출이라(검색 결과의 `SendMessage`는 에이전트 간 통신, `PushNotification`은 데스크톱 알림이라 둘 다 대체재가 아니다) Bot API 직접 호출로 폴백했다(`[[dev-cycle-telegram-botapi]]`). 한글 본문을 `/tmp/dev-cycle-msg-20260807.txt`에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"` — 인라인 한글은 로케일 오류가 난다. 토큰은 `~/.claude/channels/telegram/.env`, chat_id는 `access.json`의 `allowFrom[0]`(1241277614). **환경 자가진단으로 "발송 불가"를 사전 판정하지 않았다** — 도구 노출 여부를 실제로 확인한 뒤 폴백했다.
  - **관찰 — 이 잡의 병목은 이 잡이 아니다(3일 연속 동일 결론).** 8/1~8/4 무작업 → 8/5 소비된 1건 → 8/6·8/7 완전 공백. 유입원인 오케스트레이터 버그 스캔이 작동한 8/4에는 하루에 #74·#76 두 건이 생겨 PR까지 갔다. 즉 원인은 파이프라인 고장이 아니라 **이슈 유입 0**이고, 백엔드 `.err`가 며칠째 트레이스백 0건인 것과 정합한다. 고칠 대상이 있다면 유입 스캔의 범위(로그 이외 소스 — 예: 사용자 리포트·TODO·품질 리포트 백로그)이지 이 잡의 스케줄이 아니다. 범위 변경은 🟡라 여기서 판단하지 않는다.

- 12:00 orchestrator: **서비스 전 항목 정상. 다만 이번 사이클에 5일 만의 실사용 대국 1건(#337)과 방문 14건이 관측됐고, 그 과정에서 기존 "진행 중 대국 0건" 판정 방법의 결함을 찾아 교정했다.**
  - **헬스체크 — 전부 OK.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200. `katago_alive:false`는 러닝북 명시대로 **정보용**이다 — lazy spawn 구조라 마지막 AI 호출(8/6 22:04) 이후 어댑터가 내려간 정상 상태이며, 이 필드 단독으로 incident를 만들거나 재기동을 제안하지 않는다. launchctl `com.baduk.api` PID **93501** / `com.baduk.web` **93522** 둘 다 숫자 = 가동. 디스크 **6%**(임계 90% 미만), plist drift `ops/sync-launchd.sh --check` **exit 0·무출력**, staging 중단 = 정책상 정상(경보 아님). watchdog `신규 incident 0 건`·`[api] OK`·`[web] OK`.
  - **PID 10사이클 연속 동일** — `ps lstart` = `Sun Aug 2 22:11:37`(api) / `22:11:42`(web) = **97시간 48분 무재기동**.
  - **백업 검증 — 3단계 전부 통과.** 최신 `baduk-20260807T040005.db.gz`(8/7 **04:00**, 3,284,957B ≈ 3.13MB) — `find -mtime +1` **무출력** = 30시간 이내 신선. 티어 daily **14** / weekly **8** / monthly **3**(daily 0 아님). 복원 드릴 `PRAGMA integrity_check` = **ok**, 테이블 **9개** = 3회 연속 통과. 백업 크기가 전일 3,283,653B 대비 +1,304B로 미미하게 늘었는데, 이는 아래 신규 대국·방문 기록과 정합한다.
  - **버그 스캔 — 신규 이슈 0건. 현 기동 이후 트레이스백 0건을 재실측했다.** `awk '/Started server process \[93501\]/{f=1} f'`로 현 기동 마커 이후만 잘라 `Traceback|ERROR`를 세어 **0건**. `tail -3000`에는 `database is locked` 트레이스백이 16세트 보이지만 마지막 것이 tail 기준 2804행, 현 기동 마커가 **2988행**이라 전부 재기동 *이전* 구간이다 — 누적 카운트가 아니라 발생 시점으로 판정했다(`[[bug-scan-verify-recency]]`). 열린 `bug` 이슈 0건이라 중복 차단 단계는 대상 없음.
  - **백로그 트리아지 — 대상 0건.** `gh issue list --state open --search "no:label"` 빈 결과. 열린 이슈 자체가 0건이라 라벨링·"사람 분류 필요" 항목 모두 없음.
  - **PR 감시 — 열린 PR 0건.** CI 실패·충돌·정체 어디에도 해당 없음. 3사이클 연속.
  - **🆕 5일 만의 실사용 대국 — #337.** DB 실측 `max(id)` = **337**(직전 336). `games` 행 — `ai_rank=1k`, `ai_style=combative`, `ai_player=sakata_eio`, `user_color=black`, 19로 호선 komi 6.5, `move_count=30`, `status=active`. `moves` 실측 마지막 수는 **30수 W K17, `played_at` 2026-08-06 13:04:14 UTC**(= 8/6 22:04 KST). 시작이 12:55:19 UTC라 **9분간 30수를 두고 이탈**한 형태다. `status=active`는 "지금 진행 중"이 아니다 — 4월부터 쌓인 `active` 행이 180여 건으로, 이 값은 **미완료 대국의 기본값**이지 활성 세션 표시가 아니다.
  - **⚠️ 판정 방법 결함 교정 — `.log`에서 `ws/games`를 세는 방식은 구조적으로 항상 0이 나온다.** 지난 사이클들이 "진행 중 대국 0건"의 근거로 써 온 지표인데, 실측하면 `.log` 전체 231,484줄에 `ws/games`가 **1건**, `.err`에는 **643건**이다. WebSocket 라인은 stderr로만 나가므로(`[[api-log-stdout-vs-stderr]]`의 연장) `.log` 카운트 0은 "대국이 없었다"가 아니라 **애초에 그 파일에 안 찍힌다**는 뜻이다. 실제로 #337의 WS 연결 3줄은 전부 `.err`에 있었다. **앞으로 대국 유무는 DB `max(id)`·`moves.played_at`으로 판정하고, 로그로 볼 때는 `.err`를 본다.** 다만 이번 사이클의 결론(재기동 시 진행 중 대국 없음)은 올바른 방법으로 재확인했을 때도 동일하다 — 마지막 수가 **14시간 전**이다.
  - **🆕 사람 방문 재개 — `visit_hits` 85 → 99행(+14).** 일자별 8/7 3건 · 8/6 11건 · 8/5 5건 · 8/4 8건. 6시간 델타에서도 `POST /api/analytics/hit` **14건**(직전 사이클 0건)으로 정합. 지난 사이클들의 "사람 방문 0건" 판정은 이제 유효하지 않다.
  - **트래픽 — 6시간 델타 4,991건, 5xx 0건.** 8/6 18:00 절단선(226,533)에서 직접 잘랐다(`.log` 226,533 → **231,484**). 상태코드 200 **4,932** / 401 **37** / 204 **15** / 404 **3** / 201 **3**. **5xx 0건.** 401 내역은 `GET /api/session` 14건(비로그인 폴링, 정상) + admin 4종 20건(비인증 관리자 화면 접근) + `GET /api/games/337` 2건(세션 만료 후 재조회)으로 전부 인증 경로의 정상 거절이다. 201 3건은 세션 생성 2 + **`POST /api/games` 1건**(= #337 생성). 204 15건은 `analytics/hit` 14 + admin 세션 삭제 1.
  - **`.err`의 WS 403 1건은 이슈화하지 않았다.** `WebSocket /api/ws/games/337` 403 — 앞선 401 2건과 같은 세션 만료 계열이고 **1회성**이라 러닝북의 후보 조건(2회 이상 반복)을 충족하지 않는다.
  - **🔎 보안 관측 — `GET /api/.env` 404 1건.** 자동 스캐너의 시크릿 탐침이다. 404로 거절됐고 정적 서빙 경로가 아니라 노출은 없다. 1회성이라 incident로 올리지 않고 관측만 기록한다.
  - **🔴 AP-20260805-01 미처리 48시간 — 판별 검사 3종이 다섯 번째로 동일하다.** ① `grep -rl 'resultText' web/.next/{static,server}` = **여전히 0개 파일** ② `web/.next/BUILD_ID` mtime = `Aug 4 22:23` **불변** ③ 웹 프로세스 `ps lstart` = `Aug 2 22:11:42` **불변**. `git rev-list --count HEAD..origin/main` = **0**, HEAD = `origin/main` = `a59b260`이라 이 지표로는 갭이 안 보인다는 판정도 다섯 번째 재확인(`[[merge-is-not-deploy]]`). 재빌드·재기동은 🟡라 실행하지 않았고, **중복 제안(AP-20260807-*)은 만들지 않고** 기존 항목에 재확인 각주를 덧붙였다.
  - **AP의 긴급도 근거가 이번에 바뀌었다.** 그동안 "8/1 이후 신규 대국 0건 = 사용자 영향 낮음"으로 적어 왔으나, 8/6 저녁에 실사용 대국과 방문이 재개됐다. #77(결과 표기 렌더)은 대국 기록·관전 화면의 **사용자 대면** 변경이라, 트래픽이 돌아온 지금 미배포 비용이 이전 사이클보다 높다. 반대로 재기동 위험은 여전히 낮다 — 마지막 수가 14시간 전이라 중단될 대국이 없다. 이 두 가지를 AP 각주와 Telegram에 반영했다.
  - **Telegram 발송 완료 — message_id 2522.** `ToolSearch`로 실제 조회했으나 `reply` 도구가 이 세션에 미노출이라(`SendMessage`는 에이전트 간 통신, `PushNotification`은 데스크톱 알림이라 둘 다 대체재 아님) Bot API 직접 호출로 폴백했다(`[[dev-cycle-telegram-botapi]]`). 한글 본문을 `/tmp/ops-msg-20260807-1200.txt`에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"`. 토큰은 `~/.claude/channels/telegram/.env`, chat_id는 `access.json`의 `allowFrom[0]`(1241277614). 환경 자가진단으로 사전 판정하지 않고 도구 노출을 실제 확인한 뒤 폴백했다.
  - **인바운드 답신 없음** — `~/.claude/channels/telegram/inbox`에 최신 항목이 전부 옛 이미지 파일이라 이번 세션은 승인 답신 트리거가 아니다. 정기 루프로 처리했다.
  - **incidents.md 미기록** — prod 이상이 하나도 없어 러닝북 3항(이상 시 기록)에 해당하지 않는다.
  - **갱신 파일** — `dashboard.md`(prod·staging·백업 2행·열린 이슈·열린 PR·최근 주간 리포트·보류 승인), `pending-answer` 아닌 `pending-approvals.md`(AP-20260805-01 5차 재확인 각주), 이 로그.
  - **메모리 갱신** — 위 판정 방법 결함을 `in-progress-game-detection` 메모리로 신규 저장하고 `api-log-stdout-vs-stderr`에 상호 링크를 걸었다. 매 사이클 재기동 위험 평가에서 반복되는 오판이라 세션 밖으로 남길 가치가 있다.

- 18:00 orchestrator: **서비스 전 항목 정상. 이번 사이클의 실질 성과는 며칠째 0건이던 버그 유입을 깬 신규 이슈 [#78](https://github.com/rarebirds-svg/quite-baduk/issues/78) 1건이다 — 401 폴링 루프.**
  - **헬스체크 — 전부 OK.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200. `katago_alive:false`는 러닝북 명시대로 **정보용**이며 lazy spawn 구조상 정상(마지막 AI 호출 8/6 22:04) — 단독으로 incident·재기동 사유가 되지 않는다. launchctl `com.baduk.api` PID **93501** / `com.baduk.web` **93522** 둘 다 숫자 = 가동. 디스크 **6%**(임계 90% 미만), plist drift `ops/sync-launchd.sh --check` **exit 0·무출력**, staging 중단 = 정책상 정상. watchdog `신규 incident 0 건`.
  - **PID 11사이클 연속 동일** — 웹 프로세스 `ps -o lstart,etime` = `Sun Aug 2 22:11:42` / `04-19:50:19` = **115시간 50분 무재기동**.
  - **백업 검증 — 3단계 전부 통과.** 최신 `baduk-20260807T040005.db.gz`(8/7 04:00, 3,284,957B) — `find -mtime +1` **무출력** = 30시간 이내 신선. 티어 daily **14** / weekly **8** / monthly **3**. 복원 드릴 `PRAGMA integrity_check` = **ok**, 테이블 **9개** = **4회 연속** 통과. 12:00과 동일 파일이라 값 변화 없음(다음 백업은 8/8 04:00).
  - **🆕 버그 스캔 — 신규 이슈 1건 생성([#78](https://github.com/rarebirds-svg/quite-baduk/issues/78), `bug`·`prio:low`).** 6시간 델타에서 **401이 1,167건 중 1,034건(88.6%)** 으로 튀어 파고들었다.
    - **폴링 루프임을 분포로 확정했다.** 1,032건이 어드민 4개 엔드포인트(`admin/summary`·`sessions`·`games`·`engine`)에 **정확히 258회씩 균등** 분포 — 사람 조작이면 이렇게 안 나온다. 전부 단일 운영자 IP이고, 같은 엔드포인트의 **200 응답 9건이 선행**하므로 "처음엔 인증됐다가 세션 만료 후 폴링만 남은" 형태다.
    - **원인을 코드에서 확인했다.** `web/app/admin/page.tsx:189-195`의 catch가 **403만 처리**하고 401은 조용히 삼킨다 — 상태 변화가 없으니 `setInterval`이 영원히 돈다. 로그 패턴에서 원인을 추정만 하지 않고 해당 경로를 직접 읽어 확정했다(규칙 10).
    - **리포 안에 정답 선례가 있다.** `admin/pro-games/page.tsx:43`은 `401 || 403`을 함께 처리한다. 반면 `page.tsx:191`·`sessions/[id]:118`·`login-history:60`·`stats:79`·`analytics:44` **5개 화면이 403만** 처리한다 — 즉 규칙이 없는 게 아니라 5곳이 선례에서 이탈한 것이다.
    - **간격 불일치도 설명된다.** 258회/6시간 ≈ **84초 간격**인데 `REFRESH_SEC = 5`다. 백그라운드 탭의 `setInterval` 스로틀링으로 보이며, **탭이 포그라운드면 시간당 2,880건**이 된다. 지금 관측된 172건/h는 최악이 아니라 최선의 경우다.
    - **영향은 낭비보다 오독 쪽이 크다.** 어드민 화면이 만료를 알리지 않고 옛 데이터를 계속 보여주므로 운영자가 이를 현재 상태로 읽을 수 있다. 사용자 대면 영향 없음(어드민 전용), 보안 노출 없음(서버는 401로 정상 거절, 5xx 0건).
    - 중복 차단 — 열린 `bug` 이슈 0건이고 `--state all` 검색에서도 동일 건 없음.
  - **백엔드 트레이스백은 여전히 0건 — `database is locked` 16세트는 전부 12일 전 것이다.** `tail -3000`에 락 트레이스백이 보이지만 마지막 것의 SQL이 `UPDATE games SET loss_streak WHERE id=332`이고, **DB 실측 게임 332의 마지막 수는 `2026-07-26 10:47:47 UTC`** 다. 즉 7/26 구간이며 이후 대국 333~337에서는 재현 0건이다. 누적 카운트가 아니라 발생 시점으로 판정했다(`[[bug-scan-verify-recency]]`). `.err` mtime이 `Aug 7 06:09`로 최신인 것은 재기동 INFO 라인이 덧붙어서일 뿐이라 최근성 대리지표로 쓸 수 없다.
  - **백로그 트리아지 — 신규 #78에 라벨 적용.** `gh issue list --state open --search "no:label"`은 빈 결과였고, 이번에 만든 #78은 생성 시 `bug`, 트리아지에서 `prio:low`(어드민 전용·사용자 대면 영향 없음)를 부여했다. `small`은 주지 않았다 — 5개 화면에 걸친 변경이라 "한 파일 안팎" 기준에 맞지 않는다. 사람 분류 필요 항목 없음.
  - **PR 감시 — 열린 PR 0건.** CI 실패·충돌·정체 어디에도 해당 없음. 4사이클 연속.
  - **🔴 AP-20260805-01 미처리 54시간 — 판별 검사 3종이 여섯 번째로 동일하다.** ① `grep -rl 'resultText' web/.next/{static,server}` = **여전히 0개 파일** ② `web/.next/BUILD_ID` mtime = `Aug 4 22:23` **불변** ③ 웹 프로세스 `ps lstart` = `Aug 2 22:11:42` **불변**. `git rev-list --count HEAD..origin/main` = **0**, HEAD = `origin/main` = `a59b260`이라 이 지표로는 갭이 안 보인다는 판정도 여섯 번째 재확인(`[[merge-is-not-deploy]]`). 재빌드·재기동은 🟡라 실행하지 않았고, **중복 제안(AP-20260807-*)은 만들지 않고** 기존 항목에 재확인 각주를 덧붙였다.
  - **재기동 잔여 위험은 12:00보다 더 낮아졌다.** DB `max(id)` = **337로 불변**이고 마지막 수가 `2026-08-06 13:04:14 UTC` = **20시간 전**이다(12:00 시점 14시간 → 지금 20시간). 6시간 델타에 `POST /api/games` **0건**. 중단될 진행 중 대국이 없다. 대국 유무는 로그가 아니라 DB로 판정했다(`[[in-progress-game-detection]]`).
  - **트래픽 — 6시간 델타 1,167건, 5xx 0건.** 12:00 절단선(231,484)에서 직접 잘랐다(`.log` 231,484 → **232,651**). 200 **130** / 401 **1,034** / 404 **1** / 204 **1**. 12:00의 4,991건에서 크게 줄었고, **200 130건 중 80건이 health 폴링**이라 실질 공개 트래픽은 얇은 구간이다. `visit_hits` 99 → **100행(+1)**. 프로 상세 조회는 테마·sitemap·월간 픽 위주로 소수.
  - **🔎 보안 관측 — `GET /api/.env` 404 1건(IP `195.178.110.31`).** 12:00에 이어 두 번째 탐침이나 **출처 IP가 다르다** — 동일 공격자의 반복이 아니라 무차별 스캐너의 배경 잡음이다. 404로 거절됐고 정적 서빙 경로가 아니라 노출 없음. incident로 올리지 않고 관측만 기록한다.
  - **incidents.md 미기록** — prod 이상이 하나도 없어 러닝북 3항에 해당하지 않는다.
  - **인바운드 답신 없음** — `~/.claude/channels/telegram/inbox` 최신 항목이 7/22 이미지라 이번 세션은 승인 답신 트리거가 아니다. 정기 루프로 처리했다.
  - **Telegram 발송 완료 — message_id 2523.** `ToolSearch`로 실제 조회했고 `reply` 도구가 이 세션에 **미노출**임을 확인한 뒤 Bot API로 폴백했다(`SendMessage`는 에이전트 간 통신, `PushNotification`은 데스크톱 알림이라 둘 다 대체재가 아니다). 환경 변수 자가진단으로 "발송 불가"를 사전 판정하지 않았다. 한글 본문을 `/tmp/ops-msg-20260807-1800.txt`에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"`(`[[dev-cycle-telegram-botapi]]`). 토큰 `~/.claude/channels/telegram/.env`, chat_id `access.json`의 `allowFrom[0]`(1241277614).
  - **갱신 파일** — `dashboard.md`(갱신 시각·prod·staging·백업 2행·열린 이슈·열린 PR·보류 승인), `pending-approvals.md`(AP-20260805-01 6차 재확인 각주), 이 로그.
  - **📌 이번 사이클의 방법론 교훈 — 버그 스캔이 `.err` 트레이스백만 보면 이런 결함은 영원히 못 잡는다.** #78은 서버가 **정상 동작**하며 만든 신호다(401은 올바른 거절이라 트레이스백이 없다). 며칠째 "신규 버그 0건"이었던 것은 버그가 없어서가 아니라 **보는 곳이 좁아서**였다. 액세스 로그의 상태코드 분포를 보는 절차를 러닝북에 넣을지는 러닝북 변경이라 여기서 판단하지 않고 사람 판단용으로 기록만 한다. 메모리 `bug-scan-widen-to-access-log`로 저장했다.
