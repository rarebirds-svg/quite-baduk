# 운영 로그 — 2026-06-13

## 02:00 자율 버그 사이클 (dev-cycle)

### 이슈 선택
- `/opt/homebrew/bin/gh issue list --state open --json number,title,labels` → 열린 이슈 0건.
- `bug`/`small` 라벨 적격 이슈 없음 → **처리할 버그 없음**.
- 종료. worktree·브랜치 생성 안 함.

### 비고
- `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 조회.
- 최근 닫힌 버그(#39~#42)는 모두 CLOSED. 신규 열린 이슈 없음.

### Telegram 보고
- `reply`/`SendMessage` 도구는 세션 미노출(ToolSearch `telegram` 무매칭). 환경 자가진단은 하지 않고, 봇 토큰(`~/.claude/channels/telegram/.env`)+chat_id(`ops/ops.env`)로 Bot API `sendMessage` 직접 호출 → **발송 성공**(HTTP 200, message_id 2186).
- 발송 본문.
  ```
  [inkbaduk 02:00 dev-cycle] 처리할 버그 없음
  열린 이슈 0건 (bug/small 적격 없음). worktree·브랜치 생성 안 함. 종료.
  시각: 2026-06-13 02:00
  ```

## 02:00 콘텐츠 초안 생성 사이클 (content-draft)

- 종류 선택(most-behind): FAQ 6/9(0.667) < 글로서리 13/18(0.722) → 더 뒤처진 **FAQ**. 미작성 후보 알파벳 첫 항목 = `board-sizes`(나머지 후보 resign-and-pass·spectate-pro).
- 초안 작성 → korean-copy-qa 점검: "반/판" 용어 혼용 fix(ko.json 통일 표기 "판"에 정합)·문장 다듬기 반영. QA 보류 없음.
- 라이브 게시 🟢: 덮어쓰기 가드 통과(web/content/faq/board-sizes.md 미존재) → `web/content/faq/board-sizes.md`만 스테이징·커밋 `c6d8574` → main push(eae8b47..c6d8574). state/ 변경 미포함.
- 노출: `/faq/board-sizes` 동적 렌더라 재빌드·재시작 없이 즉시.
- Telegram 사후 보고: Bot API `sendMessage` 직접 호출 발송 성공(message_id 2187).

## 12:00 오케스트레이터 (12:30 슬롯, 인바운드 없음 → 표준 루프)

### 헬스체크 🟢
- prod backend OK — `{"status":"ok","db":true,"katago_alive":false}`. `katago_alive:false`는 lazy-spawn 정상(정보용, 경보 아님).
- prod web OK (200). launchctl: `com.baduk.api` PID 78468 / `com.baduk.web` PID 26130 — 둘 다 가동.
- 디스크 4%. launchd plist drift 없음(exit 0). staging 중단됨(정책상 정상).

### 백업 검증 🟢
- 최신 `baduk-20260613T040005.db.gz`(오늘 04:00, 30h 이내 신선). 복원 드릴 integrity_check=ok, 7 tables.
- 티어 daily 14 / weekly 3 / monthly 1.

### 버그 스캔 🟢 — 신규 0건
- `baduk-api.err`에 `TypeError: Object of type bytes is not JSON serializable` 2회(6/12, line 221457·221549, `POST /api/session` 검증오류 경로). **그러나 stale**: 트레이스의 `errors.py:21 return JSONResponse`는 fix 이전 버전 줄번호. 현재 `errors.py`는 `_safe_validation_errors`(line 18~21)로 bytes를 치환 디코딩 처리 중 — 줄 21은 helper의 닫는 괄호. 즉 이미 수정·배포된 버그의 배포 전 잔여 로그. 신규 이슈 미생성. ([[bug-scan-verify-recency]] 적용 — 카운트 아닌 시점·코드 대조로 오탐 차단.)
- 열린 `bug` 이슈 0건.

### 백로그 트리아지 🟢 — 미분류 0건
- `gh issue list --search "no:label"` 빈 배열. 처리 없음.

### PR 감시 🟢 — 열린 PR 0건
- `gh pr list --state open` 빈 배열. 주의 PR 없음.

### 비고
- `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로 사용(02:00과 동일).

### Telegram 보고
- `reply`/`SendMessage` 도구 세션 미노출(ToolSearch `telegram` 무매칭 — Canva·Monitor·Push만). 자가진단 없이 Bot API `sendMessage` 직접 호출 → **발송 성공**(HTTP 200, ok:true, message_id 2188, chat 1241277614).
- 본문: 정상 ✅ 요약 — 헬스 OK / 백업 신선·복원 ok / 버그스캔 신규 0 / 미분류·PR 0 / 대기 승인 0 / 최근 CWI 06-07 09:22 new=200 / 최근 분석 2026-W23.

## 18:00 오케스트레이터 (18:30 슬롯, 인바운드 없음 → 표준 루프)

### 헬스체크 🟢
- prod backend OK — `{"status":"ok","db":true,"katago_alive":false}`. prod web OK(200). launchctl: api PID 78468 / web 26130 — 둘 다 가동(12:00과 동일 프로세스, 재기동 없음).
- 디스크 4%. launchd plist drift 없음(exit 0). staging 중단됨(정책상 정상).

### 백업 검증 🟢
- 최신 `baduk-20260613T040005.db.gz`(오늘 04:00, 30h 이내). 복원 드릴 integrity_check=ok, 7 tables. 티어 daily 14 / weekly 3 / monthly 1.

### 버그 스캔 🟢 — 신규 0건
- `baduk-api.err`의 `TypeError: bytes is not JSON serializable` 2회는 12:00에 stale로 판정한 것과 동일(line 221457·221549). 추가 검증: 두 트레이스 모두 현재 프로세스(78468, Jun 12 11:52 기동) 시작 줄보다 앞 — 이미 죽은 pre-fix 프로세스(33051 등) 소산. **라이브 재현 테스트로 확정**: 실행 중 prod에 비-dict JSON 본문 POST → 깨끗한 **422**(`_safe_validation_errors` 동작), `{"nickname":"deploy-check-77"}` → **201**. 500·TypeError 없음. fix `ce9d745` 배포 확인(Jun 12 11:52 ≥ fix Jun 11 21:12). 신규 이슈 미생성. ([[bug-scan-verify-recency]])
- 열린 `bug` 이슈 0건.

### 백로그 트리아지 🟢 — 미분류 0건
- `gh issue list --search "no:label"` 빈 배열.

### PR 감시 🟢 — 열린 PR 0건
- `gh pr list --state open` 빈 배열.

### 비고
- 라이브 재현 테스트가 nickname `deploy-check-77` 세션(user id 2)을 생성. deploy-check 프로브와 동일한 무해한 세션 row — 대국 아님(spectate 노출 무관). 별도 정리 불필요.
- `gh`는 `/opt/homebrew/bin/gh` 절대경로 사용(PATH 미등록, 02:00·12:00과 동일).

### Telegram 보고
- `reply`/`SendMessage` 도구 세션 미노출(ToolSearch 무매칭). 자가진단 없이 Bot API `sendMessage` 직접 호출 → **발송 성공**(HTTP 200, ok:true, message_id 2193, chat 1241277614).
- 본문: 정상 ✅ — 헬스 OK / 백업 신선·복원 ok / 버그스캔 신규 0(stale TypeError, 라이브 422/201 확인) / 미분류·PR 0 / 대기 승인 0 / 최근 CWI 06-07 new=200 / 최근 분석 2026-W23.
