# 2026-08-02

- 02:00 dev-cycle: **처리할 버그 없음 — 백로그 전체가 비었다.** 이슈 0건·PR 0건으로, 어제(8/1)와 달리 "PR 대기"조차 남아 있지 않다.
  - **적격 이슈 0건.** `gh issue list --state open --json number,title,labels` 결과가 빈 배열(`[]`)이다. 라벨 필터(`bug`·`small` 있고 `feature`·`in-progress` 없음) 이전에 **열린 이슈 자체가 0건**이라 선택·선점 단계가 성립하지 않는다. `in-progress` 라벨을 단 대상도 없으므로 뗄 것도 없다.
  - **열린 PR 0건 — 어제의 사람 대기 3건이 전부 해소됐다.** `gh pr list --state open`이 빈 결과다. 7/31~8/1 로그에서 사람 머지를 기다리던 #71·#72·#73이 8/1 13:40~14:00 사이에 모두 머지됐고(#66은 그전에 처리), 그에 딸린 이슈 #65·#70도 닫혔다. `origin/main` HEAD는 `fe4d755`(watchdog 성공마커 판정, #70/#72)다.
  - **워크트리 미사용.** 착수할 이슈가 없어 `.worktrees/dev-cycle` 브랜치 생성·커밋·push·PR 생성을 하지 않았다. 파일 변경 0건(아래 메모리 정정 제외 — 리포 밖 파일이다).
  - **부수 정정 — `database-locked-genmove` 메모리의 "durable fix 미완" 서술이 stale.** 실측으로 확인했다. `git merge-base --is-ancestor 38fd9e3 origin/main` 통과, `git grep genmove origin/main -- backend/app/services/game_service.py`에 `# AI responds — genmove runs with NO write lock held.`(L320)가 존재한다. 즉 PR #69로 "수 커밋 후 genmove" 재구조화가 이미 들어갔다. 에이전트 메모리를 완료 상태로 갱신했다 — 다음 사이클이 이미 끝난 작업을 다시 집지 않게 하기 위함이다(`[[database-locked-genmove]]`).
  - **다음 사이클 전제.** dev-cycle은 이슈 큐를 소비하는 잡이고 `ops/`에 이슈를 자동 생성하는 bug-scan 잡은 없다. 따라서 사람이 이슈를 열거나 오케스트레이터/watchdog가 장애를 이슈로 승격하기 전까지 이 잡은 계속 무작업으로 종료한다. 이슈가 없다는 이유로 임의의 리팩터링·개선을 자율 착수하지는 않았다(범위 밖).

## 09:00 주간 분석 리포트 (analytics-weekly)

- `docs/ops/state/reports/2026-W31.md` 생성. 이번 주 대국 **5국**(전주 18국 대비 -13, -72.2%)·누적 **336국**, `session_history` 신규 **5건**/누적 **424건**(`sessions` 테이블은 활성 0), `visit_hits` 7일 **27건**. 누적 보드 19줄 255·9줄 69·13줄 12, 핸디캡 0집 327 우세. 콘텐츠 신규 0편(글로서리 27·FAQ 12), 보류 승인 3건(AP-20260730-01·AP-20260725-01·AP-20260723-01), 최근 인시던트 OPS-20260728-01. `/api/stats`는 HTTP 401(`no_session`)이라 DB 카운트로 대체했다. Telegram 발송 완료(message_id 2493).

## 12:00 오케스트레이터

**러닝북 5종 전부 수행 — prod 정상, 신규 incident·이슈 0건.** 다만 이번 사이클의 핵심은 헬스가 아니라 **머지와 배포 사이의 간극**이다.

### 🔴 최우선 — 사람이 머지한 4커밋이 라이브에 하나도 반영되지 않았다

- 8/1 23:00 KST에 PR **#66·#71·#72**(+ 22:40 **#73**)가 전부 머지됐다(`fd68fa3`·`1ca3bcd`·`fe4d755`·`3c67d6a`). 3개월 가까이 끌던 보류 승인 3건이 한 번에 해소된 것이다.
- **그러나 prod 작업 트리는 여전히 `38fd9e3`이고 `origin/main`(`fe4d755`) 대비 4커밋 behind다.** launchd prod는 리포 작업 트리에서 직접 구동되므로 머지만으로는 아무것도 바뀌지 않는다.
- **실증** — 이번 세션 시작 로그(`orchestrator-runs.log` 12:00:05)에 #71이 고치기로 한 `Write(...)` deny 경고 2건이 **그대로 재출력**됐다. 머지 13시간 후에도 prod DB·KataGo 모델 가드레일은 실효가 없다.
- `git diff --stat HEAD origin/main` = 9파일 675+/26-. 로컬 미커밋 변경(`docs/ops/state/*`·`e2e/*`)과 유입 파일의 교집합은 **공집합**이라 `pull --ff-only`는 충돌 없이 깨끗하다(실측).
- 배포는 🟡(prod 재기동)라 자율 실행하지 않고 **AP-20260802-01**로 제안했다. 사람이 머지 버튼을 눌렀다는 것이 배포 승인과 같지 않다고 보수적으로 판정했다(정책 "모호하면 한 단계 보수적으로"). 1단계 `git pull`만으로도 #71·#72는 즉시 발효한다.

### 🟢 #69(대국 프리즈 근본 fix) 라이브 검증 — 10사이클 만에 완료

- 7/26 배포 이후 새 대국이 0판이라 미검증 상태였는데, **8/1 18:37~19:15 KST에 대국 #336이 실제로 진행됐다** — 99수, 38분, `resigned`로 정상 종료.
- 그 구간 `.err`에 **트레이스백 0건**. `database is locked` 마지막 발생은 line 225347로 현 기동(PID 512, `Jul 29 00:01:09`) **이전**이며, 기동 이후 구간(line 225395~)에는 `Error|Exception|Traceback` 0건이다. `.err`는 8/1 19:43에 3줄 늘었는데 전부 WebSocket 연결 로그다.
- 즉 **genmove 중 DB 락으로 인한 프리즈가 99수짜리 실대국에서 재현되지 않았다.** #69는 라이브에서 유효하다고 판정한다.

### 러닝북 결과

- **헬스체크 OK** — backend·web 200, `db:true`, PID 512/497 **108시간(4.5일) 무재기동**, 디스크 6%, `sync-launchd.sh --check` exit 0·무출력(drift 없음). staging 중단 = 정책상 정상. `katago_alive:**false**`지만 러닝북 명시대로 정보용 필드이며 마지막 AI 호출이 8/1 19:15이라 lazy spawn 만료로 설명된다 — 경보 아님.
- **백업 검증 OK** — 최신 `baduk-20260802T040005.db.gz`(8/2 04:00, 8시간 전), `-mtime +1` 무출력. 티어 daily 14 · weekly 8 · monthly 3. 복원 드릴 `integrity_check=ok`, 9 tables.
- **버그 스캔 — 신규 0건.** 현 기동 구간 에러 0건이라 후보 자체가 없다. 열린 `bug` 이슈도 0건.
- **백로그 트리아지 — 미분류 0건.** 열린 이슈 자체가 0건.
- **PR 감시 — 열린 PR 0건.** 어제까지 "정체"로 잡히던 #66이 머지로 해소되며 주의 PR 0건이 됐다.

### 트래픽

- `.log` 212,602 → **213,987줄(18시간 +1,385)**. 최근 1,600줄 구성은 `/api/admin/*` **949**(운영자 폴링) · **프로 상세 348** · health 247 · **대국 경로 22**(#336 hint 21 + 새 대국 생성 1) · `analytics/hit` 6.
- **프로 상세 348건은 관측 이래 압도적 최고치**다(직전 최고 4.3건/h). 5xx는 **0건** — #66이 아직 미배포인 상태에서 나온 수치라, `busy_timeout` 30s 완화만으로도 이 밀도는 견딘다는 뜻이다.
- **주의(경보 아님) — `/api/admin/*` 401이 580건.** 전부 운영자 IP `1.237.136.170`이고 같은 IP가 200도 344건 받았다. 어드민 세션이 폴링 도중 만료돼 이후 요청이 전부 401이 된 패턴이며, 최근 200줄에는 admin 요청 자체가 없어 **이미 멈췄다**. 서비스 장애가 아니라 운영자 브라우저 탭 문제로 판단 — 재로그인하면 해소된다. incident 미기록.

## 18:00 오케스트레이터

**러닝북 5종 전부 수행 — prod 정상, 신규 incident·이슈 0건.** 12:00과 달라진 것은 딱 두 가지다. 배포 갭이 6시간 더 묵었고, 크롤러 밀도가 또 최고치를 갱신했다.

### 🔴 배포 갭 미해소 — AP-20260802-01 6시간째 대기

- 작업 트리 `38fd9e3` vs `origin/main` `fe4d755` = **여전히 4커밋 behind**. 12:00 이후 아무 변화가 없다.
- **실증도 그대로 재현됐다** — 이번 세션 시작 로그(`orchestrator-runs.log` **18:00:05**)에 #71이 고치기로 한 `Write(...)` deny 무실효 경고 2건이 **또 출력**됐다. 머지(8/1 23:00) 후 19시간, 제안 후 6시간 경과.
- 신규 제안은 만들지 않았다. 같은 액션을 중복 등재하지 않고 AP-20260802-01 하나를 유지한다.
- 배포는 🟡(prod 재기동)라 자율 실행 대상이 아니다. 1단계 `git pull --ff-only`만으로도 #71(가드레일)·#72(watchdog 성공마커)는 즉시 발효하며, 이 단계는 재기동을 수반하지 않는다.

### 🟢 프로 기보 크롤러 밀도가 또 최고치 — 5xx는 여전히 0건

- **12:00→18:00 델타를 직접 절단해 측정했다**(`.log` 213,987 → **214,219줄 = 6시간 232줄**). 창 크기를 최근성 대리지표로 쓰지 않기 위해서다(`[[bug-scan-verify-recency]]`).
- 델타 232줄 구성 — **프로 상세 144** · health 79 · themes 3 · sitemap 3 · 월간 픽 3. **232건 전부 200**(4xx·5xx **0건**).
- 프로 상세 밀도 **24건/h**로 관측 이래 최고다. 추이 0.67 → 0.9 → 2.4 → 4.3 → **24건/h**. #66(원자적 UPDATE)이 **여전히 미배포**인 상태에서 나온 수치라, `busy_timeout` 30s만으로 이 밀도를 견딘다는 증거가 12:00보다 한 단계 더 강해졌다.
- **12:00의 어드민 401 예측이 맞았다** — 델타에 `/api/admin/*` 요청이 **0건**이다. 12:00에 "이미 멈췄다, 재로그인으로 해소"로 판정하고 incident를 만들지 않은 것이 옳았음이 확인됐다.

### 🟢 주간 CWI ingest 정상 — 대시보드 stale 값 3건 정정

- 8/2 03:00~03:12에 주간(일요일) ingest가 정상 실행됐다 — `fetched=2260 new=200 duplicate=2060 error=0`, cap=200 도달.
- DB 실측 `pro_games` = **3111**. 대시보드 표기 2911(7/26분)에서 정확히 +200으로 정합한다.
- 대시보드의 "최근 CWI ingest"(7/26)·"프로 기보 수"(2911)·"최근 주간 리포트"(W30, 실제 W31 존재)가 stale이라 이번에 함께 갱신했다.

### 러닝북 결과

- **헬스체크 OK** — backend·web 200, `db:true`, PID 512/497 **114시간(4일 18시간) 무재기동**(`ps lstart` = `Wed Jul 29 00:01:09`, 11사이클 연속 동일), 디스크 6%, `sync-launchd.sh --check` exit 0·무출력. staging 중단 = 정책상 정상. `katago_alive:false`는 러닝북 L19~23대로 정보용 — 마지막 AI 호출이 8/1 19:15이라 lazy spawn 만료로 설명되며 경보 아님.
- **백업 검증 OK** — 최신 `baduk-20260802T040005.db.gz`(8/2 04:00, 14시간 전, 3,281,022B), `-mtime +1` 무출력. 티어 daily 14 · weekly 8 · monthly 3. 복원 드릴 `integrity_check=ok`, 9 tables.
- **버그 스캔 — 신규 0건.** `.err` 225,401줄 / mtime `Aug 1 19:43`. 현 기동 마커(line 225395) 이후 구간에 `Error|Exception|Traceback` **0건**을 재확인했다. 마지막 `database is locked`(line 225347)는 기동 *이전* = 구 프로세스 구간이라 판정에 쓰지 않는다. 열린 `bug` 이슈 0건.
- **백로그 트리아지 — 미분류 0건.** 열린 이슈 자체가 0건이라 분류 대상이 없다. 사람 분류 필요 항목도 없다.
- **PR 감시 — 열린 PR 0건.** 주의 PR 0건.

### 사용자 방문

- `visit_hits` **69행**(8/1 18:00 63행 → +6). 최신 사람 방문은 `2026-08-01 14:46:12 UTC` = **8/1 23:46 KST**로, 최근 18시간 사람 비콘 0건이다. 이번 6시간 델타에도 `analytics/hit`이 0건이라 정합한다 — 즉 위 크롤러 밀도 최고치는 **사람 트래픽이 아니라 봇 유입**이다.
- `games` 336 · 최신 대국 #336(8/1 19:15 종료) 이후 신규 대국 0건.

### 보고·산출물

- Telegram 발송 완료 — **message_id 2496**. `reply` 도구는 `ToolSearch`로 실제 조회했으나 이 세션에 미노출이라(MCP 미로드) Bot API 직접 호출로 발송했다(`[[dev-cycle-telegram-botapi]]`).
- **자책 기록** — `curl -F "text@파일"`을 먼저 시도해 `option -F: is badly used here`로 실패했다. `[[dev-cycle-telegram-botapi]]`에 7/29 같은 실수가 이미 명시돼 있었는데 반복했다. `@`는 `--data-urlencode` 문법이고 `-F`에서는 `text=<파일`이다. 이번엔 `--data-urlencode "text@파일"`로 정정해 성공했다. 메모리는 이미 정확하므로 갱신하지 않았다.
- 갱신 파일 — `state/dashboard.md`(prod·백업·콘텐츠 인덱스·분석·개발 현황·보류 승인 섹션), `state/log/2026-08-02.md`, `state/pending-approvals.md`(AP-20260802-01에 18:00 재확인 추가).
- **코드 변경·커밋·푸시·머지 0건.** 신규 이슈·PR도 0건.

---

## 수동 배포 — main fe4d755 승급 (사람 지시)

dev-cycle이 아니라 사람의 직접 지시로 실행한 승급이다. 러닝북 `runbooks/deploy.md` 절차를 따랐다.

- **승인** — Telegram 프로토콜 대신 사람의 직접 지시("배포해")를 🟡 승인으로 갈음했다. 이 세션에서 Telegram MCP가 끊겨 제안·회신을 보내지 못했다. 결과 회신도 미발송이다.
- **롤백 SHA** — `38fd9e31a8503791088be13adab903c7f6ed8d46`.
- **승급 SHA** — `fe4d7555289b989b9fe28dac2c92bef337465f47` (4커밋).
  - #73 용어집 board 다이어그램 + 이미지 figure 렌더링
  - #66 프로 기보 조회수 원자적 UPDATE — 18:00 사이클이 "여전히 미배포"로 기록한 항목이 이번에 해소됐다
  - #71 prod DB·모델 deny 규칙 Write→Edit 정정
  - #72 watchdog 성공 마커 기준 판정
- **staging 검증 OK** — backend `pytest` 577 passed, web `npm run build` 성공 + `npm test` 158 passed. staging DB는 `.worktrees/staging/backend/data/`로 분리돼 prod DB를 건드리지 않았다.
- **마이그레이션 불필요** — 승급 범위에 `backend/alembic/` 변경이 없고 prod DB는 이미 `0017 (head)`였다. 의존성 변경도 없어 `pip install`을 건너뛰었다.
- **사고 1건 — `web/node_modules` 소실.** 빌드 단계에서 `sh: next: command not found`로 실패했다. 원인은 같은 세션에서 기능 워크트리(`.claude/worktrees/feat+glossary-media-phase1`)에 `npm ci`를 돌린 뒤 그 워크트리를 제거한 것이다. prod 체크아웃의 `web/node_modules`가 빈 디렉토리(0개)가 됐다. 실행 중이던 `com.baduk.web`(PID 497)은 모듈을 메모리에 물고 있어 계속 서빙했으므로 겉으로는 정상이었다 — **재시작했다면 그대로 기동 실패로 이어졌을 상태다.** `npm ci`로 487개 패키지를 복구한 뒤 빌드했다.
  - 교훈. 워크트리에서 `npm ci`를 돌리기 전에 그 `node_modules`가 메인 체크아웃을 가리키는지 확인한다. 재시작 전 헬스체크만으로는 이 상태를 못 잡는다 — 빌드 산출물과 `node_modules` 실체를 함께 봐야 한다.
- **사후 확인 OK** — `ops/stack.sh ps prod` 2회 연속 동일 PID(api 93501 · web 93522), `{"status":"ok","db":true,"katago_alive":true}`, web :3000 200. 배포 전 `katago_alive:false`였으나 재기동 후 `true`다.
- 롤백은 수행하지 않았다.
