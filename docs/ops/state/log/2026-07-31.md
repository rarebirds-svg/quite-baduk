# 2026-07-31

- 02:00 dev-cycle: **처리할 버그 없음 — 신규 착수 0건.** 적격 이슈 2건이 모두 이미 처리돼 PR 머지만 남은 상태다.
  - **적격 이슈 집합.** `gh issue list --state open` 결과 **2건**뿐 — #65(`bug`·`prio:high`), #70(`bug`·`small`). 둘 다 `feature`·`in-progress` 라벨은 없어 라벨상으로는 적격이지만, **각각 이미 열린 PR이 있다** — #65 → PR #66(`fix/issue-65`/`386567d`, 7/24 생성), #70 → PR #72(`fix/issue-70`/`4ff419a`, 7/30 02:00 직전 사이클 생성). 새 브랜치·PR을 만들면 중복이 되므로 착수하지 않았다. `in-progress`도 달지 않았다(선점할 작업 자체가 없다).
  - **열린 PR 3건 전부 실측 — 전원 그린이고 막힌 곳이 없다.** #66·#71·#72 모두 `MERGEABLE`·`mergeStateStatus=CLEAN`이며 CI **4잡(backend·frontend·app-shell-build·e2e) 전부 SUCCESS**. e2e 통과를 명시적으로 확인했다(`[[ci-e2e-disabled]]` — 이제 e2e ❌는 선재 실패가 아니라 그 PR이 깨뜨린 신호로 읽어야 한다). 즉 **병목은 코드가 아니라 사람 머지(🟡)** 다.
  - **#70 픽스가 미머지인 동안 감시 사각은 라이브에 그대로다.** 그래서 그 실패 모드가 재발했는지 직접 셌다 — `dev-cycle-runs.log`·`orchestrator-runs.log`에서 `Failed to authenticate`는 **7/28 3건이 마지막**이고 7/29 이후 신규 0건이다. 7/29·7/30 사이클은 모두 정상 종료 마커로 끝났다. 즉 7/28 유실 이후 재발은 없었지만, **재발했더라도 현 prod의 `check-staleness.sh`는 여전히 못 잡는다** — PR #72 머지 전까지 이 사각은 유효하다(`AP-20260730-01`).

### #65 재검증 — 무재발 50시간, 그러나 저부하 알리바이는 그대로

7/29 사이클이 "다음 부하 있는 구간이 진짜 판정 시점"이라고 남겼으므로 이번에 부하부터 쟀다.

- **무재발 근거.** 현 prod api PID **512**(기동 `Wed Jul 29 00:01:09`, 7/29 사이클과 동일 프로세스·재기동 없음). `.err`는 **225,398줄 / mtime `Jul 29 00:01:16`** 로 기동 직후 이래 **바이트 단위 불변** = 약 **50시간 트레이스백 0건**. 마지막 `pro_games` 락 실패는 여전히 line 225145(7/24)다. 같은 구간 `.log`는 207,192줄 / mtime `Jul 31 02:00`으로 계속 자라므로 로그 정지가 아니라 진짜 무에러다(`[[api-log-stdout-vs-stderr]]`).
- **첫 측정을 정정했다 — 창 크기 함정에 한 번 빠졌다.** `tail -3000`으로 세니 `/api/spectate/pro/`가 **413건**이라 7/29의 123건 대비 3.4배로 보였고 "부하 붙은 구간"으로 읽을 뻔했다. 틀렸다 — `.log`가 7/26 이후 약 1,750줄만 자라서 3,000줄 창이 **7/26 이전까지 거슬러 올라간다**. 7/30 사이클이 `POST /api/games` 106건에서 겪은 것과 **같은 함정**이다(`[[bug-scan-verify-recency]]`).
- **창 안의 `/api/health`를 시계로 삼아 환산했다** (모니터 약 4.6분 간격 = 시간당 약 13건).

  | 창 | pro 상세 | health | 환산 구간 | pro 시간당 |
  |---|---|---|---|---|
  | 최근 156줄 | 47 | 104 | 약 8h | 약 5.9건 |
  | 최근 400줄 | 124 | 268 | 약 20.6h | 약 6.0건 |
  | 최근 1,000줄 | 299 | 628 | 약 48h | 약 6.2건 |

  세 창 모두 **시간당 약 6건으로 평탄**하다. 7/23~24 크롤 버스트와는 자릿수가 다르므로 **저부하 알리바이는 해소되지 않았다.** 5xx는 모든 창에서 0건.
- **판정.** 50시간 무재발이 `busy_timeout` 30s 상향(`1bb0457`)이 에러를 지연으로 바꾼 결과인지, 그냥 부하가 없어서인지 이 구간 데이터로는 **가르지 못한다**. `--workers 1` + 블로킹 락 대기라 지연은 에러 로그에 흔적을 남기지 않는다. #66은 긴급도 낮음·취소 대상 아님으로 지난 사이클과 동일 판정이다. 이 내용을 이슈 #65에 코멘트로 남겼다.

### 사람 판단 대기 (변동 없음, 단 하나 임박)

- **보류 승인 3건** — `AP-20260730-01`(PR #72 머지) · `AP-20260725-01`(PR #66 머지) · `AP-20260723-01`(PR #71 머지, `Write`→`Edit` deny 정정).
- **PR #66이 7일 정체 기준에 임박.** 생성 `2026-07-25 02:06 KST` 기준 현재 **6.0일**이며, **2026-08-01 02:06 KST**에 PR 감시의 7일 정체 기준을 넘는다. 8/1 12:00 오케스트레이터 사이클에서 처음 "주의 PR"로 잡힐 것이다.
- `AP-20260723-01`의 대가는 이번 세션 시작 로그에도 그대로 찍혔다 — `dev-cycle-runs.log` 02:00:02 항목에 `Write(...)` deny 무실효 경고 **2건**. prod DB·모델 가드레일은 9일째 작동하지 않는다(`[[settings-deny-write-vs-edit]]`).

**머지는 하지 않았다(🟡 — 사람).** 코드 변경·커밋·푸시 없음.


- 결과 Telegram 1건 발송(message_id **2483**). `reply` 도구는 이번에도 미노출이라(`ToolSearch`로 확인 — Telegram 발송 도구 없음) Bot API 직접 호출했다. 토큰 source와 `curl`을 **같은 Bash 호출 안에** 넣어 7/30 18:00의 404 실패를 재현하지 않았고, 한글 본문은 파일에 써서 `-F "text=</파일"` + `LC_ALL=en_US.UTF-8`로 보냈다(`[[dev-cycle-telegram-botapi]]`). 1차 시도에 성공.

---

- 12:00 오케스트레이터: **러닝북 5종 전부 수행 — prod 정상, 신규 이슈·incident 0건, 조치 0건.**
  - **헬스체크 OK.** backend `{"status":"ok","db":true,"katago_alive":true}` · web 200. launchctl `com.baduk.api` PID **512** / `com.baduk.web` PID **497** — 기동 `Jul 29 00:01:09` 이래 **60시간 무재기동**(6사이클 연속 동일 PID). 디스크 **6%**, `sync-launchd.sh --check` exit 0·무출력(drift 없음). staging 중단 = 정책상 정상.
  - **백업 검증 OK — 7/31분으로 정상 회전.** 최신 `baduk-20260731T040005.db.gz`(3,167,845B ≈ 3.02MB, `Jul 31 04:00`), `-mtime +1` 미해당 = 신선. 복원 드릴 `integrity_check=**ok**` · **9 tables**. 티어 **14 / 8 / 2**(daily/weekly/monthly, 어제와 동일 = 보존 회전 정상).
  - **버그 스캔 신규 0건.** `.err` **225,398줄 / mtime `Jul 29 00:01`** 로 5사이클 연속 바이트 단위 불변 = **60시간 트레이스백 0건**. 같은 구간 `.log`는 207,347줄로 계속 자라므로 로그 정지가 아니라 진짜 무에러다(`[[api-log-stdout-vs-stderr]]`). 누적 `database is locked` 2,556건은 전부 구 프로세스 구간이라 판정에 쓰지 않았다(`[[bug-scan-verify-recency]]`). 이슈 생성 0건.
  - **백로그 트리아지 — 미분류 0건.** `gh issue list --search "no:label"` 빈 배열. 열린 이슈는 #65·#70 2건 유지, 둘 다 PR 대기 상태.
  - **PR 감시 — 3건 전부 그린, 주의 PR 0건.** #66·#71·#72 모두 `mergeable=MERGEABLE`이고 `gh pr checks`로 4잡(backend·frontend·app-shell-build·e2e) 전부 **pass** 실측. CI 실패·충돌·정체 어디에도 해당 없음.

  ### 트래픽이 3배 줄었다 — 이상 아님으로 판정, 단 #69 검증은 더 멀어졌다

  02:00 dev-cycle이 남긴 "시간당 약 6건 평탄" 뒤로 밀도가 떨어졌다. `.log`가 02:00 **207,192줄** → 12:00 **207,347줄**로 **10시간에 155줄**만 자랐다.

  | 창(최근 200줄) | 건수 | 환산 |
  |---|---|---|
  | `/api/health` | 170 | 약 4.5분 간격 → 창 길이 약 13h |
  | `/api/spectate/pro/{id}` | 26 | 약 **2.0건/h** (02:00 측정 5.9~6.2건/h 대비 약 1/3) |
  | `POST /api/games`·`/api/ws/games` | **0** | — |

  창의 85%가 health 폴링이다. 다만 이 13시간 창은 **23:00~12:00 = KST 심야 구간**을 통째로 포함하므로 크롤러·사람 트래픽이 함께 잦아드는 시간대 효과와 구분되지 않는다. 5xx는 최근 1,500줄에서 **0건**이고 공개 응답은 정상이므로 **장애 신호로 읽지 않았고 incident·이슈를 만들지 않았다.** 다음 사이클(18:00, 주간 구간 포함)에서 밀도가 복귀하는지가 실제 판별점이다.

  결과적으로 **#69(대국 프리즈 fix) 라이브 검증은 6사이클 연속 미완**이다. DB `max(games.id)`가 여전히 **335**(7/26)이고 최근 200줄에 대국 경로 0건 — 무사고가 아니라 무부하다. 부하가 더 줄었으므로 판정 시점은 오히려 멀어졌다.

  ### 그 밖에 확인만 하고 넘긴 것

  - **스캐너 프로브는 계속 유입 중이고 노출은 여전히 0건.** 최근 1,500줄 404 **24건** — `/api/settings` 3 · `/api/.env` 3 · `/api/v1/settings` 2 · `/api/v1/smtp` · `/api/v1/get/smtp/all` · `/api/v4/email/test` · `/api/v2/meta/plugins` · `/api/v1/organizations` 등. 7/30 18:00 대비 **종류·건수 모두 늘지 않았다**. 전부 404 = 라우팅되는 경로 없음. 배경 잡음으로 판단해 incident·이슈 미생성, 관찰만 유지.
  - **401 27건은 정상이다.** 전부 `GET /api/session` — 비로그인 방문자의 세션 조회이며 인증 실패 공격 흔적이 아니다.
  - **`visit_hits` 57행**(7/30 12:00 **54행** → **+3**). 비콘 정상 동작 지속. 봇 필터(`analytics.py:33-35`)로 크롤러 비콘은 적재 없이 204인 것도 기존 규명대로다.
  - **주기 불일치 재확인(미해소).** 이번 트리거도 **12:00:05** — plist는 12:00·18:00 2회인데 프롬프트·러닝북은 4회(06:30·12:30·18:30·23:30)로 적는다. `orchestrator-runs.log` 실측은 7/21 이후 하루 2회로 **누락 없이 일관**(7/28 인증 만료 건 제외)이므로 유실이 아니라 문서·plist 미정합이다. 사람 판단 대기 유지.

  ### 사람 판단 대기 3건 — 전부 머지 버튼 하나

  | ID | 대상 | 무응답 경과 | 비고 |
  |---|---|---|---|
  | AP-20260730-01 | PR #72 (watchdog 성공마커 판정, #70) | 약 **43시간** | 미머지 동안 watchdog 크래시 루프 사각지대 유지 |
  | AP-20260725-01 | PR #66 (조회수 원자적 UPDATE, #65) | 약 **150시간(6.3일)** | **8/1 02:06 KST에 7일 정체 기준 돌파** — 8/1 12:00 사이클에서 처음 "주의 PR"로 잡힌다 |
  | AP-20260723-01 | PR #71 (`Write`→`Edit` deny 정정) | 약 **192시간(8일)** | prod DB·모델 가드레일 **9일째 무실효** — 이번 세션 시작 로그에도 동일 경고 2건 재출력 |

  **코드 변경·커밋·푸시·머지 없음(🟡은 사람).** 신규 승인 제안도 없다.

  - 결과 Telegram 1건 발송(message_id **2485**). `reply` 도구는 이번에도 미노출이라(`ToolSearch`로 "telegram reply send message channel" 검색 — Telegram 발송 도구 없음) Bot API 직접 호출했다. 토큰 source와 `curl`을 **같은 Bash 호출 안에** 넣고 한글 본문은 파일에 써서 `-F "text=</파일"` + `LC_ALL=en_US.UTF-8`로 보냈다(`[[dev-cycle-telegram-botapi]]`). 1차 시도에 성공.

---

- 18:00 오케스트레이터: **러닝북 5종 전부 수행 — prod 정상, 신규 이슈·incident 0건, 조치 0건. 12:00이 남긴 "트래픽 감소" 미결 질문을 이번에 닫았다.**
  - **헬스체크 OK.** backend `{"status":"ok","db":true,"katago_alive":true}` · web 200. launchctl `com.baduk.api` PID **512** / `com.baduk.web` PID **497** — uptime 실측 **2일 18시간 00분**(`Wed Jul 29 00:01:09` 기동) = **66시간 무재기동**(7사이클 연속 동일 PID). 디스크 **6%**, `sync-launchd.sh --check` exit 0·무출력(drift 없음). staging 중단 = 정책상 정상.
  - **백업 검증 OK.** 최신 `baduk-20260731T040005.db.gz`(3,167,845B ≈ 3.02MB, `Jul 31 04:00`), `-mtime +1` 미해당 = 신선. 복원 드릴 `integrity_check=**ok**` · **9 tables**(12:00에 이어 2회 연속 ok). 티어 **14 / 8 / 2** 유지.
  - **버그 스캔 신규 0건.** `.err` **225,398줄 / mtime `Jul 29 00:01`** 로 6사이클 연속 바이트 단위 불변 = **66시간 트레이스백 0건**(api uptime과 정확히 일치 = 기동 이래 단 한 건도 없음). 같은 구간 `.log`는 207,436줄로 계속 자라므로 로그 정지가 아니다(`[[api-log-stdout-vs-stderr]]`). 이슈 생성 0건.
  - **백로그 트리아지 — 미분류 0건.** `gh issue list --search "no:label"` 빈 배열. 열린 이슈 #65·#70 2건 유지, 둘 다 PR 대기.
  - **PR 감시 — 3건 전부 그린, 주의 PR 0건.** #66·#71·#72 모두 `mergeable=MERGEABLE` + CI 4잡(backend·frontend·app-shell-build·e2e) 전부 **SUCCESS** 실측.

  ### 트래픽 감소는 시간대 효과가 아니었다 — 그러나 장애도 아니다

  12:00은 "13시간 창이 KST 심야를 포함해 시간대 효과와 구분되지 않는다. 18:00(주간 구간 포함) 밀도 복귀 여부가 판별점"으로 남겼다. **이번에 주간 구간을 깨끗하게 쟀고, 복귀하지 않았다 — 오히려 더 줄었다.**

  창 크기 추정 없이 **12:00→18:00 델타를 직접 잘라** 측정했다(`.log` 207,347 → **207,436줄 = 6시간에 89줄**). `[[bug-scan-verify-recency]]`의 창 크기 함정을 이번엔 아예 회피했다.

  | 델타 89줄 (KST 12:00~18:00 = 순수 주간) | 건수 | 환산 |
  |---|---|---|
  | `GET /api/health` | **79** | 약 4.6분 간격 — 12:00 측정과 동일 = 모니터 정상 |
  | `GET /api/spectate/pro/{id}` | **4** | **약 0.67건/h** |
  | 프로 themes·sitemap | 3·3 | 크롤러 인덱싱 |
  | 대국 경로(`/api/games`·`/api/ws/games`) | **0** | — |
  | 상태코드 | **89건 전부 200** | 4xx·5xx **0건** |

  프로 상세 밀도 추이 — 02:00 **약 6.0건/h** → 12:00 **약 2.0건/h** → 18:00 **0.67건/h**. **주간 구간이 심야 구간보다 낮으므로 시간대 효과 가설은 기각된다.** 크롤러 유입이 실제로 잦아든 것이다.

  **"방문이 없는 것"과 "공개 경로가 끊긴 것"을 능동적으로 갈랐다.** 트래픽만 보면 둘이 구분되지 않으므로 외부에서 직접 쳤다 — `https://inkbaduk.com` **200**(0.38s) · `www` **200** · `inkbaduk.com/api/health` **200** · `/sitemap.xml` **200**, `cloudflared` tunnel 가동(pid **612**). **공개 경로는 멀쩡하다.** 따라서 조용한 것이지 끊긴 게 아니다 — incident·이슈를 만들지 않았다.

  `visit_hits` **57행**(12:00과 동일, **+0**) — 6시간 동안 사람 방문 비콘 0건으로 위 판정과 정합한다.

  ### 그 대가로 #65·#69 라이브 검증은 더 멀어졌다

  - **#69(대국 프리즈 fix, `38fd9e3`) — 7사이클 연속 미검증.** DB `max(games.id)`가 여전히 **335**(7/26)이고 델타 89줄에 대국 경로 0건. **무사고가 아니라 무부하다.**
  - **#65(조회수 락) — 66시간 무재발이나 증거 가치는 사이클마다 약해지고 있다.** 0.67건/h는 #65를 재현시킨 크롤러 버스트(17.7h/16건 ≈ 0.9건/h... 밀도만으로는 비슷해 보이나 그 구간은 *동시성* 버스트였다)와 성격이 다르고, `busy_timeout` 30s가 에러를 *지연*으로 바꾸므로 무재발이 곧 해소가 아니다. PR #66 미머지 = 원자적 UPDATE는 여전히 prod에 없다. 판정 보류 유지.

  ### 그 밖에 확인만 하고 넘긴 것

  - **스캐너 프로브 횡보.** 최근 1,500줄 404 **20종/24건** — `/api/settings` 3 · `/api/.env` 3 · `/api/v1/settings` 2 · `/api/v1/credentials`·`/api/v1/get/dkim/private/`·`/api/v4/email/test` 등. 12:00 대비 **종류·건수 모두 증가 없음**. 전부 404 = 라우팅 경로 없음. 관찰만 유지.
  - **주기 불일치 재확인(미해소).** 이번 트리거도 **18:00:05** = plist(12:00·18:00 2회)대로 발화. 프롬프트·러닝북의 4회(06:30·12:30·18:30·23:30) 표기와 불일치. 유실이 아니라 문서·plist 미정합이며 사람 판단 대기 유지.
  - **CWI ingest는 stale이 아니다.** 최근 실행 7/26 03:11(`new=200`)로 5일 전이나, plist가 `Weekday 0`(일요일 03:00) = **주 1회 잡**이다. 다음 실행 **8/2**. 정상.

  ### 사람 판단 대기 3건 — 전부 머지 버튼 하나 (변동 없음)

  | ID | 대상 | 무응답 경과 | 비고 |
  |---|---|---|---|
  | AP-20260730-01 | PR #72 (watchdog 성공마커 판정, #70) | 약 **49시간** | 미머지 동안 watchdog 크래시 루프 사각지대 유지 |
  | AP-20260725-01 | PR #66 (조회수 원자적 UPDATE, #65) | 약 **156시간(6.5일)** | PR 자체는 생성 후 **6일 16시간** — **8/1 02:06 KST에 7일 정체 기준 돌파**, 8/1 12:00 사이클에서 처음 "주의 PR"로 잡힌다 |
  | AP-20260723-01 | PR #71 (`Write`→`Edit` deny 정정) | 약 **198시간(8.25일)** | prod DB·모델 가드레일 **9일째 무실효** — 이번 세션 시작 로그(`orchestrator-runs.log` 18:00:05)에도 동일 경고 **2건** 재출력 |

  **코드 변경·커밋·푸시·머지 없음(🟡은 사람).** 신규 승인 제안도 없다.

  - 결과 Telegram 1건 발송(message_id **2486**). `reply` 도구는 이번에도 미노출이라(`ToolSearch`로 "telegram reply send message channel" 검색 — Telegram 발송 도구 없음) Bot API 직접 호출했다. 토큰 source와 `curl`을 **같은 Bash 호출 안에** 넣고 한글 본문은 파일에 써서 `-F "text=</파일"` + `LC_ALL=en_US.UTF-8`로 보냈다(`[[dev-cycle-telegram-botapi]]`). 1차 시도에 성공.
