# 2026-09-17

- 04:30 dev-cycle — **#99 처리 완료 → PR [#100](https://github.com/rarebirds-svg/quite-baduk/pull/100) 생성(머지 🟡 사람 대기).** 적격 이슈 2건(#97 `bug`, #99 `bug`·`small`·`prio:low`) 중 #97은 PR #98이 열려 있어 사람 머지 대기 상태라 건너뛰고 #99 선택. `.worktrees/dev-cycle`에 `fix/issue-99` 브랜치(origin/main `2e255c1` 기준, 메인 트리 `web/node_modules` 심링크로 의존성 재사용 후 제거). TDD — vitest는 RSC 번들링을 재현하지 못하므로 실제 원인(서버 페이지가 `"use client"` 모듈에서 값 import)을 소스 수준에서 잡는 구조 테스트 + SSR fetch URL 검증을 `web/tests/spectate-pro-page.test.ts`에 작성, 구조 테스트가 수정 전 실패 확인 → `PAGE_SIZE`·`PRO_LIST_INITIAL_QUERY`를 directive 없는 `web/lib/proList.ts`로 이동, `page.tsx`·`ProGameList.tsx`가 import → 통과. vitest 241 passed·`tsc --noEmit`·`next lint` 통과. `npm run build`(워크트리) 결과 `.next/server/app/spectate/pro/page.js`에서 fetch 인자가 `new URLSearchParams({collection:"masterpiece",...}).toString()`으로 묶임을 확인(수정 전엔 클라이언트 참조). 리뷰(fable, 전경 동기) approve — 지적 1건(선행 주석 뒤 directive를 놓치는 정규식) 반영, `web/app` 서버 컴포넌트에 같은 클래스 추가 사례 없음, 잠재 지뢰 2건(`lib/proEvent.ts`→`@/lib/i18n` `t`, `lib/quickStart.ts`→`useAuthStore`, 현재 client 모듈만 import해 비활성)은 이슈 코멘트에 기록. Codex 교차 리뷰(`codex-review.sh origin/main`)는 11분 넘게 MCP 인증 오류만 내고 본문 없이 행 → 종료·건너뜀(쿼터 exit 3 아님, `rmcp AuthRequired` supabase/copilot MCP + `failed to refresh available models` timeout). 커밋 `4d77d1c` push, PR #100 본문에 web 재빌드·재기동 필요(🟡) 명시, 이슈 코멘트·`in-progress` 해제. 잡 상태 `ok` 기록. 부수 관찰 — `state/jobs/content-draft.json`이 9/16 02:04로 남아 있어 오늘 02:00 content-draft 실행 흔적 없음(09:00 오케스트레이터 확인 대상).
- 09:00 orchestrator(am) — **전 항목 정상, 신규 버그 0, PR #100 머지 승인 요청(AP-20260917-01) 등재, AP-20260916-01 24시간째 대기.** 헬스체크 — `/api/health` 200 `{db:true, migrations {current:0020, head:0020, pending:false}}`(`katago_alive:false`는 lazy spawn 정보용), web 200, 공개 `inkbaduk.com` 200·`/api/health` 200, `com.baduk.api` PID 5743(9/12 13:46 기동 유지, 115시간 무사고)·`com.baduk.web` PID 89541(9/8 23:56 기동 유지), staging 중단(정책상 정상), 디스크 8%, plist drift 없음(exit 0), Claude 인증 `ok D-15`(만료 10-02 23:41). 백업 — `baduk-20260917T040004.db.gz` 4,529,535B(9/16 4,525,500B, +4,035B) 04:00:06 완료, 9/3분 보존 정리, `find -mtime +1` 출력 없음, daily 14 / weekly 8 / monthly 4, 복원 드릴 `integrity_check` ok·테이블 11·사본 `games max(id)` 482·`pro_games` 4311 = **79회 연속** 통과. 버그 스캔 — `.err` **+6줄**(235,098 → 235,104줄, mtime 9/17 00:23) 전부 #482 WS accepted/open/closed ×2, **트레이스백 신규 0·IntegrityError 0·locked 0**. `.log` 324,140 → 324,445줄(**+305**) 분포 200 299·201 2·204 1·401 1·404 1, **5xx 0·429 0** — 404 1은 GCP `34.91.154.40` `/api/.env` 프로브, 401 1은 첫 `/api/session` 정상, 201 2는 `POST /api/session`+`POST /api/games`(#482). 소스 Cloudflare 모니터 143·127.0.0.1 SSR 98·`1.237.136.170` 59(운영자 — 9路 대국 1판 + `/api/admin/*` 관리자 화면 열람). `/api/spectate/pro?[object%20Object]` 10회는 #99(PR #100 대기, 라이브 미반영이라 예상된 재현). `baduk-web.err` +0(기준선 3,495). 21:46 KST `session_purge` removed 2(정상). DB — `games max(id)` **482**, **오늘 대국 1건·52수**(#482 9路 9k balanced, 운영자 닉 `대공`, 00:11~00:22 KST, B+73.5 finished — KataGo 슬롯 1이 00:22 9×9로 spawn·정상 응수), 진행 중 0(마지막 수 00:22:33 KST), `visit_hits` 595 → 596(+1), `pro_games` 4311. 열린 `bug` 이슈 #97(PR #98)·#99(PR #100) 모두 PR 대응 중이고 새 트레이스백 없음 → **신규 이슈 0건**. 백로그 트리아지 — 미분류 이슈 0건. PR 감시 — 열린 PR 2건 [#98](https://github.com/rarebirds-svg/quite-baduk/pull/98)(9/16 04:34 KST 생성, 28.5h, CI 4잡 SUCCESS, `MERGEABLE`/`CLEAN`, `merge-tree` 충돌 0) + [#100](https://github.com/rarebirds-svg/quite-baduk/pull/100)(04:45 KST 생성, dev-cycle #99 픽스, CI 4잡 SUCCESS, `MERGEABLE`/`CLEAN`, 충돌 0, 변경 파일 4개 `web/app/spectate/pro/page.tsx`·`web/components/ProGameList.tsx`·`web/lib/proList.ts`·테스트 1) — 둘은 backend/web으로 파일 겹침 없음 = **열린 PR 모두 정상, 주의 PR 0건**. `main` CI 최근 3런 success(최근 9/16 12:05 UTC, `2e255c1`). 배포 — `git rev-list --count HEAD..origin/main` **0**(HEAD=origin `2e255c1`), 라이브 api `c0d8d05`(9/12)·web BUILD_ID 9/8 23:56 이후 유입은 ops 문서·콘텐츠뿐이라 **미반영 0**. 잡 상태 4/4 `ok`(dev-cycle 04:46 · content-draft 9/16 02:04 · analytics-weekly 9/13 · news-hook 9/13). content-draft는 plist `Weekday 3·6`(수·토) 02:00 잡이라 오늘(목) due 아님 — 04:30 dev-cycle 로그의 "오늘 02:00 실행 흔적 없음" 관찰은 오독, 다음 실행 9/20(토). watchdog — 9/17 WD 마커 0건. 승인 — **AP-20260916-01** 대기 유지(24시간째 · 2회 재확인, PR #98 CI·`CLEAN` 재확인, 진행 중 대국 0). **AP-20260917-01 신규 등재** — PR #100 머지 + pull + web 재빌드(`npm run build`) + `com.baduk.web` 재기동(🟡). 두 건을 한 회차로 묶어 발효 권장(api 재기동 1회 + web 재빌드·재기동 1회). 관찰 유지 — 슬롯별 KataGo 재spawn, `ai-strength-levels.md` "입문부터" 문구.
- 09:05 orchestrator(am) — am 다이제스트 발송 완료(`send.py` exit 0, 마커 `~/.ops-report/markers/inkbaduk-am-2026-09-17` 생성). 승인 카드 2장(AP-20260916-01·AP-20260917-01) 동봉.
- 21:00 orchestrator(pm) — **전 항목 정상, 신규 버그 0, 오늘 대국 9판·136수(진행 중 1건), AP-20260916-01 36시간째·AP-20260917-01 12시간째 대기.** 헬스체크 — `/api/health` 200 `{db:true, migrations {current:0020, head:0020, pending:false}}`(`katago_alive:false`는 lazy spawn 정보용), web 200, 공개 `inkbaduk.com` 200·`/api/health` 200, `com.baduk.api` PID 5743(9/12 13:46 기동 유지, 127시간 무사고)·`com.baduk.web` PID 89541(9/8 23:56 기동 유지), staging 중단(정책상 정상), 디스크 8%, plist drift 없음(exit 0), Claude 인증 `ok D-15`(만료 10-02 23:41). 백업 — am과 같은 `baduk-20260917T040004.db.gz` 4,529,535B, `find -mtime +1` 출력 없음, daily 14 / weekly 8 / monthly 4, 복원 드릴 `integrity_check` ok·테이블 11 = **80회 연속** 통과. 버그 스캔 — `.err` **+41줄**(235,104 → 235,145줄, mtime 20:53) 전부 #483~#493 WS accepted/open/closed, **트레이스백 신규 0·IntegrityError 0·locked 0**(마지막 트레이스백은 여전히 9/15 12:56 #97 실사례 line 235,024). `.log` 324,445 → 324,978줄(**+533**) 분포 200 467·201 13·204 34·401 6·404 14, **5xx 0·429 0**(마지막 500은 9/15 #97 건 line 322,031로 기준선 이전). 404 14는 전부 스캐너 프로브 — `181.233.125.233` `/api/session/reset_password`·`properties` 5, GCP `34.18.231.224` `.env` 6종, `34.158.119.210` `phpinfo.php`·`config.json`. 401 6은 익명 첫 `/api/session` 정상. 소스 — 사람 `59.23.143.78` 232·Cloudflare 모니터 144·127.0.0.1 SSR 98·사람 `166.104.68.59` 19·`211.251.11.8` 18. `/api/spectate/pro?[object Object]` **9회**(#99 미반영 재현, 누적 627). `baduk-web.err` **+11줄**(3,495 → 3,506) = `Failed to find Server Action "x"` 스택 1건 — web 프로세스 기동(9/8 23:56)이 BUILD_ID와 같은 빌드라 옛 클라이언트 재생, 재기동·이슈 대상 아님(`[[web-err-stale-server-action]]`), 기준선 3,506으로 갱신. **오늘 대국 — 9판·136수, 관측 이래 최다 활동.** `59.23.143.78`이 09:20~09:58 KST에 #483~#491 9판 연속 생성(9路 8·19路 1, #487 9路 9k 64수 B+3.5 사용자 승, 나머지는 0~28수 미완), `166.104.68.59`가 20:53 KST에 #492(19路 1수)·**#493(13路 5k `hoon`, 30수, 마지막 수 21:00:51 KST — 진행 중)**. `visit_hits` 596 → 629(+33/12h). 백로그 트리아지 — 미분류 이슈 0건. PR 감시 — 열린 PR 2건 [#98](https://github.com/rarebirds-svg/quite-baduk/pull/98)(9/16 04:34 KST 생성)·[#100](https://github.com/rarebirds-svg/quite-baduk/pull/100)(9/17 04:45 KST 생성) 모두 CI 4잡 SUCCESS·`MERGEABLE`/`CLEAN`, 정체(7일)·충돌·CI 실패 0건 — "열린 PR 모두 정상". 잡 상태 4/4 `ok`(dev-cycle 04:46 · content-draft 9/16 02:04(다음 9/20 토) · analytics-weekly 9/13 · news-hook 9/13). 배포 갭 0(`HEAD`=`origin/main`=`01f84f0`, 9/12 재기동 이후 코드 커밋 없음 — 콘텐츠·docs만). 승인 — **AP-20260916-01** 대기 유지(36시간째 · 3회 재확인), **AP-20260917-01** 대기 유지(12시간째 · 1회 재확인). **주의 — 두 승인 실행 시 #493이 진행 중일 수 있으므로 절차 (4) `moves.played_at` 최근 5분 확인을 반드시 거칠 것.** 관찰 유지 — 슬롯별 KataGo 재spawn 이슈 미등록 참고.
- 21:03 orchestrator(pm) — pm 다이제스트 발송 완료(`send.py` exit 0, 마커 `~/.ops-report/markers/inkbaduk-pm-2026-09-17` 생성). 승인 카드 2장(AP-20260916-01·AP-20260917-01) 동봉, #493 진행 중 주의 문구 포함.
