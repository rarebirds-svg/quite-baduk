# 운영 로그 — 2026-06-19

## 02:00 자율 버그 사이클 (dev-cycle)

### 이슈 선택
- `/opt/homebrew/bin/gh issue list --state open --json number,title,labels` → 열린 이슈 0건.
- `bug`/`small` 라벨 적격 이슈 없음 → **처리할 버그 없음**.
- 종료. worktree·브랜치 생성 안 함.

### 비고
- `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 조회.
- 6/13~6/18과 동일하게 신규 열린 이슈 없음.

### Telegram 보고
- telegram reply/send 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`(`ops/ops.env`)로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=True, message_id=2261).

## 12:30 오케스트레이터 (정기)

### 헬스체크 🟢
- prod-backend 200 OK (`{"status":"ok","db":true,"katago_alive":false}` — lazy spawn, 정보용), prod-web 200 OK.
- launchctl: `com.baduk.api` PID 923, `com.baduk.web` PID 912 ✅.
- 디스크 6%, plist drift 없음 (`sync-launchd.sh --check` exit 0).
- staging 중단 (정책상 정상).

### 백업 검증 🟢
- 최신 `baduk-20260619T040003.db.gz` (mtime+1 비해당 = 신선), integrity_check=ok, 7 tables.
- 티어 daily 14 / weekly 4 / monthly 1.

### 버그 스캔 🟢
- `baduk-api.err`의 `TypeError: bytes is not JSON serializable` 누적 2회뿐, 파일 최종 수정 6/16T23:22 (3일 무변동) → **stale·비재발**. 신규 이슈 없음 ([[bug-scan-verify-recency]] 준수).
- 최근 로그 말단은 정상 WS open/close만.

### 백로그 트리아지 🟢
- 열린 이슈 0건 → 미분류 없음.

### PR 감시 🟢
- 열린 PR 0건 → 주의 PR 없음.

### 상태 요약
- 보류 승인 0건. 최근 CWI ingest 2026-06-14 03:04 (new=200). 최근 분석 2026-W24.

### Telegram 보고
- telegram send/reply 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=True, message_id=2262).

## 18:30 오케스트레이터 (정기)

### 헬스체크 🟢
- prod-backend 200 OK (`{"status":"ok","db":true,"katago_alive":false}` — lazy spawn, 정보용), prod-web 200 OK.
- launchctl: `com.baduk.api` PID 923, `com.baduk.web` PID 912 ✅.
- 디스크 6%, plist drift 없음 (`sync-launchd.sh --check` exit 0).
- staging 중단 (정책상 정상).

### 백업 검증 🟢
- 최신 `baduk-20260619T040003.db.gz` (mtime+1 비해당 = 신선), integrity_check=ok, 7 tables.
- 티어 daily 14 / weekly 4 / monthly 1.

### 버그 스캔 🟢
- `baduk-api.err` 최종 수정 6/16T23:22 (3일 무변동) → stale·비재발. 말단은 정상 WS open/close만. 신규 이슈 없음 ([[bug-scan-verify-recency]] 준수).

### 백로그 트리아지 🟢
- 열린 이슈 0건 → 미분류 없음.

### PR 감시 🟢
- 열린 PR 0건 → 주의 PR 없음.

### 상태 요약
- 보류 승인 0건. 최근 CWI ingest 2026-06-14 03:04 (new=200). 최근 분석 2026-W24.

### Telegram 보고
- 도구 세션 미노출 동일 → Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=True, message_id=2263).
