# 운영 로그 2026-05-28

## 02:00 — 자율 버그 사이클 (launchd 트리거)

- **이슈 후보 조회** — `gh issue list --state open` 결과 0건. `bug`·`small` 적격 0건.
- **처리 결과** — 처리할 버그 없음. worktree 작업·커밋·PR 없음.
- **참고** — 최근 닫힌 이슈는 #12·#13(2026-05-25), #15(2026-05-23). 5/26~5/28 신규 유입 0건. 자율 버그 사이클은 3일 연속(5/26·5/27·5/28) 적격 이슈 없음으로 종료.
- **Telegram 보고** — reply 도구 deferred 부재(`ToolSearch "telegram reply"`에 Canva·Gmail만 노출). fallback: `~/.claude/channels/telegram/.env` + `ops/ops.env` 로드 후 Bot API `sendMessage` curl 직접 호출 → `ok=True`, `message_id=2051`.

## 12:00 — 정기 오케스트레이터 (launchd 트리거)

- **헬스체크** — prod backend `/api/health` 200 (`{"status":"ok","db":true,"katago_alive":false}` — lazy spawn, 정보용), prod web `:3000` 200, launchctl `com.baduk.api`(PID 1466)·`com.baduk.web`(PID 1455) 가동. staging 중단(정상). 디스크 사용률 4%. `ops/sync-launchd.sh --check` exit=0 (drift 없음).
- **백업 검증** — 최신 `baduk-20260528T040005.db.gz` (약 8h, mtime 24h 미만), 티어 daily 9 / weekly 1 / monthly 0. 복원 드릴: `integrity_check=ok`, 테이블 7개.
- **버그 스캔** — `~/Library/Logs/baduk-api.err` 최근 200줄 검토. WS `RuntimeError: Cannot call "send" once a close message has been sent` 18건 누적되어 있으나 마지막 발생은 라인 220961(SQL 파라미터 timestamp `2026-05-25 14:06:43`)로 PR #30 머지(05-25 14:34 UTC) 이전 잔재. 최신 server start(라인 220975) 이후 신규 에러 0건. 신규 이슈 등록 0건.
- **백로그 트리아지** — `gh issue list --search "no:label"` 빈 배열. 분류 대상 없음.
- **PR 감시** — `gh pr list --state open` 빈 배열. 정체·실패 PR 없음.
- **보류 승인** — `state/pending-approvals.md` 대기 0건.
- **콘텐츠/분석** — 최근 CWI ingest 2026-05-27 21:41 (fetched=0, new=0, unchanged). 최근 주간 리포트 `2026-W21.md` (누적 1).
- **Watchdog** — `watchdog.out.log` 마지막 15회 연속 "신규 incident 0건" — `72ec362`(임계 9d 정합화) 적용 효과 확인.
- **Telegram 보고** — reply 도구 미노출(`ToolSearch "+telegram"`에 매칭 0건). fallback curl `sendMessage` → `ok=True`, `message_id=2053`.

## 18:00 — 정기 오케스트레이터 (launchd 트리거)

- **헬스체크** — prod backend `/api/health` 200 (`{"status":"ok","db":true,"katago_alive":true}`), prod web `:3000` 200, launchctl `com.baduk.api`(PID 1466)·`com.baduk.web`(PID 1455) 가동. staging 중단(정상). 디스크 사용률 4%. `ops/sync-launchd.sh --check` exit=0.
- **백업 검증** — 최신 `baduk-20260528T040005.db.gz` (≈14h, mtime 24h 미만), 티어 daily 9 / weekly 1 / monthly 0. 복원 드릴: `integrity_check=ok`, 테이블 7개.
- **버그 스캔** — `~/Library/Logs/baduk-api.err` 최근 200줄 검토. 마지막 `database is locked` 라인 220909(SQL 파라미터 timestamp `2026-05-25 14:06:43`)는 PR #30 머지 이전 잔재. 최신 server start(라인 220975) 이후 신규 lock 0건. 신규 이슈 등록 0건.
- **백로그 트리아지** — `gh issue list --search "no:label"` 빈 배열.
- **PR 감시** — `gh pr list --state open` 빈 배열.
- **보류 승인** — `state/pending-approvals.md` 대기 0건.
- **콘텐츠/분석** — 최근 CWI ingest 2026-05-27 21:41 (fetched=0, new=0). 최근 주간 리포트 `2026-W21.md` (누적 1).
- **Watchdog** — `watchdog.out.log` 최근 21회 연속 "신규 incident 0건" — 임계 9d 정합화(`72ec362`) 효과 지속.
- **Telegram 보고** — reply 도구 미노출(`ToolSearch "telegram"`/`"+telegram reply"` 매칭 0). fallback curl `sendMessage` → `ok=True`, `message_id=2054`.
