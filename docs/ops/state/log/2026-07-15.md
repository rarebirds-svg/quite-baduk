# 2026-07-15 운영 로그

## content-draft (02:00)

**처리할 토픽 없음** — 시드 소진으로 no-op 종료. 초안·게시·커밋 없음.

- 미작성 후보 계산 (실제 `ls` 기준):
  - **glossary 18/18 게시 완료** (비율 1.00) — 시드 슬러그 18개 전부 `web/content/glossary/`에 존재. 미작성 후보 **0건**.
  - **faq 9/9 게시 완료** (비율 1.00) — 시드 슬러그 9개 전부 `web/content/faq/`에 존재. 미작성 후보 **0건**.
  - `docs/ops/content/drafts/` 비어 있음 — 이월된 미게시 초안·"QA 보류" 건 없음.
- 두 종류 모두 후보가 0건이므로 러닝북의 "처리할 토픽 없음 → 즉시 종료" 경로를 탄다. **기존 항목은 임의 개정하지 않음** (마지막 게시: `susangjeon`, 커밋 `fdafdd7`).
- **사람 판단 필요** — 시드 파일(`docs/ops/content/seed-glossary.yml`·`seed-faq.yml`)에 새 항목이 추가되지 않는 한 이 사이클은 계속 no-op다. 콘텐츠를 더 늘리려면 시드에 신규 `{slug, title, prompt_hint}`를 추가해야 한다.
- Telegram: 게시 건이 없어 발송 없음(사후 보고는 게시 시에만).

## dev-cycle (02:00)

**#54 처리 완료 → PR #55 (머지 대기 🟡).** 6주간 반복돼 온 `gh` PATH 부재를 근본 수정했다.

- **이슈 선택** — 열린 이슈 1건: **#54** (`chore`·`small`, `feature`·`in-progress` 없음) → 적격. `in-progress` 선점 후 처리, 완료 시 라벨 제거.
- **재현 (자기 자신에게서)** — 이 세션의 **첫 `gh` 호출이 바로 exit 127로 실패**했다. 이슈 그대로의 증상. launchd가 준 PATH는 `/usr/bin:/bin:/usr/sbin:/sbin`(+플러그인 bin)뿐 — `/opt/homebrew/bin` 없음. `env -i PATH="/usr/bin:/bin:/usr/sbin:/sbin" bash -c 'command -v gh'` → not found 로 격리 재현.
- **수정** — `com.inkbaduk.*` launchd 진입 래퍼 **7개 전부**의 `set -euo pipefail` 직후에 Homebrew 경로 prepend:
  ```sh
  export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"
  ```
  대상: `run-dev-cycle` `run-orchestrator` `run-watchdog` `run-analytics-weekly` `run-content-draft` `run-content-ingest` `backup`. `export`라 자식(`claude` → Bash 툴 → `gh`)까지 전파된다 — **`gh`는 래퍼가 아니라 헤드리스 세션 *안에서* 호출되므로 이 전파가 핵심**이다.
- **설계 판단 — 왜 plist가 아니라 래퍼인가**
  - plist `EnvironmentVariables`는 `launchctl` 재등록이 필요하고, 재등록은 prod launchd 잡을 건드리므로 **그 자체가 🟡**가 된다. 래퍼는 다음 스케줄 실행부터 그냥 적용된다.
  - 수동 실행 경로도 함께 보호된다.
  - `ops/ops.env`(6개 스크립트가 이미 source)는 단일 지점으로 매력적이었으나 **`.gitignore` 대상** — 버전 관리가 안 되고 fresh clone에서 사라진다. 제외.
  - 덮어쓰기가 아닌 **prepend**라 나중에 plist가 PATH를 주더라도 안전.
- **검증** — `bash -n` **7/7** 통과. launchd bare env에서 **각 파일에 실제 커밋된 `export` 라인**을 eval → **7/7** 모두 `gh` → `/opt/homebrew/bin/gh` 해석 확인. diff는 7파일 × 2줄 = **+14줄**, 그 외 변경 없음. 메인 트리 오염 0.
  - `run-watchdog.sh` 전체 실행은 **의도적으로 하지 않았다** — `check-staleness.sh`가 `ROOT`를 **메인 트리로 하드코딩**하고 있어 워크트리에서 돌리면 실제 `incidents.md`에 append하고 Telegram 알림을 발사한다. 2줄 preamble 검증에 감수할 부작용이 아님.
- **결과** — 브랜치 `fix/issue-54`, 커밋 **`73cb3cf`**, **PR #55** 생성. 남은 사람 단계는 **머지뿐**(🟡).
- Telegram: 결과 1건 발송 ✅. 세션에 `reply` 도구 미노출(`SendMessage`는 에이전트 간 통신) → [[dev-cycle-telegram-botapi]]에 따라 `ops/notify.sh`(Bot API) 경유 발송, exit 0 확인. "미발송"으로 기록하지 않음.

### 러닝북 stale 발견 — `dev-pipeline.md`의 "push deny" 서술

`docs/ops/runbooks/dev-pipeline.md` 3단계는 *"`settings.json`의 deny 규칙이 헤드리스에서 `git push`를 차단하니 자율 세션에서 시도하지 마라"* 며 핸드오프 코멘트만 남기라고 지시한다. dev-cycle 프롬프트에도 같은 전제가 박혀 있다.

**그러나 이는 사실이 아니다.**

- `autonomy-policy.md` L43 — feature 패턴(`fix/*`·`feat/*`…) 브랜치 push = **🟢 자율**, `settings.json` allow 목록 명시.
- `autonomy-policy.md` L47·L50 — PR 생성 = 🟢, *"dev-cycle은 feature 패턴 브랜치 push·PR 생성까지 자율로 수행하고, 머지만 사람 승인을 받는다."*
- **실증** — 이 세션에서 `git push -u origin fix/issue-54` **성공**, `gh pr create` **성공**(#55). 차단 없음.

러닝북이 정책 변경([[dev-cycle-push-blocked]] 시점)을 따라가지 못한 것이다. 방치하면 dev-cycle이 매번 push 가능한 브랜치를 로컬에 묵혀두고 "사람이 push하세요" 코멘트만 남기는 [[dev-cycle-unpushed-branches]] 패턴이 재발한다. **별건으로 러닝북 3단계 갱신 필요** — 이번 PR 범위 밖이라 PR·이슈 코멘트에만 기록.

### 범위 밖 (후속 고려)

- `backend/deploy/run_local_prod.sh` (`com.baduk.api`)도 PATH 고정이 없다. 다만 `gh`를 쓰지 않고 prod 크리티컬 경로라 이번 변경에서 제외했다.
- `ops/check-staleness.sh`의 `ROOT` 하드코딩(`/Users/daegong/projects/baduk`)은 워크트리에서 실행 시 메인 트리 상태 파일을 오염시킨다. 지금은 launchd만 호출하므로 무해하나, 테스트 가능성 측면에서 개선 여지.

## orchestrator (12:00)

5개 러닝북 1회 실행. **prod 이상 0건.**

- **healthcheck** 🟢 — prod backend·web 200 OK, db OK, `katago_alive:false`(lazy spawn, 정보용·경보 아님), 디스크 6%, launchd drift 없음(exit 0). PID api 1516 / web 1504 = 7/14 18:30과 동일 → 무중단 지속. staging 중단(정책상 정상).
- **backup-verify** 🟢 — 최신 `baduk-20260715T040005.db.gz`(오늘 04:00, stale 아님). 티어 daily 14 / weekly 8 / monthly 2. 복원 드릴 `integrity_check=ok`, 7 tables. 이상 없음.
- **bug-scan** 🟢 — `baduk-api.err` 최근 로그는 정상 WS accept/close·재시작 INFO만. 스택 트레이스·반복 예외 없음. 열린 `bug` 이슈 0건. **신규 버그 없음(0건).**
- **backlog-triage** 🟢 — 미분류 이슈 0건(빈 배열). 할 일 없음.
- **pr-watch** 🟢(보고) — 열린 PR 1건: **#55**(#54 수정, 7/14 생성 ≈19h, MERGEABLE). CI: backend·frontend·app-shell-build ✅, **e2e ❌ FAILURE**. e2e는 [[ci-e2e-disabled]] 이력대로 옛 흐름 가정 스펙이라 코어 3체크 통과로 판단, 실제 e2e 수정은 dev 작업. **주의 PR 1건**으로 요약에 포함. 정체(7일↑) 아님.
- **자기 재현 메모** — 이 orchestrator 세션도 첫 `gh` 호출이 **exit 127**(launchd PATH에 `/opt/homebrew/bin` 부재) — #54/PR #55가 정확히 겨냥한 증상. PR 머지 전까지 래퍼 preamble이 미적용이라 예상된 상태. `/opt/homebrew/bin/gh` 절대경로 폴백으로 러닝북 완주.
- **보류 승인 0건**(AP-20260606-01은 자동 해소·무효). 최근 CWI ingest 2026-07-12(new=200). 최근 주간 리포트 2026-W28.
- Telegram: 상태 요약 1건 발송 시도(아래 결과).

## orchestrator (18:30)

5개 러닝북 1회 실행. **prod 이상 0건.** 12:00 대비 변동 없음.

- **healthcheck** 🟢 — prod backend·web 200 OK, db OK, `katago_alive:false`(lazy spawn, 정보용·경보 아님), 디스크 6%, launchd drift 없음(exit 0). PID api 1516 / web 1504 = 7/15 12:00과 동일 → 무중단 지속. staging 중단(정책상 정상).
- **backup-verify** 🟢 — 최신 `baduk-20260715T040005.db.gz`(오늘 04:00, stale 아님). 티어 daily 14 / weekly 8 / monthly 2. 복원 드릴 `integrity_check=ok`, 7 tables.
- **bug-scan** 🟢 — `baduk-api.err` 최근 로그는 정상 WS accept/close·재시작 INFO만. 스택 트레이스·반복 예외 없음. 열린 `bug` 이슈 0건. **신규 버그 없음(0건).**
- **backlog-triage** 🟢 — 미분류 이슈 0건(빈 배열).
- **pr-watch** 🟢(보고) — 열린 PR 1건: **#55**(#54 수정, 7/14 생성 ≈1d, MERGEABLE). CI: backend·frontend·app-shell-build ✅, **e2e ❌ FAILURE**. e2e는 [[ci-e2e-disabled]] 이력대로 옛 흐름 가정 스펙 — 코어 3체크 통과. 실제 e2e 수정·머지(🟡)는 사람 단계. **주의 PR 1건**으로 요약 포함. 정체(7일↑) 아님.
- **자기 재현 메모** — 이 세션도 첫 `gh` 호출 **exit 127**(launchd PATH에 `/opt/homebrew/bin` 부재) — #54/PR #55가 겨냥한 증상. 머지 전까지 예상된 상태. `/opt/homebrew/bin/gh` 폴백으로 완주.
- **보류 승인 0건**. 최근 CWI ingest 2026-07-12(new=200). 최근 주간 리포트 2026-W28.
- Telegram: `reply` 도구 미노출(이 세션에 telegram MCP 미로드 — Canva·Context7·Gmail·Drive·HF만). [[dev-cycle-telegram-botapi]]에 따라 `ops/notify.sh`(Telegram Bot API → macOS 폴백) 경유 상태 요약 1건 발송, **exit 0** 확인.
