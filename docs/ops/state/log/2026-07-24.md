# 2026-07-24

- 02:00 dev-cycle: **처리할 버그 없음** — `gh issue list --state open`가 빈 배열 반환. 열린 이슈 **0건**(적격 `bug`/`small` 0건), 열린 PR **0건**. gh 인증 정상(`rarebirds-svg`, scopes repo/workflow). 선점 라벨 부착 없음, `.worktrees/dev-cycle` 손대지 않음(net-zero). 커밋·push·PR 없음. 7/23 02:00에 이어 이틀 연속 무작업 — 백로그가 실제로 비어 있는 상태다.
  - 부수 점검(`[[dev-cycle-unpushed-branches]]` 재발 방지) — 로컬 feature 패턴 브랜치 11개 중 **origin 미push 6개**: `feat/format-game-result`, `feat/i18n-ja-zh`, `fix/auto-resign-respect-score-margin`, `fix/issue-12`, `fix/scoring-gate-too-strict`, `fix/sqlite-busy-timeout`. 다만 이들은 이번 사이클이 만든 것이 아니고, 대응 이슈가 전부 CLOSED(#12·#25 등 — 픽스가 squash 머지로 main 반영된 것으로 보임)이라 **미완 작업이 아니라 정리 안 된 잔여 브랜치**로 판단. `merge-base --is-ancestor`는 squash 머지를 못 잡으므로 머지 여부는 이 결과만으로 단정하지 않음(`[[git-three-dot-diff-trap]]`). 자율 삭제하지 않고 사람 판단용으로만 기록.
  - Telegram 결과 보고 1건 발송(아래).

- 12:00 오케스트레이터 — 러닝북 5종 수행.
  - **헬스체크 OK.** prod backend `{"status":"ok","db":true,"katago_alive":false}` 200, prod web 200. launchctl `com.baduk.api` PID 1516 / `com.baduk.web` PID **49162**. staging 중단(정책상 정상). 디스크 6%. `sync-launchd.sh --check` exit 0(drift 없음).
    - web PID 변경(10162 → 49162, 기동 `Thu Jul 23 23:15:17`)은 **장애 아님** — 7/23 23:15 SEO 커밋(`538192f`·`5c7df6b`·`e467c1e`) 반영 재빌드·재기동. api는 7/12 00:53 기동 이래 **12일 11시간 무중단**.
  - **백업 검증 OK.** 최신 `baduk-20260724T040005.db.gz`(7/24 04:00, mtime +1 매치 없음). 복원 드릴 `integrity_check=ok`, 7 tables. daily/weekly/monthly = 14/8/2.
  - **버그 스캔 — 신규 1건(#65) 생성.** 아래 별도 항목.
  - **백로그 트리아지 — 미분류 0건.** `no:label` 빈 배열.
  - **PR 감시 — 열린 PR 0건.** CI 실패·충돌·정체 없음.
  - **보류 승인 1건** — `AP-20260723-01`(settings.json deny `Write(...)`→`Edit(...)`), 7/23 12:00 등재 후 답신 없이 **24시간 경과**. 이번 실행에서도 동일 하네스 경고 2건이 `orchestrator-runs.log`에 재출력됨 = 가드레일 여전히 실효 없음.

- 12:00 버그 스캔 상세 — **이슈 #65 생성** (`bug`, `prio:high`).
  - 제목: 프로 기보 조회수 UPDATE에서 `database is locked` 급증.
  - **판정 방법은 7/23 확정한 절차를 그대로 따랐다** — `grep -n "Started server process \[1516\]" | tail -1`로 기동 마커(line 221718)를 잡고 **그 이후 구간만**, `[SQL: ...]` 라인으로 **에러당 1건씩** 집계. 누적 카운트·`tail`·파일 내 위치는 쓰지 않았다.
  - 결과. 기동(7/12)~#64 철회(7/23 18:03) **11일간 3건** → 철회 이후 현재(7/24 11:44)까지 **17.7시간 16건**. 0.27건/일 → 21.7건/일, 약 **80배**. 두 수치 모두 동일 프로세스 스코프 집계라 #64의 오류 모드(위치를 최근성 대리지표로 사용)에 해당하지 않는다.
  - 신규 16건 내역: `UPDATE pro_games SET view_count` **14건**, `DELETE FROM moves ... is_undone`(무르기) 1건, `UPDATE games SET hint_count` 1건. 파라미터가 `(3, 379)`·`(4, 115)`처럼 조회수 한 자릿수의 서로 다른 기보 id로 흩어짐 = 크롤러가 상세를 훑는 패턴.
  - 원인 가설. `backend/app/api/spectate_pro.py:218` `game.view_count += 1` — 상세 GET마다 요청 경로 안에서 read-modify-write 커밋. 단일 라이터 SQLite에서 직렬화되고, 크롤러 동시 유입이 `busy_timeout=5000`을 초과.
  - 타이밍 정합. web 재기동 `7/23 23:15:17`(SEO 크롤 도달성 커밋 반영) 직후 구간(line 222805~)부터 `pro_games` 실패가 연속 발생. **백엔드 코드는 7/11 이후 무변경** — 코드 회귀가 아니라 **트래픽 패턴 변화로 기존 병목이 드러난 것**.
  - `baduk.db-wal` 5,009,952 바이트 — 어제와 **동일 크기(증가 없음)**. 체크포인트 정체는 별개 관찰 항목이며 이번 급증의 직접 원인으로 보지 않음.
  - 대시보드 "SQLite locked 판정" 행의 7/23자 결론(완화책 정상·이슈화 불필요)은 **그 시점 데이터로는 옳았고**, 이번 급증은 그 이후 발생한 새 사실이다. 행을 갱신했다.

- 12:00 부수 발견 — **러닝북 주기 서술이 실제 launchd 설정과 불일치**.
  - 러닝북 5종과 오케스트레이터 프롬프트는 주기를 `매일 06:30·12:30·18:30·23:30`(4회)로 적고 있으나, `ops/launchd/com.inkbaduk.ops-orchestrator.plist`의 `StartCalendarInterval`은 **12:00·18:00 2회**뿐이다(파일 헤더 주석도 "매일 12시·18시").
  - `orchestrator-runs.log` 실측도 일치 — 7/23 18:00 → 7/24 12:00. 즉 **06:30 실행 누락이 아니라 애초에 미등록**이다. `sync-launchd.sh --check` drift 없음 = 설치본도 동일.
  - sleep 요인 배제 확인. `pmset -g` `sleep 0 (sleep prevented by Claude)`, `standby 0`, `disksleep 0`. 슬립으로 인한 트리거 유실 아님.
  - 조치 안 함 — 문서를 2회로 맞출지, plist를 4회로 늘릴지는 **사람 판단 사항**(후자는 prod 스케줄 변경이라 🟡). 보고만 한다.

- 12:00 Telegram 보고 — **발송 성공** (`ok=true`, message_id **2443**).
  - 이 세션에 Telegram `reply` 도구가 노출되지 않아(ToolSearch 결과 에이전트용 `SendMessage`만 존재) `[[dev-cycle-telegram-botapi]]`대로 Bot API 직접 호출. 한글 본문은 파일에 써서 `--data-urlencode "text@/tmp/tg-msg.txt"` + `LC_ALL=en_US.UTF-8`로 발송.
  - 인바운드 승인 답신 없음 — `~/.claude/channels/telegram/inbox/` 최신 파일이 7/22 22:48이라 신규 메시지 0건. AP-20260723-01은 계속 대기.
  - 보고 내용: 헬스·백업 OK, 이슈 #65 생성 경위(어제 #64 오탐과 무엇이 다른지 포함), 보류 승인 1건 24시간 경과, 주기 불일치 사람 판단 요청.

- 12:00 메모리 갱신 — `[[bug-scan-verify-recency]]`에 "절차 유효성 확인 + 반대 방향 주의" 항목 추가.
  - 4연속 오탐 이력과 대시보드의 "완화책 정상" 결론 때문에 다음 세션이 `database is locked`를 **반사적으로 기각**할 위험이 생겼다. 절차의 목적은 기각이 아니라 올바른 구간에서 세는 것이며, 마커 이후 집계가 직전 실행 대비 유의미하게 늘면 이슈화가 맞다는 점을 명시했다.

- 18:00 오케스트레이터 — 러닝북 5종 수행.
  - **헬스체크 OK.** prod backend `{"status":"ok","db":true,"katago_alive":false}` 200, prod web 200. launchctl `com.baduk.api` PID 1516 / `com.baduk.web` PID 49162 — **12:00과 동일 PID, 재기동 없음**. staging 중단(정책상 정상). 디스크 6%. `sync-launchd.sh --check` exit 0(drift 없음).
  - **백업 검증 OK.** 최신 `baduk-20260724T040005.db.gz`(7/24 04:00, `find -mtime +1` 무매치). 복원 드릴 `integrity_check=ok`, 7 tables. daily/weekly/monthly = 14/8/2.
  - **버그 스캔 — 신규 0건.** 아래 별도 항목.
  - **백로그 트리아지 — 미분류 0건.** `no:label` 빈 배열. 열린 이슈는 #65 1건뿐이며 이미 `bug`·`prio:high` 라벨 보유.
  - **PR 감시 — 열린 PR 0건.** CI 실패·충돌·정체 없음.
  - **보류 승인 1건** — `AP-20260723-01`, 7/23 12:00 등재 후 **30시간 무응답**. 이번 실행에서도 `dev-cycle-runs.log`(7/24 02:00)에 동일 하네스 경고 2건이 남아 있음 = 가드레일 여전히 실효 없음.
  - 로컬 브랜치 재점검 — `git for-each-ref`의 upstream 필드 기준으로 **미push 브랜치 0건**(로컬 13개 중 12개가 origin 추적, 나머지 1개는 워크트리용 `worktree-feat+glossary-media-phase1`). 7/24 02:00 dev-cycle이 "미push 6개"로 적은 것은 upstream이 설정된 브랜치를 다른 방법으로 판정한 결과로 보이며, 실제로는 전부 origin에 존재한다. `[[dev-cycle-unpushed-branches]]` 대응 조치 불필요.

- 18:00 버그 스캔 상세 — **신규 이슈 0건. 급증 소강 관측(단, 트래픽 부재로 교란됨).**
  - `database is locked` — 12:00과 **동일한 절차**(기동 마커 `Started server process [1516]` line 221718 이후만, `[SQL:]` 라인으로 에러당 1건) 재집계. 결과 **누적 19건**으로 12:00 시점(3+16=19)과 **완전 동일 = 6시간 신규 0건**. 분포도 동일(`pro_games.view_count` 16 · `DELETE FROM moves` 2 · `hint_count` 1).
  - **다만 이 "0건"을 완화 신호로 읽으면 안 된다.** `baduk-api.err`의 mtime이 **7/24 12:49에서 멈춰 있다**(현재 18:01, 5시간 12분 무기록). uvicorn 액세스 로그(WS accept·connection open/closed)까지 한 줄도 안 늘었다 = 그 구간에 **요청 자체가 없었다**. 즉 관측된 것은 "락 경합이 줄었다"가 아니라 "트래픽이 없어서 경합할 일이 없었다"이다. 다음 트래픽 유입 구간에서 재측정해야 판정 가능하다.
  - 이슈 #65는 열린 상태 유지(7/24 03:03Z 생성, 코멘트 0). 원인 가설(`spectate_pro.py:218` 요청경로 내 read-modify-write)은 코드 사실이라 트래픽 유무와 무관하게 유효하다. 7/25 02:00 dev-cycle이 적격 이슈로 선점할 것으로 예상 — 7/24 02:00 dev-cycle은 #65 생성(03:03Z) *이전*에 돌아서 백로그가 비어 보였다.
  - 그 외 예외는 전부 **stale**이라 이슈화하지 않음 — `TypeError: Object of type bytes is not JSON serializable`(line 221457·221549), `RuntimeError: WebSocket is not connected`(221072·221124), `RuntimeError: Cannot call "send" once a close message has been sent`(220962·221256)는 모두 기동 마커(221718) **이전** 구간, 즉 6월 이하 구간이다. `RequestValidationError` 2건은 클라이언트 입력 오류라 서버 버그 아님.
  - `baduk.db-wal` 5,009,952 바이트 — **7/23·7/24 12:00과 동일 크기 3회 연속 정지**. 체크포인트 정체 관찰 항목으로 계속 추적하되, 이번에도 급증의 직접 원인으로 보지 않는다.

- 18:00 Telegram 보고 — **발송 성공** (`ok=true`, message_id **2444**).
  - 이 세션에도 Telegram `reply` 도구가 노출되지 않았다(ToolSearch 결과 에이전트간 `SendMessage`만 존재) → `[[dev-cycle-telegram-botapi]]`대로 Bot API 직접 호출로 폴백. 토큰은 `~/.claude/channels/telegram/.env`, chat_id는 `access.json`의 `allowFrom[0]`(`1241277614`). 한글 본문은 `/tmp/tg-msg-1800.txt`에 써서 `--data-urlencode "text@파일"` + `LC_ALL=en_US.UTF-8`로 발송.
  - 인바운드 승인 답신 없음(`~/.claude/channels/telegram/inbox/` 최신 파일 7/22 22:48, 신규 0건) → AP-20260723-01 계속 대기.
  - 보고 내용: 헬스·백업 OK, 버그·트리아지·PR 전부 0건, #65의 "신규 0건"이 트래픽 부재로 교란된 수치라는 단서, 보류 승인 1건 30시간 경과 재촉, 미push 브랜치 오탐 정정.
