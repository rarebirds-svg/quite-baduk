# 2026-07-06 운영 로그

- 02:00 자율 버그 사이클 (dev-cycle)
  - 이슈 선택: `gh issue list --state open` 결과 열린 이슈 0건. `bug`·`small` 적격 이슈 없음.
  - 결과: **처리할 버그 없음.** worktree·코드 변경·커밋·핸드오프 없이 종료.
  - 환경 메모: `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 사용 (7/4·7/5와 동일).
  - Telegram 보고: **발송 완료** (message_id 2325). 전용 reply 도구는 미노출이나, 진실
    공급원 `~/.claude/channels/telegram/{.env,access.json}`의 `TELEGRAM_BOT_TOKEN`과
    허용 chat_id(1241277614)로 Bot API(`sendMessage`)에 직접 POST → `ok:true`.
    (7/4·7/5의 "미발송" 기록은 도구 미노출만 보고 Bot API 경로를 시도하지 않은 것 —
    이번에 curl 직접 발송으로 실제 발송 가능함을 확인.)
    - 본문: `[inkbaduk 운영] 자율 버그 사이클 — 처리할 버그 없음` / 열린 이슈 0건 ·
      bug·small 적격 이슈 없음 · 코드 변경·커밋 없음 / 시각: 2026-07-06 02:00

- 12:00 오케스트레이터 정기 실행 (12:30 슬롯)
  - **헬스체크** 🟢: prod backend/web 200 OK, db OK, katago_alive:false(lazy spawn 정상·경보 아님).
    launchctl api=923 web=73784 가동. 디스크 6%. plist drift 없음(exit 0). staging 중단(정책상 정상).
  - **백업 검증** 🟢: 최신 baduk-20260706T040001.db.gz(30h 이내), integrity_check=ok, 7 tables.
    티어 daily 14 / weekly 7 / monthly 2.
  - **버그 스캔** 🟢: `~/Library/Logs/baduk-api.err` 최근 로그는 WS accept/open/close INFO만 —
    스택 트레이스·예외 없음. WS 295 403 1건은 SESSION_REPLACED 정상 거절. **신규 버그 없음.**
  - **백로그 트리아지** 🟢: 미분류 이슈 0건.
  - **PR 감시** 🟢: 열린 PR 0건.
  - 환경 메모: `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 사용(7/4·7/5·02:00과 동일).
  - Telegram 보고: **발송 완료** (message_id 2328). 전용 reply 도구는 세션에 미노출 —
    진실 공급원 `~/.claude/channels/telegram/{.env,access.json}`의 `TELEGRAM_BOT_TOKEN`과
    허용 chat_id(1241277614, `ops/ops.env`와 일치)로 Bot API `sendMessage`에 직접 POST → `ok:true`.
    heredoc 한글이 zsh 기본 로케일에서 range 오류를 내므로 `LC_ALL=en_US.UTF-8` + `--data-urlencode text@file`로 발송.
    - 본문: 정기 상태 요약(12:30) — 헬스 정상 · 백업 신선 · 신규버그 0 · PR 0 · 미분류 0 ·
      보류승인 0 · 최근 CWI ingest 2026-07-05 · 최근 분석 2026-W27.

- 18:00 오케스트레이터 정기 실행 (18:30 슬롯)
  - **헬스체크** 🟢: prod backend/web 200 OK, db OK, katago_alive:false(lazy spawn 정상·경보 아님).
    launchctl api=923 web=73784 가동. 디스크 6%. plist drift 없음(exit 0). staging 중단(정책상 정상).
  - **백업 검증** 🟢: 최신 baduk-20260706T040001.db.gz(30h 이내), integrity_check=ok, 7 tables.
    티어 daily 14 / weekly 7 / monthly 2.
  - **버그 스캔** 🟢: `~/Library/Logs/baduk-api.err` 최근 로그는 WS accept/open/close INFO만 —
    스택 트레이스·예외 없음. WS 295 403 1건은 SESSION_REPLACED 정상 거절. **신규 버그 없음.**
  - **백로그 트리아지** 🟢: 미분류 이슈 0건.
  - **PR 감시** 🟢: 열린 PR 0건.
  - 인바운드: Telegram inbox 신규 텍스트 답신 없음(최신 항목 6/13 이미지) — 정기 루프.
  - 환경 메모: `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 사용(당일 앞선 실행과 동일).
  - Telegram 보고: **발송 완료** (message_id 2330). reply 전용 도구 세션 미노출 →
    진실 공급원 `~/.claude/channels/telegram/{.env}` 토큰 + 허용 chat_id(1241277614)로
    Bot API `sendMessage` 직접 POST. `LC_ALL=en_US.UTF-8` + `--data-urlencode text@file`로
    한글 발송(zsh 로케일 range 오류 회피). 첫 시도는 응답 파싱 단계에서 로케일 오류가 났고
    재시도로 `ok:true` 확인 — 앞 시도가 중복 발송했을 수 있으나 상태 요약이라 무해.
    - 본문: 정기 상태 요약(18:30) — 헬스 정상 · 백업 신선 · 신규버그 0 · PR 0 · 미분류 0 ·
      보류승인 0 · 최근 CWI ingest 2026-07-05 · 최근 분석 2026-W27.
