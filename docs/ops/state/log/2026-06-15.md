# 운영 로그 — 2026-06-15

## 02:00 자율 버그 사이클 (dev-cycle)

### 이슈 선택
- `/opt/homebrew/bin/gh issue list --state open --json number,title,labels` → 열린 이슈 0건.
- `bug`/`small` 라벨 적격 이슈 없음 → **처리할 버그 없음**.
- 종료. worktree·브랜치 생성 안 함.

### 비고
- `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 조회.
- 직전(6/14)과 동일하게 신규 열린 이슈 없음.

### Telegram 보고
- `reply`/telegram send 도구 세션 미노출(ToolSearch `telegram reply send message` → PushNotification·Monitor만 매칭). 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id(`ops/ops.env`, 1241277614)로 Bot API `sendMessage` 직접 호출 → **발송 성공**(ok, message_id 2248).
- 발송 본문.
  ```
  [inkbaduk 02:00 dev-cycle] 처리할 버그 없음
  열린 이슈 0건 (bug/small 적격 없음). worktree·브랜치 생성 안 함. 종료.
  시각: 2026-06-15 02:00
  ```

## 12:00 (12:30 스케줄 오케스트레이터)

### 헬스체크 🟢
- prod backend: `{"status":"ok","db":true,"katago_alive":true}` 200 OK
- prod web: 200 OK
- launchctl: `com.baduk.web` PID 912, `com.baduk.api` PID 923 — 둘 다 가동
- 디스크: 5% (여유)
- staging: 중단됨 (정책상 정상)
- launchd plist drift: exit 0, 출력 없음 — 정상

### 백업 검증 🟢
- 최신 백업: `baduk-20260615T040005.db.gz` (오늘 04:00, 30시간 이내 신선)
- 티어: daily 14 / weekly 4 / monthly 1
- 복원 드릴: integrity_check=ok, 테이블 7개 — 정상

### 버그 스캔 🟢 — 신규 0건
- `baduk-api.err`에서 `TypeError: Object of type bytes is not JSON serializable` 2건(라인 221457·221549).
- **이슈 미생성.** 두 건 모두 현재 가동 프로세스(PID 923, 라인 221583, Jun 13 22:49 기동) *이전* 발생. 현재 `app/errors.py`는 이미 수정됨 — `_safe_validation_errors`가 `custom_encoder={bytes: ...}`로 bytes 치환 디코딩(라인 12-21). 스택트레이스의 "line 21 val_handler"는 수정 전 레이아웃. 이미 배포·해소된 버그, 현재 프로세스 신규 0건. 누적 카운트가 아닌 최근성·코드 확인으로 오탐 회피 ([[bug-scan-verify-recency]]).

### 백로그 트리아지 🟢 — 할 일 없음
- `no:label` 열린 이슈 0건.

### PR 감시 🟢 — 주의 PR 없음
- 열린 PR 0건.

### 상태 요약
- 보류 승인: 0건
- 최근 CWI ingest: 2026-06-14 03:04 (fetched=838 new=200 duplicate=638 error=0)
- 최근 주간 리포트: 2026-W24

### Telegram 보고
- telegram `reply`/send 도구 세션 미노출(ToolSearch `telegram reply send` → PushNotification·Monitor·Canva만 매칭). 02:00 사이클과 동일.
- 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`(`ops/ops.env`)로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=True, message_id=2249).

## 18:00 (18:30 스케줄 오케스트레이터)

### 헬스체크 🟢
- prod backend: `{"status":"ok","db":true,"katago_alive":true}` 200 OK
- prod web: 200 OK
- launchctl: `com.baduk.web` PID 912, `com.baduk.api` PID 923 — 둘 다 가동
- 디스크: 5% (여유)
- staging: 중단됨 (정책상 정상)
- launchd plist drift: exit 0, 출력 없음 — 정상

### 백업 검증 🟢
- 최신 백업: `baduk-20260615T040005.db.gz` (오늘 04:00, 30시간 이내 신선)
- 티어: daily 14 / weekly 4 / monthly 1
- 복원 드릴: integrity_check=ok, 테이블 7개 — 정상

### 버그 스캔 🟢 — 신규 0건
- `baduk-api.err` mtime Jun 13 22:49 — 12:00 이후 신규 기록 없음. 동일한 `TypeError: bytes` 2건은 이미 수정·해소(현 `errors.py`의 `_safe_validation_errors` custom_encoder, 스택트레이스 line 21은 수정 전 레이아웃). 누적 카운트 아닌 최근성·코드 확인 ([[bug-scan-verify-recency]]).

### 백로그 트리아지 🟢 — 할 일 없음
- `no:label` 열린 이슈 0건.

### PR 감시 🟢 — 주의 PR 없음
- 열린 PR 0건.

### 상태 요약
- 보류 승인: 0건
- 최근 CWI ingest: 2026-06-14 03:04 (fetched=838 new=200 duplicate=638 error=0)
- 최근 주간 리포트: 2026-W24

### Telegram 보고
- telegram `reply`/send 도구 세션 미노출(ToolSearch `telegram reply send message channel` → PushNotification·Monitor·Canva만 매칭). 02:00·12:00 사이클과 동일.
- 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`(`ops/ops.env`)로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=True, message_id=2250).
