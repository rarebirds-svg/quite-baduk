# 2026-09-15

- 04:30 dev-cycle — **처리할 버그 없음(8일 연속).** 열린 이슈 0건(`gh issue list --state open` 빈 배열), 열린 PR 0건. `.worktrees/dev-cycle`은 `main`(2e30d2e) detach 상태에 미커밋 변경 0, 배포 갭 0(`HEAD..origin/main` 0건). 코드 변경 없음. 잡 상태 `ok` 기록.
- **9/14 전 잡 결손 발견** — 9/14 04:30 dev-cycle·09:00 오케스트레이터·21:00 오케스트레이터 세 실행 모두 시작 4~6초 만에 `You've hit your monthly spend limit`으로 비정상 종료(`dev-cycle-runs.log`·`orchestrator-runs.log`). 세션 한도(`resets Xam`)가 아니라 **월간 지출 한도**라 `session-retry.sh`가 재시도하지 않았고, 9/14 일일 로그·am/pm 다이제스트·헬스체크·백업 검증 기록이 전부 없다. 오늘 04:30 세션은 정상 기동했으므로 한도는 해제된 상태(주간 리셋 01:00 또는 사람이 한도 상향). 9/14 하루치 헬스·백업·버그 스캔 공백은 오늘 09:00 오케스트레이터가 24h 대신 48h 창으로 보강 점검해야 한다. 일일 로그 커밋은 관례대로 09:00 오케스트레이터 사이클에 맡긴다.
- 09:00 오케스트레이터 사이클(am) — **전 항목 정상. 9/14 결손을 48h 창(9/13 21:00 → 9/15 09:00)으로 보강 점검 — prod 무사고·백업 9/14·9/15 2일치 정상·신규 버그 0. 어제 대국 6건 437수. 9/14 3잡 결손은 OPS-20260914-01로 정리.**
- **헬스체크 전 항목 통과** — `/api/health` 200 `{"status":"ok","db":true,"migrations":{"current":"0020","head":"0020","pending":false}}`(`katago_alive:false`는 정보용 — 마지막 착수 9/14 19:13 KST, 14시간 idle), web `/` 200, 공개 `https://inkbaduk.com/` 200, `/api/spectate` 200, `com.baduk.api` PID 5743(9/12 13:46 기동 유지, **67시간 무사고**)·`com.baduk.web` PID 89541(9/8 23:56 기동 유지), staging 중단(정책상 정상), 디스크 8%, plist drift 없음(`sync-launchd.sh --check` exit 0), Claude 인증 `ok: D-17 (만료 10-02 23:41)`.
- **백업 검증 정상 — 9/14 결손일에도 백업 잡은 정상 실행(Claude 비의존).** 최신 `baduk-20260915T040001.db.gz` 4,522,654B(9/14 `…T040004` 4,511,812B, 9/13 4,507,706B — 하루 +4~11KB 대국 증가분), `find -mtime +1` 출력 없음. `backup.out.log` 9/14 04:00:06·9/15 04:00:03 `backup 완료`(보존 정리 8/31·9/1분). 티어 **14 / 8 / 4**. 복원 드릴 `integrity_check` ok · 테이블 11 · `pro_games` 4311(변동 없음, 다음 ingest 9/20 03:00).
- **버그 스캔(48h) — 신규 버그 없음.** `.err` 235,004 → 235,022줄(**+18**, mtime 9/14 22:28) 전부 WS accepted/open/closed(#474~#479), **트레이스백 0·IntegrityError 0·locked 0**(마지막 트레이스백 234,763줄 = 9/7 OPS-20260907-01 이전 옛 줄). `.log` 320,641 → 321,913줄(+1,272) 분포 200 1,101·404 137·204 14·401 12·201 8, **5xx 0·429 0**. 404 137은 전부 스캐너 프로브 차단(`35.204.96.119`·`34.181.249.104` GCP 대역 — `/api/graphql`·`/api/webhook`·`/api/url`·`/api/request`·`/api/redirect`·`/api/proxy`·`/api/fetch`·`/api/.env`·`POST /api`·`/api/v1/validate/code`·`/api/run` 각 4~6회), 401 12는 `GET /api/session` 첫 방문 정상. IP 분포 `127.0.0.1` 510(SSR→API)·`2a06:98c0:3600::103` 432(Cloudflare Worker 모니터 `/api/health`, 5분 간격 = 36h분 정확)·`166.104.68.53/.59` 120·`61.73.43.216` 43(대국 사용자). **`baduk-web.err` 3,484 → 3,495줄(+11, mtime 9/14 13:59)** — `Failed to find Server Action` 1건(id `67f3aca6`), 현 빌드 `web/.next/server`에 0건 + 프로세스 기동 9/8 23:56 = `BUILD_ID` mtime → stale 클라이언트 재생(`[[web-err-stale-server-action]]`), 이슈 대상 아님, 기준선 3,495로 갱신. 열린 `bug` 이슈 0건이라 중복 확인 불요.
- **대국 활동(DB 실측, `backend/data/baduk.db`)** — **48h 대국 6건·437수**(`games max(id)` 473 → 479, `moves` 9/14 UTC 349수 = 최근 2주 일일 최다). #474 19路 88수 resigned(9/13 23:16~9/14 00:15 KST), #475 19路 18수 active(9/14 15:46~15:49, 이탈), #476 19路 98수 resigned(15:50~16:12), #477 13路 46수 resigned(16:13~16:30), #478 13路 117수 resigned(17:34~18:01), #479 13路 70수 resigned(18:57~19:13). `166.104.68.x`·`61.73.43.216` 두 사용자, 재기동 없이 연속 5판 정상(#83·#86 코드). 마지막 수 9/14 19:13 KST, 현재 진행 중 0건. `visit_hits` 564 → 575(+11/36h; 9/14 7·9/15 3, 마지막 방문 07:59 KST).
- **백로그 트리아지** — 열린 이슈 0건, 미분류 0건. 할 일 없음.
- **PR 감시** — 열린 PR 0건. `main` CI 최근 4런 전부 success(최근 9/13 12:05 UTC = pm 사이클 커밋 `e51a78c`). 로컬 `fix/*` 5개는 9/13 확인대로 squash 머지 완료분(`[gone]`·미push 작업 없음).
- **잡 4/4 ok(상태파일 기준)** — dev-cycle 9/15 04:30(버그 없음 8일째)·news-hook 9/13 05:04·analytics-weekly 9/13 09:01·content-draft 9/12 02:02. **단, 9/14 dev-cycle 04:30·오케스트레이터 09:00·21:00 세 실행이 `monthly spend limit`으로 시작 4~6초 만에 종료 — 다이제스트 행은 `warn`으로 보고.** 다이제스트 마커 `inkbaduk-am/pm-2026-09-14` 부재 확인(popory 9/14 am·pm 마커는 존재).
- **OPS-20260914-01 등록** — watchdog가 자동 기록한 WD 34건(dev-cycle stale 17건 9/14 11:02~9/15 04:03, orchestrator stale 17건 9/14 16:02~9/15 08:03)을 단일 원인(월간 지출 한도)으로 묶어 `incidents.md`에 정리. 오늘 04:30 dev-cycle·09:00 본 세션 정상 기동으로 해소. 재발 방지 후보(사람 판단) — (1) 한도 상향은 유료 결제라 🔴 사람 전용, (2) `session-retry.sh`에 `monthly spend limit` 패턴을 넣어도 리셋이 주간 01:00이라 재시도는 무의미 → 대신 감지 즉시 `kind: alert` 1회 발송이 적절, (3) watchdog `check-staleness.sh`는 18h/30h 임계라 첫 경보가 결손 후 6.5시간(dev-cycle)·7시간(orchestrator) 뒤 — 러닝로그의 `비정상 종료` 직후 감지는 여전히 사각지대(대시보드 Watchdog 7/29 항목).
- **배포 갭 0** — HEAD = origin/main `e51a78c`. 9/13 21:00 이후 `main` 유입 0건. api는 `c0d8d05` 코드(9/12 13:46 기동)·web은 9/8 빌드 그대로, 코드 미반영 없음.
- **승인 대기 0건.** 대시보드 갱신 — prod 행의 `(이전)` 체인이 40KB까지 자라 최근 3회분만 남기고 정리(45KB → 축소). 다이제스트 `slot: am` 발송 결과는 아래 줄에 기록.
- **am 다이제스트 09:0x — `send.py` exit 0 발송 완료, 성공 마커 `inkbaduk-am-2026-09-15` 생성.** 전체 ⚠️(`jobs: warn` — 9/14 3잡 월간한도 결손). 교훈 — `service` 텍스트가 40자 초과로 `Claude인…`으로 잘려 D-17이 누락됐다. 다음 사이클부터 `5xx 0/1.3k(48h)` 같은 괄호 보조 표기는 빼고 40자 안에 맞춘다. 대시보드 갱신 완료.
- 21:00 오케스트레이터 사이클(pm) — **헬스·백업·PR·이슈 정상, 단 신규 버그 1건 발굴(#97). 12:56 치석 대국 생성이 `POST /api/games` 500으로 실패(사용자 9초 뒤 재시도 성공). 원인을 로그·코드로 확정해 bug 이슈로 등록. 오늘 대국 1건 73수.**
- **헬스체크 전 항목 통과** — `/api/health` 200 `{"status":"ok","db":true,"migrations":{"current":"0020","head":"0020","pending":false}}`(`katago_alive:false`는 정보용 — 마지막 착수 13:49 KST 후 idle), web `/` 200, 공개 `https://inkbaduk.com/` 200, `/api/spectate` 200, `com.baduk.api` PID 5743(9/12 13:46 기동 유지, **79시간 무사고**)·`com.baduk.web` PID 89541(9/8 23:56 기동 유지), staging 중단(정책상 정상), 디스크 8%, plist drift 없음(`sync-launchd.sh --check` exit 0), Claude 인증 `ok: D-17 (만료 10-02 23:41)`. watchdog 09:03 이후 신규 incident 0(WD 마지막 `WD-20260915-090345`, 이후 매시 "신규 incident 0 건").
- **백업 검증 정상** — 최신 `baduk-20260915T040001.db.gz` 4,522,654B(09:00과 동일 파일, 다음 생성 9/16 04:00), `find -mtime +1` 출력 없음, 티어 **14 / 8 / 4**, 복원 드릴 `integrity_check` ok · 테이블 11.
- **버그 스캔 — 신규 버그 1건 → [#97](https://github.com/rarebirds-svg/quite-baduk/issues/97) 등록.** `.err` 235,022 → 235,098줄(**+76**, mtime 13:50) = 트레이스백 **1건**(72줄) + WS accepted/open/closed(#481). `.log` 321,913 → 323,313줄(+1,400) 분포 200 1,378·404 9·401 6·204 5·**500 1**·201 1, 429 0. 404 9는 `34.178.103.229`(GCP) `/api/{,v1,v2,v3,dev,staging}/.env` 프로브 6 + `169.40.142.224` `/api/session/properties` 2 + `185.218.86.27` `/api` 1 — 전부 차단 정상. IP 분포 `127.0.0.1` 1,202(SSR)·Cloudflare 모니터 144(5분 간격 12h분 정확)·`211.185.25.84` 17(데일리 퍼즐 5문제 풀이)·`166.104.68.59` 16(대국). `baduk-web.err` +0(기준선 3,495 유지).
  - **#97 트레이스백** — `POST /api/games` 500, `games.py:71 create` → `game_service.py:168 create_game` → `adapter.py:287 play` → `ValueError: KataGo rejected play B D4: illegal move`. 대국 #480(13路·치석 6·9d 이세돌·`hoon`) 생성 12:56:20 KST. 사용자는 12:56:29 #481로 재시도해 성공(73수, B+31.5, 13:49 종료).
  - **원인 확정(코드 미수정, 이슈만)** — ① #480은 풀 슬롯 0 배정, 슬롯 0 KataGo가 12:56:21 새로 spawn(`baduk-katago-0.err` 635,414줄) → `_ensure_alive` → `_replay_state`가 슬롯 0 직전 대국 #479(13路, 9/14 19:13 종료)의 70수를 복원. #479 SGF `W[dj]` = **D4**. ② `create_game`의 `set_boardsize(13)`은 같은 크기라 KataGo가 보드를 비우지 않음 — 슬롯 0 로그에 13×13 초기화가 12:56:22 **1회뿐**, 두 번째 `boardsize 13`에 무반응. ③ 치석 첫 점 `play B D4` → 백돌 잔존 → illegal. **같은 함정이 `_reseed_adapter`에는 주석과 함께 `clear_board()` 선행으로 이미 처리돼 있고(과거 `.err` 26,444~43,254줄 130여 건이 그 경로), `create_game` 치석 배치 경로만 누락.** #481이 성공한 것은 슬롯 1 배정 + 슬롯 1 직전 대국이 19路라 `boardsize 13`이 크기 변경으로 보드를 새로 만든 덕분(슬롯 1 로그 12:56:30 19×19 → 12:56:31 13×13). 치석 0 대국은 `create_game`이 `play`를 안 보내므로 무관 — **치석 대국이 같은 크기 대국 직후 같은 슬롯이면 결정론적 재현.** 수정은 `clear_board()` 1줄 + 회귀 테스트, 내일 04:30 dev-cycle 대상.
  - 러닝북 "2회 이상 반복" 기준에는 1회지만, 원인이 코드로 결정론적으로 확정되고 과거 동일 메시지 130여 건과 같은 계열이라 이슈로 올렸다. 부수 — #480 행이 `active`·0수 고아로 잔존(`db.commit()`이 어댑터 호출보다 앞), 이슈 본문에 명시.
  - **관찰(이슈 미등록)** — 슬롯별 KataGo stderr에 `GTP ready`가 슬롯 0 누적 8,751회, 오늘 #481 진행 중 슬롯 1이 수마다 재spawn(13:48:29·:42·:49·13:49:23·:29), 20:30~20:35 슬롯 0도 데일리 퍼즐 응답 5건에 7회 재spawn. 프로세스는 사용 직후 소멸(현재 katago 프로세스 0개, `katago_alive:false`). 크래시 흔적 없이 조용히 끝나며 어댑터의 `_ensure_alive`가 매번 되살린다. 수마다 모델 로드 1~2초가 더해지는 지연 의심 — 종료 주체 미확인, 다음 사이클 또는 dev-cycle 참고용.
- **대국 활동(DB 실측)** — **오늘 대국 1건·73수**(`games max(id)` 479 → 481). #480 고아(0수), #481 13路 치석 6 73수 finished B+31.5(12:56~13:49 KST). 마지막 수 13:49:32 KST, 진행 중 0. `visit_hits` 575 → 579(+4/12h, 마지막 20:28 KR 모바일 direct).
- **백로그 트리아지** — 열린 이슈 #97 1건(본 사이클 생성, `bug` 라벨), 미분류 0건.
- **PR 감시** — 열린 PR 0건. `main` CI 최근 4런 success(최근 9/15 00:06 UTC = am 커밋 `d4621ed`).
- **잡 4/4 ok** — dev-cycle 9/15 04:30·news-hook 9/13 05:04·analytics-weekly 9/13 09:01·content-draft 9/12 02:02. 9/14 결손은 am에서 `warn`으로 보고 완료, 이후 정상 실행 확인돼 이번 행은 `ok`.
- **배포 갭 0** — HEAD = origin/main `d4621ed`. api `c0d8d05` 코드·web 9/8 빌드 그대로.
- **승인 대기 0건.** 다이제스트 `slot: pm` — `anomaly: warn`(트레이스백 1·신규 버그 1) → 전체 ⚠️. 발송 결과는 아래 줄에 기록.
