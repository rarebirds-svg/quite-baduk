# 2026-07-29

- 02:00 content-draft: **처리할 토픽 없음** — 초안·게시 0건으로 종료(커밋·push 없음).
  - **집합 실측.** `web/content/glossary/` **27개**, `web/content/faq/` **12개**, `docs/ops/content/drafts/` **비어 있음**.
  - **glossary 미작성 후보 0건.** 시드 18개 중 파일명이 그대로 없는 것은 `kasaeng` 하나뿐인데, 이건 갭이 아니라 **슬러그 통합 결과**다(`[[glossary-sahwal-slug]]`). `web/content/glossary/sahwal.md` frontmatter가 `title: 사활`로 같은 토픽을 이미 게시했고, `web/next.config.js:17`이 `/glossary/kasaeng → /glossary/sahwal`을 `permanent: true`(빌드 매니페스트상 308)로 넘긴다. 게시 비율 **18/18**.
  - **파일명만 대조했다면 `kasaeng.md`를 새로 썼을 것이고, 그 결과는 중복 콘텐츠 + 리다이렉트에 가려 영영 도달 불가능한 페이지였다.** 시드 갭은 slug가 아니라 frontmatter `title`로 확인해야 한다는 규칙이 이번에 실제로 오작동을 막았다.
  - **faq 미작성 후보 0건.** 시드 9개(`ai-baduk-vs-human`·`review-and-analysis`·`nickname-session`·`korean-vs-japanese-rules`·`how-to-improve-rank`·`board-sizes`·`ai-strength-levels`·`spectate-pro`·`resign-and-pass`) 전부 `web/content/faq/`에 존재. 게시 비율 **9/9**. 디렉터리의 나머지 3개(`handicap-vs-ai`·`play-against-katago`·`shin-jinseo-katago`)는 시드 밖 항목이라 비율 계산에서 제외했다.
  - 두 종류 모두 후보 0 → 프롬프트 절차대로 **즉시 종료**. 기존 항목의 임의 개정·확장은 하지 않았다(비율 균형 단계는 후보가 양쪽 다 없어 미해당).
  - QA·게시·IndexNow·Telegram 단계 전부 미실행 — 게시물이 없으므로 사후 보고할 것도 없다.
  - **후속은 사람 판단.** 사이클이 계속 net-zero가 되지 않으려면 `docs/ops/content/seed-glossary.yml`·`seed-faq.yml`에 신규 토픽 시드를 추가해야 한다. 시드 추가는 콘텐츠 방향 결정이라 에이전트가 임의로 하지 않았다.

- 02:00 dev-cycle: **신규 처리 0건** — 브랜치·커밋·PR 없음. 열린 이슈가 #65 하나뿐이고 이미 PR #66으로 처리돼 있어 중복을 만들지 않았다.
  - **적격 이슈 집합.** `gh issue list --state open` 결과 **전체 1건** — #65(`bug`·`prio:high`). `feature`·`in-progress` 라벨 없음이라 라벨상 적격이지만, 7/24 사이클이 이미 `fix/issue-65`/`386567d`/PR #66으로 처리했다. 중복 브랜치·PR 방지를 위해 `in-progress`도 달지 않았다(7/27 사이클과 동일 판단).
  - **#65 재발 검증 — 이번엔 부하 있는 구간에서 0건.** 7/27 사이클은 "저부하라 판정 유보"였는데 이번엔 알리바이가 약해졌다.
    - 현 prod api PID **512**, 기동 `Wed Jul 29 00:01:09`. 7/26 이후 `40446 → 78066 → 94302 → 512` 재기동 누적.
    - `.err` 225,398줄. `[SQL: UPDATE pro_games ...]` 실패는 line 225173(PID 5597 마커) 이후 **0건**. 마지막 발생 line 225145 = 7/24로 재기동 3회 이전.
    - 부하는 `.err`가 아니라 `.log`에서 셌다(`[[api-log-stdout-vs-stderr]]`) — 최근 3,000줄에 `/api/spectate/pro/` **123건 · 5xx 0건**. 7/27이 본 20건 대비 6배 밀도.
    - 위치가 아니라 프로세스 스코프 카운트 비교라 #64형 오탐 모드가 아니다(`[[bug-scan-verify-recency]]`).
  - **기동 이후 남은 락 1건은 #65가 아니라 #69 경로였다.** line 225173 이후 유일한 `[SQL:` 구문은 225348 `UPDATE games SET loss_streak=? WHERE games.id = ?` (파라미터 `(3, 332)`, WebSocket). 조회수가 아닌 **대국 경로**이고, 기동 마커 225360(78066)·225381(94302)·225395(512) *이전* = PID 40446 시절이다. `38fd9e3`(#69) 라이브는 7/26 20:24 PID 94302부터이므로 **#69 배포 이전 발생**이고, 배포 이후 대국 경로 락은 0건이다. 두 완화 모두 라이브 확인 — `1bb0457`·`38fd9e3` HEAD 조상, `backend/app/db.py:33` → `PRAGMA busy_timeout=30000`.
  - **무재발을 "#66 불필요"로 읽지 않았다.** `busy_timeout` 30s는 *에러를 지연으로 바꾸는* 완화다. api `--workers 1` + 블로킹 락 대기라 500이 사라진 자리에 최대 30초 응답 지연이 들어갔을 수 있고, **지연은 에러 로그에 흔적을 남기지 않는다.** #66의 원자적 `view_count = view_count + 1`은 락 보유 시간 자체를 줄여 그 지연 비용도 함께 줄이므로 보완 관계다.
  - **PR #66 신선도 실측.** `origin/main` 대비 6커밋 behind · 1커밋 ahead, `MERGEABLE`/`CLEAN` 충돌 없음. 변경 파일이 `backend/app/api/spectate_pro.py`+테스트 뿐이라 그 사이 머지된 #67·#68·#69와 경로가 겹치지 않는다. CI 4잡 SUCCESS는 7/24 base 기준이라 stale이지만 **리베이스는 force push(🚫)** 라 갱신하지 않았다.
  - **에스컬레이션.** `AP-20260725-01` 머지 대기 **5일**. 긴급도는 내려갔으나(재발 0 + busy_timeout 상향) 방치 대상은 아니다 — 다음 크롤 버스트 구간이 진짜 판정 시점이다. pending-approvals에 7/29 갱신 추가.
  - **큐에 방치된 별건 관찰.** `AP-20260723-01`(settings.json deny `Write(...)`→`Edit(...)`, `[[settings-deny-write-vs-edit]]`)이 7/23부터 **6일째 대기**다. 이 항목은 실행 절차 3단계가 `chore/settings-deny-edit-rules` 브랜치 push + PR 생성인데 그 자체는 🟢인데도 아직 브랜치가 없다 — 지금은 사람이 머지할 대상조차 없는 상태다. 다만 이번 사이클의 스코프는 GitHub 이슈 처리라 임의로 착수하지 않았다. **다음 오케스트레이터 실행에서 PR 생성까지 선행할지 판단 필요.**
  - 결과 Telegram 1건 발송.

- 12:00 orchestrator: 러닝북 5종 수행. **prod 정상, 신규 이슈 1건·신규 PR 1건.**
  - **헬스체크 OK.** backend `{"status":"ok","db":true,"katago_alive":true}` · web 200 · launchctl `com.baduk.web` PID **497** / `com.baduk.api` PID **512**(기동 `Wed Jul 29 00:01:09`, uptime 12h). 디스크 **6%**, `sync-launchd.sh --check` exit 0(drift 없음). staging 중단 = 정책상 정상.
  - **백업 OK.** 최신 `baduk-20260729T040006.db.gz`(04:00, 3.02MB), 신선도 정상. daily/weekly/monthly **14 / 8 / 2**. 복원 드릴 `integrity_check=ok`, **9 tables**.
  - **버그 스캔 — 신규 런타임 에러 0건, 대신 운영 결함 1건 발견.** `.err` 225,398줄, 현 기동 마커(225395/PID 512) 이후 트레이스백 **0건**. 단, 같은 구간 트래픽이 브라우징 위주라(최근 300줄 = health 198 · 프로 상세 46 · 5xx **0건**) **대국 경로는 실행되지 않았다** — #69 배포 이후 `loss_streak` 락 무재발은 아직 무부하 알리바이 상태다.
  - **⚠️ 7/28 하루가 통째로 비어 있었다 — 인증 만료로 자동 실행 3회 유실.** `Failed to authenticate: OAuth session expired and could not be refreshed` 직후 `비정상 종료`가 dev-cycle 02:00 · orchestrator 12:00 · orchestrator 18:00 세 번. 7/29 02:00 dev-cycle부터 자동 회복(자격증명 갱신됨).
    - **watchdog는 이 구간 내내 `신규 incident 0 건`이었다.** `ops/check-staleness.sh`의 `extract_last_ts()`가 로그의 마지막 `[YYYY-MM-DD HH:MM:SS]` 패턴만 뽑는데, 실패 종료도 timestamp를 새로 찍으므로 잡이 "방금 실행됨"으로 보인다. **trigger 유실은 잡지만 trigger 후 즉시 실패하는 모드는 구조적으로 은폐된다** — 크래시 루프면 영구히 무음이다.
    - 같은 구간 **prod 자체는 무사고**였다(사후 확인: 200 OK · 백업 7/28·7/29 정상 생성 · 신규 트레이스백 0건). 유실된 것은 서비스가 아니라 감시다.
    - `state/incidents.md`에 **OPS-20260728-01** 기록, 이슈 **[#70](https://github.com/rarebirds-svg/quite-baduk/issues/70)** 생성(`bug`·`small`) — 성공 종료 마커 기준 staleness 판정으로 수정 제안.
  - **백로그 트리아지 — 미분류 0건.** #70 생성 시 `bug`+`small`을 직접 부여해 잔여 없음.
  - **PR 감시 — 2건, 주의 PR 0건.** #66(5일, `MERGEABLE`/`CLEAN`, CI 4잡 SUCCESS) · #71(신규, CI 4잡 전부 SUCCESS · `MERGEABLE`/`CLEAN`). 7일 정체 기준 미달, 충돌·실패 없음.
  - **AP-20260723-01의 🟢 선행 단계를 실행했다 — PR [#71](https://github.com/rarebirds-svg/quite-baduk/pull/71) 생성.** 02:00 dev-cycle이 "실행 절차 3단계(브랜치 push + PR 생성)가 🟢인데 6일째 브랜치조차 없어 사람이 머지할 대상이 없다"고 남긴 판단 요청에 대한 응답이다.
    - `origin/main` 기반 워크트리(`/tmp/wt-settings-deny`)에서 `.claude/settings.json` deny 2건을 `Write(...)` → `Edit(...)`로 교체, JSON 유효성 확인 후 `chore/settings-deny-edit-rules` push. 워크트리는 제거했고 **prod 작업 트리는 건드리지 않았다**(브랜치 체크아웃 없음).
    - 머지는 여전히 🟡 — AP-20260723-01은 대기 상태 유지이되, 이제 "제안"이 아니라 **머지 버튼 하나로 끝나는 상태**가 됐다.
  - Telegram 요약 1건 발송(message_id **2477**). 이번 세션에도 `reply` 도구가 노출되지 않아 Bot API 직접 호출(`~/.claude/channels/telegram/.env` + `ops.env` chat_id).

- 18:00 orchestrator: 러닝북 5종 수행. **prod 정상, 신규 이슈 0건·신규 PR 0건. 조치 필요 없음.**
  - **헬스체크 OK.** backend `{"status":"ok","db":true,"katago_alive":true}` · web 200 · launchctl `com.baduk.web` PID **497** / `com.baduk.api` PID **512** — 12:00과 **동일 PID = 재기동 없음**, uptime **18시간 01분**(`Wed Jul 29 00:01:09` 기동). 디스크 **6%**, `sync-launchd.sh --check` exit 0(drift 없음). staging 중단 = 정책상 정상.
  - **백업 OK.** 최신 `baduk-20260729T040006.db.gz`(04:00, 3.02MB) 그대로, `-mtime +1` 미해당. daily/weekly/monthly **14 / 8 / 2**. 복원 드릴 재실행 — `integrity_check=ok`, **9 tables**. 다음 백업 7/30 04:00.
  - **버그 스캔 — 신규 0건. `.err`가 6시간째 바이트 단위로 불변이다.** 줄 수 **225,398**로 12:00과 완전 동일, mtime `Jul 29 00:01`(= 현 프로세스 기동 시각) 이후 무기록. 현 기동 마커(225395/PID 512) 이후 트레이스백 **0건**.
    - `.err` 전체의 `database is locked` 누적은 2,556건이지만 **전부 구 프로세스 구간**이다. 누적 카운트로 판정하지 않는다(`[[bug-scan-verify-recency]]`).
    - 마지막 락 트레이스백은 `UPDATE games SET loss_streak=? WHERE games.id = ?`(파라미터 `(3, 332)`)로 **#69 경로**이며, 02:00 dev-cycle이 이미 PID 40446 시절 = `38fd9e3` 배포 *이전* 발생으로 규명했다. 신규 사건 아님.
    - 후보 0건이라 이슈 생성 없음. 열린 `bug` 이슈는 #65·#70 그대로.
  - **⚠️ 그러나 이번 6시간도 대국 경로 0건이다 — 무부하 알리바이가 3사이클 연속 누적됐다.** `.log` 205,x → **206,502줄**, 최근 500줄 구성이 health **349** · `/api/spectate/pro/{id}` **87** · 어드민·세션 소수이고 **5xx 0건**. 같은 3,000줄 창에서 `WebSocket /api/ws/games` **0건**, `POST /api/games/{id}/moves` **0건**.
    - `visit_hits` **54행**으로 12:00과 동일(**+0**) = 6시간 동안 사람 방문 비콘이 한 건도 안 찍혔다. 관측 트래픽의 대부분은 health 폴링과 크롤러다.
    - 따라서 **#69(`loss_streak` 락) 배포 효과도, #65(조회수 락) 무재발도 여전히 "실행되지 않아서 0건"**이다. 프로 상세 87건/6시간은 12:00(46건/6시간)보다 밀도가 올랐으나 #65를 재현시킨 크롤러 버스트(17.7h/16건)와는 성격이 다르다. 판정은 다음 실사용 대국 구간에서.
  - **백로그 트리아지 — 미분류 0건.** `gh issue list --search "no:label"` 빈 배열. 할 일 없음.
  - **PR 감시 — 2건, 주의 PR 0건.** #66(`fix(spectate)`, 생성 후 **약 112시간(4.7일)**, `MERGEABLE`, CI 4잡 SUCCESS) · #71(`chore(settings)`, 생성 후 **약 6시간**, `MERGEABLE`, CI 4잡 SUCCESS). 7일 정체 기준 미달, 충돌·실패 없음 — 둘 다 남은 것은 사람 머지(🟡)뿐이다.
  - **AP-20260723-01의 실효 없음이 이번 세션에서도 실증됐다.** 세션 시작 시 하네스가 `Write(...backend/data/*.db)`·`Write(...katago/models/*)` deny 규칙 2건에 대해 **"Edit(path) 규칙만 파일 편집 도구를 포괄한다"** 경고를 또 출력했다. PR #71이 이 수정을 담고 있으나 미머지라 라이브 설정은 그대로 = **7일째 prod DB·모델이 에이전트 쓰기로부터 미보호**.
  - **content-ingest는 stale이 아니다.** 최근 실행 7/26 03:11(`fetched=2060 new=200 duplicate=1860 error=0`)로 3일 전이지만, `com.inkbaduk.content-ingest.plist`가 `Weekday 0`(일요일) 03:00 **주 1회** 잡이다. 다음 실행 8/2. `prune-visits` 04:15 정상(`deleted=0 retention_days=180`).
  - **watchdog 신규 incident 0건.** 다만 이 0건의 신뢰도는 #70이 고쳐지기 전까지 제한적이다(실패 종료를 성공으로 오독하는 사각지대 — OPS-20260728-01).
  - Telegram 요약 1건 발송(message_id **2478**). 이번 세션에도 `reply` 도구가 노출되지 않아(`ToolSearch` 실측 — 검색 결과는 `SendMessage`·Canva 계열뿐) Bot API 직접 호출로 대체했다.
    - **호출 형식 함정 기록 — 두 전송 방식의 `@` 문법이 다르다.** 첫 시도가 `curl: option -F: is badly used here`로 실패했는데, 원인은 `[[dev-cycle-telegram-botapi]]`가 적어둔 `text@파일` 표기를 **`-F`(multipart)** 에 붙인 것이다. 그 표기는 `--data-urlencode` 전용이고(`--data-urlencode "text@파일"` = 파일 내용을 필드 값으로 URL 인코딩), `-F`에서 `@`는 **파일 업로드** 의미라 `=` 없이는 파싱 자체가 깨진다. `-F`로 파일 내용을 값으로 넣으려면 **`-F "text=</파일경로"`**(`<` 리다이렉트 문법)를 써야 하고, 이번 발송은 이 형식으로 성공했다. `LC_ALL=en_US.UTF-8` 필요 조건은 양쪽 공통.
