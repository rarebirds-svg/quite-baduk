# 2026-07-13 운영 로그

## dev-cycle (02:00)

**처리할 버그 없음** — 적격 이슈 0건으로 no-op 종료. 코드 변경·커밋·브랜치 없음.

- 이슈 조회: `gh issue list --state open` **0건** (열린 이슈 자체가 없음). 따라서 `bug`·`small` 라벨 후보도 0건 → 러닝북의 "적격 이슈가 없으면 종료" 경로.
- 최근 닫힌 버그 이슈는 #53(비-JSON 본문 500), #39~#42(SQLite lock / WS write) — 모두 종료 상태. 열린 PR도 **0건**.
- 부수 확인 — 로컬/원격에 남은 브랜치 정리 상태 ([[dev-cycle-unpushed-branches]] 검증 차원):
  - `fix/non-json-body-500`, `feat/last-move-dot-marker` → **origin/main에 이미 병합됨** (원격 브랜치만 미삭제, 무해).
  - `feat/last-seen-at-debounce` → **main에 미병합, 열린 PR 없음, main 대비 3커밋 앞섬**. feature 경로라 dev-cycle 자율 범위 밖 → 사람/오케스트레이터 판단 필요 (PR 생성 또는 폐기). 워크트리 `.claude/worktrees/last-seen-debounce`에 체크아웃돼 있음.
- 도구 메모: 이번 세션에도 `gh`가 PATH에 없어 첫 호출 exit 127 → `/opt/homebrew/bin/gh` 절대경로로 우회. 어제(7/12) 오케스트레이터와 동일 증상 — launchd 세션 공통 문제로 보임.
- Telegram: 결과 1건 발송.

## 오케스트레이터 (12:00)

전 러닝북 이상 없음. 경보 없음.

- **헬스체크** — prod backend `{"status":"ok","db":true}` 200, prod web 200, launchctl `com.baduk.api`(PID 1516)·`com.baduk.web`(PID 1504) 가동. `katago_alive:false`는 lazy spawn 정상(정보용, 경보 아님). staging 중단(정책상 정상). 디스크 6%. `sync-launchd.sh --check` exit 0 — plist drift 없음.
- **백업 검증** — 최신 `baduk-20260713T040005.db.gz` (7/13 04:00, 8시간 전 → 신선). `-mtime +1` 해당 없음. 티어 daily 14 / weekly 8 / monthly 2. 복원 드릴 `integrity_check=ok`, 테이블 7개 → 정상.
- **버그 스캔** — **신규 이슈 0건**. `baduk-api.err`에서 `TypeError: Object of type bytes is not JSON serializable`(errors.py val_handler) 2회 발견, 파일 끝부분(221457·221549 / 총 221724행)이라 최근처럼 보였으나 **오탐으로 판정**:
  - 트레이스백의 `errors.py:21`은 구버전 줄번호. 현 HEAD의 `errors.py`에는 이미 `_safe_validation_errors()`(bytes → 치환 디코딩) 수정이 들어 있음 — `ce9d745` (2026-06-11).
  - 라이브 재현 시도: 로그에 찍힌 실패 입력 그대로(`Content-Type: text/plain` + `{"nickname":"deploy-check-77"}`) prod에 POST → **HTTP 422**, bytes가 문자열로 정상 디코딩된 응답. 500 재현 안 됨.
  - 결론: 해당 로그는 마지막 재기동 *이전* 구버전 코드가 남긴 기록. 이미 종료된 #53과 동일 사안 — 이슈 생성하지 않음. ([[bug-scan-verify-recency]] 규칙대로 누적 카운트가 아니라 재현 여부로 판정.)
- **백로그 트리아지** — 미분류 열린 이슈 **0건**. 열린 이슈 자체가 0건 → 할 일 없음.
- **PR 감시** — 열린 PR **0건**. CI 실패·충돌·정체 PR 없음.
- 이월 항목 — `feat/last-seen-at-debounce`(main 미병합, 3커밋 앞섬, PR 없음)는 여전히 사람 판단 대기. 오케스트레이터 자율 범위 밖이라 dashboard "개발 현황"에 명시만 하고 조치 없음.
- Telegram: 상태 요약 1건 발송.

## 오케스트레이터 (18:30)

전 러닝북 이상 없음. 경보 없음. 12:00 실행 대비 상태 변화 없음.

- **헬스체크** — prod backend `{"status":"ok","db":true,"katago_alive":false}` 200, prod web 200. launchctl `com.baduk.web`(PID 1504)·`com.baduk.api`(PID 1516) 모두 가동 — 12:00과 동일 PID로, 그 사이 재기동 없었음. staging 중단(정책상 정상). 디스크 6%. `sync-launchd.sh --check` exit 0 — plist drift 없음.
- **백업 검증** — 최신 `baduk-20260713T040005.db.gz` (7/13 04:00, 14.5시간 전 → 신선, `-mtime +1` 해당 없음). 티어 daily 14 / weekly 8 / monthly 2. 복원 드릴 `integrity_check=ok`, 테이블 7개 → 정상.
- **버그 스캔** — **신규 이슈 0건**. `baduk-api.err`의 `TypeError: Object of type bytes is not JSON serializable` 2회는 12:00 실행과 동일한 **오탐**으로 재확인:
  - 발생 위치 221457·221549행 vs. 마지막 재기동(`Started server process [1516]`) 221718행 → **두 건 모두 현재 프로세스 기동(7/12 00:53) 이전** 기록. 이후 재발 0회.
  - 현 HEAD `errors.py`에 `_safe_validation_errors()` 수정 반영됨(`ce9d745`, 6/11) — 프로세스 기동일보다 앞섬. 이미 종료된 #53과 동일 사안.
  - 로그 파일 자체가 7/12 09:15 이후 미갱신(신규 에러 없음).
- **백로그 트리아지** — 미분류 열린 이슈 **0건** (열린 이슈 자체가 0건) → 할 일 없음.
- **PR 감시** — 열린 PR **0건**. CI 실패·충돌·정체 PR 없음.
- 이월 항목 — `feat/last-seen-at-debounce` 사람 판단 대기 유지. 변동 없음.
- 도구 메모 — `gh` PATH 부재 재현(exit 127). `/opt/homebrew/bin`을 PATH에 추가해 우회. launchd 세션에서 3회 연속(7/12 · 7/13 02:00 · 7/13 18:30) 동일 증상 — 러닝북/래퍼에 PATH 고정을 넣을 만한 반복 문제.
- Telegram: 상태 요약 **1건 발송 성공** (message_id 2371). 이번 세션엔 플러그인 `reply` 도구가 노출되지 않아(ToolSearch 결과 없음) [[dev-cycle-telegram-botapi]] 대로 `~/.claude/channels/telegram/.env` 토큰 + chat_id로 Bot API 직접 호출 — `ok:true` 확인. "미발송"이 아니라 경로만 다름.
