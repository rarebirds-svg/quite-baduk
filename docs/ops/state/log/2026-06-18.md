# 운영 로그 — 2026-06-18

## 02:00 자율 버그 사이클 (dev-cycle)

### 이슈 선택
- `/opt/homebrew/bin/gh issue list --state open --json number,title,labels` → 열린 이슈 0건.
- `bug`/`small` 라벨 적격 이슈 없음 → **처리할 버그 없음**.
- 종료. worktree·브랜치 생성 안 함.

### 비고
- `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 조회.
- 6/13~6/17과 동일하게 신규 열린 이슈 없음.

### Telegram 보고
- telegram reply/send 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`(`ops/ops.env`)로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=True, message_id=2258).

## 12:30 오케스트레이터 정기 실행

### 헬스체크 (🟢)
- prod backend: `{"status":"ok","db":true,"katago_alive":false}` 200 OK. `katago_alive:false`는 lazy spawn 정보용 — 정상.
- prod web: 200 OK.
- launchctl: `com.baduk.api` PID 923 / `com.baduk.web` PID 912 가동.
- 디스크: 5%. drift: `sync-launchd.sh --check` exit 0, 출력 없음 → 정상.
- staging: 중단됨 (정책상 정상).

### 백업 검증 (🟢)
- 최신: `baduk-20260618T040001.db.gz` (오늘 04:00, -mtime +1 미해당 → 신선).
- 티어: daily 14 / weekly 4 / monthly 1.
- 복원 드릴: integrity_check=ok, tables=7 → 정상.

### 버그 스캔 (🟢) — 신규 0건
- `baduk-api.err`에 `TypeError: Object of type bytes is not JSON serializable` 2건(라인 221457·221549) 발견.
- **재발성 검증(누적 카운트 아닌 최근성)**: 두 발생 모두 현 프로세스 시작 라인(221583, proc 923, Jun 13 22:49 기동) *이전*. 이후 err 로그는 INFO WebSocket 뿐, 신규 에러 0건(마지막 기록 Jun 16 23:22).
- 원인 코드는 이미 수정·배포됨: `app/errors.py` `_safe_validation_errors`(bytes custom_encoder) — 커밋 `ce9d745`(Jun 11 21:12), 현 HEAD·proc 923에 라이브. → 이슈 생성 안 함(stale·해결됨). [[bug-scan-verify-recency]] 사례.

### 백로그 트리아지 (🟢) — 0건
- 미분류 열린 이슈 `[]`. 처리 없음.

### PR 감시 (🟢) — 0건
- 열린 PR `[]`. 주의 PR 없음.

### 보류 승인
- `pending-approvals.md` 대기 중 0건.

### Telegram 보고
- telegram reply/send 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=true, message_id=2259).

## 18:30 오케스트레이터 정기 실행

### 헬스체크 (🟢)
- prod backend: `{"status":"ok","db":true,"katago_alive":false}` 200 OK. `katago_alive:false`는 lazy spawn 정보용 — 정상.
- prod web: 200 OK.
- launchctl: `com.baduk.api` PID 923 / `com.baduk.web` PID 912 가동.
- 디스크: 5%. drift: `sync-launchd.sh --check` exit 0, 출력 없음 → 정상.
- staging: 중단됨 (정책상 정상).

### 백업 검증 (🟢)
- 최신: `baduk-20260618T040001.db.gz` (오늘 04:00, -mtime +1 미해당 → 신선).
- 티어: daily 14 / weekly 4 / monthly 1.
- 복원 드릴: integrity_check=ok, tables=7 → 정상.

### 버그 스캔 (🟢) — 신규 0건
- `baduk-api.err`의 `TypeError: Object of type bytes is not JSON serializable` 2건(라인 221457·221549)은 12:30과 동일하게 stale. 파일 mtime Jun 16 23:22, 두 발생 모두 현 proc 923 시작 이전. 이후 신규 에러 0건. 원인은 `ce9d745`(`app/errors.py`)로 수정·배포 완료 → 이슈 생성 안 함. [[bug-scan-verify-recency]].

### 백로그 트리아지 (🟢) — 0건
- 미분류 열린 이슈 `[]`. 처리 없음.

### PR 감시 (🟢) — 0건
- 열린 PR `[]`. 주의 PR 없음.

### 보류 승인
- `pending-approvals.md` 대기 중 0건.

### Telegram 보고
- telegram reply/send 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=true, message_id=2260).
