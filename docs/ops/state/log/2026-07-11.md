# 2026-07-11 운영 로그

## content-draft

- 처리할 토픽 없음 — 글로서리(18/18)·FAQ(9/9) 시드 슬러그가 모두 게시 완료. 미작성 후보 0개. 기존 항목 개정 없이 종료.

## dev-cycle (02:00)

- 이슈 선택: `gh issue list --state open --json number,title,labels` 결과 열린 이슈 0건 — `bug`·`small` 적격 이슈 없음.
- 리포 연결 정상 교차 확인 (`gh repo view` → `rarebirds-svg/quite-baduk`, `--state all` 조회 시 최근 이슈 #53까지 전부 CLOSED).
- 처리할 버그 없음 → worktree 생성·코드 변경·커밋·핸드오프 없이 종료.
- Telegram 보고: reply 도구 미노출 시 `~/.claude/channels/telegram/` 토큰으로 Bot API `sendMessage` 직접 발송.

## 오케스트레이터 (12:00 정기)

- healthcheck: prod backend·web 200 OK, db OK, katago_alive:false(lazy spawn, 정보용), launchd api 493 / web 478 가동, 디스크 6%, plist drift 없음(exit 0). staging 중단(정책상 정상).
- backup-verify: 최신 baduk-20260711T040005.db.gz (신선), daily/weekly/monthly 14/7/2, 복원 드릴 integrity=ok, 테이블 7 → 정상.
- bug-scan: baduk-api.err 최근 로그는 정상 WS 연결·재기동뿐, 스택 트레이스·반복 예외 없음 → 신규 버그 없음.
- backlog-triage: 미분류 이슈 0건.
- pr-watch: 열린 PR 0건 → 주의 PR 없음.
- 보류 승인 0건. 최근 CWI ingest 2026-07-05(new=200). 최근 리포트 2026-W27.
- 참고: 이 셸 PATH에 /opt/homebrew/bin 없어 `gh`는 절대경로로 호출.
- Telegram: reply 도구 미노출 → Bot API `sendMessage` 직접 발송 성공 (ok:true, message_id 2356).

## 오케스트레이터 (18:30 정기)

- healthcheck: prod backend `/api/health` 200 (status ok, db true), web 200 OK, launchd api 493 / web 478 가동, 디스크 6%, plist drift 없음(exit 0). staging 중단(정책상 정상). katago_alive:false는 lazy spawn — 정보용, 경보 아님.
- backup-verify: 최신 baduk-20260711T040005.db.gz (7/11 04:00, 신선 — `-mtime +1` 미해당), daily/weekly/monthly 14/7/2, 복원 드릴 integrity_check=ok, 테이블 7 → 정상.
- bug-scan: **신규 0건**. err 로그 tail에서 `TypeError: Object of type bytes is not JSON serializable` (errors.py val_handler) 2회가 잡혔으나 **오탐으로 배제** — 근거 3중: (1) 수정 커밋 `ce9d745`(6/11) 이 HEAD 조상이고 현 `errors.py`에 `_safe_validation_errors` bytes 인코더 존재, (2) 트레이스백의 `errors.py:21`은 수정 전 라인 배치(현재 21행은 헬퍼 내부), (3) 마지막 발생은 프로세스 33051 시절이고 이후 재기동 6회(→현 493, 7/7 07:27 기동, 수정 이후 코드) 동안 **발생 0건**. 누적 카운트가 아닌 최근 발생 시점으로 판정 — `[[bug-scan-verify-recency]]` 재확인 사례.
- backlog-triage: 미분류(no:label) 이슈 0건 → 라벨링 작업 없음.
- pr-watch: 열린 PR 0건 → CI 실패·충돌·정체 없음.
- 보류 승인 0건(`pending-approvals.md` "대기 중" 비어 있음). 최근 CWI ingest 2026-07-05(new=200) — 주간 주기(6/21·6/28·7/5)라 stale 아님, 다음 7/12 예정. 최근 리포트 2026-W27. watchdog 신규 incident 0건.
- 참고: 이 셸 PATH에 `/opt/homebrew/bin` 없어 `gh`는 절대경로/PATH 주입으로 호출 필요(12:00 실행과 동일).
- Telegram: 세션에 reply 도구 미노출(ToolSearch 확인) → `~/.claude/channels/telegram/` 자격증명으로 Bot API `sendMessage` 발송 성공 (ok:true, message_id 2358, chat 1241277614).
