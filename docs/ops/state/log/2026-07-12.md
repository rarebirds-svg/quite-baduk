# 2026-07-12 운영 로그

## analytics-weekly (09:00)

- 주차 `2026-W28` 확인, `reports/2026-W28.md` 부재 → idempotent 체크 통과, 신규 작성.
- 수집(DB 카운트 인용): games 누적 313 / 이번 주 3 / 전주 16 (**-13판, -81%**). sessions 누적 1 / 이번 주 1 (지난 주까지 0 → 첫 행 적재). board_size 19:241, 9:60, 13:12. handicap 0:304, 4:5, 8·6·3 각 1.
- `/api/stats`는 **401**(인증 필요)로 직접 인용 불가 — `/api/health` 200이라 백엔드는 정상(launchd api pid 1516 LISTEN :8000). W27과 동일 조건이라 DB 카운트 기준으로 기록.
- 운영 카운트: 보류 승인 0건, 글로서리 18편·FAQ 9편. 최근 7일 신규는 글로서리 `susangjeon` 1편(FAQ 신규 없음).
- incidents 최근 기록은 2026-05-27 watchdog stale(content-draft·content-ingest) — 이번 주 신규 인시던트 0건. (경로는 `state/incidents/` 디렉터리가 아니라 `state/incidents.md` 단일 파일.)
- 리포트 보관: `docs/ops/state/reports/2026-W28.md`.
- Telegram: 세션에 `reply` 도구 미노출 → `ops/ops.env`의 chat_id + `~/.claude/channels/telegram/` 토큰으로 Bot API `sendMessage` 직접 발송 성공 (ok:true, message_id 2364).

## 오케스트레이터 (18:00)

러닝북 5종 전부 수행. prod 정상, 신규 이슈·PR·승인 0건.

- **헬스체크 🟢** — prod backend `{"status":"ok","db":true}` 200, prod web 200. launchctl `com.baduk.api` pid 1516 / `com.baduk.web` pid 1504 둘 다 가동. `katago_alive:false`는 lazy spawn 정보용 필드라 경보 아님(러닝북 명시). staging은 중단 — 상시 가동 필수가 아니므로 정상. 디스크 6%, plist drift 없음(`sync-launchd.sh --check` exit 0, 무출력).
- **백업 검증 🟢** — 최신 `baduk-20260712T040005.db.gz` (오늘 04:00, 30h 이내 → 신선). 티어 daily 14 / weekly 8 / monthly 2 (weekly가 7→8로 증가). 복원 드릴 통과 — `PRAGMA integrity_check` = `ok`, 테이블 7개.
- **버그 스캔 🟢 — 신규 0건.** err 로그에서 `TypeError: Object of type bytes is not JSON serializable`(2회) + `RequestValidationError`가 잡혔으나 **또다시 동일한 stale 트레이스**였다. 근거: (1) 위치가 line 221457·221549로 6/13·6/23·6/27·7/10 로그에 기록된 것과 **완전히 동일** — 로그가 append-only라 좌표가 고정된 것; (2) 마지막 재기동(line 221718, `Started server process [1516]` = 현재 launchctl PID) **이후 에러 라인 0건**; (3) 수정 코드 `_safe_validation_errors`(bytes custom_encoder)가 `backend/app/errors.py:12,20`에 현존 — `ce9d745`(6/11) 배포분. 트리거는 `deploy-check-77` 합성 프로브의 비-JSON 바디로 사용자 트래픽 유래도 아니다. 이슈 미생성. ([[bug-scan-verify-recency]] — 누적 카운트가 아니라 발생 시점으로 판정)
- **백로그 트리아지 🟢** — 미분류 열린 이슈 0건 (`no:label` 빈 결과). 할 일 없음.
- **PR 감시 🟢** — 열린 PR 0건. CI 실패·충돌·정체 PR 없음.
- 운영 카운트: 보류 승인 **0건**, 최근 CWI ingest **2026-07-12 03:08** (fetched=1637 new=200 duplicate=1437 error=0), 최근 주간 리포트 **2026-W28**.
- 도구 메모: 이 세션의 셸에 `gh`가 PATH에 없어 최초 호출이 exit 127로 실패 → `/opt/homebrew/bin`을 PATH에 추가해 재실행. 러닝북 명령을 그대로 쓰는 세션에서 재발 가능.
- Telegram: `reply` 도구가 세션에 미노출(ToolSearch로 실제 확인 — 채널 도구 대신 SendMessage·Gmail 등만 노출) → `~/.claude/channels/telegram/`의 토큰·chat_id로 Bot API `sendMessage` 직접 발송 성공 (ok:true, message_id 2366). ([[dev-cycle-telegram-botapi]])
