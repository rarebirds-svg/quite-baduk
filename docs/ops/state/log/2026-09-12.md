# 2026-09-12

- 02:00 content-draft — glossary `chimip-sakgam`(침입·삭감) 초안 생성 → korean-copy-qa 통과(치명 0, 정밀성 제안 2건 전부 반영 — 삭감 위치 "경계 바깥"→"경계 부근"·두터움 쌓는 주체 "내 쪽도" 명시, related 3개 실존 확인) → main 게시·push(aa658e9)·IndexNow 200·라이브 200. 선택 근거: glossary 23/57(0.403) < faq 10/24(0.416). 잡 상태 `ok` 기록(state/jobs/content-draft.json).
- 04:30 dev-cycle — **처리할 버그 없음(6일 연속).** 열린 `bug` 이슈 3건(#81·#84·#87)은 각각 PR #83·#86·#95가 이미 열려 CI 4잡 전부 통과, `MERGEABLE`/`CLEAN`, `origin/main`(`aa658e9`) 대비 3브랜치 모두 충돌 0으로 사람 머지(🟡)를 기다린다. 재선택하면 중복 작업이라 착수하지 않았다. 세 이슈 모두 완료 코멘트가 있고 `in-progress` 라벨 없음. `.worktrees/dev-cycle`은 `fix/issue-87` 26ddff7로 `origin/fix/issue-87`과 일치, 미커밋·미push 변경 없음. #83은 정체 10일째, #95는 9/13 03:00 주간 ingest 전 발효 마감까지 약 22시간 남아 사람 머지 판단이 필요하다. 코드 변경 없음. 잡 상태 `ok` 기록.

## 09:00 오케스트레이터 사이클 (am)

- **헬스체크 — 전 항목 정상.** `/api/health` 200 `{db:true, migrations {current:0020, head:0020, pending:false}}`(`katago_alive:false`는 정보용 — 밤사이 AI 호출 없음), web 200, `com.baduk.api` PID 89132(9/8 23:55:44 기동)·`com.baduk.web` PID 89541(9/8 23:56:43 기동) 유지, staging 중단(정책상 정상), 디스크 7%, `sync-launchd.sh --check` exit 0(drift 없음), Claude 인증 `ok: D-20 (만료 10-02 23:41)`, `/api/spectate` 200. **복구(9/8 23:55) 후 81시간 무사고.**
- **백업 검증 — 정상.** 최신 `baduk-20260912T040001.db.gz`(9/12 04:00, 4,437,307B, +350B — 착수 없이 visit_hits·글로서리 게시분만) 신선, `find -mtime +1` 출력 없음. daily 14 / weekly 8 / monthly 4(04:00 생성과 함께 8/29분 보존 정리). 복원 드릴 `integrity_check` ok·테이블 11·사본 `pro_games` 4111 = **73회 연속** 통과.
- **버그 스캔 — 신규 버그 없음.** `.err` 234,964 → 234,964줄(**+0**, mtime 9/10 16:18 그대로 — 9/11 21:00 이후 WS 접속 없음), **트레이스백 0**(tail-200의 유일한 `OperationalError: no such column: sessions.account_id`는 OPS-20260907-01 당시 옛 줄). `.log` 316,965 → 317,922줄(+957) 분포 200 948·204 4·404 3·401 2, **5xx 0·429 0**(404 3은 `34.181.207.27`의 `/api/graphql`·`/api/.env`·`/api/.env/public/.env` 스캐닝 봇 차단, 401 2는 첫 방문 `/api/session` 정상) — 증가분 대부분 Cloudflare 모니터 프로브(`127.0.0.1` 775건), IPv6 131건, `125.248.8.109` 40건(사이트 열람), `games/*/moves` 호출 0·`ws/games` 0. **`baduk-web.err` 3,429 → 3,451줄(+22, mtime 9/12 06:36)** — `Failed to Find Server Action "x"` 2건. 액션 id가 문자 그대로 `x`라 현 빌드에도 옛 빌드에도 없는 값 = Next Server Action 스캐너 프로브(같은 시간대 `.env` 스캐닝과 동일 부류). stale 클라이언트 재생도 장애도 아님 — 기준선을 3,451줄로 갱신하고 이슈 대상 아님. 열린 `bug` 이슈 3건(#81·#84·#87) 전부 PR 대기 중이라 중복 없음.
- **대국 활동(DB 실측)** — 9/11 21:00 이후 착수 **0수**·신규 대국 0건(`games max(id)` 466 유지). **마지막 수 9/10 14:45 KST(#465) 이후 42시간 착수 0** — 5xx·트레이스백 0, 프로브·spectate 정상이라 장애 신호 아님(사용자 유입 없음). `visit_hits` 526(+4/12h, 마지막 방문 9/12 02:50 KST).
- **백로그 트리아지** — 미분류 이슈 0건, 할 일 없음.
- **PR 감시 — 주의 PR 2건(정체).** 열린 PR 3건(#95·#86·#83) 전부 CI 4잡(backend·frontend·app-shell-build·e2e) SUCCESS, `MERGEABLE`, `git merge-tree --write-tree` origin/main(`aa658e9`) 대비 충돌 0(`26ddff7`·`cb4450a`·`3490f28`). **#83 정체 9일 4시간째**(9/3 04:46 KST 생성), **#86 정체 전환 — 7일 4시간째**(9/5 04:34 KST 생성, 예고대로 오늘 04:34 7일 초과), #95 5일 4시간. 원인은 모두 🟡 머지 승인 미회신 — dev 작업 대상 아님.
- **배포 갭** — `git rev-list --count HEAD..origin/main` = 0, HEAD = origin/main `aa658e9`. 마지막 backend/web 코드 변경 #96(`7794a8f`, 9/8) 이후 프로세스 기동 9/8 23:55·`BUILD_ID` 9/8 23:56이므로 라이브 반영 갭 없음. `aa658e9`(02:02 글로서리 게시)는 마크다운 콘텐츠라 force-dynamic 목록에 즉시 반영, 재빌드 불필요.
- **잡 상태** — `state/jobs/*.json` 4/4 `ok`. content-draft 9/12 02:02 `chimip-sakgam` 게시, dev-cycle 9/12 04:31 "처리할 버그 없음(6일째) — PR #83·#86·#95 머지 대기, #95 마감 22h", analytics-weekly 9/6 09:01 W36, news-hook 9/6 05:03. watchdog 09:00 "신규 incident 0건".
- **승인 대기 3건 재확인**(`pending-approvals.md`에 기록) — AP-20260903-01(#83) **216시간째·17회(9일 정체)**, AP-20260905-01(#86) **168시간째·14회(7일 정체 전환)**, AP-20260907-02(#95) **120시간째·10회 — 9/13 03:00 ingest 전 발효 마감까지 약 18시간, 남은 정기 사이클 1회(9/12 21:00)뿐**. 착수 42시간 없음이라 #83·#86 재기동 적기도 계속 유효(실행 시 `moves.played_at` 재확인 필수).
- **장애** — 신규 incident 없음. `incidents.md` 변경 없음(mtime 9/9). prod 복구 후 81시간 무사고.
- **다이제스트** — `slot: am`, 5영역 service ok / jobs ok / deploy ok / anomaly ok / approval hold(3건), 승인 카드 3장. 발송 결과는 아래 추기.
- **다이제스트 발송 완료** — 09:04 `send.py` exit 0, 성공 마커 `~/.ops-report/markers/inkbaduk-am-2026-09-12` 생성. 승인 카드 3장 동봉(#95 발효 마감 18h·남은 사이클 1회 강조). 텔레그램 MCP 플러그인은 연결 실패 상태였으나 발송 경로는 `send.py`(Bot API)뿐이라 무관.

**사람 판단이 필요한 것**
- AP-20260907-02(PR #95)는 9/13 03:00 주간 ingest 전 발효 마감까지 약 18시간, 남은 정기 사이클은 오늘 21:00 1회뿐이다. 재기동 없이 머지·pull만으로 발효된다.
- AP-20260903-01·AP-20260905-01(PR #83·#86)은 착수가 42시간 없는 지금이 재기동 적기다. #86은 오늘 7일 정체로 전환됐다.

## 13:41~13:47 승인 3건 일괄 발효 (사람 요청 온디맨드 세션)

- #83·#86은 사람이 13:41 GitHub에서 직접 머지. 세션이 이어받아 #95 머지(13:46) → pull(`c0d8d05`, 배포 갭 0) → api 재기동 1회(13:46:27, health 200 in 3s, `migrations.pending` false).
- 재기동 시 대국 467 WS 접속 중(마지막 수 13:33)이었으나 사람 판단으로 즉시 진행. 클라이언트 즉시 자동 재접속, 트레이스백 0.
- 승인 큐 비움(0건). 상세는 `pending-approvals.md` 처리 완료 항목. 21:00 사이클은 재확인 카드 대신 발효 확인만 하면 된다 — #87은 내일 03:16 전후 `database is locked` 부재로 판정.

## 21:00 오케스트레이터 사이클 (pm)

- **헬스체크 — 전 항목 정상.** `/api/health` 200 `{db:true, migrations {current:0020, head:0020, pending:false}}`(`katago_alive:false`는 정보용 — 16:43 이후 AI 호출 없음), web 200, `com.baduk.api` **PID 5743(13:46:27 재기동 — #83·#86·#95 발효)**·`com.baduk.web` PID 89541(9/8 23:56 기동 유지), staging 중단(정책상 정상), 디스크 8%, `sync-launchd.sh --check` exit 0(drift 없음), Claude 인증 `ok: D-20 (만료 10-02 23:41)`, `/api/spectate` 200. launchctl의 api 행 두 번째 컬럼 `-15`는 13:46 `kickstart -k`의 SIGTERM 종료 코드 잔상이며 현재 프로세스는 정상. **재기동 후 7시간 무사고.**
- **발효 확인(13:41~13:47 승인 3건)** — 배포 갭 `git rev-list --count HEAD..origin/main` = 0, HEAD = origin/main `0401fa9`. 오늘 머지 3건의 변경 파일은 `backend/` 7개(`game_service.py`·`ingest_cwi_weekly.py`·tests 5)뿐이라 **web 재빌드·재기동 불필요** — `BUILD_ID` 9/8 23:56 그대로여도 라이브 갭 없음. `.err` 재기동 블록에 alembic 자동 upgrade 2줄(`Context impl SQLiteImpl` / `non-transactional DDL`)·`Application startup complete` 확인, 재기동 직전 접속 중이던 #467이 새 PID에 즉시 `[accepted]`.
- **백업 검증 — 정상.** 최신 `baduk-20260912T040001.db.gz`(9/12 04:00, 4,437,307B) 재검증 신선, `find -mtime +1` 출력 없음. daily 14 / weekly 8 / monthly 4. 복원 드릴 `integrity_check` ok·테이블 11·사본 `pro_games` 4111 = **74회 연속** 통과.
- **버그 스캔 — 신규 버그 없음.** `.err` 234,964 → 234,989줄(**+25**, mtime 16:44) — 전부 재기동 블록 13줄 + WS accepted/open/closed(#467·#468·#469×2), **트레이스백 0·IntegrityError 0·`database is locked` 0**. `.log` 317,922 → 318,660줄(+738) 분포 200 684·201 5·204 19·401 11·404 19, **5xx 0·429 0**. 404 19는 전부 GCP 대역 3개 IP(`34.181.207.27`·`34.180.46.6`·`34.18.75.38`)의 `.env`(v1~v3·dev·staging)·`/api/graphql`·windmill `get_log_file/../../proc/self/environ` 경로 순회 프로브 + `104.28.222.43`의 `/api/config` 1건 — 전부 404 차단, 이슈 대상 아님. 401 11은 첫 방문 `/api/session` 정상. IP 분포 `127.0.0.1` 489(Cloudflare 모니터 프로브)·`2a06:98c0:3600::103` 144·사용자 3개 IP 76건. **`baduk-web.err` 3,451줄 +0**(mtime 06:36 유지). 열린 `bug` 이슈 0건.
- **대국 활동(DB 실측)** — 오늘 **대국 3건·46수**(`games max(id)` 466 → 469). #467 13:30~13:49 KST 16수(`125.244.116.214`, 재기동 13:46을 가로질러 진행 — WS 자동 재접속 후 3수 더 둠), **재기동 후 #468 2수·#469 28수(16:37~16:43, `2406:5900:...`)** = 새 `game_service.py`(#83·#86) 코드로 착수 정상. 마지막 수 16:43 KST, 현재 진행 중 대국 0건. `visit_hits` 544(+18/12h, 마지막 방문 20:07 KST).
- **백로그 트리아지** — 미분류 이슈 0건, 할 일 없음.
- **PR 감시 — 열린 PR 0건.** #83·#86·#95 전부 13:41~13:46 머지 완료(원격 브랜치 삭제). 정체·CI 실패·충돌 PR 없음. `.worktrees/dev-cycle`은 머지된 `fix/issue-87` 26ddff7에 머물고 미커밋 변경 없음 — 내일 04:30 dev-cycle이 정리·재선택.
- **잡 상태** — `state/jobs/*.json` 4/4 `ok`. content-draft 9/12 02:02 `chimip-sakgam` 게시, dev-cycle 9/12 04:31 "처리할 버그 없음(6일째)", analytics-weekly 9/6 09:01 W36, news-hook 9/6 05:03(6일 경과 — 임계 9d 이내). watchdog 21:00 "신규 incident 0건", `[api] OK [web] OK`.
- **승인 대기 0건** — `pending-approvals.md` "대기 중" 비어 있음. 다이제스트 `approval: na`, 승인 카드 없음.
- **장애** — 신규 incident 없음. `incidents.md` 변경 없음.
- **다이제스트** — `slot: pm`, 5영역 service ok / jobs ok / deploy ok / anomaly ok / approval na. 발송 결과는 아래 추기.
- **다이제스트 발송 완료** — 21:03 `send.py` exit 0, 성공 마커 `~/.ops-report/markers/inkbaduk-pm-2026-09-12` 생성. 승인 카드 없음(대기 0건). service 행은 40자 한도로 `인증 D-20`을 `D-20`으로 축약(dry-run에서 절단 확인 후 수정). 텔레그램 MCP 플러그인은 연결 실패 상태였으나 발송 경로는 `send.py`(Bot API)뿐이라 무관.

**내일 09:00 사이클 확인 포인트**
- 9/13(일) 03:00 주간 CWI ingest가 #95 발효 후 첫 실행이다. `.err`에서 03:16 전후 `database is locked` 부재 + `pro_games` 4111 → 4311(+200) + `content-ingest.json` ok를 확인하면 #87 해소 확정. 일요일 04:00 백업 크기 급증은 정상(`[[backup-size-spike-weekly-ingest]]`).
- #81·#84 레이스는 재발 부재로만 판정 — `.err` IntegrityError·stale move_count 0 유지 확인.
