# 2026-07-10 운영 로그

## dev-cycle (02:00)

- 이슈 선택: `gh issue list --state open` 결과 열린 이슈 0건 — `bug`·`small` 적격 이슈 없음.
- 리포 연결 정상 교차 확인 (`gh repo view` → `rarebirds-svg/quite-baduk`, `--state closed` 조회 시 최근 이슈 #53까지 전부 CLOSED).
- 처리할 버그 없음 → worktree 생성·코드 변경·커밋·핸드오프 없이 종료.
- Telegram 보고 발송 완료 — reply 도구 미노출(ToolSearch 확인)로 Bot API `sendMessage` 직접 발송, message_id 2348, `ok:true`.

## 오케스트레이터 (12:00)

### healthcheck 🟢
- prod backend `/api/health` → `{"status":"ok","db":true,"katago_alive":false}` OK. `katago_alive:false`는 lazy spawn 정보용 필드 — 경보 아님.
- prod web `:3000` → 200 OK.
- launchctl: `com.baduk.web` PID 478, `com.baduk.api` PID 493 — 둘 다 가동.
- staging: 중단됨 (정책상 정상, 경보 아님).
- 디스크 사용률 6% (임계 90% 미만).
- launchd plist drift: `ops/sync-launchd.sh --check` exit 0, 출력 없음 → drift 없음.
- 판정: **prod 정상**. incident 없음.

### backup-verify 🟢
- 최신 백업 `baduk-20260710T040006.db.gz` (7/10 04:00) — `find -mtime +1` 무출력 → 신선.
- 티어: daily 14 / weekly 7 / monthly 2.
- 복원 드릴: `PRAGMA integrity_check` → `ok`, 테이블 7개. **정상**.

### bug-scan 🟢
- `baduk-api.err` 총 221,713줄. 마지막 기동(`Started server process [493]`, line 221699) **이후 에러 0건**.
- tail 500줄의 에러 패턴 6건은 모두 재기동 *이전* 발생분이며, 내용은 `deploy-check-77` 합성 프로브가 bytes 바디를 보내 발생한 `RequestValidationError`(#53과 동일 계열, 이미 `ce9d745`로 수정됨). 사용자 트래픽 유래 아님.
- 누적 카운트가 아니라 최근 발생 시점으로 판정.
- **신규 버그 없음** — 이슈 생성 0건.

### backlog-triage 🟢
- `gh issue list --state open --search "no:label"` → 빈 결과. 미분류 이슈 0건. 할 일 없음.

### pr-watch 🟢
- `gh pr list --state open` → `[]`. 열린 PR 0건. 주의 PR 없음.

### 부수 확인
- 보류 승인: `pending-approvals.md` "대기 중" **0건**.
- CWI ingest: 최근 2026-07-05 03:07 (fetched=1437 new=200 duplicate=1237 error=0). plist가 `Weekday=0`(일요일 03:00) 주간 잡이고 6/21·6/28·7/5 모두 일요일 → **주기대로 동작 중, stale 아님**. 다음 실행 7/12.
- 최근 주간 리포트: `2026-W27.md`.
- watchdog: 신규 incident 0건, api/web OK.
- 메모: `gh`가 이 셸의 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 호출.

### 보고
- Telegram `reply` 도구 세션 미노출(ToolSearch로 확인 — `SendMessage`/Canva/Push만 매칭). Bot API `sendMessage` 직접 발송으로 대체 → **발송 완료, message_id 2350, `ok:true`**.

## 오케스트레이터 (18:30 슬롯 · 18:00 기동)

### healthcheck 🟢
- prod backend `/api/health` → `{"status":"ok","db":true,"katago_alive":false}` OK (`katago_alive`는 정보용 필드, 경보 아님).
- prod web `:3000` → 200 OK.
- launchctl: `com.baduk.web` PID 478, `com.baduk.api` PID 493 — 12:00과 동일 PID, 재기동 없이 연속 가동.
- staging: 중단됨 (정책상 정상).
- 디스크 사용률 6%.
- launchd plist drift: `--check` exit 0, 출력 없음 → drift 없음.
- 판정: **prod 정상**. incident 없음.

### backup-verify 🟢
- 최신 백업 `baduk-20260710T040006.db.gz` (7/10 04:00) — `find -mtime +1` 무출력 → 신선.
- 티어: daily 14 / weekly 7 / monthly 2.
- 복원 드릴: `PRAGMA integrity_check` → `ok`, 테이블 7개. **정상**.

### bug-scan 🟢
- `baduk-api.err` 총 221,713줄, 파일 mtime 7/8 20:14 (12:00 이후 신규 기록 없음).
- `TypeError: Object of type bytes is not JSON serializable` 누적 2건(line 221457·221549). **누적 카운트가 아니라 최근성으로 판정** — 두 건 모두 이후 5회의 `Started server process` 재기동보다 앞서고, 현 프로세스 PID 493은 7/7 07:27 기동.
- 트레이스백의 `errors.py:21 in val_handler`는 **현 소스와 불일치**(현재 21행은 `_safe_validation_errors` 내부, `val_handler`는 33행) → 수정 이전 파일 레이아웃의 잔재임을 교차 확인. 픽스는 `ce9d745`(2026-06-11) `fix(api): 비JSON 본문 검증 오류가 500으로 둔갑하는 버그 수정`.
- **신규 버그 없음** — 이슈 생성 0건.

### backlog-triage 🟢
- `gh issue list --state open --search "no:label"` → 빈 결과. 미분류 이슈 0건.

### pr-watch 🟢
- `gh pr list --state open` → 빈 결과. 열린 PR 0건. CI 실패·충돌·정체 없음.

### 부수 확인
- 보류 승인: `pending-approvals.md` "대기 중" **0건**.
- CWI ingest: 최근 2026-07-05 03:07 (new=200). 주간 잡(일요일 03:00) 주기대로 동작, 다음 실행 7/12.
- 최근 주간 리포트: `2026-W27.md`.
- `gh`는 이 셸 PATH 미포함 → `/opt/homebrew/bin` 을 PATH에 추가해 호출.
- 인바운드 답신 없음 (`~/.claude/channels/telegram/inbox/` 최신 항목 6/13, 이미지) → 승인 처리 절차 해당 없음.

### 보고
- Telegram `reply` 도구 세션 미노출 — ToolSearch 실호출로 확인(`SendMessage`·Canva·`PushNotification`·`Monitor`만 매칭). 사전 판단 아님.
- 대체 경로로 Bot API `sendMessage` 직접 발송 (토큰 출처: `~/.claude/channels/telegram/.env`, chat_id는 `access.json`의 `allowFrom`) → **발송 완료, message_id 2352, `ok:true`**.
- 메모: zsh `--data-urlencode`가 한글 본문에서 `character not in range`로 실패 → Python `urllib`로 발송해 해결.
