# 2026-05-31

- content-draft: 글로서리 `bik`(빅) 초안 생성 → korean-copy-qa 통과(돌 무리 표현 1건 반영) → `web/content/glossary/bik.md` 게시·`main` push(a4cd9f2). /glossary/bik 라이브.
- **정정/사후(수동 검증 세션)** — 위 실행은 자율 게시 경로(승인 게이트 없이 커밋·`main` push·라이브·로그)가 정상 동작함을 확인했으나, **선택 버그**가 드러남: 이미 게시된 `bik`을 "미작성"으로 잘못 판단해 재선택·덮어씀(실제 커밋 `b48563e`, 위 로그의 `a4cd9f2`는 에이전트 오기재). 덮어쓴 커밋은 `git revert`(f7e0af0)로 원본(2026-05-23본) 복원. `content-draft-prompt.md` 선택 단계를 기계적 제외 + 게시 전 덮어쓰기 가드로 보강.
- **재검증** — 수정 후 content-draft 재실행 → 미작성 글로서리 알파벳 첫 항목 `hwajeom`(화점) 올바르게 선택·게시·main push(`22525a8`). /glossary/hwajeom 상세·목록 라이브. 선택 버그 해소 확인.
- **FAQ 신규 콘텐츠 부재 조치** — 원인은 버그 아닌 우선순위 설계(글로서리 소진 전까지 FAQ 미선택)로 FAQ 시드 4개가 굶고 있던 것. 선택 단계를 **비율 균형(most-behind: published/seed 낮은 종류 우선)**으로 변경. 현재 glossary 0.6 vs FAQ 0.2라 다음 사이클은 FAQ를 고른다.
- content-draft: 비율 균형 적용 첫 결과 — FAQ 0.2 < glossary 0.6이라 FAQ 선택, 미작성 후보 알파벳 첫 항목 `how-to-improve-rank`(단을 얻으려면) 초안 생성 → korean-copy-qa 통과(사활 패턴·돌 배치·착수마다 등 3건 반영) → `web/content/faq/how-to-improve-rank.md` 게시·`main` push(14d4890). /faq/how-to-improve-rank 라이브. 비율 균형 정상 동작 확인(FAQ 굶김 해소 시작).
- **시드 일괄 완성(사용자 지시)** — 남은 시드 7개를 수동 세션에서 작성·korean-copy-qa 검토·게시. 글로서리 4(pae·samsam·komi·kasaeng), FAQ 3(review-and-analysis·nickname-session·korean-vs-japanese-rules). QA 사실 오류 3건 코드 대조 후 반영: komi 중국 룰 인과 정정, nickname-session 쿠키 만료 방식(Max-Age 없음=브라우저 닫으면 삭제 vs 서버 idle 1시간 정리)·`country` 수집 명시, 한·일 규칙 사석 "결과적으로" 뉘앙스. 슬러그별 커밋 7개(e1ad86c~5e78648) `main` push. **시드 100% 완성 — glossary 10/10·faq 5/5 라이브.**
- content-draft: 확장 시드 첫 사이클 — 비율 균형 동률(glossary 10/18=0.56 = faq 5/9=0.56)이라 glossary 선택, 미작성 후보 알파벳 첫 항목 `dansu`(단수) 초안 생성 → korean-copy-qa 점검(주어 일관성·대응법 반격 보강·활로 표현 등 4건 반영) → `web/content/glossary/dansu.md` 게시·`main` push(fd910f4). /glossary/dansu 라이브. glossary 11/18·faq 5/9.
- content-draft: 비율 균형 — faq 5/9=0.56 < glossary 11/18=0.61이라 FAQ 선택, 미작성 후보 알파벳 첫 항목 `ai-strength-levels`(AI 기력 난이도) 초안 생성 → korean-copy-qa 점검(랭크→급수 통일·maxVisits 일반 독자용 풀이·"높은 단수일수록" 오표기·이유 중복 제거 등 5건 반영) → `web/content/faq/ai-strength-levels.md` 게시·`main` push(bdacd34). /faq/ai-strength-levels 라이브. glossary 11/18·faq 6/9.
- **dev-cycle(자율 버그, 02:00)** — 적격 이슈 #39(`bug`) 처리 2회차. 2026-05-30 코멘트로 추가된 WS **수신 경로 변종**(`ws.py:186` `receive_json()` → `RuntimeError: WebSocket is not connected...`, err 로그 70회) 해소. 원인: 1회차(6f0d556)는 외부 `except RuntimeError`에서 send-after-close(`'close message has been sent'`)만 흡수 → 수신 측은 다른 메시지라 재전파·누출. 수정: 흡수 조건에 `'not connected'` 추가, 그 외 RuntimeError 재전파 유지. TDD 회귀 테스트 1건(수정 전 실패→후 통과). **493 passed**, ruff·mypy clean. 브랜치 `fix/issue-39`(커밋 2개: 6f0d556 send + cc4593c receive), push 미수행(자율 세션 규범)·이슈에 핸드오프 코멘트 게시. 머지 🟡 사람 승인 대기.
- **오케스트레이터(12:00 실행)** — 5개 러닝북 수행. healthcheck: prod backend·web 200 OK·db OK·katago_alive:false(정보용·정상)·디스크 4%·plist drift 없음, staging 중단(정상). backup-verify: 최신 2026-05-31T04:00 integrity OK·7테이블·daily/weekly/monthly 12/2/0. backlog-triage: 미분류 0. pr-watch: 열린 PR 0. CWI ingest 최근 2026-05-31 03:00(new=0). 보류 승인 0건. 최근 분석 2026-W22.
- **bug-scan 자가정정** — err 로그에서 `database is locked` OperationalError ~1258회를 '현재 진행 중'으로 오판해 이슈 #42 생성 → **즉시 시점 검증으로 오탐 확인 후 close**. 해당 락 트레이스백은 늦은 줄 번호(220909/221133)에 있으나 가장 가까운 타임스탬프가 모두 2026-05-25 14:06:43이고 파일 전체 마지막 타임스탬프도 동일 — PR #30 이후 신규 0건. #41과 동일 케이스(누적 카운트 함정). 교훈: bug-scan은 카운트뿐 아니라 **최근 발생 시점**을 반드시 검증해야 함. WS RuntimeError(70회)는 기존 #39로 이미 추적 중이라 신규 이슈 미생성.
- **타임스탬프 표시 버그 수정·배포(사용자 지시)** — 세션·대국 로그 datetime이 naive UTC로 'Z' 없이 전송돼 프론트 new Date()가 로컬(KST)로 오해석 → 화면 시각 9시간 과거 표시. 공용 `UtcDatetime`(PlainSerializer, when_used=json)으로 UTC 'Z' 직렬화. GameSummary·AdminSessionRow·AdminLoginRow·AdminGameRow 적용. TDD(실패→통과), 493 passed·ruff·mypy clean. 커밋 `55a5dfd` push. **com.baduk.api 재시작 배포**(PID 1466→22846, 헬스 OK), 라이브 응답 `started_at=…Z` 확인.
- **Telegram 보고(12:00 실행)** — ToolSearch로 telegram reply/send 도구를 실제 조회했으나 세션에 미노출(연결 서버 대기 후에도 Canva·Monitor·PushNotification만 surface, `plugin:telegram@claude-plugins-official` 도구 없음). 사전 자가진단이 아닌 실제 호출 시도 결과로 **도구 미노출(MCP 미로드)** 기록. 발송 본문은 아래 상태 요약 — prod 정상·보류 승인 0건·최근 CWI ingest 2026-05-31 03:00(신규 0)·최근 분석 2026-W22. (참고: `[[orchestrator-no-telegram]]`은 reply 도구가 노출된 세션 한정이며, 이번 세션엔 도구 자체가 없었다.)
- **주간 분석 리포트(일요일 09:00)** — 2026-W22 작성. 게임 이번 주 24판(전주 51, −27·−52.9%)·누적 253. 신규 세션 1·누적 1(세션 테이블 사실상 빔, 재방문 추정 불가). 보드 19로 195·9로 46·13로 12, 핸디캡 0이 244로 대부분. 글로서리 11·FAQ 6(신규 `dansu`·`ai-strength-levels`). 보류 승인 0건, 최근 incident 2026-05-27 watchdog content stale. `/api/health` ok(db:true·katago_alive:false), `/api/stats` 무응답(데이터는 DB 직접 조회로 대체). 리포트 `docs/ops/state/reports/2026-W22.md` 저장·Telegram 푸시 완료(HTTP 200).
- **오케스트레이터(18:30 실행, 18:00 기동)** — 5개 러닝북 수행. healthcheck: prod backend `/api/health` status ok(db:true·katago_alive:true)·web 200·launchd api(PID 22846)·web(PID 36215) 가동, staging 중단(정상), 디스크 4%, plist drift 없음(rc=0). backup-verify: 최신 20260531T040005(integrity ok·7테이블), 티어 daily 12/weekly 2/monthly 0. bug-scan: 신규 0 — err 로그 WS RuntimeError traceback은 전부 재시작 전 프로세스(1466) 것이고 기존 #39(OPEN, `fix/issue-39` 머지 대기)로 추적 중, 현재 프로세스(22846)는 재시작 후 신규 에러 0건. backlog-triage: 미분류 0. pr-watch: 열린 PR 1건 — #43 `fix/issue-42-sqlite-pool-size`(생성 오늘 2026-05-31T03:06Z, MERGEABLE, CI 체크 없음, draft 아님) = 정상(정체·충돌 아님). dev-cycle가 #42(SQLite pool-limit 방어) 자율 픽스로 올린 것이며 머지는 🟡 사람 승인 대기. 참고: #42의 database-locked 자체는 PR #30 이후 신규 0건(마지막 2026-05-25 14:06:43)이라 본 PR은 방어적 성격. 보류 승인 0건. 최근 분석 2026-W22.
- **Telegram 보고(18:30 실행)** — ToolSearch로 telegram/reply/notification 도구를 실제 조회했으나 세션에 미노출(`plugin:telegram@claude-plugins-official` reply 도구 없음). 사전 자가진단 아닌 실제 호출 시도 결과로 **도구 미노출(MCP 미로드)** 기록. 상태 요약 본문은 본 로그로 대체 — prod 정상·보류 승인 0건·최근 CWI ingest 2026-05-31 03:00(신규 0)·최근 분석 2026-W22·열린 PR 1건(#43, 정상·머지 대기).
- **타임스탬프 'Z' 전수 점검 잔여분 수정·배포(사용자 지시)** — 클라이언트로 나가는 instant 타임스탬프 전수 점검. 잔여 2건 수정: `SpectateRow.started_at/finished_at`→UtcDatetime, `pro_sitemap.created_at`→utc_iso(헬퍼 공개화). 제외(정상): `game_date`(달력 date)·일별집계 키·미직렬화(analysis_cache·move.played_at). TDD 테스트 3건 추가, 496 passed·ruff·mypy clean. 커밋 `9b1f738` push. **com.baduk.api 재시작 배포**(PID 22846→28352, 헬스 OK), 라이브 pro 사이트맵 911건 전부 created_at `…Z` 확인.
