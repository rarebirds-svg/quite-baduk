# 2026-07-27

- 02:00 dev-cycle: **처리할 버그 없음** — 신규 작업 없이 종료(net-zero, 커밋·브랜치·PR 생성 없음).
  - 이슈 선택. `gh issue list --state open` 결과 **열린 이슈 1건뿐**(`#65`, 라벨 `bug`·`prio:high`). `feature`·`in-progress` 없어 라벨상 적격이지만 **7/24 사이클이 이미 처리해 PR #66이 열려 있다** — 재처리하면 중복 브랜치·중복 PR이 된다. `in-progress` 부착도 하지 않았다.
  - **PR #66 상태.** `fix/issue-65` / `386567d`, CI 4잡 **전부 SUCCESS**(backend·frontend·app-shell-build·e2e), `mergeable=MERGEABLE` · `mergeStateStatus=CLEAN`. 생성 `2026-07-24T17:06:26Z` = **약 48시간** 사람 머지 대기. 머지는 🟡라 손대지 않음. 승인 요청 `AP-20260725-01` 이미 등재 — 신규 등재 없음.

  ## 이번 사이클의 신규 사실 — prod 재기동으로 `busy_timeout`이 30s가 됐다

  - prod api PID **94302**, 기동 `Sun Jul 26 20:24:16`. 7/26 저녁에만 재기동 2회(`.err` 기동 마커 line 225360 PID 78066, 225381 PID 94302). 어제 18:00 시점 PID 40446에서 교체됨.
  - `main` HEAD **`38fd9e3`**(7/26 20:23, #69 대국 프리즈 fix) 배포 = 그 조상 **`1bb0457` "SQLite busy_timeout 5s→30s"(7/26 19:57)가 라이브**. 작업 트리 실측 `backend/app/db.py:33` → `PRAGMA busy_timeout=30000`.
  - **이것이 #65에 직접 걸린다.** 이슈 본문의 원인 진술이 "…`busy_timeout=5000`을 초과한다"였으므로, 500이 터질 임계가 **6배**가 됐다. #66 미머지와 무관하게 실패 조건이 바뀐 첫 사례다.

  ## 그럼에도 "완화됨" 판정은 하지 않는다 — 또 저부하 구간이다 (`[[bug-scan-verify-recency]]`)

  - 현 프로세스 마커(line 225381) 이후 `database is locked` **0건**. `.err` 225,390줄 / mtime `Jul 26 21:57`. 마지막 `pro_games` 락 실패는 line **225145**로 현 프로세스 **이전**이다.
  - 그러나 같은 구간이 저부하다. `.log` 204,575줄(7/26 18:00) → **205,439줄**, 델타 **+864줄 / 약 8시간**. 그 구간 `/api/spectate/pro/` **20건**, **5xx 0건**(200×774 · 401×63 · 204×9 · 201×6).
  - #65가 재현된 7/23~24 버스트는 크롤러가 프로 상세를 훑던 구간이다. 20건은 경합이 날 밀도가 아니다. 어제 12:00(219건/10h)·18:00(17건/6h)과 같은 판정 보류 구간.
  - 로그 채널 분리 준수(`[[api-log-stdout-vs-stderr]]`) — 요청량은 `.log`, 트레이스백·기동 마커는 `.err`.
  - **자기 정정.** 조사 초기에 "최근 20,000줄에 프로 상세 230건 → 부하 있음"으로 읽었으나, 그 20,000줄은 현 프로세스 구간이 아니라 **수일치**였고 대부분 admin 폴링(`/api/admin/*` 약 19,240줄)이었다. 기동 마커 기준으로 다시 세어 20건으로 정정했다. 위치 기반 추정을 프로세스 스코프 카운트로 교체한 것이 정답이었다.

  ## #66의 머지 가치 판단 — 긴급도는 내려갔으나 취소 대상은 아니다

  - `busy_timeout` 상향은 **에러를 지연으로 바꾸는** 완화다. api는 `--workers 1`이고 SQLite 락 대기는 블로킹이라, 버스트가 오면 500 대신 **응답 지연**(최대 30초)으로 나타날 수 있다.
  - #66의 원자적 `view_count = view_count + 1`은 read-modify-write 왕복을 없애 **락 보유 시간 자체를 줄이므로** 상향과 상충하지 않고 보완한다. 갱신 유실 제거는 덤.
  - `AP-20260725-01`에 "긴급도 갱신" 항목을 추가해 이 판단을 기록했다. 긴급 머지는 아니되 취소도 아님.
  - 이슈 #65에 위 내용을 코멘트로 남겼다 — [comment-5084498684](https://github.com/rarebirds-svg/quite-baduk/issues/65#issuecomment-5084498684).
  - 후속(조회수 디바운스 플러시)은 7/24 사이클이 "사람 판단"으로 남긴 항목이라 이번에도 이슈를 만들지 않았다.
  - 보류 승인 **2건 대기 유지** — `AP-20260725-01`(PR #66 머지, 등재 7/25 12:00 기준 약 38시간), `AP-20260723-01`(`settings.json` deny `Write`→`Edit`, **약 86시간 무응답** · `[[settings-deny-write-vs-edit]]`).
  - 워크트리 `.worktrees/dev-cycle`는 `fix/issue-65`(`386567d`)에 그대로 둔다. PR이 열려 있어 정리 대상이 아니다.
  - **Telegram 보고 발송 완료 (message_id 2456).** 세션에 `reply` 도구 미노출 → `[[dev-cycle-telegram-botapi]]` 절차대로 Bot API 직접 호출(한글 본문 파일 + `text@파일` + `LC_ALL=en_US.UTF-8`). `ok=true`.
    - 첫 시도는 `chat_id is empty`로 **400 실패**했다. `access.json`에 `chat_id` 키가 없기 때문이다 — 실제 구조는 `allowFrom: ["1241277614"]`이고 chat_id는 그 배열 원소다. `"chat_id"` 키를 grep하는 추출은 항상 빈 값을 낸다. 두 번째 시도에서 `"[0-9]{6,}"` 추출로 정정해 성공.

## 12:00 · 오케스트레이터 (러닝북 5종)

- **헬스체크 OK.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200. launchctl 가동 — api PID **94302** / web PID **54913**. 디스크 **6%**, `sync-launchd.sh --check` exit 0(drift 없음). staging 중단 = 정책상 정상. watchdog 신규 incident 0건.
  - **`katago_alive:false`는 경보로 올리지 않았다.** 러닝북 L19~23이 명시하듯 KataGo는 첫 AI 응수 요청 시 lazy spawn이라, AI 호출이 없던 구간에서 false는 정상이다. 이 필드 단독으로 incident를 만들거나 prod 재기동을 제안하는 것은 false alarm이므로 하지 않았다. 실제 이 구간은 `/api/spectate/pro/` 위주의 읽기 트래픽뿐이었다.
  - **api PID가 어제 18:00의 40446에서 94302로 바뀐 것은 신규 사건이 아니다.** 기동 `Sun Jul 26 20:24:16`, `main` HEAD `38fd9e3`(#69 대국 프리즈 fix) 배포에 해당하며 **02:00 dev-cycle이 이미 규명**했다. 오케스트레이터 조치 불필요.
- **백업 검증 OK.** 최신 `baduk-20260727T040004.db.gz`(04:00) — `-mtime +1` 미해당 = 신선. 티어 **14/8/2**. 복원 드릴 `integrity_check=ok`, 테이블 **9개**. 어제 아티팩트(0726T040005)에서 정상 회전.
- **버그 스캔 — 신규 이슈 0건. 4연속 저부하 구간이라 #65 판정은 또 보류한다.**
  - `.err` **225,390줄 / mtime `Jul 26 21:57`로 불변**. 현 프로세스 마커(line 225381) 이후 `database is locked` **0건**. 마지막 락 실패 line 225347은 마커 *이전* = 구 프로세스 구간.
  - 02:04 이후 델타 실측 — `.log` 205,439 → **205,592줄(+153줄 / 약 8시간)**. 그 구간 `/api/spectate/pro/` **10건**, 상태코드 **전부 200**(5xx 0건).
  - **완화 신호로 읽지 않는다.** `busy_timeout` 30s가 라이브라 실패 임계가 6배 올라간 것은 사실이지만(02:00 규명), 10건/8시간은 #65를 재현시킨 크롤러 버스트(7/23~24, 17.7h/16건)와 밀도가 다르다. 어제 12:00(219건/10h)·18:00(17건/6h)·오늘 02:00(20건/8h)에 이은 네 번째 판정 보류다. PR #66 미머지 = 원자적 UPDATE는 여전히 prod에 없다(`[[bug-scan-verify-recency]]`).
  - 로그 채널 분리 준수(`[[api-log-stdout-vs-stderr]]`) — 요청량은 `.log`, 트레이스백·기동 마커는 `.err`.
  - 열린 `bug` 이슈는 #65 하나뿐, 반복되는 새 후보 에러 없음 → 이슈 생성 0건.
- **백로그 트리아지 — 미분류 0건.** `no:label` 결과 빈 배열, 할 일 없음.
- **PR 감시 — 열린 PR 1건, 정상.** #66 `mergeable=MERGEABLE`, 실패 체크 0건. 7/24 17:06Z 생성 = **약 67시간(2.8일)**로 7일 정체 기준 미달. 충돌·CI 실패 없음 → 주의 PR 0건. 머지는 🟡(AP-20260725-01)이라 손대지 않음.
- **보류 승인 2건 — 둘 다 무응답 지속.**
  - `AP-20260725-01` (PR #66 머지) — **48시간**. 02:00 사이클이 등재한 "긴급도 갱신"(busy_timeout 30s 라이브로 긴급도 하향, 단 보완 관계라 취소 아님)이 그대로 유효하다.
  - `AP-20260723-01` (`settings.json` deny `Write`→`Edit`) — **96시간**. **이번 실행 `orchestrator-runs.log`에도 동일 경고 2건 재출력** = 5일째 미해소(`[[settings-deny-write-vs-edit]]`).
- **부수 확인.** `visit_hits` **50행**(7/26 18:00 43행 → +7, 비콘 동작 지속). `pro_games` **2911**(다음 CWI ingest는 8/2 일요일). `prune-visits` 04:15 정상(`deleted=0 retention_days=180`). 최근 CWI ingest **2026-07-26 03:11**(`fetched=2060 new=200 duplicate=1860 error=0`). 최근 주간 리포트 **2026-W30**.
- **오케스트레이터 주기 불일치 재확인(미해소).** 이번 트리거는 **12:00:00 정각** = plist의 `StartCalendarInterval`(12:00·18:00 2회)대로다. 러닝북·프롬프트의 4회(06:30·12:30·18:30·23:30)와 불일치는 그대로이며, 문서를 2회로 맞출지 plist를 4회로 늘릴지는 사람 판단(후자는 prod 스케줄 변경이라 🟡).
- Telegram 인바운드 답신 없음(inbox 최신 7/22) → 승인 처리 절차 미해당.
- **Telegram 상태 요약 발송 완료 (message_id 2458).** 세션에 `reply` 도구 미노출(텔레그램 MCP 플러그인 미로드) → `[[dev-cycle-telegram-botapi]]` 절차대로 Bot API 직접 호출. `ok=true`. chat_id는 `access.json`의 `allowFrom` 배열 원소를 `"[0-9]{6,}"`로 추출(7/27 02:00 기록된 함정 회피).

## 18:00 · 오케스트레이터 (러닝북 5종)

- **헬스체크 OK.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200. launchctl 가동 — api PID **94302**(`Sun Jul 26 20:24:16` 기동, uptime **21시간 36분**) / web PID **54913**. 12:00 대비 **무재기동**. 디스크 **6%**, `sync-launchd.sh --check` exit 0(drift 없음). staging 중단 = 정책상 정상. watchdog 신규 incident 0건(최신 항목 여전히 `WD-20260527-*`).
  - `katago_alive:false`는 이번에도 경보로 올리지 않았다. 러닝북 L19~23의 lazy spawn 정상 동작이며, 이 구간 트래픽도 읽기 위주였다.
- **백업 검증 OK.** 최신 `baduk-20260727T040004.db.gz`(04:00, 3.02MB) — `-mtime +1` 미해당 = 신선. 티어 **14/8/2**. 복원 드릴 `integrity_check=ok`, 테이블 **9개**. 12:00과 동일 아티팩트(다음 회전은 7/28 04:00).
- **버그 스캔 — 신규 이슈 0건. #65 판정 5연속 보류(이번 구간은 역대 최저 부하).**
  - `.err` **225,390줄 / mtime `Jul 26 21:57`로 여전히 불변**. 현 프로세스 마커(line 225381) 이후 `database is locked` **0건**. 마지막 락 실패 line 225347은 마커 *이전* = 구 프로세스 구간(`[[bug-scan-verify-recency]]` 절차대로 기동 마커 스코프 집계).
  - 12:00 이후 델타 실측 — `.log` 205,592 → **205,681줄(+89줄 / 약 6시간)**. 그 구간 `/api/spectate/pro/` **7건**, 상태코드 200×86 · 404×1 · 401×1 · 204×1, **5xx 0건**.
  - **+89줄/6시간은 지금까지 관측한 구간 중 가장 낮다**(7/26 12:00 +20,068줄 → 18:00 +1,538 → 7/27 02:00 +864 → 12:00 +153 → 18:00 **+89**). 프로 상세 7건은 #65를 재현시킨 크롤러 버스트(7/23~24, 17.7h/16건)와 밀도가 두 자릿수 배 차이다. 이 0건은 완화 근거가 될 수 없다.
  - PR #66 미머지 = 원자적 UPDATE는 여전히 prod에 없다. `busy_timeout` 30s(`1bb0457`)만 라이브. 판정은 #66 머지 이후 또는 다음 크롤러 버스트에서.
  - 로그 채널 분리 준수(`[[api-log-stdout-vs-stderr]]`) — 요청량은 `.log`, 트레이스백·기동 마커는 `.err`.
  - 열린 `bug` 이슈는 #65 하나뿐, 반복되는 새 후보 에러 없음 → 이슈 생성 0건.
- **백로그 트리아지 — 미분류 0건.** `no:label` 결과 빈 배열, 할 일 없음.
- **PR 감시 — 열린 PR 1건, 정상.** #66 `mergeable=MERGEABLE`·`mergeStateStatus=CLEAN`, CI 4잡 전부 SUCCESS(backend·frontend·app-shell-build·e2e). 7/24 17:06Z 생성 = **약 73시간(3.0일)**로 7일 정체 기준 미달 → 주의 PR 0건. 머지는 🟡(AP-20260725-01)이라 손대지 않음.
- **보류 승인 2건 — 둘 다 무응답 지속.**
  - `AP-20260725-01` (PR #66 머지) — **54시간**. 12:00의 긴급도 판단(busy_timeout 30s 라이브로 긴급도 하향, 단 락 보유시간 단축과 보완 관계라 취소 아님) 그대로 유효. 신규 사실 없음.
  - `AP-20260723-01` (`settings.json` deny `Write`→`Edit`) — **102시간**. **이번 실행 `orchestrator-runs.log`(18:00:01 시작 직후)에도 동일 경고 2건 재출력** = 6일째 미해소(`[[settings-deny-write-vs-edit]]`).
- **부수 확인.** `visit_hits` **51행**(12:00 50행 → +1, 비콘 동작 지속 — 저부하와 정합). `pro_games` **2911** 무변동(다음 CWI ingest는 8/2 일요일). `prune-visits` 04:15 정상(`deleted=0 retention_days=180`). 최근 CWI ingest **2026-07-26 03:11**(`fetched=2060 new=200 duplicate=1860 error=0`). 최근 주간 리포트 **2026-W30**.
- **오케스트레이터 주기 불일치 재확인(미해소).** 이번 트리거는 **18:00:01** = plist의 `StartCalendarInterval`(12:00·18:00 2회)대로다. 러닝북·프롬프트의 4회(06:30·12:30·18:30·23:30)와 불일치는 그대로이며 사람 판단 대기.
- Telegram 인바운드 답신 없음 → 승인 처리 절차 미해당.
- **Telegram 상태 요약 발송 완료 (message_id 2459).** 세션에 `reply` 도구 미노출(텔레그램 MCP 플러그인 미로드) → `[[dev-cycle-telegram-botapi]]` 절차대로 Bot API 직접 호출. `ok=true`. chat_id는 `access.json`의 `allowFrom` 배열 원소를 `"[0-9]{6,}"`로 추출(02:00 기록된 함정 회피).
  - **첫 두 시도는 내 문법 착오로 실패했다(메모리는 옳았다).** 메모리의 `text@파일`은 **`--data-urlencode` 전용 문법**인데 이를 `-F`에 갖다 붙여 `curl: option -F: is badly used here`가 났다. `-F`는 `name=value` 형태가 필수라 파일 내용을 넣으려면 `-F "text=</tmp/…"`(`<` = 내용 읽기)여야 하고, `@`를 쓰면 파일 *업로드*가 된다. 또 `--env LC_ALL=...`는 curl 옵션이 아니다 — 로케일은 명령 앞에 `LC_ALL=en_US.UTF-8 curl …`로 붙인다. 최종 성공 형태는 `LC_ALL=en_US.UTF-8 curl -s -F "chat_id=…" -F "text=</tmp/…"`. 메모리 정정 불필요.
