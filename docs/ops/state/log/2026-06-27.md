# 2026-06-27 운영 로그

- 02:00 자율 버그 사이클 (dev-cycle)
  - 이슈 선택: `gh issue list --state open` 결과 열린 이슈 0건. `bug`·`small` 적격 이슈 없음.
  - 참고: 6/26 이후 신규 미분류 버그 없음. 열린 PR도 0건.
  - 결과: **처리할 버그 없음.** 코드 변경·커밋·핸드오프 없이 종료.
  - 환경 메모: `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로 사용. 인증 정상(rarebirds-svg, repo rarebirds-svg/quite-baduk).
  - Telegram 보고: 플러그인 reply 도구 이 세션 미노출(ToolSearch 확인 — SendMessage·PushNotification·Canva/HF MCP만 노출). `ops/notify-telegram.sh`로 "처리할 버그 없음" 요약 발송 성공(exit 0).

- 02:00 콘텐츠 초안 생성 사이클 (content-draft)
  - 종류 선택(most-behind): glossary 15/18=0.833 < faq 8/9=0.889 → glossary. 미작성 후보 알파벳순 첫 항목 `poseok`(포석) 선택. (poseok·seryeok-silli·susangjeon 중 첫째.)
  - 초안 작성 → korean-copy-qa QA: 사실 오류·콜론 종결 없음, 문체 개선 제안만 → 종합 수정안 반영.
  - 게시(자율 🟢): 덮어쓰기 가드 통과(기존 파일 없음). `web/content/glossary/poseok.md`만 add·commit(c750e68)·push origin main 성공(cfaec4f..c750e68).
  - Telegram 사후 보고 발송.

- 12:30 오케스트레이터 정기 실행
  - 헬스체크: prod backend `{"status":"ok","db":true,"katago_alive":true}`, web 200 OK, PID api 923 / web 912 가동, 디스크 6%, plist drift 없음(exit 0). staging 중단(정책상 정상).
  - 백업 검증: 최신 `baduk-20260627T040005.db.gz`(오늘 04:00, stale 아님), integrity_check=ok, 테이블 7. daily 14 / weekly 5 / monthly 1.
  - 버그 스캔: err 로그의 `TypeError: Object of type bytes is not JSON serializable`(2회, line 221457·221549, `errors.py` val_handler) 조사 → **이미 수정됨**. 트레이스백은 `_safe_validation_errors`(bytes custom_encoder) 도입 *이전* 구버전 코드 기준이며, 트리거는 `deploy-check-77` 프로브가 `/api/session`에 raw bytes 본문 POST한 것. err 로그 mtime 6/25 19:05 이후 재발 0건. 신규 이슈 없음(recency 검증 — `[[bug-scan-verify-recency]]`).
  - 백로그 트리아지: 미분류 이슈 0건.
  - PR 감시: 열린 PR 0건.
  - 환경 메모: `gh`가 PATH에 없어 `/opt/homebrew/bin` prepend 후 사용.
  - Telegram 보고: 플러그인 reply 도구 이 세션 미노출(ToolSearch 확인 — SendMessage·PushNotification·Canva MCP만 노출). `ops/notify-telegram.sh`로 정기 상태 요약 발송 성공(exit 0).
