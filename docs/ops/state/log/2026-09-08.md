# 2026-09-08

- 04:30 dev-cycle — **처리할 버그 없음**. 열린 이슈 3건(#81·#84·#87, 라벨 `bug`)은 모두 이미 PR이 열려 사람 머지 대기 중(#81 → PR [#83](https://github.com/rarebirds-svg/quite-baduk/pull/83) `fix/issue-81` 3490f28, #84 → PR [#86](https://github.com/rarebirds-svg/quite-baduk/pull/86) `fix/issue-84` cb4450a, #87 → PR [#95](https://github.com/rarebirds-svg/quite-baduk/pull/95) `fix/issue-87` 26ddff7). 세 PR 모두 CI 4잡(backend·frontend·app-shell-build·e2e) SUCCESS, 이슈에 완료 코멘트 존재, `in-progress` 잔존 없음. `.worktrees/dev-cycle`은 `fix/issue-87` 26ddff7 = `origin/fix/issue-87`로 미커밋·미push 잔여 없음. 재선택하면 중복 작업이므로 착수하지 않음. 코드 변경 0, 잡 상태 `ok` 보고. 머지는 🟡 사람 승인 대기 유지(권장 순서 #83 → #86 → #95, backend 재기동 1회).

- 09:00 오케스트레이터 (am) — **OPS-20260907-01 지속 33시간, 승인 미회신 24시간째 (에스컬레이션 유지).**
  - 헬스체크: backend `/api/health` 200(`db:true`, `katago_alive:false` 정보용 — KataGo 프로세스 3개 28553~28555는 00:06 이후 살아 있음, 첫 응수 요청이 없어 false)·web 200, `com.baduk.api` PID 28544·`com.baduk.web` PID 28792(9/7 00:06 기동 그대로, 재기동 없음). staging 중단(정책상 정상). 디스크 7%. plist drift 없음(`ops/sync-launchd.sh --check` exit 0). Claude 인증 **D-24**(만료 10-02 23:41). **`alembic_version` `0019` vs head `0020_accounts` — 갭 유지.** 직접 프로브 `GET /api/spectate` **500 재현**, `GET /api/session` 401(쿠키 없음, 정상).
  - 백업 검증: 최신 `baduk-20260908T040005.db.gz`(4,367,309B, +1,555B — 12시간 착수 0건이라 증분 최소) 신선(`-mtime +1` 출력 없음), daily 14 / weekly 8 / monthly 4, 복원 드릴 integrity_check **ok**·테이블 10·사본 `alembic_version` 0019 = **65회 연속** 통과. 이 백업도 마이그레이션 직전 복원점.
  - 버그 스캔: `.err` 231,142줄(**+167**), 신규 트레이스백 **1건** — `no such column: games.account_id`, 발생 시각·출처가 09:00 오케스트레이터 자체 프로브(`127.0.0.1` `GET /api/spectate` 500)와 일치. `.log` 284,372줄(+788) 상태 분포 200 771 / 404 3 / 500 1 / 401 1. **실사용자 500 0건 = 밤 12시간 동안 세션 보유 실사용자 요청 자체가 0건**(어제 낮 12건과 대비). 200은 프로 기보 SSR·sitemap·health 프로브. 404 3건은 `45.148.10.120`의 `/api/json/settings/*` 스캐너 잡음. 신규 버그 이슈 0건 — 장애 트레이스백은 OPS-20260907-01 배포 절차 결손이라 이슈화하지 않는 어제 판단 유지.
  - DB 실측: `max(games.id)` 453(+0), 마지막 수 `2026-09-06 14:18:04 UTC` = **9/6 23:18 KST, 착수 0건 33시간째**. `moves.played_at > 9/7 21:00 KST` 0건. `visit_hits` 469(**+0/12h** — 8/17 이후 처음으로 12시간 증가 0). `sessions` 8행. active 잔류 257건은 미완료 기본값(판정 근거 아님).
  - 백로그 트리아지: 미분류 0건. 열린 이슈 3건(#87·#84·#81, 전부 `bug`) 변동 없음, 전부 PR 머지 대기.
  - PR 감시: 열린 PR 3건 — [#95](https://github.com/rarebirds-svg/quite-baduk/pull/95)·[#86](https://github.com/rarebirds-svg/quite-baduk/pull/86)·[#83](https://github.com/rarebirds-svg/quite-baduk/pull/83) 전부 CI 4잡 SUCCESS·`MERGEABLE`·`CLEAN`(이번엔 캐시 계산됨). 7일 정체 0건(#83은 9/10 04:46 전환). **주의 PR 0건.** main CI 최신 런 `9b58ffe`(9/7 pm 문서 커밋) **success** — 직전 `16d9e0f` failure는 어제 판정대로 e2e 플레이크로 확정.
  - 잡 상태: `state/jobs/*.json` 4/4 `ok` — dev-cycle 04:30(처리할 버그 없음, 위 항목)·content-draft 9/5·analytics-weekly 9/6·news-hook 9/6. 배포 갭 `HEAD..origin/main` **0**, BUILD_ID 9/7 00:07 = 라이브 프로세스 기동과 일치(코드는 최신, DB 스키마만 미반영).
  - 장애 대응(incident.md): 4번 에스컬레이션 유지 — 화이트리스트(재기동)로는 복구 불가, DB 마이그레이션은 🟡. 날짜가 바뀌어 once_key `prod_migration_0020` 억제가 풀리므로 `kind: alert` 재발송(일 1회). incidents.md에 09:00 재확인 줄 추가.
  - 승인 재확인: AP-20260907-01 24시간째·2회(미실행), AP-20260907-02 24시간째·2회, AP-20260903-01 120시간째·9회, AP-20260905-01 72시간째·6회 — 전부 CI 그린·차단 없음. 권장 순서 유지(마이그레이션 → #83 → #86 → #95 머지·pull → api 재기동 1회).
  - 다이제스트: `slot: am`, service `fail`·deploy `warn`·anomaly `warn`, 승인 카드 4건 동봉. 발송 결과는 아래 발송 기록 참조.
  - 발송 기록: 09:06 `kind: alert`(once_key `prod_migration_0020`, 날짜 전환으로 억제 해제) exit 0 → 09:06 am 다이제스트 + 승인 카드 4건 exit 0, 마커 `~/.ops-report/markers/inkbaduk-am-2026-09-08`·`/tmp/ops-report-once-inkbaduk-prod_migration_0020-2026-09-08` 생성 확인. 첫 시도는 zsh 변수 단일 단어 처리로 exit 127(호출 구문 오류, 발송 계층 문제 아님) — 직접 호출로 즉시 재발송.

- 21:00 오케스트레이터 (pm) — **OPS-20260907-01 지속 45시간, 승인 미회신 36시간째 (에스컬레이션 유지). 낮 동안 실사용자 3명이 세션 발급·조회 500으로 튕김.**
  - 헬스체크: backend `/api/health` 200(`db:true`, `katago_alive:false` 정보용)·web 200, `com.baduk.api` PID 28544·`com.baduk.web` PID 28792(9/7 00:06 기동 그대로, 재기동 없음). staging 중단(정책상 정상). 디스크 7%. plist drift 없음(`ops/sync-launchd.sh --check` exit 0). Claude 인증 **D-24**(만료 10-02 23:41). **`alembic_version` `0019` vs head `0020_accounts` — 갭 유지.** 직접 프로브 `GET /api/spectate` **500 재현**, `GET /api/session` 401(쿠키 없음, 정상).
  - 백업 검증: 최신은 아침과 같은 `baduk-20260908T040005.db.gz`(4,367,309B) 신선(`-mtime +1` 출력 없음, 다음 생성 9/9 04:00), daily 14 / weekly 8 / monthly 4, 복원 드릴 integrity_check **ok**·테이블 10·사본 `alembic_version` 0019 = **66회 연속** 통과. 마이그레이션 직전 복원점 유효.
  - 버그 스캔: `.err` 233,535줄(**+2,393**), 신규 트레이스백 **14건** — `sessions.account_id` SELECT 7·INSERT(`table sessions has no column named account_id`) 6·`games.account_id` 1(21:00 자체 프로브). `.log` 290,588줄(+6,216) 상태 분포 200 6,158 / 204 10 / 401 11 / 429 11 / 500 14. **실사용자 500 13건, IP 3개** — `GET /api/session` 7(`218.37.101.154` 5·`166.104.68.53` 2), `POST /api/session` 6(`125.248.8.109` 5·`166.104.68.53` 1). **새 증상 — `125.248.8.109`는 `POST /api/session` 500 5회 뒤 429 11회**(클라이언트 재시도가 세션 발급 레이트리밋에 걸림). 200 6,158 중 5,944는 `127.0.0.1` 발 `/api/spectate/pro/N` SSR(크롤러 유입 대응, 정상). 신규 버그 이슈 0건 — 전부 OPS-20260907-01 증상이라 이슈화하지 않는 판단 유지.
  - DB 실측: `max(games.id)` 453(+0), 마지막 수 `2026-09-06 14:18:04 UTC` = **9/6 23:18 KST, 착수 0건 46시간째**. `moves.played_at > 9/8 09:00 KST` 0건. `visit_hits` 476(**+7/12h**, 마지막 19:07 KST — 낮 방문은 있었으나 세션 단계에서 막힘). `sessions` 8행(신규 발급 0 = INSERT 전부 500). active 잔류 257건은 미완료 기본값.
  - 백로그 트리아지: 미분류 0건. 열린 이슈 3건(#87·#84·#81, 전부 `bug`) 변동 없음, 전부 PR 머지 대기.
  - PR 감시: 열린 PR 3건 — [#95](https://github.com/rarebirds-svg/quite-baduk/pull/95)·[#86](https://github.com/rarebirds-svg/quite-baduk/pull/86)·[#83](https://github.com/rarebirds-svg/quite-baduk/pull/83) 전부 CI 4잡 SUCCESS, `mergeable` UNKNOWN(09:07 ops 커밋 `4dafbbe` 유입 후 캐시 미계산, 실질 변화 없음). 7일 정체 0건(#83은 9/10 04:46 전환). **주의 PR 0건.** main CI 최신 런 `4dafbbe`(9/8 am 문서 커밋) **success**.
  - 잡 상태: `state/jobs/*.json` 4/4 `ok` — dev-cycle 9/8 04:30·content-draft 9/5·analytics-weekly 9/6·news-hook 9/6. 배포 갭 `HEAD..origin/main` **0**, BUILD_ID 9/7 00:07 = 라이브 프로세스 기동과 일치(코드는 최신, DB 스키마만 미반영).
  - 장애 대응(incident.md): 5번째 에스컬레이션 유지 — 화이트리스트(재기동)로는 복구 불가, DB 마이그레이션은 🟡. `kind: alert`는 once_key `prod_migration_0020`이 09:06에 이미 소진돼 날짜당 1회 억제 대상(발송 시도 결과는 아래 발송 기록). incidents.md에 21:00 재확인 줄 추가.
  - 승인 재확인: AP-20260907-01 36시간째·3회(미실행), AP-20260907-02 36시간째·3회, AP-20260903-01 132시간째·10회, AP-20260905-01 84시간째·7회 — 전부 CI 그린·차단 없음. 권장 순서 유지(마이그레이션 → #83 → #86 → #95 머지·pull → api 재기동 1회).
  - 다이제스트: `slot: pm`, service `fail`·deploy `warn`·anomaly `warn`, 승인 카드 4건 동봉. 발송 결과는 아래 발송 기록 참조.
  - 발송 기록: 21:04 `kind: alert` 시도 → `once_key 억제로 발송 생략`(exit 0, 09:06분이 오늘 1회분 — 설계대로) → 21:04 pm 다이제스트 + 승인 카드 4건 exit 0, 마커 `~/.ops-report/markers/inkbaduk-pm-2026-09-08` 생성 확인. 페이로드는 파일(`/tmp/ops-payload/*.json`)로 쓰고 `send.py`를 직접 호출해 zsh 변수 함정 없이 1회에 성공.
