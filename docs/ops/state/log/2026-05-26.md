# 운영 로그 2026-05-26

## 02:00 — 자율 버그 사이클 (launchd 트리거)

- **이슈 후보 조회** — `gh issue list --state open` 결과 0건. `bug`·`small` 라벨 적격 0건. 적격 이슈 없음.
- **처리 결과** — 처리할 버그 없음. worktree 작업·커밋·PR 없음.
- **참고** — 어제(05-25) 사이클도 0건. PR #30(`sessions.last_seen_at` 디바운스)은 여전히 오케스트레이터 사이클의 머지 승인 대기 — dev-cycle 처리 대상 아님.
- **Telegram 보고** — reply 도구 미노출. `~/.claude/channels/telegram/.env` + `ops/ops.env`로 curl 직접 발송.

## 12:00 — 오케스트레이터 (launchd 12:30 사이클, 30분 일찍 트리거)

- **healthcheck** — prod backend `{"status":"ok","db":true,"katago_alive":true}`, web HTTP 200, launchd `com.baduk.api`(1466)·`com.baduk.web`(1455) PID 정상. 디스크 4%. staging 중단(정책상 정상). `ops/sync-launchd.sh --check` exit 0 → drift 없음.
- **backup-verify** — 최신 daily `baduk-20260526T040005.db.gz` (오늘 04:00, 약 8h). 복원 드릴 `integrity_check=ok` / 테이블 7. daily 7 · weekly 1 · monthly 0.
- **bug-scan** — `~/Library/Logs/baduk-api.err`의 `database is locked` 총 2516건이지만 가장 최근 timestamp는 `2026-05-25 14:06:43 UTC` (PR #30 머지 2026-05-25 14:34 UTC 직전). 머지·재시작 후 신규 발생 없음 → PR #30 디바운스 패치가 실효. 신규 후보 0건.
- **backlog-triage** — `gh issue list --state open` 0건 → 미분류 0건.
- **pr-watch** — `gh pr list --state open` 0건 (어제 #30·#34~#38 일괄 머지 완료). 정체·실패·충돌 0건.
- **보류 승인 큐** — `pending-approvals.md` "대기 중" 항목 0건 (대시보드의 "2건" 표기는 stale — AP-20260525-01·02 모두 어제 21:30~ PR #34·#35로 처리됨, 갱신).
- **최근 CWI ingest** — 2026-05-25 21:28:37 fetched=0 new=0. **최근 주간 리포트** — `2026-W21.md`.
- **Telegram 보고** — reply 도구 미노출, curl 직접 발송 시도.

## 18:00 — 오케스트레이터 (launchd 18:30 사이클, 30분 일찍 트리거)

- **healthcheck** — prod backend `{"status":"ok","db":true,"katago_alive":true}`, web HTTP 200, launchd `com.baduk.api`(1466)·`com.baduk.web`(1455) PID 동일 유지. 디스크 4%. staging 중단(정상). `sync-launchd.sh --check` drift 0.
- **backup-verify** — 최신 daily `baduk-20260526T040005.db.gz` (오늘 04:00, 약 14h, mtime+1 미해당). 복원 드릴 `integrity_check=ok`/테이블 7. daily 7 · weekly 1 · monthly 0.
- **bug-scan** — `baduk-api.err` 마지막 트레이스는 여전히 2026-05-25 14:06 (`sessions UPDATE locked` → close 후 send) — PR #30 머지 직전 잔재. 그 이후 신규 트레이스 0건. 후보 없음 → 이슈 생성 0.
- **backlog-triage** — `gh issue list --state open` 0건 → 미분류 0건.
- **pr-watch** — 열린 PR 0건. 정체·실패·충돌 0.
- **보류 승인 큐** — 0건.
- **최근 CWI ingest** — 2026-05-25 21:28:37 fetched=0 new=0. **최근 주간 리포트** — `2026-W21.md`.
- **Telegram 보고** — reply 도구 세션 미노출(deferred 검색에 채널 reply 도구 없음). 폴백: `~/.claude/channels/telegram/.env` + `ops/ops.env` 로드 후 Bot API `sendMessage` curl 직접 호출 → ok=True, message_id=2007.
