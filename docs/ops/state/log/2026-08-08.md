# 2026-08-08

- 02:00 content-draft: **처리할 토픽 없음 — 글로서리·FAQ 시드가 모두 게시 완료. 초안·게시·커밋 0건.**
  - **미작성 후보 0건 (양쪽 다).** FAQ 시드 9개 슬러그 전부 `web/content/faq/`에 존재(9/9 = 100%). 글로서리 시드 18개 중 17개가 `web/content/glossary/`에 존재하고, 유일한 slug 미매칭인 `kasaeng`은 **이미 `sahwal.md`로 게시돼 있다** — `grep -l '^title: 사활' web/content/glossary/*.md` = `sahwal.md` 단건으로 실측 확인. 사활 페이지는 slug `sahwal`로 통합됐고 옛 `kasaeng`은 308 리다이렉트다(`[[glossary-sahwal-slug]]`). 즉 실질 커버리지도 18/18이다.
  - **slug 비교만으로 판정했다면 `kasaeng` 중복 작성이 나올 뻔했다.** 시드 파일 `docs/ops/content/seed-glossary.yml`이 통합 이전 slug(`kasaeng`)를 그대로 들고 있어 기계적 집합 차집합에서는 후보로 잡힌다. 콘텐츠 gap 판정은 slug가 아니라 **frontmatter title** 기준이어야 한다는 기록이 그대로 재현된 사례다. `drafts/`는 비어 있어 진행 중 초안도 없다.
  - **비율 균형 계산 — 종료 조건 우선.** glossary 18/18(1.00) · faq 9/9(1.00)로 most-behind 규칙상 동률이면 glossary지만, 고른 종류의 미작성 후보가 0개이고 다른 종류도 0개이므로 선택 단계에서 종료했다. **기존 항목 개정은 하지 않았다** — 사이클 규칙상 임의 개정 금지.
  - **초안 생성·QA·게시·커밋·push·IndexNow 전부 미실행.** 작업트리는 건드리지 않았고 `web/content/` 변경 0건. 게시가 없으므로 Telegram 사후 보고도 발송하지 않았다(승인 요청 성격이 아니라 게시 완료 보고이므로 발송할 내용 자체가 없음).
  - **관찰 — 이 잡은 시드 고갈 상태다.** 시드는 사람이 항목을 추가하는 파일(`seed-*.yml` 헤더 명시)이라 에이전트가 토픽을 자가 증식시키지 않는다. 새 항목이 시드에 추가되기 전까지 이후 사이클도 같은 결론(무작업)을 반복한다. 시드 확장 또는 `kasaeng`→`sahwal` slug 정정은 사람 판단 영역이라 여기서 파일을 수정하지 않았다.

- 02:00 dev-cycle: **[#78](https://github.com/rarebirds-svg/quite-baduk/issues/78)(어드민 401 무한 폴링) 처리 — [PR #79](https://github.com/rarebirds-svg/quite-baduk/pull/79) 생성. 8/5 이후 사흘 만의 실작업 사이클이다.**
  - **적격 이슈 1건, 그대로 선택.** `gh issue list --state open` = #78 단건(`bug`·`prio:low`), `feature`·`in-progress` 없음. 우선순위 비교 대상이 없어 선택은 자명했다. `in-progress` 선점 → 처리 → 완료 후 제거까지 정상 순환했다.
  - **작업 환경.** `.worktrees/dev-cycle`을 `origin/main`(a59b260) 기준 `fix/issue-78`로 잡았다. 워크트리에 `node_modules`가 없어 메인 트리 것을 심볼릭 링크했다(`web/package.json`이 `origin/main`과 동일해 lockfile 드리프트 없음, `[[worktree-shared-venv]]`의 웹 판본). **`.gitignore`의 `node_modules/`는 후행 슬래시라 심볼릭 링크를 안 걸러낸다** — `git status`에 `?? web/node_modules`로 떴고, 커밋 시 경로를 명시 스테이징하고 작업 후 링크를 제거해 유입을 막았다. 다음 사이클도 같은 함정을 만난다.
  - **TDD.** `web/tests/admin-auth-expiry.test.tsx` 신규 — 폴링 3화면(대시보드·로그인이력·통계) × 401 만료를 `describe.each`로, 대시보드 403을 별도로 검증한다. 각 케이스가 안내 문구 노출과 **해당 화면 `REFRESH_SEC` 3주기 동안 API 호출 증가 0건**을 함께 확인한다. 수정 전 4개 전부 실패 → 수정 후 통과.
  - **🔎 이슈에 없던 사실 — 403도 같은 무한 폴링 버그였다.** 이슈는 401만 지적했으나, 403은 안내 배너가 early return으로 뜨는데도 `useEffect`가 살아 있어 폴링이 계속 돌았다. 403 테스트가 15초 구간 **8건 → 20건** 증가로 재현했다. 같은 `clearInterval` 수정으로 함께 해결된다 — 401만 고쳤다면 절반만 고친 셈이 됐다.
  - **수정 범위 — 6파일.** `lib/api.ts`에 `authFailure()` 추가(401→`expired`, 403→`forbidden`, 그 외 `null`)로 분류를 한 곳에 모았다. **일시적 5xx를 만료로 오인하지 않는 것이 이 헬퍼의 핵심**이다. 폴링 4화면(대시보드·세션상세·로그인이력·통계)은 인증 실패 시 `stopped` 플래그 + `clearInterval`로 루프를 끊고, `AdminAuthNotice` 컴포넌트가 만료(재로그인 링크 포함)·권한없음 안내를 공통 렌더한다. `analytics`는 폴링이 없어 분류만 맞췄고, **이미 올바른 `pro-games`는 손대지 않았다**(규칙 3). i18n `admin.sessionExpired`·`admin.relogin`을 ko/en 동시 추가.
  - **검증 — 3종 전부 클린.** `npm test -- --run` **30 files / 185 tests 통과**, `npm run type-check` 무출력, `npm run lint` `No ESLint warnings or errors`. 대시보드에서 제거한 렌더 가드의 죽은 분기(`isAdmin === false && sessions === null`)로 고아가 된 `isAdmin` 구조분해도 함께 정리했다(내 변경이 만든 고아만).
  - **커밋·push·PR 완료 — 러닝북 3단계 자율 경로 그대로.** 커밋 `c130961`, 브랜치 `fix/issue-78` origin push 완료, PR [#79](https://github.com/rarebirds-svg/quite-baduk/pull/79) 생성. 이슈에 브랜치·SHA·PR 링크를 코멘트로 남기고 `in-progress`를 제거했다. **로컬에만 두는 실패 모드(`[[dev-cycle-unpushed-branches]]`)를 밟지 않았다 — origin에 실존함을 push 출력으로 확인했다.**
  - **머지 없음(🟡 — 사람).** `main` 코드 변경은 승인 경로다. 자율 범위는 PR 생성까지다.
  - **🔴 CI 1차 RED — `frontend` 실패. 원인은 구현이 아니라 내가 쓴 테스트의 목이었다.** 로컬 185건 전부 통과했는데 CI에서만 대시보드 401 케이스가 `expected 8 to be 4`로 터졌다. 로그를 실제로 읽고 파고들었다(`[[ci-ruff-check-all]]`와 같은 계열의 로컬-통과·CI-실패 함정).
    - **기전** — 목의 `useAuthStore`가 **매 렌더 새 `session` 객체 리터럴**을 돌려줬다. 대시보드 폴링 effect의 deps에 `session`이 있어 재렌더마다 effect가 재실행되고, 새 인스턴스는 `stopped=false`로 시작해 루프가 되살아난다. 추가 호출 4건이 `waitFor` 캡처 **전**에 들어오면 통과(로컬 8→8), **후**면 실패(CI 4→8)라 타이밍 레이스였다.
    - **프로덕션 영향 없음** — 실제 zustand는 `set()` 뒤에도 `session` **프로퍼티 참조를 유지**한다(`set({isAdmin})`은 상위 상태 객체만 새로 만든다). 목이 실제 동작보다 불안정했던 것이지 구현 결함이 아니다. `SESSION`을 모듈 상수로 고정해 목을 실제 동작에 맞췄다(커밋 `21692ae`).
    - **검증 2종** — ① 수정 후 **15회 연속** 통과로 결정성 확인(1회 통과로 플레이크 해소를 주장하지 않았다). ② `authFailure`의 401 분기를 지운 **음성 대조**에서 401 테스트 3건이 정상 실패하고 403 1건은 통과 — 테스트가 여전히 원 버그를 잡는다는 것을 확인했다.
  - **✅ CI 2차 GREEN — 4잡 전부 통과.** `frontend` 50s · `backend` 1m10s · `app-shell-build` 58s · **`e2e` 2m3s 통과**. e2e는 현재 GREEN 기준선이라 ❌면 이 PR이 깨뜨린 신호로 읽어야 하는데(`[[ci-e2e-disabled]]`), 통과했으므로 회귀 없음이다. 1차에서 e2e는 `skipping`이었어서 이번 실행이 첫 실측이다.
  - **Telegram 발송 완료 — message_id 2524.** `ToolSearch`로 실제 조회했으나 `reply` 도구가 이 세션에 미노출(`SendMessage`는 에이전트 간 통신, `PushNotification`은 데스크톱 알림이라 둘 다 대체재 아님)이라 `~/.claude/channels/telegram/`의 토큰·chat_id로 직접 호출했다(`[[dev-cycle-telegram-botapi]]`). 환경 자가진단으로 사전 판정하지 않고 도구 노출을 실제 확인한 뒤 폴백했다.

- 12:00 orchestrator: **서비스 전 항목 정상. 이번 사이클의 실질 성과는 CI 그린 PR #79의 승인 등재(AP-20260808-01)와 배포 갭 항목의 병합 정리다.**
  - **헬스체크 — 전 항목 OK.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 디스크 **6%**, `ops/sync-launchd.sh --check` exit 0·무출력(drift 없음), staging `중단됨` = 정책상 정상(경보 아님). PID api **93501** / web **93522**로 **12사이클 연속** 동일. `katago_alive:false`는 러닝북 명시대로 **정보용** — lazy spawn이라 마지막 AI 호출(8/6 22:04) 이후 어댑터가 내려간 정상 상태이며 단독으로 incident·재기동 사유가 되지 않는다. watchdog `신규 incident 0 건`·`[api] OK`·`[web] OK`.
  - **백업 검증 — 신선·무결.** 최신 `baduk-20260808T040005.db.gz` (**3,286,600B ≈ 3.14MB**, 8/8 04:00), `find -mtime +1` 무출력 = 30시간 이내. 티어 daily **14** / weekly **8** / monthly **3**. 복원 드릴 `integrity_check=ok` + **9 tables**로 **5회 연속** 통과. 전일 3,284,957B 대비 **+1,643B** 증가는 같은 구간 `visit_hits` +8행과 정합한다.
  - **버그 스캔 — 신규 이슈 0건. 다만 이번엔 "에러가 없어서 0건"이 아니라 "이미 다 잡혀 있어서 0건"이다.**
    - **401 버스트 = 기존 #78, 그리고 이미 멎었다.** 최근 5,000줄 상태 분포는 200 **3,816** / 401 **1,070** / 404 **93** / 204 **19** / 201 **1**로 401이 21.4%다. 그러나 최근 500줄에 **8건**, 최근 200줄에 **0건**이며 마지막 401은 파일 끝에서 **265줄 전**이다. 누적 카운트가 아니라 발생 시점으로 판정한 결과 버스트는 종료 구간이다(`[[bug-scan-verify-recency]]`). 고쳐진 게 아니라 운영자 탭이 닫힌 것이므로 #78은 열린 상태 유지가 맞고, 수정본은 PR #79로 대기 중이다.
    - **🔎 404 93건의 정체 — 신규 크리덴셜 스캐너 스윕. 앱 버그 아님.** 최근 300줄 404의 **82건이 단일 IP `185.177.72.56`**, 나머지도 전부 `185.177.72.0/24` 대역이다. 경로는 `/api/.env`·`*.tfvars`·`credentials.ini`·`v1/secrets`·`namespaces/default/secrets`(k8s)·`phpinfo.php`·`POST /api/v1/auto_login` 등 전형적 시크릿 탐침이다. **전량 차단 확인 — 이 대역의 요청 136건이 전부 404이고 200이 단 1건도 없다.** 노출 없음, 서버는 정상 거절, 이슈화 대상 아님. 종전 사이클들이 "`.env` 404 1건"으로 기록하던 산발 탐침이 이번에 대역 단위 스윕으로 규모가 커진 것이라 기록만 남긴다.
    - **트레이스백 0건 — 기동 마커 기준 실측.** `.err` 총 225,418줄 중 현 기동 마커(`Started server process [93501]`, line **225406**) 이후는 12줄뿐이고 `^Traceback`·`File "` **0건**. `.err` mtime `Aug 7 06:09`은 재기동 INFO 라인 탓이라 최근성 대리지표로 쓸 수 없다. 최근 20,000줄 **5xx 0건**.
  - **백로그 트리아지 — 대상 0건.** `gh issue list --search "no:label"` 빈 결과. 열린 이슈는 #78 1건이고 이미 `bug`·`prio:low` 부여 완료. 사람 분류 필요 0건.
  - **PR 감시 — 열린 PR 1건, 주의 PR 0건.** [#79](https://github.com/rarebirds-svg/quite-baduk/pull/79) `fix(admin): 401(세션 만료)에 폴링을 멈추고 만료를 알림 (#78)` — `mergeable=MERGEABLE`·`mergeStateStatus=CLEAN`·CI **4잡 전부 SUCCESS**(frontend·backend·app-shell-build·e2e). 생성 8/7 17:06으로 19시간 경과라 정체(7일) 아님. **CI 실패·충돌·정체 어디에도 해당 없음 = 러닝북 기준 "정상".**
  - **🟡 신규 승인 등재 AP-20260808-01 — PR #79 머지.** 러닝북상 "정상 PR"이지만 `main` 코드 머지는 자율 범위 밖이라(`autonomy-policy.md`) 승인 큐에 올렸다. 변경은 웹 어드민 10파일(+195/−45)이고 **백엔드·DB·마이그레이션 0건**이다. 선례(#66→AP-20260725-01, #72→AP-20260730-01)와 같은 처리다.
  - **🔴 AP-20260805-01 미처리 72시간 — 판별 검사 3종 일곱 번째 동일.** ① `resultText` 빌드 산출물 **0개 파일** ② `BUILD_ID` mtime `Aug 4 22:23` **불변** ③ 웹 `ps etime` = `05-13:49:38` = **133시간 50분 무재기동**. `git rev-list --count HEAD..origin/main` **0**이라 이 지표만으로는 갭이 안 보인다는 판정도 일곱 번째 재확인(`[[merge-is-not-deploy]]`). 중복 제안은 만들지 않고 각주만 덧붙였다.
    - **🆕 두 승인 항목을 절차적으로 묶었다.** #79를 머지하면 origin이 앞서므로 AP-20260805-01의 재빌드 전에 **`git pull --ff-only`가 반드시 선행**돼야 한다 — 빠뜨리면 #79 없는 트리에서 빌드가 돈다. AP-20260808-01의 실행 절차에 이 단계를 명시했고, 양쪽 항목에 상호 참조를 남겼다. **한 번의 재기동으로 #75·#77·#79 세 건이 함께 발효**한다.
    - **재기동 위험은 계속 낮다** — DB `max(id)` **337 불변**, 마지막 수 `2026-08-06 13:04:14 UTC` = **약 45시간 전**. 중단될 진행 중 대국 없음(`[[in-progress-game-detection]]`).
  - **잡 스케줄 — stale 아님.** content-ingest 최근 실행 **2026-08-02 03:12**(fetched=2260 new=200 duplicate=2060 error=0), analytics-weekly 최근 **2026-08-02 09:00**(리포트 `2026-W31.md`). 둘 다 plist `Weekday 0` = **주 1회 일요일** 잡이라 평일에 6일 전인 것은 정상이며 다음 실행은 **8/9(일)** 다. W32 리포트도 그때 생성된다.
  - **06:30 사이클 부재는 유실이 아니다** — plist는 12:00·18:00 2회뿐이고 프롬프트·러닝북의 4회 표기가 미정합인 기존 항목이다(`[[orchestrator-schedule-2x-not-4x]]`). incident로 올리지 않았다.
  - **Telegram 발송 완료 — message_id 2526.** `ToolSearch`로 실제 조회해 `reply` 도구 미노출을 확인한 뒤(`SendMessage`는 에이전트 간 통신, `PushNotification`은 데스크톱 알림이라 둘 다 대체재 아님) Bot API로 직접 발송했다(`[[dev-cycle-telegram-botapi]]`). 환경 변수·플래그 자가진단으로 사전 판정하지 않았다.
    - **⚠️ 기록된 함정을 반복했다** — 1차 시도에서 `-F "text@파일"`을 써서 `curl: option -F: is badly used here`로 실패했다. 메모리에 7/29자로 이미 적혀 있는 오류인데(`@`는 `--data-urlencode` 전용, `-F`에서는 `text=<파일`) 축약 요약만 기억한 결과다. **2차는 curl을 버리고 python stdlib JSON POST로 발송해 첫 시도에 성공했다** — 로케일 함정과 curl 폼 문법 함정을 동시에 우회하는 경로라 메모리에 권장 경로로 승격해 적었다. 1차 실패는 curl 인자 파싱 단계에서 죽어 요청이 나가지 않았으므로 중복 발송은 없다.
  - **`incidents.md`는 건드리지 않았다** — prod 이상 0건이라 기록할 항목이 없다. `dashboard.md`·`pending-approvals.md`·`log/2026-08-08.md` 3개 파일을 갱신했다.

- 15:47~15:49 사람 승인 배포: **AP-20260808-01 + AP-20260805-01 한 묶음 실행 완료 — #75·#77·#79가 한 번의 재기동으로 함께 발효했다.** AP-20260805-01은 제안 후 75시간·재확인 7회 만의 해소다.
  - **사람이 "정리하자"로 전체 묶음 실행을 승인했다.** 실행 전에 오케스트레이터 리포트를 그대로 믿지 않고 판별 검사를 직접 재실측했다 — `resultText` 빌드 산출물 **0개 파일**, `BUILD_ID` mtime `Aug 4 22:23`, 웹 uptime 5일 17시간, PR #79 `MERGEABLE`/`CLEAN`·CI 4잡 SUCCESS, 마지막 수 `2026-08-06 13:04:14 UTC`(약 45시간 전). 리포트와 전부 일치했다.
  - **순서가 핵심이었다 — 머지 → pull → 빌드 → 재기동.** `gh pr merge 79 --squash --delete-branch`로 `eb12635` 머지 후 `git pull --ff-only`(`a59b260..eb12635`, 10파일 +195/−45)를 **재빌드 앞에** 넣었다. 이 단계를 빠뜨리면 재빌드가 #79 없는 트리에서 돌았다. pull 전 `gh pr view 79 --json files`로 유입 파일이 `web/`뿐임을, 로컬 미커밋은 `e2e/`·`docs/ops/`뿐임을 대조해 교집합 공집합을 확인했다.
  - **로컬 브랜치 삭제만 실패했고 이는 정상 처리다** — `.worktrees/dev-cycle`가 `fix/issue-78`을 점유 중이어서 `failed to delete local branch`가 났다. 원격 브랜치는 삭제됐고 머지는 `state=MERGED`·`mergedAt 2026-08-08T06:47:38Z`로 확정이다. dev-cycle 워크트리는 다른 잡의 작업 공간이라 의도적으로 건드리지 않았다.
  - **판별 검사 3종 전부 통과.** `resultText` **0→17개 파일**(#77 발효), `authFailure|sessionExpired` **13개 파일**(#79 발효), `BUILD_ID` mtime `Aug 8 15:48`. PID api **93501→88640** / web **93522→88644**, `lstart` `2026-08-08 15:48:51` — **133시간 50분 무재기동 종료.** 헬스 backend `{"status":"ok","db":true,"katago_alive":true}` 200 · web 200, 라이브 스모크 6경로 전부 200.
  - **⚠️ #75 판별 절차에 결함이 있었다 — 승인 문서의 검사법으로는 판정이 불가능했다.** 종전 절차는 "`baduk-web.err` 새 기동 블록에 standalone 경고가 없으면 발효"였는데, **Next.js 기동 배너는 stdout(`baduk-web.log`)으로만 나가서 `.err`에는 기동 블록 자체가 없다.** `.err`에서 경고를 grep하면 마지막 발생이 line 2929로 잡히지만 그게 현 기동인지 이전 기동인지 `.err` 단독으로는 구분이 안 된다. **`baduk-web.log`의 새 기동 배너(`8/8 15:48`, `▲ Next.js 14.2.35` / `✓ Ready in 137ms`)에 경고가 없음** + `web/.next/standalone` 미생성 + `next.config.js`에 `output` 미지정의 조합으로 판정했다.
    - **중간 판정을 한 번 정정했다.** 처음엔 "`.err` mtime이 재기동 이전(`06:25`)이라 새 프로세스가 stderr에 아무것도 안 썼다"를 근거로 삼았는데, 몇 분 뒤 mtime이 **`15:50`으로 갱신**됐다. 확인해 보니 새 프로세스(PID **88656**)가 `--localstorage-file was provided without a valid path` Node 경고 2줄을 쓴 것이었다 — line 11부터 **39회** 반복되는 상시 기동 경고이며 standalone과 무관하다. **mtime은 웹 재기동 판정의 근거로 쓸 수 없다.** 올바른 절차는 신규 라인의 **PID와 내용을 직접 읽는** 것이다. standalone 경고 마지막 발생은 여전히 line **2929**로 재기동 이전이다. `[[api-log-stdout-vs-stderr]]`의 웹 판본이며 메모리에 이 정정까지 반영했다.
  - **부수 해소 확인.** 재기동 전 `.err` 말미(라인 3097~3119)에 쌓여 있던 `Failed to find Server Action. This request might be from an older or newer deployment.` 다발은 8/2 기동 프로세스가 8/4에 갈린 `.next`를 물고 있던 메모리·디스크 불일치의 증상이다. 전부 재기동 **이전** 구간이고 새 프로세스에서는 재발하지 않았다.
  - **파일 갱신 — `pending-approvals.md`(대기 중 = 없음, 완료 요약 등재) · `log/2026-08-08-approvals-archive.md`(AP 원문 2건 아카이브) · `dashboard.md`(prod 행 PID·배포 기록) · 이 파일.** 큐 파일 자체 규칙("처리 완료 항목은 큐에서 제거하고 `state/log/`로 옮긴다")대로 재확인 각주 7회분 원문을 아카이브로 분리해 큐를 8,861자 줄였다. `incidents.md`는 건드리지 않았다 — 배포는 정상 완료였고 incident가 아니다.
