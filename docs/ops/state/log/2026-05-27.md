# 운영 로그 2026-05-27

## 02:00 — 자율 버그 사이클 (launchd 트리거)

- **이슈 후보 조회** — `gh issue list --state open` 결과 0건. `bug`·`small` 적격 0건.
- **처리 결과** — 처리할 버그 없음. worktree 작업·커밋·PR 없음.
- **참고** — 최근 닫힌 이슈는 #12(2026-05-25 머지), #13(2026-05-25), #15(2026-05-23). 신규 유입 없음.
- **Telegram 보고** — `reply` 도구 노출 시 호출, 미노출이면 `~/.claude/channels/telegram/.env` + `ops/ops.env`로 curl 직접 발송.

## 12:00 — 오케스트레이터 (launchd 12:30 사이클, 30분 일찍 트리거)

- **healthcheck** — prod backend `{"status":"ok","db":true,"katago_alive":false}` (lazy spawn 미발생, 정보용 — 경보 아님), web HTTP 200, launchd `com.baduk.api`(1466)·`com.baduk.web`(1455) PID 유지. 디스크 4%. staging 중단(정책상 정상). `ops/sync-launchd.sh --check` exit 0 → drift 없음.
- **backup-verify** — 최신 daily `baduk-20260527T040005.db.gz` (오늘 04:00, 약 8h). 복원 드릴 `integrity_check=ok` / 테이블 7. daily 8 · weekly 1 · monthly 0.
- **bug-scan** — `~/Library/Logs/baduk-api.err` 마지막 트레이스 2026-05-25 14:06 (PR #30 머지 직전 `sessions UPDATE locked`) — 그 이후 신규 발생 0. 후보 0건 → 이슈 생성 0.
- **backlog-triage** — `gh issue list --state open` 0건 → 미분류 0건.
- **pr-watch** — 열린 PR 0건. 정체·실패·충돌 0.
- **보류 승인 큐** — `pending-approvals.md` 0건.
- **최근 CWI ingest** — 2026-05-25 21:28:37 fetched=0 new=0 (plist Weekday=0/Sunday 03:00 잡 → 다음 자동 트리거 2026-05-31 03:00). **최근 주간 리포트** — `2026-W21.md`.
- **신규 발견 — watchdog 임계 정합성 이슈** — `ops/check-staleness.sh` 임계가 content-draft·content-ingest 모두 108000s(30h)인데, plist는 각각 `Weekday=6` (토 02:00)·`Weekday=0` (일 03:00) 주 1회 잡. 매주 가동 후 1.5일 지나면 stale로 잡혀 다음 주 가동까지 ~5일간 매시간 incident 누적 (5/27 03:35~11:35 동안 18건 — `WD-20260527-*`). 정합 임계: 9d (777600s) 또는 analytics와 동일 8d (691200s). 임계 수정은 코드 변경 영역이라 별도 dev-cycle/PR 권장 — 이번 사이클에선 보고만.
- **Telegram 보고** — reply 도구 deferred 목록에 부재 → fallback: `~/.claude/channels/telegram/.env` + `ops/ops.env` 로드 후 Bot API `sendMessage` curl 직접 호출.

## 18:00 — 오케스트레이터 (launchd 18:30 사이클, 30분 일찍 트리거)

- **healthcheck** — prod backend `{"status":"ok","db":true,"katago_alive":false}` (lazy spawn, 정보용·경보 아님), web HTTP 200, launchd `com.baduk.api`(1466)·`com.baduk.web`(1455) PID 유지(12:00 사이클부터 동일). 디스크 4%. staging 중단(정상). `ops/sync-launchd.sh --check` exit 0 → drift 없음.
- **backup-verify** — 최신 daily `baduk-20260527T040005.db.gz` (오늘 04:00, 약 14h). 복원 드릴 `integrity_check=ok` / 테이블 7. daily 8 · weekly 1 · monthly 0. 12:00 이후 변동 없음.
- **bug-scan** — `~/Library/Logs/baduk-api.err` mtime 2026-05-26 22:58:29 (오늘 신규 갱신 없음). 누적 WS RuntimeError(`Cannot call "send" once a close message has been sent` @ `ws.py:241`) 18건은 5/25 PR #30 머지 이전·이후 합산이지만 최근 발생 윈도우는 5/26 22:58 이전, 그 후 신규 0. 이슈 생성 0.
- **backlog-triage** — `gh issue list --state open --search "no:label"` → 0건.
- **pr-watch** — `gh pr list --state open` → 0건.
- **보류 승인 큐** — `pending-approvals.md` 0건 (12:00 이후 변동 없음).
- **최근 CWI ingest** — 2026-05-25 21:28:37 fetched=0 new=0 (이전과 동일). **최근 주간 리포트** — `2026-W21.md`.
- **watchdog content-* incident 누적** — 12:00 이후 6시간 동안 12건 추가 (`WD-20260527-12*~17*`, content-draft·content-ingest 각 6건씩). 임계 정합성 misalignment 패턴 그대로 — 임계 수정 dev-cycle 권장은 12:00 사이클 결론 유지.
- **Telegram 보고** — reply 도구 deferred 부재 → fallback curl `sendMessage` 발송 성공 (`ok=true`, `message_id=2042`).

## 02:00 — 콘텐츠 초안 사이클 (launchd 토요일 02:00 트리거)

- **시드 스캔** — `seed-glossary.yml` 미작성 6건(`gyega`·`handicap`·`hwajeom`·`kasaeng`·`komi`·`pae`·`samsam`), `seed-faq.yml` 미작성 4건. 알파벳 순 첫 항목 `gyega` 선택.
- **초안 생성** — `docs/ops/content/drafts/gyega.md` (글로서리, ~530자, 3 단락) 작성.
- **korean-copy-qa** — 표현 4건·사실 오류 1건(일본 룰 차이 서술) 지적 → 본문 수정 반영. 게시 가능 수준.
- **AP 제안** — `AP-20260527-01` 추가 (pending-approvals.md), Telegram 발송.
- **AP-20260527-01 사람 승인 → 게시** — `drafts/gyega.md` → `web/content/glossary/gyega.md` 이동, commit 1건. pending-approvals 큐에서 제거.
