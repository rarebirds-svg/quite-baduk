# 2026-09-07

- 04:30 dev-cycle — **#87 처리 → PR [#95](https://github.com/rarebirds-svg/quite-baduk/pull/95)** (`fix/issue-87` 145afdd + 26ddff7). 선택 근거: 열린 bug 3건 중 #81(PR #83)·#84(PR #86)는 머지 대기라 재선택 안 함. 원인은 이슈 실측대로 `ingest_cwi_weekly.py` 루프 안 `db.add` 뒤 중복 검사 select의 autoflush INSERT가 첫 건부터 쓰기 락을 잡은 채 HTTP fetch를 약 60초 계속한 것. TDD — 파일 DB에서 SGF 요청 시점마다 별도 sqlite3 커넥션이 INSERT를 시도하는 회귀 테스트로 수정 전 g2·g3 `database is locked` 재현(red) → 루프에서는 fetch·파싱·중복 검사만 하고 `pending`에 모은 뒤 루프 뒤 `add_all`+`commit` 1회로 INSERT를 몰아 green. 같은 실행 내 중복은 `seen_hashes`로(content_hash unique라 필수). 598 passed·81.51%·ruff·mypy 통과. 리뷰 — Fable 승인(red/green·pysqlite 트랜잭션 규칙 실측, 15회 플레이크 0), **Codex 쿼터 소진(10/2 04:35 리셋)으로 건너뛰고 Opus 4.8 폴백** 승인. 교집합 1건(engine.dispose finally)+Opus 단독 테스트 위생 2건(env 격리·영속화 count)을 26ddff7로 반영, Opus 정보 항목 2건(SELECT~commit 창 확대·seed_pro_games 동일 패턴)은 PR 코멘트에 사람 판단용으로 기록. 이슈 코멘트·`in-progress` 제거 완료. 머지는 🟡(사람) — 09:00 오케스트레이터가 승인 항목 등재 예정. 배포는 prod 트리 pull만으로 다음 일요일 03:00 ingest부터 적용, 재기동 불필요. 잡 상태 `ok`.

- 09:00 오케스트레이터 (am) — **prod 반쪽 장애 감지 → 에스컬레이션 (OPS-20260907-01)**.
  - 헬스체크: backend `/api/health` 200(`db:true`, `katago_alive:false` 정보용)·web 200, `com.baduk.api` PID 28544(00:06:42 기동, 직전 종료 -15)·`com.baduk.web` PID 28792(00:07:04). staging 중단(정책상 정상). 디스크 7%. plist drift 없음(exit 0). **Claude 인증 D-25**(만료 10-02 23:41). 헬스체크 항목 자체는 전부 통과 — 그러나 아래 bug-scan이 실장애를 잡았다.
  - 백업 검증: 최신 `baduk-20260907T040004.db.gz`(4,365,754B, +47,514B) 신선(`-mtime +1` 출력 없음), daily 14 / weekly 8 / monthly 4, 복원 드릴 integrity_check **ok**·테이블 10 = **63회 연속** 통과.
  - 버그 스캔: `.err` 228,657줄(**+2,104**). **신규 트레이스백 12건 — `sqlalchemy.exc.OperationalError: no such column: games.account_id` 10건 + `sessions.account_id` 2건.** `.log` 283,194줄(+950), 최근 3천줄 **5xx 12건**(200 2759 / 204 90 / 401 64 / 201 51 / 404 15 / 500 12 / 422 1) — `GET /api/spectate` 10·`GET /api/session` 2, 실사용자 IP 2개(1.237.136.170 11건·175.116.159.236 1건, 127.0.0.1 1건은 이번 프로브). 직접 프로브 `GET /api/spectate` → **500 재현**, `/api/auth/providers` 200 `{"google":false}`, `/api/session`(쿠키 없음) 401. **원인 실측** — reflog `3bd9467 pull: Fast-forward 00:06:41` = 사람이 9/6 밤 머지된 #89~#93을 pull·웹 재빌드(BUILD_ID 00:07)·재기동했으나 `alembic upgrade head` 누락. `alembic_version`=`0019`, #93이 추가한 `0020_accounts.py`(accounts 테이블 + sessions/games.account_id) 미적용. 열린 bug 이슈(#81·#84·#87)와 무관한 배포 절차 결손이라 **이슈 대신 incident**로 처리(코드 버그 아님). `database is locked`·IntegrityError(#81) 0건. **web `.err`** 3383줄(+2, node 경고뿐) / `Failed to find Server Action` 89건(+0). 신규 이슈 0건.
  - 활동: 9/6 밤 신규 대국 9건(#445~453, 21:01~23:20 KST, 13로 6·9로 3), 종료 4건(기권), active 잔류 6건(#445·448·450~453). 마지막 수 **9/6 23:18 KST** — **00:06 배포 이후 착수 0건·WS 접속 0건**(직전 12시간 679수와 대비, 장애 정합). 12h 324수·24h 1,003수. `visit_hits` **461**(+26/12h). pro_games 4111(변동 없음).
  - 장애 대응(incident.md): 화이트리스트 해당 없음(DB 마이그레이션 🟡, 재기동만으론 복구 불가) → 4번 에스컬레이션. **`kind: alert` 즉시 경보 발송 성공(exit 0, once_key `prod_migration_0020`)**. `state/incidents.md`에 OPS-20260907-01 등재. **prod DB 사본 드라이런** — `cp data/baduk.db /tmp` 후 `DB_PATH` 지정 `alembic upgrade head`: `0019 → 0020` **0.44초**, integrity_check ok, foreign_key_check 위반 0, games/sessions `account_id` 컬럼 확인, prod DB는 `0019`·453건 그대로 무변경(사본은 WAL 미복사로 452건 — 드릴 용도엔 무관). 사본 삭제 완료.
  - 승인 큐: **AP-20260907-01(긴급)** — `alembic upgrade head` + `com.baduk.api` 재기동, 절차 5단계 등재. **AP-20260907-02** — PR #95(#87 픽스) 머지, 재기동 불필요·9/13 ingest부터 발효. AP-20260903-01 96시간째·7회, AP-20260905-01 48시간째·4회 재확인 — 둘 다 CI 4잡 SUCCESS 유지, 장애 복구 재기동과 묶어 1회 처리 권장. **대기 4건.**
  - 백로그 트리아지: 미분류 0건. 열린 이슈 3건(#87 → PR #95, #84 → PR #86, #81 → PR #83) 전부 PR 대기.
  - PR 감시: 열린 PR 3건(#95·#86·#83) 전부 CI 4잡 SUCCESS, 충돌 0(`merge-tree` #95 vs #83·#86 실측), 7일 정체 0건(#83은 9/10 04:46 전환 예정) — **주의 PR 0건**. main CI 최근 3런 success(#92~#94).
  - 배포 갭: `HEAD..origin/main` **1커밋**(12b47af #94, `web/tests/top-nav.test.tsx` 22줄 테스트 파일뿐 — 런타임 무영향). 라이브는 00:06 배포로 #93까지 반영됐으나 **마이그레이션 미적용으로 반쪽 상태**(위). 이번 사이클 ops 문서 push를 위해 `git pull --ff-only`로 #94를 받았다(테스트 파일 단독이라 🟢 판단, 재빌드·재기동 없음).
  - 잡 상태: dev-cycle ok(04:43, #87 → PR #95) · content-draft ok(9/5 02:04) · analytics-weekly ok(9/6 09:01) · news-hook ok(9/6 05:03) = **4/4 ok**.
  - 다이제스트: `slot: am`, service `fail`, 승인 카드 4건 동봉. 결과는 아래 발송 기록 참조.
  - 발송 기록: 09:00 `kind: alert`(once_key `prod_migration_0020`) exit 0 → 09:06 am 다이제스트 + 승인 카드 4건 exit 0, 마커 `~/.ops-report/markers/inkbaduk-am-2026-09-07` 생성 확인. 기록은 대시보드·incidents·pending-approvals 갱신 후 main에 커밋·push.

- 21:00 오케스트레이터 (pm) — **OPS-20260907-01 지속 21시간, 승인 미회신 12시간.** 화이트리스트 해당 없음(변동 없음), 루프 가드상 추가 자동 조치 없음.
  - 헬스체크: backend `/api/health` 200(`db:true`, `katago_alive:false` 정보용)·web 200, `com.baduk.api` PID 28544·`com.baduk.web` PID 28792(00:06 기동 그대로, 재기동 없음). staging 중단(정책상 정상). 디스크 7%. plist drift 없음(exit 0). Claude 인증 **D-25**(만료 10-02 23:41). **`alembic_version` `0019` vs head `0020_accounts` — 갭 유지.** 직접 프로브 `GET /api/spectate` **500 재현**.
  - 백업 검증: 최신 `baduk-20260907T040004.db.gz`(4,365,754B) 신선, daily 14 / weekly 8 / monthly 4, 복원 드릴 integrity_check ok·테이블 10 = **64회 연속**.
  - 버그 스캔: `.err` 230,975줄(**+2,318**), 신규 트레이스백 **14건** — `sessions.account_id` SELECT 10 + **`table sessions has no column named account_id` INSERT 2** + `games.account_id` 2. `.log` 283,584줄(+390) 상태 분포 200 329 / 404 21 / **500 14** / 204 8 / 401 6. 500 내역 — `GET /api/session` 10(218.37.101.154 4·59.16.96.17 3·166.104.68.53 3), **`POST /api/session` 2**(166.104.68.53), `GET /api/spectate` 2(127.0.0.1 = 이번 프로브). **영향 정정** — 세션 신규 생성(`POST /api/session`)도 500이므로 세션 없는 신규 방문자도 대국 착수 경로에 못 들어간다. 오전의 "신규 방문 정상"은 세션 발급 전 정적 화면까지만 참. 404 21건은 `/api/.env`·`/api/graphql` 류 스캐너. `database is locked`·IntegrityError 0. web `.err` 3383(+0)·Server Action 89(+0). 원인이 열린 bug 이슈·코드 버그가 아니라 배포 절차 결손이므로 신규 이슈 0건(오전 판정 유지).
  - 활동: games max(id) 453(변동 없음), active 잔류 257(status 기본값, 판정 불용), **12h 착수 0수**, 마지막 수 9/6 23:18 KST, 09:00 이후 `POST /api/games` 0·`/moves` 0. `visit_hits` **469**(+8/12h). pro_games 4111.
  - 장애 대응(incident.md): 4번 에스컬레이션 유지. `kind: alert` 재발송은 once_key `prod_migration_0020` 일 1회 억제 대상(마커 `/tmp/ops-report-once-inkbaduk-prod_migration_0020-2026-09-07` 09:03 실존) — pm 다이제스트 service ❌ + 승인 카드 4건으로 재통보. incidents.md에 21:00 재확인 줄 추가.
  - 승인 큐: **대기 4건, 전부 미회신.** AP-20260907-01 12시간째·1회(긴급), AP-20260907-02 12시간째·1회, AP-20260903-01 108시간째·8회, AP-20260905-01 60시간째·5회. 셋 다 CI 4잡 SUCCESS 유지·차단 없음. 처리 순서 권장 유지(마이그레이션 → #83 → #86 → #95 머지·pull → api 재기동 1회).
  - 백로그 트리아지: 미분류 0건. 열린 이슈 3건(#87·#84·#81) 전부 PR 대기, 변동 없음.
  - PR 감시: 열린 PR 3건(#95·#86·#83) CI 4잡 SUCCESS·충돌 0·7일 정체 0 — **주의 PR 0건**. **main CI 최신 런 `16d9e0f`(09:00 ops 문서 커밋) failure** — e2e `tests/review.spec.ts` "review page loads and analyze button works" `locator.click` 120s 타임아웃 1건, backend·frontend·app-shell-build SUCCESS. 문서 파일만 바뀐 커밋이라 코드 회귀가 아닌 **e2e 플레이크**로 판정, 이슈화하지 않고 다음 main 런(이번 사이클 push)에서 재확인. 2회 연속이면 플레이크 이슈 후보.
  - 배포 갭: `HEAD..origin/main` **0**. 라이브는 #94까지 코드 반영 상태이나 **마이그레이션 미적용으로 반쪽**(위) → deploy `warn`.
  - 잡 상태: dev-cycle ok(04:43) · content-draft ok(9/5 02:04) · analytics-weekly ok(9/6 09:01) · news-hook ok(9/6 05:03) = **4/4 ok**. watchdog 6회 전부 "신규 incident 0 건"·api/web OK(헬스 200이라 사각지대, 예상대로).
  - 다이제스트: `slot: pm`, service `fail`, 승인 카드 4건 동봉. 발송 결과는 아래 발송 기록 참조.
  - 발송 기록: 21:04 `kind: alert` 재시도 → once_key 억제로 발송 생략(exit 0, 설계대로). 21:04 pm 다이제스트 + 승인 카드 4건 exit 0, 마커 `~/.ops-report/markers/inkbaduk-pm-2026-09-07` 생성 확인. 기록은 대시보드·incidents·pending-approvals 갱신 후 main에 커밋·push.
