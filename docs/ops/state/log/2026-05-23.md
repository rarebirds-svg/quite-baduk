# 운영 로그 2026-05-23

## 00:59 — 운영팀 SRE(sub-project 1) 구축 완료
- 검증 기준 4/4 통과: ① 백업+launchd ② 백업검증 러닝북 ③ 배포 경로 ④ 장애 대응

## 02:00 — 자율 버그 사이클 (dev-cycle)
- 처리할 버그 없음 — 열린 이슈 0건 (`gh issue list --state open` 결과 비어 있음).
- 적격 이슈(`bug`/`small` 라벨, `feature`/`in-progress` 제외)가 없어 무처리 종료.

## 02:14 — 자율 버그 사이클 (dev-cycle) 재실행

- **이슈 #12** 선택 (`bug`+`small`, `en.json` `admin.filterNicknamePh` 소문자 placeholder).
- `.worktrees/dev-cycle`에 `fix/issue-12` 브랜치 생성, `"search..."` → `"Search..."` 수정.
- 검증 — JSON 유효, 프론트 테스트 60/61 통과. 실패 1건(`soundfx.test.ts`)은
  본 변경과 무관한 기존 결함, `origin/main` 클린 상태에서도 동일 실패 확인.
- 커밋 `54190bf` 완료. **에스컬레이션** — `.claude/settings.json` deny 목록의
  `Bash(git push*)` 때문에 브랜치 푸시 불가 → PR 생성 실패.
- `autonomy-policy.md`는 브랜치 푸시를 🟢로 규정하나 harness 권한 설정이 차단 —
  정책·설정 불일치. 이슈 #12에서 `in-progress` 제거 후 막힌 지점 코멘트 남김.
- 사람 조치 필요 — 브랜치 수동 푸시 후 PR 생성, 또는 `settings.json`의 `git push`
  deny 조정.

## 12:00 — 운영 오케스트레이터 (12시 실행)

- **healthcheck** — prod-backend OK (`db:true, katago_alive:true`), prod-web OK,
  `com.baduk.api` PID 79458 / `com.baduk.web` PID 79462 가동, 디스크 5%. staging은
  backend 가동(pid 80510), web 중단됨 (정책상 정상 기록).
- **backup-verify** — 최신 `baduk-20260523T040005.db.gz`(daily 3개, weekly·monthly 0),
  복원 드릴 `integrity_check: ok` + 테이블 7개 — 정상.
- **bug-scan** — `~/Library/Logs/baduk-api.err`에 `sqlite3.OperationalError: database
  is locked` 2066회 누적, `sessions` INSERT/UPDATE에서 `app/api/session.py:92`
  반복. 이슈 #13 생성 (`bug` 라벨, 자동 탐지). PR #6 `fix(db): busy_timeout=5000`을
  교차 참조로 코멘트 추가.
- **backlog-triage** — 미분류 이슈 0건.
- **pr-watch** — 열린 PR 7건, 모두 주의.
  - CI 실패: #11(frontend), #10(frontend). 정체 아님.
  - CONFLICTING + 정체(23일+) + e2e 실패: #9, #8, #5.
  - MERGEABLE + e2e 실패 + 정체: #7, #6.
- 신규 이슈: #13. 보류 승인: 0건.
- **Telegram 보고 미발송** — launchd 세션은 `--channels` 플래그 없이 떠
  Telegram MCP 도구(`reply`)가 로드되지 않음. ops.env에 `TELEGRAM_BOT_TOKEN` 없어
  curl 폴백도 불가. 사람 조치 필요: ① launchd plist에 `--channels
  plugin:telegram@claude-plugins-official` 추가 또는 ② `ops/ops.env`에
  `TELEGRAM_BOT_TOKEN` 저장해 curl 폴백 경로 가능하게 만들기.

## 17:41 — 개발팀(sub-project 2) 구축 완료
- 검증 기준 4/4 통과: ① 라벨+트리아지 ② bug-scan·pr-watch ③ dev-cycle (PR #14) ④ 오케스트레이터 통합

## 18:00 — 운영 오케스트레이터 (18시 실행)

- **healthcheck** — prod-backend 200 OK이나 응답 본문 `katago_alive:false` — KataGo
  서브프로세스 사망 (12시엔 alive였음). prod-web OK, `com.baduk.api` PID 9037 /
  `com.baduk.web` PID 12434 launchd 가동, 디스크 5%. staging은 backend 가동
  (pid 80510), web 중단됨 (정책상 정상).
- **incident 처리** — KataGo 죽음은 사용자 영향(AI 응수 불가) 있는 부분 장애.
  화이트리스트는 "`/api/health` 무응답/다운"만 자동 재시작 대상이고 본 케이스는
  엔드포인트가 200 + 본문 degraded라 모호 → 정책대로 한 단계 보수적으로
  🟡 승인으로 격상. `AP-20260523-02` (com.baduk.api 재시작) 제안, incidents 기록.
- **backup-verify** — 최신 `baduk-20260523T040005.db.gz` (14시간 경과 < 30시간,
  daily 3 / weekly 0 / monthly 0), 복원 드릴 `integrity_check: ok` + 테이블 7개 —
  정상.
- **bug-scan** — `~/Library/Logs/baduk-api.err` `database is locked` 누적 2096회
  (12시 2066 → +30회, 추세 안정). 가장 최근 err 스택은 04:54, 그 이후 INFO만.
  KataGo 사망 흔적은 err/out 어디에도 없음 (silent death). 신규 이슈 후보 없음 —
  기존 #13으로 추적.
- **backlog-triage** — 미분류 이슈 0건 (할 일 없음).
- **pr-watch** — 열린 PR 8건, 7건 주의 + 1건 신규 정상 후보.
  - CI 실패 (frontend): #14 (어제 03:43, 신규 dev-cycle 산출물), #11, #10.
  - CONFLICTING + 정체(23일+) + e2e 실패: #9, #8, #5.
  - MERGEABLE + e2e 실패 + 정체: #7, #6.
  - PR #14는 frontend CI만 실패 — dev-cycle 후속 정비 후보.
- **신규 이슈** — 0건. **보류 승인** — 1건 (AP-20260523-02 신규).
- **Telegram 보고 미발송** — 12시와 동일 원인. `--channels plugin:telegram` 미부여
  + `ops/ops.env`에 `TELEGRAM_BOT_TOKEN` 부재로 reply MCP·curl 폴백 모두 불가.
  파일 기반 보고만 수행. 사람 조치 미해결.

## 19:12 — 콘텐츠 수집·SEO 인덱스(sub-project 3a) 구축 완료
- 검증 기준 4/4 통과: ① CWI ingest+launchd ② 동적 sitemap (911+ URL) ③ 프로 페이지 메타·canonical ④ 오케스트레이터 통합
- staging에서 검증 완료. prod 반영은 별도 deploy.md 절차.

## 20:04 — 테마·이 달의 명국(sub-project 3b) 구축 완료
- 검증 기준 4/4 통과: ① 테마 카탈로그 6개 + 페이지 ② 결정적 월간 픽 ③ sitemap 신규 21 URL (테마 6 + 픽 인덱스 1 + 월간 14) ④ generateMetadata
- staging에서 검증 완료. prod 반영은 별도 deploy.md 절차.
- 메모: cwi(0)·honinbo(0) 테마는 데이터 라벨 mismatch — collection 컬럼이 'cwi'가 아니거나 event 문자열에 'Honinbo' 미포함. 후속 데이터 정리 대상.
