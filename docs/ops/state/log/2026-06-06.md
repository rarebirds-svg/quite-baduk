# 2026-06-06 운영 로그

## dev-cycle 02:00 — 자율 버그 사이클

- **적격 버그 스캔**: `bug`/`small` 라벨 + `feature`/`in-progress` 없는 열린 이슈 → #39 유일.
- **처리**: 재구현 없음. #39는 이미 `fix/issue-39`(`6f0d556`+`cc4593c`) / PR #45로 완료 → 머지 대기 상태만 재검증.
- **PR #45 상태**: OPEN, mergeable CLEAN(mergeStateStatus CLEAN, 충돌 없음).
- **recency 갱신** (`bug-scan-verify-recency` 준수):
  - prod 재기동됨 — 현 pid **93970**, 6/4 22:58 기동(이전 세대 pid 28352에서 교체).
  - 현 세대 startup(err 로그 line 221201) **이후** send-after-close `RuntimeError`(`ws.py:232`) **1건 신규 발생**(line 221255–221256, 로그 최종수정 6/5 21:51).
  - 6/3 사이클의 "현 세대 신규 0건" 판단을 갱신 — 미머지 상태라 현 prod 세대에서 노이즈 재개.
  - 원인: prod는 main 작업 트리(`bb6d961`)에서 직접 실행, 해당 트리에 수정 미반영 → `send_json` 무방비.
- **결과**: 신규 코드 작업 없음. #39 머지(🟡 사람 승인)만 대기. 6/3 대비 긴급도 상승.
- **핸드오프**: #39에 갱신된 머지 에스컬레이션 코멘트 게시 (`gh pr merge 45 --squash` + `launchctl kickstart com.baduk.api`).

## content-draft 02:00 — 콘텐츠 초안 사이클

- **종류·슬러그 선택**: glossary 게시 12/18(0.667)·미작성 6, faq 게시 6/9(0.667)·미작성 3. 비율 동률 → 규칙대로 glossary, 미작성 후보 알파벳 첫 항목 `jeongseok`(정석) 선택.
- **초안·QA**: `drafts/jeongseok.md` 작성 후 korean-copy-qa 점검. 수정 3건 반영(흑·백 비대칭 서술 → 한쪽/다른 쪽, 구어체 탈피, 수→득실 표현). 콜론 종결·용어·문체 이상 없음. QA 보류 없음. (참고: QA가 기존 `gongbae.md` 합쇼체 문체 불일치를 별건으로 지적 — 이 초안 문제 아님, 차후 정리 후보.)
- **라이브 게시(🟢 자율)**: 덮어쓰기 가드 통과(기존 파일 없음). `web/content/glossary/jeongseok.md`만 스테이징·커밋(`3335b89`)·`main` push 성공. `/glossary`는 동적 렌더라 즉시 노출.
- **Telegram 사후 보고**: `reply` MCP 미노출 → Bot API `sendMessage` 직접 발송(200). "jeongseok (glossary) 게시 완료 — /glossary/jeongseok".

## 오케스트레이터 12:00 — 정기 1회 실행

- **헬스체크(🟢)**: prod backend 200(`db:true`, `katago_alive:false` 정보용), web 200, launchctl `com.baduk.api`(pid 93970)·`com.baduk.web`(pid 93974) 가동, 디스크 4%, plist drift 없음(exit 0). staging 중단(정책상 정상).
- **백업 검증(🟢)**: 최신 `baduk-20260606T040005.db.gz`(6/6 04:00, 약 8h) integrity `ok`·7 tables, daily/weekly/monthly = 14/2/1. 정상.
- **버그 스캔(🟢)**: err 로그의 send-after-close `RuntimeError`는 신규가 아닌 기존 #39. 누적 20건, 최근 재발 line 221256(6/6 09:52). `bug-scan-verify-recency` 준수해 recency 확인. **신규 이슈 0건**.
- **백로그 트리아지(🟢)**: 미분류 열린 이슈 0건. 할 일 없음.
- **PR 감시(🟢)**: 열린 PR #43(`fix/issue-42-sqlite-pool-size`, MERGEABLE, CI 체크 없음 — 정상, 생성 5/31). 정체·실패·충돌 없음. **PR #45는 6/5 22:37 머지 완료**(#39 close)로 OPEN 목록에서 빠짐.
- **#45 배포 갭 발견**: 머지된 #45 수정이 작업 트리엔 있으나 현 prod 프로세스(pid 93970, 6/4 22:58 기동)는 머지 이전 세대 → RuntimeError 6/6 09:52 재발. 헬스 정상이므로 장애 복구 아님 → 배포(🟡)로 처리. **AP-20260606-01** 제안 큐 등록.
- **Telegram**: `reply` MCP 미노출(ToolSearch 미검출) → Bot API `sendMessage` 직접 발송. 상태 요약 + AP-20260606-01 제안 발송.

## 오케스트레이터 18:00 — 정기 1회 실행

- **헬스체크(🟢)**: prod backend 200(`db:true`, `katago_alive:true`), web 200, launchctl `com.baduk.api`(pid 46350)·`com.baduk.web`(pid 46354) 가동, 디스크 5%, plist drift 없음(exit 0). staging 중단(정책상 정상).
- **백업 검증(🟢)**: 최신 `baduk-20260606T040005.db.gz`(6/6 04:00) integrity `ok`·7 tables, daily/weekly/monthly = 14/2/1. 정상.
- **#45 배포 갭 해소 확인**: prod backend가 6/6 17:39:49 외부 재기동(pid 93970 → 42414 → 84647 → **46350**). 현 프로세스는 HEAD(`1ea5246`, #45 머지 이후 작업 트리)에서 기동 → `ws.py` RuntimeError 가드 라이브. 마지막 RuntimeError(err 로그 line 221256)는 재기동 *이전*, 17:39 이후 신규 0건. **AP-20260606-01 무효 처리**(오케스트레이터 재기동 불필요 — 종단 상태 이미 달성), 큐에서 제거. 보류 승인 0건.
- **버그 스캔(🟢)**: err 로그의 send-after-close / not-connected `RuntimeError`는 #39·#45 동일 건이며 가드가 라이브로 배포됨. `bug-scan-verify-recency` 준수 — 재기동 이후 재발 없음. **신규 이슈 0건**.
- **백로그 트리아지(🟢)**: 미분류 열린 이슈 0건. 할 일 없음.
- **PR 감시(🟢)**: 열린 PR #43(`fix/issue-42-sqlite-pool-size`, 생성 5/31 = 6일 경과, 7일 정체 임계 미도달, mergeable=UNKNOWN(재계산 중·무해), CI 체크 없음 — 정상). 실패·충돌 없음.
- **콘텐츠 인덱스 갱신**: 6/6 15:44 content-ingest가 CWI에서 **fetched=200 new=200 duplicate=0 error=0**(cap=200) 적재. 프로 기보 911 → **1111**(masterpiece 825 + world 286). 대시보드 콘텐츠 인덱스·CWI ingest 행 갱신.
- **Telegram**: `reply` MCP 미노출 → Bot API `sendMessage` 직접 발송. 상태 요약(헬스 OK, 보류 0, 최근 CWI 200건, 최근 분석 2026-W22).
