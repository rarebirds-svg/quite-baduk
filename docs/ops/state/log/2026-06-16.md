# 운영 로그 — 2026-06-16

## 02:00 자율 버그 사이클 (dev-cycle)

### 이슈 선택
- `/opt/homebrew/bin/gh issue list --state open --json number,title,labels` → 열린 이슈 0건.
- `bug`/`small` 라벨 적격 이슈 없음 → **처리할 버그 없음**.
- 종료. worktree·브랜치 생성 안 함.

### 비고
- `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 조회.
- 6/13~6/15와 동일하게 신규 열린 이슈 없음.

### Telegram 보고
- telegram `reply`/send 도구 세션 미노출(ToolSearch `telegram reply send message channel notify` → PushNotification·Monitor·Canva만 매칭). 직전 사이클과 동일.
- 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`(`ops/ops.env`)로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=True, message_id=2251).

## 12:30 오케스트레이터 정기 실행

### 헬스체크 — 🟢 모두 정상
- prod backend `/api/health` 200 `{status:ok, db:true, katago_alive:true}` · prod web 200 OK.
- launchd: `com.baduk.api` PID 923 · `com.baduk.web` PID 912 가동.
- 디스크 5% · plist drift 없음(exit 0) · staging 중단(정책상 정상).

### 백업 검증 — 🟢 정상
- 최신 `baduk-20260616T040005.db.gz`(오늘 04:00, 30h 이내). daily 14 / weekly 4 / monthly 1.
- 복원 드릴: integrity_check=ok, 테이블 7개.

### 버그 스캔 — 신규 0건
- `baduk-api.err`의 `TypeError: bytes is not JSON serializable`(errors.py val_handler) 검토 → **비활성**: 총 2회뿐, 마지막 발생 6/13, 이후 err 로그 미기록(파일 mtime 6/13 22:49)이며 그 뒤 서버 4회 재기동에도 재발 0. 최근성 미달로 이슈 미생성(노이즈 회피, [[bug-scan-verify-recency]]).
- 열린 `bug`/미분류 이슈 0건.

### 백로그 트리아지 — 할 일 없음
- 미분류 열린 이슈 0건.

### PR 감시 — 정상
- 열린 PR 0건. 주의 PR 없음.

### Telegram 보고
- reply/send 도구 세션 미노출(ToolSearch `telegram reply send message channel notify` → Canva·PushNotification·Monitor만 매칭). 직전 사이클과 동일.
- 진실 공급원 `.env` 토큰 + chat_id `1241277614`로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=true, message_id=2252).

## 18:30 오케스트레이터 정기 실행

### 헬스체크 — 🟢 모두 정상
- prod backend `/api/health` 200 `{status:ok, db:true, katago_alive:true}` · prod web 200 OK.
- launchd: `com.baduk.api` PID 923 · `com.baduk.web` PID 912 가동.
- 디스크 5% · plist drift 없음(exit 0) · staging 중단(정책상 정상).

### 백업 검증 — 🟢 정상
- 최신 `baduk-20260616T040005.db.gz`(오늘 04:00, 30h 이내). daily 14 / weekly 4 / monthly 1.
- 복원 드릴: integrity_check=ok, 테이블 7개.

### 버그 스캔 — 신규 0건
- `baduk-api.err`의 `TypeError: bytes is not JSON serializable`(errors.py val_handler) 재검토 → **이미 수정됨**: 현 `errors.py`에 `_safe_validation_errors`가 `custom_encoder={bytes: ...}`로 bytes를 치환 디코딩(L18-21). 스택트레이스 라인번호(L21=`return JSONResponse(`)는 수정 *이전* 코드와 일치 — 6/13 22:49 재기동 전 옛 프로세스 로그다. err 파일 mtime 6/13 22:49, 현 프로세스(PID 923) 재발 0. 최근성·코드상태 모두 미달로 이슈 미생성([[bug-scan-verify-recency]]).
- 열린 `bug`/미분류 이슈 0건.

### 백로그 트리아지 — 할 일 없음
- 미분류 열린 이슈 0건(`no:label` → `[]`).

### PR 감시 — 정상
- 열린 PR 0건. 주의 PR 없음.

### Telegram 보고
- reply/send 도구 세션 미노출(ToolSearch → Canva·PushNotification·Monitor만 매칭). 직전 사이클과 동일.
- 진실 공급원 `~/.claude/channels/telegram/.env` 토큰 + chat_id `1241277614`로 Bot API `sendMessage` 직접 호출 → **발송 성공** (ok=true, message_id=2253).
