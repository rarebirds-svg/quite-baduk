# 운영 로그 — 2026-06-12

## 02:00 자율 버그 사이클 (dev-cycle)

### 이슈 선택
- `gh issue list --state open --json number,title,labels` → 열린 이슈 0건.
- `bug`/`small` 라벨 적격 이슈 없음 → **처리할 버그 없음**.
- 종료. worktree·브랜치 생성 안 함.

### 비고
- `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 조회.
- 리포 컨텍스트 확인: `rarebirds-svg/quite-baduk`. 최근 닫힌 버그(#39~#42)는 모두 CLOSED.

### Telegram 보고
- `reply`/`SendMessage` 도구 세션 미노출 (ToolSearch `telegram` 질의 무매칭). 환경 자가진단은 하지 않음 — 도구 자체가 노출되지 않아 호출 불가.
- 발송하려던 본문:
  ```
  [inkbaduk 02:00 dev-cycle] 처리할 버그 없음
  열린 이슈 0건 (bug/small 적격 없음). worktree·브랜치 생성 안 함. 종료.
  ```

## 07:00 Stability Phase B 수동 단계 + Phase C 본체 (대화 세션)

### Phase B — 오프박스 모니터 배포 ✅
- `ops/cloudflare/health-monitor` Worker를 `wrangler deploy`로 배포 (계정 인증 기존 유효).
  - URL: https://inkbaduk-health-monitor.poporyfamily.workers.dev · cron `*/5 * * * *`
  - 시크릿 등록: `TELEGRAM_BOT_TOKEN`(~/.claude/channels/telegram/.env), `TELEGRAM_CHAT_ID`(ops/ops.env)
  - 프로브 검증: HTTP 진입점 `OK` / 200.
- pmset 점검: `autorestart 1`·`sleep 0`·`womp 1` 이미 적용 상태 확인. **잔여: `disksleep 10` → 0 (sudo 필요, 사람 실행)**.
- 자동로그인: `autoLoginUser` 미설정 확인. **잔여: 시스템 설정 → 사용자 및 그룹 → 자동 로그인 (GUI 1회, 사람 실행)**.
- 잔여 수동 검증(파괴적이라 보류): 강제 재부팅 복귀, `kill -STOP` hung 주입 복구, cloudflared 다운 → 5분 내 Telegram 알림.

### Phase C 본체 — 프로 상세 SSR 전환
- PR #50 `feat/pro-detail-ssr` — `/spectate/pro/[id]` 서버 컴포넌트(ISR 1h) 전환, 없는 ID 진짜 404, backend down 시 클라이언트 fetch 폴백. Vitest 123/123 · build ✓ · 런타임 SSR 검증 ✓.

## 08:00 Phase C 머지·배포 + backend CI 게이트 복구 (대화 세션, 07:00 항목의 후속)

### backend CI 간헐 실패 근본 수정 — PR #51 (머지)
- #47부터 backend CI를 깨던 `test_ws_accepts_query_token_without_cookie - ValueError: Connection closed`의 근본 원인 수정. 로컬 미재현 이유는 의존성 드리프트(로컬 venv starlette 1.0 vs CI fresh 1.3) — fresh venv로 로컬 재현 성공.
- 원인: WS 테스트 배선의 교차 오염 4종(last_seen_cache._cache, engine 미dispose, engine_pool 락·상태, ws._connections) + 파일 temp DB에 StaticPool을 써서 aiosqlite 커넥션이 이벤트 루프를 넘어 재사용되는 구조.
- 수정: `_wire_test_app` cleanup 도입(dispose+글로벌 초기화) + StaticPool→NullPool. starlette 1.3 환경 20연속 그린 검증.
- 메모리 `backend-dep-drift-ci` 기록: "로컬 통과·CI 실패"면 드리프트부터 재현.

### Phase C 본체 — PR #50 (머지·배포)
- `/spectate/pro/[id]` 서버 렌더(ISR 1h) 전환. CI 전체 그린(이번엔 e2e도 실행·통과).
- 배포: main pull → `npm run build` → `launchctl kickstart -k gui/501/com.baduk.web`.
- 검증: prod에서 SSR HTML(선수명 텍스트 포함), 없는 ID HTTP 404, 공개 도메인 200.

### 사람 액션 잔여 (Phase B)
- `sudo pmset -a disksleep 0` (autorestart·sleep·womp는 기적용).
- 자동 로그인 GUI 1회 설정 (시스템 설정 → 사용자 및 그룹).
- 파괴적 수동 검증 3종(강제 재부팅·hung 주입·cloudflared 다운)은 보류.

## 08:20 Phase B 사람 액션 완료 확인
- `sudo pmset -a disksleep 0` 적용 확인 (`pmset -g`: autorestart 1 / sleep 0 / disksleep 0 / womp 1 — 전 항목 목표값).
- 자동 로그인 설정 확인 (`autoLoginUser = daegong`).
- Phase B 잔여는 파괴적 수동 검증 3종(강제 재부팅 복귀·hung 주입 복구·cloudflared 다운 알림)뿐.

## 09:25 관전 목록 필터 강화 — PR #52 (머지·배포)

### fix(spectate): 미결/강제 종료·시스템 테스트 대국 관전 노출 제외

**동기.** 관전 목록에 0수 즉시 기권(result 있어도 강제 종료), result 없는 승부 미결, screenshotter·devshot·qa_* 시스템 테스트 하네스 닉네임 대국이 노출됐음.

**변경 내용 (`backend/app/api/spectate.py`).**
- `_MIN_EXPOSED_MOVES = 10`: 종료 대국은 10수 이상이어야 노출.
- `result IS NOT NULL`: 승부가 확정된 대국만 노출.
- `_TEST_NICKNAME_PREFIXES = ("screenshotter", "devshot", "qa_")`: 해당 prefix로 시작하는 닉네임 대국 전부 제외. `_prefix_pattern()`으로 `_` 와일드카드 이스케이프 처리(`escape="\\"`).

**테스트 추가 (`tests/api/test_spectate.py`).**
- 단기 종료 대국 숨김, 승부 미결 숨김, 테스트 하네스 닉네임 숨김, `qax*` 실사용 닉네임 정상 노출 — 4개 신규 테스트.

**검증.**
- 로컬 pytest 13/13 통과. CI 전체 통과 (backend·frontend·app-shell-build·e2e 전부 pass).
- PR #52 머지 완료. backend 재기동(`launchctl kickstart -k gui/501/com.baduk.api`).
- prod `/api/spectate` 50건 확인: 전부 정규 한국어 닉네임, move_count ≥ 10, result 있음, 테스트 닉네임 없음.

## 12:00 오케스트레이터 정기 실행 (12:30 슬롯)

### 헬스체크 🟢
- prod backend `{"status":"ok","db":true,"katago_alive":true}` 200 OK, web 200 OK.
- launchctl: `com.baduk.web` PID 26130 / `com.baduk.api` PID 78468 — 둘 다 가동.
- 디스크 4%, plist drift 없음(`sync-launchd.sh --check` exit 0). staging 중단(정책상 정상).

### 백업 검증 🟢
- 최신 `baduk-20260612T040005.db.gz`, mtime 1일 이내. daily/weekly/monthly = 14/3/1.
- 복원 드릴: integrity_check `ok`, 테이블 7개.

### 버그 스캔 🟢 — 신규 0건
- `baduk-api.err`에 `TypeError: Object of type bytes is not JSON serializable`(errors.py val_handler) 2회(line 221457·221549). **그러나 이미 수정·배포된 건의 잔상**.
- 근거: 두 에러는 프로세스 33051(line 221362 기동) 하에서 발생, 이후 44935→1496→**78468**(현 prod, 11:52:52 기동)로 재기동됨. 비JSON 본문 검증 500 버그 수정 `ce9d745`(6/11 21:12 커밋)는 현 프로세스에 반영(라이브 errors.py line 21 = `_safe_validation_errors` 닫는 괄호, stale 33051은 line 21이 val_handler의 `return`). 에러는 수정 배포 *이전* 프로세스의 것. [[bug-scan-verify-recency]] 패턴 — 카운트 아닌 발생 시점·배포 시점 대조로 오탐 차단. 신규 이슈 없음.

### 백로그 트리아지 🟢 — 미분류 0건
### PR 감시 🟢 — 열린 PR 0건 (#47·#50·#52 머지 완료, 주의 PR 없음)

### Telegram 보고
- reply 도구가 이 세션에 미노출(telegram 플러그인 MCP 미로드) — 상태 요약 발송 불가. 사유: 도구 미노출. 환경 자가진단 안 함.

## 18:00 오케스트레이터 정기 실행 (18:30 슬롯)

### 헬스체크 🟢
- prod backend `{"status":"ok","db":true,"katago_alive":true}` 200 OK, web 200 OK.
- launchctl: `com.baduk.web` PID 26130 / `com.baduk.api` PID 78468 — 둘 다 가동 (12:00과 동일 프로세스).
- 디스크 4%, plist drift 없음(`sync-launchd.sh --check` exit 0). staging 중단(정책상 정상).

### 백업 검증 🟢
- 최신 `baduk-20260612T040005.db.gz`, mtime 1일 이내. daily/weekly/monthly = 14/3/1.
- 복원 드릴: integrity_check `ok`, 테이블 7개.

### 버그 스캔 🟢 — 신규 0건
- `baduk-api.err`의 `bytes is not JSON serializable`(2회)는 12:00 분석대로 수정 배포(`ce9d745`) *이전* 프로세스 잔상 — 현 errors.py는 `_safe_validation_errors`로 가드됨. [[bug-scan-verify-recency]] 대조로 오탐 차단. 신규 후보 없음.

### 백로그 트리아지 🟢 — 미분류 0건
### PR 감시 🟢 — 열린 PR 0건 · 열린 bug 이슈 0건

### Telegram 보고
- reply/SendMessage 도구 이 세션 미노출 (ToolSearch `telegram` 질의 → Canva/Context7/Google Drive MCP만 매칭, telegram 플러그인 MCP 미로드). 호출 자체가 불가하여 상태 요약 발송 못 함. 사유: **도구 미노출**. 환경 변수·플래그 자가진단 안 함.
- 발송하려던 본문:
  ```
  [inkbaduk 운영] 18:00 상태 요약
  헬스 OK (prod backend·web 200, 디스크 4%, drift 없음)
  백업 OK (최신 06-12T04:00, integrity ok, 14/3/1)
  보류 승인 0건 · 열린 PR/이슈 0건
  최근 CWI ingest: 2026-06-07 (new=200)
  최근 분석: 2026-W23
  시각: 2026-06-12 18:00
  ```
