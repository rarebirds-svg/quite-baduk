# 2026-05-30

## 02:00 content-draft (콘텐츠 초안 사이클)

- 글로서리 미작성 슬러그 중 알파벳 첫 항목 `handicap`(접바둑·치수) 선택. 초안 `docs/ops/content/drafts/handicap.md` 생성(3단락, 한국 룰 기준).
- korean-copy-qa 검토 반영 — ① 치석 배치 순서(귀→변→천원) 명시, ② 접바둑 덤은 사이트가 따르는 한국 룰 0.5집임을 출처와 함께 명확화. QA가 제기한 "덤 0집(대한바둑협회)" 지적은 프로젝트 canonical 기준(seed-glossary·룰 엔진 0.5집)과 충돌해 0.5 유지, 출처 명시로 해소.
- AP-20260530-01 제안(초안 게시, 🟡 승인) — `pending-approvals.md` 등록 + Telegram 발송 성공(`ok=true`, `message_id=2056`).

## 02:00 dev-cycle (자율 버그 사이클)

- **이슈 선택** — 열린 적격 이슈는 #39(`bug`, feature·in-progress 없음) 1건. 선점(`in-progress` 부착).
- **이슈 #39** — WS `place_move` 중 소켓 종료 레이스 → send-after-close `RuntimeError` err 로그 누적. `.worktrees/dev-cycle`에서 `fix/issue-39`(origin/main 기준) 생성.
- **수정** — TDD. 회귀 테스트(`test_ws_flow.py::test_ws_send_after_close_during_place_move_is_silent`)로 이슈 트레이스백 재현 → `ws.py` 외부 핸들러에 send-after-close `RuntimeError` 흡수절 추가(메시지 문자열로 식별, 타 RuntimeError 재전파) → 통과 확인.
- **검증** — 전체 백엔드 492 passed, `ruff check` clean, `mypy app` clean.
- **커밋** — `6f0d556` (push 안 함, 자율 세션 deny). 이슈에 핸드오프 코멘트(브랜치·SHA·사람 실행 push+PR 명령) 등록, `in-progress` 제거. 머지는 🟡 사람 승인 대기.
- **Telegram 보고** — 이 세션엔 `reply` MCP 도구가 미노출되어, 동일 채널 봇 토큰(`~/.claude/channels/telegram/.env`)+chat_id(`ops/ops.env` 1241277614)로 Bot API `sendMessage` 직접 발송. 성공(`ok=true`, `message_id=2057`).

## 12:00 orchestrator (정기 사이클)

- **healthcheck** — prod backend·web 200 OK, db OK, `katago_alive:false`(정보용·정상). launchctl `com.baduk.api`(1466)·`com.baduk.web`(1455) 가동. 디스크 4%. staging 중단(정책상 정상). launchd plist drift 없음(exit 0). → 정상.
- **backup-verify** — 최신 `baduk-20260530T040005.db.gz`(약 8h, 신선). daily 11 / weekly 1 / monthly 0. 복원 드릴 `integrity_check=ok`, 테이블 7개. → 정상.
- **bug-scan** — err 로그에서 `database is locked` 1257회·WS `receive_json` not-connected 70회 발견. ① db-locked는 **오탐** — 임베디드 타임스탬프 전부 ≤2026-05-25 14:06:43, 이후 4일·다수 재시작에도 0건. err 로그 미rotate로 누적 카운트가 과거분을 노출(#40과 동일 함정). PR #30(`last_seen_at` 디바운스)으로 해소. 신규 이슈 #41 생성 후 검증해서 **즉시 close**. ② WS receive 변종(`ws.py:186`)은 현 서버 세대 기준 4건 진행형 — #39와 동일 disconnect 레이스라 별도 이슈 대신 #39에 코멘트 등록. → 순 신규 이슈 0건.
- **backlog-triage** — 미분류 이슈 0건.
- **pr-watch** — 열린 PR 0건. (#39 픽스는 dev-cycle 로컬 브랜치 `fix/issue-39`@6f0d556로 사람 push+머지 대기 — PR 아님.)
- **pending-approvals** — 대기 1건(AP-20260530-01 handicap 게시).
- **Telegram 보고** — `reply` MCP 도구 미노출 → 동일 채널 봇 토큰(`~/.claude/channels/telegram/.env`)+chat_id(1241277614)로 Bot API `sendMessage` 직접 발송. 성공(`ok=true`, `message_id=2059`).

## 18:00 orchestrator (정기 사이클)

- **healthcheck** — prod backend·web 200 OK, db OK, `katago_alive:false`(정보용·정상). launchctl `com.baduk.api`(1466)·`com.baduk.web`(1455) 가동. 디스크 4%. staging 중단(정책상 정상). launchd plist drift 없음(exit 0). → 정상.
- **backup-verify** — 최신 `baduk-20260530T040005.db.gz`(약 14h, mtime+1 미출력=신선). daily 11 / weekly 1 / monthly 0. 복원 드릴 `integrity_check=ok`, 테이블 7개. → 정상.
- **bug-scan** — err 로그 WS `receive_json` not-connected 누적 70회(전체 로그 기준). #39와 동일 disconnect 레이스라 별도 이슈 대신 #39에 누적치·수신경로(line 186) 데이터포인트 코멘트 추가(이전 12:00엔 "4건/현 세대"로 기록, 동일 케이스). db-locked는 12:00에 오탐 확정·#41 close 완료로 재플래그 안 함. → 순 신규 이슈 0건.
- **backlog-triage** — 미분류 이슈 0건.
- **pr-watch** — 열린 PR 0건.
- **pending-approvals** — 대기 1건(AP-20260530-01 handicap 게시).
- **Telegram 보고** — `reply` MCP 도구 미노출 → 동일 채널 봇 토큰(`~/.claude/channels/telegram/.env`)+chat_id(`ops/ops.env` 1241277614)로 Bot API `sendMessage` 직접 발송. 성공(`ok=true`, `message_id=2060`).

## handicap 게시 (AP-20260530-01 사람 승인 실행)

- 사용자 지시로 `handicap`(접바둑·치수) 초안을 라이브 prod 웹에 게시. `docs/ops/content/drafts/handicap.md` → `web/content/glossary/handicap.md` 이동 + 커밋. `pending-approvals.md` 큐에서 AP-20260530-01 제거.
- **노출 수준** — 상세 페이지 `/glossary/handicap`는 동적 렌더(`generateStaticParams` 없음, 요청 시 `fs` 읽기)라 재빌드 없이 즉시 라이브. 라이브 검증 — 본문 "접바둑은…" 노출 확인.
- **잔여 갭** — 글로서리 목록 `/glossary`는 정적 생성(현 `.next` 빌드 2026-05-26)이라 재빌드+`com.baduk.web` 재시작 전까지 목록 카드에 미노출. gyega(2026-05-27 게시)도 동일하게 직접 URL만 노출 중. 목록 반영은 별도 재빌드 필요 — 사람 판단 대기.
