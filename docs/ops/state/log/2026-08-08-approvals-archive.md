# 승인 큐 아카이브 — AP-20260808-01 · AP-20260805-01 (2026-08-08 15:48 KST 실행 완료)

`pending-approvals.md`에서 옮겨온 원문이다. 두 건은 한 번의 재기동으로 함께 발효했고, 요약은 큐 파일의 `처리 완료 — 최근` 절에 남겨 뒀다.

### AP-20260808-01
- 액션: PR [#79](https://github.com/rarebirds-svg/quite-baduk/pull/79)(어드민 401/403 폴링 정지, 이슈 #78 수정) 머지 — 그리고 이어지는 배포는 **AP-20260805-01에 합류**시킨다.
- 근거: 8/8 02:00 dev-cycle이 이슈 #78을 고쳐 올린 PR이고, `main` 코드 머지는 🟡라 자율 범위 밖이다. 지금 상태는 "고쳐졌는데 머지만 안 된" 것이다.
  - **CI 4잡 전부 SUCCESS** — `frontend`·`backend`·`app-shell-build`·`e2e`. `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. e2e는 현재 GREEN 기준선이라 통과가 곧 무회귀 신호다(`[[ci-e2e-disabled]]`).
  - **변경 범위 10파일 / +195−45** — `web/app/admin/` 4화면 + `components/admin/AdminAuthNotice.tsx` 신규 + `lib/api.ts` `authFailure()` + ko/en 사전 2줄 + 테스트 신규. **웹 전용이고 백엔드·DB·마이그레이션 변경 0건.**
  - **버그가 라이브에 살아 있음을 이번 사이클에 재실측했다** — 최근 5,000줄에 401 **1,070건**(전체의 21.4%). 다만 최근 500줄 8건 · 최근 200줄 **0건**으로 버스트는 이미 멎었다(운영자 탭이 닫힌 것이지 고쳐진 게 아니다).
- 영향: 어드민 5개 화면이 401(만료)·403(권한없음)에서 `clearInterval`로 폴링을 끊고 재로그인 안내를 띄운다. 운영자가 만료된 화면의 옛 데이터를 현재 상태로 오독하는 문제가 사라지고, 무의미한 요청 폭주(포그라운드 환산 시간당 2,880건)가 멎는다. **사용자 대면 화면·보안 표면 변경 없음**(서버는 이미 401로 정상 거절 중).
- 잔여 위험: 낮음. 어드민 전용 경로이고 `admin/pro-games`(이미 올바른 화면)는 손대지 않았다. 머지만으로는 라이브가 바뀌지 않는다 — 웹 재빌드가 필요하므로 아래 절차대로 AP-20260805-01의 배포와 한 번에 처리하는 것이 맞다.
- 실행 절차:
  1. `gh pr merge 79 --squash --delete-branch`
  2. `cd /Users/daegong/projects/baduk && git pull --ff-only` — **AP-20260805-01의 절차에는 없던 단계다.** #79 머지로 origin이 앞서므로 배포 전에 반드시 넣는다.
  3. 이후는 AP-20260805-01의 실행 절차 1~5를 그대로 수행 — `web && npm run build` → `ops/stack.sh restart prod` → 헬스 확인 → 판별 검사 2종. 이 한 번의 배포로 **#75·#77·#79 세 건이 함께 발효**한다.
  4. **#79 판별 검사** — 재기동 후 `grep -rl 'authFailure\|sessionExpired' web/.next/static web/.next/server`가 1개 이상이면 발효.
- 상태: 대기

### AP-20260805-01
- 액션: 8/4~8/5에 머지된 웹 커밋 2건(#75·#77)을 prod에 배포 — `web` 재빌드 + `ops/stack.sh restart prod`.
- 근거: **`git rev-list --count HEAD..origin/main` = 0이지만 라이브는 2커밋 뒤처져 있다.** 그 지표는 작업 트리가 origin을 따라잡았는지만 보고, 실행 중인 프로세스가 작업 트리를 반영하는지는 보지 않는다(`[[merge-is-not-deploy]]`). launchd prod는 리포 작업 트리에서 직접 구동되므로 pull만으로는 웹이 바뀌지 않는다(`[[prod_runs_on_launchd]]`).
  - **결정적 증거** — `grep -rl 'resultText' web/.next/{static,server}` = **0개 파일**. #77이 추가한 `game.resultText.*` i18n 네임스페이스가 빌드 산출물에 아예 없다 = `a59b260` 미반영 확정.
  - **시각 증거** — HEAD `a59b260`는 8/5 **06:25:31** pull, 직전 `13eb069`는 8/4 **22:29:04** pull. 그런데 `web/.next/BUILD_ID` mtime은 **`Aug 4 22:23`**으로 두 pull보다 먼저다. 웹 프로세스 기동은 그보다도 이른 `Aug 2 22:11:42`(PID 93522, 62시간 무재기동).
  - **#75도 프로세스 기준 미반영** — 현재 떠 있는 프로세스의 기동 블록에 `⚠ "next start" does not work with "output: standalone"` 경고가 남아 있다 = standalone 설정으로 기동된 구 프로세스다.
- 영향:
  - **#77 (`a59b260`)** — SGF 결과 표기를 사람이 읽는 문장으로 렌더. 18파일 +237/−39, `web/lib/formatResult.ts` 신규 + 화면 12곳 호출부 + ko/en 사전. **재빌드 + 재기동 필요.** 지금은 대국 기록·관전 화면의 결과 표기가 여전히 구 형식이다.
  - **#75 (`13eb069`)** — `next.config.js`에서 웹 타깃 `output:"standalone"` 제거. **재빌드 + 재기동 필요.** 매 기동 경고가 사라지고, 쓰지 않는 standalone 번들 생성이 멈춘다.
  - **부수 해소** — 8/2 22:11 기동한 `next start`가 8/4 22:23에 통째로 갈린 `.next`를 물고 있어, 메모리(구 빌드 매니페스트)와 디스크(신 빌드 청크)가 불일치하는 비정상 상태다. 재기동으로 정리된다.
- 검증 상태: main CI 최근 5런 전부 `success`(#77 머지 시점 4잡 SUCCESS). 작업 트리 미커밋 변경은 `docs/ops/state/*`·`e2e/*`뿐이라 유입 파일 집합과 교집합 없음. 배포 갭 커밋 자체는 이미 pull된 상태라 이번 절차에 `git pull`은 불필요하다.
- 잔여 위험: 재기동 중 수십 초 다운타임. **진행 중 대국 0건**(최근 2,000줄 `WebSocket /api/ws/games` 0건, 마지막 대국 #336은 8/1 19:15 종료)이라 대국 중단 위험은 낮다. 웹 빌드 실패 시 기존 `.next`가 남아 롤백은 재기동만으로 가능하다.
- 실행 절차:
  1. `cd /Users/daegong/projects/baduk/web && npm run build` — #75 config + #77 렌더러 번들 반영.
  2. `ops/stack.sh restart prod` — web(#75·#77) 재기동. backend 변경분은 없으나 스크립트가 함께 재기동한다.
  3. `curl -fs http://localhost:8000/api/health` 200 + `curl -fs http://localhost:3000 >/dev/null` 200 확인.
  4. **판별 검사** — `grep -rl 'resultText' web/.next/static web/.next/server`가 **1개 이상**을 출력하면 #77 발효. 0개면 빌드가 실패한 것이다.
  5. **판별 검사** — 재기동 후 `baduk-web.err` 새 기동 블록에 `does not work with "output: standalone"` 경고가 **없으면** #75 발효.
- 상태: 대기
- **재확인 (8/5 18:00, 제안 후 6시간)** — 판별 검사 3종을 다시 돌렸고 **전부 12:00과 동일**하다. ① `grep -rl 'resultText' web/.next/{static,server}` = **0개 파일** ② `web/.next/BUILD_ID` mtime = `Aug 4 22:23` **불변** ③ 웹 프로세스 `ps lstart` = `Sun Aug 2 22:11:42` **불변**. 실행 절차 1(재빌드)·2(재기동) 모두 미수행 상태이며 갭은 그대로다. 잔여 위험 판단도 유지된다 — 최근 2,000줄 `ws/games` **0건**, `games` **336**으로 8/1 이후 신규 대국 0건이라 진행 중 대국이 없다. 중복 제안(AP-20260805-02)은 만들지 않았다.
- **재확인 (8/6 12:00, 제안 후 24시간)** — 판별 검사 3종을 세 번째로 돌렸고 **전부 8/5 12:00·18:00과 동일**하다. ① `grep -rl 'resultText' web/.next/{static,server}` = **0개 파일** ② `web/.next/BUILD_ID` mtime = `Aug 4 22:23` **불변** ③ 웹 프로세스 `ps lstart` = `Sun Aug 2 22:11:42` **불변**(= 85시간 48분 무재기동). 실행 절차 1(재빌드)·2(재기동) 모두 미수행이며 갭은 그대로다. `git rev-list --count HEAD..origin/main`은 여전히 **0**(HEAD = `origin/main` = `a59b260`). 잔여 위험 판단도 유지된다 — 8/5 18:00 이후 18시간 델타 4,721건에 `ws/games` **0건**, `games` **336**으로 8/1 이후 5일째 신규 대국 0건이라 진행 중 대국이 없다. 같은 구간 5xx·404 **0건**. 중복 제안(AP-20260806-*)은 만들지 않았다.
- **재확인 (8/7 12:00, 제안 후 48시간) — 판별 검사는 그대로지만 긴급도 근거가 바뀌었다.** 판별 검사 3종을 다섯 번째로 돌렸고 **전부 이전 네 번과 동일**하다. ① `grep -rl 'resultText' web/.next/{static,server}` = **0개 파일** ② `web/.next/BUILD_ID` mtime = `Aug 4 22:23` **불변** ③ 웹 프로세스 `ps lstart` = `Sun Aug 2 22:11:42` **불변**(= **97시간 48분** 무재기동). `git rev-list --count HEAD..origin/main`은 여전히 **0**(HEAD = `origin/main` = `a59b260`). 실행 절차 1·2단계 모두 미수행이다. 중복 제안(AP-20260807-*)은 만들지 않았다.
  - **🆕 "8/1 이후 신규 대국 0건"이라는 종전 근거는 더 이상 사실이 아니다.** DB 실측 `max(id)` = **337**(직전 336). 대국 #337이 **8/6 21:55 KST 생성**돼 9분간 30수가 두어졌고(`moves` 마지막 수 30수 W K17, `played_at` 8/6 13:04:14 UTC = **22:04 KST**), 같은 구간 `visit_hits`가 85 → **99행(+14)**, `POST /api/analytics/hit` **14건**으로 사람 방문도 재개됐다. 5일간의 무트래픽 구간이 끝났다.
  - **따라서 미배포 비용은 올라갔다.** #77은 대국 기록·관전 화면의 결과 표기라 **사용자 대면** 변경이고, 실사용자가 돌아온 지금 구 형식 노출이 실제로 보인다.
  - **재기동 잔여 위험은 그대로 낮다.** 마지막 수가 **14시간 전**이라 중단될 진행 중 대국이 없다. `status=active`인 #337은 활성 세션이 아니라 미완료 대국의 기본값이다(같은 값의 4월 이후 잔여 행이 180여 건). 세션도 이미 만료됐다(`GET /api/games/337` 401 2건 · WS 403 1건).
  - **판정 방법 정정** — 종전 각주들이 근거로 쓴 "`.log`의 `ws/games` 0건"은 신뢰할 수 없다. WS 라인은 stderr로만 나가서 `.log` 231,484줄 중 `ws/games`는 **1건**, `.err`에는 **643건**이다. 대국 유무는 DB(`max(id)`·`moves.played_at`)로 판정해야 한다.
  - 그 외 서비스 지표는 정상 — 6시간 델타 4,991건에 **5xx 0건**(200 4,932 / 401 37 / 204 15 / 404 3 / 201 3), 현 기동 이후 백엔드 트레이스백 **0건**.
- **재확인 (8/7 18:00, 제안 후 54시간) — 판별 검사 여섯 번째 동일. 재기동 위험은 오히려 더 내려갔다.** 판별 검사 3종이 **전부 이전 다섯 번과 동일**하다. ① `grep -rl 'resultText' web/.next/{static,server}` = **0개 파일** ② `web/.next/BUILD_ID` mtime = `Aug 4 22:23` **불변** ③ 웹 프로세스 `ps -o lstart,etime -p 93522` = `Sun Aug 2 22:11:42` / `04-19:50:19` = **115시간 50분** 무재기동. `git rev-list --count HEAD..origin/main`은 여전히 **0**(HEAD = `origin/main` = `a59b260`). 실행 절차 1·2단계 모두 미수행이다. 중복 제안은 만들지 않았다.
  - **재기동 잔여 위험이 12:00보다 낮아졌다.** DB `max(id)` = **337 불변**, 마지막 수 `2026-08-06 13:04:14 UTC` = **20시간 전**(12:00 시점 14시간 → 20시간). 6시간 델타에 `POST /api/games` **0건**. 중단될 진행 중 대국이 없다(`[[in-progress-game-detection]]` 절차대로 DB로 판정).
  - **긴급도 근거(8/7 12:00자 상향)는 유지된다.** 8/6 저녁의 실사용 대국 #337·방문 재개는 사실 그대로이고, #77의 사용자 대면 결과 표기가 미배포인 상태도 그대로다. 다만 이번 6시간은 트래픽이 얇았다 — 델타 1,167건 중 실질 공개 트래픽은 소수이고 `visit_hits`는 +1행에 그쳤다.
  - 서비스 지표 정상 — 6시간 델타 **5xx 0건**, 백엔드 트레이스백 0건. 401 1,034건은 배포 갭과 무관한 별건(어드민 401 폴링 루프)이며 이슈 [#78](https://github.com/rarebirds-svg/quite-baduk/issues/78)로 분리했다.
- **재확인 (8/8 12:00, 제안 후 72시간) — 판별 검사 일곱 번째 동일. 이번엔 배포 대상이 2건에서 3건으로 늘었다.** 판별 검사 3종이 **전부 이전 여섯 번과 동일**하다. ① `grep -rl 'resultText' web/.next/{static,server}` = **0개 파일** ② `web/.next/BUILD_ID` mtime = `Aug 4 22:23` **불변** ③ 웹 프로세스 `ps -o lstart,etime -p 93522` = `Sun Aug 2 22:11:42` / `05-13:49:38` = **133시간 50분(5일 14시간) 무재기동**. `git rev-list --count HEAD..origin/main` **0**(HEAD = `origin/main` = `a59b260`). 실행 절차 1·2단계 모두 미수행이다. 중복 제안은 만들지 않았다.
  - **🆕 이 항목은 이제 단독으로 실행하면 안 된다.** 8/8 02:00 dev-cycle이 PR [#79](https://github.com/rarebirds-svg/quite-baduk/pull/79)(어드민 401/403 폴링, #78 수정)를 올렸고 CI 4잡 그린이다. 신규 **AP-20260808-01**에 머지를 제안했으며, 승인 시 **`git pull --ff-only`를 이 항목의 1단계 앞에 넣어야 한다** — 그러지 않으면 재빌드가 #79 없는 트리에서 돈다. 한 번의 재기동으로 #75·#77·#79가 함께 발효한다.
  - **재기동 잔여 위험은 계속 낮다.** DB `max(id)` = **337 불변**, 마지막 수 `2026-08-06 13:04:14 UTC` = **약 45시간 전**(8/7 18:00 시점 20시간 → 45시간). `visit_hits` **108행**(8/7 18:00 100행 → +8). 중단될 진행 중 대국이 없다(`[[in-progress-game-detection]]` 절차대로 DB로 판정).
  - **긴급도 근거(8/7 12:00자 상향)는 유지되나 이번 18시간은 사람 트래픽이 얇았다** — `visit_hits` +8행, 신규 대국 0건. #77의 사용자 대면 결과 표기 미배포 상태는 그대로다.
- **재확인 (8/6 18:00, 제안 후 30시간)** — 판별 검사 3종을 네 번째로 돌렸고 **전부 8/5 12:00·18:00·8/6 12:00과 동일**하다. ① `grep -rl 'resultText' web/.next/{static,server}` = **0개 파일** ② `web/.next/BUILD_ID` mtime = `Aug 4 22:23` **불변** ③ 웹 프로세스 `ps lstart` = `Sun Aug 2 22:11:42` **불변**(= **91시간 48분** 무재기동). 실행 절차 1(재빌드)·2(재기동) 모두 미수행이며 갭은 그대로다. `git rev-list --count HEAD..origin/main`은 여전히 **0**(HEAD = `origin/main` = `a59b260`). 잔여 위험 판단도 유지된다 — 12:00 이후 6시간 델타 1,525건에 `ws/games` **0건**, `games` **336**으로 8/1 이후 5일째 신규 대국 0건이라 진행 중 대국이 없다. 같은 구간은 **1,525건 전부 200**으로 4xx·5xx가 하나도 없다. 중복 제안(AP-20260806-*)은 만들지 않았다.
