# 2026-08-17

- 02:00 dev-cycle: **비정상 종료 — Claude 세션 한도.** `dev-cycle-runs.log` 실측 — 02:00:02 시작 직후 `You've hit your session limit · resets 4am (Asia/Seoul)` 출력 후 02:00:06 종료. 이슈 조회·워크트리·커밋 어느 단계도 진입하지 못했고 `state/jobs/dev-cycle.json`도 미갱신(8/16 02:00 ok가 마지막 마커)이다.
  - 원인이 자명(한도는 04:00 리셋)하고 일회성이라 incident 미등재. 다음 실행 8/18 02:00이 정상 복귀 판정점 — 그때도 같은 사유면 스케줄 대 한도 충돌로 보고할 것.
  - 참고로 백로그는 열린 이슈 0건이라 이번 중단으로 잃은 작업도 0건이다.
- 04:00 backup: daily 생성 + 8/3분 보존 정리. 04:15 prune-visits: deleted=0 (retention 180d).
- 04:30 dev-cycle (재실행, 신규 시각): **처리할 버그 없음.** `gh issue list --state open` 결과 0건 — `bug`·`small` 적격 이슈 없음. 워크트리·브랜치 작업 없이 종료. 02:00 세션 한도 중단 건은 스케줄이 04:30으로 이동돼 해소(커밋 31e63d4).
- 09:00 오케스트레이터 사이클 (am)

### 헬스체크 — 전 항목 정상

- backend `{"status":"ok","db":true,"katago_alive":false}` 200 · web 200 · 공개 도메인 `inkbaduk.com` **200**. launchctl PID api 88640 / web 88644 (`-15`는 직전 인스턴스 SIGTERM 기록 = 오경보 아님). 디스크 **6%**. drift 없음(exit 0). staging 중단 = 정책상 정상. `katago_alive:false`는 lazy 정상.

### 백업 검증 — 23회 연속 통과 + 마이그레이션 반영 첫 백업

- 최신 `baduk-20260817T040005.db.gz` (04:00, 5시간 전, 30h 임계 여유). 티어 **14/8/3** (8/3분 daily 보존 정리로 개수 유지). 복원 드릴 `integrity_check=ok`.
- **드릴본 테이블 수 9 → 10** — 8/16 08:17 적용된 마이그레이션(0019 `analytics_salts` 등)이 담긴 **첫 백업**이라 예정된 증가. 크기 3,526,133 → **3,529,191B (+3,058B)** = 대국 #339(88수) + 방문 유입과 정합.

### 버그 스캔 — 신규 0건

- `.err` **225,435행 불변**(12h 델타 0줄) · 어제 +6줄(게임 338·339 WS INFO)이 마지막 움직임. 트레이스백 0건. `database is locked` 재발 0건 14일째(마지막 225,347행 < 기동 마커). #78 어드민 401 무재발 14일째.
- `.log` 239,215 → **239,782 (+567/12h)**. 최근 5,000줄 상태코드 200 **4,838** / 401 56 / 204 53 / 404 45 / 201 5, **5xx 0건**. 401은 전부 `/api/session`(54) + `/api/stats`(2) = 비로그인 세션 조회. 404는 전부 스캐너 탐침(`/api/.env`·SSRF `169.254.169.254` 등) — 실제 앱 라우트 404 0건, 조치 불요.
- `visit_hits` 일별: 8/14 3 → 8/15 2 → 8/16 **7**(반등 유지) → 8/17 0(오전). 신규 대국 없음 — `max(games.id)` **339** · `moves.max(played_at)` `2026-08-16 03:45:38` UTC 그대로. 신규 GitHub `bug` 이슈 0건.

### 큐 4종

- 열린 이슈 **0건**(미분류 0) — 백로그 공백 열하루째. 열린 PR **0건** — 주의 PR 0건. 승인 대기 **1건**(AP-20260816-01, 48시간째 · 2회 재확인). 배포 갭: behind 0이나 **로컬 21커밋 미push**(코드 19 + ops 2 `bda8f60`·`f9e242b`) = deploy `warn` 유지.

### AP-20260816-01 재확인 (48시간째) — 상태 변화 없음

- `origin/main..HEAD` **21커밋**(코드 19 + ops 2), `BUILD_ID`·PID 8/8 15:48 그대로(**209시간 무재기동**), `sessions` 0행 = 90일 세션 미발효 지속. 진행 중 대국 0건(마지막 수 약 29시간 전)이라 재기동 위험 낮음 — 절차 불변, 실행 시점 재확인만 권장. 중복 제안 없이 각주 누적.

### 잡 상태

- `dev-cycle` ok(8/16 02:00 — **8/17 02:00 실행은 세션 한도로 마커 미기록**, 위 참조) · `content-draft` ok(8/15, 다음 실행 수요일) · `analytics-weekly` ok(8/16 09:01, 다음 실행 8/23 일요일) — json 합산 **3/3 ok**. 신선도 판정은 watchdog 소관.

### 보고

- 09:00 am 다이제스트 발송 성공(exit 0, 마커 `inkbaduk-am-2026-08-17`) — 전체 warn(배포 미반영), 승인 카드 AP-20260816-01 포함.
- ops 로그 커밋 — **push 보류 유지**(AP-20260816-01 승인 시 일괄 push).

## 17:30 온디맨드 bug-scan (사람 요청 — 백로그 충전 목적)

러닝북 `docs/ops/runbooks/bug-scan.md` 절차에 `.log` 상태코드·DB 시각 교차를 더해 수동 실행했다. **신규 이슈 0건.**

로그상의 에러 4종은 전부 **과거분**으로, 재발 시점 기준 후보 요건(반복 + 최근)을 만족하지 않는다.

- `sqlalchemy.exc.OperationalError: database is locked` — 누적 1,276건이나 마지막 발생은 `.err` 225,346행 인근(대국 329·332 참조 → 7/23~7/26). 3주 무재발. 누적 카운트로 판정하면 오탐(#41·#42 전례).
- `RuntimeError: Cannot call "send" once a close message has been sent.` / `WebSocket is not connected.` — 누적 50건, 마지막은 대국 271 인근(6/06). `app/api/ws.py:357`에 `except RuntimeError` 가드가 이미 있고 주석이 #39를 명시한다. **처리 완료된 버그.**
- `TypeError: Object of type bytes is not JSON serializable` — 트레이스백이 가리키는 `errors.py:21 val_handler`가 현재 소스(34행)와 불일치. 커밋 `ce9d745`(비JSON 본문 검증 오류 500 둔갑 수정)로 **이미 수정된 뒤의 잔존 로그**다.
- 액세스 로그 5xx 누적 1,257건(대부분 `/api/admin/*`) — 마지막 5xx는 175,237행/240,049행, **최근 20,000줄 내 0건**. 인근 대국 326~329 → 7월 말.

**판정 유보 사항.** 8/16 03:45(UTC) 이후 신규 수(手)가 없다(`moves.max(played_at)` 불변, 대국 340은 0수). 즉 무에러 구간이 코드 견고성이 아니라 **트래픽 부재**에서 왔을 수 있어, 위 4종의 "해소" 판정 중 `database is locked`는 부하가 돌아온 뒤 재확인이 필요하다.
