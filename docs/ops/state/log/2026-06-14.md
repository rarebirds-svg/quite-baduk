# 운영 로그 — 2026-06-14

## 02:00 자율 버그 사이클 (dev-cycle)

### 이슈 선택
- `/opt/homebrew/bin/gh issue list --state open --json number,title,labels` → 열린 이슈 0건.
- `bug`/`small` 라벨 적격 이슈 없음 → **처리할 버그 없음**.
- 종료. worktree·브랜치 생성 안 함.

### 비고
- `gh`가 PATH에 없어 `/opt/homebrew/bin/gh` 절대경로로 조회.
- 최근 닫힌 버그(#39~#42)는 모두 CLOSED. 신규 열린 이슈 없음.

### Telegram 보고
- `reply`/`SendMessage` 도구 세션 미노출(ToolSearch `telegram` 무매칭 — Canva·Monitor·PushNotification만). 환경 자가진단 없이 봇 토큰(`~/.claude/channels/telegram/.env`)+chat_id(`ops/ops.env`)로 Bot API `sendMessage` 직접 호출 → **발송 성공**(ok, message_id 2243).
- 발송 본문.
  ```
  [inkbaduk 02:00 dev-cycle] 처리할 버그 없음
  열린 이슈 0건 (bug/small 적격 없음). worktree·브랜치 생성 안 함. 종료.
  시각: 2026-06-14 02:00
  ```
- 09:00 주간 분석 리포트 2026-W24 작성·게시(게임 15판/누적 288, FAQ board-sizes 신규). Telegram 푸시 완료(http 200).

## 12:30 오케스트레이터 정기 실행

### 헬스체크 🟢
- prod-backend `{"status":"ok","db":true,"katago_alive":true}` 200 OK, prod-web 200 OK.
- launchctl: com.baduk.api PID 923 / com.baduk.web PID 912 가동.
- 디스크 5%, launchd plist drift 없음(`sync-launchd.sh --check` exit 0).
- staging: 중단(정책상 정상, 경보 아님).

### 백업 검증 🟢
- 최신 `baduk-20260614T040005.db.gz`(오늘 04:00, stale 아님).
- 티어 daily 14 / weekly 4 / monthly 1.
- 복원 드릴: integrity_check=ok, 테이블 7개. 정상.

### 버그 스캔 🟢
- `baduk-api.err`에 `TypeError: bytes is not JSON serializable` 2건(라인 221457·221549, 파일 mtime 6/13 22:49).
- **재현 시점 검증**: 해당 트레이스는 옛 코드 레이아웃(val_handler가 line 21에서 직접 JSONResponse). 현재 `errors.py`는 `_safe_validation_errors`+`custom_encoder`로 이미 수정(commit ce9d745, 6/11 21:12). 현재 가동 프로세스 PID 923은 마지막 로그 기록(6/13 22:49) 이후 기동 → 수정 코드 반영. 6/13 22:49 이후 err 로그 무증가, 재발 없음.
- 결론: 이미 수정·배포 완료. 신규 bug 이슈 생성 안 함(누적 카운트 아닌 최근 재발 시점 기준 — [[bug-scan-verify-recency]]).

### 백로그 트리아지 🟢
- 미분류 열린 이슈 0건. 할 일 없음.

### PR 감시 🟢
- 열린 PR 0건. 주의 PR 없음.

### 상태 갱신
- `dashboard.md` 갱신(스택·백업·CWI ingest 6/14 03:04·주간 W24).
- 보류 승인 0건. incidents 신규 0건.

### Telegram 보고
- `reply`/telegram send 도구 세션 미노출(ToolSearch `telegram reply send` → Canva·PushNotification·Monitor만 매칭). 진실 공급원 `~/.claude/channels/telegram/.env`+chat_id(`ops/ops.env`)로 Bot API `sendMessage` 직접 호출 → **발송 성공**(ok, message_id 2245).

## 18:30 오케스트레이터 정기 실행

### 헬스체크 🟢
- prod-backend `{"status":"ok","db":true,"katago_alive":true}` 200 OK, prod-web 200 OK.
- launchctl: com.baduk.api PID 923 / com.baduk.web PID 912 가동.
- 디스크 5%, launchd plist drift 없음(`sync-launchd.sh --check` exit 0).
- staging: 중단(정책상 정상, 경보 아님).

### 백업 검증 🟢
- 최신 `baduk-20260614T040005.db.gz`(오늘 04:00, stale 아님).
- 티어 daily 14 / weekly 4 / monthly 1.
- 복원 드릴: integrity_check=ok, 테이블 7개. 정상.

### 버그 스캔 🟢
- `baduk-api.err`(mtime 6/13 22:49) 변동 없음. bytes-JSON 에러 2건은 12:30 분석과 동일 — 마지막 발생 line 221549, 이후 재기동 3회(44935→1496→78468→**923**)하며 현 프로세스는 수정본(commit ce9d745, 6/11) 가동. 재발 0건.
- 결론: 신규 bug 이슈 없음([[bug-scan-verify-recency]]).

### 백로그 트리아지 🟢
- 미분류 열린 이슈 0건. 할 일 없음.

### PR 감시 🟢
- 열린 PR 0건. 주의 PR 없음.

### 상태 갱신
- `dashboard.md` 타임스탬프 18:30 갱신. 보류 승인 0건, incidents 신규 0건.

### Telegram 보고
- reply/send 도구 세션 미노출(ToolSearch `telegram reply send message` → Canva·PushNotification·Monitor만). Bot API `sendMessage` 직접 호출(token `~/.claude/channels/telegram/.env` + chat_id `ops/ops.env`) → **발송 성공**(ok, message_id 2246).
