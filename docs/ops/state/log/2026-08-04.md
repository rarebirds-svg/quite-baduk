# 2026-08-04

- 02:00 dev-cycle: **처리할 버그 없음 — 이슈 0건·PR 0건으로 사흘 연속 무작업.**
  - **적격 이슈 0건.** `gh issue list --state open --json number,title,labels` = `[]`. 라벨 필터(`bug`·`small` 있고 `feature`·`in-progress` 없음)를 적용하기 전에 **열린 이슈 자체가 0건**이라 선택·선점 단계가 성립하지 않는다. `gh`는 정상 동작한다 — `--state all`은 #70·#65·#64 등 CLOSED 이슈를 정상 반환했으므로 인증·PATH 실패로 인한 빈 결과 오인이 아니다(#54류 exit 127 아님).
  - **잔여 선점 없음.** `gh issue list --state all --label in-progress` 빈 결과 = 중단된 이전 사이클이 남긴 `in-progress` 라벨 없음. 뗄 라벨도 없다.
  - **열린 PR 0건.** `gh pr list --state open` 빈 결과. 8/2·8/3과 동일하게 사람 머지를 기다리는 항목이 없다. 보류 승인도 0건(`pending-approvals.md`에 `상태: 대기` 0개).
  - **배포 갭 없음.** `git fetch origin` 후 `git rev-list --count HEAD..origin/main` = **0**, `HEAD` = `origin/main` = **fe4d755**. 8/2 22:11 사람 수동 배포 이후 새 유입이 없다(`[[merge-is-not-deploy]]`).
  - **워크트리 미사용 — 다만 `.worktrees/dev-cycle`가 머지 완료된 `fix/issue-70`(4ff419a)에 그대로 얹혀 있다.** origin 쪽 브랜치는 `gone` 상태다. 러닝북 1단계가 매 사이클 `origin/main` 기준으로 새 브랜치를 만들므로 기능상 무해하나, 다음에 착수할 이슈가 생기면 브랜치 생성 전에 `origin/main`으로 옮겨야 한다. 이번 사이클은 착수할 이슈가 없어 손대지 않았다 — 코드 변경 0건(이 로그 파일 제외).
  - **범위 판단 — 8/3과 동일.** 이슈 큐가 비었다는 이유로 임의의 리팩터링·개선을 자율 착수하지 않았다. dev-cycle은 이슈 큐를 소비하는 잡이고, 이슈를 자동 생성하는 잡은 오케스트레이터의 버그 스캔 단계(12:00·18:00)다. 사람이 이슈를 열거나 오케스트레이터·watchdog가 장애를 이슈로 승격하기 전까지 이 잡은 계속 무작업으로 종료한다.
  - **관찰 — 3일 연속 무작업은 큐 고갈 신호이지 파이프라인 고장 신호가 아니다.** 8/1 23:00 4건 머지로 백로그가 소진됐고, 그 뒤 오케스트레이터 버그 스캔이 8/3까지 신규 이슈 0건을 보고했다(재기동 후 무에러). 즉 유입원 자체가 조용한 상태다. 만약 이 상태가 더 길게 이어지면 dev-cycle의 매일 2회 기동 자체를 재검토할 여지가 있으나, 그건 🟡 판단이라 여기서 하지 않고 기록만 남긴다.
  - **Telegram 발송 완료 — message_id 2505.** `ToolSearch`로 실제 조회했으나 `reply` 도구가 이 세션에 미노출이라 Bot API 직접 호출로 발송했다. 한글 본문을 `/tmp` 파일에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"`로 보냈다(`[[dev-cycle-telegram-botapi]]`). 사전 환경 자가진단으로 "발송 불가" 판정하지 않았다.

- 12:00 오케스트레이터: **헬스·백업 전부 정상. 웹 로그로 스캔 범위를 넓혀 실제 결함 1건을 이슈화했다(#74).**
  - **헬스체크 — prod 정상.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web 200. launchctl `com.baduk.api` PID **93501** / `com.baduk.web` **93522** = 8/3 12:00·18:00에 이어 **3사이클 연속 동일**, `ps lstart` 둘 다 `Sun Aug 2 22:11:37 / 22:11:42` = 배포 이후 **38시간 무재기동**. 디스크 **6%**(경보선 90% 대비 여유), `ops/sync-launchd.sh --check` **exit 0·무출력** = plist drift 없음. staging은 backend·web 둘 다 중단이나 러닝북상 상시 가동이 필수가 아니므로 **중단으로만 기록(경보 아님)**. `katago_alive:true`라 lazy spawn 해석도 불필요. 신규 incident 0건.
  - **백업 검증 — 3항목 전부 통과.** 최신 `baduk-20260804T040005.db.gz`(8/4 04:00, **3,281,828B**), `find -mtime +1` 미출력 = 신선도 정상(생성 8시간 전). 티어 **daily 14 / weekly 8 / monthly 3** — daily 0개 아님. 복원 드릴 `PRAGMA integrity_check` = **ok**, 테이블 **9개** = 손상 없음. 8/3분 대비 +352B는 주간 ingest 없는 평일의 정상 증분이다.
  - **버그 스캔 — 백엔드 0건, 웹에서 신규 1건.** 백엔드 `.err`는 **225,409줄·mtime 8/2 22:11**로 8/3 18:00과 **완전히 동일** = 재기동 후 38시간 트레이스백 0건. 누적 `database is locked` 2,556건은 마지막 발생(line 225347)이 현 기동 마커(225406) *이전*이라 전부 배포 전 구간이다 — 카운트가 아닌 발생 시점으로 판정했다(`[[bug-scan-verify-recency]]`).
    - **범위를 `baduk-web.err`로 넓힌 것이 이번 사이클의 차이다.** 지금까지 스캔은 백엔드 `.err`만 봤는데, 백엔드가 조용해진 만큼 웹 쪽을 갈라 볼 여지가 생겼다.
    - **🆕 이슈 [#74](https://github.com/rarebirds-svg/quite-baduk/issues/74) 생성(`chore`)** — `next.config.js`가 웹 타깃에 `output: "standalone"`을 주는데 `web/package.json`의 `"start": "next start"`로 launchd가 구동해, 매 기동마다 Next.js가 `"next start" does not work with "output: standalone"` 경고를 낸다. 누적 **93회**이고 현 기동 구간에도 출력됐다. 라이브 장애는 아니다(`/` 200, 프로 상세·용어집 정상 — Next 14의 `next start`가 `.next`에서 계속 서빙). 다만 공식 미지원 조합이라 버전업에서 깨질 수 있고, 쓰지 않는 `web/.next/standalone/`(8/2 22:10 생성)을 매 빌드마다 만든다. 선택지 A(웹 타깃에서 `output: "standalone"` 제거) / B(`start`를 `node .next/standalone/server.js`로 전환, `public/`·`.next/static` 복사와 rewrite·redirect 재검증 필요)를 적었고 현 운영 형태(launchd + 작업 트리 직접 구동)에는 A가 맞다고 봤다. 수정은 dev-cycle 몫이다.
    - **이슈화하지 않은 것 — `Failed to find Server Action` 53건.** 러닝북의 "반복 2회 이상" 기준은 넘지만 **"일시적 잡음이 아니다" 기준에서 탈락**시켰다. 액션 ID 분포가 `"x"` **12건** · `"0000…0"` **11건** · 8자리 난수 다수로, 실제 빌드가 생성하는 40자리 해시와 형태가 다르다. 현 기동 이후로는 **38시간에 4건**뿐이고 전부 서로 다른 ID다 = 배포 전환기의 stale 클라이언트가 아니라 Server Action 엔드포인트를 긁는 외부 스캐너다. 앱은 에러 응답만 낼 뿐 크래시하지 않는다. 동반 관측된 `Could not find a production build` **7건**도 8/2 재빌드 창의 일시적 산물이라 제외했다.
  - **백로그 트리아지 — 대상 0건.** `gh issue list --state open --search "no:label"` 빈 배열. 이번에 만든 #74는 생성 시점에 `chore`를 붙여 미분류로 남지 않았다. "사람 분류 필요" 항목 없음.
  - **PR 감시 — 열린 PR 0건.** `gh pr list --state open` 빈 배열 = CI 실패·충돌·정체 해당 없음. 배포 갭도 없다 — `git rev-list --count HEAD..origin/main` = **0**, HEAD = `origin/main` = `fe4d755`(`[[merge-is-not-deploy]]`).
  - **트래픽 — 18:00→12:00 델타 445건, 5xx 0건.** 창 크기를 추정하지 않고 18:00 줄 번호(215,393)에서 직접 절단했다(`.log` 215,393 → **215,838 = 18시간 445줄**). 구성 — health **236**(약 4.6분 간격, 창 18h와 정합) · **프로 상세 165**(≈ **9.2건/h**) · themes 8 · sitemap 8 · 월간 픽 14 · `analytics/hit` 3 · session 3. 상태코드 **200 437 · 401 3 · 204 3 · 404 2**. 프로 상세 밀도가 18:00 창의 11.3건/h에서 9.2로 내려온 것은 크롤러 버스트의 정상 감쇠(전부 200)다. 404 2건은 `/api/.env` 스캐너 프로브로 기존 관측 그대로이며 경로가 실재하지 않아 404다 — 증가 없어 incident 미생성. 월간 픽 요청에 **2031~2032년 미래 월**이 섞인 것도 크롤러의 URL 패턴 확장 탐색이고 전부 200(빈 결과 정상 응답)이라 결함이 아니다.
  - **사람 방문 재개.** `visit_hits` **73행**(8/3 18:00 70행 → **+3**), 최신 `2026-08-04 00:15:27 UTC` = **8/4 09:15 KST**. 델타의 `analytics/hit` 3건과 정확히 정합 = 봇 필터를 통과한 실제 사람 유입이다. 18:00의 "6시간 사람 방문 0건"에서 회복됐다. `games` **336**(신규 대국 0건, #336의 8/1 19:15 종료 이후 3일째), `pro_games` **3111**(불변 — 주 1회 일요일 잡이라 다음 ingest는 8/9).
  - **보류 승인 0건.** `pending-approvals.md`의 "대기 중" 섹션 = `_(없음)_`. AP-20260802-01 완료 이후 큐가 비어 있다.
  - **자율성 준수.** 이번 사이클의 변경은 GitHub 이슈 생성 1건(🟢)과 `state/` 문서 갱신뿐이다. #74의 실제 수정(`next.config.js` 또는 `package.json` 변경 + prod 재빌드·재기동)은 코드 변경이자 prod 배포라 🟡이므로 착수하지 않았다.
  - **📌 미해결(사람 판단 대기) 유지** — 프롬프트·러닝북 5종의 주기 표기 "4회/일"(06:30·12:30·18:30·23:30)과 실제 plist "12:00·18:00 2회/일" 불일치. 8/3 두 사이클에 이어 이번에도 자율 변경하지 않았고 incident로도 올리지 않았다(`[[orchestrator-schedule-2x-not-4x]]`).
  - **Telegram 발송 완료 — message_id 2507.** `ToolSearch`로 실제 조회했으나 `reply` 도구가 이 세션에 미노출이라 Bot API 직접 호출로 발송했다. 한글 본문을 `/tmp` 파일에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"`로 보냈다(`[[dev-cycle-telegram-botapi]]`). 토큰·chat_id는 `~/.claude/channels/telegram/{.env,access.json}`에서 읽었고, 환경 변수 자가진단으로 "발송 불가"를 사전 판정하지 않았다. 인바운드 승인 답신은 없었다(보류 큐가 비어 있어 처리할 항목도 없음).

- 18:00 오케스트레이터: **헬스·백업 전부 정상. 6시간 델타에서 프로 상세 897건 = 관측 이래 최고 밀도인데 5xx 0건 — #66 원자적 UPDATE의 최강 부하 실증이다.**
  - **헬스체크 — prod 정상.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web 200. launchctl `com.baduk.api` PID **93501** / `com.baduk.web` **93522** = 8/3 12:00·18:00, 8/4 12:00에 이어 **4사이클 연속 동일** = 8/2 22:11 배포 이후 **44시간 무재기동**. 디스크 **6%**(경보선 90% 대비 여유), `ops/sync-launchd.sh --check` **exit 0·무출력** = plist drift 없음. staging은 backend·web 둘 다 중단이나 러닝북상 상시 가동이 필수가 아니므로 **중단으로만 기록(경보 아님)**. watchdog 최근 사이클 `신규 incident 0건`·`[api] OK`·`[web] OK`. 신규 incident 0건.
  - **백업 검증 — 3항목 전부 통과.** 최신 `baduk-20260804T040005.db.gz`(8/4 04:00, **3,281,828B**), `find -mtime +1` 미출력 = 신선도 정상(생성 14시간 전). 티어 **daily 14 / weekly 8 / monthly 3** — daily 0개 아님. 복원 드릴 `PRAGMA integrity_check` = **ok**, 테이블 **9개** = 손상 없음. 12:00에 이어 **2회 연속 ok**.
  - **🟢 트래픽 — 12:00→18:00 델타 1,003건, 5xx 0건. 프로 상세가 6시간에 897건(≈149.5건/h)으로 폭증했다.** 창 크기를 추정하지 않고 12:00 줄 번호(215,838)에서 직접 절단했다(`.log` 215,838 → **216,841 = 6시간 1,003줄**). 구성 — **프로 상세 897** · health **79**(약 4.6분 간격, 창 6h와 정합) · 월간 픽 8 · `analytics/hit` 5 · sitemap 5 · session 5 · themes 4. **상태코드 200 993 · 401 5 · 204 5 = 4xx 프로브 0건·5xx 0건.**
    - **밀도가 직전 창의 16배다** — 12:00 창 9.2건/h → **149.5건/h**. 관측 이래 최고였던 8/2 18:00의 24건/h도 6배 넘게 갈아치웠다.
    - **소스는 전부 `127.0.0.1`(914건)** = 외부 크롤러가 웹 티어(:3000)를 긁고 Next SSR이 백엔드(:8000)로 프록시한 것이다(프로 상세 SSR #50 구조). 즉 백엔드 직격이 아니라 정상 경로다.
    - **고유 기보 id 894종 / 897요청** = 중복 3건뿐. 크롤러가 sitemap을 따라 **거의 전부 서로 다른 기보를 순차 스윕**했다는 뜻이다.
    - **이것이 #66의 가장 강한 실증이다.** 프로 상세 조회는 매 요청이 조회수 UPDATE를 유발하므로, 894개 서로 다른 행에 6시간 동안 쓰기가 몰렸는데 **`database is locked` 0건·500 0건**이다. 8/2 배포 전이라면 #65가 재현됐을 부하다.
  - **버그 스캔 — 신규 0건.** 백엔드 `.err`는 **225,409줄·mtime `Aug 2 22:11`**로 12:00과 **바이트 단위 동일** = 재기동 후 **44시간 트레이스백 0건**이며, 위 최고 밀도 구간을 통째로 포함한다. 마지막 `database is locked`(line 225347)은 현 기동 마커(225406) *이전*이라 여전히 전부 배포 전 구간이다 — 카운트가 아닌 발생 시점으로 판정했다(`[[bug-scan-verify-recency]]`). `.log` 쪽 마지막 5xx는 line 175,237로 **끝에서 41,604줄 전**이며 전부 `/api/spectate/pro/*` 500 = 배포 전 #65 구간이다.
    - **웹 `.err`도 조용하다** — 2,975줄 / mtime `Aug 3 06:16`으로 12:00과 동일, `standalone` 경고 누적도 **93건 그대로**(웹 재기동이 없었으므로 증가분 0). #74는 열린 채 유지, 중복 이슈 미생성.
    - **이슈화하지 않은 것 — 월간 픽의 미래 월 요청.** 12:00에 "빈 결과 정상 응답"으로 적었으나 이번에 **실제 응답을 확인해 서술을 정정한다.** `/pick/monthly/2032-09`는 빈 결과가 아니라 정상 기보 1건(id 2650, `2002-09-22`)을 반환한다 — 이 엔드포인트는 실제 날짜가 아니라 `yyyymm` 문자열을 시드로 기보를 결정론적으로 고르는 편집 기능이라 미래 월도 유효하다. sitemap에는 미래 월이 없으므로(`203x` grep 0건) 크롤러의 URL 패턴 확장 탐색이며, 설계대로 200이다. 결함 아님.
  - **백로그 트리아지 — 대상 0건.** `gh issue list --state open --search "no:label"` 빈 배열. 열린 이슈는 #74 하나이고 생성 시 `chore`가 붙어 미분류가 아니다. "사람 분류 필요" 항목 없음.
  - **PR 감시 — 열린 PR 0건.** `gh pr list --state open` 빈 배열 = CI 실패·충돌·정체 해당 없음. 배포 갭도 없다 — `git rev-list --count HEAD..origin/main` = **0**(`[[merge-is-not-deploy]]`).
  - **사람 방문 지속.** `visit_hits` **78행**(12:00 73행 → **+5**), 최신 `2026-08-04 06:14:43 UTC` = **8/4 15:14 KST**. 델타의 `analytics/hit` 5건과 정확히 정합 = 봇 필터를 통과한 실제 사람 유입이다. 위 897건 크롤러 버스트와는 별개 신호다. `games` **336**(신규 대국 0건, #336의 8/1 19:15 종료 이후 3일째), `pro_games` **3111**(불변 — 주 1회 일요일 잡이라 다음 ingest는 8/9).
  - **보류 승인 0건.** `pending-approvals.md`의 "대기 중" 섹션 = `_(없음)_`. AP-20260802-01 완료 이후 큐가 비어 있다. 인바운드 승인 답신 없음(launchd 정시 기동 세션).
  - **자율성 준수.** 이번 사이클의 변경은 `state/` 문서 갱신뿐이다. 이슈·PR·코드 변경 0건. #74의 수정은 코드 변경 + prod 재빌드·재기동이라 🟡이므로 이번에도 착수하지 않았고 dev-cycle 큐에 남겨 뒀다.
  - **📌 미해결(사람 판단 대기) 유지** — 프롬프트·러닝북 5종의 주기 표기 "4회/일"(06:30·12:30·18:30·23:30)과 실제 plist "12:00·18:00 2회/일" 불일치. 8/3·8/4 사이클에 이어 이번에도 자율 변경하지 않았고 incident로도 올리지 않았다(`[[orchestrator-schedule-2x-not-4x]]`).
  - **Telegram 발송 완료 — message_id 2508.** `ToolSearch`로 실제 조회했으나 `reply` 도구가 이 세션에 미노출이라 Bot API 직접 호출로 발송했다. 한글 본문을 `/tmp` 파일에 쓰고 `LC_ALL=en_US.UTF-8 curl --data-urlencode "text@파일"`로 보냈다(`[[dev-cycle-telegram-botapi]]`). 토큰·chat_id는 `~/.claude/channels/telegram/{.env,access.json}`에서 읽었고, 환경 변수 자가진단으로 "발송 불가"를 사전 판정하지 않았다.
