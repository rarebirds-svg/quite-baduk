# 운영 대시보드

- 갱신: 2026-09-12 21:00

## 스택 상태

| 스택 | 상태 | 마지막 확인 |
|---|---|---|
| prod | **✅ 정상 — 승인 3건 발효(13:41~13:47) 후 첫 정기 점검, api 재기동 후 7시간 무사고.** 9/12 21:00 헬스체크 전 항목 통과 — `/api/health` 200 `migrations {current:0020, head:0020, pending:false}`, web 200, `com.baduk.api` PID 5743(**13:46:27 재기동** — #83·#86·#95 발효)·`com.baduk.web` PID 89541(9/8 23:56 기동 유지 — 오늘 머지 3건은 backend 7파일뿐이라 web 재빌드·재기동 불필요), 디스크 8%, drift 없음, Claude 인증 D-20, `/api/spectate` 200. 09:00 이후 `.err` +25줄 전부 재기동 블록(alembic 자동 upgrade 2줄·startup complete)·WS accepted(#467~#469), **트레이스백 0·IntegrityError 0·locked 0**, `.log` +738줄 **5xx 0·429 0**(401 11은 첫 방문 정상, 404 19는 34.x GCP 스캐너의 `.env`·graphql·windmill 경로 순회 프로브 차단, 127.0.0.1 모니터 프로브 489건). `baduk-web.err` +0줄. **오늘 낮 대국 3건·46수(#467 13:30~13:49 16수, 재기동 후 #468·#469 16:37~16:43 30수 = 새 코드 착수 정상)**, 마지막 수 16:43 KST, `visit_hits` 544(+18/12h).<br>(이전) **✅ 정상 — 복구 후 81시간 무사고.** 9/12 09:00 헬스체크 전 항목 통과 — `/api/health` 200 `migrations {current:0020, head:0020, pending:false}`, web 200, `com.baduk.api` PID 89132·`com.baduk.web` PID 89541(9/8 23:55·23:56 기동 유지), 디스크 7%, drift 없음, Claude 인증 D-20, `/api/spectate` 200. 9/11 21:00 이후 `.err` **+0줄**·**트레이스백 0**, `.log` +957줄 **5xx 0·429 0**(401 2는 첫 방문 정상, 404 3은 `.env`·graphql 스캐닝 봇 차단, 증가분 대부분 Cloudflare 모니터 프로브 775건). `baduk-web.err` +22줄은 Server Action id `"x"` 스캐너 프로브 2건(빌드 무관, 기준선 3,451줄로 갱신). **밤사이 착수 0수·신규 대국 0건(마지막 수 9/10 14:45 KST 이후 42시간 무활동 — 사용자 유입 없음, 장애 신호 아님)**, `visit_hits` 526(+4/12h).<br>(이전) **✅ 정상 — 복구 후 69시간 무사고.** 9/11 21:00 헬스체크 전 항목 통과 — `/api/health` 200 `migrations {current:0020, head:0020, pending:false}`, web 200, `com.baduk.api` PID 89132·`com.baduk.web` PID 89541(9/8 23:55·23:56 기동 유지), 디스크 7%, drift 없음, Claude 인증 D-21, `/api/spectate` 200. 09:00 이후 `.err` **+0줄**·**트레이스백 0**, `.log` +1,052줄 **5xx 0·429 0**(401 6은 첫 방문 정상, 404 2는 `.env` 스캐닝 봇 차단, 증가분 대부분 Cloudflare 모니터 프로브 887건). **낮 동안 착수 0수·신규 대국 0건(마지막 수 9/10 14:45 KST 이후 30시간 무활동 — 사용자 유입 없음, 장애 신호 아님)**, `visit_hits` 522(+5/12h).<br>(이전) **✅ 정상 — 복구 후 57시간 무사고.** 9/11 09:00 헬스체크 전 항목 통과 — `/api/health` 200 `migrations {current:0020, head:0020, pending:false}`, web 200, `com.baduk.api` PID 89132·`com.baduk.web` PID 89541(9/8 23:55·23:56 기동 유지), 디스크 7%, drift 없음, Claude 인증 D-21, `/api/spectate` 200. 9/10 21:00 이후 `.err` **+0줄**·**트레이스백 0**, `.log` +860줄 **5xx 0·429 0·404 0**(401 1은 첫 방문 정상, 증가분 대부분 Cloudflare 모니터 프로브). **밤사이 착수 0수·신규 대국 0건(마지막 수 9/10 14:45 KST, 정상 야간)**, `visit_hits` 517(+1/12h).<br>(이전) **✅ 정상 — 복구 후 48시간 무사고.** 9/10 21:00 헬스체크 전 항목 통과 — `/api/health` 200 `migrations {current:0020, head:0020, pending:false}`, web 200, `com.baduk.api` PID 89132·`com.baduk.web` PID 89541(9/8 23:55·23:56 기동 유지), 디스크 7%, drift 없음, Claude 인증 D-22. 09:00 이후 `.err` +9줄 전부 WS accepted(#457·#460~#466)·**트레이스백 0**, `.log` +600줄 **5xx 0·429 0**(401 7은 첫 방문 정상, 404 6은 `.env` 스캐닝 봇 차단, 증가분 대부분 Cloudflare 모니터 프로브). **착수 76수·신규 대국 3건(#464~466, #465 resigned 74수), 마지막 수 14:45 KST**, 이후 6시간 착수 0, 실사용자 IP 4개, `visit_hits` 516(+10/12h).<br>(이전) **✅ 정상 — 복구 후 36시간 무사고.** 9/10 09:00 헬스체크 전 항목 통과 — `/api/health` 200 `migrations {current:0020, head:0020, pending:false}`, web 200, `com.baduk.api` PID 89132·`com.baduk.web` PID 89541(9/8 23:55·23:56 기동 유지), 디스크 7%, drift 없음, Claude 인증 D-22, `/api/spectate` 200. 9/9 21:00 이후 `.err` +18줄 전부 WS accepted(#462·#463)·**트레이스백 0**, `.log` +4,270줄 **5xx 0·429 0**(401 2는 첫 방문 정상, 증가분 대부분 Cloudflare 모니터 프로브). **착수 179수·신규 대국 1건(#463 9×9, #462·#463 finished), 마지막 수 9/9 21:30 KST**, 밤사이 착수 0(정상 야간), `visit_hits` +2/12h.<br>(이전) **✅ 정상 — 복구 후 첫 온전한 낮 12시간.** 9/9 21:00 헬스체크 전 항목 통과 — `/api/health` 200 `migrations {current:0020, head:0020, pending:false}`, web 200, `com.baduk.api` PID 89132·`com.baduk.web` PID 89541(9/8 23:55·23:56 기동 유지), 디스크 7%, drift 없음, Claude 인증 D-23, `/api/spectate` 200. 09:00 이후 `.err` +18줄 전부 WS accepted(#457~462)·**트레이스백 0**, `.log` +1,084줄 **5xx 0·429 0**(401 6은 첫 방문 정상). **착수 222수·신규 대국 6건(#457~462, 전부 active), 마지막 수 21:01 KST**, 실사용자 IP 5개, `visit_hits` +12/12h.<br>(이전) **✅ 정상 — OPS-20260907-01 해소(9/8 23:55 사람 배포, 장애 약 48시간).** 9/9 09:00 헬스체크 전 항목 통과 — `/api/health` 200에 #96이 노출한 `migrations {current:0020, head:0020, pending:false}`, web 200, `com.baduk.api` PID 89132(9/8 23:55:44 기동)·`com.baduk.web` PID 89541(23:56:43, BUILD_ID 23:56 재빌드), 디스크 7%, drift 없음, Claude 인증 D-23. `alembic_version` **0020**(사람이 #96 머지 23:52 → pull 23:55 → api 재기동 시 자동 `alembic upgrade` 0019→0020). 직접 프로브 `/api/spectate` **200**. 복구 후 `.err` 트레이스백 0·`.log` 500 0, **착수 재개 — 복구 후 110수·대국 2건(#454~456), 마지막 수 9/9 03:32 KST**, 세션 발급 201 2건, `visit_hits` +17/12h. 복구 전 자정 직전 실사용자 1명(IP 1개) 세션 500 8건이 마지막 피해.<br>(이전) **❌ 반쪽 장애 지속 45시간 — OPS-20260907-01 (에스컬레이션, AP-20260907-01 승인 미회신 36시간째).** 9/8 21:00 헬스체크 항목은 전부 통과(`/api/health` 200·web 200·api PID 28544·web 28792 유지·디스크 7%·drift 없음·Claude 인증 D-24)하나 `alembic_version` 여전히 `0019`(head `0020_accounts`), `/api/spectate` 500 재현. 09:00 이후 실사용자 500 13건(IP 3개, 세션 GET 7·POST 6) + 재시도 429 11건, `visit_hits` +7 전부 세션 단계에서 막힘, 착수 0건 46시간째.<br>(이전) **❌ 반쪽 장애 지속 33시간 — OPS-20260907-01 (에스컬레이션, AP-20260907-01 승인 미회신 24시간째).** 9/8 09:00 헬스체크 항목은 전부 통과(`/api/health` 200·web 200·api PID 28544·web 28792 유지·디스크 7%·drift 없음·Claude 인증 D-24)하나 `alembic_version` 여전히 `0019`(head `0020_accounts`), `/api/spectate` 500 재현. 21:00 이후 `.err` +167줄·신규 트레이스백 **1건**(09:00 자체 프로브), `.log` +788줄 중 500 1건(같은 프로브) — **실사용자 500 0건 = 세션 보유 실사용자 요청 자체가 밤 12시간 0건.** **착수 0건 33시간째**(마지막 수 9/6 23:18 KST), `visit_hits` 469(**+0/12h**, 8/17 이후 최초). 날짜 전환으로 once_key 억제 해제 → `kind: alert` 재발송 + am 다이제스트 ❌ + 승인 카드.<br>(이전) 9/7 21:00 **❌ 반쪽 장애 지속 21시간 — OPS-20260907-01 (에스컬레이션, AP-20260907-01 승인 미회신 12시간째).** 21:00 헬스체크 항목은 전부 통과(`/api/health` 200·web 200·api PID 28544·web 28792 유지·디스크 7%·drift 없음·Claude 인증 D-25)하나 `alembic_version` 여전히 `0019`(head `0020_accounts`), `/api/spectate` 500 재현. 09:00 이후 `.err` +2,318줄·신규 트레이스백 **14건**(`sessions.account_id` SELECT 10·**INSERT 2**·`games.account_id` 2), `.log` +390줄 중 500 **14건** — `GET /api/session` 10(실사용자 IP 3개)·**`POST /api/session` 2(세션 신규 생성도 500 — 오전의 '신규 방문 정상' 판정 정정, 신규 방문자도 대국 착수 불가)**·`/api/spectate` 2(프로브). **00:06 이후 착수 0건 21시간째**(12h 0수, 마지막 수 9/6 23:18 KST), `visit_hits` 469(+8/12h). web `.err` 3383(+0)·Server Action 89(+0). 재경보는 once_key 일 1회 억제로 생략, pm 다이제스트 ❌ + 승인 카드로 재통보.<br>(이전) 9/7 09:00 **❌ 반쪽 장애 — OPS-20260907-01 (에스컬레이션, AP-20260907-01 승인 대기).** `/api/health` 200·web 200·디스크 7%·plist drift 없음·**Claude 인증 D-25**로 헬스체크 항목은 전부 통과하지만, 9/7 00:06 사람 배포(#89~#93 pull·재빌드·재기동)에서 `alembic upgrade head`가 빠져 `alembic_version`=`0019`, 코드는 `0020_accounts`의 `games.account_id`·`sessions.account_id`를 읽는다. `.err` 신규 트레이스백 **12건**(`no such column`), `.log` 최근 3천줄 **5xx 12건**(`/api/spectate` 10·`/api/session` 2), 직접 프로브 `/api/spectate` 500 재현. **00:06 이후 착수 0건·WS 0건** — 세션 보유 재방문자는 이어보기·대국 불가, 관전 목록 불가. 신규 방문·일일 도전·프로 기보는 정상. 09:00 즉시 경보 발송 성공. 사본 드라이런 0.44초·integrity ok로 절차 검증 완료. 9/6 밤 신규 대국 9건(#445~453, 마지막 수 23:18 KST), active 잔류 6건, `visit_hits` **461**(+26/12h). web `.err` Server Action 89건(+0).<br>(이전) 9/6 21:00 정상 — 신규 대국 13건(#432~444), 5xx 0건, 트레이스백 0건, Claude 인증 D-26.<br>(이전) 9/6 09:35 정상 — 신규 대국 19건(#413~431), `database is locked` 1건(주간 ingest 락 → 이슈 #87). | 2026-09-12 21:00 |
| staging | 중단 (정책상 정상) | 2026-09-12 21:00 |

## 백업 상태

| 항목 | 값 |
|---|---|
| 최신 백업 | **2026-09-12T04:00** (baduk-20260912T040001.db.gz, 4,437,307B) — 9/12 21:00 재검증 신선(`find -mtime +1` 출력 없음), integrity_check ok·테이블 11·사본 `pro_games` 4111 = **74회 연속** 통과, daily 14 / weekly 8 / monthly 4.<br>(이전) **2026-09-12T04:00** (baduk-20260912T040001.db.gz, 4,437,307B, +350B) — 9/12 09:00 검증 신선, integrity_check ok·테이블 11·사본 `pro_games` 4111 = **73회 연속** 통과, daily 14 / weekly 8 / monthly 4(8/29분 보존 정리).<br>(이전) **2026-09-11T04:00** (baduk-20260911T040004.db.gz, 4,436,957B) — 9/11 21:00 재검증 신선, integrity_check ok·테이블 11·사본 `pro_games` 4111 = **72회 연속** 통과, daily 14 / weekly 8 / monthly 4.<br>(이전) **2026-09-11T04:00** (baduk-20260911T040004.db.gz, 4,436,957B, +452B) — 9/11 09:00 검증 신선, integrity_check ok·테이블 11·사본 `pro_games` 4111 = **71회 연속** 통과, daily 14 / weekly 8 / monthly 4.<br>(이전) **2026-09-10T04:00** (baduk-20260910T040005.db.gz, 4,436,505B) — 9/10 21:00 재검증 신선, integrity_check ok·테이블 11·사본 `pro_games` 4111 = **70회 연속** 통과, daily 14 / weekly 8 / monthly 4.<br>(이전) **2026-09-10T04:00** (baduk-20260910T040005.db.gz, 4,436,505B, +2,687B) — 9/10 09:00 검증 신선, integrity_check ok·테이블 11·사본 `games max(id)` 463·`pro_games` 4111 = **69회 연속** 통과, daily 14 / weekly 8 / monthly 4. 8/27분 daily 보존 정리.<br>(이전) **2026-09-09T04:00** (baduk-20260909T040005.db.gz, 4,433,818B) — 9/9 21:00 재검증 신선, integrity_check ok·테이블 11·사본 `alembic_version` 0020 = **68회 연속** 통과, daily 14 / weekly 8 / monthly 4.<br>(이전) **2026-09-09T04:00** (baduk-20260909T040005.db.gz, 4,433,818B, +66,509B — 복구 후 착수 재개 반영) — 9/9 09:00 검증 신선, integrity_check ok·테이블 **11**(`accounts` 추가)·사본 `alembic_version` **0020** = **67회 연속** 통과, daily 14 / weekly 8 / monthly 4. 마이그레이션 적용 후 첫 백업.<br>(이전) **2026-09-08T04:00** (baduk-20260908T040005.db.gz, 4,367,309B) — 9/8 21:00 재검증 신선, integrity_check ok·테이블 10·사본 `alembic_version` 0019 = **66회 연속** 통과, daily 14 / weekly 8 / monthly 4. AP-20260907-01 마이그레이션 직전 복원점.<br>(이전) **2026-09-08T04:00** (baduk-20260908T040005.db.gz, 4,367,309B, +1,555B — 착수 0건이라 증분 최소) — 신선, integrity_check ok·테이블 10·사본 `alembic_version` 0019 = **65회 연속** 통과, daily 14 / weekly 8 / monthly 4. 이 백업도 AP-20260907-01 마이그레이션 직전 복원점이다.<br>(이전) 9/7 04:00 baduk-20260907T040004.db.gz 4,365,754B, 64회 연속.<br>(이전) 9/6 04:00 baduk-20260906T040002.db.gz 4,318,240B, 62회 연속.<br>(이전) 9/5 검증 60회 연속 (baduk-20260905T040002.db.gz, 4,125,268B).
| daily / weekly / monthly | **14 / 8 / 3** (8/16 09:00 실측). 8/16 04:00 생성과 함께 daily 8/2분·weekly 6/21분이 보존 정리돼 개수 유지. |

## 콘텐츠 인덱스

| 항목 | 값 |
|---|---|
| 프로 기보 수 | **4111** (9/6 09:35 DB 실측 `SELECT count(*) FROM pro_games`). 8/30의 3911에서 **+200** = 9/6 03:17 주간 ingest `new=200`(id 3912~4111)과 정합, 백업 크기 +192,972B로도 교차 확인.<br>(이전) **3911** (8/30 09:00 DB 실측). 8/16의 3511에서 +400 = 8/23·8/30 두 번의 주간 ingest(각 +200)와 정합.<br>(이전) **3511** (8/16 09:00 DB 실측). 직전 3311(8/9)에서 +200 = 8/16 03:13 주간 ingest `new=200`과 정합.<br>(이전) **3311** (8/9 12:00 DB 실측 `SELECT count(*) FROM pro_games`). 직전 3111(8/2)에서 **+200** = 8/9 03:12 주간 ingest의 `new=200`과 정확히 정합하며, 같은 증가가 백업 크기 +117,938B·sitemap +200으로도 확인됐다.<br>(이전) **3111** (8/2 18:00 DB 실측 `SELECT count(*) FROM pro_games`). 직전 2911(7/26)에서 **+200** = 8/2 03:12 주간 ingest의 `new=200`과 정확히 정합. (이전) 2911 — 7/26 12:00 실측. 그 이전 표기 `1111(825+286)`은 1800건 벌어진 stale 값이었다. **이 행은 매주 일요일 ingest 이후 갱신할 것.** |
| 테마 수 | 6 |
| 월간 픽 URL | 14 (최근 12개월 + 현재 + 다음 달) |
| sitemap URL 수 | **4188** (9/6 09:35 prod `/sitemap.xml` `<loc>` 실측). 8/30의 3986에서 **+202** = 9/6 ingest(+200) + 글로서리·FAQ 각 1편, force-dynamic이라 재빌드 없이 반영.<br>(이전) **3986** (8/30 09:00 실측). 8/16의 3579에서 +407.<br>(이전) **3579** (8/16 09:00 실측). 8/9의 3379에서 +200 = 8/16 ingest와 정합.<br>(이전) **3379** (8/9 12:00 prod `/sitemap.xml` `<loc>` 실측). 7/26의 2979에서 **+400** = 8/2·8/9 두 번의 ingest(각 +200)와 정합. **재빌드 없이 반영됐다** — 목록이 `force-dynamic`이라 ingest만으로 라이브 sitemap이 늘어난다. |
| 최근 CWI ingest | **2026-09-06 03:17** (fetched=3300 new=200 duplicate=3100 error=0, cap=200 도달) — 일요일 정기 실행, **IndexNow 200건 제출 성공**. 다음 실행 9/13. **주의 — 이번 실행이 진행 중 대국 #427을 `database is locked`로 끊었다**(단일 트랜잭션 200건 적재로 쓰기 락 60초 보유, 이슈 #87). 03시대에 대국이 있으면 매주 재현 가능.<br>(이전) **2026-08-30 03:16** (fetched=3100 new=200 duplicate=2900 error=0) — IndexNow 200건 성공.<br>(이전) **2026-08-16 03:13** (fetched=2660 new=200 duplicate=2460 error=0, cap=200 도달) — IndexNow는 코드 미발효로 미작동.<br>(이전) **2026-08-09 03:12** (fetched=2460 new=200 duplicate=2260 error=0, cap=200 도달). 8/2 12:00에 예고한 "다음 실행 8/9"가 예정대로 수행됐다. **주 1회(일요일 03:00) 잡** — 실행 이력 6/14·6/21·6/28·7/5·7/12·7/19·7/26·8/2·8/9가 전부 일요일. 평일에 "최근 실행이 며칠 전"인 것은 stale이 아니며 다음 실행은 8/16. |
| 이 달의 명국 | 353 |
| 글로서리 | **32편** (9/6 09:35 `web/content/glossary/*.md` 실측) — 최근 게시 `bokgi`, 그 전 `beollim`(8/29).<br>(이전) **31편** (8/30 09:00 실측).<br>(이전) **29편** (8/22 09:00 실측) — `badukpan`(8/22 02:00, e896576).<br>(이전) **28편** (8/19 09:00 실측) — 1차 시드 18편 소진(7/8) 후 2차 시드로 재개, 최근 게시 `ai-chuchunsu`(8/19 02:00, df52866) |
| FAQ | **13편** (9/6 09:35 실측) — 최근 게시 `baduk-vs-omok`(9/5 02:00, 1fc0f48). 2차 시드로 재개.<br>(이전) 9/9 — 1차 시드 소진 |
| sitemap 글로서리·FAQ URL | 27 (글로서리 18 + FAQ 9, prod sitemap 실측 7/8) |

## 분석

| 항목 | 값 |
|---|---|
| 최근 주간 리포트 | **2026-W36.md** (9/6 09:01 생성 — 방문 **122 hits·47 visitors**(전주 67, +55), 게임 **70건**(전주 6), 세션 7, 글로서리 32(+1)·FAQ 13(+1), 보류 승인 2건). `/api/stats` 401로 DB 직접 조회. 상태파일 정상 갱신.<br>(이전) **2026-W35.md** (8/30 09:01 — 방문 67건, 게임 6건).<br>(이전) **2026-W33.md** (8/16 09:01 생성 — 대국 **1국**(#338, 8/11)/누적 **338국**, `visit_hits` 이번 주 **47건**(전주 56, -16%), 유입 소스 search 25 · direct 22 — 네이버 모바일 15건 중심, 착지 대부분 글로서리(37). 4주 추이 18 → 5 → 1 → **1**. 신규 콘텐츠 0편.<br>(이전) **2026-W32.md** (8/9 09:00 생성 — 대국 **1국**(전주 5국 대비 -80.0%)/누적 **337국**, `visit_hits` 이번 주 56건(전주 27), `session_history` 신규 5건). **사용량 3주 연속 감소(18 → 5 → 1국)** 이며 8/6 이후 신규 대국이 0건이다. 인프라는 전 항목 정상이므로 장애가 아니라 유입 문제다.<br>(이전) **2026-W31.md** (8/2 09:00 생성 — 대국 **5국**(전주 18국 대비 -72.2%)/누적 **336국**). W32는 미생성이나 **stale 아님** — `com.inkbaduk.analytics-weekly`는 `Weekday 0` = 주 1회 일요일 잡이라 다음 실행은 **8/9(일)** 이고 그때 W32가 나온다. 참고 — 리포트 이후 8/6 21:55 KST에 대국 **#337**이 추가돼 DB 실측 누적은 **337국**, `visit_hits`는 **108행**(8/7 18:00 100행)이다. |
| 누적 리포트 수 | 13 |
| analytics-weekly 상태파일 | **✅ 8/16 09:01 최초 생성 — 배선 정상 확정.** `{"status":"ok","summary":"2026-W33 리포트 생성 — 방문 47건","at":"2026-08-16T09:01:14+09:00"}`. 8/15까지 걸어 둔 "미생성이면 배선 결함" 전환 조건은 발동하지 않았다. 이로써 `state/jobs/*.json` 3잡(dev-cycle·content-draft·analytics-weekly) 체계 완성.<br>(이전) **없는 것이 정상 (8/15 21:00 재확인 — 오늘까지만).** plist가 `Weekday 0` = 일요일 09:00 주 1회이고 마지막 실행이 **8/9 09:01**인데, `state/jobs/*.json` 기록 방식(`report-job-status.sh`)은 같은 날 **19:38**(`0c9fdd2`)에 도입됐다 — 아직 이 방식으로 실행된 적이 한 번도 없다. `docs/ops/analytics-prompt.md`에는 호출이 이미 들어 있으므로 첫 상태파일은 **8/16(일)** 실행분이다. 파일 부재를 `fail`로 읽지 말 것. **8/16 실행 후에도 파일이 없으면 그때는 배선 결함**이니 이 행을 결손으로 전환할 것. |

## 개발 현황

| 항목 | 값 |
|---|---|
| 열린 이슈 | **(9/12 21:00) 0건 — 미분류 0건.** 13:41~13:47 #83·#86·#95 머지로 #81·#84·#87 전부 자동 종료. 열린 `bug` 이슈 0건 → 내일 04:30 dev-cycle은 '처리할 버그 없음' 예상(`.worktrees/dev-cycle`은 머지된 `fix/issue-87` 26ddff7에 머묾, 미커밋 없음).<br>(이전) **(9/12 09:00) 3건 — 미분류 0건, 변동 없음.** #87 → PR #95, #84 → PR #86, #81 → PR #83 전부 머지 대기. 04:30 dev-cycle '처리할 버그 없음' 6일 연속.<br>(이전) **(9/11 21:00) 3건 — 미분류 0건, 변동 없음.** #87 → PR #95, #84 → PR #86, #81 → PR #83 전부 머지 대기.<br>(이전) **(9/11 09:00) 3건 — 미분류 0건, 변동 없음.** #87 → PR #95, #84 → PR #86, #81 → PR #83 전부 머지 대기. 04:30 dev-cycle '처리할 버그 없음' 5일 연속.<br>(이전) **(9/10 21:00) 3건 — 미분류 0건, 변동 없음.** #87 → PR #95, #84 → PR #86, #81 → PR #83 전부 머지 대기.<br>(이전) **(9/10 09:00) 3건 — 미분류 0건, 변동 없음.** #87 → PR #95, #84 → PR #86, #81 → PR #83 전부 머지 대기. 04:30 dev-cycle '처리할 버그 없음' 4일 연속.<br>(이전) **(9/9 21:00) 3건 — 미분류 0건, 변동 없음.** #87 → PR #95, #84 → PR #86, #81 → PR #83 전부 머지 대기.<br>(이전) **(9/9 09:00) 3건 — 미분류 0건, 변동 없음.** #87 → PR #95, #84 → PR #86, #81 → PR #83 전부 머지 대기. 04:30 dev-cycle '처리할 버그 없음' 2일 연속.<br>(이전) **(9/8 21:00) 3건 — 미분류 0건, 변동 없음.** #87 → PR #95, #84 → PR #86, #81 → PR #83 전부 머지 대기.<br>(이전) **(9/8 09:00) 3건 — 미분류 0건, 변동 없음.** #87 → PR #95, #84 → PR #86, #81 → PR #83 전부 머지 대기. 04:30 dev-cycle은 처리할 버그 없음으로 코드 변경 0.<br>(이전) **(9/7 21:00) 3건 — 미분류 0건, 변동 없음.** #87 → PR #95, #84 → PR #86, #81 → PR #83 전부 머지 대기.<br>(이전) **(9/7 09:00) 3건 — 미분류 0건.** #87 → **PR [#95](https://github.com/rarebirds-svg/quite-baduk/pull/95)**(9/7 04:30 dev-cycle, AP-20260907-02 등재). #81 → PR #83, #84 → PR #86 머지 대기. 오늘 장애(OPS-20260907-01)는 배포 절차 결손이라 이슈화하지 않음.<br>(이전) (9/6 21:00) 3건 — #87은 다음 dev-cycle 대상.<br>(이전) **(9/6 09:35) 3건 — 미분류 0건.** **[#87](https://github.com/rarebirds-svg/quite-baduk/issues/87) `bug` 신규** — 주간 CWI ingest 단일 트랜잭션이 쓰기 락 60초 보유 → 진행 중 대국 착수 `database is locked`(game 427 실측). 다음 dev-cycle 대상. [#81](https://github.com/rarebirds-svg/quite-baduk/issues/81) `bug` — PR #83, 머지 대기(AP-20260903-01). [#84](https://github.com/rarebirds-svg/quite-baduk/issues/84) `bug` — PR #86, 머지 대기(AP-20260905-01).<br>(이전) **(9/5 21:00) 2건** — 둘 다 PR 대기.
| 열린 PR | **(9/12 21:00) 0건 — 열린 PR 없음.** #83(9일 9시간 정체)·#86(7일 9시간 정체)·#95 전부 13:41~13:46 머지(squash). 정체 PR 0건.<br>(이전) **(9/12 09:00) 3건 — 주의 PR 2건(정체).** #83 정체 9일 4시간째, **#86 정체 전환(7일 4시간째)** — 둘 다 🟡 머지 승인 미회신, dev 작업 대상 아님. #95·#86·#83 CI 4잡 SUCCESS·`MERGEABLE`, merge-tree origin/main(`aa658e9`) 충돌 0.<br>(이전) **(9/11 21:00) 3건 — 주의 PR 1건(정체).** #83 정체 8일 16시간째(🟡 머지 승인 미회신, dev 작업 대상 아님), #86은 내일 9/12 04:34 정체 전환. #95·#86·#83 CI 4잡 SUCCESS·`MERGEABLE`·`CLEAN`(`gh pr view` 재조회), merge-tree origin/main(`a73c8bf`) 충돌 0.<br>(이전) **(9/11 09:00) 3건 — 주의 PR 1건(정체).** #83 정체 8일 4시간째(🟡 머지 승인 미회신, dev 작업 대상 아님), #86은 9/12 04:34 정체 전환 예정. #95·#86·#83 CI 4잡 SUCCESS·`MERGEABLE`·`CLEAN`(`gh pr view` 재조회), merge-tree origin/main(`3ff6307`) 충돌 0.<br>(이전) **(9/10 21:00) 3건 — 주의 PR 1건(정체).** #83 정체 7일 16시간째(🟡 머지 승인 미회신, dev 작업 대상 아님). #95·#86·#83 CI 4잡 SUCCESS·`MERGEABLE`·`CLEAN`(`gh pr view` 재조회), merge-tree origin/main(`08c1055`) 충돌 0.<br>(이전) **(9/10 09:00) 3건 — 주의 PR 1건(정체).** **#83이 9/10 04:46부로 7일 정체 진입**(원인은 🟡 머지 승인 미회신, dev 작업 대상 아님). #95·#86·#83 CI 4잡 SUCCESS·`MERGEABLE`, merge-tree 충돌 0. main CI 최신 런 `89bb083` **success**.<br>(이전) **(9/9 21:00) 3건 — 주의 PR 0건.** #95·#86·#83 CI 4잡 SUCCESS·`MERGEABLE`·`CLEAN`(`gh pr view` 재조회), merge-tree 충돌 0, 7일 정체 0(#83은 9/10 04:46 전환). main CI 최신 런 `99f1909` **success**.<br>(이전) **(9/9 09:00) 3건 — 주의 PR 0건.** #95·#86·#83 CI 4잡 SUCCESS·`MERGEABLE` 유지, 7일 정체 0(#83은 9/10 04:46 전환). main CI 최신 런 `b0e7630`·`7794a8f`(#96) **success**.<br>(이전) **(9/8 21:00) 3건 — 주의 PR 0건.** #95·#86·#83 CI 4잡 SUCCESS 유지(`mergeable` UNKNOWN = 09:07 ops 커밋 유입 후 캐시 미계산), 7일 정체 0(#83은 9/10 04:46 전환). main CI 최신 런 `4dafbbe` **success**.<br>(이전) **(9/8 09:00) 3건 — 주의 PR 0건.** #95·#86·#83 CI 4잡 SUCCESS·`MERGEABLE`·`CLEAN`(캐시 계산됨), 7일 정체 0(#83은 9/10 04:46 전환). main CI 최신 런 `9b58ffe` **success** — 직전 `16d9e0f` failure는 e2e 플레이크로 확정.<br>(이전) **(9/7 21:00) 3건 — 주의 PR 0건.** #95·#86·#83 CI 4잡 SUCCESS 유지(`mergeable` UNKNOWN = 캐시), 7일 정체 0(#83은 9/10 04:46 전환). **main CI 최신 런 `16d9e0f`(docs(ops) 문서 커밋) failure** — e2e `review.spec.ts` "analyze button" click 120s 타임아웃 1건, backend·frontend·app-shell-build SUCCESS. 문서만 바뀐 커밋이라 코드 회귀가 아닌 e2e 플레이크로 판정, 다음 main 런에서 재확인.<br>(이전) **(9/7 09:00) 3건 — 주의 PR 0건.** [#95](https://github.com/rarebirds-svg/quite-baduk/pull/95)(#87 픽스, ingest 스크립트+테스트) CI 4잡 SUCCESS·`MERGEABLE`·`CLEAN`, #83·#86과 `merge-tree` 충돌 0. #83·#86 CI SUCCESS 유지(`mergeable` UNKNOWN = 캐시). #83은 9/10 04:46 7일 정체 전환 예정. main CI 최근 3런 success(#92~#94).<br>(이전) **(9/6 21:00) 2건 — 주의 PR 0건.** #83·#86 CI 4잡 SUCCESS 유지(`mergeable` 이번 조회 UNKNOWN = GitHub 캐시 미계산, main 유입은 ops 문서뿐). #83은 9/10 04:46에 7일 정체 전환 예정. AP-20260903-01 84시간째·AP-20260905-01 36시간째.<br>(이전) **(9/6 09:35) 2건 — 주의 PR 0건.** [#83](https://github.com/rarebirds-svg/quite-baduk/pull/83)(#81 픽스) CI 4잡 SUCCESS·MERGEABLE·CLEAN, 생성 4일째 — AP-20260903-01 72시간째. [#86](https://github.com/rarebirds-svg/quite-baduk/pull/86)(#84 픽스) CI 4잡 SUCCESS·MERGEABLE·CLEAN — AP-20260905-01 24시간째. main CI 최근 3런 success. 둘 다 `game_service.py`를 고치나 독립 브랜치·`merge-tree` 충돌 0, #83 → #86 순차 머지 가능.<br>(이전) **(9/5 21:00) 2건** — 동일.
| CI e2e 상태 | **(7/25 12:00) PR #66 런에서 e2e 잡 SUCCESS 확인** — #59 머지 이후 회복 상태 유지. `[[ci-e2e-disabled]]`의 "main 만성 RED" 서술은 현재 사실과 다르므로 갱신 대상. e2e 잡은 활성(`if:` 조건 없음). |
| SQLite locked 판정 | **(9/12 21:00) 재발 0건 — #95(ingest 단일 트랜잭션 이동) 13:46 발효.** 재기동 후 대국 2건·30수 동안 `database is locked`·UNIQUE 0. **결정적 판정은 내일 9/13 03:16 전후 주간 ingest 창** — 09:00 사이클이 `.err`의 `database is locked` 부재와 `pro_games` +200으로 #87 해소를 확정한다.<br>(이전) **(9/6 21:00) 재발 0건** — 오늘 낮 대국 13건·679수 동안 `database is locked`·UNIQUE 0건. 재발 조건은 일요일 03시대 ingest 창뿐(#87).<br>**(9/6 09:35) 재발 1건 — 36일 무재발 종료. 단 #69(genmove 중 락)와 다른 새 원인.** 9/6 03:16 KST game #427 착수가 `database is locked`(line 226235~226396). 동시 쓰기 주체는 03:00 주간 CWI ingest(200건 단일 트랜잭션, `pro_games.created_at` 18:16:34~18:17:34 UTC = 락 60초) > busy_timeout 30s. 이슈 #87. 일요일 03:16 전후 외에는 재발 조건 없음. UNIQUE(#81)는 9/2 이후 재발 없음(오늘 새벽 1,119수 동안 0건).<br>(이전) **(9/5 21:00) 재발 0건 36일째.**
| 미병합 브랜치 | **(7/24 18:00 정정) 로컬 미push 브랜치는 0건이다.** `git for-each-ref --format='%(upstream:short)'` 실측 — 로컬 13개 중 12개가 origin 추적 브랜치를 갖고, 나머지 1개는 워크트리용 `worktree-feat+glossary-media-phase1`(5/26)뿐이다. 7/24 02:00 dev-cycle 로그의 "미push 6개"(`feat/format-game-result` 등)와 아래 "로컬 전용 2건" 서술은 **모두 오탐** — 해당 브랜치들은 전부 origin에 존재한다. `[[dev-cycle-unpushed-branches]]` 대응 조치 불필요. 남은 것은 push 여부가 아니라 **정리(삭제) 여부**이며 이는 사람 재량.<br>**(이전 기록 7/14 18:30 · 7/17 18:30)** 원격 `feat/last-seen-at-debounce`·`fix/issue-39`는 **오탐** — PR #30·#45가 **squash merge**돼 원본 커밋이 main 조상이 아닐 뿐, 내용은 main에 반영됨(`git grep` 확인). 사람 판단 불필요. → 판정은 `--no-merged`가 아니라 PR 상태+`git grep <ref>`로. 원격 브랜치 잔여물 삭제는 사람 재량.<br>**superseded(폐기 가능) 4건** — `fix/sqlite-busy-timeout`·`fix/auto-resign-respect-score-margin`·`fix/scoring-gate-too-strict`·`fix/issue-12`: 수정 내용이 다른 커밋으로 main에 이미 반영됨.<br>**실제 사람 판단 대기 = 로컬 전용 2건**: `feat/format-game-result`(main에 `formatResult` 부재), `feat/i18n-ja-zh`(main 로케일 ko·en만) — PR 생성 또는 폐기. 둘 다 **최종 커밋 4/30 = 2.5개월 경과**, feature 경로라 dev-cycle 자율 범위 밖 → 오케스트레이터는 자율 push하지 않고 보고만 유지(정책 L16 보수 처리). |

## 보류 승인

`state/pending-approvals.md` 참조 — **대기 0건 (9/12 21:00 기준).** 9/12 13:41~13:47 사람 요청 온디맨드 세션에서 AP-20260903-01(#83)·AP-20260905-01(#86)·AP-20260907-02(#95) 3건 일괄 발효 — #83·#86은 사람이 GitHub에서 직접 머지, #95는 세션이 머지 → pull(`c0d8d05`) → api 재기동 1회. 21:00 사이클 발효 확인 — 배포 갭 0, api PID 5743 정상, 재기동 후 대국 2건·30수 트레이스백 0. #87은 9/13 03:16 ingest 창에서 최종 판정.<br>(이전) `state/pending-approvals.md` 참조 — **대기 3건 (9/12 09:00 기준).** 재확인 — **AP-20260903-01** 216시간째·17회(#83, 9일 정체), **AP-20260905-01** 168시간째·14회(#86, 7일 정체 전환), **AP-20260907-02** 120시간째·10회(#95, **9/13 03:00 ingest 전 발효 마감까지 약 18시간·남은 정기 사이클 1회(9/12 21:00)**). 마지막 수 9/10 14:45 KST 이후 42시간 착수 0이라 #83·#86 재기동 적기 계속 유효(실행 시 `moves.played_at` 재확인 필수). 세 건 모두 CI 4잡 SUCCESS·`MERGEABLE`·merge-tree 충돌 0, 차단 요소 없음.

- **AP-20260802-01 (8/2 12:00 신규 등재, 18:00 기준 6시간 무응답)** — 머지된 4커밋의 prod 배포(🟡). `git pull --ff-only` + `web` 재빌드 + `ops/stack.sh restart prod`.<br>**미배포의 대가가 이번 사이클에도 실측됐다** — `orchestrator-runs.log` **18:00:05** 항목에 #71이 고칠 `Write(...)` deny 무실효 경고 2건이 또 출력됐다(머지 후 19시간). 즉 prod DB·KataGo 모델 가드레일은 아직 실효가 없다.<br>**단계별 발효 범위** — 1단계 `git pull`만으로 #71(가드레일)·#72(watchdog 성공마커, 크래시 루프 사각지대)가 **재기동 없이** 즉시 발효한다. #66(spectate 원자적 UPDATE)은 backend 재기동, #73(용어집 다이어그램)은 웹 재빌드가 필요하다.<br>충돌 없음 실측(유입 9파일 ∩ 로컬 미커밋 = 공집합), 4커밋 전부 머지 시점 CI 4잡 SUCCESS. 진행 중 대국 0건이라 재기동 리스크 낮음. 중복 제안은 만들지 않았다.

- (처리 완료) **AP-20260730-01 (7/30 12:00 신규 등재)** — PR [#72](https://github.com/rarebirds-svg/quite-baduk/pull/72) 머지(🟡). 7/30 02:00 dev-cycle이 #70을 고쳐 올린 PR인데 승인 큐에 등재돼 있지 않아 이번 실행에서 추가했다. watchdog가 "마지막 timestamp"가 아니라 "마지막 **성공** 종료 마커"로 신선도를 판정하게 되는 수정으로, 7/28 실사례에 대입하면 orchestrator 25.9h·dev-cycle 41.9h로 임계를 넘겨 그날 저녁 알림이 나갔을 건이다(구 로직 2.0h·18.0h로 무감지). CI 4잡 SUCCESS·`CLEAN`, 변경 파일 `ops/check-staleness.sh` + 신규 테스트 2개뿐 — **마이그레이션·prod 재기동 불필요**. **이 PR이 미머지인 동안 watchdog의 크래시 루프 사각지대는 그대로 열려 있다.**
- (처리 완료) **AP-20260725-01 — 8/1 23:00 사람 머지(`fd68fa3`). 제안 후 189시간(7.9일).** 단 backend 재기동 전까지 라이브 미반영이라 AP-20260802-01로 승계됐다. (등재 당시 기록) PR #66 머지(🟡). 이슈 #65 수정본이고 CI 4잡 전부 그린·`mergeable=MERGEABLE`·마이그레이션 없음인데 머지 대기 중. **긴급도는 내려갔다** — 7/26 20:24 배포로 `busy_timeout` 30s가 라이브가 돼 500 임계가 6배 올라갔다(02:00 dev-cycle 규명). 다만 그 완화는 *에러를 지연으로 바꾸는* 성격이라 #66의 락 보유시간 단축과 보완 관계이고, 머지 가치는 유지된다. 긴급 머지는 아니되 취소 대상도 아님.
- (처리 완료) **AP-20260723-01 — 8/1 23:00 사람 머지(`1ca3bcd`). 제안 후 227시간(9.5일).** 단 `git pull` 전까지 경고 2건이 계속 재출력되며 8/2 12:00·18:00 세션에서 연속 실측됐다 — AP-20260802-01로 승계. (등재 당시 기록) `.claude/settings.json` deny 규칙 2건이 `Write(...)`로 작성돼 **실효 없음**(하네스 경고: `Edit(path)` 규칙만 파일 편집 도구를 포괄). 결과적으로 라이브 prod DB(`backend/data/baduk.db`, 백업 원본)와 KataGo 모델이 에이전트 쓰기로부터 보호되지 않음. 설정 변경이라 🟡. 7/26·7/27·7/29 실행 모두 동일 경고 2건 재출력 = **6일째 미해소 확인**.<br>**(7/29 12:00) 상태 변경 — 이제 머지만 남았다.** 실행 절차 중 🟢 구간(브랜치 push + PR 생성)을 오케스트레이터가 선행해 **PR [#71](https://github.com/rarebirds-svg/quite-baduk/pull/71)** 을 올렸다. 6일간 "브랜치조차 없어 사람이 머지할 대상이 없는" 상태였던 것이 해소됐다.

AP-20260716-01(#55)·AP-20260717-01(#57)은 7/21 19:54 KST 사람 승인·머지로 처리 완료.

## 최근 장애

`state/incidents.md` 참조.

**OPS-20260907-01 (2026-09-07 00:06 ~ 09-08 23:55, 해소 — 약 48시간)**: 9/8 23:55 사람이 #96(기동 시 자동 `alembic upgrade`·헬스에 리비전 노출) 머지·pull·api 재기동·web 재빌드로 복구, `alembic_version` 0020·`/api/spectate` 200·착수 재개(복구 후 110수) 확인. 승인 회신 없이 직접 실행. 재발 방지 (1)·(2) 후보는 #96으로 코드에 반영됐고 (3) 5xx watchdog 신호는 미착수.<br>(이전) **신규 — OPS-20260907-01 (2026-09-07 00:06 ~ , 9/8 21:00 기준 45시간 지속 · 에스컬레이션 · 승인 미회신 36시간)**: 사람 배포에서 `alembic upgrade head` 누락 → `no such column: games.account_id`로 `/api/session`·`/api/spectate` 500, 착수 0건. `/api/health` 200이라 watchdog 무감지 — bug-scan(트레이스백·5xx 델타)이 잡았다. 09:00 즉시 경보 + AP-20260907-01. 21:00 재확인에서 `POST /api/session`도 500임을 확인해 영향 범위를 "신규 방문자도 착수 불가"로 정정. 재발 방지 후보 3건은 incidents.md 참조(헬스체크에 `alembic_version` 비교 추가 등).

**OPS-20260904-01 (2026-09-03 21:03 ~ 09-04 21:00, 해소)**: OAuth 만료(`OAuth session expired and could not be refreshed`)로 04:30 dev-cycle·09:00 오케스트레이터가 시작 직후 비정상 종료 — am 다이제스트 미발송, #84 dev-cycle 유실, 24h 감시 공백. prod 무사고(사후 검증 200·백업 정상·5xx 0·트레이스백 0). 18:06 사람 재로그인으로 복구, 21:00 사이클 정상 완주. OPS-20260728-01과 같은 유형의 **재발** — OAuth 갱신 실패는 session-retry 범위 밖이라 자동 복구 수단 없음. dev-cycle 1회 재트리거(화이트리스트).

**OPS-20260830-01 (2026-08-30 09:03 ~ 23:31, 해소)**: 09:00 사이클이 작업 완주 후 세션 한도로 비정상 종료(성공 마커 없음 → WD stale 경보 7건), 20:59 사람 재부팅로 21:00 트리거 미발화(prod는 launchd 자동 복구, 중단 약 6분). 23:31 수동 실행 사이클이 성공 종료해 해소. 재발 방지 2건이 승인 대기(AP-20260830-01/02).

**OPS-20260728-01 (2026-07-28 02:00 ~ 07-29 02:00, 자동 복구됨)**: 인증 만료(`OAuth session expired and could not be refreshed`)로 dev-cycle 1회 + orchestrator 2회가 **시작 직후 비정상 종료**. 약 24시간 운영 감시 공백. **같은 구간 prod는 무사고**(사후 검증: 200 OK · 백업 2회 정상 · 신규 트레이스백 0건) — 유실된 것은 서비스가 아니라 감시다. **watchdog는 전 구간 `신규 incident 0 건`으로 오판**했고, 원인은 아래 Watchdog 섹션의 사각지대 항목이다. 후속 이슈 [#70](https://github.com/rarebirds-svg/quite-baduk/issues/70).

기존 3건은 복구·정리 완료. 2026-05-25 watchdog 활성화 직후 5건 (WD-20260525-2122*) 자동 기록 — 41시간 무음 정지 사건의 사후 감지. 5/27 03:35~17:35 동안 content-draft·content-ingest stale 30건 자동 누적은 watchdog 임계와 plist Weekday 잡 정합성 misalignment에 의한 false-positive였고, 이후 `72ec362`에서 임계를 9d로 정합화. 5/28 04:00 이후 watchdog 신규 incident 0건 — 누적 해소 확인.

## Watchdog

| 항목 | 값 |
|---|---|
| 오케스트레이터 주기 정합성 | **(8/1 12:00 재확인, 미해소)** 이번 트리거도 **12:00:04** = plist(12:00·18:00 2회)대로 발화. 프롬프트·러닝북의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지 — 유실이 아니라 문서·plist 미정합이다. **7/31 18:00에 예고한 실무 영향이 그대로 실현됐다** — 23:30 사이클이 실재하지 않아 PR #66의 7일 정체 전환(8/1 02:06)이 **10시간 뒤인 이번 12:00에야** 감지됐다. 사람 판단 대기 유지.<br>**(7/31 18:00 재확인, 미해소)** 이번 트리거도 **18:00:05** = plist(12:00·18:00 2회)대로 발화. 프롬프트·러닝북의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지 — 유실이 아니라 문서·plist 미정합이다. 실무적 영향 1건 확인 — **23:30 사이클이 실재하지 않으므로 PR #66의 7일 정체 전환(8/1 02:06)은 8/1 12:00에야 감지된다.** 사람 판단 대기 유지.<br>**(7/31 12:00 재확인, 미해소)** 이번 트리거도 **12:00:05** = plist(12:00·18:00 2회)대로 발화. `orchestrator-runs.log` 실측은 7/21 이후 하루 2회로 **누락 없이 일관**(7/28 인증 만료 건 제외)이므로 유실이 아니라 문서·plist 미정합이다. 사람 판단 대기 유지.<br>**(7/30 18:00 재확인, 미해소)** 이번 트리거도 **18:00** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지, 사람 판단 대기.<br>**(7/30 12:00 재확인, 미해소)** 이번 트리거도 **12:00:05** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지, 사람 판단 대기.<br>**(7/29 18:00 재확인, 미해소)** 이번 트리거도 **18:00:05** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회 표기와 불일치 유지, 사람 판단 대기.<br>**(7/29 12:00 재확인, 미해소)** 이번 트리거도 **12:00:05** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회 표기와 여전히 불일치, 사람 판단 대기 유지. **다만 7/28 12:00·18:00은 발화는 됐으나 인증 만료로 무작업 종료** — 주기 문제가 아니라 OPS-20260728-01 건이다(위 사각지대 행 참조).<br>**(7/27 18:00 재확인, 미해소)** 이번 트리거도 **18:00:01** = plist(12:00·18:00 2회)대로 발화(누락 아님). 사람 판단 대기 유지.<br>**(7/27 12:00 재확인, 미해소)** 이번 트리거도 **12:00:00 정각** = plist대로 발화(누락 아님). 문서·프롬프트의 06:30·12:30·18:30·23:30과 여전히 불일치. 사람 판단 대기 유지.<br>**(7/26 18:00 재확인, 미해소)** plist는 여전히 12:00·18:00 2회이며 이번 18:00 트리거도 plist대로 정상 발화 = 누락 아님. 사람 판단 대기 유지.<br>**(7/24 12:00 신규) 문서 4회 vs 실제 2회 불일치.** 러닝북 5종·오케스트레이터 프롬프트는 `매일 06:30·12:30·18:30·23:30`으로 적으나, `ops/launchd/com.inkbaduk.ops-orchestrator.plist`의 `StartCalendarInterval`은 **12:00·18:00 2회**뿐(헤더 주석도 "매일 12시·18시"). `orchestrator-runs.log` 실측 일치(7/23 18:00 → 7/24 12:00) = **누락이 아니라 미등록**. `sync-launchd.sh --check` drift 없음, `pmset` sleep 0 → 슬립 유실 아님. 문서를 2회로 맞출지 plist를 4회로 늘릴지는 사람 판단(후자는 prod 스케줄 변경이라 🟡). |
| 잡 등록 | `com.inkbaduk.ops-watchdog` 가동 중 (StartInterval 3600s) |
| 첫 실행 | 2026-05-25 21:22 (수동 kickstart, 신규 incident 5건) |
| 알림 채널 | Telegram (토큰 미설정 → fallback) → macOS notification |
| 임계 | orchestrator 18h · dev-cycle/backup 30h · content-draft/content-ingest 9d · analytics-weekly 8d |
| 쿨다운 | 잡당 1시간 1회 |
| **사각지대 (7/29 12:00 신규)** | **`ops/check-staleness.sh`는 "trigger 후 즉시 실패"를 감지하지 못한다.** `extract_last_ts()`가 로그의 마지막 `[YYYY-MM-DD HH:MM:SS]` 패턴만 뽑아 임계와 비교하는데, 실패 종료도 `[...] 비정상 종료` 라인으로 timestamp를 새로 찍으므로 잡이 "방금 실행됨"으로 보인다. launchd trigger **유실**은 잡지만 trigger는 됐는데 아무 일도 못 하는 모드는 **크래시 루프여도 영구히 무음**이다. OPS-20260728-01(3회 유실, watchdog 0건)이 실증 사례. 이슈 [#70](https://github.com/rarebirds-svg/quite-baduk/issues/70) — 성공 종료 마커 기준 판정으로 수정 제안. |
| 정합성 이슈 | (해소) `72ec362`에서 content-draft·content-ingest 임계를 30h → 9d로 plist 일정과 정합화. 신규 incident 0건 확인. |
