# 운영 대시보드

- 갱신: 2026-08-17 09:00

## 스택 상태

| 스택 | 상태 | 마지막 확인 |
|---|---|---|
| prod | 정상 — backend·web 200 OK, db OK, 디스크 6%, plist drift 없음. PID api **88640** / web **88644**.<br>**(8/17 09:00) 인프라 전 항목 정상. 🚩 웨이브 1은 여전히 라이브 미반영(ahead 21 = 코드 19 + ops 2) — AP-20260816-01 48시간째 · 2회 재확인. ⚠️ 02:00 dev-cycle이 Claude 세션 한도로 비정상 종료(마커 미기록, 한도는 04:00 리셋 = 일회성, 백로그 0건이라 상실 작업 0).** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 공개 도메인 `inkbaduk.com` **200**, 디스크 6%, drift 없음(exit 0), staging 중단 = 정책상 정상. PID 88640/88644 · `BUILD_ID` 8/8 15:48 = **209시간(8.7일) 무재기동**, `sessions` 0행 = 미발효 지속.<br>**✅ `.err` 225,435행 불변**(12h 델타 0줄) — 트레이스백 0건, `database is locked` 재발 0건 14일째, #78 무재발 14일째. **✅ `.log` +567/12h**, 최근 5,000줄 200 **4,838** / 401 56 / 204 53 / 404 45 / 201 5, **5xx 0건**. 404는 전부 스캐너 탐침. `visit_hits` 8/16 **7건**으로 반등 유지(8/15 2건). 신규 대국 없음 — #339(8/16 12:45 KST)가 마지막, 진행 중 0건.<br>(이전 8/16 21:00) **인프라 전 항목 정상 + 🎯 5일 만의 신규 대국 #339. 🚩 웨이브 1 19커밋은 여전히 라이브 미반영(ahead 20 = 코드 19 + ops 1) — AP-20260816-01 12시간째 · 1회 재확인.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 디스크 6%, drift 없음(exit 0), staging 중단 = 정책상 정상. PID 88640/88644 · `BUILD_ID` 8/8 15:48 = **197시간(8.2일) 무재기동**, `alembic_version` 0019 유지 = 판별 조건 전부 09:00과 동일.<br>**🎯 대국 #339** — `max(games.id)` 338 → **339**, 88수, `moves.max(played_at)` `2026-08-16 03:45:38` UTC(12:45 KST 종료). 8/11 #338 이후 5일 만의 실사용. `visit_hits` 오늘 **5건**(8/15 2건)으로 반등.<br>**✅ `.err`가 아홉 사이클 만에 처음 +6줄 움직였다 — 전부 무해.** 225,429 → **225,435행**, 신규 6줄 전부 게임 338·339 WS 접속/종료 INFO로 **트레이스백 0건** = 실사용 재개의 부수 증거. `database is locked` 재발 0건 14일째, #78 어드민 401 무재발 14일째.<br>**✅ `.log` +626/12h.** 최근 3,000줄 200 **2,941** / 401 26 / 204 24 / 404 4 / 201 3, **5xx 0건**. 404 급감(27→4) = 스캐너 버스트 소강. `sessions` 0행 = 90일 세션 코드 미발효(배포 대기)로 예상대로.<br>(이전 8/16 09:00) **인프라 전 항목 정상. 단, 🚩 웨이브 1(사이트 활성화) 19커밋이 08:17 로컬 main에 머지됐으나 push·빌드·재기동 미실행 = 라이브 미반영 → AP-20260816-01 등재.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 공개 도메인 `inkbaduk.com` **200**, 디스크 6%, drift 없음, staging 중단 = 정책상 정상. PID 88640/88644 `lstart 8/8 15:48` = **191시간(8일) 무재기동** — 프로세스·`BUILD_ID` 모두 옛 코드라 90일 세션·비로그인 개방 등 전부 미발효. 마이그레이션만 선행 적용됨(`alembic_version`=0019, 5-5절 안전 순서 준수). 구 코드+신 스키마 동거는 안전 확인(제약 제거+신규 테이블뿐).<br>**✅ `.err` 225,429행 불변(아홉 사이클 연속)** · mtime 8/11 20:46. `.log` +855/12h. 최근 3,000줄 200 **2,909** / 401 32 / 204 30 / 404 27 / 201 1, **최근 20,000줄 5xx 0건**. #78 무재발 13일째(마지막 232,613행 고정), `database is locked` 재발 0건 13일째.<br>**ℹ️ 일요일 예정 3건 전부 정상** — CWI ingest new=200(03:13), W33 리포트 생성, `analytics-weekly.json` 최초 생성(배선 정상 확정). `visit_hits` 8/15는 2건(21:00 실측 1에서 +1)으로 저공비행 지속.<br>(이전 8/15 21:00) **전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 열다섯 사이클 연속.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 공개 도메인 `inkbaduk.com` **200**, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `lstart Aug 8 15:48:51` = **173시간(7.2일) 무재기동**.<br>**✅ `.err`가 96시간 델타에 단 한 줄도 늘지 않았다 — 여덟 사이클 연속 라인 수 동일.** 총 **225,429행** 불변, mtime `8/11 20:46` 불변. 최근 3,000줄 200 **2,902** / 401 34 / 204 33 / 404 28 / 201 2, **최근 20,000줄 5xx 0건**(마지막 5xx는 175,237행 = 파일 끝에서 62,497줄).<br>**⚠️ `.log` +963행 반등은 사람 방문이 아니다 — 09:00 판단을 정정한다.** 963줄을 요청자별로 분해하니 **773건이 `127.0.0.1` → `/api/spectate/pro/*`**(크롤러가 `/spectate/pro/<id>`를 순차 순회한 Next.js SSR fetch), **157건이 `/api/health`**(외부 141 = Cloudflare Worker 모니터 프로브 + 로컬 16), 나머지는 sitemap·themes·월간 픽 등 크롤 부수 요청이다. **실제 사람 방문(`POST /api/analytics/hit`)은 이 12시간에 1건뿐이다.** `visit_hits` 일별 실측은 8/8 **17** → 12 → 5 → 10 → 9 → 6 → 3 → 8/15 **1**로 **일주일 연속 하강** — `.log` 증가폭은 봇·SSR·헬스프로브 지표이지 사람 트래픽 지표가 아니다.<br>**✅ #78 무재발 12일째.** 어드민 401 밴드 마지막 발생 **232,613행** 고정 + 파일 성장으로 **최근 5,121줄에 어드민 401 0건**(09:00엔 4,158줄). 401 34건은 전부 `/api/session`.<br>**✅ `database is locked` 재발 0건 — 12일째.** 마지막 발생 `.err` **225,347행** < 현 기동 마커 **225,423행**(`[[database-locked-genmove]]`).<br>**ℹ️ 404 28건은 전부 스캐너 봇**(`/api/.env`·`/api/v1/credentials` 시크릿 탐침 + SSRF 탐침 `169.254.169.254` 4건 반복). 전부 404 거절, 라우트 부재, prod는 맥미니라 메타데이터 서비스 없음 — 조치 불요. 실제 앱 라우트 404는 0건.<br>**ℹ️ 월간 픽의 `2058-XX` 요청은 버그가 아니다.** `/api/spectate/pro/pick/monthly/2058-01`이 200 + 실재 기보(id 2709, `2019-01-28`)를 반환한다 — 날짜 필터가 아니라 yyyymm 키 기반 결정적 선택이라 임의 월에 정상 동작한다. `pro_games.game_date` 최대 연도는 **2026**으로 미래 날짜 오염 없음.<br>**ℹ️ 약 96시간 신규 대국 없음.** `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC로 여덟 사이클 연속 동일. 인프라 정상이므로 장애가 아니라 **수요 부재**이며, 위 방문 하강과 같은 방향이다 — 8/16 W33 리포트에서 확인할 것.<br>(이전 8/15 09:00) **(8/15 09:00) 전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 열네 사이클 연속.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 공개 도메인 `inkbaduk.com` **200**, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `lstart Aug 8 15:48:51` = **161시간(6.7일) 무재기동**.<br>**⚠️ 판정 메모 유지 — `launchctl list`의 `com.baduk.api` 2번째 컬럼 `-15`는 실패가 아니다.** 직전 인스턴스의 SIGTERM 기록(8/8 `stack.sh restart prod`)이고 현재 상태는 1번째 컬럼 PID **88640**이 결정한다.<br>**✅ `.err`가 84시간 델타에 단 한 줄도 늘지 않았다 — 일곱 사이클 연속 라인 수 동일.** 총 **225,429행**이 8/12 09:00 이래 여섯 사이클과 완전 동일하고 mtime도 `8/11 20:46` 불변이다. 반면 `.log`는 236,576 → **236,771행**(+195)으로 살아 있어 조합의 의미는 트래픽 0이 아니라 **에러 없는 트래픽**이다(`[[api-log-stdout-vs-stderr]]`). 최근 3,000줄 200 **2,848** / 204 59 / 401 57 / 404 32 / 201 2, **최근 20,000줄 5xx 0건**(마지막 5xx는 175,237행 = 파일 끝에서 61,534줄 떨어짐).<br>**✅ #78 무재발 11일째.** 어드민 401 밴드 마지막 발생은 **232,613행**으로 고정인데 파일만 자라 **최근 4,158줄에 어드민 401 0건**(8/14 21:00엔 3,963줄). 401 57건 내역 실측 = `/api/session` 55 + `/api/stats` 2로 **어드민 401은 0건**이다.<br>**✅ `database is locked` 재발 0건 — 11일째.** 마지막 발생 `.err` **225,347행** < 현 기동 마커 **225,423행**(`[[database-locked-genmove]]`).<br>**ℹ️ 404 32건에 SSRF 탐침이 섞였다.** `/api/webhook?url=http://169.254.169.254/latest/meta-data/…`·`/api/v1/fetch?url=…` 각 2건 = 클라우드 인스턴스 메타데이터 노림수. 전부 404 거절이고 해당 라우트는 존재하지 않으며 prod는 맥미니라 메타데이터 서비스 자체가 없다 — 조치 불요. 나머지도 `/api/.env`류 시크릿 탐침이고 실제 앱 라우트 404는 0건이다.<br>**ℹ️ 약 84시간 신규 대국 없음.** `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC로 일곱 사이클 연속 동일. 인프라 정상이라 장애가 아니라 **수요 부재**다. 다만 `.log` 증가폭이 +382 → +179 → +130 → **+195**로 반등했고 백업 드릴본 `visit_hits` **170행**이라 방문 자체는 얇게 유지된다 — 8/16 W33 리포트에서 확인할 것.<br>**✅ push 정체 3커밋 해소** — 로컬 `main`이 `origin/main`보다 3커밋 앞서 있던 것(`be1cd64`·`09ece53`·`1aef335`, 전부 `docs/ops/state/**`)을 `git push origin main`으로 처리(`ea53b81..1aef335`). 인증 프롬프트 없이 즉시 통과 = 자격증명·네트워크 문제가 아니라 세 사이클의 단순 단계 누락이었다. 매 사이클 종료 시 `git rev-list --count origin/main..HEAD` = 0 확인으로 재발 방지.<br>(이전 8/14 21:00) **(8/14 21:00) 전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 열세 사이클 연속.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 공개 도메인 `inkbaduk.com` **200**, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `lstart Aug 8 15:48:51` = **149시간 무재기동**.<br>**⚠️ 판정 메모 유지 — `launchctl list`의 `com.baduk.api` 2번째 컬럼 `-15`는 실패가 아니다.** 직전 인스턴스의 SIGTERM 기록(8/8 `stack.sh restart prod`)이고 현재 상태는 1번째 컬럼 PID **88640**이 결정한다.<br>**✅ `.err`가 72시간 델타에 단 한 줄도 늘지 않았다 — 여섯 사이클 연속 라인 수 동일.** 총 **225,429행**이 8/12 09:00부터 8/14 09:00까지 다섯 사이클과 완전 동일하고 mtime도 `8/11 20:46` 불변이다. 반면 `.log`는 236,446 → **236,576행**(+130)으로 살아 있어 조합의 의미는 트래픽 0이 아니라 **에러 없는 트래픽**이다(`[[api-log-stdout-vs-stderr]]`). 최근 3,000줄 200 **2,802** / 204 68 / 401 66 / 404 59 / 201 3, 최근 5,000줄 **5xx 0건**.<br>**✅ #78 무재발 10일째.** 어드민 401 밴드 마지막 발생은 **232,612~232,613행**으로 고정인데 파일만 자라 **최근 3,963줄에 어드민 401 0건**(09:00엔 3,833줄). 401 66건 내역 실측 = `/api/session` 64 + `/api/stats` 2로 **어드민 401은 0건**이다.<br>**✅ `database is locked` 재발 0건 — 10일째.** 마지막 발생 `.err` **225,347행** < 현 기동 마커 **225,423행**(`[[database-locked-genmove]]`).<br>**ℹ️ 약 72시간 신규 대국 없음.** `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC로 여섯 사이클 연속 동일. 인프라 정상이라 장애가 아니라 **수요 부재**이나, `.log` 증가폭이 +382 → +179 → **+130**으로 세 사이클 연속 줄어 방문 자체도 얇아지는 추세다 — 8/16 W33 리포트에서 확인할 것.<br>(이전 8/14 09:00) **(8/14 09:00) 전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 열두 사이클 연속.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 공개 도메인 `inkbaduk.com` **200**, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `lstart Aug 8 15:48:51` = **137시간 무재기동**.<br>**⚠️ 판정 메모 유지 — `launchctl list`의 `com.baduk.api` 2번째 컬럼 `-15`는 실패가 아니다.** 직전 인스턴스의 SIGTERM 기록(8/8 `stack.sh restart prod`)이고 현재 상태는 1번째 컬럼 PID **88640**이 결정한다.<br>**✅ `.err`가 48시간 델타에 단 한 줄도 늘지 않았다 — 다섯 사이클 연속 라인 수 동일.** 총 **225,429행**이 8/12 09:00·8/12 21:00·8/13 09:00·8/13 21:00과 완전 동일하고 mtime도 `8/11 20:46` 불변이다. 반면 `.log`는 236,267 → **236,446행**(+179)으로 살아 있어 조합의 의미는 트래픽 0이 아니라 **에러 없는 트래픽**이다(`[[api-log-stdout-vs-stderr]]`). 최근 3,000줄 200 **2,804** / 204 66 / 401 64 / 404 59 / 201 4, 최근 5,000줄 **5xx 0건**.<br>**✅ #78 무재발 9일째.** 어드민 401 밴드 마지막 발생은 **232,612~232,613행**으로 고정인데 파일만 자라 **최근 3,834줄에 어드민 401 0건**(8/13 21:00엔 3,654줄). 위 401 64건은 어드민이 아닌 비로그인 세션 조회분이다.<br>**✅ `database is locked` 재발 0건 — 9일째.** 마지막 발생 `.err` **225,347행** < 현 기동 마커 **225,423행**(`[[database-locked-genmove]]`).<br>**ℹ️ 약 60시간 신규 대국 없음.** `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC로 다섯 사이클 연속 동일. 인프라 정상 + 방문 트래픽 생존(`.log` +179행)이므로 장애가 아니라 **수요 부재**다 — 사람의 제품 판단 영역.<br>(이전 8/13 21:00) **(8/13 21:00) 전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 열한 사이클 연속.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 공개 도메인 `inkbaduk.com` **200**, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `lstart Aug 8 15:48:51` = **125시간 무재기동**.<br>**⚠️ 판정 메모 유지 — `launchctl list`의 `com.baduk.api` 2번째 컬럼 `-15`는 실패가 아니다.** 직전 인스턴스의 SIGTERM 기록(8/8 `stack.sh restart prod`)이고 현재 상태는 1번째 컬럼 PID **88640**이 결정한다.<br>**✅ `.err`가 36시간 델타에 단 한 줄도 늘지 않았다 — 네 사이클 연속 라인 수 동일.** 총 **225,429행**이 8/12 09:00·8/12 21:00·8/13 09:00과 완전 동일하고 mtime도 `8/11 20:46` 불변이다. 반면 `.log`는 235,885 → **236,267행**(+382)으로 살아 있어 조합의 의미는 트래픽 0이 아니라 **에러 없는 트래픽**이다(`[[api-log-stdout-vs-stderr]]`). 최근 3,000줄 200 **2,785** / 204 75 / 401 73 / 404 60 / 201 4, 최근 5,000줄 **5xx 0건**.<br>**✅ #78 무재발 8일째 — 여유 구간이 또 넓어졌다.** 어드민 401 밴드 마지막 발생은 **232,612~232,613행**으로 고정인데 파일만 자라 **최근 3,654줄에 어드민 401 0건**(09:00엔 3,272줄).<br>**✅ `database is locked` 재발 0건 — 8일째.** 마지막 발생 `.err` **225,347행** < 현 기동 마커 **225,423행**(`[[database-locked-genmove]]`).<br>**ℹ️ 404가 146 → 60건으로 줄었다 — 스캐너 버스트가 잦아든 것이다.** 잔여는 `/api/settings`·`/api/env`·`/api/config.json`·`/api/..%3B/actuator/env`(경로 우회 시도)·`/api/.env` 등 시크릿 탐침이고 실제 앱 라우트 404는 0건. **404 감소를 개선 신호로 읽어도 안 된다** — 봇 트래픽 변동일 뿐이다.<br>**ℹ️ 약 72시간 신규 대국 없음.** `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC로 네 사이클 연속 동일. 인프라 정상 + 방문 트래픽 생존(`.log` +382행)이므로 장애가 아니라 유입 문제다.<br>(이전) **(8/13 09:00) 전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 열 사이클 연속.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 공개 도메인 `inkbaduk.com` **200**, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `lstart Aug 8 15:48:51` = **113시간 무재기동**.<br>**⚠️ 판정 메모 유지 — `launchctl list`의 `com.baduk.api` 2번째 컬럼 `-15`는 실패가 아니다.** 직전 인스턴스의 종료 시그널(SIGTERM = 8/8 `stack.sh restart prod`) 기록이고, 현재 상태는 1번째 컬럼 PID **88640**이 결정한다. 러닝북 기준 **가동**이며 이 컬럼을 실패로 읽으면 오경보다.<br>**✅ `.err`가 24시간 델타에 단 한 줄도 늘지 않았다.** 총 라인 수 **225,429행**이 8/12 09:00·21:00과 **연속 동일**하고 mtime도 `8/11 20:46` 불변 — 두 사이클 연속 일치라 mtime 단독보다 강한 증거다. 반면 `.log`는 235,655 → **235,885행**(+230)으로 살아 있어, 조합의 의미는 트래픽 0이 아니라 **에러 없는 트래픽**이다(`[[api-log-stdout-vs-stderr]]`). 최근 3,000줄 200 **2,693** / 404 146 / 204 78 / 401 76 / 201 4, 최근 5,000줄 **5xx 0건**.<br>**✅ #78 무재발 7일째 — 여유 구간이 또 넓어졌다.** 어드민 401 밴드 마지막 발생은 **232,612~232,613행**으로 고정인데 파일만 자라 **최근 3,272줄에 어드민 401 0건**(8/12 21:00엔 3,042줄). 밴드 끝단 고정 + 파일 성장이라 신뢰구간이 사이클마다 넓어진다(`[[bug-scan-verify-recency]]`).<br>**✅ `database is locked` 재발 0건 — 7일째.** 마지막 발생 `.err` **225,347행** < 현 기동 마커 **225,423행**으로 전부 기동 이전 구간이며, 게임 336·337·338 세션 로그(225,399~225,429행)에도 락 트레이스백 0건(`[[database-locked-genmove]]`).<br>**ℹ️ 404 146건은 전부 스캐너 봇이다.** `/api/.env`·`/api/v1/auto_login`·`/api/config`·`/api/credentials`·`/api/exec` 등 시크릿·RCE 탐침이고 실제 앱 라우트 404는 0건. 전부 404 거절, 5xx·우회 흔적 0 — 조치 불요이나 **404 카운트를 앱 버그 지표로 읽으면 안 된다**.<br>**ℹ️ 약 60시간 신규 대국 없음.** `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC로 8/12 두 사이클과 동일. 인프라 정상 + 방문 트래픽 생존(`.log` +230행, 백업 +2,397B)이므로 장애가 아니라 유입 문제다. `katago_alive:false`도 8/11 20:45 마지막 AI 호출 이후의 lazy 상태로 러닝북 명시대로 정보용이다.<br>(이전) **(8/12 21:00) 전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 아홉 사이클 연속.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 공개 도메인 `inkbaduk.com` **200**, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `lstart Aug 8 15:48:51` = **101시간 무재기동**.<br>**⚠️ 판정 메모 — `launchctl list`의 `com.baduk.api` 2번째 컬럼 `-15`는 실패가 아니다.** 그것은 *직전* 인스턴스의 종료 시그널(SIGTERM = 8/8 `stack.sh restart prod`) 기록이고, 현재 상태는 1번째 컬럼 PID **88640**이 결정한다. 러닝북 기준상 **가동**이며 이 컬럼을 실패로 읽으면 오경보다.<br>**✅ `.err`가 12시간 델타에 단 한 줄도 늘지 않았다 — 라인 수로 확정.** mtime `8/11 20:46` 불변에 더해 **총 라인 수 225,429행이 09:00 실측치와 완전 동일**하다. mtime만 보는 것보다 강한 증거다. 반면 `.log`는 235,450 → **235,655행**(+205)으로 살아 있어, 조합의 의미는 트래픽 0이 아니라 **에러 없는 트래픽**이다(`[[api-log-stdout-vs-stderr]]`). 최근 3,000줄 200 **2,722** / 404 121 / 204 76 / 401 74 / 201 4, 최근 5,000줄 **5xx 0건**.<br>**✅ #78 무재발 6일째 — 여유 구간이 넓어지고 있다.** 어드민 401 밴드 마지막 발생은 **232,613행**으로 고정인데 파일만 자라 **최근 3,042줄에 어드민 401 0건**(09:00엔 2,837줄). 밴드 끝단 고정 + 파일 성장이라 신뢰구간이 사이클마다 넓어진다(`[[bug-scan-verify-recency]]`).<br>**✅ `database is locked` 재발 0건 — 6일째.** 마지막 발생 `.err` **225,347행** < 현 기동 마커 **225,423행**으로 전부 기동 이전 구간(`[[database-locked-genmove]]`).<br>**ℹ️ 대국은 09:00 이후 추가 없음.** `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC로 09:00 값과 동일하며 백업 드릴본과도 일치한다. `katago_alive:false`도 8/11 20:45 마지막 AI 호출 이후의 lazy 상태로 러닝북 명시대로 정보용이다.<br>(이전) **(8/12 09:00) 전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 여덟 사이클 연속.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 공개 도메인 `inkbaduk.com` **200**, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `lstart Aug 8 15:48:51` = **89시간 무재기동**.<br>**✅ 백업이 게임 #338을 실제로 담았다 — 어제 예측의 검증.** 8/11 21:00 사이클은 "8/11 04:00 백업본에는 338이 없고 8/12 04:00 백업에 포함된다"고 적었다. 이번 드릴본 `SELECT max(id) FROM games` = **338**로 예측대로 담겼고, 크기도 3,406,435B → **3,411,862B**(+5,427B)로 103수 유입과 정합한다. 무결성 `ok`를 넘어 **내용 수준에서 파이프라인을 확인**한 첫 사례다.<br>**트레이스백 0건 · 5xx 0건.** `.err` mtime이 `8/11 20:46` **그대로**로 12시간 넘게 신규 쓰기 0줄. `.log`는 mtime `8/12 09:00`으로 살아 있어(총 **235,450행**) 조합은 트래픽 0이 아니라 **에러 없는 트래픽**이다(`[[api-log-stdout-vs-stderr]]`). 최근 3,000줄 200 **2,579** / 401 223 / 404 121 / 204 70 / 201 4, 최근 5,000줄 **5xx 0건**.<br>**✅ `database is locked` 재발 0건 — 6일째.** 마지막 발생 `.err` **225,346행** < 현 기동 마커 **225,423행**으로 전부 기동 이전 구간. 어제 붙은 103수 + 힌트 14회 부하에서도 락 0건이던 판정이 유지된다(`[[database-locked-genmove]]`).<br>**✅ #78 무재발 6일째 — 라인 위치로 실측했다.** 어드민 401 밴드 마지막 발생이 **232,612~232,613행**, 파일 총 길이 **235,450행**이라 **최근 2,837줄에 어드민 401 0건**이다. 3,000줄 창에 39건씩 보이는 것은 창이 밴드 끝단을 물기 때문이지 신규 발생이 아니다(`[[bug-scan-verify-recency]]`).<br>**✅ 어제의 미커밋 편집 해소.** 8/11 21:00이 "다른 세션 저작물이라 대신 커밋하지 않는다"고 남겨둔 `ops/check-staleness.sh` 6h 쿨다운 편집이 **`6aba6d9`**로 정상 커밋됐다. 예고했던 "다음 사이클에도 미커밋이면 사람 확인" 조건은 발동하지 않았다.<br>**ℹ️ 대국은 어제 이후 추가 없음.** `max(games.id)` **338** · `moves` `max(played_at)` `2026-08-11 11:45:53` UTC로 21:00 값과 동일. 밤사이 유입이 없었을 뿐 장애 신호가 아니다. `katago_alive:false`도 AI 호출이 끊긴 뒤의 lazy 상태로 러닝북 명시대로 정보용이다. | 2026-08-16 21:00 |
| staging | 중단 (정책상 정상) | 2026-08-17 09:00 |

## 백업 상태

| 항목 | 값 |
|---|---|
| 최신 백업 | **2026-08-17T04:00** (baduk-20260817T040005.db.gz, **3,529,191B**) — **(8/17 09:00)** 5시간 전, 30h 임계 여유. 복원 드릴 `integrity_check` = `ok` · **테이블 10** = **23회 연속** 통과. 테이블 9 → 10은 8/16 08:17 적용 마이그레이션(0019 `analytics_salts` 등)이 담긴 **첫 백업**이라 예정된 증가. 크기 +3,058B = 대국 #339(88수) + 방문 유입과 정합. daily 8/3분 보존 정리로 티어 14/8/3 유지.<br>(이전 8/16 21:00) **2026-08-16T04:00** (baduk-20260816T040005.db.gz, **3,526,133B**) — 09:00과 같은 파일로 17시간 전, 30h 임계 미달. 복원 드릴 `integrity_check` = `ok` · 테이블 9 = **22회 연속** 통과. 다음 생성은 8/17 04:00.<br>(이전 8/16 09:00) 5시간 전, 30h 임계 여유. 복원 드릴 `integrity_check` = `ok` · 테이블 9 = **21회 연속** 통과. 크기 **+110,934B 급증은 03:00 CWI ingest new=200과 정합하는 예정된 정상**(`[[backup-size-spike-weekly-ingest]]`) — 8/15 21:00에 예고한 그대로다.<br>(이전 8/15 21:00) **2026-08-15T04:00** (baduk-20260815T040005.db.gz, **3,415,199B**) — 09:00과 같은 파일로 17시간 전, 30h 임계 미달. 복원 드릴 `integrity_check` = `ok` · 테이블 9 · `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC가 라이브와 일치 = **20회 연속** 통과. 다음 생성은 8/16 04:00이며, 같은 날 03:00 CWI 주간 ingest가 예정돼 있어 **크기 급증이 예상되고 이는 정상**이다(`[[backup-size-spike-weekly-ingest]]`).<br>(이전 8/15 09:00) 5시간 전으로 30h 임계 여유. 복원 드릴 `integrity_check` = `ok` · 테이블 9 · `max(games.id)` **338** · `moves.max(played_at)` `2026-08-11 11:45:53` UTC가 **라이브 DB와 완전 일치**해 이 델타의 백업 지연 노출은 0이다 — **19회 연속** 통과. 신규 대국 0인데 3,414,916 → 3,415,199B로 **+283B** 자랐고, 드릴본 `visit_hits`는 **170행**이다 = 대국 0국 구간에도 방문 트래픽이 살아 있다는 별도 증거.<br>(이전 8/14 21:00) **2026-08-14T04:00** (baduk-20260814T040005.db.gz, **3,414,916B**) 17시간 전으로 09:00과 같은 파일이다. 30h 임계 미달, 복원 드릴 `integrity_check` = `ok` · 테이블 9 · `max(games.id)` **338**로 **18회 연속** 통과. 다음 생성은 8/15 04:00.<br>(이전 8/14 09:00) 5시간 전. 드릴본 `moves.max(played_at)` `2026-08-11 11:45:53` UTC가 **라이브 DB와 완전 일치**해 이 델타의 백업 지연 노출은 0이다 — 파일 존재가 아니라 내용으로 확인했다. 신규 대국 0인데 3,414,259 → 3,414,916B로 **+657B** 자랐다. `games`·`moves` 불변이므로 증가분은 `visit_hits`·`session_history` 등 부수 테이블 유입 = 대국 0국 구간에도 방문 트래픽이 살아 있다는 별도 증거다.<br>(이전 8/13 21:00) `baduk-20260813T040005.db.gz` **3,414,259B**, 16회 연속 통과. |
| daily / weekly / monthly | **14 / 8 / 3** (8/16 09:00 실측). 8/16 04:00 생성과 함께 daily 8/2분·weekly 6/21분이 보존 정리돼 개수 유지. |

## 콘텐츠 인덱스

| 항목 | 값 |
|---|---|
| 프로 기보 수 | **3511** (8/16 09:00 DB 실측 `SELECT count(*) FROM pro_games`). 직전 3311(8/9)에서 **+200** = 8/16 03:13 주간 ingest `new=200`과 정합하며, 백업 크기 +110,934B·sitemap +200으로도 교차 확인.<br>(이전) **3311** (8/9 12:00 DB 실측 `SELECT count(*) FROM pro_games`). 직전 3111(8/2)에서 **+200** = 8/9 03:12 주간 ingest의 `new=200`과 정확히 정합하며, 같은 증가가 백업 크기 +117,938B·sitemap +200으로도 확인됐다.<br>(이전) **3111** (8/2 18:00 DB 실측 `SELECT count(*) FROM pro_games`). 직전 2911(7/26)에서 **+200** = 8/2 03:12 주간 ingest의 `new=200`과 정확히 정합. (이전) 2911 — 7/26 12:00 실측. 그 이전 표기 `1111(825+286)`은 1800건 벌어진 stale 값이었다. **이 행은 매주 일요일 ingest 이후 갱신할 것.** |
| 테마 수 | 6 |
| 월간 픽 URL | 14 (최근 12개월 + 현재 + 다음 달) |
| sitemap URL 수 | **3579** (8/16 09:00 prod `/sitemap.xml` `<loc>` 실측). 8/9의 3379에서 **+200** = 8/16 ingest와 정합, force-dynamic이라 재빌드 없이 반영. `/daily`·`/spectate` 등 신규 정적 URL은 웨이브 1 배포(재빌드) 후 추가된다.<br>(이전) **3379** (8/9 12:00 prod `/sitemap.xml` `<loc>` 실측). 7/26의 2979에서 **+400** = 8/2·8/9 두 번의 ingest(각 +200)와 정합. **재빌드 없이 반영됐다** — 목록이 `force-dynamic`이라 ingest만으로 라이브 sitemap이 늘어난다. |
| 최근 CWI ingest | **2026-08-16 03:13** (fetched=2660 new=200 duplicate=2460 error=0, cap=200 도달) — 일요일 정기 실행. IndexNow 통보(158470f)는 코드 미발효 상태의 실행이라 이번 주 미작동, 웨이브 1 배포 후 다음 주부터. 다음 실행 8/23.<br>(이전) **2026-08-09 03:12** (fetched=2460 new=200 duplicate=2260 error=0, cap=200 도달). 8/2 12:00에 예고한 "다음 실행 8/9"가 예정대로 수행됐다. **주 1회(일요일 03:00) 잡** — 실행 이력 6/14·6/21·6/28·7/5·7/12·7/19·7/26·8/2·8/9가 전부 일요일. 평일에 "최근 실행이 며칠 전"인 것은 stale이 아니며 다음 실행은 8/16. |
| 이 달의 명국 | 353 |
| 글로서리 | 18/18 — 시드 소진 (7/8 `susangjeon` 게시로 완료) |
| FAQ | 9/9 — 시드 소진 |
| sitemap 글로서리·FAQ URL | 27 (글로서리 18 + FAQ 9, prod sitemap 실측 7/8) |

## 분석

| 항목 | 값 |
|---|---|
| 최근 주간 리포트 | **2026-W33.md** (8/16 09:01 생성 — 대국 **1국**(#338, 8/11)/누적 **338국**, `visit_hits` 이번 주 **47건**(전주 56, -16%), 유입 소스 search 25 · direct 22 — 네이버 모바일 15건 중심, 착지 대부분 글로서리(37). 4주 추이 18 → 5 → 1 → **1**. 신규 콘텐츠 0편. 에이전트 특이사항 2건은 오케스트레이터 검증 결과 둘 다 결함 아님 — `/api/stats` 401은 W32와 동일(배포 이후 변화라는 추정은 시점 오류), `sessions` 0행은 90일 세션 코드 미발효(재기동 전) 때문이며 웨이브 1 배포 후 적재 여부를 다음 주에 확인.<br>(이전) **2026-W32.md** (8/9 09:00 생성 — 대국 **1국**(전주 5국 대비 -80.0%)/누적 **337국**, `visit_hits` 이번 주 56건(전주 27), `session_history` 신규 5건). **사용량 3주 연속 감소(18 → 5 → 1국)** 이며 8/6 이후 신규 대국이 0건이다. 인프라는 전 항목 정상이므로 장애가 아니라 유입 문제다.<br>(이전) **2026-W31.md** (8/2 09:00 생성 — 대국 **5국**(전주 18국 대비 -72.2%)/누적 **336국**). W32는 미생성이나 **stale 아님** — `com.inkbaduk.analytics-weekly`는 `Weekday 0` = 주 1회 일요일 잡이라 다음 실행은 **8/9(일)** 이고 그때 W32가 나온다. 참고 — 리포트 이후 8/6 21:55 KST에 대국 **#337**이 추가돼 DB 실측 누적은 **337국**, `visit_hits`는 **108행**(8/7 18:00 100행)이다. |
| 누적 리포트 수 | 12 |
| analytics-weekly 상태파일 | **✅ 8/16 09:01 최초 생성 — 배선 정상 확정.** `{"status":"ok","summary":"2026-W33 리포트 생성 — 방문 47건","at":"2026-08-16T09:01:14+09:00"}`. 8/15까지 걸어 둔 "미생성이면 배선 결함" 전환 조건은 발동하지 않았다. 이로써 `state/jobs/*.json` 3잡(dev-cycle·content-draft·analytics-weekly) 체계 완성.<br>(이전) **없는 것이 정상 (8/15 21:00 재확인 — 오늘까지만).** plist가 `Weekday 0` = 일요일 09:00 주 1회이고 마지막 실행이 **8/9 09:01**인데, `state/jobs/*.json` 기록 방식(`report-job-status.sh`)은 같은 날 **19:38**(`0c9fdd2`)에 도입됐다 — 아직 이 방식으로 실행된 적이 한 번도 없다. `docs/ops/analytics-prompt.md`에는 호출이 이미 들어 있으므로 첫 상태파일은 **8/16(일)** 실행분이다. 파일 부재를 `fail`로 읽지 말 것. **8/16 실행 후에도 파일이 없으면 그때는 배선 결함**이니 이 행을 결손으로 전환할 것. |

## 개발 현황

| 항목 | 값 |
|---|---|
| 열린 이슈 | **(8/17 09:00) 0건 — 미분류 0건.** `gh issue list --state open --search "no:label"` 0, `--label bug` 0. 백로그 공백 열하루째, 트리아지 대상 0건. ⚠️ 02:00 dev-cycle은 세션 한도로 비정상 종료(백로그 0건이라 상실 작업 0, 8/18 02:00 복귀 판정).<br>(이전) **(8/16 21:00) 0건 — 미분류 0건.** `gh issue list --state open` 0, `--search "no:label"` 0, `--label bug` 0. 백로그 공백 열흘째, 트리아지 대상 0건.<br>(이전) **(8/16 09:00) 0건 — 미분류 0건.** `gh issue list --state open --search "no:label"` 0, `--label bug` 0. 백로그 공백 아흐레째, 트리아지 대상 0건.<br>(이전) **(8/15 21:00) 0건 — 미분류 0건.** `gh issue list --state open` 0, `--search "no:label"` 0, `--label bug` 0. 백로그 공백 **여드레째** 유지, 트리아지 대상 0건.<br>(이전) **(8/15 09:00) 0건 — 미분류 0건.** `gh issue list --state open --label bug` 0. 백로그 공백 **이레째** 유지. 02:00 dev-cycle도 `gh repo view --json issues`의 `totalCount=0`으로 교차 검증했고 `--state all`은 과거 이슈를 정상 반환 = 조회 실패가 아니라 상류가 조용한 것.<br>(이전) **(8/14 21:00) 0건 — 미분류 0건.** `gh issue list --state open` 0, `--search "no:label"` 0, `--label bug` 0. 백로그 공백 **엿새째** 유지.<br>(이전) **(8/14 09:00) 0건 — 미분류 0건.** `gh issue list --state open` 0, `--search "no:label"` 0. 백로그 공백 **엿새째** 유지. 02:00 dev-cycle도 `gh repo view --json issues`의 `totalCount=0`으로 교차 검증했고 `--state all`은 과거 이슈를 정상 반환 = 조회 실패가 아니라 상류가 조용한 것.<br>(이전) **(8/13 21:00) 0건 — 미분류 0건.** `gh issue list --state open` 0, `--search "no:label"` 0. 백로그 공백 닷새째 유지.<br>(이전) **(8/13 09:00) 0건 — 미분류 0건.** `gh issue list --state open` 0, `--search "no:label"` 0. 백로그 공백 닷새째. 02:00 dev-cycle이 `gh repo view --json issues`의 `totalCount=0`으로 교차 검증했고 `--state all`은 과거 이슈를 정상 반환 = 조회 실패가 아니라 상류가 조용한 것.<br>(이전) **(8/12 21:00) 0건 — 미분류 0건.** `gh issue list --state open` 0, `--search "no:label"` 0. 백로그 공백 나흘째 유지.<br>(이전) **(8/12 09:00) 0건 — 미분류 0건.** `gh issue list --state open` 0, `--search "no:label"` 0. 백로그 공백 나흘째이나 트레이스백 0 · 5xx 0 · #78 무재발이 근거로 있어 **자동화 장애가 아니라 상류가 조용하다는 신호**다. |
| 열린 PR | **(8/17 09:00) 0건 — 주의 PR 0건.** CI 실패·충돌·정체 각 0건.<br>(이전) **(8/16 21:00) 0건 — 주의 PR 0건.** CI 실패·충돌·정체 각 0건.<br>(이전) **(8/16 09:00) 0건 — 주의 PR 0건.** CI 실패·충돌·정체 각 0건. 웨이브 1은 PR 경로가 아니라 사람이 로컬 머지(08:17)로 들여왔다 — 배포는 AP-20260816-01 대기.<br>(이전) **(8/15 21:00) 0건 — 주의 PR 0건.** CI 실패·충돌·정체 각 0건. 8/8 06:47 #79 머지 이후 **열흘째** 신규 PR 없음.<br>(이전) **(8/15 09:00) 0건 — 주의 PR 0건.** CI 실패·충돌·정체 각 0건. 8/8 06:47 #79 머지 이후 **열흘째** 신규 PR 없음.<br>(이전) **(8/14 21:00) 0건 — 주의 PR 0건.** CI 실패·충돌·정체 각 0건. 8/8 06:47 #79 머지 이후 **아흐레째** 신규 PR 없음.<br>(이전) **(8/14 09:00) 0건 — 주의 PR 0건.** CI 실패·충돌·정체 각 0건. 8/8 06:47 #79 머지 이후 **아흐레째** 신규 PR 없음.<br>(이전) **(8/13 21:00) 0건 — 주의 PR 0건.** CI 실패·충돌·정체 각 0건. 8/8 06:47 #79 머지 이후 **여드레째** 신규 PR 없음. |
| CI e2e 상태 | **(7/25 12:00) PR #66 런에서 e2e 잡 SUCCESS 확인** — #59 머지 이후 회복 상태 유지. `[[ci-e2e-disabled]]`의 "main 만성 RED" 서술은 현재 사실과 다르므로 갱신 대상. e2e 잡은 활성(`if:` 조건 없음). |
| SQLite locked 판정 | **(8/15 21:00) 재발 0건 12일째.** `.err`가 96시간 델타에 한 줄도 늘지 않아(225,429행 **여덟 사이클 연속** 불변) 마지막 발생은 여전히 225,347행 < 기동 마커 225,423행이다. 근본 수정(수 커밋 후 genmove)은 미완이라 회귀 감시는 유지한다.<br>(이전) **(8/15 09:00) 재발 0건 11일째.** `.err`가 84시간 델타에 한 줄도 늘지 않아(225,429행 **일곱 사이클 연속** 불변) 마지막 발생은 여전히 225,347행 < 기동 마커 225,423행이다. 근본 수정(수 커밋 후 genmove)은 미완이라 회귀 감시는 유지한다.<br>(이전) **(8/14 21:00) 재발 0건 10일째.** `.err`가 72시간 델타에 한 줄도 늘지 않아(225,429행 **여섯 사이클 연속** 불변) 마지막 발생은 여전히 225,347행 < 기동 마커 225,423행이다. 근본 수정(수 커밋 후 genmove)은 미완이라 회귀 감시는 유지한다.<br>(이전) **(8/14 09:00) 재발 0건 9일째.** `.err`가 48시간 델타에 한 줄도 늘지 않아(225,429행 **다섯 사이클 연속** 불변) 마지막 발생은 여전히 225,347행 < 기동 마커 225,423행이다. 근본 수정(수 커밋 후 genmove)은 미완이라 회귀 감시는 유지한다.<br>(이전) **(8/13 21:00) 재발 0건 8일째.** `.err`가 36시간 델타에 한 줄도 늘지 않아(225,429행 **네 사이클 연속** 불변) 마지막 발생은 여전히 225,347행 < 기동 마커 225,423행이다.<br>(이전) **(8/13 09:00) 재발 0건 7일째 유지.** `.err`가 24시간 델타에 한 줄도 늘지 않아(225,429행 두 사이클 연속 불변) 마지막 발생은 여전히 225,347행 < 기동 마커 225,423행이다. 게임 336·337·338 세션 로그(225,399~225,429행)에도 락 0건.<br>(이전) **(8/12 21:00) 재발 0건 6일째 유지.** `.err`가 12시간 델타에 한 줄도 늘지 않아(225,429행 불변) 마지막 발생은 여전히 225,347행 < 기동 마커 225,423행이다.<br>(이전) **(8/12 09:00) 재발 0건 6일째.** 마지막 발생 `.err` 225,346행 < 기동 마커 225,423행 = 전부 기동 이전. 8/11 게임 #338의 103수 + 힌트 14회 실사용 쓰기 부하에서도 0건이었다. 표본 1국이라 해소 확정은 유보하나 "부하 부재" 알리바이는 사라졌다(`[[database-locked-genmove]]`). |
| 미병합 브랜치 | **(7/24 18:00 정정) 로컬 미push 브랜치는 0건이다.** `git for-each-ref --format='%(upstream:short)'` 실측 — 로컬 13개 중 12개가 origin 추적 브랜치를 갖고, 나머지 1개는 워크트리용 `worktree-feat+glossary-media-phase1`(5/26)뿐이다. 7/24 02:00 dev-cycle 로그의 "미push 6개"(`feat/format-game-result` 등)와 아래 "로컬 전용 2건" 서술은 **모두 오탐** — 해당 브랜치들은 전부 origin에 존재한다. `[[dev-cycle-unpushed-branches]]` 대응 조치 불필요. 남은 것은 push 여부가 아니라 **정리(삭제) 여부**이며 이는 사람 재량.<br>**(이전 기록 7/14 18:30 · 7/17 18:30)** 원격 `feat/last-seen-at-debounce`·`fix/issue-39`는 **오탐** — PR #30·#45가 **squash merge**돼 원본 커밋이 main 조상이 아닐 뿐, 내용은 main에 반영됨(`git grep` 확인). 사람 판단 불필요. → 판정은 `--no-merged`가 아니라 PR 상태+`git grep <ref>`로. 원격 브랜치 잔여물 삭제는 사람 재량.<br>**superseded(폐기 가능) 4건** — `fix/sqlite-busy-timeout`·`fix/auto-resign-respect-score-margin`·`fix/scoring-gate-too-strict`·`fix/issue-12`: 수정 내용이 다른 커밋으로 main에 이미 반영됨.<br>**실제 사람 판단 대기 = 로컬 전용 2건**: `feat/format-game-result`(main에 `formatResult` 부재), `feat/i18n-ja-zh`(main 로케일 ko·en만) — PR 생성 또는 폐기. 둘 다 **최종 커밋 4/30 = 2.5개월 경과**, feature 경로라 dev-cycle 자율 범위 밖 → 오케스트레이터는 자율 push하지 않고 보고만 유지(정책 L16 보수 처리). |

## 보류 승인

`state/pending-approvals.md` 참조 — **대기 1건 (8/17 09:00 기준): AP-20260816-01 — 48시간째 · 2회 재확인, 상태 변화 없음.** ahead **21**(코드 19 + ops 2), `BUILD_ID`·PID 8/8 15:48 그대로(209시간 무재기동), `sessions` 0행 = 미발효 지속. 진행 중 대국 0건(마지막 수 29시간 전)이라 재기동 위험 낮음 — 절차 불변. push 보류 방침 유지.<br>(이전) **대기 1건 (8/16 21:00 기준): AP-20260816-01 — 12시간째 · 1회 재확인, 상태 변화 없음.** ahead 20 유지, `BUILD_ID`·PID 8/8 15:48 그대로, `alembic_version` 0019 유지. 위험 재평가 — 오늘 12:45 KST 대국 #339(88수) 발생·종료로 실사용이 재개됐으니 배포 실행 시점에 진행 중 대국 재확인 권장(절차 불변). push 보류 방침 유지.<br>(이전) **대기 1건 (8/16 09:00 기준): AP-20260816-01 — 웨이브 1(사이트 활성화) 19커밋 배포**(origin push + 웹 리빌드 + prod 재기동 + 5-3 스모크). 사람이 08:17 로컬 main에 머지한 것의 라이브 반영 절차로, 마이그레이션(0018·0019)은 선행 적용 확인됨. 재기동 위험 낮음(진행 중 대국 0건). **이 승인 전까지 오케스트레이터는 ops 로그 커밋을 로컬에만 두고 push하지 않는다** — main push가 코드 19커밋을 함께 밀기 때문.<br>(이전) **대기 0건 (8/15 21:00 기준).** '## 대기 중' = `(없음)`. 이번 사이클 수행분은 전부 🟢 자율 범위(헬스체크·백업 검증·버그 스캔·트리아지·PR 감시 + `docs/ops/state/**` 운영 로그 push)라 신규 🟡 등재 항목이 없다.<br>(이전) **대기 0건 (8/15 09:00 기준).** '## 대기 중' = `(없음)`. 이번 사이클 수행분은 전부 🟢 자율 범위(헬스체크·백업 검증·버그 스캔·트리아지·PR 감시 + `docs/ops/state/**` 운영 로그 push)라 신규 🟡 등재 항목이 없다.<br>(이전) **대기 0건 (8/14 21:00 기준).** '## 대기 중' = `(없음)`. 이번 사이클 수행분은 전부 🟢 자율 범위(헬스체크·백업 검증·버그 스캔·트리아지·PR 감시)라 신규 🟡 등재 항목이 없다.<br>(이전) **대기 0건 (8/14 09:00 기준).** '## 대기 중' = `(없음)`. 이번 사이클 수행분은 전부 🟢 자율 범위(헬스체크·백업 검증·버그 스캔·트리아지·PR 감시)라 신규 🟡 등재 항목이 없다.<br>(이전) **대기 0건 (8/13 21:00 기준).** '## 대기 중' = `(없음)`. 이번 사이클 수행분은 전부 🟢 자율 범위(헬스체크·백업 검증·버그 스캔·트리아지·PR 감시)라 신규 🟡 등재 항목이 없다.<br>(이전) **대기 0건 (8/13 09:00 기준).** '## 대기 중' = `(없음)`. 이번 사이클에 수행한 것은 🟢 자율 범위(헬스체크·백업 검증·버그 스캔·트리아지·PR 감시)뿐이라 신규 🟡 등재 항목이 없다.<br>(이전) **대기 0건 (8/12 21:00 기준).** '## 대기 중' = `(없음)`. 이번 사이클에 수행한 것은 🟢 자율 범위(헬스체크·백업 검증·버그 스캔·트리아지·PR 감시)뿐이라 신규 🟡 등재 항목이 없다.<br>(이전) **대기 0건 (8/9 18:00 기준, 세 사이클 연속 큐 비어 있음).** '## 대기 중' = `(없음)`. 이번 사이클에 🟡 등급 액션을 만난 적이 없어(🟢 자율 범위인 헬스체크·백업 검증·버그 스캔·트리아지·PR 감시만 수행) 신규 등재 항목도 없다.<br>(이전) **대기 0건 (8/9 12:00 기준, 두 사이클 연속 큐 비어 있음).** '## 대기 중' = `(없음)`. 신규 등재할 🟡 항목도 없다 — 열린 PR 0건 · 배포 갭 0커밋이고, 이번 사이클에 🟡 등급 액션을 만난 적이 없다(🟢 자율 범위인 헬스체크·백업 검증·버그 스캔·트리아지·PR 감시만 수행).<br>(이전) **대기 0건 (8/8 18:00 기준, 큐 비어 있음).** AP-20260808-01 + AP-20260805-01이 8/8 15:47~15:49 사람 승인으로 함께 실행돼 큐가 비었고, 이번 사이클에 신규 등재할 🟡 항목도 없다(열린 PR 0건 · 배포 갭 0커밋).<br>(이전 8/8 **12:00** 기준 — **대기 2건**. **① AP-20260808-01 = PR #79 머지(신규 등재)** — CI 4잡 그린·CLEAN·웹 어드민 10파일·마이그레이션 없음. **② AP-20260805-01 = #75·#77 prod 배포, 제안 후 72시간 무응답** — 판별 검사 3종이 일곱 번째로 동일해 미반영 확정 유지, 웹 프로세스 133시간 50분 무재기동.<br>**🆕 두 항목은 이제 한 묶음이다.** #79를 머지하면 origin이 앞서므로 AP-20260805-01의 재빌드 앞에 **`git pull --ff-only`가 반드시 선행**돼야 한다 — 빠뜨리면 #79 없는 트리에서 빌드가 돈다. 이 단계를 AP-20260808-01 실행 절차에 명시하고 양쪽에 상호 참조를 남겼다. **한 번의 재기동으로 #75·#77·#79 세 건이 함께 발효**한다. 재기동 위험은 계속 낮다 — DB `max(id)` 337 불변, 마지막 수 약 45시간 전이라 중단될 대국 없음. 중복 제안은 만들지 않고 기존 항목에 재확인 각주만 누적했다.)<br>(이전 8/7 18:00 기준. **AP-20260805-01 = #75·#77 prod 배포(웹 재빌드 + 재기동), 제안 후 54시간 무응답.** 판별 검사 3종이 여섯 번째로 동일해 미반영이 확정 유지된다. **재기동 위험은 12:00보다 더 내려갔다** — 마지막 수가 20시간 전이고 6시간 델타에 `POST /api/games` 0건이라 중단될 대국이 없다. 긴급도 근거(8/6 실사용 재개로 #77 사용자 대면 미배포 비용 상승)는 유지되나, 이번 6시간은 트래픽이 얇았다(`visit_hits` +1행). 중복 제안은 만들지 않고 기존 항목에 재확인 각주만 누적한다.)<br>(이전 8/7 12:00 기준. **AP-20260805-01 = #75·#77 prod 배포(웹 재빌드 + 재기동), 제안 후 48시간 무응답.** 판별 검사 3종이 다섯 번째로 동일해 미반영이 확정 유지된다. ⚠️ 긴급도 근거는 이번에 상향됐다 — 5일 만에 실사용 대국 #337과 방문 14건이 재개돼 #77의 사용자 대면 결과 표기 미배포가 실제로 보이는 상태다. 재기동 위험은 그대로 낮다(마지막 수 14시간 전). 중복 제안은 만들지 않고 기존 항목에 재확인 각주만 누적한다.)

- **AP-20260802-01 (8/2 12:00 신규 등재, 18:00 기준 6시간 무응답)** — 머지된 4커밋의 prod 배포(🟡). `git pull --ff-only` + `web` 재빌드 + `ops/stack.sh restart prod`.<br>**미배포의 대가가 이번 사이클에도 실측됐다** — `orchestrator-runs.log` **18:00:05** 항목에 #71이 고칠 `Write(...)` deny 무실효 경고 2건이 또 출력됐다(머지 후 19시간). 즉 prod DB·KataGo 모델 가드레일은 아직 실효가 없다.<br>**단계별 발효 범위** — 1단계 `git pull`만으로 #71(가드레일)·#72(watchdog 성공마커, 크래시 루프 사각지대)가 **재기동 없이** 즉시 발효한다. #66(spectate 원자적 UPDATE)은 backend 재기동, #73(용어집 다이어그램)은 웹 재빌드가 필요하다.<br>충돌 없음 실측(유입 9파일 ∩ 로컬 미커밋 = 공집합), 4커밋 전부 머지 시점 CI 4잡 SUCCESS. 진행 중 대국 0건이라 재기동 리스크 낮음. 중복 제안은 만들지 않았다.

- (처리 완료) **AP-20260730-01 (7/30 12:00 신규 등재)** — PR [#72](https://github.com/rarebirds-svg/quite-baduk/pull/72) 머지(🟡). 7/30 02:00 dev-cycle이 #70을 고쳐 올린 PR인데 승인 큐에 등재돼 있지 않아 이번 실행에서 추가했다. watchdog가 "마지막 timestamp"가 아니라 "마지막 **성공** 종료 마커"로 신선도를 판정하게 되는 수정으로, 7/28 실사례에 대입하면 orchestrator 25.9h·dev-cycle 41.9h로 임계를 넘겨 그날 저녁 알림이 나갔을 건이다(구 로직 2.0h·18.0h로 무감지). CI 4잡 SUCCESS·`CLEAN`, 변경 파일 `ops/check-staleness.sh` + 신규 테스트 2개뿐 — **마이그레이션·prod 재기동 불필요**. **이 PR이 미머지인 동안 watchdog의 크래시 루프 사각지대는 그대로 열려 있다.**
- (처리 완료) **AP-20260725-01 — 8/1 23:00 사람 머지(`fd68fa3`). 제안 후 189시간(7.9일).** 단 backend 재기동 전까지 라이브 미반영이라 AP-20260802-01로 승계됐다. (등재 당시 기록) PR #66 머지(🟡). 이슈 #65 수정본이고 CI 4잡 전부 그린·`mergeable=MERGEABLE`·마이그레이션 없음인데 머지 대기 중. **긴급도는 내려갔다** — 7/26 20:24 배포로 `busy_timeout` 30s가 라이브가 돼 500 임계가 6배 올라갔다(02:00 dev-cycle 규명). 다만 그 완화는 *에러를 지연으로 바꾸는* 성격이라 #66의 락 보유시간 단축과 보완 관계이고, 머지 가치는 유지된다. 긴급 머지는 아니되 취소 대상도 아님.
- (처리 완료) **AP-20260723-01 — 8/1 23:00 사람 머지(`1ca3bcd`). 제안 후 227시간(9.5일).** 단 `git pull` 전까지 경고 2건이 계속 재출력되며 8/2 12:00·18:00 세션에서 연속 실측됐다 — AP-20260802-01로 승계. (등재 당시 기록) `.claude/settings.json` deny 규칙 2건이 `Write(...)`로 작성돼 **실효 없음**(하네스 경고: `Edit(path)` 규칙만 파일 편집 도구를 포괄). 결과적으로 라이브 prod DB(`backend/data/baduk.db`, 백업 원본)와 KataGo 모델이 에이전트 쓰기로부터 보호되지 않음. 설정 변경이라 🟡. 7/26·7/27·7/29 실행 모두 동일 경고 2건 재출력 = **6일째 미해소 확인**.<br>**(7/29 12:00) 상태 변경 — 이제 머지만 남았다.** 실행 절차 중 🟢 구간(브랜치 push + PR 생성)을 오케스트레이터가 선행해 **PR [#71](https://github.com/rarebirds-svg/quite-baduk/pull/71)** 을 올렸다. 6일간 "브랜치조차 없어 사람이 머지할 대상이 없는" 상태였던 것이 해소됐다.

AP-20260716-01(#55)·AP-20260717-01(#57)은 7/21 19:54 KST 사람 승인·머지로 처리 완료.

## 최근 장애

`state/incidents.md` 참조.

**신규 — OPS-20260728-01 (2026-07-28 02:00 ~ 07-29 02:00, 자동 복구됨)**: 인증 만료(`OAuth session expired and could not be refreshed`)로 dev-cycle 1회 + orchestrator 2회가 **시작 직후 비정상 종료**. 약 24시간 운영 감시 공백. **같은 구간 prod는 무사고**(사후 검증: 200 OK · 백업 2회 정상 · 신규 트레이스백 0건) — 유실된 것은 서비스가 아니라 감시다. **watchdog는 전 구간 `신규 incident 0 건`으로 오판**했고, 원인은 아래 Watchdog 섹션의 사각지대 항목이다. 후속 이슈 [#70](https://github.com/rarebirds-svg/quite-baduk/issues/70).

기존 3건은 복구·정리 완료. 2026-05-25 watchdog 활성화 직후 5건 (WD-20260525-2122*) 자동 기록 — 41시간 무음 정지 사건의 사후 감지. 5/27 03:35~17:35 동안 content-draft·content-ingest stale 30건 자동 누적은 watchdog 임계와 plist Weekday 잡 정합성 misalignment에 의한 false-positive였고, 이후 `72ec362`에서 임계를 9d로 정합화. 5/28 04:00 이후 watchdog 신규 incident 0건 — 누적 해소 확인.

## Watchdog

| 항목 | 값 |
|---|---|
| 오케스트레이터 주기 정합성 | **(8/1 12:00 재확인, 미해소)** 이번 트리거도 **12:00:04** = plist(12:00·18:00 2회)대로 발화. 프롬프트·러닝북의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지 — 유실이 아니라 문서·plist 미정합이다. **7/31 18:00에 예고한 실무 영향이 그대로 실현됐다** — 23:30 사이클이 실재하지 않아 PR #66의 7일 정체 전환(8/1 02:06)이 **10시간 뒤인 이번 12:00에야** 감지됐다. 사람 판단 대기 유지.<br>**(7/31 18:00 재확인, 미해소)** 이번 트리거도 **18:00:05** = plist(12:00·18:00 2회)대로 발화. 프롬프트·러닝북의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지 — 유실이 아니라 문서·plist 미정합이다. 실무적 영향 1건 확인 — **23:30 사이클이 실재하지 않으므로 PR #66의 7일 정체 전환(8/1 02:06)은 8/1 12:00에야 감지된다.** 사람 판단 대기 유지.<br>**(7/31 12:00 재확인, 미해소)** 이번 트리거도 **12:00:05** = plist(12:00·18:00 2회)대로 발화. `orchestrator-runs.log` 실측은 7/21 이후 하루 2회로 **누락 없이 일관**(7/28 인증 만료 건 제외)이므로 유실이 아니라 문서·plist 미정합이다. 사람 판단 대기 유지.<br>**(7/30 18:00 재확인, 미해소)** 이번 트리거도 **18:00** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지, 사람 판단 대기.<br>**(7/30 12:00 재확인, 미해소)** 이번 트리거도 **12:00:05** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지, 사람 판단 대기.<br>**(7/29 18:00 재확인, 미해소)** 이번 트리거도 **18:00:05** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회 표기와 불일치 유지, 사람 판단 대기.<br>**(7/29 12:00 재확인, 미해소)** 이번 트리거도 **12:00:05** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회 표기와 여전히 불일치, 사람 판단 대기 유지. **다만 7/28 12:00·18:00은 발화는 됐으나 인증 만료로 무작업 종료** — 주기 문제가 아니라 OPS-20260728-01 건이다(위 사각지대 행 참조).<br>**(7/27 18:00 재확인, 미해소)** 이번 트리거도 **18:00:01** = plist(12:00·18:00 2회)대로 발화(누락 아님). 사람 판단 대기 유지.<br>**(7/27 12:00 재확인, 미해소)** 이번 트리거도 **12:00:00 정각** = plist대로 발화(누락 아님). 문서·프롬프트의 06:30·12:30·18:30·23:30과 여전히 불일치. 사람 판단 대기 유지.<br>**(7/26 18:00 재확인, 미해소)** plist는 여전히 12:00·18:00 2회이며 이번 18:00 트리거도 plist대로 정상 발화 = 누락 아님. 사람 판단 대기 유지.<br>**(7/24 12:00 신규) 문서 4회 vs 실제 2회 불일치.** 러닝북 5종·오케스트레이터 프롬프트는 `매일 06:30·12:30·18:30·23:30`으로 적으나, `ops/launchd/com.inkbaduk.ops-orchestrator.plist`의 `StartCalendarInterval`은 **12:00·18:00 2회**뿐(헤더 주석도 "매일 12시·18시"). `orchestrator-runs.log` 실측 일치(7/23 18:00 → 7/24 12:00) = **누락이 아니라 미등록**. `sync-launchd.sh --check` drift 없음, `pmset` sleep 0 → 슬립 유실 아님. 문서를 2회로 맞출지 plist를 4회로 늘릴지는 사람 판단(후자는 prod 스케줄 변경이라 🟡). |
| 잡 등록 | `com.inkbaduk.ops-watchdog` 가동 중 (StartInterval 3600s) |
| 첫 실행 | 2026-05-25 21:22 (수동 kickstart, 신규 incident 5건) |
| 알림 채널 | Telegram (토큰 미설정 → fallback) → macOS notification |
| 임계 | orchestrator 18h · dev-cycle/backup 30h · content-draft/content-ingest 9d · analytics-weekly 8d |
| 쿨다운 | 잡당 1시간 1회 |
| **사각지대 (7/29 12:00 신규)** | **`ops/check-staleness.sh`는 "trigger 후 즉시 실패"를 감지하지 못한다.** `extract_last_ts()`가 로그의 마지막 `[YYYY-MM-DD HH:MM:SS]` 패턴만 뽑아 임계와 비교하는데, 실패 종료도 `[...] 비정상 종료` 라인으로 timestamp를 새로 찍으므로 잡이 "방금 실행됨"으로 보인다. launchd trigger **유실**은 잡지만 trigger는 됐는데 아무 일도 못 하는 모드는 **크래시 루프여도 영구히 무음**이다. OPS-20260728-01(3회 유실, watchdog 0건)이 실증 사례. 이슈 [#70](https://github.com/rarebirds-svg/quite-baduk/issues/70) — 성공 종료 마커 기준 판정으로 수정 제안. |
| 정합성 이슈 | (해소) `72ec362`에서 content-draft·content-ingest 임계를 30h → 9d로 plist 일정과 정합화. 신규 incident 0건 확인. |
