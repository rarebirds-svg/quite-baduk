# 운영 대시보드

- 갱신: 2026-08-10 21:00

## 스택 상태

| 스택 | 상태 | 마지막 확인 |
|---|---|---|
| prod | 정상 — backend·web 200 OK, db OK, 디스크 6%, plist drift 없음. PID api **88640** / web **88644**.<br>**(8/10 21:00) 전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 다섯 사이클 연속. 새 스케줄의 첫 `pm` 슬롯이며, 이번 사이클의 실질 성과는 09:00 사이클이 남긴 미push 커밋 1건을 발견해 origin에 올린 것이다.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web 200, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `ps etime` **02-05:13:46 = 53시간 14분** 무재기동(`lstart Aug 8 15:48:51`). watchdog `[api] OK`·`[web] OK`·`신규 incident 0 건`.<br>**🆕 09:00 ops 커밋 `08f9bda`가 미push 상태였고 이번 사이클에 push했다.** `origin/main..HEAD` = **1**. 배포 갭 지표(`HEAD..origin/main`)는 **방향이 반대라 이 결함을 구조적으로 못 잡는다** — 두 방향을 다 봐야 보인다. 대상이 `docs/ops/state/**` 상태 로그뿐이라 🟡(코드·설정 `main` push) 대상이 아니며, 선례(`2de2de0`·`6e92f4a`)와도 일치한다.<br>**배포 갭 0 — 라이브가 최신 런타임 코드임을 09:00과 같은 두 축으로 재확인.** `HEAD..origin/main` **0** + 재기동 이후 유입 **17커밋**에 `backend/app/**`·`web/{app,components,lib,store}/**` 변경 **0건**. `BUILD_ID` mtime `Aug 8 15:48` 고정은 갭이 아니라 정상(`[[merge-is-not-deploy]]`).<br>**트레이스백 0건 · 5xx 0건** — `.err` mtime `Aug 8 15:48` **불변(3일째)**. 최근 3,000줄 200 **1,963** / 401 855 / 404 120 / 204 57 / 201 2, 최근 300줄 200 **293** / 404 3 / 401 2 / 204 2.<br>**✅ #78 무재발 4일째** — 최근 1,000줄 어드민 401 **0건**, 잔여 401은 `/api/session` 정상 거절(`[[bug-scan-verify-recency]]`).<br>**🔎 404 120건은 크리덴셜 스캐너 스윕** — `auto_login` 7·`.env` 5·`{run,exec,execute}`·`{env,settings,config.json}` 탐침, 출처 `185.177.72.56`(82)·`136.115.79.21`(27)·`209.112.91.75`(3)·`2a04:c300:400::15`(1). **해당 대역 200 응답 0건**(노출 없음)이라 incident 아님.<br>**자동화 잡 — 보고 1건 `ok`.** `jobs/dev-cycle.json` ok(02:01). `content-draft`(다음 8/12)·`analytics-weekly`(다음 8/16)는 실행 시각 미도래라 파일 부재가 정상.<br>**⚠️ 대국 활동 0건 4일째** — DB `max(id)` **337** 불변, 마지막 수 `2026-08-06 13:04:14 UTC` = 약 **95시간 전**. 다만 `visit_hits` 최근 24시간 **9건**으로 방문 자체는 이어지므로 끊긴 지점은 접속이 아니라 **대국 시작**이다.<br>(이전) **(8/10 09:00) 전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 네 사이클 연속. 새 스케줄(09:00·21:00)의 첫 실행이며, 이번 사이클의 실질 성과는 "배포 갭 0"이 위장 0이 아님을 유입 커밋의 경로 집합으로 확정한 것이다.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web 200, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `ps etime` **01-17:11:56 = 41시간 12분** 무재기동(`lstart Aug 8 15:48:51`). watchdog `[api] OK`·`[web] OK`·`신규 incident 0 건`. `launchd-sync.log` 말미 `[2026-08-09 22:55:16] resynced com.inkbaduk.ops-orchestrator.plist` + 이번 세션 기동 **09:00:13** = plist 재설치 발효 확인.<br>**✅ 배포 갭 0이 위장 0이 아님을 확정 — 유입 18커밋이 전량 비런타임이다.** `HEAD..origin/main` **0** / `origin/main..HEAD` **0**(HEAD = `origin/main` = `e8fc5fd`)에서 멈추지 않고, 8/8 15:48 재기동 이후 main에 들어온 **18커밋**의 변경 경로를 실측했다 — `docs/ops/**`·`ops/**`·`.gitignore`·`e2e/package*.json`·`web/tests/admin-auth-expiry.test.tsx`뿐이고 **`backend/app/**`·`web/{app,components,lib}/**` 변경 0건**이다. 따라서 `BUILD_ID` mtime `Aug 8 15:48` 고정은 갭이 아니라 정상이며 재빌드·재기동이 불필요하다. `ops/check-staleness.sh`·plist는 작업 트리 직접 실행이라 pull 시점에 이미 발효. **향후 판정은 `BUILD_ID` mtime 단독보다 `git log --since=<재기동시각> --name-only`의 런타임 경로 포함 여부가 정확하다**(`[[merge-is-not-deploy]]`).<br>**트레이스백 0건 · 5xx 0건** — `.err` mtime `Aug 8 15:48` **불변(2일째)**, 말미 60줄이 전부 WS accept/close·uvicorn 기동 INFO. 최근 4,000줄 200 **2,708** / 401 1,105 / 404 121 / 204 60 / 201 3.<br>**✅ #78 무재발 3일째** — 어드민 401 1,048건(4엔드포인트 × 262회 균등)은 여전히 8/6~8/7 밴드에 갇혀 있고 **최근 1,000줄 0건**, 최근 300줄은 200 280 / 204 8 / 401 8 / 404 4다. 잔여 401은 `/api/session` 55·`/api/stats` 2로 전부 정상 거절(`[[bug-scan-verify-recency]]`).<br>**자동화 잡 — 보고 1건 전부 `ok`.** `jobs/dev-cycle.json` ok(02:01). `content-draft`(`Weekday 3·6` 02:00 → 다음 8/12)·`analytics-weekly`(`Weekday 0` 09:00 → 다음 8/16)는 jobs 디렉터리 생성(8/9 22:39) 이후 실행 시각이 없어 **파일 부재가 정상**이며 `fail`로 읽지 않는다.<br>**⚠️ 대국 활동 0건 4일째** — DB `max(id)` **337** 불변, 마지막 수 `2026-08-06 13:04:14 UTC` = 약 **92시간 전**. 인프라 전 항목 정상이므로 장애가 아니라 유입 신호.<br>(이전) **(8/9 18:00) 전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 세 사이클 연속. 이번 사이클의 실질 성과는 12:00이 로그 위치로 내린 #78 판정을 DB 타임스탬프라는 독립 경로로 재확인한 것이다.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web 200, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `ps etime` **01-02:14:04 = 26시간 14분** 무재기동(`lstart Aug 8 15:48:51`). watchdog `신규 incident 0 건`. 라이브 스모크 `/`·`/spectate/pro`·`/glossary`·`/api/spectate/pro?limit=1` 전부 **200**.<br>**✅ #78 무재발 — 밴드의 절대 시각을 못 박았다.** 어드민 401 1,049건은 라인 **229,926~232,613**(전체 234,297)에 갇혀 있고 최근 1,000줄 0건이다. 밴드 시작부 문맥에 `GET /api/games/337` 401이 섞여 있어 DB로 조회하니 게임 337의 마지막 수가 **`2026-08-06 13:04:14 UTC`** — 즉 밴드는 **8/6~8/7**로 #78 본문의 실측 창과 같고 8/8 15:48 배포보다 전체가 앞선다. 액세스 로그에 timestamp가 없는 한계를 DB로 우회했다(`[[bug-scan-verify-recency]]`·`[[in-progress-game-detection]]`).<br>**⚠️ 방법 메모 — health 프로브 개수를 경과시간으로 환산하지 말 것.** 실측 cadence는 90초에 1건(백그라운드 90초 측정, delta=1)인데 12:00→18:00 프로브 증가는 6시간에 78건(≈4.6분/건)으로 **3배 어긋난다**. Cloudflare 프로브는 버스트성이라 개수→시간 환산이 성립하지 않는다.<br>**트레이스백 0건 · 5xx 0건** — `.err` mtime `Aug 8 15:48` 불변. 최근 3,000줄 200 **1,732** / 401 1,095 / 404 117 / 204 50 / 201 3. 잔여 401은 `/api/session` 45·`/api/stats` 2·`games/337` 2로 전부 정상 거절.<br>**🔎 web `.err`의 `Failed to find Server Action` 67건은 이슈화 안 함** — mtime `Aug 8 15:50`으로 재빌드 직후 2분에 멈춰 있다. 배포로 Server Action ID가 갈리며 **구 번들 탭이 낸 일회성 오류**이고 이후 26시간 신규 0건이라 "일시적 아님" 조건 미충족.<br>**🔎 404 117건은 8/2부터의 동일 크리덴셜 스캐너 스윕**, 200 응답 0건(노출 없음). 신규 사실 없어 incident 아님.<br>**⚠️ 대국 활동 0건 3일째** — DB `max(id)` **337** 불변, 마지막 수 **약 68시간 전**, `pro_games` 3311(다음 ingest 8/16). 인프라 정상이므로 장애가 아니라 유입 신호.<br>(이전) **(8/9 12:00) 전 항목 정상 + 큐 4종(이슈·PR·승인·배포 갭) 전부 0건이 두 사이클 연속. 이번 사이클의 실질 성과는 주간 CWI ingest의 라이브 반영 실증과 #78 무재발 20시간 확인이다.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web 200, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 `ps etime` **20:14:22** = 8/8 15:48 배포 후 무재기동 20시간. watchdog `신규 incident 0 건`. 라이브 스모크 `/`·`/spectate/pro`·`/glossary`·`/api/spectate/pro?limit=1` 전부 **200**.<br>**✅ #78 무재발 20시간 — 8/8 18:00 확정 판정이 더 긴 창에서도 유지.** 최근 5,000줄에 어드민 401 **1,049건**(4엔드포인트 × 262회 균등 = 폴링 루프 서명)이 보이나 **최근 1,000줄·300줄 0건**이다. 마지막 어드민 401은 EOF에서 **1,585줄 전**이고 그 사이 health 프로브 **582건**, 결정적으로 **바로 다음 줄이 `GET /api/admin/me` 200**(재로그인) → 이후 어드민 전 경로 200이다. 전환점이 배포 시각(커밋 `eb12635` 8/8 15:47:37 → `lstart` 15:48:51)과 일치한다. 누적 17,344건은 판정에 쓰지 않았다(`[[bug-scan-verify-recency]]`).<br>**트레이스백 0건 · 5xx 0건** — `.err` mtime `Aug 8 15:48` 불변, 기동 마커(line **225423**) 이후 3줄뿐. 최근 5,000줄 200 **3,716** / 401 1,103 / 404 120 / 204 55 / 201 3. 잔여 401은 `GET /api/session` 50(비로그인 크롤러)·`/api/stats` 2(주간 분석 잡)·`games/337` 2로 전부 정상 거절.<br>**🆕 백업 +117,938B 급증은 주간 ingest와 정합 — 이상 신호 아님.** 3,286,600B → **3,404,538B**(종전 일일 증가폭 +1.3~1.6KB의 90배). DB `pro_games` **3111 → 3311(+200)**, 라이브 `/sitemap.xml` `<loc>` **3379**(7/26 2979 + 8/2·8/9 각 +200). **재빌드 없이 라이브 반영됨이 실증됐다**(목록 `force-dynamic`).<br>**🔎 404 120건은 8/2부터 이어지는 동일 크리덴셜 스캐너 스윕** — `auto_login`·`.env`·`{run,exec,execute,system}`·`config.json` 탐침, **200 응답 0건**(노출 없음). 규모만 커진 동일 패턴이라 incident 아님.<br>**🔎 `database is locked` — 마지막 발생 line 225346으로 8/8 판정과 동일(기동 마커 이전).** 8/6 이후 신규 대국 0건이라 알리바이가 강해진 게 아니라 그대로다 — `busy_timeout` 30s가 에러를 지연으로 바꾸는 성격은 미검증인 채이므로 "무재발 = 해소"로 읽지 않는다.<br>**⚠️ 대국 활동 0건 3일째 — 장애 아닌 유입 신호.** DB `max(id)` **337** 불변, 마지막 수 `2026-08-06 13:04:14 UTC` = **62시간 전**, 최근 48h 착수 **0건**. 09:00 W32 리포트의 "3주 연속 감소(18 → 5 → 1국)"와 정합하다. 인프라는 전 항목 정상이므로 서비스 장애가 아니다.<br>(이전) **(8/8 18:00) 전 항목 정상 + 승인 큐·이슈·PR 전부 0건. 이번 사이클의 실질 성과는 15:48 배포의 사후 효과 검증이다.** backend `{"status":"ok","db":true,"katago_alive":true}` 200, web 200, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. PID 88640/88644 uptime **2시간 14분**(`lstart 2026-08-08 15:48:51`) — 배포 후 첫 사이클. `launchctl`의 api 종료코드 `-15`는 배포 시 SIGTERM 흔적이고 현재 PID 생존이라 정상. `katago_alive`가 12:00의 `false`→`true`로 바뀐 것도 lazy spawn 정상 동작이며 러닝북 명시대로 **정보용**이라 어느 값도 단독 판정 근거가 아니다. watchdog `신규 incident 0 건`.<br>**✅ #78 라이브 수정 확정 — 12:00에 유보한 판정을 이번에 근거 3종으로 확정했다.** 12:00은 401 버스트가 멎은 것을 "운영자 탭이 닫힌 결과"로 읽고 이슈를 열어 뒀는데, 배포 이후 구간은 **어드민이 폴링 중인데도 401이 0건**이다. ① 최근 400줄 200 **344** / 404 32 / 401 **11** / 204 11. ② 그 401 **11건 전부 `GET /api/session`** = 비로그인 방문자 정상 거절, 어드민 경로 401 **0건**. ③ 어드민 401 마지막 발생은 EOF에서 **967줄 전**(배포 이전)인데 어드민 **200**은 **31줄 전** = 세션이 살아서 폴링 중이고 응답이 200이다. 최근 2,000줄 누적으로는 어드민 401이 975건이나 전량 배포 이전이다(`[[bug-scan-verify-recency]]`).<br>**트레이스백 0건** — `.err` mtime `Aug 8 15:48` 불변, 기동 마커(line **225423**) 이후 uvicorn 3줄뿐. 최근 20,000줄 **5xx 0건**.<br>**🔎 `database is locked` 재점검 — 최소 5일·기동 5회 전으로 확정.** 누적 2,556건이나 마지막 발생 line **225347** 이후 기동 마커가 **5개**(78066→94302→512→93501→88640) 지나갔고, 93501이 8/2 기동해 12사이클을 버틴 프로세스이므로 마지막 락은 **8/2 이전**이다. SQL은 `UPDATE games SET loss_streak WHERE id=332`(#69 계열)이고 이후 대국 333~337에서 재현 0건. 닫힌 #40·#41·#42·#64·#65 계열이라 이슈화 대상 아님.<br>**404 32건은 8/2부터 이어지는 동일 크리덴셜 스캐너 스윕** — `POST /api/v1/auto_login`이 `185.177.72.0/24`에서 반복되며 전량 404, 200 응답 0건(노출 없음). 신규 사실 없어 incident 아님.<br>**대국 활동 0건** — DB `max(games.id)` **337** 불변, 마지막 수 `2026-08-06 13:04:14 UTC` = 약 **53시간 전**.<br>(이전) **(8/8 15:49 배포 완료 — 사람 승인 AP-20260808-01 + AP-20260805-01)** **5일 14시간 무재기동 구간이 끝났고 #75·#77·#79 세 건이 한 번의 재기동으로 함께 발효했다.** PR #79 스쿼시 머지(`eb12635`) → `git pull --ff-only`(`a59b260..eb12635`) → `npm run build` → `ops/stack.sh restart prod`. PID api **93501→88640** / web **93522→88644**, `lstart` = `2026-08-08 15:48:51`. 판별 검사 3종 전부 통과 — `resultText` 빌드 산출물 **0→17개 파일**(#77), `authFailure|sessionExpired` **13개 파일**(#79), 새 기동 블록에 standalone 경고 **없음** + `.next/standalone` 미생성(#75). `BUILD_ID` mtime `Aug 4 22:23` → **`Aug 8 15:48`**(5일 만에 첫 갱신). 헬스 backend `{"status":"ok","db":true,"katago_alive":true}` 200 · web 200, 라이브 스모크 `/`·`/spectate`·`/spectate/pro`·`/spectate/pro/1`·`/glossary`·`/history` 전부 200. 재기동 직전 DB로 진행 중 대국 0건 재확인(`max(id)` 337, 마지막 수 약 45시간 전). **승인 큐 `대기 중`은 이제 비었다.**<br>(이전) **(8/8 12:00) 서비스 전 항목 정상. 이번 사이클의 실질 성과는 CI 그린 PR [#79](https://github.com/rarebirds-svg/quite-baduk/pull/79)의 승인 등재와 두 배포 항목의 절차 병합이다.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. `katago_alive:false`는 러닝북 명시대로 **정보용**(lazy spawn, 마지막 AI 호출 8/6 22:04). watchdog `신규 incident 0 건`. PID 93501/93522 **12사이클 연속** 동일, 웹 `ps etime` = `05-13:49:38` = **133시간 50분 무재기동**.<br>**버그 스캔 신규 0건 — 단 "에러가 없어서"가 아니라 "이미 다 잡혀 있어서" 0건이다.** ① **401 버스트는 기존 #78이고 이미 멎었다** — 최근 5,000줄 401 **1,070건(21.4%)** 이지만 최근 500줄 **8건** / 최근 200줄 **0건**, 마지막 401은 파일 끝에서 **265줄 전**이다. 고쳐진 게 아니라 운영자 탭이 닫힌 것이므로 #78은 열린 상태 유지(`[[bug-scan-verify-recency]]`). ② **트레이스백 0건** — 현 기동 마커(line **225406**) 이후 `.err`가 12줄뿐이고 `^Traceback` 0건. 최근 20,000줄 **5xx 0건**.<br>**🔎 보안 관측 — 404 93건은 신규 크리덴셜 스캐너 스윕(앱 버그 아님, 노출 없음).** 최근 300줄 404의 **82건이 단일 IP `185.177.72.56`**, 나머지도 전부 `185.177.72.0/24` 대역. 경로는 `/api/.env`·`*.tfvars`·`credentials.ini`·`v1/secrets`·`namespaces/default/secrets`(k8s)·`phpinfo.php`·`POST /api/v1/auto_login` 등 전형적 시크릿 탐침이다. **전량 차단 실측 — 이 대역 요청 136건이 전부 404, 200이 단 1건도 없다.** 종전의 산발 `.env` 탐침(1건/사이클)이 대역 단위 스윕으로 규모만 커진 것이라 incident 아님.<br>**🔴 AP-20260805-01 미처리 72시간 — 판별 검사 3종 일곱 번째 동일.** ① `resultText` 빌드 산출물 **0개 파일** ② `BUILD_ID` mtime `Aug 4 22:23` **불변** ③ 웹 `ps lstart` `Aug 2 22:11:42` **불변**. `git rev-list --count HEAD..origin/main` **0**이라 이 지표만으로는 갭이 안 보인다는 판정도 일곱 번째 재확인(`[[merge-is-not-deploy]]`). **🆕 이 항목은 이제 단독 실행 불가** — #79 머지 시 origin이 앞서므로 재빌드 전 `git pull --ff-only`가 선행돼야 하고, 그러면 한 번의 재기동으로 **#75·#77·#79 세 건이 함께 발효**한다. 재기동 위험은 계속 낮다(DB `max(id)` 337 불변, 마지막 수 **약 45시간 전**). 중복 제안은 만들지 않았다.<br>(이전) **(8/7 18:00) 서비스 전 항목 정상. 이번 사이클의 실질 성과는 며칠째 0건이던 버그 유입을 깬 신규 이슈 [#78](https://github.com/rarebirds-svg/quite-baduk/issues/78) 1건이다.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. `katago_alive:false`는 러닝북 명시대로 **정보용**(lazy spawn, 마지막 AI 호출 8/6 22:04). watchdog `신규 incident 0 건`. PID 93501/93522 **11사이클 연속** 동일, 웹 `ps etime` = `04-19:50:19` = **115시간 50분 무재기동**.<br>**🆕 버그 스캔 신규 1건 — 어드민 401 무한 폴링(#78).** 6시간 델타 1,167건 중 **401이 1,034건(88.6%)** 으로 튀어 파고들었다. 1,032건이 어드민 4개 엔드포인트에 **정확히 258회씩 균등** 분포 = 사람 조작 아닌 폴링 루프이고, 같은 엔드포인트 **200 응답 9건이 선행**하므로 세션 만료 후 폴링만 남은 형태다. 원인을 코드에서 확정 — `web/app/admin/page.tsx:189-195`의 catch가 **403만 처리**하고 401을 조용히 삼켜 `setInterval`이 영원히 돈다. **리포 안에 정답 선례 존재**(`admin/pro-games/page.tsx:43`은 `401 || 403` 처리) — 규칙 부재가 아니라 5개 화면의 선례 이탈이다. 258회/6h ≈ 84초 간격은 백그라운드 탭 스로틀링이며 **포그라운드면 시간당 2,880건**이 된다. 영향은 낭비보다 **오독** 쪽 — 만료를 알리지 않고 옛 데이터를 계속 보여준다. 사용자 대면·보안 노출 없음(서버는 401로 정상 거절).<br>**백엔드 트레이스백 0건 — `database is locked` 16세트는 전부 12일 전 것.** 마지막 락의 SQL이 `UPDATE games SET loss_streak WHERE id=332`인데 **DB 실측 게임 332의 마지막 수가 `2026-07-26 10:47:47 UTC`** 다. 이후 대국 333~337에서 재현 0건. `.err` mtime `Aug 7 06:09`은 재기동 INFO 라인 탓이라 최근성 대리지표로 쓸 수 없다(`[[bug-scan-verify-recency]]`).<br>**트래픽 — 6시간 델타 1,167건, 5xx 0건.** 12:00 절단선(231,484)에서 직접 절단(`.log` → **232,651**). 200 **130**(그중 **80건이 health 폴링** — 실질 공개 트래픽은 얇다) / 401 **1,034**(위 #78) / 404 **1** / 204 **1**. `visit_hits` 99 → **100행(+1)**. 🔎 보안 관측 — `GET /api/.env` 404 1건(IP `195.178.110.31`), 12:00에 이어 두 번째지만 **출처 IP가 달라** 동일 공격자 반복이 아닌 무차별 스캐너 잡음. 거절됨·노출 없음, incident 아님.<br>**🔴 AP-20260805-01 미처리 54시간 — 판별 검사 3종이 여섯 번째로 동일.** ① `resultText` 빌드 산출물 **0개 파일** ② `BUILD_ID` mtime `Aug 4 22:23` **불변** ③ 웹 `ps lstart` `Aug 2 22:11:42` **불변**. `git rev-list --count HEAD..origin/main` **0**이라 이 지표만으로는 갭이 안 보인다는 판정도 여섯 번째 재확인(`[[merge-is-not-deploy]]`). **재기동 위험은 12:00보다 더 낮아졌다** — 마지막 수가 **20시간 전**(12:00 시점 14시간)이고 6시간 델타에 `POST /api/games` 0건. 중복 제안은 만들지 않았다.<br>(이전) **(8/7 12:00) 서비스 전 항목 정상. 이번 사이클에 5일 만의 실사용 대국·방문이 관측됐고, 🔴 배포 갭은 48시간째다.** backend `{"status":"ok","db":true,"katago_alive":false}` 200, web 200, 디스크 6%, drift 없음(exit 0·무출력), staging 중단 = 정책상 정상. `katago_alive:false`는 러닝북 명시대로 **정보용** — lazy spawn이라 마지막 AI 호출(8/6 22:04) 이후 어댑터가 내려간 정상 상태이며 단독으로 incident·재기동 사유가 되지 않는다. watchdog `신규 incident 0 건`·`[api] OK`·`[web] OK`. PID 93501/93522 **10사이클 연속** 동일, `ps lstart` = `Sun Aug 2 22:11:37/22:11:42` = **97시간 48분 무재기동**.<br>**🔴 AP-20260805-01 미처리 48시간 — 판별 검사 3종이 다섯 번째로 동일하다.** ① `grep -rl 'resultText' web/.next/{static,server}` = **여전히 0개 파일** ② `web/.next/BUILD_ID` mtime = `Aug 4 22:23` **불변** ③ 웹 프로세스 `ps lstart` = `Aug 2 22:11:42` **불변**. `git rev-list --count HEAD..origin/main`은 **여전히 0**(HEAD = `origin/main` = `a59b260`)이라 이 지표만으로는 갭이 안 보인다는 판정이 다섯 번째로 재확인됐다(`[[merge-is-not-deploy]]`). **중복 제안은 만들지 않았다.** ⚠️ **다만 긴급도 근거가 이번에 바뀌었다** — 종전 각주의 "8/1 이후 신규 대국 0건"이 더 이상 사실이 아니라(아래 트래픽), #77의 사용자 대면 결과 표기 미배포 비용이 올라갔다. 재기동 위험은 그대로 낮다(마지막 수 14시간 전).<br>**버그 스캔 신규 0건 — 현 기동 이후 트레이스백 0건 재실측.** 현 기동 마커(`Started server process [93501]`) 이후만 잘라 `Traceback`·`ERROR` **0건**. `tail -3000`에 `database is locked` 16세트가 보이나 마지막 트레이스백이 tail 2804행, 기동 마커가 **2988행**이라 전부 재기동 *이전* 구간이다 — 누적 카운트 아닌 발생 시점으로 판정(`[[bug-scan-verify-recency]]`). 열린 `bug` 이슈 **0건**. `.err`의 WS 403 1건은 세션 만료 계열 **1회성**이라 후보 조건(2회 이상) 미충족으로 이슈화하지 않았다.<br>**🆕 트래픽 재개 — 6시간 델타 4,991건, 5xx 0건.** 18:00 절단선(226,533)에서 직접 절단(`.log` 226,533 → **231,484**). 200 **4,932** / 401 **37** / 204 **15** / 404 **3** / 201 **3**. 401은 `GET /api/session` 14 + admin 4종 20 + `games/337` 2로 전부 인증 경로 정상 거절. 201 3건 중 1건이 **`POST /api/games`** = 대국 #337 생성. `visit_hits` 85 → **99행(+14)**, `analytics/hit` **14건**(직전 0건)으로 **사람 방문 재개**. 🔎 보안 관측 — `GET /api/.env` 404 1건(스캐너 탐침, 거절됨·노출 없음, 1회성이라 incident 아님).<br>**⚠️ 판정 방법 결함 교정 — `.log`의 `ws/games` 카운트는 구조적으로 항상 0이다.** 종전 사이클들이 "진행 중 대국 0건"의 근거로 쓴 지표인데, `.log` 231,484줄에 `ws/games` **1건** / `.err`에 **643건**이다. WS 라인은 stderr로만 나가므로 0은 "대국 없음"이 아니라 "그 파일에 안 찍힘"이다. **대국 유무는 DB `max(id)`·`moves.played_at`으로, 로그로 볼 때는 `.err`로 판정한다.** | 2026-08-07 12:00 |
| staging | 중단 (정책상 정상) | 2026-08-10 21:00 |

## 백업 상태

| 항목 | 값 |
|---|---|
| 최신 백업 | **2026-08-10T04:00** (baduk-20260810T040005.db.gz, **3,405,938B ≈ 3.25MB**, integrity OK, **9 tables** — 8/10 **21:00** 복원 드릴 통과(**10회 연속**, 백업본 `max(games.id)` 337로 라이브 DB와 일치). 8/10 **09:00**에도 동일 통과(9회 연속). `find -mtime +1` 무출력 = 신선. 다음 백업 8/11 04:00). 전일 3,404,538B 대비 **+1,400B** = 종전 일일 증가폭(+1.3~1.6KB) 범위로, 8/9의 +117,938B 급증이 주간 ingest 1회성이었음이 확인된다(`[[backup-size-spike-weekly-ingest]]`).<br>(이전) **2026-08-09T04:00** (baduk-20260809T040005.db.gz, **3,404,538B ≈ 3.25MB**, integrity OK, **9 tables** — 8/9 **18:00** 복원 드릴 통과(8회 연속). 8/9 12:00에도 동일 통과. `find -mtime +1` 무출력 = 신선. 다음 백업 8/10 04:00). **전일 3,286,600B 대비 +117,938B로 종전 일일 증가폭(+1.3~1.6KB)의 90배지만 이상이 아니다** — 같은 날 03:00 CWI ingest `new=200`의 결과이며 DB `pro_games` 3111 → **3311(+200)**, sitemap `<loc>` **3379**로 교차 확인했다.<br>(이전) 2026-08-08T04:00 (3,286,600B, 전일 대비 +1,643B — `visit_hits` +8행과 정합). |
| daily / weekly / monthly | 14 / 8 / 3 (8/10 **21:00** 실측 — 09:00과 동일. 다음 백업 8/11 04:00이므로 이 사이클에 변화가 없는 것이 정상. 복원 드릴 `integrity_check=ok` **10회 연속**, 9 tables.)<br>(이전) 14 / 8 / 3 (8/10 **09:00** 실측 — 8/10 daily 백업 생성과 `daily/baduk-20260727T040004.db.gz` 보존 정리가 상쇄돼 개수 유지. daily 0 아님, weekly·monthly는 0이어도 정상. 복원 드릴 `integrity_check=ok` **9회 연속**, 9 tables. 다음 백업 8/11 04:00.)<br>(이전) 14 / 8 / 3 (8/9 **12:00** 실측 — 8/9 백업이 weekly 티어에도 복사되고 daily·weekly 각 1건이 보존 정리돼 개수는 유지됐다. daily 0 아님, weekly·monthly는 0이어도 정상. 복원 드릴 `integrity_check=ok` **7회 연속**, 9 tables. 다음 백업 8/10 04:00.) |

## 콘텐츠 인덱스

| 항목 | 값 |
|---|---|
| 프로 기보 수 | **3311** (8/9 12:00 DB 실측 `SELECT count(*) FROM pro_games`). 직전 3111(8/2)에서 **+200** = 8/9 03:12 주간 ingest의 `new=200`과 정확히 정합하며, 같은 증가가 백업 크기 +117,938B·sitemap +200으로도 확인됐다.<br>(이전) **3111** (8/2 18:00 DB 실측 `SELECT count(*) FROM pro_games`). 직전 2911(7/26)에서 **+200** = 8/2 03:12 주간 ingest의 `new=200`과 정확히 정합. (이전) 2911 — 7/26 12:00 실측. 그 이전 표기 `1111(825+286)`은 1800건 벌어진 stale 값이었다. **이 행은 매주 일요일 ingest 이후 갱신할 것.** |
| 테마 수 | 6 |
| 월간 픽 URL | 14 (최근 12개월 + 현재 + 다음 달) |
| sitemap URL 수 | **3379** (8/9 12:00 prod `/sitemap.xml` `<loc>` 실측). 7/26의 2979에서 **+400** = 8/2·8/9 두 번의 ingest(각 +200)와 정합. **재빌드 없이 반영됐다** — 목록이 `force-dynamic`이라 ingest만으로 라이브 sitemap이 늘어난다. |
| 최근 CWI ingest | **2026-08-09 03:12** (fetched=2460 new=200 duplicate=2260 error=0, cap=200 도달). 8/2 12:00에 예고한 "다음 실행 8/9"가 예정대로 수행됐다. **주 1회(일요일 03:00) 잡** — 실행 이력 6/14·6/21·6/28·7/5·7/12·7/19·7/26·8/2·8/9가 전부 일요일. 평일에 "최근 실행이 며칠 전"인 것은 stale이 아니며 다음 실행은 8/16. |
| 이 달의 명국 | 353 |
| 글로서리 | 18/18 — 시드 소진 (7/8 `susangjeon` 게시로 완료) |
| FAQ | 9/9 — 시드 소진 |
| sitemap 글로서리·FAQ URL | 27 (글로서리 18 + FAQ 9, prod sitemap 실측 7/8) |

## 분석

| 항목 | 값 |
|---|---|
| 최근 주간 리포트 | **2026-W32.md** (8/9 09:00 생성 — 대국 **1국**(전주 5국 대비 -80.0%)/누적 **337국**, `visit_hits` 이번 주 56건(전주 27), `session_history` 신규 5건). **사용량 3주 연속 감소(18 → 5 → 1국)** 이며 8/6 이후 신규 대국이 0건이다. 인프라는 전 항목 정상이므로 장애가 아니라 유입 문제다.<br>(이전) **2026-W31.md** (8/2 09:00 생성 — 대국 **5국**(전주 18국 대비 -72.2%)/누적 **336국**). W32는 미생성이나 **stale 아님** — `com.inkbaduk.analytics-weekly`는 `Weekday 0` = 주 1회 일요일 잡이라 다음 실행은 **8/9(일)** 이고 그때 W32가 나온다. 참고 — 리포트 이후 8/6 21:55 KST에 대국 **#337**이 추가돼 DB 실측 누적은 **337국**, `visit_hits`는 **108행**(8/7 18:00 100행)이다. |
| 누적 리포트 수 | 12 |

## 개발 현황

| 항목 | 값 |
|---|---|
| 열린 이슈 | **(8/9 18:00) 0건 — 미분류 0건, 사람 분류 필요 0건. 세 사이클 연속 공백이다.** 버그 스캔이 이번에도 0건인 것은 `.err` 트레이스백 0건 + 최근 3,000줄 5xx 0건 + #78 무재발이라 후보 자체가 없기 때문이다. 어드민 401 1,049건은 밴드가 8/6~8/7로 확정돼 후보에서 제외했고, web `.err`의 `Failed to find Server Action` 67건은 배포 순간의 구 번들 탭 오류(이후 26시간 신규 0건)라 "일시적 아님" 조건 미충족으로 제외했다.<br>(이전) **(8/9 12:00) 0건 — 미분류 0건, 사람 분류 필요 0건. 두 사이클 연속 공백이다.** `gh issue list --state open --search "no:label"` 빈 배열. **유입원인 버그 스캔이 이번에도 0건인 것은 근거가 있다** — `.err` 트레이스백 0건 + 최근 5,000줄 5xx 0건 + #78 무재발이라 올릴 후보 자체가 없다. 어드민 401 1,049건은 전량 배포 이전 구간이라 후보에서 제외했다.<br>(이전) **(8/8 18:00) 0건 — 미분류 0건, 사람 분류 필요 0건.** [#78](https://github.com/rarebirds-svg/quite-baduk/issues/78)이 PR #79 스쿼시 머지로 **자동 종료**됐다(`closedAt 2026-08-08T06:47:39Z`, `stateReason=COMPLETED` — 사람이 닫은 게 아니라 PR 본문의 이슈 참조로 닫혔다). 12:00에 "닫지 않는 것이 맞다"고 유지했던 판단은 그 시점 기준으로 옳았고, 배포로 라이브에서 실제 해소됨을 이번에 로그로 확인했다(스택 상태 행 근거 3종).<br>(이전) **(8/8 12:00) 1건 — [#78](https://github.com/rarebirds-svg/quite-baduk/issues/78) 유지. 미분류 0건, 사람 분류 필요 0건.** 8/8 02:00 dev-cycle이 이미 처리해 PR #79로 올렸으므로 이슈는 "수정 대기"가 아니라 "머지 대기" 상태다. **닫지 않는 것이 맞다** — 최근 5,000줄 401 1,070건으로 버그는 여전히 라이브다(버스트가 멎은 것은 수정이 아니라 운영자 탭이 닫힌 결과).<br>(이전) **(8/7 18:00) 1건 — 5사이클 연속 공백이 이번에 깨졌다.** 신규 [#78](https://github.com/rarebirds-svg/quite-baduk/issues/78) `어드민 화면이 401(세션 만료)을 처리하지 않아 무한 폴링` (`bug`·`prio:low`). 라벨은 생성 시 `bug` + 트리아지에서 `prio:low`(어드민 전용·사용자 대면 영향 없음). `small`은 주지 않았다 — 5개 화면에 걸친 변경이라 "한 파일 안팎" 기준 미충족. 미분류 0건, 사람 분류 필요 0건. **유입원이 로그 스캔이라는 점은 그대로다** — 8/7 02:00 dev-cycle이 지적한 "병목은 이슈 유입 0"이 이번 사이클에 액세스 로그 상태코드 분포를 파고들면서 해소됐다(트레이스백만 보면 놓쳤을 건이다).<br>(이전) **(8/7 12:00) 0건 — 5사이클 연속 백로그 공백. 신규 0건, 미분류 0건.** `gh issue list --state open` 빈 결과, `--search "no:label"`도 빈 결과라 트리아지 대상 없음. 8/7 02:00 dev-cycle이 `gh auth status` + `--state closed` 교차 조회로 이 `[]`가 인증 만료의 위장이 아님을 검증해 뒀다. 유입원인 버그 스캔이 며칠째 0건인 것은 백엔드 `.err` 트레이스백 0건과 정합하다. |
| 열린 PR | **(8/9 18:00) 0건 — 주의 PR 0건.** `gh pr list --state open` 빈 배열, CI 실패·충돌·정체 어디에도 해당 없음. 배포 갭도 0(`git rev-list --count HEAD..origin/main` = 0)이며 프로세스 `lstart` 8/8 15:48이 배포 이후 값이라 `[[merge-is-not-deploy]]`의 위장 0이 아니다.<br>(이전) **(8/9 12:00) 0건 — 주의 PR 0건.** `gh pr list --state open` 빈 배열, CI 실패·충돌·정체 어디에도 해당 없음. 배포 갭도 0(`git rev-list --count HEAD..origin/main` = 0)이며, **이번엔 배포 판별 검사가 전부 배포 *이후* 값이라 `[[merge-is-not-deploy]]`의 위장 0이 아니다**(프로세스 `lstart` 8/8 15:48 · 어드민 401 전환점 실측). 로컬 미push 커밋 1건(`f56983e`)은 09:00 분석 잡의 ops 상태 커밋이라 배포 갭이 아니다.<br>(이전) **(8/8 18:00) 0건 — 주의 PR 0건.** `gh pr list --state open` 빈 결과. #79가 15:47 스쿼시 머지(`eb12635`)되며 4사이클 만에 열렸던 PR 큐가 다시 비었다. **머지 대기 PR도 미배포 커밋도 없어 이번 사이클에 새로 등재할 🟡 항목이 없다** — `git rev-list --count HEAD..origin/main` **0**이고, 이번엔 배포 판별 검사(빌드 산출물·`BUILD_ID` mtime·프로세스 uptime)가 전부 배포 *이후* 값이라 `[[merge-is-not-deploy]]`의 위장 0이 아니다.<br>(이전) **(8/8 12:00) 1건 — 주의 PR 0건. 4사이클 이어지던 공백이 깨졌다.** [#79](https://github.com/rarebirds-svg/quite-baduk/pull/79) `fix(admin): 401(세션 만료)에 폴링을 멈추고 만료를 알림 (#78)` — `mergeable=MERGEABLE`·`mergeStateStatus=CLEAN`·CI **4잡 전부 SUCCESS**(frontend·backend·app-shell-build·e2e). 생성 8/7 17:06 = 19시간 경과라 정체(7일 기준) 아님. **CI 실패·충돌·정체 어디에도 해당 없음 = 러닝북 기준 "정상"**이지만, `main` 코드 머지는 🟡라 **AP-20260808-01로 승인 큐에 등재**했다(선례 #66→AP-20260725-01, #72→AP-20260730-01과 동일 처리). 변경은 웹 어드민 10파일(+195/−45), **백엔드·DB·마이그레이션 0건**.<br>(이전) **(8/7 18:00) 0건 — 주의 PR 0건. 4사이클 연속.** `gh pr list --state open` 빈 결과. |
| CI e2e 상태 | **(7/25 12:00) PR #66 런에서 e2e 잡 SUCCESS 확인** — #59 머지 이후 회복 상태 유지. `[[ci-e2e-disabled]]`의 "main 만성 RED" 서술은 현재 사실과 다르므로 갱신 대상. e2e 잡은 활성(`if:` 조건 없음). |
| SQLite locked 판정 | **(8/9 12:00) 변화 없음 — 마지막 발생 line 225346이 기동 마커(225423) 이전이라 8/8 판정 그대로다.** 알리바이가 강해진 게 아니라 **정지 상태**다 — 8/6 이후 신규 대국 0건(48h 착수 0)이라 대국 경로에 부하가 걸린 적이 없다. `busy_timeout` 30s가 에러를 지연으로 바꾸는 성격도 그대로이므로 "무재발 = 해소"로 읽지 않는다. 진짜 판정 시점은 여전히 다음 실사용 대국이다.<br>(이전) **(8/8 18:00) 마지막 락은 기동 5회 전 = 8/2 이전으로 확정.** 마지막 발생 line **225347** 이후 기동 마커가 5개(78066→94302→512→93501→88640) 지나갔고, 93501은 8/2 기동해 12사이클을 버틴 프로세스다. SQL은 `UPDATE games SET loss_streak WHERE id=332`(#69 계열)이며 이후 대국 333~337에서 재현 0건. 누적 2,556건은 판정에 쓰지 않는다(`[[bug-scan-verify-recency]]`). 단 8/6 이후 신규 대국이 0건이라 **#69 수정본의 진짜 부하 검증은 여전히 다음 실사용 대국이 시점**이고, `busy_timeout` 30s가 에러를 지연으로 바꾸는 성격도 그대로다 — "무재발 = 해소"로 읽지 않는다.<br>(이전) **(8/2 18:00) 114시간 트레이스백 0건 — 프로 상세 밀도 24건/h(관측 이래 최고, 직전 최고의 5.6배)에서도 0건이다. #65 쪽 증거는 이번에 결정적으로 강해졌다.**<br>`.err` **225,401줄 / mtime `Aug 1 19:43`** — 현 기동 마커(line 225395) 이후 트레이스백 **0건**. 마지막 `database is locked`(line 225347)는 기동 *이전* = 구 프로세스 구간이라 판정에서 제외(`[[bug-scan-verify-recency]]`). 같은 구간 `.log`는 214,219줄로 진행 중이라 진짜 무에러다.<br>**부하 실측 — 12:00→18:00 델타를 직접 절단했다.** 6시간 232줄, 그중 **프로 상세 144건 = 24건/h**, 상태코드 **232건 전부 200**(4xx·5xx 0건). 밀도 추이 0.67 → 0.9 → 2.4 → 4.3 → **24건/h**로 5사이클 연속 상승이며 이번 상승폭이 압도적이다. **어드민 폴링이 0건이라 이번 델타는 희석 없이 순수 공개 트래픽**이다.<br>**① #65(조회수 락) — 사실상 해소 근거 확보.** #66(원자적 UPDATE)은 **여전히 미배포**인데도 24건/h에서 락 0건이다. 즉 라이브 완화는 `busy_timeout` 30s 하나뿐인 상태로 이 밀도를 견뎠다. 7/23~24 재현 버스트의 *요청* 밀도는 여전히 실측 불가지만(당시 로그에 남은 건 락 에러 16건뿐), 24건/h는 지금까지 관측한 어떤 구간보다 높다. 다만 `busy_timeout`은 에러를 *지연*으로 바꾸는 성격이고 지연은 로그에 흔적을 남기지 않으므로 "0건 = 무지연"은 아니다 — #66 배포(AP-20260802-01) 후 최종 판정.<br>**② #69(`loss_streak` 락, `38fd9e3`) — 8/1 대국 #336(99수·38분)에서 무재현으로 12:00에 판정 완료.** 이후 신규 대국 0건이라 추가 증거는 없다.<br>(이전) **(8/1 18:00) 90시간 트레이스백 0건 — 프로 상세 밀도가 관측 이래 최고(4.3건/h)인데도 0건이라, 지금까지 중 가장 강한 무재발 증거다. 그래도 보류 유지.**<br>`.err` **225,398줄 / mtime `Jul 29 00:01`** 로 9사이클 연속 바이트 단위 동일 = 현 프로세스(PID 512) 기동 이후 트레이스백 **0건**(uptime `3일 18:00:26`과 정확히 일치). 같은 구간 `.log`는 212,602줄로 진행 중이라 진짜 무에러다.<br>**부하 실측 — 12:00→18:00 델타를 직접 절단했다.** 6시간에 1,565줄이지만 **92%가 운영자 어드민 폴링**(1,443건)이고, 프로 상세는 **26건 = 4.3건/h**, 5xx·404 **0건**. 밀도 추이 0.67 → 0.9 → 2.4 → **4.3건/h**로 4사이클 연속 상승.<br>**보류 유지 사유.** ① #65(조회수 락) — 밀도가 오르는데도 0건이라 증거는 **강해졌다**. 다만 7/23~24 재현 버스트의 *요청* 밀도는 실측 수단이 없어(당시 로그에 남은 건 락 에러 16건뿐) 임계 도달 여부는 판정 불가이고, `busy_timeout` 30s가 에러를 *지연*으로 바꾸는 성격도 그대로다. PR #66 미머지 = 원자적 UPDATE는 여전히 prod에 없다. ② #69(`loss_streak` 락, `38fd9e3`) — **10사이클 연속 무부하 알리바이**(DB `max(games.id)`=**335**, `started_at >= 2026-07-27` 대국 **0건** 직접 확인, 델타에 대국 경로 0건). 다음 실사용 대국이 진짜 판정 시점.<br>(이전) **(8/1 12:00) 84시간 트레이스백 0건 — 부하는 소폭 올랐으나(2.4건/h) 여전히 저부하 알리바이. 보류 유지.**<br>`.err` **225,398줄 / mtime `Jul 29 00:01`** 로 8사이클 연속 바이트 단위 동일 = 현 프로세스(PID 512) 기동 이후 트레이스백 **0건**(uptime `3일 12:00:16`과 정확히 일치). 같은 구간 `.log`는 211,037줄로 진행 중이라 진짜 무에러다.<br>**부하 실측 — 02:00→12:00 델타를 직접 절단했다.** 10시간에 2,560줄이지만 **94%가 운영자 어드민 폴링**(2,404건)이고, 프로 상세는 **24건 = 2.4건/h**, 5xx **0건**. 18:00(0.67) → 02:00(0.9) → 12:00(**2.4**)로 회복 방향이나 #65를 재현시킨 크롤러 버스트(17.7h/16건)와는 여전히 성격이 다르다.<br>**판정 그대로 보류.** ① #65(조회수 락) — 무재발이나 `busy_timeout` 30s가 에러를 *지연*으로 바꾸므로 무재발이 곧 해소가 아니다. PR #66 미머지 = 원자적 UPDATE는 여전히 prod에 없다. ② #69(`loss_streak` 락, `38fd9e3`) — **9사이클 연속 무부하 알리바이**(DB `max(games.id)`=**335**, `started_at 2026-07-26 11:11 UTC`, 델타에 대국 경로 0건). 다음 실사용 대국이 진짜 판정 시점.<br>(이전) **(7/31 18:00) 66시간 트레이스백 0건 — 부하는 또 줄었다(0.67건/h). 판정 시점은 사이클마다 멀어지는 중. 보류 유지.**<br>`.err` **225,398줄 / mtime `Jul 29 00:01`** 로 6사이클 연속 바이트 단위 동일 = 현 프로세스(PID 512) 기동 이후 트레이스백 **0건**(uptime `2일 18:00:24`와 정확히 일치). 같은 구간 `.log`는 207,436줄로 진행 중이라 진짜 무에러다.<br>**부하 실측 — 창 크기 함정을 회피하고 12:00→18:00 델타를 직접 절단했다.** 6시간에 89줄, 그중 프로 상세 **4건 = 0.67건/h**, 5xx **0건**. 02:00 약 6.0 → 12:00 약 2.0 → 18:00 **0.67건/h**로 단조 감소이며, **주간 구간이 심야보다 낮으므로 시간대 효과가 아니다**.<br>**판정 그대로 보류.** ① #65(조회수 락) — 무재발이나 `busy_timeout` 30s가 에러를 *지연*으로 바꾸므로 무재발이 곧 해소가 아니고, 부하가 더 낮아져 증거 가치는 12:00보다 **또 약해졌다**. PR #66 미머지 = 원자적 UPDATE는 여전히 prod에 없다. ② #69(`loss_streak` 락, `38fd9e3`) — **7사이클 연속 무부하 알리바이**(DB `max(games.id)`=**335**=7/26, 델타 89줄에 대국 경로 0건). 다음 실사용 대국이 진짜 판정 시점.<br>(이전) **(7/31 12:00) 60시간 트레이스백 0건 — 그러나 부하가 오히려 1/3로 줄어 판정 시점이 멀어졌다. 보류 유지.**<br>`.err` **225,398줄 / mtime `Jul 29 00:01`** 로 7/29 12:00 이래 5사이클 연속 바이트 단위 동일 = 현 프로세스(PID 512) 기동 이후 트레이스백 **0건**. 같은 구간 `.log`는 207,347줄로 진행 중이라 진짜 무에러다.<br>**부하 실측 — 감소했다.** 최근 200줄에 health **170** · 프로 상세 **26**(≈2.0건/h). 02:00 dev-cycle의 5.9~6.2건/h 대비 약 1/3이며, 최근 1,500줄 5xx **0건**. 다만 창이 KST 심야를 포함하므로 시간대 효과와 미분리.<br>**판정 그대로 보류.** ① #65(조회수 락) — 무재발이나 `busy_timeout` 30s가 에러를 *지연*으로 바꾸므로 무재발이 곧 해소는 아니고, 부하가 더 낮아져 증거 가치는 어제보다 **약해졌다**. PR #66 미머지 = 원자적 UPDATE는 여전히 prod에 없다. ② #69(`loss_streak` 락, `38fd9e3`) — **6사이클 연속 무부하 알리바이**(DB `max(games.id)`=**335**=7/26, 최근 200줄 대국 경로 0건). 다음 실사용 대국이 진짜 판정 시점.<br>(이전) **(7/30 18:00) 42시간 트레이스백 0건 — 대국 경로는 5사이클 연속 무부하다. 판정 보류 유지.**<br>`.err` **225,398줄 / mtime `Jul 29 00:01`로 7/29 12:00·18:00·7/30 12:00과 바이트 단위 완전 동일** = 현 프로세스(PID 512) 기동 이후 트레이스백 **0건**. 같은 구간 `.log`는 207,036줄(+128줄)로 진행 중이라 진짜 무에러다.<br>**부하 구성 실측** — 최근 400줄에 health **252** · `/api/spectate/pro/{id}` **123**, 최근 2,000줄 **5xx 0건**. 프로 상세 밀도는 12:00(135/500줄)과 사실상 동일.<br>**판정 그대로 보류.** ① #65(조회수 락) — 무재발이나 `busy_timeout` 30s가 에러를 지연으로 바꾸므로 무재발이 곧 해소는 아니고, PR #66 미머지 = 원자적 UPDATE는 아직 prod에 없다. ② #69(`loss_streak` 락, `38fd9e3`) — **현 기동 이후 실사용 대국 0판, 5사이클 연속 무부하 알리바이**(DB 최신 게임 id 335 = 7/26, 최근 1,200줄에 `/api/games`·`/api/ws/` 0건). 다음 실사용 대국이 진짜 판정 시점.<br>(이전) **(7/30 12:00) 36시간 트레이스백 0건 — 프로 상세 부하는 올랐으나 대국 경로는 4사이클 연속 무부하다.**<br>`.err` **225,398줄 / mtime `Jul 29 00:01`로 7/29 12:00·18:00과 바이트 단위 완전 동일** = 현 프로세스(PID 512) 기동 이후 트레이스백 **0건**. 같은 구간 `.log`는 206,908줄로 계속 진행 중이라 로그 정지가 아니라 진짜 무에러다.<br>**부하 구성 실측** — 최근 500줄에 health **299** · `/api/spectate/pro/{id}` **135** · `analytics/hit` 20 · session 20, 최근 3,000줄 **5xx 0건**. 프로 상세는 87(7/29 18:00) → **135**로 상승.<br>**함정 1건을 잡았다.** `tail -3000` 창에서 `POST /api/games` 201×4 · `/hint` 200×102가 잡혀 대국 트래픽으로 보였으나 **전부 7/26 게임 335 건이다** — 액세스 로그에 timestamp가 없어 3,000줄 창이 7/26까지 거슬러 올라갔고, DB 최신 게임도 여전히 id 335(7/26 11:11~11:58)다. `tail -1500`으로 좁히면 `/api/games`·`/api/ws/` **0건**. 창 크기를 최근성 대리지표로 쓰면 안 된다(`[[bug-scan-verify-recency]]`).<br>**따라서 판정은 그대로 보류.** ① #65(조회수 락) — 135건은 관측 이래 가장 높은 밀도지만 재현 버스트(17.7h/16건)와는 성격이 다르고, `busy_timeout` 30s가 에러를 지연으로 바꾸므로 무재발이 곧 해소는 아니다. ② #69(`loss_streak` 락, `38fd9e3`) — **현 기동 이후 실사용 대국 0판, 4사이클 연속 무부하 알리바이**. 다음 실사용 대국이 진짜 판정 시점.<br>(이전) **(7/29 18:00) 6시간 추가 관측 — 락 0건, 그러나 대국 경로는 이번에도 실행되지 않았다. 무부하 알리바이 3사이클 연속.**<br>`.err` **225,398줄 / mtime `Jul 29 00:01`로 12:00과 완전 동일** = 현 프로세스 기동 이후 트레이스백 **0건**. `.log`는 +약 900줄 진행했으므로 로그 정지가 아니라 진짜 무에러다.<br>**부하 구성 실측** — 최근 500줄에 health **349** · `/api/spectate/pro/{id}` **87** · 5xx **0건**. 같은 3,000줄 창에서 `WebSocket /api/ws/games` **0건**, `POST /api/games/{id}/moves` **0건**. `visit_hits`도 54행 그대로(+0).<br>**두 경로 모두 판정 불가 유지.** ① #65(조회수 락) — 프로 상세 87건은 12:00의 46건보다 밀도가 올랐으나 재현 버스트(17.7h/16건) 성격과는 다르다. ② #69(`loss_streak` 락) — **대국 트래픽이 0이라 `38fd9e3` 배포 효과는 여전히 미검증**이다.<br>누적 `database is locked` 2,556건은 전부 구 프로세스 구간이므로 판정에 쓰지 않는다(`[[bug-scan-verify-recency]]`).<br>(이전) **(7/29 12:00) 현 프로세스(PID 512, 기동 `Jul 29 00:01:09`) 이후 락 0건 — 단 대국 경로는 아예 실행되지 않았다.**<br>`.err` 225,398줄, 기동 마커 225395 이후 트레이스백 **0건**. 최근 300줄 트래픽 구성은 health 198 · `/api/spectate/pro/{id}` **46** · 어드민·세션 소수, **5xx 0건**이고 `POST /api/games` 계열은 **0건**이다.<br>**두 경로를 분리해 읽어야 한다.** ① `pro_games` 조회수 락(#65) — 46건은 02:00 사이클이 본 123건보다 약한 부하이나 여전히 무재발. ② 대국 경로 락(`loss_streak`, #69 `38fd9e3`로 수정) — **현 기동 이후 대국 트래픽 자체가 0이라 #69 배포 효과는 아직 무부하 알리바이 상태**다. 다음 실사용 대국 구간이 진짜 판정 시점.<br>`busy_timeout` 30s는 에러를 지연으로 바꾸는 완화라 지연은 로그에 흔적을 남기지 않는다는 단서도 유지(`[[bug-scan-verify-recency]]`).<br>(이전) **(7/27 18:00) 21시간 36분 트레이스백 0건 — 그러나 관측 이래 최저 부하 구간이다. 5연속 판정 보류.**<br>`.err`는 **225,390줄 / mtime `Jul 26 21:57`로 불변**. 현 프로세스 마커(line 225381, PID 94302) 이후 `database is locked` **0건**. 마지막 락 실패 line 225347은 마커 *이전* = 구 프로세스 구간.<br>12:00 이후 델타 실측 — `.log` 205,592 → **205,681줄(+89줄 / 약 6시간)**, 그 구간 `/api/spectate/pro/` **7건**, 5xx **0건**(200×86 · 404×1 · 401×1 · 204×1).<br>**부하 추세가 단조 감소다** — 7/26 12:00 +20,068줄(프로 상세 219건) → 18:00 +1,538(17) → 7/27 02:00 +864(20) → 12:00 +153(10) → **18:00 +89(7)**. 7건/6시간은 #65를 재현시킨 크롤러 버스트(17.7h/16건)와 밀도가 두 자릿수 배 차이라, 이번 0건은 지금까지 중 **가장 약한 증거**다.<br>PR #66 미머지 = 원자적 UPDATE는 아직 prod에 없고, 라이브 완화는 `busy_timeout` 30s 하나뿐. 판정은 #66 머지 이후 또는 다음 크롤러 버스트에서(`[[bug-scan-verify-recency]]`).<br>**(7/27 12:00) 현 프로세스 기동 후 15시간 36분 트레이스백 0건 — 그러나 또 저부하다. 판정 보류 유지.**<br>`.err`는 **225,390줄 / mtime `Jul 26 21:57`로 불변**. 현 프로세스 마커(line 225381, PID 94302) 이후 `database is locked` **0건**. 마지막 락 실패는 line 225347로 마커 *이전* = 구 프로세스 구간(`[[bug-scan-verify-recency]]` 절차대로 위치·누적 카운트가 아니라 기동 마커 스코프로 집계).<br>02:04 이후 델타 실측 — `.log` 205,439 → **205,592줄(+153줄 / 약 8시간)**, 그 구간 `/api/spectate/pro/` **10건**, 상태코드 **전부 200**(5xx 0건).<br>**두 가지가 모두 참이다** — ① `1bb0457`(`busy_timeout` 5s→30s)이 7/26 20:24 배포로 라이브라 #65의 실패 임계가 6배 올라갔다 ② 그럼에도 10건/8시간은 경합이 날 밀도가 아니라 이 0건은 여전히 **저부하의 결과**다. 어제 12:00(219건/10h)·18:00(17건/6h)·오늘 02:00(20건/8h)에 이어 4연속 판정 보류 구간.<br>#65 열린 상태 유지. PR #66 미머지 = 원자적 UPDATE는 아직 prod에 없다. 판정은 #66 머지 이후 또는 다음 크롤러 버스트에서.<br>**(7/26 18:00) 다시 저부하 구간으로 복귀 — 17요청·락 0건·5xx 0건. #65 미해결 유지.**<br>12:00 이후 6시간 델타 실측 — `.log` 203,037 → **204,575줄(+1,538)**, 그 구간 `/api/spectate/pro/` **17건**, 5xx 0건(200×1,536 · 401×1 · 204×1). `.err`는 라인 수·mtime 모두 여전히 불변(225,184줄 / 07-25 21:21) = **재기동 후 20시간 39분 트레이스백 0건**.<br>**12:00의 219건과 대비된다** — 오늘 오전의 부하는 지속 유입이 아니라 일시적이었고, 이번 6시간은 경합이 날 밀도가 아니다. 따라서 이번 0건은 12:00보다도 약한 증거다. PR #66 미머지 = 수정이 prod에 없다는 사실도 그대로. 판정은 #66 머지 이후 또는 다음 크롤러 버스트에서(`[[bug-scan-verify-recency]]`).<br>**(7/26 12:00) 재기동 이후 첫 "부하 있는" 구간 — 219요청·락 0건·5xx 0건. 그래도 #65는 미해결 유지.**<br>지금까지의 0건은 전부 "요청이 없어서 0건"이었으나 **이번엔 다르다**. `.log` 182,969줄(02:00) → **203,037줄(+20,068/10시간)**, 그 구간 `/api/spectate/pro/` **219건**에 **5xx 0건**. `.err`는 라인 수·mtime 모두 불변(225,184줄 / 07-25 21:21) = 트레이스백 0건.<br>**그럼에도 완화로 읽지 않는다** — ① **PR #66이 미머지라 수정이 prod에 없다**. 219건 무사고는 수정 효과일 수 없다. ② 문제 구간(7/23 18:03~7/24 11:44, 17.7h/16건)의 *요청 밀도*를 실측할 수단이 없어, 219건이 그 버스트 임계에 도달했는지 **판정 불가**다. 낙관도 반사적 기각도 하지 않는다(`[[bug-scan-verify-recency]]`).<br>결론 — #65 열린 상태 유지. 판정은 #66 머지 이후 또는 다음 크롤러 버스트 구간에서.<br>**(7/25 18:00) 배포 이후 구간 실측 — `500` 0건, 락 실패 0건. 그래도 #65는 미해결이다.**<br>**stdout 로그에 쓸 새 경계 마커를 찾았다** — `.log`에는 `Started server process` 기동 마커가 없지만, `POST /api/analytics/hit`은 #67에서 신설된 엔드포인트라 **첫 출현 라인(175,907)이 곧 08:57 배포 경계**다.<br>그 이후 168개 요청 실측: `200`×146 · `401`×10 · `204`×10 · `422`×2, **`500` 0건**. `.err`도 마커 이후 트레이스백 0건. 조회수 쓰기 경로 `GET /api/spectate/pro/{id}` **16회**에 락 실패 0건.<br>**0건을 완화 신호로 읽지 말 것** — ① 16회/9시간은 경합이 생길 밀도가 아니다(문제 구간 7/23~24는 크롤러 버스트였다) ② **PR #66 미머지 = 수정이 prod에 없다**. 0건은 수정 효과가 아니라 저부하의 결과다. 판정은 다음 크롤러 유입 구간에서.<br>**함정 기록** — `.log` 최근 2000줄 창에는 `500`×15가 있으나 **전부 경계 이전 = 구 프로세스 구간**이다. `tail` 창 크기를 최근성 대리지표로 쓰면 안 된다(`[[bug-scan-verify-recency]]`).<br>**(7/25 12:00, 일부 정정됨) 08:57 재기동으로 집계 구간이 리셋됐다 — 이전 19건과 직접 비교 불가.** 새 기동 마커는 `Started server process [5597]`(line 225173)이며 그 이후 **0건**(마지막 발생 line 225144 = 마커 이전, 구 프로세스 구간).<br>**이 0건도 완화 신호가 아니다** — 마커 이후 로그가 총 4줄(uvicorn 기동 라인뿐)이라 3시간 요청 0건, 경합할 일 자체가 없었다. **(7/25 18:00 정정 — 요청은 있었다. `.err`만 본 오독이고 액세스 로그는 `.log`에 있다. "미해결 유지" 결론 자체는 다른 근거로 유효.)** `[[bug-scan-verify-recency]]`의 반대 방향 주의(반사적 기각 금지)에 따라 **#65는 미해결로 간주**. 다음 트래픽 유입 구간에서 새 마커 기준 재집계 필요.<br>**(7/24 18:00) 6시간 신규 0건 — 단, 트래픽 부재로 교란된 수치다.** 동일 절차 재집계 결과 누적 **19건으로 12:00과 완전 동일**(분포도 동일: `pro_games.view_count` 16 · `DELETE FROM moves` 2 · `hint_count` 1). 그러나 같은 구간에 `baduk-api.err` 자체가 **12:49 이후 무기록**(액세스 로그 포함) = 요청이 아예 없었다. **"완화됐다"로 읽지 말 것** — 다음 트래픽 유입 구간에서 재측정해야 판정 가능하다. #65는 열린 상태 유지.<br>**(7/24 12:00) 진짜 급증 발생 — 이슈 #65 생성.** 7/23의 "완화책 정상·이슈화 불필요" 결론은 **그 시점 데이터로는 옳았고**, 그 이후 새 사실이 생겼다.<br>동일 절차(기동 마커 line 221718 이후만, `[SQL:]` 라인으로 에러당 1건) 재집계 결과 — 기동(7/12)~7/23 18:03 **11일 3건** → 7/23 18:03~7/24 11:44 **17.7시간 16건**. 0.27건/일 → 21.7건/일(**약 80배**). 두 수치가 같은 프로세스 스코프라 #64의 오류 모드(위치=최근성)에 해당하지 않는다.<br>신규 16건 = `pro_games.view_count` UPDATE 14 + 무르기 `DELETE FROM moves` 1 + `hint_count` 1. 원인 가설 `backend/app/api/spectate_pro.py:218`의 요청경로 내 read-modify-write. 방아쇠는 7/23 23:15 SEO 크롤 도달성 배포로 인한 크롤러 유입(**백엔드 코드는 7/11 이후 무변경**).<br>절차 자체는 유효하다 — 기동 마커 기준 집계를 계속 쓸 것. 누적 카운트·`tail`·파일 내 위치는 여전히 금지. |
| 미병합 브랜치 | **(7/24 18:00 정정) 로컬 미push 브랜치는 0건이다.** `git for-each-ref --format='%(upstream:short)'` 실측 — 로컬 13개 중 12개가 origin 추적 브랜치를 갖고, 나머지 1개는 워크트리용 `worktree-feat+glossary-media-phase1`(5/26)뿐이다. 7/24 02:00 dev-cycle 로그의 "미push 6개"(`feat/format-game-result` 등)와 아래 "로컬 전용 2건" 서술은 **모두 오탐** — 해당 브랜치들은 전부 origin에 존재한다. `[[dev-cycle-unpushed-branches]]` 대응 조치 불필요. 남은 것은 push 여부가 아니라 **정리(삭제) 여부**이며 이는 사람 재량.<br>**(이전 기록 7/14 18:30 · 7/17 18:30)** 원격 `feat/last-seen-at-debounce`·`fix/issue-39`는 **오탐** — PR #30·#45가 **squash merge**돼 원본 커밋이 main 조상이 아닐 뿐, 내용은 main에 반영됨(`git grep` 확인). 사람 판단 불필요. → 판정은 `--no-merged`가 아니라 PR 상태+`git grep <ref>`로. 원격 브랜치 잔여물 삭제는 사람 재량.<br>**superseded(폐기 가능) 4건** — `fix/sqlite-busy-timeout`·`fix/auto-resign-respect-score-margin`·`fix/scoring-gate-too-strict`·`fix/issue-12`: 수정 내용이 다른 커밋으로 main에 이미 반영됨.<br>**실제 사람 판단 대기 = 로컬 전용 2건**: `feat/format-game-result`(main에 `formatResult` 부재), `feat/i18n-ja-zh`(main 로케일 ko·en만) — PR 생성 또는 폐기. 둘 다 **최종 커밋 4/30 = 2.5개월 경과**, feature 경로라 dev-cycle 자율 범위 밖 → 오케스트레이터는 자율 push하지 않고 보고만 유지(정책 L16 보수 처리). |

## 보류 승인

`state/pending-approvals.md` 참조 — **대기 0건 (8/9 18:00 기준, 세 사이클 연속 큐 비어 있음).** '## 대기 중' = `(없음)`. 이번 사이클에 🟡 등급 액션을 만난 적이 없어(🟢 자율 범위인 헬스체크·백업 검증·버그 스캔·트리아지·PR 감시만 수행) 신규 등재 항목도 없다.<br>(이전) **대기 0건 (8/9 12:00 기준, 두 사이클 연속 큐 비어 있음).** '## 대기 중' = `(없음)`. 신규 등재할 🟡 항목도 없다 — 열린 PR 0건 · 배포 갭 0커밋이고, 이번 사이클에 🟡 등급 액션을 만난 적이 없다(🟢 자율 범위인 헬스체크·백업 검증·버그 스캔·트리아지·PR 감시만 수행).<br>(이전) **대기 0건 (8/8 18:00 기준, 큐 비어 있음).** AP-20260808-01 + AP-20260805-01이 8/8 15:47~15:49 사람 승인으로 함께 실행돼 큐가 비었고, 이번 사이클에 신규 등재할 🟡 항목도 없다(열린 PR 0건 · 배포 갭 0커밋).<br>(이전 8/8 **12:00** 기준 — **대기 2건**. **① AP-20260808-01 = PR #79 머지(신규 등재)** — CI 4잡 그린·CLEAN·웹 어드민 10파일·마이그레이션 없음. **② AP-20260805-01 = #75·#77 prod 배포, 제안 후 72시간 무응답** — 판별 검사 3종이 일곱 번째로 동일해 미반영 확정 유지, 웹 프로세스 133시간 50분 무재기동.<br>**🆕 두 항목은 이제 한 묶음이다.** #79를 머지하면 origin이 앞서므로 AP-20260805-01의 재빌드 앞에 **`git pull --ff-only`가 반드시 선행**돼야 한다 — 빠뜨리면 #79 없는 트리에서 빌드가 돈다. 이 단계를 AP-20260808-01 실행 절차에 명시하고 양쪽에 상호 참조를 남겼다. **한 번의 재기동으로 #75·#77·#79 세 건이 함께 발효**한다. 재기동 위험은 계속 낮다 — DB `max(id)` 337 불변, 마지막 수 약 45시간 전이라 중단될 대국 없음. 중복 제안은 만들지 않고 기존 항목에 재확인 각주만 누적했다.)<br>(이전 8/7 18:00 기준. **AP-20260805-01 = #75·#77 prod 배포(웹 재빌드 + 재기동), 제안 후 54시간 무응답.** 판별 검사 3종이 여섯 번째로 동일해 미반영이 확정 유지된다. **재기동 위험은 12:00보다 더 내려갔다** — 마지막 수가 20시간 전이고 6시간 델타에 `POST /api/games` 0건이라 중단될 대국이 없다. 긴급도 근거(8/6 실사용 재개로 #77 사용자 대면 미배포 비용 상승)는 유지되나, 이번 6시간은 트래픽이 얇았다(`visit_hits` +1행). 중복 제안은 만들지 않고 기존 항목에 재확인 각주만 누적한다.)<br>(이전 8/7 12:00 기준. **AP-20260805-01 = #75·#77 prod 배포(웹 재빌드 + 재기동), 제안 후 48시간 무응답.** 판별 검사 3종이 다섯 번째로 동일해 미반영이 확정 유지된다. ⚠️ 긴급도 근거는 이번에 상향됐다 — 5일 만에 실사용 대국 #337과 방문 14건이 재개돼 #77의 사용자 대면 결과 표기 미배포가 실제로 보이는 상태다. 재기동 위험은 그대로 낮다(마지막 수 14시간 전). 중복 제안은 만들지 않고 기존 항목에 재확인 각주만 누적한다.)

- **AP-20260802-01 (8/2 12:00 신규 등재, 18:00 기준 6시간 무응답)** — 머지된 4커밋의 prod 배포(🟡). `git pull --ff-only` + `web` 재빌드 + `ops/stack.sh restart prod`.<br>**미배포의 대가가 이번 사이클에도 실측됐다** — `orchestrator-runs.log` **18:00:05** 항목에 #71이 고칠 `Write(...)` deny 무실효 경고 2건이 또 출력됐다(머지 후 19시간). 즉 prod DB·KataGo 모델 가드레일은 아직 실효가 없다.<br>**단계별 발효 범위** — 1단계 `git pull`만으로 #71(가드레일)·#72(watchdog 성공마커, 크래시 루프 사각지대)가 **재기동 없이** 즉시 발효한다. #66(spectate 원자적 UPDATE)은 backend 재기동, #73(용어집 다이어그램)은 웹 재빌드가 필요하다.<br>충돌 없음 실측(유입 9파일 ∩ 로컬 미커밋 = 공집합), 4커밋 전부 머지 시점 CI 4잡 SUCCESS. 진행 중 대국 0건이라 재기동 리스크 낮음. 중복 제안은 만들지 않았다.

- (처리 완료) **AP-20260730-01 (7/30 12:00 신규 등재)** — PR [#72](https://github.com/rarebirds-svg/quite-baduk/pull/72) 머지(🟡). 7/30 02:00 dev-cycle이 #70을 고쳐 올린 PR인데 승인 큐에 등재돼 있지 않아 이번 실행에서 추가했다. watchdog가 "마지막 timestamp"가 아니라 "마지막 **성공** 종료 마커"로 신선도를 판정하게 되는 수정으로, 7/28 실사례에 대입하면 orchestrator 25.9h·dev-cycle 41.9h로 임계를 넘겨 그날 저녁 알림이 나갔을 건이다(구 로직 2.0h·18.0h로 무감지). CI 4잡 SUCCESS·`CLEAN`, 변경 파일 `ops/check-staleness.sh` + 신규 테스트 2개뿐 — **마이그레이션·prod 재기동 불필요**. **이 PR이 미머지인 동안 watchdog의 크래시 루프 사각지대는 그대로 열려 있다.**
- (처리 완료) **AP-20260725-01 — 8/1 23:00 사람 머지(`fd68fa3`). 제안 후 189시간(7.9일).** 단 backend 재기동 전까지 라이브 미반영이라 AP-20260802-01로 승계됐다. (등재 당시 기록) PR #66 머지(🟡). 이슈 #65 수정본이고 CI 4잡 전부 그린·`mergeable=MERGEABLE`·마이그레이션 없음인데 머지 대기 중. **긴급도는 내려갔다** — 7/26 20:24 배포로 `busy_timeout` 30s가 라이브가 돼 500 임계가 6배 올라갔다(02:00 dev-cycle 규명). 다만 그 완화는 *에러를 지연으로 바꾸는* 성격이라 #66의 락 보유시간 단축과 보완 관계이고, 머지 가치는 유지된다. 긴급 머지는 아니되 취소 대상도 아님.
- (처리 완료) **AP-20260723-01 — 8/1 23:00 사람 머지(`1ca3bcd`). 제안 후 227시간(9.5일).** 단 `git pull` 전까지 경고 2건이 계속 재출력되며 8/2 12:00·18:00 세션에서 연속 실측됐다 — AP-20260802-01로 승계. (등재 당시 기록) `.claude/settings.json` deny 규칙 2건이 `Write(...)`로 작성돼 **실효 없음**(하네스 경고: `Edit(path)` 규칙만 파일 편집 도구를 포괄). 결과적으로 라이브 prod DB(`backend/data/baduk.db`, 백업 원본)와 KataGo 모델이 에이전트 쓰기로부터 보호되지 않음. 설정 변경이라 🟡. 7/26·7/27·7/29 실행 모두 동일 경고 2건 재출력 = **6일째 미해소 확인**.<br>**(7/29 12:00) 상태 변경 — 이제 머지만 남았다.** 실행 절차 중 🟢 구간(브랜치 push + PR 생성)을 오케스트레이터가 선행해 **PR [#71](https://github.com/rarebirds-svg/quite-baduk/pull/71)** 을 올렸다. 6일간 "브랜치조차 없어 사람이 머지할 대상이 없는" 상태였던 것이 해소됐다.

AP-20260716-01(#55)·AP-20260717-01(#57)은 7/21 19:54 KST 사람 승인·머지로 처리 완료.

## 최근 장애

`state/incidents.md` 참조.

**신규 — OPS-20260728-01 (2026-07-28 02:00 ~ 07-29 02:00, 자동 복구됨)**: 인증 만료(`OAuth session expired and could not be refreshed`)로 dev-cycle 1회 + orchestrator 2회가 **시작 직후 비정상 종료**. 약 24시간 운영 감시 공백. **같은 구간 prod는 무사고**(사후 검증: 200 OK · 백업 2회 정상 · 신규 트레이스백 0건) — 유실된 것은 서비스가 아니라 감시다. **watchdog는 전 구간 `신규 incident 0 건`으로 오판**했고, 원인은 아래 Watchdog 섹션의 사각지대 항목이다. 후속 이슈 [#70](https://github.com/rarebirds-svg/quite-baduk/issues/70).

기존 3건은 복구·정리 완료. 2026-05-25 watchdog 활성화 직후 5건 (WD-20260525-2122*) 자동 기록 — 41시간 무음 정지 사건의 사후 감지. 5/27 03:35~17:35 동안 content-draft·content-ingest stale 30건 자동 누적은 watchdog 임계와 plist Weekday 잡 정합성 misalignment에 의한 false-positive였고, 이후 `72ec362`에서 임계를 9d로 정합화. 5/28 04:00 이후 watchdog 신규 incident 0건 — 누적 해소 확인.

## Watchdog

| 항목 | 값 |
|---|---|
| 오케스트레이터 주기 정합성 | **(8/1 12:00 재확인, 미해소)** 이번 트리거도 **12:00:04** = plist(12:00·18:00 2회)대로 발화. 프롬프트·러닝북의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지 — 유실이 아니라 문서·plist 미정합이다. **7/31 18:00에 예고한 실무 영향이 그대로 실현됐다** — 23:30 사이클이 실재하지 않아 PR #66의 7일 정체 전환(8/1 02:06)이 **10시간 뒤인 이번 12:00에야** 감지됐다. 사람 판단 대기 유지.<br>**(7/31 18:00 재확인, 미해소)** 이번 트리거도 **18:00:05** = plist(12:00·18:00 2회)대로 발화. 프롬프트·러닝북의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지 — 유실이 아니라 문서·plist 미정합이다. 실무적 영향 1건 확인 — **23:30 사이클이 실재하지 않으므로 PR #66의 7일 정체 전환(8/1 02:06)은 8/1 12:00에야 감지된다.** 사람 판단 대기 유지.<br>**(7/31 12:00 재확인, 미해소)** 이번 트리거도 **12:00:05** = plist(12:00·18:00 2회)대로 발화. `orchestrator-runs.log` 실측은 7/21 이후 하루 2회로 **누락 없이 일관**(7/28 인증 만료 건 제외)이므로 유실이 아니라 문서·plist 미정합이다. 사람 판단 대기 유지.<br>**(7/30 18:00 재확인, 미해소)** 이번 트리거도 **18:00** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지, 사람 판단 대기.<br>**(7/30 12:00 재확인, 미해소)** 이번 트리거도 **12:00:05** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회(06:30·12:30·18:30·23:30) 표기와 불일치 유지, 사람 판단 대기.<br>**(7/29 18:00 재확인, 미해소)** 이번 트리거도 **18:00:05** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회 표기와 불일치 유지, 사람 판단 대기.<br>**(7/29 12:00 재확인, 미해소)** 이번 트리거도 **12:00:05** = plist(12:00·18:00 2회)대로 발화. 문서·프롬프트의 4회 표기와 여전히 불일치, 사람 판단 대기 유지. **다만 7/28 12:00·18:00은 발화는 됐으나 인증 만료로 무작업 종료** — 주기 문제가 아니라 OPS-20260728-01 건이다(위 사각지대 행 참조).<br>**(7/27 18:00 재확인, 미해소)** 이번 트리거도 **18:00:01** = plist(12:00·18:00 2회)대로 발화(누락 아님). 사람 판단 대기 유지.<br>**(7/27 12:00 재확인, 미해소)** 이번 트리거도 **12:00:00 정각** = plist대로 발화(누락 아님). 문서·프롬프트의 06:30·12:30·18:30·23:30과 여전히 불일치. 사람 판단 대기 유지.<br>**(7/26 18:00 재확인, 미해소)** plist는 여전히 12:00·18:00 2회이며 이번 18:00 트리거도 plist대로 정상 발화 = 누락 아님. 사람 판단 대기 유지.<br>**(7/24 12:00 신규) 문서 4회 vs 실제 2회 불일치.** 러닝북 5종·오케스트레이터 프롬프트는 `매일 06:30·12:30·18:30·23:30`으로 적으나, `ops/launchd/com.inkbaduk.ops-orchestrator.plist`의 `StartCalendarInterval`은 **12:00·18:00 2회**뿐(헤더 주석도 "매일 12시·18시"). `orchestrator-runs.log` 실측 일치(7/23 18:00 → 7/24 12:00) = **누락이 아니라 미등록**. `sync-launchd.sh --check` drift 없음, `pmset` sleep 0 → 슬립 유실 아님. 문서를 2회로 맞출지 plist를 4회로 늘릴지는 사람 판단(후자는 prod 스케줄 변경이라 🟡). |
| 잡 등록 | `com.inkbaduk.ops-watchdog` 가동 중 (StartInterval 3600s) |
| 첫 실행 | 2026-05-25 21:22 (수동 kickstart, 신규 incident 5건) |
| 알림 채널 | Telegram (토큰 미설정 → fallback) → macOS notification |
| 임계 | orchestrator 18h · dev-cycle/backup 30h · content-draft/content-ingest 9d · analytics-weekly 8d |
| 쿨다운 | 잡당 1시간 1회 |
| **사각지대 (7/29 12:00 신규)** | **`ops/check-staleness.sh`는 "trigger 후 즉시 실패"를 감지하지 못한다.** `extract_last_ts()`가 로그의 마지막 `[YYYY-MM-DD HH:MM:SS]` 패턴만 뽑아 임계와 비교하는데, 실패 종료도 `[...] 비정상 종료` 라인으로 timestamp를 새로 찍으므로 잡이 "방금 실행됨"으로 보인다. launchd trigger **유실**은 잡지만 trigger는 됐는데 아무 일도 못 하는 모드는 **크래시 루프여도 영구히 무음**이다. OPS-20260728-01(3회 유실, watchdog 0건)이 실증 사례. 이슈 [#70](https://github.com/rarebirds-svg/quite-baduk/issues/70) — 성공 종료 마커 기준 판정으로 수정 제안. |
| 정합성 이슈 | (해소) `72ec362`에서 content-draft·content-ingest 임계를 30h → 9d로 plist 일정과 정합화. 신규 incident 0건 확인. |
