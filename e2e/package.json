{
  "name": "baduk-e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
