"""Placeholder — password/JWT auth was removed in favor of ephemeral
nickname sessions. Kept as an empty module so existing imports fail
loudly rather than silently pulling stale helpers; new code should
import from :mod:`app.deps` / :mod:`app.api.session`.
"""
