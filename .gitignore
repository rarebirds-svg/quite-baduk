# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/

# Node
node_modules/
.next/
dist/
coverage/
*.tsbuildinfo

# IDE / OS
.DS_Store
.idea/
.vscode/

# Env & data
.env
.env.local
ops/ops.env
backend/data/*.db
backend/data/*.db-wal
backend/data/*.db-shm
backups/
backend/katago/models/*.bin.gz
e2e/playwright-report/
e2e/test-results/

# Superpowers brainstorm mockups
.superpowers/

# Claude Code — personal overrides (shared config is committed as .claude/settings.json)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Local SQLite (root-level dev DB)
backend/baduk.db
backend/baduk.db-wal
backend/baduk.db-shm
.coverage

# Git worktrees (Superpowers convention)
.worktrees/

# 네이티브 실행 스크립트 런타임 (start.sh / stop.sh)
.run/

# KataGo runtime logs (gtp_example.cfg writes here when run from cwd)
gtp_logs/

# 작업별 임시 작업물 (rule 7 — Plan + Checklist + Context Notes)
checklist.md
context-notes.md

# 모바일 점검 스크린샷 산출물
e2e/screenshots-mobile/

# watchdog cooldown 마커 파일 (임시 상태, 커밋 불필요)
docs/ops/state/.watchdog-cooldown-*
